"""Chronos — Brain-local subjective-time subsystem (placeholder).

Phase 10b plan 10b-04 will populate this module. This stub exists so that
Wave 0's grep gate (brain/test/test_bios_no_walltime.py) can assert the
chronos source directory contains Python files with no wall-clock reads.

NO wall-clock reads, NO random, NO uuid — same determinism contract as bios.
"""
