---
wave: 62.5-05
title: Invariant gate + treasury-record retirement + Issue #9 E2E
status: complete
date: 2026-07-11
closes: ["Issue #9"]
---

# 62.5-05 — Invariant gate, treasury-record retirement, clean-DB E2E (FINAL wave)

Locks in the unified civic-DID-keyed ledger and proves the loop Issue #9 reported broken. This closes Phase 62.5 (all waves 01–05 done).

## What shipped

- **CI gate — `scripts/check-ledger-b-money.mjs`.** Fails the build on any `.transferWei(` call or JS `.balance_wei` property mutation. The leading-dot discriminator keeps the legitimate Ledger-A SQL rails (`balance_wei = balance_wei + VALUES(...)`, no dot) and the v74 migration clean. Wired into `pretest` + `.github/workflows/rig-invariants.yml`. Proven: passes on the current tree; catches a reintroduced `transferWei` + `.balance_wei` mutation.
- **Treasury record retired.** Genesis no longer seeds the inert `did:noesis:system:treasury` NousRecord (`launcher.ts`); migration **v75** `retire_system_treasury_record` deletes any leftover row from existing snapshots. The `TREASURY_DID` string still labels reclaimed-parcel ownership (no registry lookup — reclaim verified green without the record).
- **Census exclusion KEPT (documented deviation).** The literal wave spec said "delete PR #11", but the `did:noesis:system:*` census filter (`registry` active/count, `server`, `system-map`) is a cheap defensive net against ANY infra DID (existing snapshots / future accounts). Removing it would reintroduce the census/quorum-inflation bug PR #11 fixed for zero benefit — so it stays, reframed as general infra-DID hygiene. **Deviation flagged for operator awareness.**
- **Issue #9 E2E (real MySQL).** `test/economy/issue-9-endow-buy-land-irs.e2e.test.ts` drives the exact money moves the routes make — `NousAccountStore.getBalance` + `chargeToTreasury` (civic-parcels.ts) and the IRS `SELECT balance_wei FROM civic_treasury` (irs.ts) — proving a freshly-endowed citizen buys land on the SAME ledger and the IRS sees the revenue (was stuck at 0 under the split ledger). Skips gracefully with no DB.

## Verification

- `node scripts/check-ledger-b-money.mjs` → exit 0; proven to catch a reintroduced offender.
- `tsc --noEmit` clean.
- Issue #9 E2E: 3 pass against live `noesis-mysql`; skips with no DB.
- Affected suites (genesis/registry/reclaim/census/db/e2e): 180+ green.
- Migration tests: schema-v74 (sequential invariant relaxed), schema-v75 added.
- Allowlist byte-unchanged (D-8); audit zero-diff R-31-01; zero custody.

## Deviations

1. **Census exclusion kept** (not deleted) — see above. Safety-preserving; documented.

## Outcome

Phase 62.5 COMPLETE. The in-DB economy is one civic-DID-keyed ledger (`nous_accounts` + `civic_treasury`); `nous_registry.balance_wei` / `transferWei` are retired and CI-gated. **Issue #9 closed** (E2E). Ready for Phase 63 on-chain settlement to bridge a single balance. Operator: `gh issue close 9` when ready (left to you).
