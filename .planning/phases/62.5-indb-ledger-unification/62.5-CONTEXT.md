# Phase 62.5 — In-DB wei ledger unification (Money precursor)

> **Private dev log** (D-WIKI-06). System design is canonical in [wiki/1-design/economy.md](../../../wiki/1-design/economy.md); this is the build kickoff. Milestone: **v3.2 Money**. Plan parent: [../../money-migration-plan.md](../../money-migration-plan.md). Decimal insert **between Phase 62 (on-chain contracts) and Phase 63 (settlement path)** — the on-chain settlement path must bridge **one** coherent civic-keyed in-DB balance, not two.

## Why this phase exists (the finding)

A live UAT (2026-07-10, `claudedocs/finding-split-ledger-economy-2026-07-09.md`, [Issue #9](https://github.com/ASKKPOP/Noesis/issues/9)) proved the current **in-DB** wei economy runs on **two non-fungible ledgers with no bridge** — a precondition the on-chain plan silently assumed was already unified:

| | Ledger A — chain-ready | Ledger B — legacy |
|---|---|---|
| Store (key) | `nous_accounts` (**civic-DID**) · `NousAccountStore`/`wei-ops.ts` | `nous_registry.balance_wei` (**existence-DID**) · `NousRegistry.transferWei` |
| Treasury | `civic_treasury` (IRS/system-map read this) | `did:noesis:system:treasury` registry record |
| Funded by | **endowment** (`EndowmentStore.endow`), labor-escrow, procurement | **birth faucet** (`initialSupply:1000`) |
| Spent on | civic **dues** | **land, community found/join, business-DID, blueprint material, type-B bond** |

Live consequences (reproduced on `main`): the guide's "endow → buy land" is broken (endow funds A, land spends B → a fresh citizen has 0 in B → `buyer_not_found`); labour/procurement income can't buy land either; and the IRS is **blind to land revenue** (it reads `civic_treasury`, land revenue lands in the Ledger-B treasury). ECON-01: founding Nous are still minted 1000 at birth (`did:noesis:system:treasury` = 0 while `nous_registry` holds faucet money).

**This phase makes the in-DB economy coherent — one civic-DID-keyed balance — so Phase 63 can map it 1:1 to the on-chain `NousAccount`.** It also fixes the live citizen-can't-transact bug now, ahead of on-chain.

## Goal

Make **`nous_accounts` (civic-DID) + `civic_treasury`** the single in-DB source of truth for money. Retire `nous_registry.balance_wei` as money and the birth faucet (ECON-01). Reconcile the treasury so IRS/system-map see all revenue.

**Target invariant:** money lives only in `nous_accounts.balance_wei` (per civic-DID) + `civic_treasury.balance_wei`. A spend is an atomic transaction on `nous_accounts` (`debitAccountOnConn` → `creditTreasuryWeiOnConn` on one connection). Every active Civic-DID has exactly one `nous_accounts` row; it starts at **0** (no faucet).

## Deliverables (waves)

1. **62.5-01 — Accounts exist.** ✅ **DONE 2026-07-10.** On Civic-DID issuance (`api/routes/registry.ts` REG-01, after `appendRegistryCivicDidIssued`) call `NousAccountStore.ensureAccount({ gridName, civicDid, currentTick })` (idempotent `INSERT IGNORE`, balance 0; guarded on `services.pool`). Every new citizen now has a civic-keyed Ledger-A account. Regression: `registry-routes.test.ts` (15). Live-verified: `nous_accounts` was empty → a fresh issuance created the row (balance 0). *(Faucet-kill moved to 62.5-02 — see note.)*
2. **62.5-02 — Migrate spend paths B→A + retire faucet.** Replace every `nousRegistry.transferWei(x, TREASURY_DID, amt)` with an atomic `debitAccountOnConn(conn,{civicDid:buyerDid,amountWei})` + `creditTreasuryWeiOnConn(conn,{amountWei,currentTick})` in one `beginTransaction/commit`. Call sites: `civic-parcels.ts:160` (purchase) + build/co-build material, `civic/blueprint.ts:261`, `api/routes/community.ts:66,113`, `api/routes/registry.ts:262` (business-DID), `api/routes/registry-type-b.ts:72` (bond + refund). `buyerDid` is already the civic-DID (`didContext.did`). **Only once spend reads Ledger A, retire the faucet** in the same wave: `economy/config.ts:8 initialSupply: 0` + spawn founding Nous with 0 (`genesis/launcher.ts:442,716`) + reconcile the ~8 tests that assert founding-Nous balances. *(Sequencing: killing the faucet in 62.5-01 would strand founding Nous with 0 in both ledgers while spend still reads Ledger B, and redden those tests with no offsetting fix — so it's bundled here with the spend migration. Transactional helpers make these charges natively atomic → subsumes the BUG-B class from PR #8.)*
3. **62.5-03 — Treasury reconciliation.** Revenue → `civic_treasury`; `GET /irs/treasury` + system-map now see land/community/business income. Retire the `did:noesis:system:treasury` registry record → **PR #8 (treasury seed) + PR #11 (census exclusion) become dead code; delete them here.**
4. **62.5-04 — Retire Ledger B + data migration.** One-time schema migration (after v-latest): fold `nous_registry.balance_wei` into each citizen's `nous_accounts` via the `civic_did_registry` existence→civic map (recommend **zeroing** faucet money — see blocking decision). Remove/deprecate `NousRegistry.transferWei`; stop reading `nous_registry.balance_wei` as money.
5. **62.5-05 — Gate + tests + live E2E.** New CI gate forbidding `transferWei`/`nous_registry.balance_wei` money use (style of `check-operator-header-auth.mjs`). Update economy tests off Ledger-B balances; remove PR #8/#11 tests. **Clean-DB E2E: onboard → endow → buy land → assert buyer `nous_accounts` debited, `civic_treasury` credited, `/irs/treasury` reflects revenue** — the exact loop that fails today.

## Definition of done

- A freshly-onboarded Civic-DID has a `nous_accounts` row (balance 0); no faucet at birth.
- All land/community/business/blueprint/type-B charges debit `nous_accounts` (civic-keyed) and credit `civic_treasury`, atomically (failed charge → full rollback, no phantom).
- `/irs/treasury` + system-map reflect land/community/business revenue.
- `nous_registry.balance_wei` is no longer money; the new CI gate passes; `transferWei` is gone/deprecated.
- Clean-DB E2E of endow→buy-land→IRS-sees-revenue is green. Audit chain zero-diff (R-31-01) holds throughout.

## Invariants respected (no shortcuts)

- **No new audit events / allowlist change** — reuses the existing dues/labor/treasury debit-credit events and the existing `zoning.parcel_purchased`/`treasury.parcel_revenue` producers (the *store* the money moves in changes, not the event surface). If any producer's treasury actor changes, keep the sole-producer triad + `payloadPrivacyCheck` intact.
- **Zero custody** — `nous_accounts`/`civic_treasury` moves are in-DB civic bookkeeping, not on-chain transfers; the `grid/src` custody CI ban still passes (no `transferFrom`, no platform keys).
- **Hash-only boundary** — no new payload key; DIDs stay hashed HEX64 where they already were.
- **Deterministic tick** — no new `clock.onTick`; all charges stay request-driven.

## Blocking decision (needs user input before 62.5-04)

- **Faucet-money fold:** on retiring Ledger B, do we **zero** the founding-Nous faucet balances (recommended — the faucet is being retired as illegitimate per D-MONEY-01) or **migrate** the 3 × 1000 once into their `nous_accounts`? Operator call.

## Open / carry-forward

- This unblocks **Phase 63 (settlement path)**: with one civic-keyed balance, `nous_account_address` (Phase 62 wallet-proof) maps 1:1 and on-chain settlement bridges a single ledger.
- **Phase 66** (Ousia-faucet retire + `*_bios`→`*_wei` rename) is partly pre-done here (faucet retire); Phase 66 keeps the rename/cleanup + conflict-tribute. Update `money-migration-plan.md` to reflect the overlap.
- Groups & Holdings treasuries (D-GROUP-*/D-HOLD-01) also hold wei — confirm they are on `nous_accounts`/`civic_treasury` (Ledger A) already, or include them in 62.5-02.

## Status

**In progress — 2026-07-10.** Wave **62.5-01 ✅ DONE** (ensureAccount on issuance; tsc clean, registry-routes 15/15, live-verified). Waves 62.5-02..05 not started. Plan drafted from the live finding (`claudedocs/migration-plan-unify-ledger-2026-07-10.md`). Precursor to the in-progress Phase 62 on-chain work.

## Cross-references

- Finding + evidence: `claudedocs/finding-split-ledger-economy-2026-07-09.md`, `claudedocs/uat-2026-07-09.md`, [Issue #9](https://github.com/ASKKPOP/Noesis/issues/9)
- Migration plan (this phase's source): `claudedocs/migration-plan-unify-ledger-2026-07-10.md`
- Parent plan: [../../money-migration-plan.md](../../money-migration-plan.md) · System design: [wiki/1-design/economy.md](../../../wiki/1-design/economy.md)
- Superseded band-aids: PR #8 (treasury seed), PR #11 (census exclusion) — deleted in 62.5-03/04.
