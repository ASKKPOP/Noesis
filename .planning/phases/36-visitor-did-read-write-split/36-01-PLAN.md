---
phase: 36
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - grid/test/api/visitor-public-routes.test.ts
  - grid/test/api/visitor-audit-redaction.test.ts
  - grid/test/api/did-required-enforcement.test.ts
  - grid/test/api/did-revoked-behavior.test.ts
  - grid/test/api/policy-coverage.test.ts
  - grid/test/api/polis-bills-privacy.test.ts
  - grid/test/audit/firehose-hub-redaction.test.ts
  - grid/test/audit/firehose-hub-zero-diff.test.ts
  - grid/test/audit/append-portal-did-issued.test.ts
  - grid/test/audit/append-portal-did-revoked.test.ts
  - grid/test/audit/append-grid-recognition-granted.test.ts
  - grid/test/audit/append-grid-recognition-revoked.test.ts
  - grid/test/audit/append-portal-notification-dispatched.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - dashboard/src/app/portal/page.test.tsx
  - dashboard/src/app/portal/civic-map/CivicMap.test.tsx
  - .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
tags: [validation, tdd, wave-zero, allowlist-lock-in]
threat_refs: [T-36-R3101, T-36-VIS05, T-36-AL, T-36-VOTE, T-36-REVOKE]

must_haves:
  truths:
    - "All 14 grid test files + 2 dashboard test files listed in VALIDATION.md §Wave 0 Requirements exist before any implementation plan starts"
    - "broadcast-allowlist.test.ts asserts ALLOWLIST_MEMBERS.length === 60 (open-question 2 locked: portal.notification_dispatched is NOT on the broadcast allowlist)"
    - "All new grid tests FAIL on red (vitest run shows expected-fail markers — proves tests verify behaviour, not vacuously pass)"
    - "VALIDATION.md frontmatter flipped to nyquist_compliant: true with allowlist-count open-question resolved"
  artifacts:
    - path: grid/test/audit/broadcast-allowlist.test.ts
      provides: "Updated count assertion 56 → 60 per Wave 0 open-question resolution"
      contains: "expect(ALLOWLIST_MEMBERS.length).toBe(60)"
    - path: grid/test/api/policy-coverage.test.ts
      provides: "Asserts ROUTE_DID_POLICY covers every Fastify route in api/v1/"
      min_lines: 30
    - path: grid/test/audit/firehose-hub-zero-diff.test.ts
      provides: "R-31-01 regression guard — chain head hash byte-identical with/without visitor subscribers"
      min_lines: 40
    - path: .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md
      provides: "Wave 0 sign-off; nyquist_compliant flipped to true; open-question 1 locked to 60"
      contains: "nyquist_compliant: true"
  key_links:
    - from: grid/test/audit/broadcast-allowlist.test.ts
      to: grid/src/audit/broadcast-allowlist.ts
      via: "import ALLOWLIST_MEMBERS; assert length === 60"
      pattern: "ALLOWLIST_MEMBERS\\.length.*60"
    - from: grid/test/audit/firehose-hub-zero-diff.test.ts
      to: grid/src/audit/firehose-hub.ts
      via: "constructs hub, attaches visitor + did subscribers, asserts chain.headHash unchanged across both states"
      pattern: "headHash|chain\\.head"
---

<objective>
Create the complete Wave 0 validation infrastructure for Phase 36: 14 grid test files + 2 dashboard test files that encode the visitor/DID read-write contract as executable specifications. Lock the allowlist-count open question (60, NOT 61) by updating the existing broadcast-allowlist.test.ts assertion and documenting the decision in VALIDATION.md.

Purpose: Every implementation plan (02-07) depends on these tests existing as failing red specifications. Plans 02-07 turn them green by implementing the contracts the tests assert. Without this Wave 0, plans 02-07 would have no automated verification — violating Nyquist sampling.

Output: 16 test files (15 new + 1 update) plus amended VALIDATION.md with open-question 1 resolved to 60.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-visitor-did-read-write-split/36-CONTEXT.md
@.planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md
@.planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md
@.planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md
@.planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md

<interfaces>
<!-- Existing exports the test files will reference. -->
<!-- Executor copies these signatures, does not discover them. -->

From grid/src/audit/broadcast-allowlist.ts (current state — count = 56):
```typescript
export const ALLOWLIST_MEMBERS: readonly string[] = [ /* 56 entries */ ];
export function buildFrozenAllowlist(members: readonly string[]): ReadonlySet<string>;
export function payloadPrivacyCheck(payload: unknown): { ok: boolean; offendingPath?: string; offendingKeyword?: string };
```

From grid/src/audit/append-portal-auth-login.ts (canonical sole-producer reference):
```typescript
export const LOGIN_METHOD_ENUM = ['email', 'siwe'] as const;
export interface PortalAuthLoginPayload {
  readonly human_did: string;
  readonly method: LoginMethod;
  readonly tick: number;
}
export function appendPortalAuthLogin(audit: AuditChain, payload: PortalAuthLoginPayload): AuditEntry;
// Throws TypeError on bad shape, bad DID regex, bad tick, extra/missing keys, privacy violation.
```

From grid/src/audit/firehose-hub.ts:
```typescript
export interface FirehoseStats {
  readonly client_count: number;
  readonly frames_sent_total: number;
  readonly frames_dropped_total: number;
  readonly last_frame_at: number | null;
  readonly watermark_bytes: number;
}
export class WsFirehoseHub {
  constructor(audit: AuditChain, gridName: string);
  onConnect(socket: ServerSocket): void;
  stats(): FirehoseStats;
}
```

From grid/src/audit/chain.ts:
```typescript
export class AuditChain {
  append(eventType: string, actorDid: string, payload: object): AuditEntry;
  get headHash(): string; // hex digest of running chain hash
  onAppend(listener: (entry: AuditEntry) => void): () => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 8 audit-layer test files (sole-producers, WS-hub redaction, zero-diff, allowlist count)</name>
  <files>
    grid/test/audit/append-portal-did-issued.test.ts,
    grid/test/audit/append-portal-did-revoked.test.ts,
    grid/test/audit/append-grid-recognition-granted.test.ts,
    grid/test/audit/append-grid-recognition-revoked.test.ts,
    grid/test/audit/append-portal-notification-dispatched.test.ts,
    grid/test/audit/firehose-hub-redaction.test.ts,
    grid/test/audit/firehose-hub-zero-diff.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts
  </files>
  <read_first>
    grid/src/audit/append-portal-auth-login.ts,
    grid/test/audit/operator-payload-privacy.test.ts,
    grid/test/audit/operator-exported-producer-boundary.test.ts,
    grid/test/audit/zero-diff-bios.test.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/src/audit/firehose-hub.ts,
    grid/src/audit/chain.ts,
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md,
    .planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md
  </read_first>
  <behavior>
    Per file:

    **append-portal-did-issued.test.ts** (event `portal.did_issued`, alphabetical keys `[human_or_nous_did, issued_at_tick, issuer_portal_id]`, actor = `human_or_nous_did`):
    - Throws TypeError when payload is null, an array, or a primitive.
    - Throws TypeError when `human_or_nous_did` is not a string or fails `DID_RE`.
    - Throws TypeError when `issued_at_tick` is not a non-negative integer (test: -1, 1.5, NaN).
    - Throws TypeError when `issuer_portal_id` is not a non-empty string.
    - Throws TypeError when key set is wrong (extra key, missing key, wrong-name key).
    - On valid payload: returns an AuditEntry whose `eventType === 'portal.did_issued'` and `actorDid === payload.human_or_nous_did`.

    **append-portal-did-revoked.test.ts** (event `portal.did_revoked`, keys `[human_or_nous_did, revoked_at_tick, revoker_portal_id]`, actor = `human_or_nous_did`):
    - Same 6 assertion shapes as portal.did_issued, substituting `revoked_at_tick` + `revoker_portal_id`.

    **append-grid-recognition-granted.test.ts** (event `grid.recognition_granted`, keys `[grid_name, granted_at_tick, nous_did]`, actor = `nous_did`):
    - Same 6 shapes; `grid_name` is non-empty string; `granted_at_tick` non-negative integer; `nous_did` matches DID_RE.

    **append-grid-recognition-revoked.test.ts** (event `grid.recognition_revoked`, keys `[grid_name, nous_did, revoked_at_tick]`, actor = `nous_did`):
    - Same 6 shapes; key order ALPHABETICAL (grid_name, nous_did, revoked_at_tick).

    **append-portal-notification-dispatched.test.ts** (event `portal.notification_dispatched`, keys `[dispatched_at_tick, notification_type, operator_did]`, actor = `operator_did`):
    - Same 6 shapes; `notification_type` is non-empty string; `operator_did` matches DID_RE.
    - Plus: separate `it('is NOT on the broadcast allowlist (private queue event per D-36-19)')` — imports `ALLOWLIST_MEMBERS` and asserts `ALLOWLIST_MEMBERS.includes('portal.notification_dispatched') === false`.

    **firehose-hub-redaction.test.ts** (extends grid/src/audit/firehose-hub.ts):
    - Constructs `WsFirehoseHub(chain, 'genesis')`, opens two mock sockets via `onConnect(socket, didContext)` — one with `{tier:'civic_member', did:'did:civic:noesis:test1'}`, one with `null` (anonymous).
    - Triggers an event via `appendPortalAuthLogin(chain, {human_did:'did:noesis:human:0xabc', method:'siwe', tick:5})`.
    - Asserts the civic_member socket received a frame containing the full entry (`actor_did` and `payload.human_did` present).
    - Asserts the anonymous socket received a frame containing ONLY `{tick, event_type, family}` — `actor_did`, `payload` keys are ABSENT (`expect(frame.entry).not.toHaveProperty('actor_did')` and `expect(frame.entry).not.toHaveProperty('payload')`).
    - Asserts frame.entry.family === 'portal' (split of event_type on '.').

    **firehose-hub-zero-diff.test.ts** (R-31-01 regression guard):
    - Constructs `chain = new AuditChain()` and `WsFirehoseHub(chain, 'genesis')`.
    - Open 0 subscribers; append the same canonical event; record `head0Sub = chain.headHash`.
    - Build a new chain + hub; open 1 anonymous subscriber; append the same event; record `head1Anon`.
    - Build a new chain + hub; open 1 civic_member subscriber + 1 anonymous subscriber; append the same event; record `headMixed`.
    - Assert `head0Sub === head1Anon === headMixed` (subscriber composition does NOT affect chain head hash — R-31-01).
    - Include the comment: `// R-31-01 zero-diff: chain head hash MUST NOT vary with subscriber composition`.

    **broadcast-allowlist.test.ts** (UPDATE existing line 16):
    - Change `expect(ALLOWLIST_MEMBERS.length).toBe(56)` → `expect(ALLOWLIST_MEMBERS.length).toBe(60)` per D-36-17 and Wave 0 open-question resolution (4 new VIS-05 events; portal.notification_dispatched stays OFF the allowlist).
    - Add a new test block: `it('Phase 36 VIS-05 additions are present', () => { ... })` asserting `ALLOWLIST_MEMBERS.includes('portal.did_issued')`, `'portal.did_revoked'`, `'grid.recognition_granted'`, `'grid.recognition_revoked'` all return true.
    - Add a new test block: `it('portal.notification_dispatched is intentionally NOT on the broadcast allowlist (D-36-19 private queue event)', () => { expect(ALLOWLIST_MEMBERS.includes('portal.notification_dispatched')).toBe(false); })`.
  </behavior>
  <action>
    Create the 8 test files (7 new + 1 update) listed in `<files>` above.

    Style discipline:
    - Use `import { describe, it, expect } from 'vitest'` (matches existing grid tests).
    - Each sole-producer test imports the (not-yet-existing) module from `'../../src/audit/append-<name>.js'`. The .js extension is required by NodeNext module resolution. Tests will fail with import error until Plan 04 creates the source file — this is the expected RED state.
    - WS-hub tests import `WsFirehoseHub` from `'../../src/audit/firehose-hub.js'` and reference the not-yet-added `didContext` parameter on `onConnect`. Tests will fail (signature mismatch) until Plan 03 lands the extension.
    - For mock sockets: use a tiny inline class `class MockSocket { sent: string[] = []; send(msg: string) { this.sent.push(msg); } on(_event: string, _handler: () => void) {} }` rather than importing a real WS lib.
    - For DID test data: use canonical examples `'did:noesis:human:0xabcdef0123456789'`, `'did:civic:noesis:gen-001'`, `'did:noesis:nous:zX9'` — all matching `/^did:[a-z0-9:_\-]+$/i` (DID_RE shape).
    - For chain construction: `new AuditChain()` with no args (matches the in-memory chain default).

    Do NOT create the source files this plan asserts against — Plans 03 + 04 do that. The tests must FAIL initially (RED state).
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx vitest run test/audit/append-portal-did-issued.test.ts test/audit/append-portal-did-revoked.test.ts test/audit/append-grid-recognition-granted.test.ts test/audit/append-grid-recognition-revoked.test.ts test/audit/append-portal-notification-dispatched.test.ts test/audit/firehose-hub-redaction.test.ts test/audit/firehose-hub-zero-diff.test.ts test/audit/broadcast-allowlist.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - File `grid/test/audit/append-portal-did-issued.test.ts` exists and contains the literal strings `'portal.did_issued'`, `'human_or_nous_did'`, `'issued_at_tick'`, `'issuer_portal_id'`.
    - File `grid/test/audit/append-portal-did-revoked.test.ts` exists and contains the literal strings `'portal.did_revoked'`, `'revoked_at_tick'`, `'revoker_portal_id'`.
    - File `grid/test/audit/append-grid-recognition-granted.test.ts` exists and contains the literal strings `'grid.recognition_granted'`, `'grid_name'`, `'granted_at_tick'`, `'nous_did'`.
    - File `grid/test/audit/append-grid-recognition-revoked.test.ts` exists and contains the literal strings `'grid.recognition_revoked'`, `'grid_name'`, `'nous_did'`, `'revoked_at_tick'`.
    - File `grid/test/audit/append-portal-notification-dispatched.test.ts` exists and contains `'portal.notification_dispatched'` AND `'notification_type'` AND `ALLOWLIST_MEMBERS.includes`.
    - File `grid/test/audit/firehose-hub-redaction.test.ts` exists and references `didContext`, `civic_member`, and `family`.
    - File `grid/test/audit/firehose-hub-zero-diff.test.ts` exists and references `headHash` AND contains a comment with the literal string `R-31-01`.
    - `grid/test/audit/broadcast-allowlist.test.ts` now contains `expect(ALLOWLIST_MEMBERS.length).toBe(60)` (the previous `toBe(56)` was replaced).
    - `grid/test/audit/broadcast-allowlist.test.ts` contains the literal string `'portal.notification_dispatched'` AND `.toBe(false)` in the same `it()` block.
    - Running `cd grid &amp;&amp; npx vitest run test/audit/append-portal-did-issued.test.ts` exits NON-ZERO (RED state — proves the test is real, not vacuous).
  </acceptance_criteria>
  <done>
    All 8 audit-layer test files created with the exact event types, payload key tuples, and structural assertions specified. The allowlist count assertion updated 56→60. Open-question 1 (portal.notification_dispatched off allowlist) encoded in two separate it() blocks. Tests fail RED on missing source modules — this is the expected initial state.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create 6 grid API-layer test files (visitor routes, DID enforcement, policy coverage, VOTE-05)</name>
  <files>
    grid/test/api/visitor-public-routes.test.ts,
    grid/test/api/visitor-audit-redaction.test.ts,
    grid/test/api/did-required-enforcement.test.ts,
    grid/test/api/did-revoked-behavior.test.ts,
    grid/test/api/policy-coverage.test.ts,
    grid/test/api/polis-bills-privacy.test.ts
  </files>
  <read_first>
    grid/test/api/audit-firehose.test.ts,
    grid/test/api/governance-ballot.test.ts,
    grid/test/api/humans.test.ts,
    grid/test/api/server.cors.test.ts,
    grid/src/api/server.ts,
    grid/src/api/routes/health-detailed.ts,
    grid/src/api/portal/check-frozen.ts,
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md,
    .planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md
  </read_first>
  <behavior>
    Per file:

    **visitor-public-routes.test.ts** — boots a Fastify instance via `buildServer(servicesStub)` and asserts:
    - `GET /api/v1/civic-map/state` returns 200 with body shape `{zones: Array, nous: Array}` for an unauthenticated request (no Authorization header, no cookie).
    - `GET /api/v1/library/entries` returns 200 with body shape `{entries: Array}`.
    - `GET /api/v1/market/listings` returns 200 with body shape `{listings: Array}`.
    - `GET /api/v1/polis/bills` returns 200 with body shape `{bills: Array}`.
    - `GET /api/v1/nous/<did-hash>/public` returns 200 with body shape `{display_name, zone, civic_standing_tier, public_bio, status}` and does NOT include keys `memory`, `karpathy`, `hypnos`, `pneuma`, `treasury_balance` (D-36-11).
    - Each assertion: `expect(res.statusCode).toBe(200)` AND structural shape check.

    **visitor-audit-redaction.test.ts** — asserts:
    - `GET /api/v1/audit/trail` returns entries whose `actor_did` field has been replaced with family prefix (no full DID) when called WITHOUT bearer.
    - Same endpoint WITH valid civic-DID bearer returns the full `actor_did`.
    - Use mock chain entries: `{eventType:'nous.spoke', actorDid:'did:civic:noesis:gen-001', tick:1}` — visitor sees `'nous'`, civic-member sees `'did:civic:noesis:gen-001'`.

    **did-required-enforcement.test.ts** — for EACH of the following write-route paths, asserts `POST <path>` without Authorization header returns `401 {error:'did_required', accepted_methods: [...]}`:
    - `POST /api/v1/trade`
    - `POST /api/v1/governance/propose`
    - `POST /api/v1/governance/commit`
    - `POST /api/v1/governance/reveal`
    - `POST /api/v1/spawn`
    Use a single `it.each([routes])` loop to cover all 5. Plus a separate `it()` block asserting `POST /portal/auth/oauth/google` returns 501 (stub, public route — no 401).

    **did-revoked-behavior.test.ts** — D-36-09 contract:
    - Constructs a server with a mock DID-status store that returns `{status: 'revoked'}` for `did:civic:noesis:revoked-test`.
    - Sends `GET /api/v1/civic-map/state` with `Authorization: Bearer <jwt-for-revoked-did>` — asserts 200 (revoked DID retains READ access).
    - Sends `POST /api/v1/trade` with same bearer — asserts 401 (revoked DID loses WRITE access; falls through to visitor tier).
    - The 401 body MUST include `{error:'did_required'}` (NOT a hard-block 403).

    **policy-coverage.test.ts** — VIS-04 contract:
    - Imports `ROUTE_DID_POLICY` from `'../../src/api/policy.js'` (not yet existing).
    - Boots a server with `buildServer()`, calls `app.printRoutes()` and parses output (or `app.routes` if Fastify v5 exposes it), then asserts: for every registered route under `/api/v1/` AND `/portal/`, `ROUTE_DID_POLICY[`${method} ${path}`]` is defined.
    - Test fails if any route is missing from the table (mirrors CI gate logic).
    - Also asserts the enum is exactly the 6 strings: `['public', 'portal_session_required', 'civic_did_required', 'business_did_required', 'government_only', 'police_only']` (per D-36-17).

    **polis-bills-privacy.test.ts** — D-36-15 / VOTE-05 contract:
    - Constructs server with mock bill store containing a bill with attached ballots: `{id:'bill-1', body:'...', tally:{pass:5,fail:2,abstain:1}, ballots:[{voter_did:'...',choice:'pass'}]}`.
    - Calls `GET /api/v1/polis/bills/bill-1` (no bearer — visitor request).
    - Asserts response body has `tally` field but the `ballots` field is ABSENT (`expect(body.ballots).toBeUndefined()`).
    - Asserts response body does NOT contain any string matching `/voter_did|ballot/` (no leak through nested objects either).
    - Asserts response body DOES contain `tally.pass`, `tally.fail`, `tally.abstain`.
    - Comment: `// VOTE-05 invariant: ballot privacy preserved even at tally exposure (D-36-15)`.
  </behavior>
  <action>
    Create the 6 test files in `<files>`. Style discipline:

    - Use Fastify's inject API: `const res = await app.inject({method:'GET', url:'/api/v1/civic-map/state'})`.
    - Import shape: `import { buildServer } from '../../src/api/server.js'` (matches `grid/test/api/server.cors.test.ts`).
    - Use minimal `servicesStub` — most tests in `grid/test/api/` already define stub services inline (`humans.test.ts` defines a `mkServices()` helper near the top). Reuse that style.
    - For did-required-enforcement: tests should use `it.each([['POST', '/api/v1/trade'], ...])` to keep test code under 60 lines.
    - For policy-coverage: use `app.printRoutes()` (Fastify returns a string tree) — parse line-by-line.
    - For polis-bills-privacy: a hand-rolled JSON.stringify check is acceptable: `expect(JSON.stringify(body)).not.toMatch(/voter_did|ballots/)`.

    All 6 tests MUST fail RED on first run (modules/routes don't exist yet). This is correct — Plans 02, 05, 07 turn them green.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx vitest run test/api/visitor-public-routes.test.ts test/api/visitor-audit-redaction.test.ts test/api/did-required-enforcement.test.ts test/api/did-revoked-behavior.test.ts test/api/policy-coverage.test.ts test/api/polis-bills-privacy.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `grid/test/api/visitor-public-routes.test.ts` exists and contains literal route strings `'/api/v1/civic-map/state'`, `'/api/v1/library/entries'`, `'/api/v1/market/listings'`, `'/api/v1/polis/bills'`, `'/api/v1/nous/'` AND asserts `200` status code at least 5 times.
    - `grid/test/api/visitor-audit-redaction.test.ts` exists and asserts the family prefix (`'nous'`) is present and the full DID (`'did:civic:noesis:gen-001'`) is absent for an unauthenticated request.
    - `grid/test/api/did-required-enforcement.test.ts` exists and references all 5 write routes (`/api/v1/trade`, `/api/v1/governance/propose`, `/api/v1/governance/commit`, `/api/v1/governance/reveal`, `/api/v1/spawn`) AND asserts response `error === 'did_required'`.
    - `grid/test/api/did-revoked-behavior.test.ts` exists and contains BOTH a 200-on-read assertion AND a 401-on-write assertion for the same revoked-DID context (D-36-09 revert-to-visitor).
    - `grid/test/api/policy-coverage.test.ts` exists and contains the literal strings `'public'`, `'portal_session_required'`, `'civic_did_required'`, `'business_did_required'`, `'government_only'`, `'police_only'` (the 6-value enum check).
    - `grid/test/api/polis-bills-privacy.test.ts` exists and contains `expect(body.ballots).toBeUndefined()` OR equivalent `.not.toHaveProperty('ballots')` assertion AND a comment referencing `VOTE-05`.
    - Running `cd grid &amp;&amp; npx vitest run test/api/policy-coverage.test.ts` exits NON-ZERO (RED state — ROUTE_DID_POLICY module does not exist yet).
  </acceptance_criteria>
  <done>
    All 6 API-layer tests created with verifiable structural assertions. All 5 known write routes covered for 401-enforcement. D-36-09 revert-to-visitor encoded as two-part assertion (200 read + 401 write). VOTE-05 ballot privacy encoded as absent-field assertion. Tests RED on first run.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 2 dashboard component tests + amend VALIDATION.md (lock open question 1)</name>
  <files>
    dashboard/src/app/portal/page.test.tsx,
    dashboard/src/app/portal/civic-map/CivicMap.test.tsx,
    .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md
  </files>
  <read_first>
    dashboard/src/app/portal/page.tsx,
    dashboard/src/app/portal/portal-dashboard.test.ts,
    .planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md,
    .planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md,
    .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md
  </read_first>
  <behavior>
    **dashboard/src/app/portal/page.test.tsx** (NEW — component test):
    - Renders `<PortalLandingPage tier="anonymous" />` (the v3.0 page accepts tier as prop OR reads it server-side; for tests, render with explicit tier).
    - Asserts the document title contains the literal `'Noēsis · Polis'` (UI-SPEC §Copywriting Contract).
    - Asserts the rendered text contains `'Welcome to the Polis'` (HERO_H1) AND `'A digital city where Nous earn, learn, trade, form communities, and self-govern.'` (HERO_SUBTITLE).
    - Asserts the rendered text contains `'By entering Genesis Grid, you agree to the Grid Charter and the Laws of Themis.'` (TOS — D-36-22).
    - Asserts the anonymous banner shows: `'Welcome to Noēsis. Sign up to participate in the Polis.'`.
    - Asserts the rendered DOM contains an element with text `'Sign up'` (CTA).

    **dashboard/src/app/portal/civic-map/CivicMap.test.tsx** (NEW):
    - Renders `<CivicMap zones={mockZones} nous={mockNous} />` with mock data: 6 zones (Business, Manufacture, Shopping, Residential, Infrastructure, Government Quarter — exact D-V3-32 names) and 2 Nous avatars.
    - Asserts the rendered SVG contains exactly 6 `<polygon>` elements (`container.querySelectorAll('polygon').length === 6`).
    - Asserts each of the 6 zone labels appears in the rendered text: `'Business'`, `'Manufacture'`, `'Shopping'`, `'Residential'`, `'Infrastructure'`, `'Government Quarter'`.
    - Asserts the SVG has `role="img"` and an aria-label containing `'Civic Map'`.

    **VALIDATION.md amendment** (UPDATE):
    - Add a new H3 section near the top (after the "Test Infrastructure" table): `### Wave 0 Allowlist-Count Decision (LOCKED)`. Body:
      > **Decision (2026-05-25):** `ALLOWLIST_MEMBERS.length === 60` after Phase 36.
      > **Rationale:** Per D-36-19, `portal.notification_dispatched` is a personal-queue event (server-pushed-via-poll to one operator-DID), NOT a broadcast city event. It enters the audit chain but does NOT enter `ALLOWLIST_MEMBERS`. Final allowlist additions for Phase 36: 4 (portal.did_issued, portal.did_revoked, grid.recognition_granted, grid.recognition_revoked). Pre-phase count: 56. Post-phase count: **60**. ROADMAP entry "Allowlist count grows 56 → 60" stands.
      > **Open Question 1 (Allowlist count target: 60 or 61?): RESOLVED → 60.**
    - In the existing `## Open Questions (resolve in Wave 0)` section, mark Question 1 with strikethrough: `~~1. **Allowlist count target: 60 or 61?**~~ → **RESOLVED to 60** (see "Wave 0 Allowlist-Count Decision (LOCKED)" above).`
    - Update frontmatter: flip `nyquist_compliant: false` → `nyquist_compliant: true`, flip `wave_0_complete: false` → `wave_0_complete: true`.
    - In the existing `## Validation Sign-Off` checklist, mark all 6 boxes as `[x]` and change "**Approval:** pending" to "**Approval:** approved 2026-05-25 (Wave 0 plan 36-01)".
  </behavior>
  <action>
    Create 2 dashboard test files and update 1 markdown file.

    For the test files:
    - Match the import style of `dashboard/src/app/portal/portal-dashboard.test.ts` — uses `@testing-library/react` and `vitest`. Use `import { describe, it, expect } from 'vitest'` and `import { render } from '@testing-library/react'`.
    - The page test should mock the tier resolution (don't try to render the full server-side page — render the JSX directly with tier prop OR mock the `resolveVisitorTier()` helper).
    - For the CivicMap test, construct mock data inline; use `const { container } = render(<CivicMap zones={mockZones} nous={mockNous} />)` and `container.querySelectorAll('polygon').length`.

    Tests will fail RED because `PortalLandingPage` will not yet accept a `tier` prop and `CivicMap` component will not yet exist. This is expected.

    For VALIDATION.md: use Edit-style precision — preserve the existing frontmatter except the two flag flips; preserve the existing per-task verification map table; only ADD the resolution section and the sign-off update.
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; npx vitest run src/app/portal/page.test.tsx src/app/portal/civic-map/CivicMap.test.tsx 2>&amp;1 | tail -30 ; echo '---VALIDATION---' ; grep -c "RESOLVED" .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md ; grep -c "nyquist_compliant: true" .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/app/portal/page.test.tsx` exists and contains the literal strings `'Noēsis · Polis'`, `'Welcome to the Polis'`, `'A digital city where Nous earn, learn, trade, form communities, and self-govern.'`, `'By entering Genesis Grid, you agree to the Grid Charter and the Laws of Themis.'`, `'Welcome to Noēsis. Sign up to participate in the Polis.'`, `'Sign up'`.
    - `dashboard/src/app/portal/civic-map/CivicMap.test.tsx` exists and asserts `querySelectorAll('polygon').length === 6` AND contains the literal strings `'Business'`, `'Manufacture'`, `'Shopping'`, `'Residential'`, `'Infrastructure'`, `'Government Quarter'`, `'role="img"'` OR `getAttribute('role')`, `'Civic Map'`.
    - `.planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md` frontmatter contains `nyquist_compliant: true` AND `wave_0_complete: true`.
    - `36-VALIDATION.md` contains the literal heading `Wave 0 Allowlist-Count Decision (LOCKED)` AND the literal string `RESOLVED → 60`.
    - `36-VALIDATION.md` "Validation Sign-Off" section has all 6 `[ ]` boxes changed to `[x]` AND `**Approval:** approved 2026-05-25 (Wave 0 plan 36-01)`.
    - Running `cd dashboard &amp;&amp; npx vitest run src/app/portal/page.test.tsx` exits NON-ZERO (RED state — page does not yet accept tier prop).
  </acceptance_criteria>
  <done>
    Both dashboard tests created with verbatim UI-SPEC copy strings embedded. 6-zone polygon count asserted with exact D-V3-32 zone names. VALIDATION.md sign-off complete; Wave 0 open-question 1 locked to 60; frontmatter flags flipped. Wave 0 ready for downstream waves to begin.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test files → source modules | Test files reference yet-to-exist source modules; verify the contract, not the implementation. Wave 0 tests MUST fail RED before any implementation lands. |
| VALIDATION.md → Wave 0 sign-off | Documents that Wave 0 is complete; downstream plans use this as a precondition gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-AL | Tampering | grid/src/audit/broadcast-allowlist.ts count assertion | mitigate | Test file pins `ALLOWLIST_MEMBERS.length === 60`; running CI shows mismatch immediately on count drift (D-36-19 lock). |
| T-36-VIS05 | Tampering | grid/src/audit/append-*.ts sole producers (5 new) | mitigate | Each test asserts TypeError on each bad-payload shape (8-step triad); covers undeclared producers + key-set drift. |
| T-36-VOTE | Information Disclosure | grid/src/api/visitor/polis-bills.ts (Plan 05) | mitigate | polis-bills-privacy.test.ts asserts `ballots` field absent for visitor request; preserves VOTE-05 ballot privacy at tally-exposure boundary (D-36-15). |
| T-36-REVOKE | Elevation of Privilege | DID enforcement after revocation | mitigate | did-revoked-behavior.test.ts asserts revoked DID retains read (200) but loses write (401) — D-36-09 revert-to-visitor contract pinned. |
| T-36-R3101 | Tampering | grid/src/audit/firehose-hub.ts visitor serializer | mitigate | firehose-hub-zero-diff.test.ts asserts chain.headHash byte-identical across subscriber compositions; regression guard for R-31-01 zero-diff invariant. |
</threat_model>

<verification>
- All 16 test files exist at exact paths in `<files>` blocks.
- All 16 tests are RED on initial run (modules/components don't exist yet — proves tests have non-vacuous expectations).
- VALIDATION.md frontmatter flags `nyquist_compliant: true` and `wave_0_complete: true`.
- VALIDATION.md contains explicit lock-in for allowlist count = 60.
- `grep -c "ALLOWLIST_MEMBERS.length).toBe(60)" grid/test/audit/broadcast-allowlist.test.ts` returns 1 (and the old `toBe(56)` is gone).
</verification>

<success_criteria>
- 14 grid test files + 2 dashboard test files exist with the exact behaviors specified in `<behavior>` blocks.
- All test files RED on first run (no source modules exist yet → import errors OR assertion failures expected).
- broadcast-allowlist.test.ts updated to assert length 60 + the new it() blocks for VIS-05 inclusion and notification-dispatched exclusion.
- VALIDATION.md amended with allowlist-count lock-in + sign-off complete + frontmatter flags flipped.
- No source modules touched (this plan is tests-only).
</success_criteria>

<output>
After completion, create `.planning/phases/36-visitor-did-read-write-split/36-01-SUMMARY.md` documenting:
- 16 test files created + 1 amendment to VALIDATION.md
- Allowlist count open question RESOLVED to 60 (per D-36-19 reasoning)
- Wave 0 complete — Waves 1+ may now proceed
- Expected RED count from `vitest run` baseline before any Plan 02-07 implementation
</output>
