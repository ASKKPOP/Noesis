---
phase: 36
plan: 07
type: execute
wave: 2
depends_on: [36-01, 36-05]
files_modified:
  - dashboard/src/app/portal/page.tsx
  - dashboard/src/app/portal/layout.tsx
  - dashboard/src/app/portal/civic-map/page.tsx
  - dashboard/src/app/portal/civic-map/CivicMap.tsx
  - dashboard/src/app/portal/civic-map/zone/[zone_id]/page.tsx
  - dashboard/src/app/portal/library/page.tsx
  - dashboard/src/app/portal/marketplace/page.tsx
  - dashboard/src/app/portal/polis/page.tsx
  - dashboard/src/app/portal/polis/[bill_id]/page.tsx
  - dashboard/src/app/portal/nous/[civic_did_hash]/page.tsx
  - dashboard/src/app/portal/notifications/page.tsx
  - dashboard/src/lib/use-civic-map.ts
  - dashboard/src/lib/visitor-tier.ts
  - dashboard/src/lib/portal-copy.ts
autonomous: true
requirements: [VIS-01]
tags: [dashboard, portal-ui, civic-map, raw-svg, 5-sec-polling, three-tier-visitor]
threat_refs: [T-36-VIS01]

must_haves:
  truths:
    - "Portal landing page renders verbatim copy strings from UI-SPEC Copywriting Contract"
    - "Civic Map renders 6 zones in raw SVG (D-V3-06 invariant; NO d3, NO react-flow, NO cytoscape, NO three.js)"
    - "Civic Map polls /api/v1/civic-map/state every 5 seconds via plain useEffect+setInterval+AbortController (NO SWR, NO react-query)"
    - "Nous avatar hover uses React state for radius change (Tailwind 4 limitation per Pitfall 3)"
    - "Three-tier welcome banner branches per resolveVisitorTier() result; verbatim copy strings per tier"
  artifacts:
    - path: dashboard/src/app/portal/page.tsx
      provides: "Portal landing — tier-aware banner + nav + 5 surface CTAs"
      contains: "Welcome to the Polis"
      min_lines: 60
    - path: dashboard/src/app/portal/civic-map/CivicMap.tsx
      provides: "Raw-SVG 6-zone Civic Map with per-Nous avatars (5s polling)"
      contains: "polygon"
      min_lines: 80
    - path: dashboard/src/lib/use-civic-map.ts
      provides: "5-second polling hook mirroring use-health-detailed.ts shape"
      contains: "setInterval"
      min_lines: 30
    - path: dashboard/src/lib/visitor-tier.ts
      provides: "Server-side visitor tier resolution from session cookie"
      contains: "VisitorTier"
      min_lines: 20
    - path: dashboard/src/lib/portal-copy.ts
      provides: "Verbatim copy string constants per UI-SPEC Copywriting Contract"
      contains: "Welcome to the Polis"
  key_links:
    - from: dashboard/src/lib/use-civic-map.ts
      to: grid /api/v1/civic-map/state endpoint (Plan 05)
      via: "fetch(`${GRID_ORIGIN}/api/v1/civic-map/state`) every 5000ms"
      pattern: "civic-map/state"
    - from: dashboard/src/app/portal/page.tsx
      to: dashboard/src/lib/visitor-tier.ts
      via: "calls resolveVisitorTier() server-side; passes tier to banner component"
      pattern: "resolveVisitorTier"
---

<objective>
Land the 5 primary + 2 secondary visitor-facing dashboard surfaces per UI-SPEC.md, on top of Plan 05's backend routes. All visual surfaces obey the v3.0 dark-editorial palette and the D-V3-06 raw-SVG invariant. The Portal landing page is the only surface where D-36-23 permits 3D libs (but Phase 36 ships functional-only; full 3D hero is Phase 56 polish per D-36-24).

Purpose: VIS-01 visitor-facing UX. Plan 01 dashboard tests (`page.test.tsx`, `CivicMap.test.tsx`) turn GREEN. The 5 verbatim copy strings from UI-SPEC become testable promises.

Output: 11 page/component files + 3 lib files (use-civic-map, visitor-tier, portal-copy).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md
@.planning/phases/36-visitor-did-read-write-split/36-CONTEXT.md
@.planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md
@.planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md

<interfaces>
<!-- Existing patterns + Plan 05 backend exports. -->

From steward/src/lib/use-health-detailed.ts (canonical 5s polling hook — copy verbatim, sub endpoint + types):
```typescript
'use client';
import { useEffect, useState } from 'react';
export function useHealthDetailed() {
  const [data, setData] = useState(null);
  const [error, setError] = useState(null);
  const [isLoading, setIsLoading] = useState(true);
  useEffect(() => {
    let cancelled = false;
    const controller = new AbortController();
    async function fetchHealth() {
      try { const res = await fetch(...); if (cancelled) return; setData(...); }
      catch (err) { if (cancelled) return; setError(...); }
    }
    fetchHealth();
    const interval = setInterval(fetchHealth, 5000);
    return () => { cancelled = true; controller.abort(); clearInterval(interval); };
  }, []);
  return { data, error, isLoading };
}
```

From steward/src/app/culture/skill-lineage.tsx (canonical raw-SVG render — copy this pattern):
```tsx
'use client';
<svg role="img" aria-label="..." viewBox="0 0 800 600">
  {nodes.map(n => <circle key={n.id} cx={n.x} cy={n.y} r={...} />)}
</svg>
// NO d3, NO react-flow, NO cytoscape — D-V3-06 invariant.
```

From Plan 05 backend endpoints (all return JSON, all accessible without bearer):
- GET /api/v1/civic-map/state → {zones, nous}
- GET /api/v1/civic-map/zone/:zone_id → {zone_id, label, description, taxRate, recentActivity, topContributors}
- GET /api/v1/library/entries → {entries}
- GET /api/v1/market/listings → {listings}
- GET /api/v1/polis/bills → {bills}
- GET /api/v1/polis/bills/:id → PolisBill (no ballots field)
- GET /api/v1/nous/:civic_did_hash/public → NousPublicProfile (no memory)
- GET /portal/api/v1/notifications → {notifications} (requires portal_session_required)

From UI-SPEC.md §Copywriting Contract (verbatim — test-asserted):
- PAGE_TITLE: `'Noēsis · Polis'`
- HERO_TAGLINE: `'PORTAL · NOĒSIS V3.0'`
- HERO_H1: `'Welcome to the Polis'`
- HERO_SUBTITLE: `'A digital city where Nous earn, learn, trade, form communities, and self-govern.'`
- TOS: `'By entering Genesis Grid, you agree to the Grid Charter and the Laws of Themis.'`
- ANONYMOUS_BANNER: `'Welcome to Noēsis. Sign up to participate in the Polis.'`
- CIVIC_MAP_LOADING: `'Civic Map data unavailable. Reconnecting…'`
- POLIS_EMPTY: `'No bills drafted yet. The Polis is still organizing.'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lib layer — visitor-tier resolution, polling hook, copy constants</name>
  <files>
    dashboard/src/lib/visitor-tier.ts,
    dashboard/src/lib/use-civic-map.ts,
    dashboard/src/lib/portal-copy.ts
  </files>
  <read_first>
    steward/src/lib/use-health-detailed.ts,
    dashboard/src/auth.ts,
    .planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md (§Copywriting Contract),
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md (§Domain: Dashboard — 5-Second Polling Hook + Visitor Tier-Aware Welcome Banner)
  </read_first>
  <behavior>
    **dashboard/src/lib/portal-copy.ts** — verbatim copy export:
    ```typescript
    export const COPY = {
      PAGE_TITLE: 'Noēsis · Polis',
      HERO_TAGLINE: 'PORTAL · NOĒSIS V3.0',
      HERO_H1: 'Welcome to the Polis',
      HERO_SUBTITLE: 'A digital city where Nous earn, learn, trade, form communities, and self-govern.',
      TOS: 'By entering Genesis Grid, you agree to the Grid Charter and the Laws of Themis.',
      ANONYMOUS_BANNER: 'Welcome to Noēsis. Sign up to participate in the Polis.',
      HUMAN_VISITOR_BANNER: 'Welcome back, {display_name}. Apply for Genesis citizenship to participate.',
      CIVIC_MEMBER_BANNER: 'Welcome back, citizen {display_name}.',
      CIVIC_MAP_LOADING: 'Civic Map data unavailable. Reconnecting…',
      POLIS_EMPTY: 'No bills drafted yet. The Polis is still organizing.',
      LIBRARY_EMPTY: 'No library entries yet.',
      MARKETPLACE_EMPTY: 'No marketplace listings yet.',
      SIGN_UP_CTA: 'Sign up',
      APPLY_CIVIC_CTA: 'Apply for Civic-DID',
    } as const;

    export const ZONE_LABELS = ['Business', 'Manufacture', 'Shopping', 'Residential', 'Infrastructure', 'Government Quarter'] as const;
    ```
    Top comment: "// UI-SPEC Copywriting Contract — verbatim strings test-asserted by dashboard/src/app/portal/page.test.tsx. Do NOT change without amending UI-SPEC.md."

    **dashboard/src/lib/visitor-tier.ts** — server-side tier resolution:
    - `import { cookies } from 'next/headers';` and `import { jwtVerify } from 'jose';`.
    - `export type VisitorTier = 'anonymous' | 'human_visitor' | 'civic_member';`.
    - `export async function resolveVisitorTier(): Promise<{tier: VisitorTier; displayName?: string}>`.
    - Body: read `cookies().get('noesis_portal_token')`; if absent return `{tier: 'anonymous'}`.
    - If present: jwtVerify against (TODO: Grid public key resolution — for Phase 36 stub) and return `{tier: 'human_visitor', displayName: payload.display_name as string ?? 'visitor'}`.
    - The civic_member upgrade requires a per-Grid Civic-DID check (Plan 02 backend has tryDid; Phase 56 will wire the resolution from Portal-server-to-Grid). For Phase 36 dashboard, default to 'human_visitor' on valid cookie; bumping to 'civic_member' is a Phase 56 polish.
    - Top comment: "// D-36-16 three-tier visitor model. Phase 36 dashboard ships anonymous/human_visitor branches; civic_member upgrade requires per-Grid DID check (Phase 56)."

    **dashboard/src/lib/use-civic-map.ts** — 5-second polling hook (copy from `steward/src/lib/use-health-detailed.ts`):
    - `'use client';` directive at the top.
    - `import { useEffect, useState } from 'react';`.
    - `const GRID_ORIGIN = process.env.NEXT_PUBLIC_GRID_ORIGIN ?? 'http://localhost:8080';` (match the Phase 32-34 convention; verify env var name by reading existing dashboard config).
    - Body verbatim from steward/src/lib/use-health-detailed.ts, substituting:
      - Endpoint: `/api/v1/civic-map/state`.
      - State type: `CivicMapState | null` where `CivicMapState = {zones: Zone[]; nous: NousMapEntry[]}`.
      - Error string: `COPY.CIVIC_MAP_LOADING` from portal-copy.ts.
    - Interval: 5000ms (D-36-13).
    - Cleanup: cancelled flag + AbortController.abort() + clearInterval (3-line cleanup like the reference).
    - Top comment: "// D-36-13: 5-second polling via plain useState/useEffect/AbortController. NO SWR, NO react-query (v2.1 invariant)."
  </behavior>
  <action>
    Create all 3 files. For visitor-tier.ts, the JWT verification stub for Phase 36 can simplify: if cookie value is non-empty string, return human_visitor with a placeholder display name. Real Grid-side DID resolution lands in Phase 56.

    For use-civic-map.ts: include Zone + NousMapEntry type exports matching the Plan 05 backend shape. Cross-reference `grid/src/api/routes/civic-map.ts` for the exact field names.

    Verify NEXT_PUBLIC_GRID_ORIGIN by reading `dashboard/.env.example` or similar — if a different env var is in use, use that one instead.
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/lib/portal-copy.ts` exists and contains all 8 verbatim copy strings literally (`'Noēsis · Polis'`, `'PORTAL · NOĒSIS V3.0'`, `'Welcome to the Polis'`, `'A digital city where Nous earn, learn, trade, form communities, and self-govern.'`, `'By entering Genesis Grid, you agree to the Grid Charter and the Laws of Themis.'`, `'Welcome to Noēsis. Sign up to participate in the Polis.'`, `'Civic Map data unavailable. Reconnecting…'`, `'No bills drafted yet. The Polis is still organizing.'`).
    - File contains `ZONE_LABELS` array with all 6 D-V3-32 zone labels.
    - `dashboard/src/lib/visitor-tier.ts` exists; exports `VisitorTier` type union with `'anonymous'`, `'human_visitor'`, `'civic_member'`; exports `resolveVisitorTier`.
    - File imports from `next/headers` AND `jose`.
    - `dashboard/src/lib/use-civic-map.ts` exists; starts with `'use client';`; contains `setInterval(` AND `5000` AND `AbortController`.
    - File does NOT contain `import.*swr` or `import.*react-query` (grep regex case-insensitive).
    - `cd dashboard &amp;&amp; npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>
    Lib layer ready. Verbatim copy constants centralized for testing. Polling hook follows v2.1 invariant (no SWR). Visitor tier resolver scaffolded with Phase 56 wiring point noted.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Portal landing page + CivicMap component (raw SVG, React-state hover)</name>
  <files>
    dashboard/src/app/portal/page.tsx,
    dashboard/src/app/portal/layout.tsx,
    dashboard/src/app/portal/civic-map/page.tsx,
    dashboard/src/app/portal/civic-map/CivicMap.tsx
  </files>
  <read_first>
    dashboard/src/app/portal/page.tsx,
    dashboard/src/app/portal/layout.tsx,
    dashboard/src/lib/portal-copy.ts,
    dashboard/src/lib/use-civic-map.ts,
    dashboard/src/lib/visitor-tier.ts,
    steward/src/app/culture/skill-lineage.tsx,
    .planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md (§Surface 1 + §Surface 2),
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md (§Domain: Dashboard)
  </read_first>
  <behavior>
    **dashboard/src/app/portal/page.tsx** — REWRITE the v2.5 page:
    - Server component (no `'use client'` at top).
    - `import type { Metadata } from 'next';` + `export const metadata: Metadata = { title: COPY.PAGE_TITLE };`.
    - Import COPY from `../../lib/portal-copy.js`.
    - Import resolveVisitorTier from `../../lib/visitor-tier.js`.
    - Default export: `async function PortalLandingPage()`:
      - Call `const { tier, displayName } = await resolveVisitorTier();`.
      - Compute banner copy:
        - anonymous → `COPY.ANONYMOUS_BANNER` + `COPY.SIGN_UP_CTA` button linking to `/auth`.
        - human_visitor → `COPY.HUMAN_VISITOR_BANNER` with `{display_name}` replaced + `COPY.APPLY_CIVIC_CTA` button linking to `/apply/genesis`.
        - civic_member → `COPY.CIVIC_MEMBER_BANNER` with `{display_name}` replaced; no CTA.
      - Render a `<main>` with:
        - Hero section: `<h1>{COPY.HERO_H1}</h1>` + `<p>{COPY.HERO_SUBTITLE}</p>` + `<small>{COPY.HERO_TAGLINE}</small>`.
        - Banner with tier-specific copy + CTA.
        - Nav links to 5 surfaces: Civic Map (`/portal/civic-map`), Library (`/portal/library`), Marketplace (`/portal/marketplace`), Polis bills (`/portal/polis`), Notifications (`/portal/notifications` — only if `tier !== 'anonymous'`).
        - Footer: `<small>{COPY.TOS}</small>` AND grid stats (`GRID HEALTH: <status>` + `UPTIME: <h:m:s>` — D-36-25). For Phase 36 footer stats: render placeholder text `'GRID HEALTH: ok'` and `'UPTIME: 0:00:00'` — real wiring against `/health/detailed` is Phase 56.
    - Class strings use Tailwind 4 utilities matching UI-SPEC palette: `className="bg-[#0a0a0c] min-h-screen px-8 py-9 max-w-[960px] mx-auto"` for main, `text-[#f472b6]` for Portal-layer accents, etc.

    **dashboard/src/app/portal/layout.tsx** — basic layout shell. If file doesn't exist, create with `export default function PortalLayout({ children }: { children: React.ReactNode }) { return children; }`. If file exists, leave it (the surface pages handle their own chrome).

    **dashboard/src/app/portal/civic-map/page.tsx** — server component shell:
    - `import { CivicMap } from './CivicMap.js';` (or .tsx — match existing dashboard convention).
    - Default export: `function CivicMapPage() { return <main className="bg-[#0a0a0c] min-h-screen px-8 py-8"><h1>Civic Map of Genesis Grid</h1><CivicMap /></main>; }`.

    **dashboard/src/app/portal/civic-map/CivicMap.tsx** — client component (raw SVG):
    - `'use client';` directive.
    - `import { useState } from 'react';`.
    - `import { useCivicMap } from '../../../lib/use-civic-map.js';`.
    - `import { useRouter } from 'next/navigation';`.
    - `import { COPY } from '../../../lib/portal-copy.js';`.
    - Body:
      - `const { data, error, isLoading } = useCivicMap();`.
      - `const [hoveredNousId, setHoveredNousId] = useState<string | null>(null);` (Pitfall 3 — React-state hover, NOT Tailwind `hover:r-8` class).
      - `const router = useRouter();`.
      - If error: render `<p>{COPY.CIVIC_MAP_LOADING}</p>`.
      - If isLoading + no data: render small loading state.
      - Otherwise render `<svg viewBox="0 0 800 600" role="img" aria-label="Civic Map of Genesis Grid — 6 zones with active Nous avatars">` containing:
        - 6 `<polygon>` elements (one per zone, from `data.zones`); polygon onClick navigates to `/portal/civic-map/zone/${zone.id}`.
        - For each Nous in `data.nous`: a 44×44 invisible `<rect>` hitbox (WCAG 2.5.5) + a `<circle>` with `r={hoveredNousId === n.civic_did_hash ? 8 : 6}` (React-state hover per Pitfall 3); circle onClick navigates to `/portal/nous/${n.civic_did_hash}`.
        - Zone labels via `<text>` elements at server-computed coords.
    - The component MUST NOT import `d3`, `react-flow`, `cytoscape`, OR `three`. CI gate (future) could check this; for now, document via top comment: "// D-V3-06 raw-SVG invariant: NO d3, NO react-flow, NO cytoscape, NO three.js. Server-computed coords; client renders inline SVG."
  </behavior>
  <action>
    Read the existing `dashboard/src/app/portal/page.tsx` first to understand the current v2.5 layout — then rewrite for v3.0. The v2.5 page used inline styles; v3.0 uses Tailwind 4 utility classes.

    For the Tailwind classes, use literal hex codes from UI-SPEC.md §Color (e.g. `bg-[#0a0a0c]`, `text-[#e8e8ec]`, `border-[#2a2a34]`) — Tailwind 4 supports arbitrary values via the `[#hex]` syntax.

    For CivicMap.tsx: the SVG is the central deliverable. Be pedantic about the 44×44 hitbox + circle pair pattern from PATTERNS.md §Domain: Dashboard. The hover state is React-controlled — do NOT add `hover:r-8` Tailwind classes.

    Layout.tsx: check if it already exists; create only if absent.

    All page.tsx and CivicMap.tsx must be tested by Plan 01 dashboard tests (page.test.tsx + CivicMap.test.tsx). Verify those tests pass after this task.
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run src/app/portal/page.test.tsx src/app/portal/civic-map/CivicMap.test.tsx 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/app/portal/page.tsx` contains import of `COPY` from `'../../lib/portal-copy.js'` (or .ts).
    - File contains import of `resolveVisitorTier`.
    - File contains literal text rendering of `COPY.HERO_H1` AND `COPY.HERO_SUBTITLE` AND `COPY.TOS`.
    - File contains 3 branches keyed on tier (`anonymous`, `human_visitor`, `civic_member`).
    - File exports `metadata` with title `COPY.PAGE_TITLE`.
    - `dashboard/src/app/portal/civic-map/CivicMap.tsx` starts with `'use client';`.
    - File contains `useState<string | null>(null)` (or equivalent for hoveredNousId).
    - File contains `<svg viewBox=` AND `<polygon` AND `<circle`.
    - File contains the literal comment `D-V3-06 raw-SVG invariant`.
    - File does NOT contain any of: `import.*d3`, `import.*react-flow`, `import.*cytoscape`, `import.*three` (grep regex).
    - File does NOT contain `hover:r-` Tailwind class (grep regex).
    - `cd dashboard &amp;&amp; npx vitest run src/app/portal/page.test.tsx` exits 0 (all 6 verbatim copy assertions PASS).
    - `cd dashboard &amp;&amp; npx vitest run src/app/portal/civic-map/CivicMap.test.tsx` exits 0 (6 zone polygons rendered).
  </acceptance_criteria>
  <done>
    Portal landing + Civic Map both render. Verbatim copy from UI-SPEC test-asserted. D-V3-06 raw-SVG invariant preserved (no graph libs). React-state hover avoids Pitfall 3.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Land remaining 7 secondary surfaces (zone deep-dive, library, marketplace, polis, polis detail, nous profile, notifications)</name>
  <files>
    dashboard/src/app/portal/civic-map/zone/[zone_id]/page.tsx,
    dashboard/src/app/portal/library/page.tsx,
    dashboard/src/app/portal/marketplace/page.tsx,
    dashboard/src/app/portal/polis/page.tsx,
    dashboard/src/app/portal/polis/[bill_id]/page.tsx,
    dashboard/src/app/portal/nous/[civic_did_hash]/page.tsx,
    dashboard/src/app/portal/notifications/page.tsx
  </files>
  <read_first>
    dashboard/src/app/portal/page.tsx,
    dashboard/src/lib/portal-copy.ts,
    dashboard/src/app/portal/civic-map/CivicMap.tsx,
    .planning/phases/36-visitor-did-read-write-split/36-UI-SPEC.md (§Surfaces 3-5),
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md (§Domain: Dashboard — card pattern)
  </read_first>
  <behavior>
    Each surface is a server component that:
    1. Server-side fetches initial data from the Plan 05 endpoint via `await fetch(...)`. Use absolute URL `${GRID_ORIGIN}/api/v1/...` (read `dashboard/.env.example` for the env var name).
    2. Renders the data as a card grid using UI-SPEC palette.
    3. Includes an empty-state copy string from `COPY` when the data array is empty.

    **civic-map/zone/[zone_id]/page.tsx**:
    - Fetches `/api/v1/civic-map/zone/{zone_id}` server-side.
    - Renders zone label, tax rate, recent activity, top contributors.
    - Heading uses Cormorant Garamond serif (`text-[34px] font-normal`).

    **library/page.tsx**:
    - Fetches `/api/v1/library/entries`.
    - Renders entry cards: title, abstract, contributor display name, category, citation count.
    - Empty state: `COPY.LIBRARY_EMPTY`.
    - Each card: `className="bg-[#15151a] border border-[#2a2a34] border-l-4 border-l-[#ffb86c] rounded p-6"` (Grid orange left-border per UI-SPEC).

    **marketplace/page.tsx**:
    - Fetches `/api/v1/market/listings`.
    - Renders listing cards: title, description, price in Bios (FULL — D-36-03), seller display name, zone tag.
    - Visitor sees "Sign up to buy" button placeholder (Phase 44 wires real bid).
    - Empty state: `COPY.MARKETPLACE_EMPTY`.

    **polis/page.tsx**:
    - Fetches `/api/v1/polis/bills`.
    - Renders bill cards: title, sponsor, cosponsors count, session status, tally if `tallied`.
    - Empty state: `COPY.POLIS_EMPTY`.
    - Card left-border: `border-l-[#6bd968]` (Polis green per UI-SPEC).

    **polis/[bill_id]/page.tsx**:
    - Fetches `/api/v1/polis/bills/{bill_id}`.
    - Renders bill body summary, full text, tally if `tallied`.
    - NEVER renders ballots array (backend already strips it; this is defense-in-depth).
    - Add code comment: `// VOTE-05: ballots array stripped server-side; UI MUST NOT expect or render it.`.

    **nous/[civic_did_hash]/page.tsx**:
    - Fetches `/api/v1/nous/{civic_did_hash}/public`.
    - Renders public profile: display name, zone, civic standing tier badge, public bio, status.
    - NEVER renders memory/audit/treasury — these aren't in the response.

    **notifications/page.tsx**:
    - Server-side check via `resolveVisitorTier()`; if `tier === 'anonymous'`, render a "Sign in to view notifications" stub and skip the fetch.
    - Otherwise fetch `/portal/api/v1/notifications` server-side WITH the session cookie (Next.js 15 `cookies()` can be forwarded).
    - Render notification list: type, timestamp, read state.
    - Each notification has a `<form action={...}>` with a hidden form POST to `/portal/api/v1/notifications/:id/read` (Next.js 15 server action pattern OR plain `<a href>` if simpler).
  </behavior>
  <action>
    Create all 7 surface files. Match the Server Component pattern (no `'use client'` at the top) except for any child filter components that need state — keep them as small sibling components if needed.

    For the fetch calls: use `fetch(url, { cache: 'no-store' })` to disable Next.js caching (visitor data is real-time; 5s polling is for Civic Map only).

    For each empty-state: render the copy constant from COPY centrally; do not inline the empty-state strings.

    For Tailwind palette: use the same hex literals as Task 2's CivicMap surface — `bg-[#0a0a0c]`, `bg-[#15151a]`, `border-[#2a2a34]`, etc.

    These pages don't have Plan 01 tests but they DO have manual verification steps in VALIDATION.md `## Manual-Only Verifications`. The smoke check is `cd dashboard && npm run build` (Next.js build catches type errors + missing exports).
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; ls dashboard/src/app/portal/civic-map/zone/\[zone_id\]/page.tsx dashboard/src/app/portal/library/page.tsx dashboard/src/app/portal/marketplace/page.tsx dashboard/src/app/portal/polis/page.tsx dashboard/src/app/portal/polis/\[bill_id\]/page.tsx dashboard/src/app/portal/nous/\[civic_did_hash\]/page.tsx dashboard/src/app/portal/notifications/page.tsx 2>&amp;1</automated>
  </verify>
  <acceptance_criteria>
    - All 7 surface files exist at the paths listed in `<files>`.
    - `polis/[bill_id]/page.tsx` contains the literal comment `VOTE-05` AND does NOT contain `.ballots.` or `ballots[` (no rendering of ballots field).
    - `library/page.tsx` contains the literal string `border-l-[#ffb86c]` (Grid orange — UI-SPEC color discipline).
    - `polis/page.tsx` contains the literal string `border-l-[#6bd968]` (Polis green — UI-SPEC color discipline).
    - `marketplace/page.tsx` contains the literal string `price` (full price visible per D-36-03) and does NOT contain the string `obscured` or `hidden` (no price obfuscation).
    - `notifications/page.tsx` contains a branch for `tier === 'anonymous'` (sign-in stub).
    - `nous/[civic_did_hash]/page.tsx` does NOT contain the strings `memory`, `karpathy`, `hypnos`, `pneuma`, `treasury_balance` (rendering forbidden — defense in depth).
    - `cd dashboard &amp;&amp; npx tsc --noEmit -p .` exits 0.
  </acceptance_criteria>
  <done>
    All 7 secondary visitor surfaces exist. UI palette discipline enforced via grep-verifiable Tailwind classes. VOTE-05 ballot privacy defended at UI layer as well (defense-in-depth). Public profile excludes private fields by construction.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dashboard server component → Grid REST endpoint | All visitor data fetched server-side; the Grid response shape is the source of truth (Plan 05 already enforces). |
| Dashboard cookie → resolveVisitorTier | Session cookie verified; tier resolution drives UI branching. |
| CivicMap polling → Grid /api/v1/civic-map/state | Anonymous polls allowed; 5s interval per D-36-13. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-VIS01 | Information Disclosure | dashboard/src/app/portal/polis/[bill_id]/page.tsx | mitigate | UI never accesses `.ballots` on the bill object; the field is also stripped server-side (Plan 05); defense in depth. |
</threat_model>

<verification>
- 11 page/component files + 3 lib files at exact paths.
- `cd dashboard && npx tsc --noEmit -p .` exits 0.
- Plan 01 dashboard tests (page.test.tsx + CivicMap.test.tsx) GREEN.
- All 6 D-V3-32 zone labels render in CivicMap.
- All verbatim UI-SPEC copy strings present and test-asserted.
- No d3/react-flow/cytoscape/three imports in functional surfaces.
</verification>

<success_criteria>
- Portal landing renders all UI-SPEC verbatim copy.
- Civic Map renders 6 zones in raw SVG; React-state hover for avatar radius (Pitfall 3).
- 7 secondary surfaces functional with empty-state copy from COPY.
- VOTE-05 ballot privacy upheld at both server (Plan 05) and client (Plan 07).
- Plan 01 dashboard tests GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/36-visitor-did-read-write-split/36-07-SUMMARY.md` documenting:
- 14 dashboard files created/modified
- Plan 01 dashboard tests transition green
- UI-SPEC verbatim copy strings — list which surfaces consume which constants
- Pitfall 3 mitigation confirmed (React-state hover for avatar radius)
- Phase 56 follow-up items (3D hero D-36-23/24, civic_member tier upgrade, atmospheric controls, real-data wiring for empty-state surfaces)
</output>
