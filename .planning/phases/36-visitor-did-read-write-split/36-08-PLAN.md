---
phase: 36
plan: 08
type: execute
wave: 3
depends_on: [36-02, 36-03, 36-04, 36-05, 36-06, 36-07]
files_modified:
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-04, VIS-05]
tags: [docs-sync, milestone-log, retrospective-prep, phase-shipped]
threat_refs: []

must_haves:
  truths:
    - "STATE.md current focus + progress block reflects Phase 36 SHIPPED with allowlist count 56 → 60"
    - "MILESTONES.md has a new entry for Phase 36 with shipped date, plan count (8), allowlist delta (+4), and key CI gates added"
    - "ROADMAP.md Phase 36 entry marked complete with checkbox flipped + Plans line populated"
    - "PROJECT.md moves VIS-01..05 from Pending to Validated"
    - "REQUIREMENTS.md Traceability table marks VIS-01..05 Status as Validated"
    - "README.md Current status section reflects Phase 36 shipped if it mentions v3.0 status"
  artifacts:
    - path: .planning/STATE.md
      provides: "Updated progress block + current focus line for post-Phase-36 state"
      contains: "Phase 36"
    - path: .planning/MILESTONES.md
      provides: "Phase 36 shipping entry following the established v3.0 milestone format"
      contains: "Phase 36"
    - path: .planning/ROADMAP.md
      provides: "Phase 36 marked complete; Plans line filled in with 8 plan names; allowlist line updated"
      contains: "8 plans"
  key_links:
    - from: .planning/REQUIREMENTS.md
      to: .planning/STATE.md
      via: "VIS-01..05 status change from Pending to Validated; STATE.md notes the validation event date"
      pattern: "VIS-01.*Validated|Phase 36 SHIPPED"
---

<objective>
Execute the CLAUDE.md Documentation Sync Rule for Phase 36 shipping. Update 6 source-of-truth documents in one coherent commit so the project's working-history representation matches the actual repository state after Phase 36 lands.

Purpose: keep STATE.md, MILESTONES.md, ROADMAP.md, PROJECT.md, REQUIREMENTS.md, and README.md in lockstep with code reality. Future phases (37+) read these files for context; stale docs cause planning drift.

Output: 6 modified markdown files. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/ROADMAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@README.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update STATE.md + ROADMAP.md (mark phase shipped, advance progress)</name>
  <files>
    .planning/STATE.md,
    .planning/ROADMAP.md
  </files>
  <read_first>
    .planning/STATE.md,
    .planning/ROADMAP.md,
    .planning/phases/36-visitor-did-read-write-split/36-01-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-02-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-03-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-04-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-05-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-06-PLAN.md,
    .planning/phases/36-visitor-did-read-write-split/36-07-PLAN.md
  </read_first>
  <behavior>
    **STATE.md updates** (preserving everything else):
    - Frontmatter:
      - `status: ready_to_plan_phase_36` → `status: phase_36_shipped_ready_to_plan_phase_37`.
      - `stopped_at: ...` → `stopped_at: Phase 36 SHIPPED 2026-05-25; allowlist 56 → 60 (+4); 8 plans; visit/action enforcement live with CI gates`.
      - `last_updated: "2026-05-25T19:30:00.000Z"` → bump to current ISO timestamp.
      - `last_activity:` → `Phase 36 (Visitor/DID Read-Write Split) SHIPPED. 8 plans (36-01..36-08). ROUTE_DID_POLICY + WS firehose redaction + 5 sole producers + 4 CI gates + visitor surfaces. R-31-01 zero-diff preserved. Ready for Phase 37 (DID Registry).`
      - progress block: `completed_phases: 0` → `completed_phases: 1`, `total_plans: 0` → `total_plans: 8`, `completed_plans: 0` → `completed_plans: 8`, `percent: 0` → `percent: 4` (1 / 24 phases per current ROADMAP table; recompute from STATE's actual total_phases value at edit time).
    - Body "Current Position" section: rewrite to "Phase: Phase 36 SHIPPED 2026-05-25" + "Plan: 36-01..36-08 all complete" + "Status: Ready to plan Phase 37 (DID Registry)" + brief Next-Action line.
    - Add a new section `### v3.0 Phase 36 close-out (locked 2026-05-25)` mirroring the existing `v2.6 Phase 31 close-out` pattern with these bullets:
      - `Phase 36 SHIPPED. Plans 36-01 through 36-08 all complete. Allowlist 56 → 60 (+4: portal.did_issued, portal.did_revoked, grid.recognition_granted, grid.recognition_revoked). portal.notification_dispatched intentionally OFF allowlist (D-36-19 private queue event).`
      - `Inherits to Phases 37+:` — list 5 carry-forward items (ROUTE_DID_POLICY table now extant; tryDid/requireDid preHandlers wired globally; WsFirehoseHub.onConnect accepts didContext; 4 new CI gates enforced in rig-invariants.yml; visitor read surfaces extant in dashboard/src/app/portal/).
      - `Mitigations carried forward:` — R-31-01 zero-diff preserved (regression test + CI gate); VOTE-05 ballot privacy preserved at server (PUBLIC_KEYS allowlist in polis-bills.ts) and client (no .ballots access); D-36-09 revert-to-visitor encoded in tryDid.
      - `Cross-phase deferred:` — Real Polis data → Phase 46; real Library data → Phase 48; real Marketplace data → Phase 44; Civic Map per-Nous data → Phase 37 (Civic-DID registry); 3D Portal landing hero → Phase 56 (D-36-23/24); civic_member tier upgrade in resolveVisitorTier → Phase 56; per-DID rate limiting → Phase 39.
    - Update the `v3.0 allowlist additions (planned — across 9 phases)` section: Phase 36's `+4 → 60` line annotate ` → SHIPPED 2026-05-25`.

    **ROADMAP.md updates**:
    - Line ~50 (`- [ ] **Phase 36: Visitor/DID Read-Write Split** — ...`): change `- [ ]` → `- [x]` and append ` (SHIPPED 2026-05-25)` at end.
    - Phase 36 entry header (line ~76): no change to Goal / Requirements / Success Criteria.
    - Phase 36 `**Plans**: TBD` (around line 89): replace with:

      ```
      **Plans:** 8 plans
      Plans:
      - [x] 36-01-PLAN.md — Wave 0 validation infrastructure + allowlist lock-in (16 test files; ALLOWLIST count locked to 60)
      - [x] 36-02-PLAN.md — ROUTE_DID_POLICY foundation + tryDid/requireDid preHandlers (VIS-02 + VIS-04)
      - [x] 36-03-PLAN.md — WS firehose per-subscriber redaction (VIS-03; R-31-01 zero-diff preserved)
      - [x] 36-04-PLAN.md — 5 sole-producer audit events + allowlist +4 (VIS-05; notification_dispatched OFF allowlist per D-36-19)
      - [x] 36-05-PLAN.md — 7 visitor read routes + OAuth stubs (x2) + notification queue + 120/min rate limiter (VIS-01 + VIS-02)
      - [x] 36-06-PLAN.md — 4 CI gates (policy coverage, admin isolation, WS redaction zero-diff, OAuth exception count)
      - [x] 36-07-PLAN.md — 5 primary + 2 secondary dashboard visitor surfaces + raw-SVG Civic Map + 3-tier banner (VIS-01)
      - [x] 36-08-PLAN.md — Documentation sync (STATE + MILESTONES + ROADMAP + PROJECT + REQUIREMENTS + README)
      ```
    - Phase 36 `**Allowlist additions**` already reads `**+4**. Running total: **60**.` — leave as-is (matches reality).
  </behavior>
  <action>
    Read both files top-to-bottom. Make surgical edits ONLY at the locations specified above. Do not refactor unrelated sections.

    For STATE.md frontmatter: edit exactly the documented YAML lines, no more.

    For ROADMAP.md Phase 36 entry: replace ONLY the `**Plans**: TBD` line + add the checkbox `[x]` to the wave-1 list entry. Other entries (Phases 37-50) remain untouched.

    Verify after edit by grepping for `Phase 36 SHIPPED`, `[x] **Phase 36`, and `36-08-PLAN.md`.
  </action>
  <verify>
    <automated>grep -c "Phase 36 SHIPPED\|phase_36_shipped" .planning/STATE.md ; grep -c "x. .Phase 36" .planning/ROADMAP.md ; grep -c "36-08-PLAN.md" .planning/ROADMAP.md ; grep -c "8 plans" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` frontmatter contains `status: phase_36_shipped_ready_to_plan_phase_37` AND `completed_phases: 1` AND `completed_plans: 8`.
    - `.planning/STATE.md` body contains the literal section heading `v3.0 Phase 36 close-out (locked 2026-05-25)`.
    - `.planning/STATE.md` mentions all 4 new CI gates by name: `check-did-policy-coverage`, `check-admin-policy-isolation`, `check-ws-redaction-zero-diff`, `check-no-did-exception-count`.
    - `.planning/ROADMAP.md` Phase 36 line contains `[x]` (not `[ ]`) AND the literal text `SHIPPED 2026-05-25`.
    - `.planning/ROADMAP.md` Phase 36 detail section contains `**Plans:** 8 plans` AND lists 36-01 through 36-08 with `[x]` checkboxes.
    - No other Phase entry (37-50) had its checkbox state changed (grep `[x]` count among Phase 37-50 lines stays at 0).
  </acceptance_criteria>
  <done>
    STATE.md and ROADMAP.md reflect Phase 36 shipped state. Close-out section captures the carry-forward items for Phase 37+.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update MILESTONES.md + PROJECT.md + REQUIREMENTS.md (VIS-01..05 to Validated)</name>
  <files>
    .planning/MILESTONES.md,
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    .planning/MILESTONES.md,
    .planning/PROJECT.md,
    .planning/REQUIREMENTS.md
  </read_first>
  <behavior>
    **MILESTONES.md**: append a new entry under the v3.0 section. Format (mirror the most recent shipped phase entry style):

    ```
    ### Phase 36 — Visitor/DID Read-Write Split (SHIPPED 2026-05-25)

    Wave: v3.0 Wave 1 (Foundations)
    Plans shipped: 8 (36-01 through 36-08)
    Requirements validated: VIS-01, VIS-02, VIS-03, VIS-04, VIS-05
    Allowlist delta: +4 (56 -> 60). New events: portal.did_issued, portal.did_revoked, grid.recognition_granted, grid.recognition_revoked. portal.notification_dispatched added to chain but intentionally OFF broadcast allowlist (D-36-19 private queue event).
    CI gates added: 4 — check-did-policy-coverage.mjs, check-admin-policy-isolation.mjs, check-ws-redaction-zero-diff.mjs, check-no-did-exception-count.mjs.
    Critical invariants preserved: R-31-01 zero-diff (chain head hash independent of subscriber composition), VOTE-05 ballot privacy (PUBLIC_KEYS allowlist in polis-bills handler), PORTAL_AUTH_FORBIDDEN_KEYS at 13 keys, Phase 32 frozen contracts (D-32-C1/C2/C3).
    Key artifacts: grid/src/api/policy.ts (ROUTE_DID_POLICY 6-value enum), grid/src/api/preHandlers/{tryDid,requireDid}.ts, grid/src/audit/firehose-redaction.ts, 5 sole-producer files, 7 visitor routes, dashboard/src/app/portal/* (8 page files).
    Decisions locked: D-36-01..25 (25 locked decisions from discuss-phase, including D-36-21 OAuth methods, D-36-22 Grid Charter + Laws of Themis terminology, D-36-23 raw-SVG functional surfaces, D-36-25 visitor footer scoped to GRID HEALTH + UPTIME only).
    Carry-forward for Phase 37: Civic-DID registry will be issued via Portal-gated flow (D-V3-33); the issuance handler MUST call appendPortalDidIssued + appendGridRecognitionGranted (Phase 36 sole producers).
    ```

    Place this entry in chronological/reverse-chronological order matching the file's existing convention. Read the most recent entry first to determine direction.

    **PROJECT.md**: locate the v3.0 milestone block (current milestone is v3.0 Polis per STATE.md). Find the "Validated REQs" subsection (or equivalent — read existing structure). Move `VIS-01, VIS-02, VIS-03, VIS-04, VIS-05` from "Active REQs (pending validation)" to "Validated REQs". If the project uses a different structure, mirror what v2.6 did when a phase shipped.

    **REQUIREMENTS.md Traceability table** (lines 257-271): change 5 lines:
    - `| VIS-01 | 36 | Pending |` → `| VIS-01 | 36 | Validated |`
    - `| VIS-02 | 36 | Pending |` → `| VIS-02 | 36 | Validated |`
    - `| VIS-03 | 36 | Pending |` → `| VIS-03 | 36 | Validated |`
    - `| VIS-04 | 36 | Pending |` → `| VIS-04 | 36 | Validated |`
    - `| VIS-05 | 36 | Pending |` → `| VIS-05 | 36 | Validated |`

    Also change the 5 corresponding checkboxes at the top of REQUIREMENTS.md (`- [ ] **VIS-01**: ...` → `- [x] **VIS-01**: ...`) — 5 boxes flip in the "VIS — Visitor/DID Read-Write Split" section near the top (lines ~16-20).
  </behavior>
  <action>
    Read each file's current shape before editing. For PROJECT.md, the "Validated REQs" section format may differ from what's described — adapt to match the existing structure (do not invent a new format).

    For REQUIREMENTS.md: the 5 traceability rows are at known positions; the 5 checkboxes are in the VIS section near the top.

    Be careful with the alignment of pipe-delimited tables in markdown — preserve column widths.

    For MILESTONES.md: search for the heading pattern matching the most recent shipped entry (likely starts with `### Phase 35` or similar v2.6 entry). Insert the new Phase 36 entry above the older Phase 35 entry to maintain reverse-chronological order if that's the file's convention; otherwise append at the end of the v3.0 section.
  </action>
  <verify>
    <automated>grep -c "Phase 36 .. Visitor/DID Read-Write Split" .planning/MILESTONES.md ; grep -c "VIS-0. . 36 . Validated" .planning/REQUIREMENTS.md ; grep -c "x. ..VIS-0" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/MILESTONES.md` contains the literal heading text `Phase 36` AND `SHIPPED 2026-05-25` AND `+4 (56` AND `R-31-01 zero-diff`.
    - `.planning/REQUIREMENTS.md` Traceability table contains 5 rows where VIS-01 through VIS-05 status field is `Validated` (not `Pending`).
    - `.planning/REQUIREMENTS.md` "VIS — Visitor/DID Read-Write Split" section has 5 checkboxes `[x]` (not `[ ]`) on the VIS-01..05 list.
    - `.planning/PROJECT.md` contains a section listing VIS-01..05 as Validated (exact format depends on existing PROJECT.md structure).
  </acceptance_criteria>
  <done>
    MILESTONES.md, PROJECT.md, REQUIREMENTS.md all reflect Phase 36 validated state. 5 REQ statuses flipped to Validated. 5 checkboxes flipped to checked.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update README.md current-status section</name>
  <files>
    README.md
  </files>
  <read_first>
    README.md,
    .planning/STATE.md,
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    Read README.md to find any "Current status" or "v3.0 progress" section. If present:
    - Update the v3.0 progress indicator to reflect 1 / 24 phases complete (or current denominator from ROADMAP).
    - Add a bullet noting Phase 36 (Visitor/DID Read-Write Split) shipped 2026-05-25 with: visitor can browse without DID, all writes require Civic-DID, WS firehose per-subscriber redaction, 4 CI gates enforce the invariants.

    If README has NO current-status section: append a one-line "v3.0 Phase 36 (Visitor/DID Read-Write Split) shipped 2026-05-25" at the appropriate location (likely near the top under a "Status" header OR in a "What's new" section). If neither exists, create a minimal `## v3.0 Status` H2 section near the top with the bullet.

    Do NOT change project description, feature lists, or installation instructions.
  </behavior>
  <action>
    Read README.md first. Identify the most appropriate location for the Phase 36 status update without disrupting the document's overall structure. Make a single surgical edit.
  </action>
  <verify>
    <automated>grep -c "Phase 36\|v3.0 Phase 36" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `README.md` contains the literal text `Phase 36` OR `v3.0 Phase 36`.
    - README.md contains a reference to `2026-05-25` near the Phase 36 mention (shipping date).
    - No other README section was structurally rewritten (only the targeted status update).
  </acceptance_criteria>
  <done>
    README.md reflects Phase 36 as the most recently shipped phase. Project description and feature lists untouched.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Code reality → planning docs | Documentation MUST reflect code; staleness causes Phase 37+ planning drift. |
| STATE.md → next-phase orchestrator | `/gsd-plan-phase 37` reads STATE.md to understand current position; outdated state misdirects planning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| (none) | — | — | accept | This is documentation work; no code paths, no user-facing surfaces. Risk is planning drift if forgotten — mitigated by listing this plan as Wave 3 mandatory before phase close. |
</threat_model>

<verification>
- 6 markdown files modified.
- All checkboxes/status fields flipped per acceptance criteria.
- No code files touched.
- Pre-existing content preserved (only surgical edits at known locations).
</verification>

<success_criteria>
- STATE.md, MILESTONES.md, ROADMAP.md, PROJECT.md, REQUIREMENTS.md, README.md all consistent with Phase 36 shipped reality.
- VIS-01..05 marked Validated in REQUIREMENTS.md.
- Phase 36 listed in MILESTONES.md with full close-out details.
- README mentions Phase 36 shipped 2026-05-25.
</success_criteria>

<output>
After completion, create `.planning/phases/36-visitor-did-read-write-split/36-08-SUMMARY.md` documenting:
- 6 docs synced
- Phase 36 officially shipped marker date
- Carry-forward items for Phase 37 surfaced in STATE.md close-out section
- Total Phase 36 plan count, allowlist count, CI gate count
</output>
