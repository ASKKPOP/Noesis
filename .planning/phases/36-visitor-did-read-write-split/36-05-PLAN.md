---
phase: 36
plan: 05
type: execute
wave: 1
depends_on: [36-01, 36-02, 36-04]
files_modified:
  - grid/src/api/routes/civic-map.ts
  - grid/src/api/routes/civic-map-zone.ts
  - grid/src/api/routes/library-entries.ts
  - grid/src/api/routes/market-listings.ts
  - grid/src/api/routes/polis-bills.ts
  - grid/src/api/routes/nous-public-profile.ts
  - grid/src/api/routes/visitor-audit-trail.ts
  - grid/src/api/portal/auth.ts
  - grid/src/api/portal/oauth-stub.ts
  - grid/src/api/portal/notifications.ts
  - grid/src/api/rate-limit/visitor-bucket.ts
  - grid/src/api/server.ts
autonomous: true
requirements: [VIS-01, VIS-02, VIS-03, VIS-05]
tags: [visitor-routes, auth-endpoints, oauth-stubs, notification-queue, rate-limit]
threat_refs: [T-36-VIS01, T-36-VIS02, T-36-VOTE, T-36-EXC]

must_haves:
  truths:
    - "7 visitor read routes return 200 for anonymous requests with the contracted body shapes (civic-map, library, marketplace, polis-bills, nous public profile, audit trail, civic-map zone)"
    - "Polis bill endpoint returns tally counts but never ballots array (VOTE-05 ballot privacy)"
    - "5 portal auth endpoints exist: SIWE + email/signup + email/signin + oauth/google (stub 501) + oauth/apple (stub 501)"
    - "Notification queue GET + POST/:id/read endpoints require Portal session (D-36-19); use the new sole producer to emit portal.notification_dispatched on enqueue"
    - "Visitor rate limiter caps at 120 req/min/IP with 429 + Retry-After header (D-36-05/07)"
  artifacts:
    - path: grid/src/api/routes/civic-map.ts
      provides: "GET /api/v1/civic-map/state returning {zones, nous} for visitors"
      contains: "registerCivicMapRoute"
      min_lines: 40
    - path: grid/src/api/routes/polis-bills.ts
      provides: "GET /api/v1/polis/bills + /:id with ballots stripped at handler level (VOTE-05)"
      contains: "tally"
      min_lines: 50
    - path: grid/src/api/portal/oauth-stub.ts
      provides: "POST /portal/auth/oauth/google + /apple stub routes returning 501"
      contains: "coming_soon"
      min_lines: 25
    - path: grid/src/api/portal/notifications.ts
      provides: "Portal notification queue REST endpoints; uses appendPortalNotificationDispatched"
      contains: "appendPortalNotificationDispatched"
      min_lines: 50
    - path: grid/src/api/rate-limit/visitor-bucket.ts
      provides: "In-process IP sliding-window bucket (120/min) emits 429 + Retry-After"
      contains: "120"
      min_lines: 40
  key_links:
    - from: grid/src/api/server.ts
      to: grid/src/api/rate-limit/visitor-bucket.ts
      via: "buildServerWithHub registers visitor-bucket onRequest hook BEFORE the global preHandler"
      pattern: "registerVisitorRateLimit"
    - from: grid/src/api/portal/notifications.ts
      to: grid/src/audit/append-portal-notification-dispatched.ts
      via: "POST /portal/api/v1/notifications/:id/read calls appendPortalNotificationDispatched on dispatch step"
      pattern: "appendPortalNotificationDispatched"
    - from: grid/src/api/routes/polis-bills.ts
      to: grid/src/api/routes/polis-bills.ts (internal handler)
      via: "handler returns {tally:{pass,fail,abstain}}; ballots array absent from response shape"
      pattern: "ballots|tally"
---

<objective>
Land the visitor-facing read routes, the 2 new OAuth-stub auth endpoints (D-36-21 + Pitfall 4 count drift fix), the Portal notification queue (D-36-19), and the visitor rate limiter (D-36-05/07). This is the request-handling layer above Plan 02's policy/preHandler foundation.

Purpose: turns the remaining Plan 01 RED tests GREEN — visitor-public-routes, visitor-audit-redaction, did-revoked-behavior (read side), polis-bills-privacy, policy-coverage (route registration side). Without this plan, Plan 02 has nothing to enforce and Plan 07 (dashboard) has no backend to call.

Output: 7 new route files + 1 modification to portal/auth.ts (to register the oauth-stub) + 1 new oauth-stub + 1 new notifications route + 1 new rate-limit module + 1 modification to server.ts wiring everything.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-visitor-did-read-write-split/36-CONTEXT.md
@.planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md
@.planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md

<interfaces>
<!-- Plan 02 + 04 exports the new routes use; do not discover. -->

From grid/src/api/routes/health-detailed.ts (canonical route registration shape):
```typescript
export function registerHealthDetailedRoute(
  app: FastifyInstance,
  services: GridServices,
  launcher: GenesisLauncher,
): void {
  app.get('/health/detailed', async (_req, reply) => { /* ... */ });
}
```

From Plan 02:
```typescript
// grid/src/api/preHandlers/tryDid.ts
export async function tryDid(req, services?): Promise<DIDContext | null>;
// (req.didContext is populated by the global preHandler from Plan 02)
```

From Plan 04:
```typescript
// grid/src/audit/append-portal-notification-dispatched.ts
export interface PortalNotificationDispatchedPayload {
  readonly dispatched_at_tick: number;
  readonly notification_type: string;
  readonly operator_did: string;
}
export function appendPortalNotificationDispatched(audit: AuditChain, payload: PortalNotificationDispatchedPayload): AuditEntry;
```

From grid/src/api/portal/auth.ts (canonical auth route registration — uses registerPortalAuthRoutes export):
```typescript
export function registerPortalAuthRoutes(app: FastifyInstance, services: GridServices): void;
// Currently registers: POST /portal/auth/siwe + /portal/auth/email/signup + /portal/auth/email/signin
// Plus session check + cookie clearing.
```

From grid/src/api/server.ts:
```typescript
// Look for the registerPortalAuthRoutes call in buildServerWithHub (around line 480-490).
// Visitor read routes register near other GET routes (around line 296-580).
// Rate-limit hook registers FIRST (before any other preHandler).
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create visitor read routes (civic-map, library, marketplace, polis-bills, public profile, audit trail) + wire into server.ts</name>
  <files>
    grid/src/api/routes/civic-map.ts,
    grid/src/api/routes/civic-map-zone.ts,
    grid/src/api/routes/library-entries.ts,
    grid/src/api/routes/market-listings.ts,
    grid/src/api/routes/polis-bills.ts,
    grid/src/api/routes/nous-public-profile.ts,
    grid/src/api/routes/visitor-audit-trail.ts,
    grid/src/api/server.ts
  </files>
  <read_first>
    grid/src/api/routes/health-detailed.ts,
    grid/src/api/routes/humans.ts,
    grid/src/api/routes/lore.ts,
    grid/src/api/routes/audit-firehose.ts,
    grid/src/api/server.ts,
    grid/src/api/policy.ts,
    grid/src/api/preHandlers/tryDid.ts,
    grid/test/api/visitor-public-routes.test.ts,
    grid/test/api/visitor-audit-redaction.test.ts,
    grid/test/api/polis-bills-privacy.test.ts,
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md
  </read_first>
  <behavior>
    Per route file:

    **registerCivicMapRoute** (`grid/src/api/routes/civic-map.ts`):
    - `app.get('/api/v1/civic-map/state', ...)` returns `{zones: Zone[], nous: NousMapEntry[]}`.
    - `Zone` shape: `{id: string; label: string; polygon: string; labelX: number; labelY: number; fillColor: string; strokeColor: string; taxRate: number}` (server-computed coordinates per D-V3-06 + Phase 21 raw-SVG invariant).
    - `NousMapEntry` shape: `{civic_did_hash: string; x: number; y: number; display_name: string; type: 'A'|'B'; status: 'online'|'away'; zone_label: string}`.
    - For v3.0 Phase 36 with no real data yet: return a deterministic 6-zone stub layout (D-V3-32) hard-coded inside the handler — exact zone labels `'Business'`, `'Manufacture'`, `'Shopping'`, `'Residential'`, `'Infrastructure'`, `'Government Quarter'`. Polygon coordinates: simple 2×3 grid divided across a 800×600 viewBox. Empty `nous: []` array is acceptable for Phase 36; Phase 37 wires real Civic-DID census.
    - Top-of-handler comment: `// ROUTE_DID_POLICY: 'public' — listed in grid/src/api/policy.ts`.

    **registerCivicMapZoneRoute** (`grid/src/api/routes/civic-map-zone.ts`):
    - `app.get<{Params:{zone_id: string}}>('/api/v1/civic-map/zone/:zone_id', ...)` returns `{zone_id, label, description, taxRate, recentActivity: AuditEvent[], topContributors: TopContributor[]}`.
    - For v3.0 Phase 36 stub: lookup zone_id against the 6 hard-coded zones from civic-map.ts; return empty `recentActivity` and `topContributors` arrays. Real data layer is Phase 57 ZONE-*.

    **registerLibraryEntriesRoute** (`grid/src/api/routes/library-entries.ts`):
    - `app.get('/api/v1/library/entries', ...)` returns `{entries: LibraryEntry[]}`.
    - LibraryEntry shape: `{id, title, abstract, contributor_civic_did_hash, category, tick_contributed, citation_count}` — no `body` field for visitor view (full body via Phase 48 endpoint requires expansion).
    - For Phase 36: if `services.loreStorage` exists, query it; else return `{entries: []}` stub.

    **registerMarketListingsRoute** (`grid/src/api/routes/market-listings.ts`):
    - `app.get('/api/v1/market/listings', ...)` returns `{listings: MarketListing[]}`.
    - MarketListing shape: `{id, title, description, price_bios, seller_business_did_hash, seller_display_name, category, zone_tag, expires_at_tick}`.
    - Full price visible to visitors per D-36-03.
    - For Phase 36: stub `{listings: []}` (Phase 44 lands real data).

    **registerPolisBillsRoute** (`grid/src/api/routes/polis-bills.ts`) — CRITICAL VOTE-05 enforcement:
    - `app.get('/api/v1/polis/bills', ...)` returns `{bills: PolisBill[]}`.
    - `app.get<{Params:{id:string}}>('/api/v1/polis/bills/:id', ...)` returns `PolisBill` (single).
    - `PolisBill` shape EXPOSED to visitors: `{id, title, body_summary, sponsor_civic_did_hash, sponsor_display_name, cosponsors_count, session_status: 'drafting'|'debate'|'voted'|'tallied', scheduled_open_tick?, tally?: {pass: number, fail: number, abstain: number}}`.
    - `tally` only appears AFTER session_status === 'tallied'.
    - **NEVER** include `ballots`, `voter_did`, or any field whose name contains 'ballot' or 'vote' in the response. Use an explicit allowlist of output keys in the handler — `const PUBLIC_KEYS = ['id', 'title', 'body_summary', 'sponsor_civic_did_hash', 'sponsor_display_name', 'cosponsors_count', 'session_status', 'scheduled_open_tick', 'tally'] as const;` and reconstruct response from those keys only. Top-of-handler comment: `// VOTE-05 invariant: ballots array MUST NOT appear in visitor response (D-36-15). Reconstruct from PUBLIC_KEYS allowlist.`
    - For Phase 36: return `{bills: []}` stub (Phase 46 lands real Polis data).

    **registerNousPublicProfileRoute** (`grid/src/api/routes/nous-public-profile.ts`):
    - `app.get<{Params:{civic_did_hash:string}}>('/api/v1/nous/:civic_did_hash/public', ...)` returns `NousPublicProfile`.
    - Shape (D-36-11): `{civic_did_hash, display_name, zone, civic_standing_tier: 'provisional'|'full', public_bio, status: 'online'|'away'}`.
    - MUST NOT include: `memory`, `karpathy`, `hypnos`, `pneuma`, `treasury_balance`, `active_contracts`, `audit_history`.
    - Comment: `// D-36-11: public profile excludes memory, audit history, brain config, treasury balance, contracts.`
    - For Phase 36: return a stub profile (Phase 37 lands real Nous registry).

    **registerVisitorAuditTrailRoute** (`grid/src/api/routes/visitor-audit-trail.ts`):
    - `app.get<{Querystring:{limit?:string}}>('/api/v1/audit/trail', ...)` returns `{entries: VisitorAuditEntry[]}`.
    - When `req.didContext?.tier === 'civic_member'`: return full entries (`{tick, event_type, actor_did, payload}`).
    - When tier is null OR not civic_member: return entries with `actor_did` replaced by family prefix (`actor_did.split(':')[1]` — e.g. `'civic'` for `'did:civic:noesis:gen-001'`; for `'did:noesis:human:0xabc'` → `'noesis'`; per supplement §4 "family prefix"). Wait — for VIS-01 family prefix per supplement actually corresponds to `event_type.split('.')[0]` (e.g. `'nous'` for `nous.spoke`). The visitor-audit-redaction test in Plan 01 uses `event_type='nous.spoke'` and expects family `'nous'` in `actor_did`. Use the EVENT FAMILY (event_type split on first `.`) for the redacted `actor_did`. Replace `actor_did` field value with the family string; payload key set replaced with the empty object `{}`.
    - Cap at last 1000 events per D-36-06; default limit 100, max 1000.
    - Top-of-handler comment: `// D-V3-12 + D-36-06: visitor sees last 1000 events sliding window; actor_did replaced by family prefix; payload redacted.`

    **Wire all 7 into `grid/src/api/server.ts`**:
    - Add import lines for each `register*Route` function (after the existing `registerHealthDetailedRoute` import block).
    - Add 7 registration calls inside `buildServerWithHub` AFTER `registerHumansRoutes(app, services)` (around line 487) but BEFORE the existing `app.get('/api/v1/governance/laws', ...)` block (so route registration order is consistent with HTTP method/path locality).
    - Order: civic-map, civic-map-zone, library-entries, market-listings, polis-bills, nous-public-profile, visitor-audit-trail.
  </behavior>
  <action>
    Create 7 route files. Each file's top exports a single `register<Name>Route(app, services)` function matching the canonical shape from `health-detailed.ts`.

    For stub data: hard-code minimal valid shapes inside the handler — Phase 36 ships the contract; Phases 37-48 wire real data sources. This is intentional and documented in the route file headers as "PHASE-36 STUB — Phase NN wires real <subsystem>".

    For visitor-audit-trail: query `services.audit` chain entries via the existing accessor (read `grid/src/audit/chain.ts` for the method name — likely `entries()` or `getRange(start, end)`). If no entries available, return `{entries: []}`. Cap defensively: `Math.min(parseInt(req.query.limit ?? '100', 10), 1000)`.

    For polis-bills: the response-shape reconstruction is the critical security check. Do NOT spread the source bill object; explicitly pick the 9 PUBLIC_KEYS fields.

    Update server.ts: add 7 imports at the top + 7 registration calls in buildServerWithHub.

    The ROUTE_DID_POLICY table in policy.ts already lists all 7 of these routes (added in Plan 02 Task 1) — verify no drift between policy.ts and the actual registered route paths. If any path differs, FIX the route path to match policy.ts (policy is the source of truth).
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run test/api/visitor-public-routes.test.ts test/api/visitor-audit-redaction.test.ts test/api/polis-bills-privacy.test.ts test/api/did-revoked-behavior.test.ts test/api/policy-coverage.test.ts 2>&amp;1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - All 7 route files exist at the paths listed in `<files>`.
    - Each route file exports a `register*Route` function.
    - `grid/src/api/server.ts` contains 7 new import lines for the new register functions AND 7 new call lines inside buildServerWithHub.
    - polis-bills.ts contains the literal constant `PUBLIC_KEYS` AND a comment containing `VOTE-05`.
    - polis-bills.ts does NOT contain the literal string `ballots` outside of comments (grep with context: the word `ballots` should appear ONLY in comments, never in response object construction).
    - nous-public-profile.ts contains the literal comment string `D-36-11: public profile excludes memory`.
    - civic-map.ts contains all 6 D-V3-32 zone labels literally: `'Business'`, `'Manufacture'`, `'Shopping'`, `'Residential'`, `'Infrastructure'`, `'Government Quarter'`.
    - visitor-audit-trail.ts contains the literal comment `D-V3-12 + D-36-06: visitor sees last 1000 events`.
    - `cd grid &amp;&amp; npx vitest run test/api/visitor-public-routes.test.ts` exits 0 (all 5 surfaces return 200).
    - `cd grid &amp;&amp; npx vitest run test/api/visitor-audit-redaction.test.ts` exits 0 (family prefix substitution works).
    - `cd grid &amp;&amp; npx vitest run test/api/polis-bills-privacy.test.ts` exits 0 (ballots field absent).
    - `cd grid &amp;&amp; npx vitest run test/api/did-revoked-behavior.test.ts` exits 0 (read = 200, write = 401 both pass now).
  </acceptance_criteria>
  <done>
    7 visitor read routes registered. VOTE-05 ballot privacy enforced via explicit PUBLIC_KEYS allowlist. Visitor audit trail returns family-prefix-redacted actor_did. did-revoked-behavior fully passes (read side).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add OAuth stub routes + extend registerPortalAuthRoutes</name>
  <files>
    grid/src/api/portal/oauth-stub.ts,
    grid/src/api/portal/auth.ts
  </files>
  <read_first>
    grid/src/api/portal/auth.ts,
    grid/src/api/policy.ts,
    grid/test/api/did-required-enforcement.test.ts (the OAuth stub assertion)
  </read_first>
  <behavior>
    `grid/src/api/portal/oauth-stub.ts` exports `registerOAuthStubRoutes(app: FastifyInstance): void`. Body:
    - `app.post('/portal/auth/oauth/google', async (_req, reply) => reply.code(501).send({status: 'coming_soon', provider: 'google', planned_phase: '52-54'}));`
    - `app.post('/portal/auth/oauth/apple', async (_req, reply) => reply.code(501).send({status: 'coming_soon', provider: 'apple', planned_phase: '52-54'}));`
    - Top-of-file comment: "// Phase 36 / D-36-21 — OAuth stubs return 501 Not Implemented. Full PKCE flow lands in Phase 52-54 Portal Infrastructure. CI gate `scripts/check-no-did-exception-count.mjs` (Plan 06) asserts count = 5 no-DID auth exceptions (was 3 pre-v3.0)."

    `grid/src/api/portal/auth.ts` — extend `registerPortalAuthRoutes`:
    - Add `import { registerOAuthStubRoutes } from './oauth-stub.js';` at the top.
    - Inside `registerPortalAuthRoutes`, after the existing SIWE/email route registrations, add the line `registerOAuthStubRoutes(app);`.
    - No other changes to existing SIWE or email flows.
  </behavior>
  <action>
    Create the stub file (small — under 30 lines). Surgically edit auth.ts to add 1 import + 1 call. Do not touch any existing route handler in auth.ts.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run test/api/did-required-enforcement.test.ts 2>&amp;1 | tail -15 ; echo '---curl---' ; echo 'oauth stub structure verified at compile time'</automated>
  </verify>
  <acceptance_criteria>
    - `grid/src/api/portal/oauth-stub.ts` exists; exports `registerOAuthStubRoutes`.
    - File contains both route paths literally: `'/portal/auth/oauth/google'` AND `'/portal/auth/oauth/apple'`.
    - File contains `501` AND `'coming_soon'` AND `'planned_phase'`.
    - File contains the comment string `D-36-21` AND `Phase 52-54`.
    - `grid/src/api/portal/auth.ts` contains `import { registerOAuthStubRoutes } from './oauth-stub.js';`.
    - File contains the line `registerOAuthStubRoutes(app);` inside `registerPortalAuthRoutes`.
    - `cd grid &amp;&amp; npx vitest run test/api/did-required-enforcement.test.ts` — the it() block asserting `POST /portal/auth/oauth/google` returns 501 PASSES.
  </acceptance_criteria>
  <done>
    OAuth stub endpoints land 2 new public no-DID exceptions, bringing the count to 5. Real PKCE flow deferred to Phase 52-54.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create Portal notification queue + visitor rate limiter + wire into server.ts</name>
  <files>
    grid/src/api/portal/notifications.ts,
    grid/src/api/rate-limit/visitor-bucket.ts,
    grid/src/api/server.ts
  </files>
  <read_first>
    grid/src/api/portal/auth.ts,
    grid/src/api/portal/check-frozen.ts,
    grid/src/audit/append-portal-notification-dispatched.ts,
    grid/src/api/server.ts,
    grid/test/api/whisper-fastify-rate-limit.test.ts
  </read_first>
  <behavior>
    **Portal notifications** (`grid/src/api/portal/notifications.ts`) exports `registerPortalNotificationsRoutes(app, services)`:
    - `GET /portal/api/v1/notifications` — requires Portal session (policy already set to `portal_session_required` by Plan 02). Returns `{notifications: Notification[]}`.
    - `POST /portal/api/v1/notifications/:id/read` — marks the notification read.
    - In-process Map for the queue: `Map<operatorDid: string, Notification[]>`. Stub for Phase 36 — Phase 56 lands MySQL persistence.
    - `Notification` shape: `{id: string, type: string, body_hash: string, dispatched_at_tick: number, read_at_tick?: number}`.
    - On enqueue (the enqueue endpoint is NOT exposed in Phase 36 — internal helper only): call `appendPortalNotificationDispatched(services.audit, {dispatched_at_tick: services.clock.state.tick, notification_type: type, operator_did: operatorDid})`.
    - Export an internal helper `enqueueNotification(services, operatorDid, type, bodyHash)` for use by other phases (Phase 56 will call from the soft-interaction handlers).
    - Top-of-file comment: "// Phase 36 / D-36-19 — Portal notification queue (private, per-operator). Server-pushed-via-poll (NOT WS). Uses portal.notification_dispatched audit event (NOT on broadcast allowlist)."

    **Visitor rate limiter** (`grid/src/api/rate-limit/visitor-bucket.ts`) exports `registerVisitorRateLimit(app: FastifyInstance): void`:
    - In-process `Map<ip, {count: number, windowStart: number}>`.
    - Window = 60000ms (1 min). Max = 120 requests (D-36-05).
    - On `onRequest` hook (BEFORE preHandler — runs earlier in lifecycle):
      - Skip rate-limit if `req.didContext?.tier === 'civic_member'` (DID-holders get per-DID buckets later in Phase 39).
      - Actually `req.didContext` is set in preHandler, not onRequest. So rate-limit applies to ALL requests at v3.0 single-server scale; Phase 39 will refactor.
      - For now: read `req.ip`; check bucket; if count exceeds 120 within the window, `reply.header('Retry-After', String(secondsRemaining))` and `reply.code(429).send({error: 'rate_limit_exceeded', retry_after: secondsRemaining})`.
    - Top comment: "// Phase 36 / D-36-05 + D-36-07 — visitor rate-limit 120 req/min per IP. In-process Map; single-server v3.0 scope. Phase 39 will refactor to per-DID buckets + multi-instance backend."

    **Wire both into `grid/src/api/server.ts`**:
    - Imports: `import { registerVisitorRateLimit } from './rate-limit/visitor-bucket.js';` and `import { registerPortalNotificationsRoutes } from './portal/notifications.js';`.
    - Inside `buildServerWithHub`, register the rate limiter as the FIRST hook (before `registerFrozenCheck` and before the global preHandler from Plan 02). This ensures rate-limit short-circuit happens before any other work.
    - Register notifications routes after `registerPortalAuthRoutes(app, services)`.
  </behavior>
  <action>
    Create the 2 new files. For the rate-limit Map, include a periodic cleanup or rely on lazy eviction (delete bucket entries older than 2 windows on any new check). For simplicity at v3.0: lazy eviction inside the hook is acceptable; document as "TODO Phase 39: real per-DID buckets".

    For notifications.ts: keep the in-process Map module-scoped (not service-scoped). Export `enqueueNotification` helper.

    Update server.ts: add 2 imports + 2 registration calls. Order matters — rate limiter FIRST.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run test/api/ 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grid/src/api/portal/notifications.ts` exists; exports `registerPortalNotificationsRoutes` AND `enqueueNotification`.
    - File contains both route paths: `'/portal/api/v1/notifications'` AND `'/portal/api/v1/notifications/:id/read'`.
    - File imports `appendPortalNotificationDispatched` and calls it inside `enqueueNotification`.
    - File contains the comment `D-36-19` AND `NOT on broadcast allowlist`.
    - `grid/src/api/rate-limit/visitor-bucket.ts` exists; exports `registerVisitorRateLimit`.
    - File contains the integer literal `120` AND the string `'Retry-After'` AND status code `429`.
    - File contains the comment `D-36-05` AND `D-36-07`.
    - `grid/src/api/server.ts` contains both new imports AND both registration calls.
    - Rate limiter hook is registered BEFORE `registerFrozenCheck` (registration order verifiable by reading the function body sequentially).
    - `cd grid &amp;&amp; npx tsc --noEmit -p .` exits 0.
    - All Plan 01 grid tests under `test/api/` and `test/audit/` PASS (full Wave 0 → green transition).
  </acceptance_criteria>
  <done>
    Notification queue functional (in-process Map; Phase 56 wires MySQL). Rate limiter active at 120 req/min/IP with 429 + Retry-After. All Plan 01 grid tests now GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Visitor HTTP request → rate limiter | IP bucket; first short-circuit on the request lifecycle. |
| Visitor request → polis-bills handler | Explicit PUBLIC_KEYS allowlist on output ensures ballots field cannot leak. |
| Visitor request → audit trail handler | Family-prefix substitution + payload stripping at handler output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-VIS01 | Information Disclosure | grid/src/api/routes/visitor-audit-trail.ts | mitigate | Handler replaces actor_did with event-family prefix; payload object replaced with empty {}. Cap at 1000 events. |
| T-36-VIS02 | Elevation of Privilege | grid/src/api/portal/oauth-stub.ts | mitigate | Stub returns 501; cannot create operator-DID until Phase 52-54 lands real PKCE. CI gate (Plan 06) enforces exactly 5 no-DID exceptions. |
| T-36-VOTE | Information Disclosure | grid/src/api/routes/polis-bills.ts | mitigate | Explicit PUBLIC_KEYS allowlist; response reconstructed from those 9 keys; `ballots` never copied. Tested via polis-bills-privacy.test.ts. |
| T-36-EXC | Tampering | OAuth exception count drift | mitigate | Stub registration adds exactly 2 routes; Plan 06 CI gate asserts count = 5. |
</threat_model>

<verification>
- 7 visitor route files + 2 OAuth stubs + 1 notifications + 1 rate-limit + server.ts wiring.
- All Plan 01 grid tests (audit-layer + api-layer) GREEN.
- `cd grid && npx tsc --noEmit -p .` exits 0.
- Pre-existing tests still GREEN (no regression in humans, governance, audit-firehose, server.cors).
</verification>

<success_criteria>
- 7 visitor routes return contracted shapes for anonymous requests.
- Polis bills response shape has no `ballots` field for visitor calls.
- OAuth stubs return 501 with `coming_soon` body.
- Notification queue REST endpoints registered; enqueue helper emits the new audit event.
- Rate limiter blocks at 120 req/min/IP with 429 + Retry-After.
- Plan 01 RED tests turn fully GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/36-visitor-did-read-write-split/36-05-SUMMARY.md` documenting:
- 11 files touched
- Plan 01 test transition green/total counts
- Stub-vs-real boundaries noted (which routes return stubs for Phase 36; which phases land real data)
- OAuth stub count = 5 confirmed (matches D-V3-15 amended count for Plan 06 CI gate)
</output>
