---
phase: 36
plan: 04
type: execute
wave: 1
depends_on: [36-01]
files_modified:
  - grid/src/audit/append-portal-did-issued.ts
  - grid/src/audit/append-portal-did-revoked.ts
  - grid/src/audit/append-grid-recognition-granted.ts
  - grid/src/audit/append-grid-recognition-revoked.ts
  - grid/src/audit/append-portal-notification-dispatched.ts
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/audit/portal-auth-forbidden-keys.ts
autonomous: true
requirements: [VIS-05]
tags: [audit-producers, allowlist-extension, sole-producer-discipline, forbidden-keys]
threat_refs: [T-36-VIS05, T-36-AL]

must_haves:
  truths:
    - "5 new sole-producer functions exist; each throws TypeError on every documented bad-payload shape; each commits the expected event type on valid payloads"
    - "ALLOWLIST_MEMBERS length is exactly 60 (56 + 4 VIS-05 events; portal.notification_dispatched intentionally OFF allowlist per D-36-19)"
    - "PORTAL_AUTH_FORBIDDEN_KEYS preserved at 13 keys; no Phase 36 producer payload contains any forbidden key"
    - "All 5 producers use the 8-step Phase 33 sole-producer triad (type guard → DID_RE guard → integer/string guards → closed-tuple structural check → explicit reconstruction → payloadPrivacyCheck → audit.append)"
  artifacts:
    - path: grid/src/audit/append-portal-did-issued.ts
      provides: "Sole producer for portal.did_issued event (Phase 36 VIS-05)"
      contains: "payload.human_or_nous_did"
      min_lines: 80
    - path: grid/src/audit/append-portal-did-revoked.ts
      provides: "Sole producer for portal.did_revoked"
      min_lines: 80
    - path: grid/src/audit/append-grid-recognition-granted.ts
      provides: "Sole producer for grid.recognition_granted"
      min_lines: 80
    - path: grid/src/audit/append-grid-recognition-revoked.ts
      provides: "Sole producer for grid.recognition_revoked"
      min_lines: 80
    - path: grid/src/audit/append-portal-notification-dispatched.ts
      provides: "Sole producer for portal.notification_dispatched (NOT on broadcast allowlist)"
      min_lines: 80
    - path: grid/src/audit/broadcast-allowlist.ts
      provides: "ALLOWLIST_MEMBERS extended +4 (56 → 60); explicit comment that notification_dispatched is intentionally excluded"
      contains: "portal.did_issued"
  key_links:
    - from: grid/src/audit/append-portal-did-issued.ts
      to: grid/src/audit/broadcast-allowlist.ts
      via: "imports payloadPrivacyCheck; commits event type that is present in ALLOWLIST_MEMBERS"
      pattern: "payloadPrivacyCheck"
    - from: grid/src/audit/append-portal-notification-dispatched.ts
      to: grid/src/audit/broadcast-allowlist.ts
      via: "imports payloadPrivacyCheck but commits event NOT in ALLOWLIST_MEMBERS — chain entry only, no WS broadcast"
      pattern: "notification_dispatched"
---

<objective>
Implement the 5 new sole-producer files for Phase 36 audit events and extend ALLOWLIST_MEMBERS by exactly 4 entries (notification_dispatched intentionally OFF allowlist per D-36-19). Each producer follows the canonical Phase 33 8-step triad pattern from `append-portal-auth-login.ts` verbatim.

Purpose: VIS-05 fulfillment. Plan 01 sole-producer tests (`append-portal-did-issued.test.ts` through `append-portal-notification-dispatched.test.ts`) and the broadcast-allowlist.test.ts updated count assertion all turn GREEN. Plan 05's auth + notification endpoints will call these producers.

Output: 5 new sole-producer files + 1 modification to broadcast-allowlist.ts + 1 minor extension to portal-auth-forbidden-keys.ts (none of the new payloads contain forbidden keys, but adding the comment that they were reviewed under the existing 13-key contract).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/36-visitor-did-read-write-split/36-CONTEXT.md
@.planning/phases/36-visitor-did-read-write-split/36-RESEARCH.md
@.planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md

<interfaces>
<!-- Canonical sole-producer reference; copy this exact structure 5 times with payload substitutions. -->

From grid/src/audit/append-portal-auth-login.ts:
```typescript
import type { AuditChain } from './chain.js';
import type { AuditEntry } from './types.js';
import { payloadPrivacyCheck } from './broadcast-allowlist.js';
import { DID_RE } from './append-human-joined.js';

export const LOGIN_METHOD_ENUM = ['email', 'siwe'] as const;
export type LoginMethod = typeof LOGIN_METHOD_ENUM[number];

export interface PortalAuthLoginPayload {
  readonly human_did: string;
  readonly method: LoginMethod;
  readonly tick: number;
}
const EXPECTED_KEYS = ['human_did', 'method', 'tick'] as const;

export function appendPortalAuthLogin(audit: AuditChain, payload: PortalAuthLoginPayload): AuditEntry {
  // 1. Type guard (plain non-null non-array object)
  // 2. DID_RE guard (human_did)
  // 3. Closed-enum guard (method) — if applicable
  // 4. Non-negative integer guard (tick)
  // 5. Closed-tuple structural check (Object.keys.sort === EXPECTED_KEYS)
  // 6. Explicit reconstruction (no spread)
  // 7. payloadPrivacyCheck(cleanPayload)
  // 8. audit.append(eventType, actorDid, cleanPayload)
}
```

From grid/src/audit/broadcast-allowlist.ts (current state):
```typescript
export const ALLOWLIST_MEMBERS: readonly string[] = [
  // ... 56 entries currently ...
];
export const ALLOWLIST: ReadonlySet<string> = buildFrozenAllowlist(ALLOWLIST_MEMBERS);
// Phase 36 will append 4 new entries (positions 57-60) and DO NOT add portal.notification_dispatched.
```

From grid/src/audit/portal-auth-forbidden-keys.ts (Phase 33 — preserved):
```typescript
export const PORTAL_AUTH_FORBIDDEN_KEYS: ReadonlySet<string>; // 13 keys
export const FORBIDDEN_KEY_PATTERN: RegExp;
// payloadPrivacyCheck in broadcast-allowlist.ts uses these.
```

Payload contracts (alphabetical key order) for the 5 new events:

| File | Event Type | Keys (alphabetical) | Actor field | Allowlist? |
|------|------------|---------------------|-------------|------------|
| append-portal-did-issued.ts | portal.did_issued | human_or_nous_did, issued_at_tick, issuer_portal_id | human_or_nous_did | YES (57) |
| append-portal-did-revoked.ts | portal.did_revoked | human_or_nous_did, revoked_at_tick, revoker_portal_id | human_or_nous_did | YES (58) |
| append-grid-recognition-granted.ts | grid.recognition_granted | grid_name, granted_at_tick, nous_did | nous_did | YES (59) |
| append-grid-recognition-revoked.ts | grid.recognition_revoked | grid_name, nous_did, revoked_at_tick | nous_did | YES (60) |
| append-portal-notification-dispatched.ts | portal.notification_dispatched | dispatched_at_tick, notification_type, operator_did | operator_did | NO (private queue event per D-36-19) |
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create 4 broadcast-allowlisted sole producers (did_issued, did_revoked, recognition_granted, recognition_revoked)</name>
  <files>
    grid/src/audit/append-portal-did-issued.ts,
    grid/src/audit/append-portal-did-revoked.ts,
    grid/src/audit/append-grid-recognition-granted.ts,
    grid/src/audit/append-grid-recognition-revoked.ts
  </files>
  <read_first>
    grid/src/audit/append-portal-auth-login.ts,
    grid/src/audit/append-portal-auth-register.ts,
    grid/src/audit/append-human-joined.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/test/audit/append-portal-did-issued.test.ts,
    grid/test/audit/append-portal-did-revoked.test.ts,
    grid/test/audit/append-grid-recognition-granted.test.ts,
    grid/test/audit/append-grid-recognition-revoked.test.ts,
    .planning/phases/36-visitor-did-read-write-split/36-PATTERNS.md
  </read_first>
  <behavior>
    For each of the 4 files, copy the structure of `append-portal-auth-login.ts` EXACTLY, substituting the payload contract:

    **append-portal-did-issued.ts** (event `portal.did_issued`):
    - `export interface PortalDidIssuedPayload { readonly human_or_nous_did: string; readonly issued_at_tick: number; readonly issuer_portal_id: string }`
    - `const EXPECTED_KEYS = ['human_or_nous_did', 'issued_at_tick', 'issuer_portal_id'] as const;` (alphabetical)
    - Function: `export function appendPortalDidIssued(audit: AuditChain, payload: PortalDidIssuedPayload): AuditEntry`
    - Triad steps in order: 1) plain object guard, 2) DID_RE on human_or_nous_did, 3) (no enum — skip step 3), 4) non-negative integer on issued_at_tick, 4b) non-empty string on issuer_portal_id, 5) Object.keys.sort === EXPECTED_KEYS, 6) explicit reconstruct `{human_or_nous_did, issued_at_tick, issuer_portal_id}`, 7) payloadPrivacyCheck, 8) `return audit.append('portal.did_issued', payload.human_or_nous_did, cleanPayload)`.

    **append-portal-did-revoked.ts** (event `portal.did_revoked`):
    - Same shape; substitute keys `[human_or_nous_did, revoked_at_tick, revoker_portal_id]`.

    **append-grid-recognition-granted.ts** (event `grid.recognition_granted`):
    - Keys `[grid_name, granted_at_tick, nous_did]`. Actor = `nous_did`. Guards: DID_RE on nous_did, non-empty string on grid_name, non-negative integer on granted_at_tick.

    **append-grid-recognition-revoked.ts** (event `grid.recognition_revoked`):
    - Keys `[grid_name, nous_did, revoked_at_tick]`. Actor = `nous_did`. Guards: DID_RE on nous_did, non-empty string on grid_name, non-negative integer on revoked_at_tick.

    Each file MUST include a header docblock referencing: "Phase 36 / VIS-05 — sole producer for `<event.type>`. Allowlist position: 57/58/59/60 (Phase 36 D-36-17). Closed-tuple payload; PORTAL_AUTH_FORBIDDEN_KEYS preserved. Tested via grid/test/audit/append-<file>.test.ts."

    NO step may be skipped. NO spread operator allowed. NO mutation of input `payload`.
  </behavior>
  <action>
    Copy `grid/src/audit/append-portal-auth-login.ts` 4 times. For each copy:
    1. Rename the file.
    2. Replace `LOGIN_METHOD_ENUM` and `LoginMethod` blocks with the new payload interface (no enum for these 4).
    3. Replace `EXPECTED_KEYS` array with the alphabetical key tuple for this event.
    4. Replace function name `appendPortalAuthLogin` with the new name.
    5. Replace TypeError message prefixes from `'appendPortalAuthLogin:'` to the new function name (5 prefixes per file).
    6. Replace the closed-enum guard step (step 3) with the appropriate guard for this event's third typed field (string or integer).
    7. Replace the explicit reconstruction object with the new field set.
    8. Replace the `audit.append('portal.auth.login', payload.human_did, cleanPayload)` call with the new event type + actor field.

    Verify each file is between 80-120 lines (matches reference shape).
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run test/audit/append-portal-did-issued.test.ts test/audit/append-portal-did-revoked.test.ts test/audit/append-grid-recognition-granted.test.ts test/audit/append-grid-recognition-revoked.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 4 files exist at the paths in `<files>`.
    - Each file imports `payloadPrivacyCheck` from `./broadcast-allowlist.js` AND `DID_RE` from `./append-human-joined.js`.
    - Each file contains its respective `EXPECTED_KEYS` array with the exact alphabetical key tuple specified in the interfaces table.
    - Each file calls `audit.append(` with the exact event type string: `'portal.did_issued'`, `'portal.did_revoked'`, `'grid.recognition_granted'`, `'grid.recognition_revoked'` respectively.
    - Each file contains the literal string `payloadPrivacyCheck(cleanPayload)` (step 7).
    - Each file contains NO `...payload` spread operator (grep regex `\.\.\.payload`).
    - `cd grid &amp;&amp; npx tsc --noEmit -p .` exits 0.
    - Tests `append-portal-did-issued.test.ts`, `append-portal-did-revoked.test.ts`, `append-grid-recognition-granted.test.ts`, `append-grid-recognition-revoked.test.ts` all GREEN after Task 2 lands the allowlist update (Task 1 alone may fail the allowlist assertion in these tests; that resolves with Task 2).
  </acceptance_criteria>
  <done>
    All 4 broadcast-allowlisted producers exist and follow the 8-step triad verbatim. TypeScript compiles. Producer-side tests pass once Task 2 also lands.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create notification-dispatched producer + extend ALLOWLIST_MEMBERS +4 (NOT +5)</name>
  <files>
    grid/src/audit/append-portal-notification-dispatched.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/src/audit/portal-auth-forbidden-keys.ts
  </files>
  <read_first>
    grid/src/audit/append-portal-auth-login.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/src/audit/portal-auth-forbidden-keys.ts,
    grid/test/audit/append-portal-notification-dispatched.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts,
    .planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md
  </read_first>
  <behavior>
    **append-portal-notification-dispatched.ts** — same 8-step triad shape as Task 1 producers, with:
    - Keys `[dispatched_at_tick, notification_type, operator_did]` (alphabetical).
    - Actor = `operator_did`. Guard: DID_RE on operator_did.
    - `notification_type`: non-empty string (later phase may convert to closed enum — for v3.0 plan keep as string per CONTEXT D-36-19 Claude's-discretion).
    - `dispatched_at_tick`: non-negative integer.
    - Event type string: `'portal.notification_dispatched'`.

    The header docblock for this file MUST explicitly state: "PRIVATE QUEUE EVENT — NOT on broadcast allowlist per D-36-19. This event enters the audit chain (via audit.append) but is NOT forwarded to WS subscribers; it is delivered to the operator's Portal notification queue (Plan 05) via REST poll. See `.planning/phases/36-visitor-did-read-write-split/36-VALIDATION.md` Wave 0 Allowlist-Count Decision."

    **broadcast-allowlist.ts** — extend ALLOWLIST_MEMBERS:
    - Locate the current end of `ALLOWLIST_MEMBERS` (after line 56 entry `'human.identified'`).
    - Add a Phase 36 comment block (mirror the Phase 33 OBS-08/09 comment style around lines 199-204):
      ```
      // Phase 36 (VIS-05 / D-36-17) — Portal DID lifecycle + Grid recognition events. Allowlist 56 → 60.
      // portal.did_issued: closed 3-key {human_or_nous_did, issued_at_tick, issuer_portal_id}.
      //   Emitted ONLY via appendPortalDidIssued (grid/src/audit/append-portal-did-issued.ts).
      // portal.did_revoked: closed 3-key {human_or_nous_did, revoked_at_tick, revoker_portal_id}.
      //   Emitted ONLY via appendPortalDidRevoked (grid/src/audit/append-portal-did-revoked.ts).
      // grid.recognition_granted: closed 3-key {grid_name, granted_at_tick, nous_did}.
      //   Emitted ONLY via appendGridRecognitionGranted (grid/src/audit/append-grid-recognition-granted.ts).
      // grid.recognition_revoked: closed 3-key {grid_name, nous_did, revoked_at_tick}.
      //   Emitted ONLY via appendGridRecognitionRevoked (grid/src/audit/append-grid-recognition-revoked.ts).
      // NOTE: portal.notification_dispatched (D-36-19) is intentionally NOT here — private personal-queue event,
      // delivered via REST poll, not WS broadcast. See 36-VALIDATION.md Wave 0 Allowlist-Count Decision (60, NOT 61).
      ```
    - Append 4 new entries (in this order):
      ```
      'portal.did_issued',       // (57)
      'portal.did_revoked',      // (58)
      'grid.recognition_granted', // (59)
      'grid.recognition_revoked', // (60)
      ```

    **portal-auth-forbidden-keys.ts** — review only:
    - DO NOT add or remove keys (the 13-key set is frozen per D-33-B3).
    - Add a top-of-file comment: "// Phase 36 review (2026-05-25): all 5 new VIS-05 producer payloads reviewed; none contain forbidden keys. 13-key set preserved."
  </behavior>
  <action>
    Create `append-portal-notification-dispatched.ts` using the same template as Task 1 producers.

    For broadcast-allowlist.ts: locate the line `'human.identified'` (allowlist position 56). Insert the comment block AFTER that line, then the 4 new array entries. The order matters: TypeScript's `as const` infers tuple positions; new entries go at the END to preserve historical position numbers.

    Be careful with array trailing commas — match the existing style (current file uses trailing commas).

    For portal-auth-forbidden-keys.ts: add ONLY the comment line at the top of the existing exports.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit -p . 2>&amp;1 | tail -10 ; echo '---' ; npx vitest run test/audit/broadcast-allowlist.test.ts test/audit/append-portal-notification-dispatched.test.ts test/audit/append-portal-did-issued.test.ts test/audit/append-portal-did-revoked.test.ts test/audit/append-grid-recognition-granted.test.ts test/audit/append-grid-recognition-revoked.test.ts 2>&amp;1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `grid/src/audit/append-portal-notification-dispatched.ts` exists; contains literal event string `'portal.notification_dispatched'` AND the comment string `PRIVATE QUEUE EVENT — NOT on broadcast allowlist`.
    - `grid/src/audit/broadcast-allowlist.ts` contains exactly 4 new array entries: `'portal.did_issued'`, `'portal.did_revoked'`, `'grid.recognition_granted'`, `'grid.recognition_revoked'`.
    - File does NOT contain `'portal.notification_dispatched'` in the ALLOWLIST_MEMBERS array (grep with context confirms it appears only in the comment block, not as an array entry).
    - File contains the comment block referencing D-36-17 and "60, NOT 61".
    - `grid/src/audit/portal-auth-forbidden-keys.ts` contains the Phase 36 review comment.
    - `cd grid &amp;&amp; npx vitest run test/audit/broadcast-allowlist.test.ts` exits 0 (count assertion 60 PASSES; new it() blocks for VIS-05 inclusion + notification exclusion PASS).
    - `cd grid &amp;&amp; npx vitest run test/audit/append-portal-notification-dispatched.test.ts` exits 0 (producer tests PASS; the it() block checking `ALLOWLIST_MEMBERS.includes(...) === false` PASSES).
    - All 5 producer tests GREEN: `append-portal-did-issued`, `append-portal-did-revoked`, `append-grid-recognition-granted`, `append-grid-recognition-revoked`, `append-portal-notification-dispatched`.
  </acceptance_criteria>
  <done>
    All 5 sole-producer files exist. ALLOWLIST_MEMBERS extended by exactly +4 (56 → 60). notification_dispatched intentionally OFF the allowlist. PORTAL_AUTH_FORBIDDEN_KEYS preserved at 13 keys (review comment added). All 5 producer tests + the allowlist test GREEN.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Caller → sole producer → audit chain | Producer is the boundary where untrusted payload structure is rejected; only the canonical cleanPayload reaches audit.append. |
| ALLOWLIST_MEMBERS → WS broadcast | Membership in ALLOWLIST determines whether the event crosses the wire to subscribers; notification_dispatched intentionally excluded. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-VIS05 | Tampering | grid/src/audit/append-*.ts (5 new) | mitigate | 8-step triad enforced per file: type guard, DID_RE, integer guards, closed-tuple structural check, explicit reconstruction (no spread), payloadPrivacyCheck, audit.append. Tested per-file via Plan 01 Wave 0 tests. |
| T-36-AL | Tampering | grid/src/audit/broadcast-allowlist.ts ALLOWLIST_MEMBERS array | mitigate | Comment block explicitly documents +4 (NOT +5); notification_dispatched intentional omission cross-referenced to VALIDATION.md. Allowlist test asserts length === 60. |
</threat_model>

<verification>
- 5 new producer files at exact paths.
- broadcast-allowlist.ts ALLOWLIST_MEMBERS length is exactly 60 (verified by `grep -c` on array entries).
- portal-auth-forbidden-keys.ts still has exactly 13 keys (no addition).
- All 6 audit-layer Plan 01 tests GREEN.
- `cd grid && npx tsc --noEmit -p .` exits 0.
</verification>

<success_criteria>
- 5 sole-producer files, each 80-120 lines, following the 8-step triad verbatim.
- ALLOWLIST_MEMBERS = exactly 60 entries.
- notification_dispatched present in audit chain via producer but absent from broadcast allowlist.
- All 6 audit-layer Plan 01 tests turn GREEN.
- No regression in pre-existing producer tests.
</success_criteria>

<output>
After completion, create `.planning/phases/36-visitor-did-read-write-split/36-04-SUMMARY.md` documenting:
- 5 producer files created; allowlist count 56 → 60
- Why notification_dispatched is OFF the allowlist (D-36-19 reasoning, cross-link to Wave 0 decision)
- PORTAL_AUTH_FORBIDDEN_KEYS still at 13 keys (no contract change)
- Tests turned GREEN by this plan
</output>
