---
phase: 20-lore-commons
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/lore/types.ts
  - grid/test/lore/lore-allowlist-baseline.test.ts
  - grid/test/lore/lore-producer-boundary.test.ts
  - grid/test/lore/appendLoreContributed.test.ts
  - grid/test/lore/appendLoreCited.test.ts
  - grid/test/lore/lore-migration.test.ts
  - grid/test/lore/lore-citation-listener.test.ts
autonomous: true
requirements: [LORE-01, LORE-02]

must_haves:
  truths:
    - "FORBIDDEN_KEY_PATTERN extended with lore_body|lore_content|title_text|summary_text before any lore emitter lands"
    - "LORE_FORBIDDEN_KEYS constant exported from broadcast-allowlist.ts (4 keys)"
    - "ALLOWLIST_MEMBERS.length === 41 asserted by lore-allowlist-baseline.test.ts (Wave 0 RED gate passes)"
    - "grid/src/lore/types.ts exists with LoreContributedPayload, LoreCitedPayload, LORE_CONTRIBUTED_KEYS, LORE_CITED_KEYS, DEFAULT_LORE_CATEGORIES, VALID_LORE_CATEGORIES"
    - "lore-producer-boundary.test.ts exists as RED stub (sole-producer grep gate)"
    - "appendLoreContributed.test.ts and appendLoreCited.test.ts exist as RED stubs"
    - "lore-migration.test.ts exists as RED stub for version 8 SQL inspection"
    - "lore-citation-listener.test.ts exists as RED stub"
  artifacts:
    - path: "grid/src/audit/broadcast-allowlist.ts"
      provides: "LORE_FORBIDDEN_KEYS constant + FORBIDDEN_KEY_PATTERN extended"
      contains: "LORE_FORBIDDEN_KEYS"
    - path: "grid/src/lore/types.ts"
      provides: "LoreContributedPayload, LoreCitedPayload, LORE_CONTRIBUTED_KEYS, LORE_CITED_KEYS, DEFAULT_LORE_CATEGORIES, VALID_LORE_CATEGORIES"
      exports: ["LoreContributedPayload", "LoreCitedPayload", "LORE_CONTRIBUTED_KEYS", "LORE_CITED_KEYS", "DEFAULT_LORE_CATEGORIES", "VALID_LORE_CATEGORIES"]
    - path: "grid/test/lore/lore-allowlist-baseline.test.ts"
      provides: "Wave 0 baseline assertion: length === 41, position 40 === norm.crystallized"
    - path: "grid/test/lore/lore-producer-boundary.test.ts"
      provides: "Sole-producer grep gate for lore.contributed and lore.cited"
    - path: "grid/test/lore/appendLoreContributed.test.ts"
      provides: "RED stub for 10-step validation ladder"
    - path: "grid/test/lore/appendLoreCited.test.ts"
      provides: "RED stub for 10-step validation ladder"
    - path: "grid/test/lore/lore-migration.test.ts"
      provides: "RED stub for version 8 migration SQL inspection"
    - path: "grid/test/lore/lore-citation-listener.test.ts"
      provides: "RED stub for LoreCitationListener zero-append invariant"
  key_links:
    - from: "grid/src/audit/broadcast-allowlist.ts"
      to: "payloadPrivacyCheck"
      via: "FORBIDDEN_KEY_PATTERN regex covers lore_body|lore_content|title_text|summary_text"
      pattern: "lore_body|lore_content"
    - from: "grid/src/lore/types.ts"
      to: "grid/src/lore/appendLoreContributed.ts"
      via: "LORE_CONTRIBUTED_KEYS closed-tuple import"
      pattern: "LORE_CONTRIBUTED_KEYS"
---

<objective>
Wave 0 safety gate for Phase 20. Extends FORBIDDEN_KEY_PATTERN before any lore emitter code lands. Creates the types contract and all RED test stubs that subsequent waves must make green.

Purpose: Establish the privacy guardrail and type contracts that sole-producer emitters (Plans 03–04) must satisfy. Every test stub created here will fail until the implementation lands — this is the RED gate that enforces correct ordering.

Output: Modified broadcast-allowlist.ts, new grid/src/lore/types.ts, six RED test stubs in grid/test/lore/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-lore-commons/20-CONTEXT.md
@.planning/phases/20-lore-commons/20-RESEARCH.md
@.planning/phases/20-lore-commons/20-PATTERNS.md

<interfaces>
<!-- Existing constants to mirror exactly — extracted from codebase. -->

From grid/src/audit/broadcast-allowlist.ts (RESEARCH.md §1):
```typescript
// Current FORBIDDEN_KEY_PATTERN ends at (line ~382):
// |norm_text|fingerprint_text|rule_content/i
// Extension: append |lore_body|lore_content|title_text|summary_text before closing /i

// SKILL_FORBIDDEN_KEYS pattern (lines 301-305):
export const SKILL_FORBIDDEN_KEYS = Object.freeze([
    'skill_body', 'skill_text', 'rule_text',
] as const);

// NORM_FORBIDDEN_KEYS pattern (lines 314-318):
export const NORM_FORBIDDEN_KEYS = Object.freeze([
    'norm_text', 'fingerprint_text', 'rule_content',
] as const);

// WHISPER_FORBIDDEN_KEYS (lines 335-349) holds field names (NOT message prefixes).
// Do NOT add '__lore_request:' or '__lore_response:' as array entries.
// Document in JSDoc only.

// ALLOWLIST_MEMBERS ends at (line ~168):
'norm.crystallized',  // (41)
// Next entries go here (Wave 3 only — not in this plan)
```

From grid/test/audit/skill-allowlist-baseline.test.ts (RESEARCH.md §10):
```typescript
import { describe, it, expect } from 'vitest';
import { ALLOWLIST_MEMBERS } from '../../src/audit/broadcast-allowlist.js';
describe('Phase N allowlist baseline', () => {
    it('allowlist has exactly N events before additions', () => {
        expect((ALLOWLIST_MEMBERS as readonly string[]).length).toBe(N);
    });
    it('position N is last_event_name', () => {
        expect((ALLOWLIST_MEMBERS as readonly string[])[N-1]).toBe('last_event_name');
    });
});
```

From grid/test/norms/norm-producer-boundary.test.ts (RESEARCH.md §11):
```typescript
const SOLE_EMITTERS: Record<string, string> = {
    'norm.candidate': 'norms/appendNormCandidate.ts',
    'norm.crystallized': 'norms/appendNormCrystallized.ts',
};
// Walk grid/src; assert each event string appears ONLY in:
//   1. audit/broadcast-allowlist.ts
//   2. The sole emitter file listed above
// AND assert no other file calls audit.append('{event}', ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend broadcast-allowlist.ts with LORE_FORBIDDEN_KEYS and types contract</name>
  <files>grid/src/audit/broadcast-allowlist.ts, grid/src/lore/types.ts</files>
  <read_first>
    - grid/src/audit/broadcast-allowlist.ts (read full file to find SKILL_FORBIDDEN_KEYS block, NORM_FORBIDDEN_KEYS block, FORBIDDEN_KEY_PATTERN line, WHISPER_FORBIDDEN_KEYS JSDoc, and ALLOWLIST_MEMBERS array end)
    - grid/src/skills/types.ts (read to see the exact locked-key-tuple pattern to mirror for lore types)
    - grid/src/norms/types.ts (read to see DEFAULT_LORE_CATEGORIES / VALID_LORE_CATEGORIES injection pattern)
  </read_first>
  <action>
**In grid/src/audit/broadcast-allowlist.ts:**

1. After the NORM_FORBIDDEN_KEYS block (which ends with `] as const);`), insert the LORE_FORBIDDEN_KEYS constant with this exact text:
```typescript
/**
 * Phase 20 (LORE-01 / D-20-13): lore-leaf keys that MUST NOT appear in any
 * broadcast payload. Lore body text and title text are Brain-private and NEVER
 * cross the Brain↔Grid wire. Only content_hash (64-char hex) is permitted.
 * Per D-20-13 — exactly 4 keys. Do NOT add extras without a CONTEXT.md decision.
 *
 * NOTE: __lore_request: and __lore_response: are Brain-internal whisper prefixes;
 * they are never payload field names and are NOT added to WHISPER_FORBIDDEN_KEYS.
 * The Brain-side on_message() prefix check is the guard for lore plaintext. (RESEARCH.md §8)
 */
export const LORE_FORBIDDEN_KEYS = Object.freeze([
    'lore_body',
    'lore_content',
    'title_text',
    'summary_text',
] as const);
```

2. In the FORBIDDEN_KEY_PATTERN regex, append `|lore_body|lore_content|title_text|summary_text` immediately before the closing `/i`. The current tail is `|norm_text|fingerprint_text|rule_content`. The new tail must be `|norm_text|fingerprint_text|rule_content|lore_body|lore_content|title_text|summary_text`. Never replace the whole regex — append only to avoid breaking prior Phase entries.

3. Update the WHISPER_FORBIDDEN_KEYS JSDoc (above the constant declaration) to add a note: "Phase 20: lore whispers use __lore_request: and __lore_response: Brain-internal prefixes. These prefixes are NOT field names and are not in this array. See RESEARCH.md §8 and D-20-13."

4. Do NOT add lore.contributed or lore.cited to ALLOWLIST_MEMBERS in this task — that is Wave 3 (Plan 04). The ALLOWLIST_MEMBERS must remain at exactly 41 entries so the baseline test passes.

**Create grid/src/lore/types.ts** with the following content (verbatim — these are the locked tuples from D-20-12):

```typescript
/**
 * Lore Grid types — Phase 20 D-20-12.
 * Payload interfaces and EXPECTED_KEYS tuples for lore.contributed + lore.cited sole-producer emitters.
 *
 * 3-keys-not-5 invariant: Brain metadata carries 1-2 keys;
 * Grid injects contributor_did/citing_did + tick at emit time.
 *
 * Closed-tuple: EXPECTED_KEYS are alphabetically sorted to match Object.keys(payload).sort().
 */

export interface LoreContributedPayload {
    category_tag: string;    // alphabetical — locked by D-20-12
    content_hash: string;
    contributor_did: string;
    tick: number;
}

export interface LoreCitedPayload {
    citing_did: string;      // alphabetical — locked by D-20-12
    content_hash: string;
    tick: number;
}

/** Alphabetically sorted key tuples — locked by D-20-12. */
export const LORE_CONTRIBUTED_KEYS = [
    'category_tag', 'content_hash', 'contributor_did', 'tick',
] as const;

export const LORE_CITED_KEYS = [
    'citing_did', 'content_hash', 'tick',
] as const;

/** Default valid category tags — D-20-04. */
export const DEFAULT_LORE_CATEGORIES = new Set([
    'cultural',
    'historical',
    'observation',
    'synthesis',
] as const);

/**
 * Mutable at startup — GenesisLauncher overwrites from TOML lore_categories key (D-20-03).
 * Grid validates category_tag against this set at the appendLoreContributed boundary.
 */
export let VALID_LORE_CATEGORIES: Set<string> = new Set(DEFAULT_LORE_CATEGORIES);
```

Also create `grid/src/lore/index.ts` as a barrel export:
```typescript
export { appendLoreContributed } from './appendLoreContributed.js';
export { appendLoreCited } from './appendLoreCited.js';
export { LoreCitationListener } from './LoreCitationListener.js';
export { LoreCommonsListener } from './LoreCommonsListener.js';
export { LoreStorage } from './LoreStorage.js';
export * from './types.js';
```
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noesis/grid && grep -c "LORE_FORBIDDEN_KEYS" src/audit/broadcast-allowlist.ts && grep "lore_body|lore_content|title_text|summary_text" src/audit/broadcast-allowlist.ts && grep "LORE_CONTRIBUTED_KEYS" src/lore/types.ts && grep "LORE_CITED_KEYS" src/lore/types.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "LORE_FORBIDDEN_KEYS" grid/src/audit/broadcast-allowlist.ts` returns at least 2 lines (declaration + export)
    - `grep "lore_body" grid/src/audit/broadcast-allowlist.ts` returns a line inside the FORBIDDEN_KEY_PATTERN regex
    - `grep "lore_content" grid/src/audit/broadcast-allowlist.ts` returns a line inside the FORBIDDEN_KEY_PATTERN regex
    - `grep "title_text" grid/src/audit/broadcast-allowlist.ts` returns a line inside the FORBIDDEN_KEY_PATTERN regex (NOT as a new ALLOWLIST entry)
    - `grep "summary_text" grid/src/audit/broadcast-allowlist.ts` returns a line inside the FORBIDDEN_KEY_PATTERN regex
    - `grep "__lore_request" grid/src/audit/broadcast-allowlist.ts` finds only JSDoc comment text — NOT an array entry in WHISPER_FORBIDDEN_KEYS
    - `grep "lore.contributed\|lore.cited" grid/src/audit/broadcast-allowlist.ts` returns empty (not yet added to ALLOWLIST_MEMBERS)
    - `cat grid/src/lore/types.ts` contains all 6 exports: LoreContributedPayload, LoreCitedPayload, LORE_CONTRIBUTED_KEYS, LORE_CITED_KEYS, DEFAULT_LORE_CATEGORIES, VALID_LORE_CATEGORIES
    - LORE_CONTRIBUTED_KEYS array elements in alphabetical order: category_tag, content_hash, contributor_did, tick
    - LORE_CITED_KEYS array elements in alphabetical order: citing_did, content_hash, tick
  </acceptance_criteria>
  <done>LORE_FORBIDDEN_KEYS exported, FORBIDDEN_KEY_PATTERN extended with 4 lore keys, grid/src/lore/types.ts created with all required exports, barrel index.ts created.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED test stubs (Wave 0 gate tests)</name>
  <files>
    grid/test/lore/lore-allowlist-baseline.test.ts,
    grid/test/lore/lore-producer-boundary.test.ts,
    grid/test/lore/appendLoreContributed.test.ts,
    grid/test/lore/appendLoreCited.test.ts,
    grid/test/lore/lore-migration.test.ts,
    grid/test/lore/lore-citation-listener.test.ts
  </files>
  <read_first>
    - grid/test/audit/skill-allowlist-baseline.test.ts (baseline assertion exact template)
    - grid/test/norms/norm-producer-boundary.test.ts (grep gate exact template)
    - grid/test/norms/norm-migration.test.ts (migration SQL inspection template)
    - grid/test/norms/appendNormCrystallized.test.ts (validation ladder stub template)
  </read_first>
  <action>
Create `grid/test/lore/lore-allowlist-baseline.test.ts` — this test MUST PASS immediately (it tests the current pre-lore state of 41 entries):

```typescript
import { describe, it, expect } from 'vitest';
import { ALLOWLIST_MEMBERS } from '../../src/audit/broadcast-allowlist.js';

describe('Phase 20 allowlist baseline — Wave 0 gate (D-20-13)', () => {
    it('allowlist has exactly 41 events before lore additions', () => {
        // Wave 0 RED gate: lore.contributed and lore.cited not yet in ALLOWLIST_MEMBERS.
        // If this fails with count > 41, a lore event landed before Wave 0 — STOP and fix.
        expect((ALLOWLIST_MEMBERS as readonly string[]).length).toBe(41);
    });

    it('position 41 is norm.crystallized (last Phase 19 event)', () => {
        expect((ALLOWLIST_MEMBERS as readonly string[])[40]).toBe('norm.crystallized');
    });
});
```

Create `grid/test/lore/lore-producer-boundary.test.ts` — mirrors norm-producer-boundary.test.ts exactly (read it to get the walking logic):
```typescript
/**
 * Sole-producer boundary test for lore.contributed and lore.cited.
 * Phase 20 D-20-12: each event string MUST appear only in:
 *   1. audit/broadcast-allowlist.ts
 *   2. The designated sole emitter file
 * AND: no file other than the designated emitter calls audit.append('lore.*', ...)
 *
 * This is a RED stub — imports will fail until Plan 03 ships the emitters.
 */
import { describe, it, expect } from 'vitest';
import * as fs from 'node:fs';
import * as path from 'node:path';

const SRC_DIR = path.resolve(__dirname, '../../src');

const SOLE_EMITTERS: Record<string, string> = {
    'lore.contributed': 'lore/appendLoreContributed.ts',
    'lore.cited': 'lore/appendLoreCited.ts',
};

function walkSync(dir: string, files: string[] = []): string[] {
    for (const entry of fs.readdirSync(dir, { withFileTypes: true })) {
        const full = path.join(dir, entry.name);
        if (entry.isDirectory()) walkSync(full, files);
        else if (entry.isFile() && entry.name.endsWith('.ts')) files.push(full);
    }
    return files;
}

describe('lore sole-producer boundary (D-20-12)', () => {
    const allFiles = walkSync(SRC_DIR);

    for (const [event, emitterRelPath] of Object.entries(SOLE_EMITTERS)) {
        const emitterAbsPath = path.resolve(SRC_DIR, emitterRelPath);

        it(`"${event}" string only in allowlist + sole emitter`, () => {
            const forbidden = allFiles.filter((f) => {
                if (f === emitterAbsPath) return false;
                if (f.includes('audit/broadcast-allowlist.ts')) return false;
                const content = fs.readFileSync(f, 'utf8');
                return content.includes(JSON.stringify(event));
            });
            expect(forbidden).toHaveLength(0);
        });

        it(`audit.append called with "${event}" only in sole emitter`, () => {
            const pattern = `audit.append('${event}'`;
            const pattern2 = `audit.append("${event}"`;
            const violators = allFiles.filter((f) => {
                if (f === emitterAbsPath) return false;
                const content = fs.readFileSync(f, 'utf8');
                return content.includes(pattern) || content.includes(pattern2);
            });
            expect(violators).toHaveLength(0);
        });
    }
});
```

Create `grid/test/lore/appendLoreContributed.test.ts` — RED stub (appendLoreContributed.ts doesn't exist yet):
```typescript
/**
 * RED stub — becomes GREEN in Plan 03 when appendLoreContributed.ts ships.
 * Tests the 10-step validation ladder for lore.contributed (pos 42, D-20-12).
 */
import { describe, it, expect, vi } from 'vitest';
import { appendLoreContributed } from '../../src/lore/appendLoreContributed.js';
import type { AuditChain } from '../../src/audit/chain.js';

const VALID_DID = 'did:noesis:test-nous-01';
const VALID_HASH = 'a'.repeat(64);
const mockAudit = { append: vi.fn().mockReturnValue({ eventType: 'lore.contributed' }) } as unknown as AuditChain;

describe('appendLoreContributed — 10-step validation ladder', () => {
    it('rejects invalid actorDid', () => {
        expect(() => appendLoreContributed(mockAudit, 'bad-did', { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: 'bad-did', tick: 1 })).toThrow(/actorDid/);
    });
    it('rejects invalid contributor_did', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: 'bad-did', tick: 1 })).toThrow(/contributor_did/);
    });
    it('rejects self-report violation (contributor_did !== actorDid)', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: 'did:noesis:other', tick: 1 })).toThrow(/self-report/);
    });
    it('rejects negative tick', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: VALID_DID, tick: -1 })).toThrow(/tick/);
    });
    it('rejects invalid content_hash (not 64-char hex)', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: 'tooshort', contributor_did: VALID_DID, tick: 1 })).toThrow(/content_hash/);
    });
    it('rejects unknown category_tag', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'unknown_cat', content_hash: VALID_HASH, contributor_did: VALID_DID, tick: 1 })).toThrow(/category_tag/);
    });
    it('rejects extra keys (closed-tuple violation)', () => {
        expect(() => appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: VALID_DID, tick: 1, extra: 'bad' } as any)).toThrow(/closed-tuple/);
    });
    it('calls audit.append with lore.contributed on valid payload', () => {
        const result = appendLoreContributed(mockAudit, VALID_DID, { category_tag: 'observation', content_hash: VALID_HASH, contributor_did: VALID_DID, tick: 1 });
        expect(mockAudit.append).toHaveBeenCalledWith('lore.contributed', VALID_DID, expect.objectContaining({ category_tag: 'observation' }));
    });
});
```

Create `grid/test/lore/appendLoreCited.test.ts` — RED stub:
```typescript
/**
 * RED stub — becomes GREEN in Plan 03 when appendLoreCited.ts ships.
 * Tests the 10-step validation ladder for lore.cited (pos 43, D-20-12).
 */
import { describe, it, expect, vi } from 'vitest';
import { appendLoreCited } from '../../src/lore/appendLoreCited.js';
import type { AuditChain } from '../../src/audit/chain.js';

const VALID_DID = 'did:noesis:test-nous-01';
const VALID_HASH = 'b'.repeat(64);
const mockAudit = { append: vi.fn().mockReturnValue({ eventType: 'lore.cited' }) } as unknown as AuditChain;

describe('appendLoreCited — 10-step validation ladder', () => {
    it('rejects invalid actorDid', () => {
        expect(() => appendLoreCited(mockAudit, 'bad-did', { citing_did: 'bad-did', content_hash: VALID_HASH, tick: 1 })).toThrow(/actorDid/);
    });
    it('rejects invalid citing_did', () => {
        expect(() => appendLoreCited(mockAudit, VALID_DID, { citing_did: 'bad-did', content_hash: VALID_HASH, tick: 1 })).toThrow(/citing_did/);
    });
    it('rejects self-report violation (citing_did !== actorDid)', () => {
        expect(() => appendLoreCited(mockAudit, VALID_DID, { citing_did: 'did:noesis:other', content_hash: VALID_HASH, tick: 1 })).toThrow(/self-report/);
    });
    it('rejects negative tick', () => {
        expect(() => appendLoreCited(mockAudit, VALID_DID, { citing_did: VALID_DID, content_hash: VALID_HASH, tick: -1 })).toThrow(/tick/);
    });
    it('rejects invalid content_hash (not 64-char hex)', () => {
        expect(() => appendLoreCited(mockAudit, VALID_DID, { citing_did: VALID_DID, content_hash: 'short', tick: 1 })).toThrow(/content_hash/);
    });
    it('rejects extra keys (closed-tuple violation)', () => {
        expect(() => appendLoreCited(mockAudit, VALID_DID, { citing_did: VALID_DID, content_hash: VALID_HASH, tick: 1, extra: 'x' } as any)).toThrow(/closed-tuple/);
    });
    it('calls audit.append with lore.cited on valid payload', () => {
        const result = appendLoreCited(mockAudit, VALID_DID, { citing_did: VALID_DID, content_hash: VALID_HASH, tick: 2 });
        expect(mockAudit.append).toHaveBeenCalledWith('lore.cited', VALID_DID, expect.objectContaining({ citing_did: VALID_DID }));
    });
});
```

Create `grid/test/lore/lore-migration.test.ts` — RED stub:
```typescript
/**
 * RED stub — becomes GREEN in Plan 02 when migration v8 is added to schema.ts.
 * Inspects the SQL string of migration version 8 for required table shape.
 */
import { describe, it, expect } from 'vitest';
import { MIGRATIONS } from '../../src/db/schema.js';

describe('lore_commons migration — version 8', () => {
    const migration8 = MIGRATIONS.find((m) => m.version === 8);

    it('migration version 8 exists', () => {
        expect(migration8).toBeDefined();
    });

    it('migration is named create_lore_commons', () => {
        expect(migration8?.name).toBe('create_lore_commons');
    });

    it('up SQL creates lore_commons table', () => {
        expect(migration8?.up).toContain('CREATE TABLE IF NOT EXISTS lore_commons');
    });

    it('lore_commons has content_hash CHAR(64)', () => {
        expect(migration8?.up).toMatch(/content_hash\s+CHAR\(64\)/);
    });

    it('lore_commons has title_hash CHAR(64)', () => {
        expect(migration8?.up).toMatch(/title_hash\s+CHAR\(64\)/);
    });

    it('lore_commons has citation_count INT UNSIGNED', () => {
        expect(migration8?.up).toMatch(/citation_count\s+INT UNSIGNED/);
    });

    it('lore_commons has category_tag column', () => {
        expect(migration8?.up).toContain('category_tag');
    });

    it('down SQL drops lore_commons', () => {
        expect(migration8?.down).toContain('DROP TABLE IF EXISTS lore_commons');
    });
});
```

Create `grid/test/lore/lore-citation-listener.test.ts` — RED stub:
```typescript
/**
 * RED stub — becomes GREEN in Plan 04 when LoreCitationListener.ts ships.
 * Verifies pure-observer invariant: zero audit.append calls in listener.
 */
import { describe, it, expect, vi } from 'vitest';
import { LoreCitationListener } from '../../src/lore/LoreCitationListener.js';
import type { AuditChain } from '../../src/audit/chain.js';

describe('LoreCitationListener — pure-observer invariant', () => {
    it('does not call audit.append when lore.cited fires', () => {
        const handlers: ((e: any) => void)[] = [];
        const mockAudit = {
            onAppend: vi.fn((h) => handlers.push(h)),
            append: vi.fn(),
        } as unknown as AuditChain;
        const mockStorage = { incrementCitationCount: vi.fn().mockResolvedValue(undefined) } as any;
        new LoreCitationListener(mockAudit, mockStorage, 'test-grid');

        // Fire a lore.cited entry
        handlers.forEach((h) => h({ eventType: 'lore.cited', payload: { content_hash: 'a'.repeat(64), citing_did: 'did:noesis:x', tick: 1 } }));

        expect(mockAudit.append).not.toHaveBeenCalled();
        expect(mockStorage.incrementCitationCount).toHaveBeenCalledWith('test-grid', 'a'.repeat(64));
    });

    it('ignores non-lore.cited events', () => {
        const handlers: ((e: any) => void)[] = [];
        const mockAudit = {
            onAppend: vi.fn((h) => handlers.push(h)),
            append: vi.fn(),
        } as unknown as AuditChain;
        const mockStorage = { incrementCitationCount: vi.fn().mockResolvedValue(undefined) } as any;
        new LoreCitationListener(mockAudit, mockStorage, 'test-grid');

        handlers.forEach((h) => h({ eventType: 'skill.taught', payload: {} }));
        expect(mockStorage.incrementCitationCount).not.toHaveBeenCalled();
    });
});
```

Run the baseline test to confirm it passes immediately:
`cd grid && npm test -- --testPathPattern=lore-allowlist-baseline`

The sole-producer, appendLoreContributed, appendLoreCited, migration, and citation-listener tests will be RED (expected — they import files that don't exist yet).
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noesis/grid && npm test -- --testPathPattern=lore-allowlist-baseline 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --testPathPattern=lore-allowlist-baseline` exits 0 with 2 passing tests
    - Both tests pass: length === 41 AND position 40 === 'norm.crystallized'
    - All six test files exist in grid/test/lore/
    - The lore-producer-boundary, appendLoreContributed, appendLoreCited, lore-migration, lore-citation-listener tests exist as .test.ts files (they will fail with import errors until Plans 02-04 ship — that is expected RED state)
  </acceptance_criteria>
  <done>Wave 0 gate tests created. Baseline test passes (length=41). All RED stubs exist for future waves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit payload → broadcast | Any object passed to audit.append() must not contain lore body text |
| FORBIDDEN_KEY_PATTERN | Regex gate run by payloadPrivacyCheck() at every sole-producer step 9 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | Information Disclosure | broadcast-allowlist.ts FORBIDDEN_KEY_PATTERN | mitigate | Extend regex with lore_body\|lore_content\|title_text\|summary_text before any emitter lands (this plan) |
| T-20-02 | Information Disclosure | WHISPER_FORBIDDEN_KEYS | accept | __lore_request:/__lore_response: are Brain-internal text prefixes not payload field names; correct guard is FORBIDDEN_KEY_PATTERN extension + Brain-side on_message() prefix check (RESEARCH.md §8) |
</threat_model>

<verification>
After both tasks complete:

```bash
cd /Users/desirey/Programming/src/Noesis/grid && npm test -- --testPathPattern=lore-allowlist-baseline
```

Expected: 2 passing (length===41, position 40===norm.crystallized).

```bash
grep "LORE_FORBIDDEN_KEYS\|lore_body" /Users/desirey/Programming/src/Noesis/grid/src/audit/broadcast-allowlist.ts
```

Expected: LORE_FORBIDDEN_KEYS declaration line + regex line with lore_body|lore_content|title_text|summary_text.

```bash
ls /Users/desirey/Programming/src/Noesis/grid/test/lore/
```

Expected: 6 .test.ts files present.
</verification>

<success_criteria>
- FORBIDDEN_KEY_PATTERN extended with 4 lore-specific keys
- LORE_FORBIDDEN_KEYS exported from broadcast-allowlist.ts
- ALLOWLIST_MEMBERS remains at 41 (no lore events added yet)
- grid/src/lore/types.ts exists with all 6 locked exports
- lore-allowlist-baseline.test.ts passes (GREEN)
- 5 RED stub tests exist (will turn GREEN in Plans 02-04)
</success_criteria>

<output>
After completion, create `.planning/phases/20-lore-commons/20-01-SUMMARY.md`
</output>
