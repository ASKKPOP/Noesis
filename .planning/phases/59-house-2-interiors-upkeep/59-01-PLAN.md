---
phase: 59
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - grid/test/civic/furniture-catalog.test.ts
  - grid/test/civic/interior-tree.test.ts
  - grid/test/civic/upkeep-scanner.test.ts
  - grid/test/civic/condition-ladder.test.ts
  - grid/test/api/civic-interior-routes.test.ts
  - grid/test/audit/append-zoning-interior-extended.test.ts
  - grid/test/audit/append-zoning-condition-changed.test.ts
  - grid/test/audit/append-zoning-parcel-reclaimed.test.ts
  - grid/test/audit/append-treasury-upkeep-collected.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - grid/test/civic/house-2-e2e.test.ts
autonomous: true
requirements: [R-59-01, R-59-02, R-59-03, R-59-04, R-59-05, R-59-06, R-59-07, R-59-08, R-59-12]
tags: [nous-house, house-2, interiors, upkeep, tdd-scaffold, allowlist-95]

must_haves:
  truths:
    - "Wave 0 skip-stub test files exist for every HOUSE-2 deliverable (furniture catalog, interior tree, upkeep scanner, condition ladder, interior routes, 4 sole-producer events, e2e)"
    - "broadcast-allowlist.test.ts moves EVERY .toBe(91) -> .toBe(95) and adds presence assertions for zoning.interior_extended, zoning.condition_changed, zoning.parcel_reclaimed, treasury.upkeep_collected"
    - "The allowlist suite is EXPECTED-RED at 95 until Wave 4 lands the 4 members (documented expected-RED) — all OTHER new stub blocks are describe.skip and pass green"
    - "Stub it-names express the closed 4-tuple payload shapes, mirror-only-in-home, period-boundary debit, ladder maintained->worn->derelict->reclaimed, commons exempt, single-onTick (no new clock.onTick)"
  artifacts:
    - path: "grid/test/civic/furniture-catalog.test.ts"
      provides: "describe.skip stub: closed v1 catalog (mirror 6 home-only render-only; functional 7 with affordances); isValidFurniture rejects mirror outside home"
    - path: "grid/test/civic/interior-tree.test.ts"
      provides: "describe.skip stub: extendInterior validates catalog+structure fit, mutates areas->objects tree, rejects unknown kind + mirror-in-non-home; interior never serialized to chain"
    - path: "grid/test/civic/upkeep-scanner.test.ts"
      provides: "describe.skip stub: period-boundary debit owner->TREASURY_DID + treasury.upkeep_collected; commons exempt; single clock.onTick (no second subscription)"
    - path: "grid/test/civic/condition-ladder.test.ts"
      provides: "describe.skip stub: maintained->worn->derelict->reclaimed advance on missed payment; reset on payment; derelict closes visitors; reclaim razes + ejects + returns to treasury"
    - path: "grid/test/api/civic-interior-routes.test.ts"
      provides: "describe.skip stub: POST interior/extend (owner-only, catalog-validated, 403 not_owner, 422 invalid_furniture); GET interior entry-policy + derelict gating (403 closed_to_visitors)"
    - path: "grid/test/audit/append-zoning-interior-extended.test.ts"
      provides: "describe.skip stub: closed 4-tuple {object_class, object_kind, parcel_id, tick}; actorDid = parcel_id; no interior names/state on chain"
    - path: "grid/test/audit/append-zoning-condition-changed.test.ts"
      provides: "describe.skip stub: closed 4-tuple {condition, owner_civic_did_hash, parcel_id, tick}; actorDid = owner_civic_did_hash"
    - path: "grid/test/audit/append-zoning-parcel-reclaimed.test.ts"
      provides: "describe.skip stub: closed 4-tuple {former_owner_civic_did_hash, parcel_id, reason, tick}; reason='upkeep_default'; actorDid = parcel_id"
    - path: "grid/test/audit/append-treasury-upkeep-collected.test.ts"
      provides: "describe.skip stub: closed 4-tuple {amount_bios, owner_civic_did_hash, parcel_id, tick}; actorDid = parcel_id (mirrors #83)"
    - path: "grid/test/audit/broadcast-allowlist.test.ts"
      provides: "UPDATED: every .toBe(91) -> .toBe(95) + presence assertions for the 4 new members (EXPECTED-RED until Wave 4)"
      contains: "toBe(95)"
    - path: "grid/test/civic/house-2-e2e.test.ts"
      provides: "describe.skip stub: full HOUSE-2 scenario (extend interior -> view gated -> miss upkeep through ladder to reclaim; commons untouched)"
  key_links:
    - from: "Wave 0 stubs"
      to: "Waves 1-6 implementations"
      via: "describe.skip blocks -> describe (un-skip when implementation lands)"
      pattern: "describe(\\.skip)?\\("
    - from: "broadcast-allowlist.test.ts -> 95"
      to: "Wave 4 allowlist members"
      via: "expected-RED until the 4 members land"
      pattern: "toBe\\(95\\)"
---

<objective>
Create Wave 0 test infrastructure for Phase 59 Nous House HOUSE-2. Per GSD Wave 0 discipline, lock every HOUSE-2 contract in skip-stub test files BEFORE any implementation lands. CRITICALLY: move `broadcast-allowlist.test.ts` from `.toBe(91)` to `.toBe(95)` (with presence assertions for the 4 new members) so any accidental allowlist drift fails CI. The allowlist suite is **EXPECTED-RED at 95 until Wave 4** lands the members — every other new stub block is `describe.skip` and passes green. Each later wave un-skips the stubs it satisfies.

Output: 10 new skip-stub test files + 1 UPDATED allowlist test (now asserting 95, expected-RED until Wave 4).
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@.planning/phases/59-house-2-interiors-upkeep/59-VALIDATION.md
@grid/src/civic/parcel-registry.ts
@grid/src/civic/types.ts
@grid/src/civic/founding-law.ts
@grid/src/audit/broadcast-allowlist.ts
@grid/src/audit/append-treasury-parcel-revenue.ts
@grid/test/audit/broadcast-allowlist.test.ts

<interfaces>
<!-- Phase 59 introduces these symbol names. Tests reference them by import path.
     Implementations land in later waves; Wave 0 tests are describe.skip stubs that DESCRIBE the contracts. -->

From grid/src/civic/furniture.ts (Wave 1 creates):
```typescript
export type FurnitureClass = 'mirror' | 'functional';
export interface FurnitureEntry { kind: string; class: FurnitureClass; affordances: readonly string[]; }
export const FURNITURE_CATALOG: Readonly<Record<string, FurnitureEntry>>;   // 6 mirror + 7 functional
export function isValidFurniture(kind: string, structureType: string): boolean; // mirror only in 'home'
```

From grid/src/civic/founding-law.ts (Wave 1 extends — single patch point):
```typescript
export const UPKEEP_PERIOD_TICKS = 10080;
export const UPKEEP_RATE_BPS = 200;
export const RECLAIM_GRACE_PERIODS: { worn: 1; derelict: 2; reclaim: 3 };
export function upkeepDue(parcel: Parcel): number;   // floor(price_bios * UPKEEP_RATE_BPS / 10000)
```

From grid/src/civic/parcel-registry.ts (Waves 1+4 extend):
```typescript
extendInterior(address: string, ownerDid: string, item: { area: string; kind: string }): Structure;
advanceCondition(address: string): 'worn' | 'derelict' | 'reclaimed';  // ladder step
resetCondition(address: string): void;                                  // on paid upkeep
```

From grid/src/civic/upkeep-scanner.ts (Wave 4 creates):
```typescript
export function onUpkeepTick(tick: number, deps: UpkeepDeps): Promise<void>;  // fire-and-forget from launcher's EXISTING clock.onTick
```

From grid/src/audit/append-*.ts (Wave 4 creates — closed 4-tuples):
```typescript
export function appendZoningInteriorExtended(audit, { object_class, object_kind, parcel_id, tick }): AuditEntry;
export function appendZoningConditionChanged(audit, { condition, owner_civic_did_hash, parcel_id, tick }): AuditEntry;
export function appendZoningParcelReclaimed(audit, { former_owner_civic_did_hash, parcel_id, reason, tick }): AuditEntry;
export function appendTreasuryUpkeepCollected(audit, { amount_bios, owner_civic_did_hash, parcel_id, tick }): AuditEntry;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Civic skip-stubs (furniture-catalog / interior-tree / upkeep-scanner / condition-ladder)</name>
  <files>
    grid/test/civic/furniture-catalog.test.ts,
    grid/test/civic/interior-tree.test.ts,
    grid/test/civic/upkeep-scanner.test.ts,
    grid/test/civic/condition-ladder.test.ts
  </files>
  <read_first>
    grid/src/civic/parcel-registry.ts (live state-transition surface to test against; Phase 58 purchase/build/join precedent),
    grid/src/civic/types.ts (Parcel/Structure/ZoneId/structureType),
    grid/src/civic/founding-law.ts (gravityPrice + GENESIS_CORE_SEED_PLAN — upkeep constants land beside these in Wave 1),
    .planning/phases/58-house-1-foundations/58-01-PLAN.md (canonical Wave 0 describe.skip pattern)
  </read_first>
  <behavior>
    Create four vitest files, all using `describe.skip` so the suite passes before Wave 1/4 implementation. Each imports the not-yet-existing symbols (acceptable — describe.skip bodies never execute the callsites).

    `furniture-catalog.test.ts` — skip-stub asserts:
    - `FURNITURE_CATALOG` has EXACTLY 6 mirror kinds (`bed, closet, shelf, kitchen, bathroom, decor`) + 7 functional kinds (`work_desk, sim_board, meeting_table, game_table, task_board, skill_terminal, shop_counter`).
    - mirror entries declare `class: 'mirror'` and (near-)empty affordances; functional entries declare `class: 'functional'` and non-empty affordances (e.g. `work_desk` → `['billing','accounting']`).
    - `isValidFurniture('bed','home') === true`; `isValidFurniture('bed','shop') === false` (mirror only in home).
    - `isValidFurniture('work_desk','shop') === true`; an unknown kind → `false`.

    `interior-tree.test.ts` — skip-stub asserts:
    - `extendInterior(addr, ownerDid, {area:'bedroom', kind:'bed'})` on an owned `home` mutates the `interior.areas` tree (area created if absent; object appended).
    - rejects an unknown furniture kind (`invalid_furniture`).
    - rejects mirror furniture in a non-home structure (mirror-only-in-home).
    - the mutated interior tree is NOT serialized into any audit payload (a spy on the append-* producers never receives object names/state — only `object_class`/`object_kind` cross).

    `upkeep-scanner.test.ts` — skip-stub asserts:
    - on a period boundary (`last_upkeep_tick` a full `UPKEEP_PERIOD_TICKS` behind), an owned non-commons parcel is debited `upkeepDue` via `registry.transferOusia(owner → TREASURY_DID)` and emits `treasury.upkeep_collected`; `last_upkeep_tick` advances.
    - Polis Commons (`owner_civic_did` NULL, rings 0–1) are SKIPPED entirely — never debited.
    - single-onTick: `onUpkeepTick` is a plain function called from the EXISTING `clock.onTick`; the scanner module adds NO `clock.onTick` subscription (grep assertion in a stub it-name).

    `condition-ladder.test.ts` — skip-stub asserts:
    - missed payment advances maintained → worn (1 missed) → derelict (2 missed) → reclaimed (3 missed).
    - paying upkeep resets `missed_periods=0` and `condition='maintained'`.
    - derelict CLOSES the structure to visitors (GET interior + join refuse).
    - reclaim returns ownership to `TREASURY_DID`, razes structure+interior, ejects occupants, resets condition to maintained on the treasury parcel.
    - each transition emits `zoning.condition_changed`; reclaim additionally emits `zoning.parcel_reclaimed {reason:'upkeep_default'}`.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- --reporter=verbose civic/furniture-catalog civic/interior-tree civic/upkeep-scanner civic/condition-ladder 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls grid/test/civic/furniture-catalog.test.ts grid/test/civic/interior-tree.test.ts grid/test/civic/upkeep-scanner.test.ts grid/test/civic/condition-ladder.test.ts` lists all 4 files
    - `grep -c "describe.skip" grid/test/civic/furniture-catalog.test.ts` >= 1 (and the other 3 likewise)
    - `grep -E "mirror|functional|isValidFurniture" grid/test/civic/furniture-catalog.test.ts` returns matching lines
    - `grep -E "maintained|worn|derelict|reclaimed" grid/test/civic/condition-ladder.test.ts` returns matching lines
    - `grep -E "UPKEEP_PERIOD_TICKS|TREASURY_DID|commons|onTick" grid/test/civic/upkeep-scanner.test.ts` returns matching lines
    - `cd grid && npm run test -- civic/furniture-catalog civic/interior-tree civic/upkeep-scanner civic/condition-ladder` exits 0 with all blocks skipped
  </acceptance_criteria>
  <done>4 civic skip-stub files exist expressing the catalog/interior/upkeep/ladder contracts; suite exits 0 with all blocks skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Routes + 4 sole-producer skip-stubs + e2e stub; UPDATE allowlist to 95 (expected-RED)</name>
  <files>
    grid/test/api/civic-interior-routes.test.ts,
    grid/test/audit/append-zoning-interior-extended.test.ts,
    grid/test/audit/append-zoning-condition-changed.test.ts,
    grid/test/audit/append-zoning-parcel-reclaimed.test.ts,
    grid/test/audit/append-treasury-upkeep-collected.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts,
    grid/test/civic/house-2-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/policy.ts (ROUTE_DID_POLICY tiers: public / civic_did_required / human_visitor),
    grid/src/api/routes/civic-parcels.ts (Phase 58 route shape the interior routes extend),
    grid/src/audit/append-treasury-parcel-revenue.ts (the sole-producer triad to CLONE — closed-tuple Object.keys().sort() + payloadPrivacyCheck + audit.append; PARCEL_ID_RE),
    grid/src/audit/broadcast-allowlist.ts (ALLOWLIST_MEMBERS at 91, FORBIDDEN_KEY_PATTERN, HEX64 discipline),
    grid/test/audit/broadcast-allowlist.test.ts (existing 91-member assertions — UPDATE the count to 95)
  </read_first>
  <behavior>
    Create six new skip-stub files and UPDATE the allowlist test.

    `civic-interior-routes.test.ts` — describe.skip stub covering:
    - `POST /api/v1/civic/parcels/:id/interior/extend`: `civic_did_required` + owner-only; non-owner → 403 `not_owner`; unknown/mismatched kind → 422 `invalid_furniture`; success → 200 mutating the tree + `zoning.interior_extended` emitted.
    - `GET /api/v1/civic/parcels/:id/interior`: owner always; visitor only if structure open or allowlisted AND `condition != derelict`; derelict → 403 `closed_to_visitors`; a human attempting a private interior → refused.
    - the public feed (GET parcels) still exposes exterior + condition only — never the interior tree.

    `append-zoning-interior-extended.test.ts` — describe.skip stub asserts the closed 4-tuple `{object_class, object_kind, parcel_id, tick}`: `object_class ∈ {'mirror','functional'}`; `object_kind ∈` catalog; `parcel_id` PARCEL_ID_RE; `tick` non-negative int; `actorDid = parcel_id`; a 5th key throws (closed tuple); NO interior name/state key; no `FORBIDDEN_KEY_PATTERN` key.

    `append-zoning-condition-changed.test.ts` — describe.skip stub asserts the closed 4-tuple `{condition, owner_civic_did_hash, parcel_id, tick}`: `condition ∈ {'maintained','worn','derelict'}`; `owner_civic_did_hash` HEX64; `parcel_id` PARCEL_ID_RE; `tick` non-negative int; `actorDid = owner_civic_did_hash`; closed-tuple + privacy assertions.

    `append-zoning-parcel-reclaimed.test.ts` — describe.skip stub asserts the closed 4-tuple `{former_owner_civic_did_hash, parcel_id, reason, tick}`: HEX64; PARCEL_ID_RE; `reason ∈ {'upkeep_default'}`; non-negative tick; `actorDid = parcel_id`; closed-tuple + privacy assertions.

    `append-treasury-upkeep-collected.test.ts` — describe.skip stub asserts the closed 4-tuple `{amount_bios, owner_civic_did_hash, parcel_id, tick}`: `amount_bios` positive int; HEX64; PARCEL_ID_RE; non-negative tick; `actorDid = parcel_id` (mirrors #83); closed-tuple + privacy assertions.

    `house-2-e2e.test.ts` — describe.skip stub naming the full scenario: owner extends a home interior (mirror + functional) → `zoning.interior_extended`; a visitor GETs the interior while open, is refused once derelict; with insufficient funds, ticking past `UPKEEP_PERIOD_TICKS` boundaries walks maintained→worn→derelict→reclaimed emitting `treasury.upkeep_collected` (when funded), `zoning.condition_changed`, `zoning.parcel_reclaimed`; commons never decay; no interior name/state on any payload.

    `broadcast-allowlist.test.ts` — **UPDATE**: move EVERY `.toBe(91)` → `.toBe(95)` (both `ALLOWLIST.size` and `ALLOWLIST_MEMBERS.length` assertions), and ADD presence assertions for the four new members: `zoning.interior_extended`, `zoning.condition_changed`, `zoning.parcel_reclaimed`, `treasury.upkeep_collected`. This suite is **EXPECTED-RED until Wave 4** lands the members in `ALLOWLIST_MEMBERS` — document this expected-RED in the SUMMARY. Do NOT touch events 82–91 ordering.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- --reporter=verbose api/civic-interior-routes audit/append-zoning-interior-extended audit/append-zoning-condition-changed audit/append-zoning-parcel-reclaimed audit/append-treasury-upkeep-collected audit/broadcast-allowlist 2>&1 | tail -35</automated>
  </verify>
  <acceptance_criteria>
    - `ls grid/test/api/civic-interior-routes.test.ts grid/test/audit/append-zoning-interior-extended.test.ts grid/test/audit/append-zoning-condition-changed.test.ts grid/test/audit/append-zoning-parcel-reclaimed.test.ts grid/test/audit/append-treasury-upkeep-collected.test.ts grid/test/civic/house-2-e2e.test.ts` lists all 6 files
    - `grep -c "describe.skip" grid/test/api/civic-interior-routes.test.ts` >= 1 (and the 4 append stubs + e2e likewise)
    - `grep "closed_to_visitors" grid/test/api/civic-interior-routes.test.ts` returns >= 1 line
    - `grep "invalid_furniture" grid/test/api/civic-interior-routes.test.ts` returns >= 1 line
    - `grep -c "toBe(91)" grid/test/audit/broadcast-allowlist.test.ts` is 0 (all moved to 95)
    - `grep -c "toBe(95)" grid/test/audit/broadcast-allowlist.test.ts` equals the prior count of toBe(91) lines
    - `grep -E "zoning.interior_extended|zoning.condition_changed|zoning.parcel_reclaimed|treasury.upkeep_collected" grid/test/audit/broadcast-allowlist.test.ts` returns the 4 presence assertions
    - `cd grid && npm run test -- api/civic-interior-routes audit/append-zoning-interior-extended audit/append-zoning-condition-changed audit/append-zoning-parcel-reclaimed audit/append-treasury-upkeep-collected` exits 0 (all skipped)
    - `cd grid && npm run test -- audit/broadcast-allowlist` is RED at 95 (expected until Wave 4)
  </acceptance_criteria>
  <done>6 skip-stub files exist; broadcast-allowlist.test.ts updated to 95 with presence assertions (expected-RED until Wave 4).</done>
</task>

</tasks>

<success_criteria>
- 10 new skip-stub test files committed under `grid/test/`
- Every new file uses `describe.skip(...)` so Vitest reports them skipped, not failed
- `broadcast-allowlist.test.ts` moved from 91 → 95 with presence assertions for the 4 new members (expected-RED until Wave 4 — documented)
- Each later wave (1–6) can cite a pre-existing test file it un-skips
- `cd grid && npm run test -- civic furniture interior upkeep condition api/civic-interior` exits 0 with new blocks skipped
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-01-SUMMARY.md` summarising the 10 new skip-stub files, their coverage of R-59-01..08/12, and the allowlist move to 95 (expected-RED until Wave 4).
</output>
