---
phase: 59
plan: 07
type: execute
wave: 6
depends_on: [59-04, 59-05, 59-06]
files_modified:
  - grid/test/civic/house-2-e2e.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - scripts/check-sole-producer-discipline.mjs
  - scripts/check-wallclock-forbidden.mjs
  - scripts/check-civic-did-issuance-path.mjs
autonomous: true
requirements: [R-59-04, R-59-06, R-59-07, R-59-08, R-59-12]
tags: [nous-house, house-2, e2e, gates, definition-of-done]

must_haves:
  truths:
    - "End-to-end: owner extends a home interior (mirror + functional) -> zoning.interior_extended on the trail; a visitor GETs the interior while open, is refused once derelict"
    - "End-to-end: with insufficient funds, ticking past UPKEEP_PERIOD_TICKS boundaries walks maintained->worn->derelict->reclaimed, emitting treasury.upkeep_collected (when funded), zoning.condition_changed, then zoning.parcel_reclaimed {reason:'upkeep_default'}; reclaimed parcel returns to treasury; commons never decay"
    - "No interior name/state appears in any audit payload (only object_class/object_kind enums); all DIDs hashed HEX64"
    - "broadcast-allowlist GREEN at 95; sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, privacy walker all exit 0; zero-diff R-31-01 holds (no chain code edited)"
    - "cd grid && npm run test exits 0 with ZERO remaining describe.skip in Phase 59 test files"
  artifacts:
    - path: "grid/test/civic/house-2-e2e.test.ts"
      provides: "Definition-of-Done E2E: extend -> view-gated -> miss-upkeep-to-reclaim with all 4 events + commons untouched"
  key_links:
    - from: "house-2-e2e miss-upkeep-to-reclaim"
      to: "treasury.upkeep_collected + zoning.condition_changed + zoning.parcel_reclaimed on the trail"
      via: "ticking past UPKEEP_PERIOD_TICKS boundaries through the full ladder"
      pattern: "upkeep_collected|condition_changed|parcel_reclaimed"
    - from: "all Phase 59 test files"
      to: "0 remaining describe.skip"
      via: "every Wave un-skipped its stubs"
      pattern: "describe\\.skip"
---

<objective>
Prove the Definition of Done and keep every CI gate green. The HOUSE-2 acceptance scenario: an owner extends a home interior (mirror + functional), a visitor views it while open and is refused once derelict, then with insufficient funds the structure is ticked through the full upkeep ladder maintained→worn→derelict→reclaimed — emitting all four new events along the trail (`zoning.interior_extended`, `treasury.upkeep_collected` when funded, `zoning.condition_changed`, `zoning.parcel_reclaimed`) — while commons never decay and no interior name/state ever crosses the chain.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@.planning/phases/59-house-2-interiors-upkeep/59-VALIDATION.md
@grid/src/civic/upkeep-scanner.ts
@grid/src/civic/parcel-registry.ts
@grid/src/api/routes/civic-parcels.ts
@grid/src/audit/broadcast-allowlist.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOUSE-2 end-to-end Definition-of-Done test</name>
  <files>
    grid/test/civic/house-2-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (interior/extend + GET interior path),
    grid/src/civic/upkeep-scanner.ts (onUpkeepTick period-boundary path),
    grid/src/civic/founding-law.ts (UPKEEP_PERIOD_TICKS, UPKEEP_RATE_BPS, RECLAIM_GRACE_PERIODS, upkeepDue),
    grid/src/audit/append-zoning-interior-extended.ts (+ siblings — payload shapes for the trail assertions)
  </read_first>
  <behavior>
    Un-skip + author `grid/test/civic/house-2-e2e.test.ts` exercising the HOUSE-2 acceptance scenario end-to-end against a seeded Genesis Core (build on the Phase 58 buy→build path):
    - a Nous owns a built ring-3 home; it `extend_interior`s a mirror item (e.g. `bed` in `bedroom`) and a functional item → assert `zoning.interior_extended` appears on the trail with ONLY `{object_class, object_kind, parcel_id, tick}` (no interior name/state); a second Nous GETs the interior while the structure is open → allowed.
    - with the owner's balance set to insufficient, drive `onUpkeepTick` across successive `UPKEEP_PERIOD_TICKS` boundaries: 1st miss → `condition='worn'` + `zoning.condition_changed`; 2nd miss → `condition='derelict'` + `zoning.condition_changed`, and the visitor's GET interior now → 403 `closed_to_visitors`; 3rd miss → reclaim: owner → `TREASURY_DID`, structure+interior razed, occupants ejected, `zoning.parcel_reclaimed {reason:'upkeep_default'}` on the trail; the reclaimed parcel is back in the treasury for resale.
    - a FUNDED period (separate parcel/owner with balance) → `treasury.upkeep_collected` on the trail with `{amount_bios, owner_civic_did_hash, parcel_id, tick}`; condition stays maintained; `missed_periods` reset.
    - a Polis Commons parcel (rings 0–1, owner NULL) → ticked across boundaries, NEVER debited, NEVER decayed.
    - assert no raw owner DID anywhere in the payloads (all HEX64); no interior name/state on any payload.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/house-2-e2e 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test -- civic/house-2-e2e` is green
    - the test asserts all 4 new events on the trail (interior_extended, upkeep_collected, condition_changed, parcel_reclaimed) and the derelict-closes-visitors + reclaim-to-treasury + commons-exempt cases
    - the test confirms no interior name/state on any payload and all DIDs HEX64
  </acceptance_criteria>
  <done>house-2-e2e.test.ts green: extend → view-gated → miss-upkeep-to-reclaim with all 4 events; commons untouched; interiors never on the chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: all gates green + all Phase 59 skip-stubs un-skipped</name>
  <files>
    grid/test/audit/broadcast-allowlist.test.ts,
    scripts/check-sole-producer-discipline.mjs,
    scripts/check-wallclock-forbidden.mjs,
    scripts/check-civic-did-issuance-path.mjs
  </files>
  <read_first>
    grid/test/audit/broadcast-allowlist.test.ts (must assert 95; do NOT weaken),
    scripts/check-sole-producer-discipline.mjs (must stay green — read-only confirmation),
    scripts/check-wallclock-forbidden.mjs (must stay green — read-only confirmation),
    scripts/check-civic-did-issuance-path.mjs (must stay green — read-only confirmation)
  </read_first>
  <behavior>
    Verify (do NOT weaken) every CI gate:
    - `broadcast-allowlist.test.ts` GREEN at 95 (delta +4 confirmed) with presence assertions for all 4 new members.
    - `scripts/check-sole-producer-discipline.mjs` passes (one producer per event, no foreign `audit.append` for the 4 events).
    - the privacy walker passes (no payload key matches `FORBIDDEN_KEY_PATTERN`; interior contents never broadcast).
    - `scripts/check-civic-did-issuance-path.mjs` and `scripts/check-wallclock-forbidden.mjs` pass unchanged.
    - zero-diff (R-31-01) holds — no chain / persistence-of-chain code was edited; the 4 events ride new standard sole producers.
    - single-onTick invariant preserved — `grep -rn "clock.onTick" grid/src` shows exactly one subscription.
    - the full grid suite is green; all Wave-0 skip-stubs from 59-01 are now un-skipped — `grep -c describe.skip` across the Phase 59 test files is 0.

    These gate scripts are listed in files_modified only as touch-points; if they pass unchanged, do not edit them. If a gate fails, the fix belongs in the offending earlier wave's source, NOT in the gate (never weaken a gate to pass).
  </behavior>
  <verify>
    <automated>cd grid && npm run test; node scripts/check-sole-producer-discipline.mjs; node scripts/check-wallclock-forbidden.mjs; node scripts/check-civic-did-issuance-path.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test` is fully green
    - `cd grid && npm run test -- audit/broadcast-allowlist` GREEN at 95 with presence assertions for all 4
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
    - `node scripts/check-civic-did-issuance-path.mjs` exits 0
    - `grep -rn "clock.onTick" grid/src | wc -l` shows exactly 1 (single-onTick)
    - `grep -rc "describe.skip" grid/test/civic grid/test/api/civic-interior-routes.test.ts grid/test/audit/append-zoning-interior-extended.test.ts grid/test/audit/append-zoning-condition-changed.test.ts grid/test/audit/append-zoning-parcel-reclaimed.test.ts grid/test/audit/append-treasury-upkeep-collected.test.ts | grep -v ':0$'` returns nothing (no remaining skips in Phase 59 test files)
    - no chain/persistence-of-chain file appears in the phase diff (zero-diff R-31-01)
  </acceptance_criteria>
  <done>All CI gates green; allowlist at 95 (+4); single-onTick preserved; every Phase 59 skip-stub un-skipped; zero-diff holds.</done>
</task>

</tasks>

<success_criteria>
- End-to-end DoD passes: extend interior → view-gated → miss-upkeep through the full ladder to reclaim; all 4 new events on the trail; commons never decay; interiors never on the chain
- broadcast-allowlist GREEN at 95 (+4); sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, privacy walker, zero-diff (R-31-01) all green and unchanged
- single-onTick invariant preserved (exactly one clock.onTick in grid/src)
- No remaining describe.skip in any Phase 59 test file
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-07-SUMMARY.md` and the phase-completion report.
</output>
