---
phase: 59
plan: 02
type: execute
wave: 1
depends_on: [59-01]
files_modified:
  - grid/src/db/schema.ts
  - grid/src/civic/founding-law.ts
  - grid/src/civic/furniture.ts
  - grid/src/civic/types.ts
  - grid/src/civic/parcel-registry.ts
  - grid/src/civic/parcel-store.ts
  - grid/test/civic/furniture-catalog.test.ts
  - grid/test/civic/interior-tree.test.ts
autonomous: true
requirements: [R-59-01, R-59-02, R-59-03, R-59-05]
tags: [nous-house, house-2, persistence, migration-v39, furniture-catalog, interior-tree, upkeep-constants]

must_haves:
  truths:
    - "Migration v39 (next free after shipped v38) ALTERs civic_parcels ADD structure_interior JSON + condition ENUM('maintained','worn','derelict') DEFAULT 'maintained' + last_upkeep_tick INT UNSIGNED + missed_periods TINYINT UNSIGNED DEFAULT 0; down drops all four; applies on top of v38"
    - "furniture.ts exports a frozen closed v1 catalog (mirror 6 home-only render-only; functional 7 with affordances) + isValidFurniture(kind, structureType); mirror rejected outside home"
    - "Structure type carries interior?:{areas:[{name,objects:[{kind,class,state?}]}]}; ParcelRegistry.extendInterior validates catalog+fit and mutates the tree Grid-side; rejects unknown kinds + mirror-in-non-home"
    - "founding-law.ts gains UPKEEP_PERIOD_TICKS=10080, UPKEEP_RATE_BPS=200, RECLAIM_GRACE_PERIODS, upkeepDue(parcel); no upkeep constant lives anywhere else"
    - "ParcelStore.persistInterior writes DB-first; hydrate() reads the 4 new columns"
    - "furniture-catalog.test.ts + interior-tree.test.ts un-skipped and green"
  artifacts:
    - path: "grid/src/db/schema.ts"
      provides: "Migration v39 ALTER civic_parcels (4 ADD columns) + down (4 DROP)"
    - path: "grid/src/civic/furniture.ts"
      provides: "FURNITURE_CATALOG (closed v1) + isValidFurniture(kind, structureType) — single validation point"
    - path: "grid/src/civic/founding-law.ts"
      provides: "UPKEEP_PERIOD_TICKS + UPKEEP_RATE_BPS + RECLAIM_GRACE_PERIODS + upkeepDue (single patch point)"
    - path: "grid/src/civic/types.ts"
      provides: "Structure.interior tree type (areas->objects->{kind,class,state?}) + condition/last_upkeep_tick/missed_periods on Parcel"
    - path: "grid/src/civic/parcel-registry.ts"
      provides: "extendInterior(address, ownerDid, {area, kind}) catalog-validated tree mutation"
    - path: "grid/src/civic/parcel-store.ts"
      provides: "persistInterior writer (DB-first) + hydrate reads the 4 new columns"
  key_links:
    - from: "grid/src/civic/furniture.ts isValidFurniture"
      to: "grid/src/civic/parcel-registry.ts extendInterior"
      via: "single catalog validation point reused by registry (and Wave 2 route)"
      pattern: "isValidFurniture\\("
    - from: "grid/src/civic/founding-law.ts upkeepDue"
      to: "grid/src/civic/upkeep-scanner.ts (Wave 4)"
      via: "upkeep charge derived ONLY from founding-law constants"
      pattern: "upkeepDue\\(|UPKEEP_RATE_BPS"
    - from: "civic_parcels v39 columns"
      to: "ParcelRegistry in-memory state"
      via: "ParcelStore.hydrate(registry) reads condition/interior/upkeep columns on boot"
      pattern: "hydrate\\("
---

<objective>
Lay the HOUSE-2 storage + data model. Add migration v39 (ALTER civic_parcels with the interior/condition/upkeep columns), the closed furniture catalog, the interior tree on the Structure type + `ParcelRegistry.extendInterior`, the upkeep founding-law constants (single patch point), and the `ParcelStore` writers/hydrate for the new columns. Un-skip the furniture-catalog + interior-tree stubs.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@grid/src/db/schema.ts
@grid/src/civic/founding-law.ts
@grid/src/civic/types.ts
@grid/src/civic/parcel-registry.ts
@grid/src/civic/parcel-store.ts

<lesson>
HumanRegistry incident (2026-06-11): an in-memory registry not backed by MySQL lost state on restart. The registry is a READ CACHE; MySQL is the source of truth. The v39 columns are written DB-first then mirrored to memory; boot hydrates memory from DB. The interior tree is the JSON column — it lives Grid-side and is NEVER embedded in an audit payload.
</lesson>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migration v39 + upkeep founding-law constants + furniture catalog</name>
  <files>
    grid/src/db/schema.ts,
    grid/src/civic/founding-law.ts,
    grid/src/civic/furniture.ts,
    grid/test/civic/furniture-catalog.test.ts
  </files>
  <read_first>
    grid/src/db/schema.ts (MIGRATIONS array — latest shipped version is 38; v39 is next free; v38 created civic_parcels),
    grid/src/civic/founding-law.ts (gravityPrice + GENESIS_CORE_SEED_PLAN — upkeep constants live BESIDE these as the single patch point),
    grid/test/civic/furniture-catalog.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Add migration **v39** to the MIGRATIONS array in `grid/src/db/schema.ts` ALTERing `civic_parcels`:
    - `ADD COLUMN structure_interior JSON NULL`
    - `ADD COLUMN condition ENUM('maintained','worn','derelict') NOT NULL DEFAULT 'maintained'`
    - `ADD COLUMN last_upkeep_tick INT UNSIGNED NULL`
    - `ADD COLUMN missed_periods TINYINT UNSIGNED NOT NULL DEFAULT 0`
    - a working `down` that DROPs all four columns. v39 is the next free version after shipped v38; it must apply cleanly on top of v38.

    Extend `grid/src/civic/founding-law.ts` (the SINGLE upkeep patch point — Phase 60 Polis amendments edit only this file):
    - `export const UPKEEP_PERIOD_TICKS = 10080;` (1 week @ 1 tick/min, gov debate-window precedent)
    - `export const UPKEEP_RATE_BPS = 200;` (2% of price_bios per period)
    - `export const RECLAIM_GRACE_PERIODS = { worn: 1, derelict: 2, reclaim: 3 } as const;`
    - `export function upkeepDue(parcel: Parcel): number` = `Math.floor(parcel.price_bios * UPKEEP_RATE_BPS / 10000)`. Commons (price 0 / owner NULL) yield 0 / are exempt at the scanner.

    Create `grid/src/civic/furniture.ts` — the frozen closed v1 catalog:
    - mirror (render-only, near-zero cost, home-only): `bed, closet, shelf, kitchen, bathroom, decor` — `class:'mirror'`, empty/near-empty affordances.
    - functional (declares affordances): `work_desk:['billing','accounting'], sim_board, meeting_table, game_table, task_board, skill_terminal, shop_counter`.
    - `export function isValidFurniture(kind: string, structureType: string): boolean` — unknown kind → false; mirror kind → true ONLY when `structureType === 'home'`; functional kind → true in non-home structures per affordance. This is the SINGLE validation point reused by the registry and the Wave 2 route.

    Un-skip `furniture-catalog.test.ts` (remove `.skip`) and make it green.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/furniture-catalog && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "version: 39|structure_interior|condition ENUM|last_upkeep_tick|missed_periods" grid/src/db/schema.ts` shows the migration + 4 columns
    - migration v39 applies cleanly on top of v38; down drops all four columns
    - `grep -E "UPKEEP_PERIOD_TICKS|UPKEEP_RATE_BPS|RECLAIM_GRACE_PERIODS|upkeepDue" grid/src/civic/founding-law.ts` shows the constants + helper
    - `grep -E "isValidFurniture|mirror|functional" grid/src/civic/furniture.ts` shows the catalog + gate
    - furniture-catalog.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/furniture-catalog` is green (6 mirror + 7 functional; mirror-only-in-home)
    - typecheck passes
  </acceptance_criteria>
  <done>Migration v39 ALTERs civic_parcels; founding-law holds the only copy of upkeep constants; furniture.ts is the closed catalog + single gate; furniture-catalog.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: interior tree type + extendInterior + ParcelStore writers/hydrate</name>
  <files>
    grid/src/civic/types.ts,
    grid/src/civic/parcel-registry.ts,
    grid/src/civic/parcel-store.ts,
    grid/test/civic/interior-tree.test.ts
  </files>
  <read_first>
    grid/src/civic/types.ts (Parcel/Structure shapes from Phase 58 — surgical extension only),
    grid/src/civic/parcel-registry.ts (Phase 58 purchase/build/setEntryPolicy state-transition surface),
    grid/src/civic/parcel-store.ts (Phase 58 persistPurchase/persistBuild/persistEntryPolicy + hydrate — DB-first write-through pattern to MIRROR),
    grid/src/civic/furniture.ts (isValidFurniture — the validation point extendInterior reuses),
    grid/test/civic/interior-tree.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Extend `grid/src/civic/types.ts` (surgical):
    - add `interior?: { areas: Array<{ name: string; objects: Array<{ kind: string; class: 'mirror' | 'functional'; state?: string }> }> }` to `Structure` (D-NH-02 Smallville-shaped).
    - add `condition: 'maintained' | 'worn' | 'derelict'` (default maintained), `last_upkeep_tick?: number`, `missed_periods: number` (default 0) to `Parcel`.
    - keep all existing fields/exports intact.

    `grid/src/civic/parcel-registry.ts` — add `extendInterior(address, ownerDid, { area, kind })`:
    - require the caller to be the parcel owner (`not_owner` otherwise — the route also enforces this; defense in depth).
    - `isValidFurniture(kind, structure.type)` → on false throw `invalid_furniture`.
    - find-or-create the `area` in `structure.interior.areas`, append `{ kind, class }` (class from the catalog entry; `state?` optional, defaults undefined).
    - return the mutated `Structure`. The tree lives ONLY in registry/DB state — it is NEVER passed to any append-* producer (only `object_class`/`object_kind` cross, wired in Wave 4).

    `grid/src/civic/parcel-store.ts` — add writers mirroring the Phase 58 DB-first pattern:
    - `persistInterior(parcel)` — UPDATE `structure_interior` (JSON) DB-first; the caller mirrors into memory afterward.
    - `persistCondition(parcel)` — UPDATE `condition` + `missed_periods` (used Wave 4).
    - `persistUpkeep(parcel)` — UPDATE `last_upkeep_tick` + `missed_periods` (used Wave 4).
    - `persistReclaim(parcel)` — UPDATE owner→NULL + clear `structure_interior` + reset `condition='maintained'` + `missed_periods=0` (used Wave 4).
    - extend `hydrate(registry)` to READ the four new columns into the registry on boot (interior JSON → `structure.interior`; condition/last_upkeep_tick/missed_periods → Parcel fields).

    Un-skip `interior-tree.test.ts` and make it green: extend mutates the tree on a home; rejects unknown kind (`invalid_furniture`); rejects mirror-in-non-home; interior contents are never handed to a producer.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/interior-tree civic/furniture-catalog 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "interior\\?:|areas|condition|missed_periods" grid/src/civic/types.ts` shows the new Structure/Parcel fields
    - `grep -n "extendInterior" grid/src/civic/parcel-registry.ts` shows the method
    - `grep -nE "persistInterior|persistCondition|persistUpkeep|persistReclaim" grid/src/civic/parcel-store.ts` shows the 4 writers
    - `grep -nE "structure_interior|condition|last_upkeep_tick|missed_periods" grid/src/civic/parcel-store.ts` confirms hydrate reads the new columns
    - interior-tree.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/interior-tree civic/furniture-catalog` is green
    - no `clock.onTick` subscription added (single-onTick invariant)
  </acceptance_criteria>
  <done>Structure carries the interior tree; extendInterior validates + mutates Grid-side; ParcelStore persists/hydrates the v39 columns DB-first; interior-tree.test.ts green.</done>
</task>

</tasks>

<success_criteria>
- Migration v39 added and reversible; civic_parcels carries structure_interior/condition/last_upkeep_tick/missed_periods
- Upkeep constants + upkeepDue centralized in founding-law.ts (single patch point)
- furniture.ts is the closed v1 catalog + single isValidFurniture gate (mirror-only-in-home)
- Structure carries the interior tree; ParcelRegistry.extendInterior validates + mutates Grid-side; interior never handed to a producer
- ParcelStore persists/hydrates the new columns DB-first; occupants stay memory-only
- furniture-catalog.test.ts + interior-tree.test.ts un-skipped and green
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-02-SUMMARY.md`.
</output>
