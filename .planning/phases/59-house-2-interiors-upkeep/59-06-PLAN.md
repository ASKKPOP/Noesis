---
phase: 59
plan: 06
type: execute
wave: 5
depends_on: [59-04]
files_modified:
  - brain/src/noesis_brain/rpc/types.py
  - brain/src/noesis_brain/wire/client.py
  - brain/src/noesis_brain/prompts/system.py
  - dashboard/src/components/worldmap/OrbitalGenesisMap.tsx
  - dashboard/src/app/worldmap/orbital/page.tsx
  - brain/test/test_civic_interior_verbs.py
  - dashboard/src/components/worldmap/__tests__/interior-viewer.test.tsx
autonomous: true
requirements: [R-59-10, R-59-11]
tags: [nous-house, house-2, brain, action-types, interior-viewer, dashboard, my-places]

must_haves:
  truths:
    - "rpc/types.py ActionType gains extend_interior(area, kind) + view_interior(parcel); wire/client.py dispatches them to POST interior/extend and GET interior; verbs are capabilities (no autoplay loop)"
    - "The my_places prompt block in prompts/system.py surfaces each owned house's condition (maintained/worn/derelict) and pending upkeep cost so the Nous feels upkeep pressure"
    - "OrbitalGenesisMap: click an owned/open non-derelict module -> interior view rendering the tree (mirror static meshes, functional highlighted), condition styling; humans see only entry-policy-permitted open structures, exteriors otherwise; derelict shows closed"
    - "Existing /worldmap city view + Phase 58 orbital exterior are UNCHANGED (additive only)"
  artifacts:
    - path: "brain/src/noesis_brain/rpc/types.py"
      provides: "ActionType extend_interior {area, kind} + view_interior {parcel} with metadata shapes"
    - path: "brain/src/noesis_brain/prompts/system.py"
      provides: "my_places block extended with condition + pending upkeep cost"
    - path: "dashboard/src/components/worldmap/OrbitalGenesisMap.tsx"
      provides: "click-to-enter interior viewer (mirror static, functional highlighted) + condition styling + derelict-closed"
  key_links:
    - from: "ActionType.extend_interior / view_interior"
      to: "POST :id/interior/extend / GET :id/interior"
      via: "GridWireClient dispatch (capabilities, no autoplay)"
      pattern: "extend_interior|view_interior"
    - from: "owned house condition + upkeepDue"
      to: "my_places prompt block"
      via: "prompt builder surfaces upkeep pressure"
      pattern: "my_places|condition|upkeep"
---

<objective>
Give the Nous interior capabilities (not scripts) and make upkeep pressure FELT: new `ActionType` verbs dispatched to the Grid interior routes, and a `my_places` block that surfaces each owned house's condition + pending upkeep cost. Render the interior viewer in the dashboard orbital map (mirror static, functional highlighted, condition styling), additive beside the existing exterior. Decision pressure stays with the Nous.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@grid/src/api/routes/civic-parcels.ts
@brain/src/noesis_brain/rpc/types.py
@brain/src/noesis_brain/wire/client.py
@brain/src/noesis_brain/prompts/system.py
@dashboard/src/components/worldmap/OrbitalGenesisMap.tsx

<context_note>
**Phase 58 established the patterns.** The Phase 58 ActionType civic-land verbs (`list_parcels/buy_parcel/build/visit/leave/set_entry_policy`) + the `my_places` prompt block already exist (in `prompts/system.py` for v3.1, the Phase 58 successor to `builder.py`). EXTEND them: add the two interior verbs beside the existing ones; extend the existing `my_places` block with condition + upkeep, don't create a parallel block. The OrbitalGenesisMap exterior + `/worldmap/orbital` route from Phase 58 stay — the interior viewer is a click-to-enter overlay on top.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: extend_interior / view_interior ActionType verbs + my_places upkeep pressure</name>
  <files>
    brain/src/noesis_brain/rpc/types.py,
    brain/src/noesis_brain/wire/client.py,
    brain/src/noesis_brain/prompts/system.py,
    brain/test/test_civic_interior_verbs.py
  </files>
  <read_first>
    brain/src/noesis_brain/rpc/types.py (Phase 58 ActionType civic-land members + metadata-shape convention),
    brain/src/noesis_brain/wire/client.py (GridWireClient dispatch to the civic-parcels routes),
    brain/src/noesis_brain/prompts/system.py (the Phase 58 my_places block to EXTEND)
  </read_first>
  <behavior>
    Add `ActionType` members to `brain/src/noesis_brain/rpc/types.py`:
    - `extend_interior` metadata `{area, kind}` (kind ∈ the furniture catalog; the Grid validates).
    - `view_interior` metadata `{parcel}`.

    `wire/client.py`: dispatch `extend_interior` → `POST /api/v1/civic/parcels/:id/interior/extend`; `view_interior` → `GET /api/v1/civic/parcels/:id/interior`. Verbs are CAPABILITIES — no autoplay loop; decision pressure stays with the Nous.

    `prompts/system.py`: EXTEND the existing `my_places` block so each owned house line surfaces its **condition** (maintained/worn/derelict) and **pending upkeep cost** (`upkeepDue`), e.g. "I own genesis:residential:0007 (home, worn, upkeep due: 8 Bios)". This makes upkeep pressure legible to the Nous. The block stays empty when the Nous owns no land.

    Author `brain/test/test_civic_interior_verbs.py` asserting: the two new ActionType values exist; metadata validation (extend_interior requires area+kind; view_interior requires parcel); dispatch routing maps each to the correct route; the `my_places` block renders condition + upkeep when the Nous owns land.
  </behavior>
  <verify>
    <automated>cd brain && .venv/bin/pytest test/test_civic_interior_verbs.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "extend_interior|view_interior" brain/src/noesis_brain/rpc/types.py` shows the new members
    - `grep -nE "interior/extend|:id/interior|interior" brain/src/noesis_brain/wire/client.py` shows the dispatch
    - `grep -nE "condition|upkeep" brain/src/noesis_brain/prompts/system.py` shows the extended my_places block
    - `cd brain && .venv/bin/pytest test/test_civic_interior_verbs.py` passes (ActionType values, metadata validation, dispatch routing, my_places condition+upkeep)
  </acceptance_criteria>
  <done>extend_interior/view_interior verbs dispatch to the interior routes as capabilities; my_places surfaces condition + upkeep cost; test_civic_interior_verbs.py green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dashboard interior viewer (mirror static, functional highlighted, condition styling)</name>
  <files>
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx,
    dashboard/src/app/worldmap/orbital/page.tsx,
    dashboard/src/components/worldmap/__tests__/interior-viewer.test.tsx
  </files>
  <read_first>
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx (Phase 58 exterior orbital map — the interior viewer is a click-to-enter overlay; do NOT restructure the exterior),
    dashboard/src/app/worldmap/orbital/page.tsx (Phase 58 additive route),
    grid/src/api/routes/civic-parcels.ts (GET :id/interior shape + the condition field on the feed)
  </read_first>
  <behavior>
    Extend `dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` with a click-to-enter interior view:
    - clicking an owned/open NON-derelict module opens an interior view that fetches `GET /api/v1/civic/parcels/:id/interior` and renders the tree: mirror furniture as STATIC meshes, functional furniture HIGHLIGHTED (it carries affordances).
    - condition shown as a visual state: maintained (normal), worn (faded/cracked styling), derelict (closed/boarded styling).
    - humans browse only entry-policy-permitted OPEN, non-derelict structures; otherwise they see the exterior. A derelict module shows a "closed" state and does not open.
    - the existing exterior map + `/worldmap` city view stay UNCHANGED (additive overlay only).

    Author `dashboard/src/components/worldmap/__tests__/interior-viewer.test.tsx` asserting: clicking an owned open module renders the interior tree (mirror vs functional distinguished); condition styling reflects maintained/worn/derelict; a derelict module shows closed and does not open the interior; no raw owner DID is rendered (only `owner_civic_did_hash`).
  </behavior>
  <verify>
    <automated>cd dashboard && npm run test:unit -- interior-viewer 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "interior|condition|derelict" dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` shows the interior viewer + condition styling
    - the interior viewer fetches `GET /api/v1/civic/parcels/:id/interior` and distinguishes mirror (static) vs functional (highlighted)
    - a derelict module renders closed and does not open the interior
    - `cd dashboard && npm run test:unit -- interior-viewer` passes
    - the existing /worldmap city view + Phase 58 orbital exterior are unchanged (additive)
  </acceptance_criteria>
  <done>OrbitalGenesisMap interior viewer renders the tree (mirror static, functional highlighted) with condition styling, entry-policy-gated for humans; interior-viewer.test.tsx green.</done>
</task>

</tasks>

<success_criteria>
- extend_interior / view_interior ActionType verbs dispatch to the Grid interior routes as capabilities (no autoplay)
- my_places prompt block surfaces each owned house's condition + pending upkeep cost
- OrbitalGenesisMap interior viewer renders the tree (mirror static, functional highlighted) with condition styling, entry-policy-gated for humans; derelict shows closed
- Existing /worldmap + Phase 58 orbital exterior unchanged (additive)
- brain/test/test_civic_interior_verbs.py + dashboard interior-viewer.test.tsx green
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-06-SUMMARY.md`.
</output>
