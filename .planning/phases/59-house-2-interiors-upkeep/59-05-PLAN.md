---
phase: 59
plan: 05
type: execute
wave: 4
depends_on: [59-04]
files_modified:
  - grid/src/civic/upkeep-scanner.ts
  - grid/src/civic/parcel-registry.ts
  - grid/src/civic/parcel-store.ts
  - grid/src/genesis/launcher.ts
  - grid/test/civic/upkeep-scanner.test.ts
  - grid/test/civic/condition-ladder.test.ts
autonomous: true
requirements: [R-59-06, R-59-07, R-59-09]
tags: [nous-house, house-2, upkeep-scanner, condition-ladder, reclaim, single-ontick, tick-driven]

must_haves:
  truths:
    - "upkeep-scanner.onUpkeepTick(tick) is invoked fire-and-forget from the EXISTING clock.onTick block in launcher.ts (~line 468, mirroring governance.onTickClosed at line 484); NO second clock.onTick subscription anywhere in grid/src"
    - "Period-boundary assessment: an owned non-commons parcel whose last_upkeep_tick is a full UPKEEP_PERIOD_TICKS behind is debited upkeepDue via registry.transferOusia(owner -> TREASURY_DID), emits treasury.upkeep_collected, advances last_upkeep_tick; persisted DB-first"
    - "Insufficient funds advances the ladder maintained->worn->derelict and emits zoning.condition_changed; paying upkeep resets missed_periods=0 and condition='maintained'"
    - "At the 3rd missed period the parcel is reclaimed: ownership -> TREASURY_DID, structure+interior razed, occupants ejected, emits zoning.parcel_reclaimed {reason:'upkeep_default'} + a maintained reset on the treasury parcel"
    - "Polis Commons (owner_civic_did NULL, rings 0-1) are skipped entirely — never debited, never decayed"
  artifacts:
    - path: "grid/src/civic/upkeep-scanner.ts"
      provides: "onUpkeepTick(tick, deps): lazy period-boundary debit + ladder advance + reclaim; rides the existing tick callback"
    - path: "grid/src/civic/parcel-registry.ts"
      provides: "advanceCondition / resetCondition / reclaimToTreasury ladder state machine"
    - path: "grid/src/genesis/launcher.ts"
      provides: "fire-and-forget onUpkeepTick call inside the EXISTING clock.onTick block (no new subscription)"
  key_links:
    - from: "EXISTING clock.onTick block (launcher.ts ~468)"
      to: "upkeep-scanner.onUpkeepTick(event.tick)"
      via: "fire-and-forget, mirroring governance.onTickClosed at line 484 — NO new subscription"
      pattern: "onUpkeepTick"
    - from: "insufficient funds on a period boundary"
      to: "zoning.condition_changed then (at 3 missed) zoning.parcel_reclaimed"
      via: "advanceCondition -> reclaimToTreasury"
      pattern: "advanceCondition|reclaimToTreasury"
---

<objective>
Make ownership COST something. Implement the tick-driven upkeep scanner riding the EXISTING `clock.onTick` in `launcher.ts`, the condition ladder state machine on `ParcelRegistry`, the auto-debit + the three emitting events (`treasury.upkeep_collected`, `zoning.condition_changed`, `zoning.parcel_reclaimed`), and reclaim-to-treasury. Commons are exempt. Un-skip the upkeep-scanner + condition-ladder stubs.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@grid/src/genesis/launcher.ts
@grid/src/civic/parcel-registry.ts
@grid/src/civic/parcel-store.ts
@grid/src/civic/founding-law.ts
@grid/src/audit/append-treasury-upkeep-collected.ts
@grid/src/audit/append-zoning-condition-changed.ts
@grid/src/audit/append-zoning-parcel-reclaimed.ts

<context_note>
**Single-onTick invariant (R-H-03 / STATE.md):** `launcher.ts` has EXACTLY ONE `clock.onTick(...)` subscription (the block at ~line 468). Inside it, `governance.onTickClosed(event.tick)` is called fire-and-forget at line 484. Add the upkeep call the SAME way — `void upkeepScanner.onUpkeepTick(event.tick)` inside the existing block. Do NOT add a second `clock.onTick`. `setInterval` is also forbidden for this (the line ~579 comment). All timing is tick-based — the wallclock gate must stay green.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: condition ladder state machine on ParcelRegistry</name>
  <files>
    grid/src/civic/parcel-registry.ts,
    grid/src/civic/parcel-store.ts,
    grid/test/civic/condition-ladder.test.ts
  </files>
  <read_first>
    grid/src/civic/parcel-registry.ts (Phase 58 occupant/owner state + the Wave-1 condition/missed_periods fields),
    grid/src/civic/parcel-store.ts (Wave-1 persistCondition / persistReclaim writers),
    grid/src/civic/founding-law.ts (RECLAIM_GRACE_PERIODS: worn 1, derelict 2, reclaim 3),
    grid/test/civic/condition-ladder.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Add the ladder state machine to `grid/src/civic/parcel-registry.ts`:
    - `advanceCondition(address): 'worn' | 'derelict' | 'reclaimed'` — increments `missed_periods`; maps via `RECLAIM_GRACE_PERIODS` to `worn` (1), `derelict` (2), `reclaimed` (3). Returns the new state; the caller (scanner) emits the event + persists.
    - `resetCondition(address)` — on a PAID upkeep, sets `missed_periods=0` and `condition='maintained'`.
    - `reclaimToTreasury(address)` — sets owner → `TREASURY_DID` (NULL owner_civic_did), clears `structure.interior`, razes the structure, ejects occupants (clears the memory-only occupant set), resets `condition='maintained'` and `missed_periods=0` on the now-treasury parcel.
    - derelict gating: a `condition === 'derelict'` structure refuses `join` and (via the Wave-2 route) `GET interior` for non-owners (the route already checks; expose a `isClosedToVisitors(parcel)` helper if useful).

    Persist each transition DB-first via `store.persistCondition` / `store.persistReclaim` (the scanner calls these; the ladder methods mutate registry state, the scanner persists). Commons (`owner_civic_did` NULL) are never passed to `advanceCondition`.

    Un-skip `condition-ladder.test.ts` and make it green: maintained→worn→derelict→reclaimed on successive misses; reset on payment; derelict closes visitors (join refuse); reclaim razes + ejects + returns to treasury + resets.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/condition-ladder 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "advanceCondition|resetCondition|reclaimToTreasury" grid/src/civic/parcel-registry.ts` shows the 3 ladder methods
    - condition-ladder.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/condition-ladder` is green (ladder advance/reset, derelict closes visitors, reclaim razes+ejects+returns)
    - reclaim sets owner -> TREASURY_DID (NULL owner_civic_did), clears interior, ejects occupants, resets condition
  </acceptance_criteria>
  <done>Condition ladder state machine on ParcelRegistry (advance/reset/reclaim); derelict closes visitors; condition-ladder.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: upkeep scanner + launcher wiring (single-onTick) + 3 emits</name>
  <files>
    grid/src/civic/upkeep-scanner.ts,
    grid/src/genesis/launcher.ts,
    grid/test/civic/upkeep-scanner.test.ts
  </files>
  <read_first>
    grid/src/genesis/launcher.ts (the EXISTING clock.onTick block ~line 468 + governance.onTickClosed at line 484 — the precedent to mirror; DO NOT add a second subscription),
    grid/src/civic/founding-law.ts (UPKEEP_PERIOD_TICKS, upkeepDue),
    grid/src/registry/registry.ts (transferOusia(from, TREASURY_DID, amount) funds-move precedent),
    grid/src/audit/append-treasury-upkeep-collected.ts (+ condition-changed + parcel-reclaimed producers from Wave 3),
    grid/test/civic/upkeep-scanner.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/upkeep-scanner.ts` exporting `onUpkeepTick(tick, deps)` where `deps` carries the parcel registry, parcel store, nous registry (for `transferOusia` + balance read), and the audit chain. Lazy period-boundary assessment:
    - for each parcel that is OWNED and NON-commons (`owner_civic_did` not NULL): if `last_upkeep_tick` is undefined OR a full `UPKEEP_PERIOD_TICKS` (or more) behind `tick`, a period is DUE.
    - read the owner's Ousia balance from the nous registry; compute `upkeepDue(parcel)`.
    - **funded:** `nousRegistry.transferOusia(ownerDid, TREASURY_DID, due)` → emit `appendTreasuryUpkeepCollected({ amount_bios: due, owner_civic_did_hash, parcel_id, tick })` → set `last_upkeep_tick = tick` and `resetCondition` → `store.persistUpkeep`.
    - **insufficient:** `advanceCondition(parcel)` → emit `appendZoningConditionChanged({ condition, owner_civic_did_hash, parcel_id, tick })`; if the result is `reclaimed`, `reclaimToTreasury(parcel)` → emit `appendZoningParcelReclaimed({ former_owner_civic_did_hash, parcel_id, reason:'upkeep_default', tick })` and `store.persistReclaim`. Advance `last_upkeep_tick = tick` either way so the next assessment is the next boundary.
    - **commons** (`owner_civic_did` NULL): skipped entirely — never debited, never decayed.

    Wire the call in `grid/src/genesis/launcher.ts` INSIDE the EXISTING `clock.onTick` block (~line 468), fire-and-forget, exactly mirroring `governance.onTickClosed` at line 484: `void upkeepScanner.onUpkeepTick(event.tick, deps);`. Construct/resolve the scanner deps from the already-wired services (parcels + registry + audit). Add NO new `clock.onTick` subscription and NO `setInterval`.

    Un-skip `upkeep-scanner.test.ts` and make it green: period-boundary debit + `treasury.upkeep_collected`; insufficient → ladder advance + `zoning.condition_changed`; 3rd miss → reclaim + `zoning.parcel_reclaimed`; commons exempt; single-onTick (assert grid/src has exactly one `clock.onTick`).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/upkeep-scanner civic/condition-ladder && node scripts/check-wallclock-forbidden.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "clock.onTick" grid/src | wc -l` shows EXACTLY ONE subscription (the existing launcher block); the upkeep call is `onUpkeepTick`, not a new `clock.onTick`
    - `grep -n "onUpkeepTick" grid/src/genesis/launcher.ts` shows the fire-and-forget call inside the existing block
    - `grep -nE "transferOusia|TREASURY_DID|appendTreasuryUpkeepCollected|appendZoningConditionChanged|appendZoningParcelReclaimed" grid/src/civic/upkeep-scanner.ts` confirms the funds-move + 3 emits
    - upkeep-scanner.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/upkeep-scanner civic/condition-ladder` is green
    - `node scripts/check-wallclock-forbidden.mjs` exits 0 (all timing tick-based)
    - commons (owner NULL) never debited or decayed
  </acceptance_criteria>
  <done>upkeep-scanner rides the existing tick callback (single-onTick); period-boundary debit + 3 emits + reclaim; commons exempt; both stubs green; wallclock gate green.</done>
</task>

</tasks>

<success_criteria>
- onUpkeepTick rides the EXISTING clock.onTick block in launcher.ts (no new subscription — grep confirms exactly one clock.onTick in grid/src)
- Period-boundary auto-debit owner -> TREASURY_DID + treasury.upkeep_collected; persisted DB-first
- Insufficient funds advances maintained->worn->derelict + zoning.condition_changed; payment resets to maintained
- 3rd missed period reclaims: owner -> TREASURY_DID, structure+interior razed, occupants ejected, zoning.parcel_reclaimed {reason:'upkeep_default'} + maintained reset
- Polis Commons exempt entirely
- upkeep-scanner.test.ts + condition-ladder.test.ts un-skipped and green; node scripts/check-wallclock-forbidden.mjs exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-05-SUMMARY.md`.
</output>
