---
phase: 59
plan: 03
type: execute
wave: 2
depends_on: [59-02]
files_modified:
  - grid/src/api/routes/civic-parcels.ts
  - grid/src/api/policy.ts
  - grid/test/api/civic-interior-routes.test.ts
autonomous: true
requirements: [R-59-04, R-59-09]
tags: [nous-house, house-2, http-routes, interior, entry-policy, route-policy]

must_haves:
  truths:
    - "POST /api/v1/civic/parcels/:id/interior/extend: civic_did_required + owner-only; 403 not_owner; 422 invalid_furniture; 200 mutates the tree (via parcelRegistry.extendInterior + store.persistInterior)"
    - "GET /api/v1/civic/parcels/:id/interior: owner always; visitor only if structure open or allowlisted AND condition != derelict; derelict -> 403 closed_to_visitors; humans never see private interiors"
    - "ROUTE_DID_POLICY gains the two new route entries; node scripts/check-did-policy-coverage.mjs exits 0"
    - "The public map feed (GET parcels) still exposes exterior + condition only — NEVER the interior tree"
    - "The extend route emits zoning.interior_extended via the Wave-4 sole producer (typed seam until Wave 4 lands appendZoningInteriorExtended)"
  artifacts:
    - path: "grid/src/api/routes/civic-parcels.ts"
      provides: "POST interior/extend (owner, catalog-validated) + GET interior (entry-policy + derelict gated)"
    - path: "grid/src/api/policy.ts"
      provides: "ROUTE_DID_POLICY entries: POST interior/extend civic_did_required; GET interior entry-policy-gated"
  key_links:
    - from: "POST :id/interior/extend"
      to: "parcelRegistry.extendInterior + store.persistInterior + appendZoningInteriorExtended (Wave 4)"
      via: "owner-only, isValidFurniture-validated mutation then sole-producer emit"
      pattern: "extendInterior|interior_extended"
    - from: "GET :id/interior"
      to: "403 closed_to_visitors when condition === 'derelict'"
      via: "entry-policy + condition gate"
      pattern: "closed_to_visitors"
---

<objective>
Expose the interior over HTTP: an owner-only, catalog-validated `POST interior/extend` and an entry-policy-gated `GET interior` (derelict structures closed to visitors; humans never see private interiors). Interior CONTENTS stay Grid-side — only the `zoning.interior_extended` enums cross the chain (emit landed Wave 4; a typed seam until then). Add the two `ROUTE_DID_POLICY` entries.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@grid/src/api/routes/civic-parcels.ts
@grid/src/api/policy.ts
@grid/src/civic/parcel-registry.ts
@grid/src/civic/parcel-store.ts
@grid/src/civic/furniture.ts

<context_note>
**Phase 58 established the route shape.** `grid/src/api/routes/civic-parcels.ts` already resolves `parcelRegistry = services.parcels?.registry`, `store = services.parcels?.store`, the owner check, the `did:civic:noesis:human:*` defensive refusal, and the entry-policy (open/allowlist) precedent for join. The interior routes EXTEND this file — reuse the existing owner-resolution + entry-policy helpers; do not duplicate them.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: POST interior/extend + GET interior + ROUTE_DID_POLICY entries</name>
  <files>
    grid/src/api/routes/civic-parcels.ts,
    grid/src/api/policy.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (Phase 58: owner resolution, the entry-policy open/allowlist gate on join, the humans_cannot_own_land defensive check — REUSE these),
    grid/src/api/policy.ts (ROUTE_DID_POLICY shape; public / civic_did_required; entry-policy-gated read precedent),
    grid/src/civic/parcel-registry.ts (extendInterior from Wave 1; condition field; occupant/entry-policy helpers),
    grid/src/civic/furniture.ts (isValidFurniture — the single validation gate the route reuses)
  </read_first>
  <behavior>
    Extend `grid/src/api/routes/civic-parcels.ts`:

    `POST /api/v1/civic/parcels/:id/interior/extend` (`civic_did_required`):
    - resolve `parcelRegistry`/`store` (503 if absent, mirroring the Phase 58 guard).
    - owner-only: if the resolved DID is not the parcel owner → **403 `not_owner`**. (Phase 58's `did:civic:noesis:human:*` defensive refusal still applies via the tier.)
    - validate the body `{area, kind}` via `isValidFurniture(kind, structure.type)` → on false **422 `invalid_furniture`**.
    - `parcelRegistry.extendInterior(addr, ownerDid, {area, kind})` mutates the tree; `store.persistInterior(parcel)` writes DB-first; mirror into memory.
    - emit `zoning.interior_extended` (closed 4-tuple `{object_class, object_kind, parcel_id, tick}`) via the Wave-4 sole producer. **Until Wave 4** lands `appendZoningInteriorExtended`, wire a typed seam (a TODO-marked call site with the exact argument shape) so Wave 4 only fills the import — do NOT emit interior names/state.
    - 200 with the updated structure summary (NO raw interior contents beyond the area/object structure the OWNER is allowed to see).

    `GET /api/v1/civic/parcels/:id/interior` (entry-policy-gated):
    - owner → always allowed (full tree).
    - a visitor (other Nous) → allowed ONLY if the structure is open OR the visitor is on the entry allowlist AND `condition != 'derelict'`.
    - `condition === 'derelict'` → **403 `closed_to_visitors`** (for non-owners; the owner can still view their own derelict interior).
    - a human cookie session attempting a PRIVATE interior → refused (humans never see private interiors; D-NH-07). Public exterior remains via the existing GET parcels feed.

    The public map feed (`GET /api/v1/civic/parcels`) must continue to expose exterior + the new `condition` field ONLY — never the interior tree (assert this stays true).

    Add `ROUTE_DID_POLICY` entries in `grid/src/api/policy.ts`: `POST :id/interior/extend` → `civic_did_required`; `GET :id/interior` → the entry-policy-gated read tier consistent with the Phase 58 detail-read policy (so `check-did-policy-coverage.mjs` resolves it, not default-deny). The route-coverage gate must exit 0.
  </behavior>
  <verify>
    <automated>node scripts/check-did-policy-coverage.mjs && cd grid && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "interior/extend|:id/interior" grid/src/api/routes/civic-parcels.ts` shows both endpoints
    - `grep -nE "not_owner|invalid_furniture|closed_to_visitors" grid/src/api/routes/civic-parcels.ts` shows the three refusal codes
    - `grep -n "isValidFurniture" grid/src/api/routes/civic-parcels.ts` confirms the catalog gate is reused (not re-implemented)
    - `grep -nE "interior" grid/src/api/policy.ts` shows the 2 new ROUTE_DID_POLICY entries
    - `node scripts/check-did-policy-coverage.mjs` exits 0
    - the GET parcels feed does NOT serialize the interior tree (only exterior + condition)
    - typecheck passes
  </acceptance_criteria>
  <done>interior/extend (owner, catalog-validated) + GET interior (entry-policy + derelict gated) registered with policy entries; the feed stays exterior+condition only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: interior-route auth + gating tests</name>
  <files>
    grid/test/api/civic-interior-routes.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (the interior handlers from Task 1),
    grid/test/api/civic-interior-routes.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Un-skip `civic-interior-routes.test.ts` and make it green across the matrix:
    - POST extend: non-owner → 403 `not_owner`; unknown/mismatched kind → 422 `invalid_furniture`; owner + valid mirror-in-home → 200 mutating the tree.
    - GET interior: owner always; visitor on an OPEN non-derelict structure → 200; visitor on a derelict structure → 403 `closed_to_visitors`; a human attempting a private interior → refused.
    - the public feed (GET parcels) exposes `condition` but NOT the interior tree (assert the response body has no `interior`/`areas` key).

    Until Wave 4 lands `appendZoningInteriorExtended`, assert the extend route's emit SEAM carries only the closed 4-tuple shape `{object_class, object_kind, parcel_id, tick}` (no interior names/state) — a spy/typed assertion is acceptable; the actual producer wiring is verified in Wave 4.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- api/civic-interior-routes 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - civic-interior-routes.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- api/civic-interior-routes` is green for the full matrix (not_owner / invalid_furniture / closed_to_visitors / open-visit / feed-has-no-interior)
    - the test asserts the feed exposes condition but never the interior tree
  </acceptance_criteria>
  <done>interior-route auth + gating tests green; derelict closes visitors; feed never leaks the interior tree.</done>
</task>

</tasks>

<success_criteria>
- POST interior/extend (owner-only, catalog-validated, 403 not_owner / 422 invalid_furniture) + GET interior (entry-policy + derelict gated, 403 closed_to_visitors) registered
- ROUTE_DID_POLICY gains both entries; node scripts/check-did-policy-coverage.mjs exits 0
- The public feed exposes exterior + condition only — never the interior tree
- The extend route carries only the closed 4-tuple interior_extended shape (emit wired in Wave 4)
- civic-interior-routes.test.ts un-skipped and green
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-03-SUMMARY.md`.
</output>
