---
phase: 59
plan: 04
type: execute
wave: 3
depends_on: [59-03]
files_modified:
  - grid/src/audit/append-zoning-interior-extended.ts
  - grid/src/audit/append-zoning-condition-changed.ts
  - grid/src/audit/append-zoning-parcel-reclaimed.ts
  - grid/src/audit/append-treasury-upkeep-collected.ts
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/api/routes/civic-parcels.ts
  - grid/test/audit/append-zoning-interior-extended.test.ts
  - grid/test/audit/append-zoning-condition-changed.test.ts
  - grid/test/audit/append-zoning-parcel-reclaimed.test.ts
  - grid/test/audit/append-treasury-upkeep-collected.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
autonomous: true
requirements: [R-59-08, R-59-09, R-59-12]
tags: [nous-house, house-2, audit, sole-producer, allowlist-95, privacy-walker]

must_haves:
  truths:
    - "Four append-*.ts sole producers created, each cloning the Phase 58 triad: closed-tuple Object.keys(payload).sort() check, per-field validation, explicit no-spread reconstruction, payloadPrivacyCheck gate, single audit.append with the decided actorDid"
    - "Closed tuples EXACTLY: interior_extended {object_class, object_kind, parcel_id, tick}; condition_changed {condition, owner_civic_did_hash, parcel_id, tick}; parcel_reclaimed {former_owner_civic_did_hash, parcel_id, reason, tick}; upkeep_collected {amount_bios, owner_civic_did_hash, parcel_id, tick}"
    - "ALLOWLIST_MEMBERS appends the 4 under zoning.*/treasury.* with sole-producer reference comments; broadcast-allowlist.test.ts now GREEN at 95 with presence assertions"
    - "node scripts/check-sole-producer-discipline.mjs exits 0 (one producer per event, no foreign audit.append); no payload key matches FORBIDDEN_KEY_PATTERN"
    - "interior/extend route emits appendZoningInteriorExtended (fills the Wave-2 typed seam); interior CONTENTS never broadcast — only object_class/object_kind enums"
  artifacts:
    - path: "grid/src/audit/append-zoning-interior-extended.ts"
      provides: "Sole producer #92: closed 4-tuple {object_class, object_kind, parcel_id, tick}; actorDid = parcel_id"
    - path: "grid/src/audit/append-zoning-condition-changed.ts"
      provides: "Sole producer #93: closed 4-tuple {condition, owner_civic_did_hash, parcel_id, tick}; actorDid = owner_civic_did_hash"
    - path: "grid/src/audit/append-zoning-parcel-reclaimed.ts"
      provides: "Sole producer #94: closed 4-tuple {former_owner_civic_did_hash, parcel_id, reason, tick}; actorDid = parcel_id"
    - path: "grid/src/audit/append-treasury-upkeep-collected.ts"
      provides: "Sole producer #95: closed 4-tuple {amount_bios, owner_civic_did_hash, parcel_id, tick}; actorDid = parcel_id (mirrors #83)"
    - path: "grid/src/audit/broadcast-allowlist.ts"
      provides: "ALLOWLIST_MEMBERS grows 91 -> 95 with the 4 members + sole-producer comments"
  key_links:
    - from: "POST :id/interior/extend"
      to: "appendZoningInteriorExtended (#92)"
      via: "fills the Wave-2 typed seam — object_class/object_kind enums only, never interior contents"
      pattern: "appendZoningInteriorExtended"
    - from: "broadcast-allowlist.test.ts -> 95"
      to: "ALLOWLIST_MEMBERS 4 new members"
      via: "expected-RED from Wave 0 turns GREEN here"
      pattern: "toBe\\(95\\)"
---

<objective>
Land the four new sole-producer audit events. Create the 4 dedicated `append-*.ts` files cloning the Phase 58 triad, register the 4 members in `broadcast-allowlist.ts` with sole-producer comments under the pre-cleared `zoning.*` / `treasury.*` prefixes, and wire the interior/extend route to `appendZoningInteriorExtended` (filling the Wave-2 typed seam). Turn the Wave-0 allowlist test GREEN at 95. Un-skip the 4 append stubs. Interior CONTENTS never broadcast — only `object_class`/`object_kind` enums.
</objective>

<context>
@.planning/phases/59-house-2-interiors-upkeep/59-CONTEXT.md
@grid/src/audit/append-treasury-parcel-revenue.ts
@grid/src/audit/append-zoning-structure-built.ts
@grid/src/audit/broadcast-allowlist.ts
@grid/src/api/routes/civic-parcels.ts

<context_note>
**Clone the EXACT triad** from `append-treasury-parcel-revenue.ts` (#83) and `append-zoning-structure-built.ts` (#84): the JSDoc header naming the allowlist position + actorDid rationale; `PARCEL_ID_RE = /^[a-z0-9_-]+:[a-z_]+:\d{4}$/`; the type guard; per-field validation (positive int / non-negative int / HEX64_RE / enum membership); `Object.keys(payload).sort()` closed-tuple equality against `EXPECTED_KEYS`; explicit no-spread reconstruction of the payload; `payloadPrivacyCheck(...)`; a single `audit.append({ type, actorDid, payload })`. NEVER spread the input; NEVER include an interior name/state key.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: four append-*.ts sole producers (closed tuples + privacy walker)</name>
  <files>
    grid/src/audit/append-zoning-interior-extended.ts,
    grid/src/audit/append-zoning-condition-changed.ts,
    grid/src/audit/append-zoning-parcel-reclaimed.ts,
    grid/src/audit/append-treasury-upkeep-collected.ts
  </files>
  <read_first>
    grid/src/audit/append-treasury-parcel-revenue.ts (#83 — the canonical triad to clone: PARCEL_ID_RE, EXPECTED_KEYS sort check, no-spread reconstruction, payloadPrivacyCheck, actorDid = parcel_id),
    grid/src/audit/append-zoning-structure-built.ts (#84 — HEX64 owner-hash + name_hash discipline reference),
    grid/src/audit/broadcast-allowlist.ts (FORBIDDEN_KEY_PATTERN, HEX64_RE, payloadPrivacyCheck)
  </read_first>
  <behavior>
    Create four sole producers, each the full triad:

    `append-zoning-interior-extended.ts` (#92): closed 4-tuple `{object_class, object_kind, parcel_id, tick}`. `object_class ∈ {'mirror','functional'}`; `object_kind ∈` the furniture catalog kinds; `parcel_id` PARCEL_ID_RE; `tick` non-negative int. `actorDid = parcel_id`. NO interior name/state key — only the two enums. JSDoc notes interior contents NEVER cross.

    `append-zoning-condition-changed.ts` (#93): closed 4-tuple `{condition, owner_civic_did_hash, parcel_id, tick}`. `condition ∈ {'maintained','worn','derelict'}`; `owner_civic_did_hash` HEX64; `parcel_id` PARCEL_ID_RE; `tick` non-negative int. `actorDid = owner_civic_did_hash`.

    `append-zoning-parcel-reclaimed.ts` (#94): closed 4-tuple `{former_owner_civic_did_hash, parcel_id, reason, tick}`. HEX64; PARCEL_ID_RE; `reason ∈ {'upkeep_default'}`; non-negative tick. `actorDid = parcel_id` (the land returns to treasury).

    `append-treasury-upkeep-collected.ts` (#95): closed 4-tuple `{amount_bios, owner_civic_did_hash, parcel_id, tick}`. `amount_bios` positive int; HEX64; PARCEL_ID_RE; non-negative tick. `actorDid = parcel_id` (mirrors #83 land-attribution).

    Every producer: `Object.keys(payload).sort()` deep-equals the sorted `EXPECTED_KEYS` (a 5th key throws); explicit no-spread reconstruction; `payloadPrivacyCheck` gate; a single `audit.append`. No key matches `FORBIDDEN_KEY_PATTERN`.

    Un-skip the 4 append test stubs and make them green (closed-tuple, per-field validation, actorDid, no-FORBIDDEN-key, no interior-contents).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- audit/append-zoning-interior-extended audit/append-zoning-condition-changed audit/append-zoning-parcel-reclaimed audit/append-treasury-upkeep-collected && node scripts/check-sole-producer-discipline.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - the 4 append-*.ts files exist; each has `EXPECTED_KEYS`, an `Object.keys(payload).sort()` check, `payloadPrivacyCheck`, and exactly one `audit.append`
    - `grep -nE "FORBIDDEN_KEY_PATTERN" grid/src/audit/append-zoning-interior-extended.ts || true` — confirm NO interior-content key; only object_class/object_kind
    - the 4 append test stubs have no remaining `describe.skip`
    - `cd grid && npm run test -- audit/append-zoning-interior-extended audit/append-zoning-condition-changed audit/append-zoning-parcel-reclaimed audit/append-treasury-upkeep-collected` is green
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
  </acceptance_criteria>
  <done>4 sole producers created with the full triad + closed tuples; append stubs green; sole-producer-discipline gate green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: register 4 members in broadcast-allowlist (91 -> 95) + wire interior/extend emit</name>
  <files>
    grid/src/audit/broadcast-allowlist.ts,
    grid/src/api/routes/civic-parcels.ts,
    grid/test/audit/broadcast-allowlist.test.ts
  </files>
  <read_first>
    grid/src/audit/broadcast-allowlist.ts (ALLOWLIST_MEMBERS array at 91 — append the 4 under zoning.*/treasury.*; sole-producer reference-comment convention),
    grid/src/api/routes/civic-parcels.ts (the Wave-2 typed seam in the interior/extend handler to fill with appendZoningInteriorExtended),
    grid/test/audit/broadcast-allowlist.test.ts (Wave-0 updated test now asserting 95 — turn it GREEN)
  </read_first>
  <behavior>
    Append the 4 members to `ALLOWLIST_MEMBERS` in `grid/src/audit/broadcast-allowlist.ts`, each with a sole-producer reference comment (e.g. `// #92 sole producer: append-zoning-interior-extended.ts`), under the pre-cleared `zoning.*` / `treasury.*` prefixes. Order: `zoning.interior_extended` (#92), `zoning.condition_changed` (#93), `zoning.parcel_reclaimed` (#94), `treasury.upkeep_collected` (#95). Do NOT rename or reorder events 82–91.

    Wire the interior/extend route to emit `appendZoningInteriorExtended` — fill the Wave-2 typed seam with the real import + call, passing ONLY the closed 4-tuple (object_class/object_kind from the catalog entry, parcel_id, currentTick). Interior names/state are NEVER passed.

    The Wave-0 `broadcast-allowlist.test.ts` (already at 95 with presence assertions) now turns GREEN — confirm `ALLOWLIST.size === 95`, `ALLOWLIST_MEMBERS.length === 95`, and all 4 presence assertions pass.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- audit/broadcast-allowlist audit && node scripts/check-sole-producer-discipline.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "interior_extended|condition_changed|parcel_reclaimed|upkeep_collected" grid/src/audit/broadcast-allowlist.ts` shows the 4 members with sole-producer comments
    - `grep -n "appendZoningInteriorExtended" grid/src/api/routes/civic-parcels.ts` shows the real emit (seam filled)
    - `cd grid && npm run test -- audit/broadcast-allowlist` is GREEN at 95 with presence assertions for all 4
    - `cd grid && npm run test -- audit` exits 0
    - `node scripts/check-sole-producer-discipline.mjs` exits 0 (no foreign audit.append for the 4 events)
    - events 82-91 are unchanged in order and count
  </acceptance_criteria>
  <done>ALLOWLIST_MEMBERS at 95 with the 4 members + comments; interior/extend emits #92; broadcast-allowlist suite GREEN at 95; sole-producer gate green.</done>
</task>

</tasks>

<success_criteria>
- 4 sole-producer append-*.ts files (closed tuples, no-spread reconstruction, payloadPrivacyCheck, single audit.append, decided actorDid)
- ALLOWLIST_MEMBERS grows 91 -> 95 with the 4 members + sole-producer comments; events 82-91 untouched
- interior/extend route emits appendZoningInteriorExtended (enums only, no interior contents)
- broadcast-allowlist.test.ts GREEN at 95 with presence assertions; node scripts/check-sole-producer-discipline.mjs exits 0
- No payload key matches FORBIDDEN_KEY_PATTERN; all DIDs hashed HEX64
</success_criteria>

<output>
After completion, create `.planning/phases/59-house-2-interiors-upkeep/59-04-SUMMARY.md`.
</output>
