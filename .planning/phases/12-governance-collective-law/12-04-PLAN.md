---
phase: 12
slug: governance-collective-law
plan: 04
type: execute
wave: 4
depends_on: [12-00, 12-01, 12-02, 12-03]
files_modified:
  - scripts/check-governance-isolation.mjs
  - scripts/check-governance-plaintext.mjs
  - scripts/check-governance-weight.mjs
  - scripts/check-state-doc-sync.mjs
  - dashboard/src/app/grid/governance/page.tsx
  - dashboard/src/app/grid/governance/governance-dashboard.tsx
  - dashboard/src/app/grid/governance/voting-history-modal.tsx
  - dashboard/src/app/grid/governance/use-governance-proposals.ts
  - dashboard/src/lib/governance-types.ts
  - dashboard/test/components/governance-dashboard.test.tsx
  - dashboard/test/components/governance-voting-history.test.tsx
  - dashboard/test/lib/governance-types.drift.test.ts
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - PHILOSOPHY.md
  - README.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [VOTE-05, VOTE-06, VOTE-07]
must_haves:
  truths:
    - "scripts/check-governance-isolation.mjs exits 0 when run against current tree"
    - "scripts/check-governance-plaintext.mjs exits 0; flags any forbidden body/text/content/etc. key in governance src"
    - "scripts/check-governance-weight.mjs exits 0; flags any weight/reputation/relationship_score/ousia_weight key"
    - "scripts/check-state-doc-sync.mjs recognizes the new 26-event allowlist count"
    - "Dashboard /grid/governance page renders open proposals list (H1+) without per-Nous breakdown"
    - "Voting history modal is hidden for H1–H4 callers and visible for H5"
    - "Dashboard governance types match Grid governance types byte-for-byte (drift detector passes)"
    - "STATE.md, ROADMAP.md, PHILOSOPHY.md, README.md, MILESTONES.md all reflect Phase 12 SHIPPED state in same commit"
  artifacts:
    - path: "scripts/check-governance-isolation.mjs"
      provides: "CI grep gate — no operator-events import in governance/**, no operator.* emit, no law.triggered from operator-tainted callers"
    - path: "scripts/check-governance-plaintext.mjs"
      provides: "CI grep gate — no body|text|content|description|rationale|proposal_text|law_text|body_text in governance src"
    - path: "scripts/check-governance-weight.mjs"
      provides: "CI grep gate — VOTE-06 lock; no weight|reputation|relationship_score|ousia_weight keys"
    - path: "dashboard/src/app/grid/governance/page.tsx"
      provides: "Next.js page for /grid/governance"
    - path: "dashboard/src/app/grid/governance/governance-dashboard.tsx"
      provides: "Client component — proposals list with SWR 2s polling"
    - path: "dashboard/src/app/grid/governance/voting-history-modal.tsx"
      provides: "Native <dialog> H5-only modal showing per-Nous voting history"
    - path: "dashboard/src/lib/governance-types.ts"
      provides: "Frontend mirror of Grid governance types (drift-detected)"
  key_links:
    - from: "scripts/check-governance-isolation.mjs"
      to: "grid/src/governance/**, grid/src/api/governance/**"
      via: "grep for forbidden import + forbidden audit.append targets"
      pattern: "operator-events|operator\\.[a-z_]+"
    - from: "dashboard/src/app/grid/governance/page.tsx"
      to: "/api/v1/governance/proposals"
      via: "useGovernanceProposals SWR hook with 2000ms refreshInterval"
      pattern: "refreshInterval:\\s*2000"
    - from: "dashboard/src/lib/governance-types.ts"
      to: "grid/src/governance/types.ts"
      via: "drift detector test reads both files and asserts type-shape equivalence"
      pattern: "PROPOSAL_OPENED_KEYS|BALLOT_COMMITTED_KEYS"
    - from: ".planning/STATE.md"
      to: "scripts/check-state-doc-sync.mjs"
      via: "doc-sync rule: STATE.md updates same commit as allowlist count change"
      pattern: "26 events|22 events"
---

<objective>
Land the three governance CI gates, the Dashboard governance page (H1+ list, H2+ body, H5 voting-history modal), the type-drift detector, and execute the doc-sync rule (STATE.md + ROADMAP.md + PHILOSOPHY.md + README.md + MILESTONES.md updated atomically with Phase 12 SHIPPED).

Purpose: Lock the structural guarantees of Phase 12 (operator exclusion, body privacy, no vote-weighting) into CI; give operators the read-only governance surface VOTE-07 promises; close the loop on the CLAUDE.md doc-sync rule by ensuring no document lags reality after Phase 12 ships.

Output:
- 3 new mjs CI gates in scripts/
- 1 modification to scripts/check-state-doc-sync.mjs (22→26)
- Dashboard /grid/governance page + 3 client components + 1 SWR hook
- Drift detector test for governance types
- 5 source-of-truth docs updated atomically
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/MILESTONES.md
@PHILOSOPHY.md
@README.md
@.planning/phases/12-governance-collective-law/12-CONTEXT.md
@.planning/phases/12-governance-collective-law/12-RESEARCH.md
@.planning/phases/12-governance-collective-law/12-PATTERNS.md
@.planning/phases/12-governance-collective-law/12-UI-SPEC.md
@.planning/phases/12-governance-collective-law/12-VALIDATION.md
@.planning/phases/12-governance-collective-law/12-00-SUMMARY.md
@.planning/phases/12-governance-collective-law/12-01-SUMMARY.md
@.planning/phases/12-governance-collective-law/12-02-SUMMARY.md
@.planning/phases/12-governance-collective-law/12-03-SUMMARY.md

<!-- Existing patterns to clone -->
@scripts/check-whisper-plaintext.mjs
@scripts/check-wallclock-forbidden.mjs
@scripts/check-state-doc-sync.mjs
@scripts/check-relationship-graph-deps.mjs
@dashboard/src/app/grid/relationships/page.tsx
@dashboard/src/app/grid/relationships/relationship-graph.tsx
@dashboard/src/app/grid/relationships/relationship-graph.test.tsx
@dashboard/src/app/grid/hooks.ts

<interfaces>
<!-- Cumulative interfaces from prior waves; this plan consumes only. -->

From grid/src/governance/types.ts (Wave 0):
```typescript
export const PROPOSAL_OPENED_KEYS  = ['deadline_tick','proposal_id','proposer_did','quorum_pct','supermajority_pct','title_hash'] as const;
export const BALLOT_COMMITTED_KEYS = ['commit_hash','proposal_id','voter_did'] as const;
export const BALLOT_REVEALED_KEYS  = ['choice','nonce','proposal_id','voter_did'] as const;
export const PROPOSAL_TALLIED_KEYS = ['abstain_count','no_count','outcome','proposal_id','quorum_met','yes_count'] as const;
export const GOVERNANCE_FORBIDDEN_KEYS = [
  'text','body','content','description','rationale','proposal_text','law_text','body_text',
  'weight','reputation','relationship_score','ousia_weight',
] as const;
```

From grid/src/api/governance/routes.ts (Wave 3):
```typescript
// GET /api/v1/governance/proposals → { proposals: [{ proposal_id, status, opened_at_tick, deadline_tick, commit_count, reveal_count, outcome? }] }
// GET /api/v1/governance/proposals/:id/body → H2+ → { proposal_id, body_text, title_hash, proposer_did, quorum_pct, supermajority_pct, deadline_tick }
// GET /api/v1/governance/proposals/:id/ballots/history → H5 → { ballots: [{ voter_did, committed_at_tick, revealed_at_tick, choice }] }
```

From scripts/check-whisper-plaintext.mjs (clone target):
```javascript
// Walks grid/src/whisper/** with fs.readdirSync, regex-scans each file for forbidden keys,
// process.exit(1) with a helpful diff if any match found, else exit(0).
```

From scripts/check-state-doc-sync.mjs (modify target):
```javascript
// Asserts STATE.md, ROADMAP.md, PHILOSOPHY.md all reference the same allowlist count.
// Currently looks for "22 events" or "22 audit events" — Wave 4 changes to "26".
```

From dashboard/src/app/grid/relationships/relationship-graph.tsx (UI clone target):
```typescript
// SWR-based component pattern: useSWR(endpoint, fetcher, { refreshInterval: 2000 }).
// Returns React.Fragment with loading / error / empty / list states.
```
</interfaces>
</context>

<tasks>

<task type="auto" requirement="VOTE-05,VOTE-06">
  <name>Task 1 (W4-01..03): Three CI grep gates — isolation, plaintext, weight</name>
  <files>
    scripts/check-governance-isolation.mjs (new, ~120 LOC),
    scripts/check-governance-plaintext.mjs (new, ~80 LOC),
    scripts/check-governance-weight.mjs (new, ~70 LOC),
    scripts/check-state-doc-sync.mjs (modify — bump 22→26),
    package.json (modify — add three to "scripts" + CI runner)
  </files>
  <read_first>
    - scripts/check-whisper-plaintext.mjs end-to-end (canonical fs.readdirSync + regex + helpful-diff exit pattern)
    - scripts/check-wallclock-forbidden.mjs (similar pattern — confirm Node version assumptions)
    - scripts/check-state-doc-sync.mjs (full file — Wave 0 already nudged the count expectation; confirm Wave 4 needs no further change OR that the 22→26 update is here)
    - scripts/check-relationship-graph-deps.mjs (governance variant of: forbidden-import gate)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-11 (CI gates spec)
    - package.json "scripts" block (where to register the three new gates)
  </read_first>
  <behavior>
    **check-governance-isolation.mjs** — guards VOTE-05 (no operators in governance):
    - Walks `grid/src/governance/**` and `grid/src/api/governance/**`.
    - Forbidden patterns:
      1. `import.*from\s+['"]\.\.?/.*audit/operator-events` — no operator-events import.
      2. `audit\.append\(\s*['"]operator\.` — no operator.* event emit from governance modules.
      3. `from\s+['"]\.\.?/.*api/operator/governance-laws` — no import of operator law-management module.
    - Exits 0 if 0 hits across all forbidden patterns. Exits 1 with the offending file:line:match printed if any hit.
    - ALSO walks `grid/src/audit/operator-events.ts` (if exists) and confirms it never imports anything from `grid/src/governance/**` (reverse direction guard).

    **check-governance-plaintext.mjs** — guards T-09-12 (body privacy):
    - Walks `grid/src/governance/**`, `grid/src/api/governance/**`, `brain/src/noesis_brain/governance/**`.
    - Forbidden keys (literal-string match in source — clone whisper plaintext gate exactly): `text`, `body`, `content`, `description`, `rationale`, `proposal_text`, `law_text`, `body_text`.
    - **Allowlisted exceptions (must be enumerated explicitly):**
      - `body_text` is the column name in the governance_proposals migration AND the request body field on POST /proposals AND the property in build_propose_action — these locations need the literal. Allowlist BY filepath: `grid/src/db/schema.ts`, `grid/src/api/governance/routes.ts`, `grid/src/governance/store.ts`, `grid/src/governance/appendProposalOpened.ts`, `brain/src/noesis_brain/governance/proposer.py`. ANY OTHER occurrence of `body_text` is a violation.
      - `body` appears in HTTP body destructuring (`request.body`); regex must match only when key appears as a `payload.body` literal or `'body'` quoted, NOT as `request.body` parameter access. Tighten the regex accordingly.
    - Exits 0 if every match is on the allowlist; exits 1 otherwise with file:line and the matching string.

    **check-governance-weight.mjs** — guards VOTE-06 (no vote-weighting):
    - Walks `grid/src/governance/**`, `grid/src/api/governance/**`, `brain/src/noesis_brain/governance/**`.
    - Forbidden keys: `weight`, `reputation`, `relationship_score`, `ousia_weight`.
    - No allowlist exceptions in v2.2. Any occurrence is a violation.
    - Exits 0 / 1 with matching diagnostics.

    **check-state-doc-sync.mjs**: Wave 0 may have already updated 22→26 (check the file before editing). If still showing 22, bump to 26. Also bump the expected event names to include the four new entries: `proposal.opened`, `ballot.committed`, `ballot.revealed`, `proposal.tallied`.

    Acceptance criteria:
    - All three new scripts exit 0 against the current tree (after Waves 0–3 land).
    - Each script exits 1 with a non-empty stderr message when a deliberate violation is added (test by adding a temp string to a governance file, running the script, asserting exit code 1, then removing the temp).
    - package.json gains three entries: `"check:governance:isolation"`, `"check:governance:plaintext"`, `"check:governance:weight"` — pointing to the scripts. Also, the existing CI aggregator script (likely `"check:all"` or similar — find it and append) gets the three new entries.
  </behavior>
  <action>
    1. Open `scripts/check-whisper-plaintext.mjs` and read it end-to-end. Use it as a structural template — same imports (node:fs, node:path), same `walkDir` helper, same `process.exit(1)` shape with a numbered violation listing.

    2. Implement `check-governance-isolation.mjs` — three forbidden patterns enumerated above. The reverse-direction guard for operator-events.ts is a separate function that returns a list of violations and is OR-ed into the final exit decision.

    3. Implement `check-governance-plaintext.mjs` — clone whisper plaintext, swap the directory list and the forbidden-key list. Implement the filepath allowlist as a Set<string> and skip violations whose file is in the set. **Test the false-positive case** by temporarily adding `const x = 'body_text';` to a non-allowlisted governance source file, running the script, confirming exit 1, then removing.

    4. Implement `check-governance-weight.mjs` — simplest of the three (no allowlist). Identical structure to plaintext gate.

    5. Open `scripts/check-state-doc-sync.mjs`. If the count is still 22, change to 26 and add the four new event-name expectations to its allowlist of recognized events. If Wave 0 already did this (read the file first to confirm — DO NOT assume), document in the SUMMARY that no edit was needed.

    6. Update `package.json` — add three `"check:governance:*"` entries, and append all three to whatever aggregator script CI uses. Verify locally with `npm run check:governance:isolation && npm run check:governance:plaintext && npm run check:governance:weight`.

    Pitfalls:
    - The `body_text` allowlist is the trickiest part. Test it both ways: (1) running against the current tree exits 0; (2) deliberately adding `body_text` to a non-allowlisted file makes it exit 1.
    - Regex for `body` (as a key, not as `request.body`) is fragile. Prefer matching `['"]body['"]` (quoted key) and `payload\.body` (property access on a known forbidden context). Document the regex in a top-of-file comment.
    - Do NOT add a `--fix` flag or auto-correction. These gates are purely diagnostic.
    - All three scripts must run on Node 20+ stdlib only (no external deps). Match the existing scripts' Node version assumptions.
  </action>
  <verify>
    <automated>node scripts/check-governance-isolation.mjs && node scripts/check-governance-plaintext.mjs && node scripts/check-governance-weight.mjs && node scripts/check-state-doc-sync.mjs</automated>
  </verify>
  <done>
    All four scripts exit 0 against the current tree. False-positive test for plaintext gate confirmed (manual sanity-check, removed before commit). package.json scripts updated; CI aggregator includes new gates. SUMMARY documents whether check-state-doc-sync.mjs needed editing or had been pre-updated by Wave 0.
  </done>
</task>

<task type="auto" tdd="true" requirement="VOTE-07">
  <name>Task 2 (W4-04): Dashboard governance page — list, body view, H5 voting history</name>
  <files>
    dashboard/src/app/grid/governance/page.tsx (new, ~30 LOC),
    dashboard/src/app/grid/governance/governance-dashboard.tsx (new, ~180 LOC),
    dashboard/src/app/grid/governance/voting-history-modal.tsx (new, ~120 LOC),
    dashboard/src/app/grid/governance/use-governance-proposals.ts (new, ~50 LOC),
    dashboard/test/components/governance-dashboard.test.tsx (new),
    dashboard/test/components/governance-voting-history.test.tsx (new)
  </files>
  <read_first>
    - dashboard/src/app/grid/relationships/page.tsx (page wrapper shape — read-only operator views)
    - dashboard/src/app/grid/relationships/relationship-graph.tsx (SWR + 2s polling pattern; clone the entire file structure)
    - dashboard/src/app/grid/relationships/relationship-graph.test.tsx (vitest + msw test pattern; clone structurally)
    - dashboard/src/app/grid/hooks.ts (existing useSWR wrappers; add useGovernanceProposals there OR in a sibling file — pick the convention)
    - .planning/phases/12-governance-collective-law/12-UI-SPEC.md (canonical visual + tier matrix)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-09 (Dashboard tier matrix)
    - .planning/phases/12-governance-collective-law/12-PATTERNS.md (any documented divergences from relationships page — if pattern says "X does NOT exist", verify via Read tool BEFORE proceeding)
  </read_first>
  <behavior>
    **page.tsx** — Next.js 15 server component wrapper:
    - Renders `<GovernanceDashboard />` client component.
    - Reads `x-operator-tier` from request headers; passes as `initialTier` prop.
    - Pulls layout/chrome from the existing `relationships/page.tsx` shape — same header, same nav.

    **governance-dashboard.tsx** — main client component:
    - Uses `useGovernanceProposals()` SWR hook.
    - Renders a table of open proposals: proposal_id (truncated to 8 chars), status, opened_tick, deadline_tick, commit_count, reveal_count, outcome (if tallied).
    - Each row has a "View body" button → fetches `/api/v1/governance/proposals/:id/body` (sends `x-operator-tier` header). Disabled with tooltip "H2+ required" when tier < 2.
    - Each row has a "View votes" button → opens VotingHistoryModal. Visible only when tier ≥ 5; hidden (display:none, NOT just disabled) for H1–H4. **No vote/propose buttons under any condition** (VOTE-05).
    - Loading state: skeleton rows. Empty state: "No open proposals." Error state: error message + retry button.
    - SWR config: `{ refreshInterval: 2000, revalidateOnFocus: false }` — clone relationships pattern.

    **voting-history-modal.tsx** — H5-only modal:
    - Native HTML `<dialog>` element (no Radix/Headless UI — keep dependency-free per project convention).
    - Fetches `/api/v1/governance/proposals/:id/ballots/history` on open.
    - Renders table: voter_did (truncated to first 12 + "…"), committed_tick, revealed_tick, choice (or "—" if not revealed).
    - Closes via Esc key, backdrop click, or close button. Locks page scroll while open.
    - 403 response → modal shows "H5 required" message. (Defensive: page already hides the trigger button for non-H5, but the modal is callable via direct URL hash so it must self-guard.)

    **use-governance-proposals.ts** — SWR hook:
    ```typescript
    export interface ProposalSummary {
      proposal_id: string;
      status: 'open' | 'tallied';
      opened_at_tick: number;
      deadline_tick: number;
      commit_count: number;
      reveal_count: number;
      outcome?: 'passed' | 'rejected' | 'quorum_fail';
    }
    export function useGovernanceProposals(): { proposals: ProposalSummary[]; isLoading: boolean; error: Error | null };
    ```

    Acceptance criteria for tests:
    - governance-dashboard.test.tsx:
      - msw mocks `/api/v1/governance/proposals` returning two open proposals.
      - Renders with tier=1: list visible; "View votes" buttons NOT in DOM (querySelector returns null).
      - Renders with tier=2: "View body" buttons enabled.
      - Renders with tier=5: "View votes" buttons visible and clickable.
      - Loading state: shows skeleton.
      - Error state: msw returns 500; component shows error + retry.
      - Empty state: msw returns `{ proposals: [] }`; component shows "No open proposals."
    - governance-voting-history.test.tsx:
      - Modal closed by default.
      - Open via prop; fetches /history; renders rows.
      - 403 from API → renders "H5 required" message.
      - Esc key closes modal.
      - Backdrop click closes modal.
  </behavior>
  <action>
    1. Verify `dashboard/src/app/grid/relationships/page.tsx` exists (it does — confirmed via ls). The 12-PATTERNS.md note about it not existing is stale; the file is present. Document this in the SUMMARY.

    2. Create `page.tsx` — minimal server wrapper. Copy the relationships/page.tsx shape verbatim, swap the imported component.

    3. Create `governance-dashboard.tsx`:
       - Use `useGovernanceProposals()` (Task next-step in this same task).
       - Render the proposals table. Use Tailwind 4 utilities matching the relationships table styling.
       - Conditional render of "View votes" — wrap in `{tier >= 5 && (...)}`. Do NOT render-then-hide-with-CSS — the DOM should not contain the trigger for non-H5 callers (defense in depth on top of the API gate).
       - Body-fetch button uses fetch() with `headers: { 'x-operator-tier': String(tier) }`. Render the body in an inline expandable region (no separate modal needed for body since H2+ is broad).

    4. Create `voting-history-modal.tsx`:
       - Use the `<dialog>` ref pattern: `dialogRef.current?.showModal()` to open. Esc handled natively. Backdrop click via `onClick` checking `event.target === dialogRef.current`.
       - Inside, fetch the history on open. Show a loader, then the rows.
       - 403 branch: render an alert message; do NOT silently fail.

    5. Create `use-governance-proposals.ts`:
       - useSWR hook hitting `/api/v1/governance/proposals` with refreshInterval 2000.
       - Return `{ proposals, isLoading, error }`.
       - Type the response shape to match the Wave 3 route output.

    6. Create both test files. Use msw for HTTP mocks (clone the relationship-graph.test.tsx setup). Use `@testing-library/react` for rendering and assertions. Each test renders inside a `<TierProvider>` (or whatever the tier-prop convention is — check the existing pattern in hooks.ts and clone it).

    Pitfalls:
    - VOTE-05 is the cardinal rule for this UI. The dashboard MUST NOT contain any button or affordance that calls POST /proposals, POST /commit, or POST /reveal. Code review and the test for tier=5 should both verify there is no propose/commit/reveal button.
    - Tier mismatch between `x-operator-tier` header and the component prop is a possible bug source. Page.tsx is the single source — pass `initialTier` from server-component-side request headers, then trust it client-side.
    - Date.now() is forbidden in governance code per the wall-clock ban — but the Dashboard is presentation-layer, NOT governance core. Tick numbers come from the server. The Dashboard may format `opened_at_tick` as "tick N" but should NOT compute time-since values from wall clock. Document this in SUMMARY.
    - Native `<dialog>` is supported in all modern browsers (Next.js 15 / React 19 audience). Do NOT pull in a polyfill.
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run test/components/governance-dashboard.test.tsx test/components/governance-voting-history.test.tsx</automated>
  </verify>
  <done>
    Both test files green. Tier-1 render contains zero "vote" trigger DOM nodes; tier-5 render exposes the modal; modal handles 403 gracefully; SWR polls at 2s. The page renders successfully under `cd dashboard && npm run build` (no SSR errors). VOTE-05 hand-audit confirms: no propose/commit/reveal button anywhere in the governance directory.
  </done>
</task>

<task type="auto" tdd="true" requirement="VOTE-07">
  <name>Task 3 (W4-05): Type drift detector — Dashboard governance types ≡ Grid governance types</name>
  <files>
    dashboard/src/lib/governance-types.ts (new, ~50 LOC),
    dashboard/test/lib/governance-types.drift.test.ts (new, ~80 LOC)
  </files>
  <read_first>
    - grid/src/governance/types.ts (Wave 0 — canonical type definitions; this file is the source of truth)
    - dashboard/src/lib/ — locate existing type-mirror files if any (relationships, economy); use the same convention
    - dashboard/src/app/grid/relationships/relationship-graph.tsx (locate any similar type-import pattern)
  </read_first>
  <behavior>
    **governance-types.ts** — frontend mirror of Grid governance types:
    - Declares `BallotChoice`, `ProposalStatus`, `Outcome`, the four payload interfaces, and the four KEYS arrays — byte-for-byte identical to Grid.
    - Adds frontend-only types: `ProposalSummary` (shape returned by GET /proposals), `ProposalDetail` (GET /proposals/:id/body), `BallotHistoryEntry` (GET /history).
    - The frontend-only types use the canonical types as building blocks (`status: ProposalStatus`, etc.).

    **governance-types.drift.test.ts** — drift detector:
    - Reads `grid/src/governance/types.ts` as a string.
    - Reads `dashboard/src/lib/governance-types.ts` as a string.
    - Extracts each `KEYS` array literal from both files via regex (e.g., capture the array between `PROPOSAL_OPENED_KEYS = [` and `]`).
    - Asserts the extracted string content of each KEYS array is identical between the two files.
    - Asserts the four payload interfaces have the same key set (parse interface bodies, sort keys, compare).
    - Asserts `BallotChoice` union members are identical.
    - Asserts `GOVERNANCE_FORBIDDEN_KEYS` arrays are identical between the two files.

    The detector does NOT need full TS AST parsing — regex extraction of the literal arrays is sufficient. If the regex misses, we'd rather false-fail and force a manual reconciliation than false-pass and let drift accrue.

    Acceptance criteria:
    - Test passes when both files match.
    - Test fails when a developer edits one file but not the other.
    - Failure message lists the specific drift (e.g., "PROPOSAL_OPENED_KEYS in dashboard has extra: ['foo']; missing: ['title_hash']").
  </behavior>
  <action>
    1. Create `dashboard/src/lib/governance-types.ts` — copy each declaration from `grid/src/governance/types.ts`. Do NOT use `import` from the grid package; the frontend ships independently. The drift detector enforces equivalence.

    2. Create `dashboard/test/lib/governance-types.drift.test.ts`:
       - Use `node:fs` to read both source files.
       - Define a helper `extractKeysArray(source: string, name: string): string[]` that finds `${name} = [` and reads until `]`, splits on commas, trims quotes/whitespace.
       - Define a helper `extractInterfaceKeys(source: string, name: string): string[]` that finds `interface ${name} {` and reads field names until matching `}`.
       - One vitest test per artifact: 4 KEYS arrays + 4 payload interfaces + GOVERNANCE_FORBIDDEN_KEYS + BallotChoice union = 10 assertions.
       - Each assertion uses `expect(dashboardKeys.sort()).toEqual(gridKeys.sort())` so order-only differences also fail.

    3. Verify the test passes against the current tree (after Tasks 1-2 of this plan land).

    Pitfalls:
    - Don't use TS AST tools (e.g., ts-morph) — they're heavy and the regex approach is sufficient.
    - The regex must handle multi-line array literals (use `[\s\S]*?` non-greedy for the array body).
    - If grid/src/governance/types.ts uses `as const` after the array, strip it during comparison.
    - Comments inside the array literals will break naive splits — skip lines starting with `//`.
  </action>
  <verify>
    <automated>cd dashboard && npx vitest run test/lib/governance-types.drift.test.ts</automated>
  </verify>
  <done>
    Drift detector test green. Manually verified that editing PROPOSAL_OPENED_KEYS in only one of the two files makes the test fail with a useful diff message (sanity-check, then revert). The dashboard build (`cd dashboard && npm run build`) compiles cleanly with the new types module imported by governance-dashboard.tsx.
  </done>
</task>

<task type="auto" requirement="VOTE-01,VOTE-02,VOTE-03,VOTE-04,VOTE-05,VOTE-06,VOTE-07">
  <name>Task 4 (W4-06): Atomic doc-sync — STATE.md, ROADMAP.md, PHILOSOPHY.md, README.md, MILESTONES.md</name>
  <files>
    .planning/STATE.md (modify),
    .planning/ROADMAP.md (modify),
    PHILOSOPHY.md (modify),
    README.md (modify),
    .planning/MILESTONES.md (modify)
  </files>
  <read_first>
    - .planning/STATE.md (current Phase 12 status; "Accumulated Context" section — likely needs Phase 12 entry)
    - .planning/ROADMAP.md (Phase 12 entry — mark complete; bump allowlist count if listed)
    - PHILOSOPHY.md (search for "allowlist" — bump count and add four new event names; check §6 Economy and §7 Tier Discipline for any governance-relevant invariants to add)
    - README.md ("Current status" section — add v2.2 governance milestone)
    - .planning/MILESTONES.md (append Phase 12 SHIPPED entry following the format of prior phases)
    - CLAUDE.md (review the doc-sync rule one more time before editing — especially the "Examples of what to sync" matrix)
    - .planning/phases/11-mesh-whisper/11-04-PLAN.md (or whatever the doc-sync-task example is in Phase 11) for shape reference
  </read_first>
  <behavior>
    All five files updated atomically (single commit) per the CLAUDE.md doc-sync rule. The phase ships when the docs match reality.

    **STATE.md** — at the start of the file (Current Status block) and in Accumulated Context:
    - Current focus: Phase 12 SHIPPED → Phase 13 [next phase TBD] OPEN.
    - Accumulated Context: append a "Phase 12 governance lockdowns" subsection listing:
      - Allowlist now 26 events (4 new: proposal.opened, ballot.committed, ballot.revealed, proposal.tallied).
      - Operators read-only at all tiers including H5 (no propose/commit/reveal route exists for operators).
      - commit_hash = sha256(choice + '|' + nonce + '|' + voter_did) — pipe delimiters.
      - law.triggered now widened with enacted_by: 'collective' | 'operator'.
      - Three CI gates added: check-governance-isolation, check-governance-plaintext, check-governance-weight.
      - Tombstoned proposer/voter return 410 from all three governance POST routes; existing committed ballots tally normally.

    **ROADMAP.md** — Phase 12 entry:
    - Status: ✅ Shipped {date}.
    - Move from "Active" to "Shipped" section if those exist.
    - Bump phase total / allowlist running total as the file conventions dictate.
    - Mark all VOTE-01..07 requirements as Validated.
    - If a Phase 13 entry exists with placeholder goal, leave it; if not, do NOT invent one.

    **PHILOSOPHY.md** — three updates:
    1. Allowlist invariant block: bump 22 → 26; add four new event names to the enumerated list.
    2. §6 Economy: confirm the existing "no token-weighted voting" line still holds; add a citation to VOTE-06 + check-governance-weight.mjs.
    3. §7 Human Agency Tiers: add a one-line invariant: "Operators are read-only on governance at all tiers including H5; collective enactment is forensically distinguished by `enacted_by: 'collective'` on `law.triggered`." Cite VOTE-05 + T-09-15.
    4. (If a "non-negotiables" section exists) Add: "Governance modules MUST NOT import operator-events.ts; CI gate enforced." Cite check-governance-isolation.mjs.

    **README.md** — "Current status" section:
    - Add a v2.2 milestone bullet: "Phase 12 (Governance & Collective Law) — collective lawmaking via commit-reveal ballots; operators read-only at all tiers".
    - If a feature checklist exists with governance items, mark them shipped.
    - Quickstart section: NO new commands required (governance is autonomous Brain behavior + Dashboard read view); confirm no command-line additions.

    **MILESTONES.md** — append a new Phase 12 entry:
    - Date.
    - REQs validated: VOTE-01..07.
    - Allowlist count delta: 22 → 26.
    - Lessons learned bullets (extract from 12-RESEARCH.md and 12-PATTERNS.md if any are noteworthy).
    - Pointer to phase summary: `.planning/phases/12-governance-collective-law/`.

    Acceptance criteria:
    - `node scripts/check-state-doc-sync.mjs` exits 0 against the updated docs.
    - All five files reflect 26-event allowlist and Phase 12 SHIPPED.
    - No file claims pre-Phase-12 state (e.g., README still saying "22 events" is a violation).
    - `git diff --stat` shows all five files modified in the same commit (this is a manual commit-time check).
  </behavior>
  <action>
    1. Read all five files completely. Identify every line that mentions:
       - The allowlist count (22 / 23 / 24 / 25 — anything pre-Phase-12).
       - Phase 12 status (open / pending / in progress).
       - "Operator can vote" / "operator may propose" / similar incorrect statements (unlikely but check).
       - Existing law.triggered payload shape (must be widened with enacted_by mention).

    2. Apply edits in a single round per file. Use Edit tool, NOT Write — preserve unrelated content.

    3. Run `node scripts/check-state-doc-sync.mjs` after all five edits. If it exits non-zero, the docs are still inconsistent — re-read the script's expectations and fix.

    4. Verify with a final round: re-read each of the five files (sample sections only) to confirm the Phase 12 references are correct.

    Pitfalls:
    - The doc-sync rule is the most-violated rule in this project (see MEMORY.md "Doc-sync every task"). Resist the urge to defer "minor" docs.
    - PHILOSOPHY.md changes are worldview-level. Be conservative — add invariants that came from research/decisions, not opinions.
    - README.md changes should be surgical — this is the public face. Don't restructure unrelated sections.
    - MILESTONES.md format: read the most recent Phase 11 entry as template; clone byte-for-byte structure.
    - Do NOT commit yet — the planner workflow's git_commit step handles that. Just edit the files. The orchestrator's gsd-sdk commit call will pick all five up.
  </action>
  <verify>
    <automated>node scripts/check-state-doc-sync.mjs</automated>
  </verify>
  <done>
    All five files updated. `check-state-doc-sync.mjs` exits 0. Phase 12 SHIPPED status reflected consistently. PHILOSOPHY.md has the three new entries (allowlist count, operator-read-only invariant, no-operator-events-import invariant). README.md "Current status" mentions Phase 12. MILESTONES.md has the Phase 12 SHIPPED entry following the canonical format. Git status shows exactly these five files modified in this task plus the test/source files from Tasks 1-3.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator (browser) → Dashboard pages | Operator-tier header is the trust assertion; H1+/H2+/H5 gates enforced both client-side (UI) and server-side (API in Wave 3). |
| CI runner → repository tree | CI gates are the structural lock that prevents future regressions; gates run on every commit. |
| Documentation → reality | Doc-sync rule (CLAUDE.md): all source-of-truth docs match shipped behavior atomically. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | I (Information Disclosure) | check-governance-plaintext.mjs | mitigate | Gate scans all governance src for forbidden body/text/etc. keys. Allowlist is filepath-restricted to known-safe locations (schema, route, store, emitter, brain proposer). Any other occurrence fails CI. |
| T-09-14 | E (Elevation of Privilege) — vote-weighting reintroduction | check-governance-weight.mjs | mitigate | Gate scans governance src for weight/reputation/relationship_score/ousia_weight. No allowlist exceptions. |
| T-09-15 | R (Repudiation) — operator-as-collective | check-governance-isolation.mjs + Dashboard UI | mitigate | CI gate: governance modules MAY NOT import operator-events.ts; MAY NOT emit operator.* events. UI guard: Dashboard renders zero propose/commit/reveal triggers under any tier (including H5). Defense in depth: API also rejects (Wave 3). |
| T-09-16 | D (Denial of Service) — Dashboard SWR storm | Dashboard governance page | accept | SWR refreshInterval 2s with revalidateOnFocus:false bounds the load. Operator concurrency is small (≤5 in v2.2). Rate-limiting deferred to v2.3. |
| T-09-17 | T (Tampering) — Dashboard type drift | governance-types.drift.test.ts | mitigate | Drift detector test reads both Grid and Dashboard type files; fails CI if any KEYS array, payload interface, or forbidden-keys list diverges. Vitest suite runs on every commit. |
</threat_model>

<verification>
After all four tasks land:

```bash
# All three CI gates green
node scripts/check-governance-isolation.mjs
node scripts/check-governance-plaintext.mjs
node scripts/check-governance-weight.mjs
node scripts/check-state-doc-sync.mjs
node scripts/check-wallclock-forbidden.mjs   # regression — governance code did not introduce wall-clock

# Dashboard tests green
cd dashboard && npx vitest run test/components/governance-dashboard.test.tsx test/components/governance-voting-history.test.tsx test/lib/governance-types.drift.test.ts

# Dashboard build clean
cd dashboard && npm run build

# Full Grid + Brain regression
cd grid && npx vitest run --reporter=dot
cd brain && python -m pytest -q
```

Expected counts after Wave 4:
- Grid: 1147+ from Wave 3 (no new Grid tests in Wave 4).
- Brain: 513+ from Wave 3 (no new Brain tests in Wave 4).
- Dashboard: baseline + 3 new test files (~10–12 cases).
</verification>

<success_criteria>
- Three new CI gates exist, exit 0 against current tree, and false-positive sanity-tested.
- Dashboard /grid/governance page renders under H1, H2, and H5 with the documented tier-aware affordances.
- No propose/commit/reveal trigger DOM node exists in the Dashboard at any tier (VOTE-05 lock).
- Drift detector confirms Dashboard governance types match Grid governance types.
- STATE.md, ROADMAP.md, PHILOSOPHY.md, README.md, MILESTONES.md all reflect Phase 12 SHIPPED state — same commit.
- check-state-doc-sync.mjs exits 0.
- Allowlist count is 26 in every doc that references it.
- Phase 12 milestone closes: all VOTE-01..07 requirements validated.
- Full regression suite green (Grid 1147+, Brain 513+, Dashboard pass).
</success_criteria>

<output>
After completion, create `.planning/phases/12-governance-collective-law/12-04-SUMMARY.md` documenting:
- Files created and modified per task.
- Reference files actually read in each read_first phase.
- check-state-doc-sync.mjs status — whether Wave 0 had pre-updated 22→26 or Wave 4 needed to.
- The body_text allowlist final filepath set (Task 1) and rationale.
- VOTE-05 hand-audit result (no propose/commit/reveal trigger in Dashboard at any tier).
- Drift detector pass/fail history during development (any caught divergences).
- Full Phase 12 retrospective bullets (move to .planning/RETROSPECTIVE.md if appropriate per CLAUDE.md doc-sync rule).
- Confirmation that Phase 12 is SHIPPED end-to-end and ready for the orchestrator's verification step.
</output>
