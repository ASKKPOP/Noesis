---
phase: 12
plan: 00
wave: 0
plan_slug: 00-setup
wave_number: 0
type: execute
gap_closure: false
depends_on: []
files_modified:
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/governance/types.ts
  - grid/src/governance/config.ts
  - dashboard/src/lib/protocol/governance-types.ts
  - brain/src/noesis_brain/governance/__init__.py
  - brain/src/noesis_brain/governance/types.py
  - grid/src/db/schema.ts
  - grid/test/governance/governance-producer-boundary.test.ts
  - grid/test/governance/governance-commit-hash.test.ts
  - grid/test/governance/governance-tally.test.ts
  - grid/test/governance/governance-privacy-matrix.test.ts
  - grid/test/governance/governance-zero-diff.test.ts
  - grid/test/audit/allowlist-twenty-six.test.ts
  - scripts/check-state-doc-sync.mjs
  - scripts/check-wallclock-forbidden.mjs
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - PHILOSOPHY.md
autonomous: true
requirements: [VOTE-01, VOTE-02, VOTE-03, VOTE-04, VOTE-05, VOTE-06]
must_haves:
  truths:
    - "Broadcast allowlist contains exactly 26 events; positions 23..26 are proposal.opened, ballot.committed, ballot.revealed, proposal.tallied (zero-indexed 22..25), in that order"
    - "STATE.md Accumulated Context enumeration reflects 26-event allowlist with the 4 governance events at positions 23..26 and Phase 12 carry-forward invariants appended"
    - "scripts/check-state-doc-sync.mjs literal '22 events' → '26 events'; required array includes proposal.opened, ballot.committed, ballot.revealed, proposal.tallied"
    - "scripts/check-wallclock-forbidden.mjs root lists include 'grid/src/governance' (TIER_B_TS_ROOTS) and 'brain/src/noesis_brain/governance' (TIER_A_ROOTS)"
    - "grid/test/governance/ directory exists with five RED stub files that compile and fail as intended against missing emitters/crypto/tally/engine"
    - "grid/src/governance/types.ts exports closed-tuple payload interfaces (ProposalOpenedPayload, BallotCommittedPayload, BallotRevealedPayload, ProposalTalliedPayload) + matching alphabetical KEYS tuples + GOVERNANCE_FORBIDDEN_KEYS array"
    - "dashboard/src/lib/protocol/governance-types.ts is the FIFTH protocol mirror with TWO SYNC pointer comments (grid + brain)"
    - "grid/src/governance/config.ts exports Object.freeze({quorumPctDefault: 50, supermajorityPctDefault: 67, nonceHexLen: 32}) — no Date.now / Math.random"
    - "grid/src/db/schema.ts contains migration version 6 creating governance_proposals + governance_ballots tables; pre-existing migrations 1..5 byte-identical to pre-edit content"
    - "grid/test/audit/allowlist-twenty-six.test.ts is GREEN at length 26 with the four new event literals at indices 22..25"
  artifacts:
    - path: "grid/src/audit/broadcast-allowlist.ts"
      provides: "26-event ALLOWLIST_MEMBERS; GOVERNANCE_FORBIDDEN_KEYS const; extended FORBIDDEN_KEY_PATTERN"
      contains: "proposal.opened"
    - path: "grid/src/governance/types.ts"
      provides: "Four closed-tuple payload interfaces + matching alphabetical KEYS tuples + GOVERNANCE_FORBIDDEN_KEYS export"
      contains: "PROPOSAL_OPENED_KEYS"
    - path: "grid/src/governance/config.ts"
      provides: "Frozen voting defaults (quorum 50, supermajority 67, nonce hex length 32)"
      contains: "quorumPctDefault"
    - path: "dashboard/src/lib/protocol/governance-types.ts"
      provides: "Fifth dashboard protocol mirror with two SYNC pointers"
      contains: "SYNC: mirrors grid/src/governance/types.ts"
    - path: "brain/src/noesis_brain/governance/types.py"
      provides: "Brain-side type mirror (frozen dataclasses)"
      contains: "PROPOSAL_OPENED_KEYS"
    - path: "grid/src/db/schema.ts"
      provides: "Migration version 6 — governance_proposals + governance_ballots"
      contains: "governance_proposals"
    - path: "grid/test/governance/governance-producer-boundary.test.ts"
      provides: "RED stub — three-describe grep boundary test x4 events (clone of whisper-producer-boundary.test.ts)"
      contains: "KNOWN_CONSUMERS_PROPOSAL_OPENED"
    - path: "grid/test/governance/governance-commit-hash.test.ts"
      provides: "RED stub — sha256(choice|nonce|voter_did) cross-language fixture vector"
      contains: "commit_hash"
    - path: "grid/test/governance/governance-tally.test.ts"
      provides: "RED stub — pessimistic quorum + outcome matrix"
      contains: "computeTally"
    - path: "grid/test/governance/governance-privacy-matrix.test.ts"
      provides: "RED stub — forbidden keys (text|body|content|description|rationale|proposal_text|law_text|body_text|weight|reputation|relationship_score|ousia_weight)"
      contains: "GOVERNANCE_FORBIDDEN_KEYS"
    - path: "grid/test/governance/governance-zero-diff.test.ts"
      provides: "RED stub — replay regression for 0/1/5/10 reveal counts (chain-tail hash byte-stable)"
      contains: "computeChainTailHash"
    - path: "grid/test/audit/allowlist-twenty-six.test.ts"
      provides: "GREEN test asserting allowlist length 26 + index-22..25 literals"
      contains: "26"
    - path: "scripts/check-state-doc-sync.mjs"
      provides: "Regression gate bumped to 26 events"
      contains: "26 events"
    - path: "scripts/check-wallclock-forbidden.mjs"
      provides: "Tier-A/Tier-B roots extended to governance trees"
      contains: "grid/src/governance"
  key_links:
    - from: "grid/src/audit/broadcast-allowlist.ts"
      to: "grid/src/governance/types.ts"
      via: "PROPOSAL_OPENED_KEYS / BALLOT_COMMITTED_KEYS / BALLOT_REVEALED_KEYS / PROPOSAL_TALLIED_KEYS imports drive closed-tuple assertions in the (yet-to-ship) emitters"
      pattern: "PROPOSAL_OPENED_KEYS"
    - from: "scripts/check-state-doc-sync.mjs"
      to: ".planning/STATE.md"
      via: "gate asserts 26-event enumeration matches STATE.md block"
      pattern: "26 events"
    - from: "dashboard/src/lib/protocol/governance-types.ts"
      to: "grid/src/governance/types.ts"
      via: "SYNC header pointer + (future) governance-types.drift.test.ts detector landing in Wave 4"
      pattern: "SYNC: mirrors grid/src/governance/types.ts"
    - from: "grid/src/db/schema.ts"
      to: "(future) grid/src/governance/store.ts"
      via: "Migration v6 creates the two tables consumed by GovernanceStore in Wave 2/3"
      pattern: "governance_proposals"
---

<objective>
Establish Phase 12 foundation: bump broadcast allowlist 22→26 with `proposal.opened`, `ballot.committed`, `ballot.revealed`, `proposal.tallied` at positions 23..26 (zero-indexed 22..25) per D-12-01; ship the closed-tuple type module + dashboard fifth-mirror skeleton + Brain type mirror; ship MySQL migration v6 (governance_proposals + governance_ballots); drop five RED-stub governance tests that compile but fail (missing emitters/crypto/tally/engine); extend the wall-clock-forbidden gate to cover governance trees on both Grid and Brain sides; complete the atomic doc-sync mandated by CLAUDE.md (STATE.md + ROADMAP.md + PHILOSOPHY.md + `scripts/check-state-doc-sync.mjs` all in the SAME commit).

Purpose: Lock the allowlist invariant at 26 so every subsequent wave inherits the sole-producer contract; make the producer-boundary test RED-ready so Wave 2 turns it GREEN by landing the four emitters; wire three-tier plaintext, weight-key, and wall-clock invariants into CI from the first commit of this phase. Establish the DB schema for the governance store before any insert path exists.

Output: 1 modified allowlist, 1 modified DB schema, 6 new source/type files (grid/dashboard/brain type trio + grid governance config + grid governance package directory + brain package init), 5 new RED test stubs, 1 new GREEN allowlist-26 test, 2 modified CI gate scripts, 3 modified planning docs. Zero runtime behavior change yet beyond schema migration; all Grid/Brain tests outside `test/governance/` must remain green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-governance-collective-law/12-CONTEXT.md
@.planning/phases/12-governance-collective-law/12-RESEARCH.md
@.planning/phases/12-governance-collective-law/12-PATTERNS.md
@.planning/phases/12-governance-collective-law/12-VALIDATION.md

<interfaces>
<!-- Copy-verbatim analogs the executor MUST read before writing. -->

From grid/src/whisper/types.ts (Phase 11 shipped) — clone the closed-tuple + KEYS tuple pattern x4:
```ts
export interface NousWhisperedPayload {
  readonly ciphertext_hash: string;
  readonly from_did: string;
  readonly tick: number;
  readonly to_did: string;
}
export const WHISPERED_KEYS = ['ciphertext_hash', 'from_did', 'tick', 'to_did'] as const;  // alphabetical
```

Adapt to FOUR payloads in grid/src/governance/types.ts (alphabetical key order is mandatory):
```ts
// proposal.opened — 6 keys
export interface ProposalOpenedPayload {
  readonly deadline_tick: number;
  readonly proposal_id: string;       // UUID v4
  readonly proposer_did: string;
  readonly quorum_pct: number;        // 1..100
  readonly supermajority_pct: number; // 1..100
  readonly title_hash: string;        // sha256(body)[:32], lowercase hex
}
export const PROPOSAL_OPENED_KEYS = [
  'deadline_tick', 'proposal_id', 'proposer_did', 'quorum_pct', 'supermajority_pct', 'title_hash',
] as const;

// ballot.committed — 3 keys
export interface BallotCommittedPayload {
  readonly commit_hash: string;       // sha256 hex (64 chars)
  readonly proposal_id: string;
  readonly voter_did: string;
}
export const BALLOT_COMMITTED_KEYS = ['commit_hash', 'proposal_id', 'voter_did'] as const;

// ballot.revealed — 4 keys
export type BallotChoice = 'yes' | 'no' | 'abstain';
export interface BallotRevealedPayload {
  readonly choice: BallotChoice;
  readonly nonce: string;             // 32 hex chars
  readonly proposal_id: string;
  readonly voter_did: string;
}
export const BALLOT_REVEALED_KEYS = ['choice', 'nonce', 'proposal_id', 'voter_did'] as const;

// proposal.tallied — 6 keys
export type ProposalOutcome = 'passed' | 'rejected' | 'quorum_fail';
export interface ProposalTalliedPayload {
  readonly abstain_count: number;
  readonly no_count: number;
  readonly outcome: ProposalOutcome;
  readonly proposal_id: string;
  readonly quorum_met: boolean;
  readonly yes_count: number;
}
export const PROPOSAL_TALLIED_KEYS = [
  'abstain_count', 'no_count', 'outcome', 'proposal_id', 'quorum_met', 'yes_count',
] as const;

// Privacy + weight forbidden keys (D-12-04 + D-12-11 + VOTE-06)
export const GOVERNANCE_FORBIDDEN_KEYS = Object.freeze([
  'text', 'body', 'content', 'description', 'rationale',
  'proposal_text', 'law_text', 'body_text',
  'weight', 'reputation', 'relationship_score', 'ousia_weight',
] as const);
```

From grid/src/audit/broadcast-allowlist.ts (current, 22 events) — append discipline:
- The `ALLOWLIST_MEMBERS` array holds the authoritative order. Append the four new events at the END at indices 22, 23, 24, 25 (positions 23–26 one-indexed) IN THIS EXACT ORDER:
  - index 22 → `'proposal.opened'`
  - index 23 → `'ballot.committed'`
  - index 24 → `'ballot.revealed'`
  - index 25 → `'proposal.tallied'`
- Array length must be 26 after edit. Indices 0..21 are byte-identical to pre-edit content (no reordering).
- Extend `FORBIDDEN_KEY_PATTERN` regex alternation to include every GOVERNANCE_FORBIDDEN_KEYS literal not already present. (`text|body|content|message|...` may already exist from prior phases — DE-DUPE; do NOT re-add.)
- Preserve the Phase-11 attribution block. Add a Phase-12 attribution block exactly mirroring Phase 11's:
  "Phase 12 (VOTE-01..04): +4 governance events at positions 23..26 — closed payloads `proposal.opened {6}`, `ballot.committed {3}`, `ballot.revealed {4}`, `proposal.tallied {6}`. Sole producers in `grid/src/governance/append*.ts` (Wave 2). Per D-12-01 / CONTEXT-12."

From grid/test/whisper/whisper-producer-boundary.test.ts (Phase 11 shipped) — clone for governance-producer-boundary.test.ts, but with FOUR describe blocks (one per event), each replicating the three-assertion structure:
- Per event: KNOWN_CONSUMERS_<EVENT> = []  (initially empty; populated when Wave 2/3 consumers reference literals)
- Per event: file-exists probe for the corresponding `grid/src/governance/append*.ts` MUST FAIL at end of Wave 0 (the four files do not exist yet — that's why this is a RED stub).
- Forbidden siblings (zero hits in `grid/src/**`): `proposal.created`, `proposal.draft`, `proposal.submitted`, `ballot.cast`, `vote.committed`, `vote.cast`, `governance.opened`, `governance.tallied`. (D-12-01 forbidden-siblings list.)

From scripts/check-wallclock-forbidden.mjs (Phase 11 extended) — the extension pattern:
- Locate `TIER_A_ROOTS` (Python trees, blanket ban). Append `'brain/src/noesis_brain/governance'`.
- Locate `TIER_B_TS_ROOTS` (TypeScript trees, blanket ban except third-party). Append `'grid/src/governance'`.
- Dashboard governance tree is render-only counts/aggregates — does NOT need wall-clock ban (Date.now allowed for UI tick state). Do NOT extend to `dashboard/src/app/grid/governance/**`.

From scripts/check-state-doc-sync.mjs (Phase 11 shipped) — the two edits:
- Bump literal `"22 events"` → `"26 events"`.
- Append to the `required` array: `'proposal.opened'`, `'ballot.committed'`, `'ballot.revealed'`, `'proposal.tallied'` (preserve existing order convention).

From grid/src/db/schema.ts (Phase 11 shipped, current migration version 5) — add migration v6:
- Append a new entry to the migrations array with `version: 6`. Use the exact SQL from CONTEXT D-12-08:
```ts
{
  version: 6,
  description: 'governance_proposals + governance_ballots',
  up: async (conn: Pool) => {
    await conn.execute(`
      CREATE TABLE IF NOT EXISTS governance_proposals (
        grid_name        VARCHAR(63)  NOT NULL,
        proposal_id      VARCHAR(36)  NOT NULL,
        proposer_did     VARCHAR(255) NOT NULL,
        title_hash       VARCHAR(32)  NOT NULL,
        body_text        TEXT         NOT NULL,
        quorum_pct       TINYINT      NOT NULL DEFAULT 50,
        supermajority_pct TINYINT     NOT NULL DEFAULT 67,
        deadline_tick    INT UNSIGNED NOT NULL,
        status           VARCHAR(32)  NOT NULL DEFAULT 'open',
        outcome          VARCHAR(32),
        opened_at_tick   INT UNSIGNED NOT NULL,
        tallied_at_tick  INT UNSIGNED,
        PRIMARY KEY (grid_name, proposal_id),
        INDEX idx_status (grid_name, status)
      ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    `);
    await conn.execute(`
      CREATE TABLE IF NOT EXISTS governance_ballots (
        grid_name        VARCHAR(63)  NOT NULL,
        proposal_id      VARCHAR(36)  NOT NULL,
        voter_did        VARCHAR(255) NOT NULL,
        commit_hash      VARCHAR(64)  NOT NULL,
        revealed         TINYINT(1)   NOT NULL DEFAULT 0,
        choice           VARCHAR(16),
        nonce            VARCHAR(32),
        committed_tick   INT UNSIGNED NOT NULL,
        revealed_tick    INT UNSIGNED,
        PRIMARY KEY (grid_name, proposal_id, voter_did),
        INDEX idx_proposal (grid_name, proposal_id)
      ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
    `);
  },
}
```
- Use the exact pattern of migration version 5 (Phase 11) for the wrapping shape — read it first; do not invent a different signature.
- The migration must be idempotent (`CREATE TABLE IF NOT EXISTS`) so test environments that re-run migrations succeed.

STATE.md edit (atomic with this plan):
- In the "Broadcast allowlist (Phase 11 — post-ship, Plan 11-00)" block:
  - Change heading to "Broadcast allowlist (Phase 12 — pre-ship, Plan 12-00)".
  - Change "**22 events.**" to "**26 events.**".
  - Append four entries (23..26):
    - `23. proposal.opened ← NEW in Phase 12 (VOTE-01) — closed 6-key {deadline_tick, proposal_id, proposer_did, quorum_pct, supermajority_pct, title_hash}; sole producer grid/src/governance/appendProposalOpened.ts · D-12-01`
    - `24. ballot.committed ← NEW in Phase 12 (VOTE-02) — closed 3-key {commit_hash, proposal_id, voter_did}; sole producer grid/src/governance/appendBallotCommitted.ts · D-12-01`
    - `25. ballot.revealed ← NEW in Phase 12 (VOTE-03) — closed 4-key {choice, nonce, proposal_id, voter_did}; sole producer grid/src/governance/appendBallotRevealed.ts · D-12-01`
    - `26. proposal.tallied ← NEW in Phase 12 (VOTE-04) — closed 6-key {abstain_count, no_count, outcome, proposal_id, quorum_met, yes_count}; sole producer grid/src/governance/appendProposalTallied.ts · D-12-01`
  - Under "Hard v2.2 invariants (inherited, non-negotiable)", append THREE new bullets:
    - "Operator governance exclusion — operators cannot vote, propose, or tally at ANY tier including H5. CI gate `scripts/check-governance-isolation.mjs` enforces no `operator.*` emit from `grid/src/governance/**` and no import from `grid/src/audit/operator-events.ts`. (D-12-11, VOTE-05)"
    - "No vote-weighting — no `weight | reputation | relationship_score | ousia_weight` keys in any governance payload. CI gate `scripts/check-governance-weight.mjs` enforces. (D-12-11, VOTE-06)"
    - "commit_hash formula frozen — `sha256(choice + '|' + nonce + '|' + voter_did)` with PIPE delimiters. Identical formula in Brain (Python `hashlib`) and Grid (`node:crypto`). Cross-language fixture in grid/test/governance/governance-commit-hash.test.ts. (D-12-02)"
  - Update "Session Continuity" block: "Stopped at: Phase 12 Wave 0 shipped — allowlist 22→26; RED stubs in place; migration v6 ready" and "Next action: `/gsd-execute-phase 12` Wave 1 (commit-reveal crypto cross-language)".

ROADMAP.md edit (atomic with this plan):
- Phase 12 heading block: ensure the "allowlist 22→26 with proposal.opened, ballot.committed, ballot.revealed, proposal.tallied" note appears (consistent with Phase 11 format).
- Update phase status marker if convention requires (e.g., "in progress" → "Wave 0 shipped").

PHILOSOPHY.md edit (atomic with this plan):
- In the allowlist-invariant block, bump "22 events" → "26 events" and add the four governance events to the enumeration if PHILOSOPHY carries an explicit list.
- Under §6 Economy / §7 Human Agency Tiers, append a sentence: "Governance is intra-Nous only — operators cannot vote, propose, or tally at ANY tier including H5. The Grid retains body text in MySQL only; the audit chain retains only `title_hash`. (Phase 12 / VOTE-05 / D-12-11)."
</interfaces>
</context>

<tasks>

<task id="12-W0-01" type="auto" tdd="true">
  <name>Task 12-W0-01: Bump broadcast allowlist 22→26 with the four governance events + GOVERNANCE_FORBIDDEN_KEYS pattern extension</name>
  <requirement>VOTE-01, VOTE-02, VOTE-03, VOTE-04</requirement>
  <threat_ref>T-09-12, T-09-13, T-09-14</threat_ref>
  <read_first>
    - grid/src/audit/broadcast-allowlist.ts (current 22 events; the post-Phase-11 attribution block at lines 40–60 is the template for the Phase-12 block)
    - grid/test/audit/broadcast-allowlist.test.ts (current length-22 assertion; pattern for length-26 assertion in new test file)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-01 (forbidden-siblings list MUST match exactly)
  </read_first>
  <files>grid/src/audit/broadcast-allowlist.ts, grid/test/audit/allowlist-twenty-six.test.ts</files>
  <behavior>
    - ALLOWLIST_MEMBERS.length === 26 after edit
    - ALLOWLIST_MEMBERS[22] === 'proposal.opened'
    - ALLOWLIST_MEMBERS[23] === 'ballot.committed'
    - ALLOWLIST_MEMBERS[24] === 'ballot.revealed'
    - ALLOWLIST_MEMBERS[25] === 'proposal.tallied'
    - Indices 0..21 byte-identical to pre-edit content (no reordering)
    - GOVERNANCE_FORBIDDEN_KEYS exported as Object.freeze with the 12 literals from &lt;interfaces&gt;
    - FORBIDDEN_KEY_PATTERN includes every GOVERNANCE_FORBIDDEN_KEYS literal (de-duped against existing literals)
    - New test grid/test/audit/allowlist-twenty-six.test.ts asserts (a) length 26, (b) the four index-literal mappings, (c) the four forbidden-siblings (`proposal.created`, `ballot.cast`, `vote.committed`, `governance.opened`) are NOT in ALLOWLIST_MEMBERS
  </behavior>
  <action>
Append the four governance event literals at the end of ALLOWLIST_MEMBERS (indices 22..25) in the order specified in &lt;interfaces&gt;. Do NOT touch any other member or reorder. Add `GOVERNANCE_FORBIDDEN_KEYS` const and extend `FORBIDDEN_KEY_PATTERN` regex (alternation, de-dupe against existing literals from prior phases — `text|body|content|message|...` may already exist; add only what is genuinely missing). Append a Phase-12 attribution comment block mirroring Phase-11's verbatim structure — cite D-12-01 + VOTE-01..04 + the four sole-producer files (which land in Wave 2). Create new test file `grid/test/audit/allowlist-twenty-six.test.ts` with the four-assertion shape above (clone `grid/test/audit/broadcast-allowlist.test.ts` if a length-22 test exists; otherwise mirror Phase 11's allowlist-22 test). NO new allowlist entries beyond the four governance events (D-12-01 guardrail).
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx vitest run test/audit/allowlist-twenty-six.test.ts test/audit/broadcast-allowlist.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/audit/allowlist-twenty-six.test.ts` exits 0 with all assertions GREEN
    - `grep -c "proposal.opened" grid/src/audit/broadcast-allowlist.ts` ≥ 1
    - `grep -c "ballot.committed" grid/src/audit/broadcast-allowlist.ts` ≥ 1
    - `grep -c "ballot.revealed" grid/src/audit/broadcast-allowlist.ts` ≥ 1
    - `grep -c "proposal.tallied" grid/src/audit/broadcast-allowlist.ts` ≥ 1
    - `grep -c "GOVERNANCE_FORBIDDEN_KEYS" grid/src/audit/broadcast-allowlist.ts` ≥ 1
    - Pre-existing `test/audit/broadcast-allowlist.test.ts` still passes (length and prior literals)
  </acceptance_criteria>
  <done>
    Allowlist test green at length 26 with index-22..25 === the four governance events; GOVERNANCE_FORBIDDEN_KEYS exported; FORBIDDEN_KEY_PATTERN covers all 12 governance forbidden keys; pre-existing allowlist tests untouched and green.
  </done>
</task>

<task id="12-W0-02" type="auto" tdd="true">
  <name>Task 12-W0-02: Ship closed-tuple type module + dashboard fifth mirror + brain mirror + governance config</name>
  <requirement>VOTE-01, VOTE-02, VOTE-03, VOTE-04</requirement>
  <threat_ref>T-09-12, T-09-14</threat_ref>
  <read_first>
    - grid/src/whisper/types.ts (Phase 11 single-payload pattern — extend to 4 payloads)
    - grid/src/whisper/config.ts (Object.freeze + env-override pattern; governance config is a STATIC frozen object — no env override needed at MVP)
    - dashboard/src/lib/protocol/whisper-types.ts (FOURTH mirror with TWO SYNC pointers — clone shape)
    - brain/src/noesis_brain/whisper/types.py (frozen dataclass + KEYS tuple convention)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-02 (commit_hash formula — referenced in types via comment)
  </read_first>
  <files>grid/src/governance/types.ts, grid/src/governance/config.ts, dashboard/src/lib/protocol/governance-types.ts, brain/src/noesis_brain/governance/__init__.py, brain/src/noesis_brain/governance/types.py</files>
  <behavior>
    - grid/src/governance/types.ts exports the four payload interfaces and four KEYS tuples exactly as in &lt;interfaces&gt;; all keys are alphabetically ordered in their KEYS tuple
    - grid/src/governance/types.ts also exports `GOVERNANCE_FORBIDDEN_KEYS` (re-exported from the same source as broadcast-allowlist.ts uses, OR declared once here and imported into broadcast-allowlist.ts — choose ONE source-of-truth and document it)
    - grid/src/governance/config.ts exports `GOVERNANCE_CONFIG = Object.freeze({quorumPctDefault: 50, supermajorityPctDefault: 67, nonceHexLen: 32, choiceValues: ['yes','no','abstain']} as const)` — no env override at MVP per CONTEXT
    - dashboard/src/lib/protocol/governance-types.ts re-declares the same four payload interfaces + four KEYS tuples + BallotChoice/ProposalOutcome string-literal types (type-only; no runtime imports from grid/) with TWO SYNC-pointer header comments
    - brain/src/noesis_brain/governance/types.py declares the four payloads as @dataclass(frozen=True) + four KEYS tuples (Python tuples of strings)
    - brain/src/noesis_brain/governance/__init__.py re-exports ONLY the types (commit_reveal/voter/proposer absent until Wave 1/3)
    - Grid, Brain, and Dashboard typecheck after addition (tsc --noEmit / mypy / next typecheck all green)
    - No Date.now, Math.random, time.time, datetime.now in any of these files (wall-clock ban enforced by Task 12-W0-05)
  </behavior>
  <action>
Clone per &lt;interfaces&gt; analogs. Establish ONE source-of-truth for `GOVERNANCE_FORBIDDEN_KEYS`: declare it in `grid/src/governance/types.ts` and import it in `grid/src/audit/broadcast-allowlist.ts`. (If circular-import concerns arise, declare in broadcast-allowlist.ts and re-export from governance/types.ts — pick whichever yields a clean dependency arrow; document the choice in a comment.)

Place SYNC header on dashboard mirror with BOTH pointers per Phase 11 D-11-16 convention (which mirrors are now standard):
```ts
/**
 * SYNC: mirrors grid/src/governance/types.ts
 * SYNC: mirrors brain/src/noesis_brain/governance/types.py
 *
 * Drift detected by dashboard/test/lib/governance-types.drift.test.ts (lands in Wave 4).
 *
 * PRIVACY — VOTE-05/06 render surface:
 *   Body text NEVER enters this file or any downstream dashboard module.
 *   Operators are read-only at all tiers — no Vote/Propose/Reveal affordance is rendered.
 *   Vote-weighting keys (weight, reputation, relationship_score, ousia_weight) MUST NOT appear.
 *
 * This is the FIFTH dashboard protocol mirror (after audit, ananke, agency, whisper).
 * The post-fifth consolidation refactor into @noesis/protocol-types is logged as deferred per CONTEXT-11 D-11-16
 * and does NOT block Phase 12.
 */
```

Use path `dashboard/src/lib/protocol/governance-types.ts` (NOT `dashboard/src/protocol/...`) — matches existing audit/ananke/agency/whisper mirror convention per PATTERNS §dashboard mirror path note.

Config file MUST be statically frozen (no env override at MVP per CONTEXT — the defaults are the contract). Export type + tuple + interface from grid/src/governance/types.ts with no runtime-side-effectful code (no top-level function calls, no `Object.freeze` of mutable state outside the GOVERNANCE_FORBIDDEN_KEYS const).

For the Python mirror, use `from dataclasses import dataclass` and the `@dataclass(frozen=True)` decorator. Tuples of keys must be Python tuples (parentheses), NOT lists, to match the immutability guarantee of the TS `as const` tuples.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../dashboard &amp;&amp; npx tsc --noEmit &amp;&amp; cd ../brain &amp;&amp; python -c "from noesis_brain.governance.types import PROPOSAL_OPENED_KEYS, BALLOT_COMMITTED_KEYS, BALLOT_REVEALED_KEYS, PROPOSAL_TALLIED_KEYS; assert PROPOSAL_OPENED_KEYS == ('deadline_tick','proposal_id','proposer_did','quorum_pct','supermajority_pct','title_hash'); assert BALLOT_COMMITTED_KEYS == ('commit_hash','proposal_id','voter_did'); assert BALLOT_REVEALED_KEYS == ('choice','nonce','proposal_id','voter_did'); assert PROPOSAL_TALLIED_KEYS == ('abstain_count','no_count','outcome','proposal_id','quorum_met','yes_count')"</automated>
  </verify>
  <acceptance_criteria>
    - All three typecheckers pass (Grid tsc, Dashboard tsc, Brain Python import + tuple equality assertion)
    - Five files exist with correct exports
    - Dashboard mirror has TWO SYNC pointer comments
    - Brain `__init__.py` re-exports only types (no premature crypto/voter exports)
    - `grep -c "Date.now\|Math.random" grid/src/governance/types.ts grid/src/governance/config.ts` returns 0
    - `grep -c "time.time\|datetime.now" brain/src/noesis_brain/governance/types.py brain/src/noesis_brain/governance/__init__.py` returns 0
  </acceptance_criteria>
  <done>
    All three typecheckers pass; Python tuple-equality assertion passes for all four KEYS; five files exist with correct exports; no wall-clock literals; SYNC pointers in place on dashboard mirror.
  </done>
</task>

<task id="12-W0-03" type="auto" tdd="true">
  <name>Task 12-W0-03: Ship MySQL migration v6 (governance_proposals + governance_ballots)</name>
  <requirement>VOTE-01, VOTE-02, VOTE-03</requirement>
  <threat_ref>T-09-12</threat_ref>
  <read_first>
    - grid/src/db/schema.ts (read the entire migrations array; identify the current latest version number — should be 5 from Phase 11)
    - grid/test/db/schema.test.ts (if it exists — pattern for migration round-trip tests)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-08 (exact SQL — copy verbatim)
  </read_first>
  <files>grid/src/db/schema.ts</files>
  <behavior>
    - migrations array length increases by exactly 1 (new entry at the end with version: 6)
    - Migration v6 description: 'governance_proposals + governance_ballots'
    - Migration v6 `up` callback runs the two `CREATE TABLE IF NOT EXISTS` statements verbatim from CONTEXT D-12-08 (governance_proposals first, governance_ballots second)
    - Migrations 1..5 are byte-identical to pre-edit content (no reordering, no signature changes)
    - Existing schema tests pass (no regression)
    - Migration v6 is idempotent — re-running migrations on a populated DB does not throw
    - Schema migration runner detects version 6 and applies it on a fresh DB
  </behavior>
  <action>
Locate the migrations array in `grid/src/db/schema.ts`. Confirm current latest version is 5 (Phase 11 — if not 5, halt and report the actual version; the task description assumes 5 per STATE.md). Append a single new entry at the end of the array following the EXACT pattern of migration v5 — same field names, same callback signature, same error-handling style. Use the SQL from CONTEXT D-12-08 verbatim (governance_proposals + governance_ballots). Do NOT modify any prior migration entry. Do NOT introduce a `down` migration unless the existing pattern uses one (the project's migration runner is forward-only per Phase 11 conventions).

If a `MAX_MIGRATION_VERSION` constant exists, bump it from 5 to 6. If the migration runner uses an enum or hard-coded list of valid versions, update it consistently. Run a manual dry-check: read the final schema.ts top-to-bottom and confirm migration entries are sorted by version with no gaps.

Do NOT run live migrations against a real MySQL instance in this task — verification is via vitest unit tests on the schema module's exports, not integration. (Integration runs in CI on the migration test job, which is out-of-scope for Wave 0.)
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx tsc --noEmit &amp;&amp; npx vitest run test/db/ --reporter=dot 2>/dev/null || cd grid &amp;&amp; npx tsc --noEmit &amp;&amp; node -e "const m = require('./src/db/schema.ts'); /* tsc-only check */ console.log('schema typechecks')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "version: 6" grid/src/db/schema.ts` ≥ 1
    - `grep -c "governance_proposals" grid/src/db/schema.ts` ≥ 1
    - `grep -c "governance_ballots" grid/src/db/schema.ts` ≥ 1
    - `grep -c "CREATE TABLE IF NOT EXISTS" grid/src/db/schema.ts` increased by exactly 2 from pre-edit count
    - `cd grid && npx tsc --noEmit` exits 0
    - Pre-existing schema tests (if present) still GREEN
    - Migration version 5 entry is byte-stable (use git diff to verify no incidental edit)
  </acceptance_criteria>
  <done>
    Migration v6 appended; schema typechecks; both governance tables defined verbatim per CONTEXT; pre-existing migrations untouched; idempotent CREATE IF NOT EXISTS clauses in place.
  </done>
</task>

<task id="12-W0-04" type="auto" tdd="true">
  <name>Task 12-W0-04: Land five RED stubs in grid/test/governance/ (producer-boundary x4-events, commit-hash, tally, privacy-matrix, zero-diff)</name>
  <requirement>VOTE-01, VOTE-02, VOTE-03, VOTE-04, VOTE-06</requirement>
  <threat_ref>T-09-12, T-09-13, T-09-14</threat_ref>
  <read_first>
    - grid/test/whisper/whisper-producer-boundary.test.ts (Phase 11 — clone with FOUR describe blocks, one per event)
    - grid/test/whisper/whisper-crypto.test.ts (Phase 11 — clone for governance-commit-hash.test.ts: cross-language fixture pattern)
    - grid/test/whisper/whisper-privacy-matrix.test.ts (Phase 11 — clone scaffolding; substitute GOVERNANCE_FORBIDDEN_KEYS as the source-of-truth for forbidden literals)
    - grid/test/whisper/whisper-rate-limit.test.ts (pattern reference for ANOTHER kind of stub — but our stub is governance-tally.test.ts, with a different shape)
    - .planning/phases/12-governance-collective-law/12-CONTEXT.md §D-12-02 (commit_hash formula), §D-12-03 (tally outcome logic + pessimistic quorum)
  </read_first>
  <files>grid/test/governance/governance-producer-boundary.test.ts, grid/test/governance/governance-commit-hash.test.ts, grid/test/governance/governance-tally.test.ts, grid/test/governance/governance-privacy-matrix.test.ts, grid/test/governance/governance-zero-diff.test.ts</files>
  <behavior>
    - `grid/test/governance/` directory exists
    - **governance-producer-boundary.test.ts**: clones `whisper-producer-boundary.test.ts` with FOUR describe blocks (one per event). Per event: (1) literal appears only in allowlist + sole-producer file; (2) only the corresponding `append*.ts` calls `audit.append('event.literal', …)`; (3) forbidden-siblings list (proposal.created, ballot.cast, vote.committed, governance.opened, etc.) has zero hits in `grid/src/**`. Plus an explicit file-exists probe for each `grid/src/governance/append*.ts` (four probes). RED at end of W0 because the four files do not yet exist.
    - **governance-commit-hash.test.ts**: imports `commit_hash(choice, nonce, voter_did)` from `grid/src/governance/commit-reveal.ts` (which does NOT exist yet → import error → RED). Test asserts: `commit_hash('yes', '00000000000000000000000000000000', 'did:noesis:alice') === <expected fixture sha256 hex>`. The expected hex value MUST be computed by hand or via `node -e "require('node:crypto').createHash('sha256').update('yes|00000000000000000000000000000000|did:noesis:alice').digest('hex')"` and pasted into the test as a literal string. Also tests cross-language fixture compatibility (Brain Python parity check is in W1).
    - **governance-tally.test.ts**: imports `computeTally(reveals: BallotRevealedPayload[], committed: string[], quorumPct: number, supermajorityPct: number, totalNousCount: number)` from `grid/src/governance/tally.ts` (which does NOT exist yet → import error → RED). Stub asserts at minimum: 0 reveals + 0 committed + totalNous=10 + quorum=50 → `outcome: 'quorum_fail', quorum_met: false`; 5 yes + 0 no + 0 abstain + 5 unrevealed-committed + totalNous=10 + quorum=50 + supermajority=67 → `quorum_met: true, yes_count: 5, outcome: 'passed'`.
    - **governance-privacy-matrix.test.ts**: imports `GOVERNANCE_FORBIDDEN_KEYS` from `grid/src/governance/types.ts` (exists from W0-02 → import succeeds). Stub asserts the array is exactly the 12-element list from &lt;interfaces&gt;. RED-flips occur in W2 when the actual emitter throws on a forbidden-key payload.
    - **governance-zero-diff.test.ts**: imports `replayProposal(...)` from `grid/src/governance/replay.ts` (which does NOT exist → RED). The test fixture loads a stored chain-tail hash for `(proposal_id='fixture-A', reveals=[0,1,5,10])` and asserts the recomputed chain-tail hash from running the four-emitter pipeline on the fixture is byte-stable. Wave 2 lands the implementation.
    - RED failures are compilation/import errors, NOT runtime false assertions; every test file must itself typecheck (the test code compiles, but the imported module is missing → vitest reports FAIL with module-resolution error).
  </behavior>
  <action>
Copy `grid/test/whisper/whisper-producer-boundary.test.ts` to `grid/test/governance/governance-producer-boundary.test.ts`. Wrap the existing single-event structure in a top-level `describe('governance producer boundary — 4 events', () => { ... })` block, then create FOUR inner describe blocks — one per event — each cloning the three-assertion structure with substitutions:
  - `nous.whispered` → the event literal
  - `appendNousWhispered.ts` → the corresponding `append*.ts` filename
  - `KNOWN_CONSUMERS_WHISPERED` → `KNOWN_CONSUMERS_PROPOSAL_OPENED` / `_BALLOT_COMMITTED` / `_BALLOT_REVEALED` / `_PROPOSAL_TALLIED` (initial value `[]` for all four)
  - Forbidden-siblings list: use the per-event subset from D-12-01 (e.g., for `proposal.opened` → `['proposal.created','proposal.draft','proposal.submitted']`; for `ballot.committed` → `['ballot.cast','vote.committed','vote.cast']`; for `proposal.tallied` → `['governance.tallied','governance.opened']`).

For governance-commit-hash.test.ts:
```ts
import { describe, it, expect } from 'vitest';
import { computeCommitHash } from '../../src/governance/commit-reveal';  // RED: module missing in W0

describe('commit_hash — sha256(choice|nonce|voter_did) cross-language fixture', () => {
  it('matches Python hashlib output for the canonical fixture', () => {
    const choice = 'yes';
    const nonce = '00000000000000000000000000000000';  // 32 hex chars
    const voter_did = 'did:noesis:alice';
    // Expected: node -e "require('node:crypto').createHash('sha256').update('yes|00000000000000000000000000000000|did:noesis:alice').digest('hex')"
    const expected = '<COMPUTE-AND-PASTE-EXPECTED-HEX-HERE>';  // executor MUST replace with computed hash
    expect(computeCommitHash(choice, nonce, voter_did)).toBe(expected);
  });
});
```
The executor must compute the actual sha256 hex and paste it. Wave 1 will turn this GREEN by landing `computeCommitHash` in `grid/src/governance/commit-reveal.ts`.

For governance-tally.test.ts: define the function signature (imported but not yet implemented). Stub three describe blocks: zero-reveal, all-yes-passed, mixed-outcome. Use the pessimistic-quorum formula from D-12-03: `quorum_met = (revealed.length + unrevealed_committed.length) / totalNousCount >= quorumPct/100`.

For governance-privacy-matrix.test.ts: a single describe block asserting `GOVERNANCE_FORBIDDEN_KEYS.length === 12` AND each of the 12 literals from &lt;interfaces&gt; is present. (This stub will be GREEN at end of W0 because GOVERNANCE_FORBIDDEN_KEYS lands in W0-02 — that's intentional; the actual RED-flip happens in W2 when emitter-side enforcement is added in `governance-emitter-enforcement.test.ts`.) Mark this test with a comment explaining: "GREEN at W0 — purely a tuple-shape regression. The W2 emitter test adds runtime forbidden-key enforcement."

For governance-zero-diff.test.ts: import a (yet-to-exist) `runFixtureReplay` function from `grid/src/governance/replay.ts`. Test asserts the chain-tail hash for fixture-A is byte-stable across runs (the actual fixture-A chain hash is computed in W2 and pasted in then; for W0, set `expected = 'TBD-W2'` and call `expect(runFixtureReplay('fixture-A')).toBe(expected)` so the test fails meaningfully).

Keep stubs as lean as possible — no fixture data beyond what's needed to make them compile or fail at import. Wave 1/2 will expand to full matrices.
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx vitest run test/governance/ --reporter=dot 2>&amp;1 | tee /tmp/w0-gov-red.log; grep -qE "(failed|FAIL)" /tmp/w0-gov-red.log</automated>
  </verify>
  <acceptance_criteria>
    - All five test files compile (no TypeScript syntax errors in the test source)
    - Running the suite produces FAIL on at least 4 of the 5 tests (privacy-matrix may be GREEN since GOVERNANCE_FORBIDDEN_KEYS landed in W0-02 — that is documented and acceptable)
    - The 4 failing tests fail at module-resolution step or import-error step (not runtime false-assertion — verify by inspecting failure output)
    - File-exists probe for `grid/src/governance/appendProposalOpened.ts`, `appendBallotCommitted.ts`, `appendBallotRevealed.ts`, `appendProposalTallied.ts` all FAIL inside producer-boundary.test.ts
    - The expected sha256 hex literal is REAL (not `<COMPUTE-AND-PASTE-EXPECTED-HEX-HERE>`) — re-running `node -e "..."` produces the same hex
    - `grep -c "import" grid/test/governance/governance-commit-hash.test.ts` ≥ 1 — actually imports the missing module
  </acceptance_criteria>
  <done>
    All five test files execute (no TypeScript compile failure in the test files themselves). Four FAIL at module-resolution / file-exists. Privacy-matrix is GREEN (purely shape regression; the W2 emitter test handles enforcement). Passing all five at this stage is a bug — emitters and crypto must land in W1/W2 first.
  </done>
</task>

<task id="12-W0-05" type="auto" tdd="false">
  <name>Task 12-W0-05: Extend wall-clock-forbidden gate to governance trees + atomic doc-sync (STATE + ROADMAP + PHILOSOPHY + check-state-doc-sync.mjs)</name>
  <requirement>VOTE-01, VOTE-02, VOTE-03, VOTE-04, VOTE-05, VOTE-06</requirement>
  <threat_ref>T-09-12, T-09-13, T-09-14</threat_ref>
  <read_first>
    - scripts/check-wallclock-forbidden.mjs (Phase 11 — find TIER_A_ROOTS and TIER_B_TS_ROOTS arrays; append, do not replace)
    - scripts/check-state-doc-sync.mjs (Phase 11 — find the literal "22 events" and the `required` array)
    - .planning/STATE.md (find the Broadcast allowlist block and the Hard v2.2 invariants block)
    - .planning/ROADMAP.md (find Phase 12 heading)
    - PHILOSOPHY.md (find allowlist-invariant block; find §6 / §7 anchors)
    - CLAUDE.md "Documentation Sync Rule" — atomic single-commit obligation
  </read_first>
  <files>scripts/check-wallclock-forbidden.mjs, scripts/check-state-doc-sync.mjs, .planning/STATE.md, .planning/ROADMAP.md, PHILOSOPHY.md</files>
  <behavior>
    - scripts/check-wallclock-forbidden.mjs: TIER_A_ROOTS includes 'brain/src/noesis_brain/governance'; TIER_B_TS_ROOTS includes 'grid/src/governance'; dashboard governance tree NOT added (counts-only UI)
    - scripts/check-state-doc-sync.mjs: literal "22 events" → "26 events"; `required` array contains all four governance event literals in the order specified in &lt;interfaces&gt;
    - .planning/STATE.md: allowlist heading bumped to "Phase 12 — pre-ship, Plan 12-00"; "**22 events.**" → "**26 events.**"; four new enumeration entries (#23..#26) appended; THREE new invariant bullets added to "Hard v2.2 invariants" block; Session Continuity block updated
    - .planning/ROADMAP.md: Phase 12 heading reflects "allowlist 22→26 with the four governance events"
    - PHILOSOPHY.md: "22 events" → "26 events"; four governance events appended to enumeration; new sentence under §6/§7 about operator-governance-exclusion + body-text-Grid-only
    - `node scripts/check-state-doc-sync.mjs` exits 0 against the updated STATE.md
    - `node scripts/check-wallclock-forbidden.mjs` exits 0 against the current tree (governance dirs are essentially empty at W0 end — only types.ts + config.ts + __init__.py + types.py — none contain Date.now / time.time / Math.random / datetime.now)
    - All five files staged together in ONE git commit (the orchestrator handles the actual commit; this task ensures atomicity)
  </behavior>
  <action>
Make ALL five edits in ONE conceptual commit. The orchestrator commits each plan individually; ensuring all five files are staged together honors CLAUDE.md's atomic doc-sync mandate.

**scripts/check-wallclock-forbidden.mjs** — append the two roots per &lt;interfaces&gt;. Preserve alphabetical / append-at-end ordering per existing convention. No other changes.

**scripts/check-state-doc-sync.mjs** — bump literal `"22 events"` → `"26 events"`. Append to the `required` array four literals in this order: `'proposal.opened'`, `'ballot.committed'`, `'ballot.revealed'`, `'proposal.tallied'`. Match existing whitespace / quote style.

**STATE.md** — see &lt;interfaces&gt; block for exact edits. Update:
1. Allowlist block heading + count + enumeration (add #23..#26 with the templates from &lt;interfaces&gt;)
2. Hard v2.2 invariants — append THREE new bullets (operator-governance-exclusion, no-vote-weighting, commit_hash-formula-frozen)
3. Session Continuity block — "Stopped at: Phase 12 Wave 0 shipped — allowlist 22→26; RED stubs in place; migration v6 ready"; "Next action: `/gsd-execute-phase 12` Wave 1 (commit-reveal crypto cross-language)"

**ROADMAP.md** — locate Phase 12 heading block. Add or update the running-total marker to "allowlist 22→26 with `proposal.opened`, `ballot.committed`, `ballot.revealed`, `proposal.tallied`". If a per-phase deltas line exists, append there.

**PHILOSOPHY.md** — bump "22 events" → "26 events" wherever it appears. Append the four governance events to any explicit allowlist enumeration. Under the appropriate principle (likely §6 Economy or §7 Human Agency Tiers), add: "Governance is intra-Nous only — operators cannot vote, propose, or tally at ANY tier including H5. The Grid retains body text in MySQL only; the audit chain retains only `title_hash`. (Phase 12 / VOTE-05 / D-12-11)."
  </action>
  <verify>
    <automated>node scripts/check-state-doc-sync.mjs &amp;&amp; node scripts/check-wallclock-forbidden.mjs &amp;&amp; grep -q "26 events" scripts/check-state-doc-sync.mjs &amp;&amp; grep -q "proposal.opened" scripts/check-state-doc-sync.mjs &amp;&amp; grep -q "26 events" .planning/STATE.md &amp;&amp; grep -q "26 events" PHILOSOPHY.md &amp;&amp; grep -q "grid/src/governance" scripts/check-wallclock-forbidden.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node scripts/check-state-doc-sync.mjs` exits 0
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
    - All four governance event literals present in scripts/check-state-doc-sync.mjs `required` array
    - STATE.md "Hard v2.2 invariants" gains 3 new bullets (count via grep `- ` lines in that section pre/post)
    - PHILOSOPHY.md mentions "26 events" at least once and the four governance event names at least once each
    - ROADMAP.md Phase 12 block references the allowlist delta
    - All five files appear in `git status` simultaneously (verified by orchestrator commit)
  </acceptance_criteria>
  <done>
    Doc-sync gate exits 0; wall-clock-forbidden gate exits 0; all five files reference the 26-event allowlist with the four governance events; single-commit atomicity preserved per CLAUDE.md rule.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Grid allowlist | Four new governance event types cross policy boundary; must be allowlisted BEFORE any producer or operator-emit pathway lands |
| Grid ↔ Dashboard types | Hand-copied fifth mirror — drift risk; SYNC pointers + Wave 4 drift detector mitigates |
| Brain ↔ Grid types | Shared closed-tuple contract; Python frozen dataclass mirrors TS interface; cross-language sha256 fixture in Wave 1 |
| Operator → Governance module | Operators forbidden from emitting governance events at ANY tier; W4 CI gate (`check-governance-isolation.mjs`) enforces; W0 establishes the type/allowlist contract that the gate scans |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-12 | Information Disclosure | broadcast-allowlist.ts (governance event addition) | mitigate | Closed-tuple KEYS tuples enforce payload shape at Wave 2 emitter; W0 lands the immutable shape contract; PHILOSOPHY explicitly forbids body text in audit |
| T-09-13 | Tampering | grid/test/governance/governance-zero-diff.test.ts | mitigate | RED stub asserts byte-stable chain-tail hash for 0/1/5/10 reveal counts; Wave 2 implementation must produce identical hashes — replay regression guard |
| T-09-14 | Spoofing (vote-weighting injection) | grid/src/governance/types.ts (GOVERNANCE_FORBIDDEN_KEYS) | mitigate | weight/reputation/relationship_score/ousia_weight in forbidden-keys array from W0; W2 emitter check uses this array; W4 CI gate (`check-governance-weight.mjs`) is a third defense layer |
| T-09-15 | Elevation of Privilege | grid/src/audit/operator-events.ts (NOT touched) | mitigate | governance/types.ts has zero imports from operator-events.ts; W4 CI gate `check-governance-isolation.mjs` enforces the boundary; W0 establishes the package directory structure that the gate scans |
| T-09-16 | Repudiation | STATE.md / ROADMAP / PHILOSOPHY doc-sync | mitigate | Atomic single-commit edit per CLAUDE.md rule; doc-sync gate enforces 26-event consistency across all four sources; STATE.md "Hard v2.2 invariants" block gains three new bullets recording the new contracts |
</threat_model>

<verification>
After all five tasks land:
- `cd grid && npm test` — all pre-existing tests green; governance stubs RED as intended (4 of 5; privacy-matrix is GREEN by design)
- `cd grid && npx vitest run test/governance/ --reporter=dot` — 5 test files; 4 RED for module-resolution reasons, 1 GREEN (privacy-matrix shape regression)
- `cd grid && npx vitest run test/audit/allowlist-twenty-six.test.ts test/audit/broadcast-allowlist.test.ts` — both GREEN
- `cd grid && npx tsc --noEmit` — GREEN
- `cd dashboard && npx tsc --noEmit` — GREEN (fifth mirror compiles)
- `cd brain && python -c "from noesis_brain.governance.types import PROPOSAL_OPENED_KEYS; assert PROPOSAL_OPENED_KEYS == ('deadline_tick','proposal_id','proposer_did','quorum_pct','supermajority_pct','title_hash')"` — exit 0
- `node scripts/check-state-doc-sync.mjs` — exit 0
- `node scripts/check-wallclock-forbidden.mjs` — exit 0
- `grep -c "proposal.opened" grid/src/audit/broadcast-allowlist.ts` — ≥ 1
- `grep -c "26 events" scripts/check-state-doc-sync.mjs` — ≥ 1
- `grep -c "version: 6" grid/src/db/schema.ts` — ≥ 1
- Regression hash from Phase 11 zero-diff fixture UNCHANGED (Wave 0 ships no runtime emission path; no new chain entries emitted yet)
- Pre-Phase-12 test suites (grid/brain/dashboard outside test/governance/) byte-stable green — no regressions
</verification>

<success_criteria>
1. Broadcast allowlist at length 26 with the four governance events at indices 22..25 in the order specified; no other members moved
2. Migration version 6 added to grid/src/db/schema.ts with both governance tables (governance_proposals + governance_ballots) using the verbatim CONTEXT D-12-08 SQL
3. Five RED governance test stubs present (4 RED for missing modules, 1 GREEN for shape regression by design)
4. Six new source/type files (grid governance types + config, dashboard mirror, brain __init__ + types, allowlist-twenty-six test) typecheck clean across all three trees
5. Two CI gates (state-doc-sync + wall-clock-forbidden) pass against updated tree
6. Atomic doc-sync: STATE.md + ROADMAP.md + PHILOSOPHY.md + check-state-doc-sync.mjs + check-wallclock-forbidden.mjs in ONE commit, all referencing 26 events + the four governance events + the three new Hard v2.2 invariants
7. Pre-Phase-12 test suites (grid/brain/dashboard outside governance/) byte-stable green — no regressions
8. Zero-diff regression hash from Phase 11 unchanged (no runtime emission yet)
</success_criteria>

<output>
After completion, create `.planning/phases/12-governance-collective-law/12-00-SUMMARY.md` capturing the five task outcomes, the five RED/GREEN stub states, the migration v6 SQL diff, and the exact STATE.md / ROADMAP.md / PHILOSOPHY.md diffs so Wave 1 can reference them.
</output>
