---
phase: 046-government-v3
plan: 01
title: Wave 0 — allowlist lock + RED skeletons + migration v36
status: planned
---

# Plan 01 — Wave 0 (RED gates + migration)

## Goal
Lock the allowlist count at the Phase 46 target, stand up RED skeleton tests for the 6 producers + routes, and add migration v36. After this plan: allowlist test asserts 81 but src still has 75 (RED), skeleton tests fail (modules absent), migration test GREEN.

## Steps
1. **broadcast-allowlist.test.ts** — flip every `toBe(75)` → `toBe(81)` (count + members.length, ~3 sites). Add `describe('ALLOWLIST_MEMBERS Phase 46 (CIVGOV-06)')`:
   - `ALLOWLIST.size === 81`
   - members at indices 75..80 are exactly `gov.bill_drafted, gov.bill_cosponsored, gov.session_opened, gov.session_closed, gov.law_enacted, gov.law_repealed` in order
   - each of the 6 `IS on allowlist` via `.toContain`
   → verify: test RED (src still 75).
2. **schema.ts** — append migration `version: 36, name: 'gov_bills_sessions_laws'` with the 5 tables + config seeds (see RESEARCH). `down` drops all 5 tables + deletes the 2 config keys.
3. **migration-schema.test.ts** (or new `gov-migration.test.ts`) — assert MIGRATIONS contains v36, up creates `gov_bills`/`gov_sessions`/`gov_laws`, down drops them, versions are contiguous (no gap/dup). → verify GREEN.
4. **RED skeletons** (dynamic-import-in-beforeAll so absent modules don't crash collection):
   - `grid/test/append-gov-bill-drafted.test.ts` — imports `../src/audit/append-gov-bill-drafted.js`; 1 placeholder `it` asserting export is a function. (the other 5 producers get full tests in Plan 02; one RED skeleton here proves the harness.)
   - `grid/test/gov-routes.test.ts` — buildApp scaffold importing `../src/api/routes/gov.js`; 1 placeholder `it`. RED until Plan 03.

## Verify
- `npx vitest run grid/test/audit/broadcast-allowlist.test.ts` → RED on count (expected).
- migration test GREEN.
- `npx tsc --noEmit` may show absent-module errors in skeleton tests — acceptable for Wave 0 (they resolve in 02/03).

## Done when
allowlist test locked to 81 (RED), v36 present + tested GREEN, 2 RED skeletons committed.
