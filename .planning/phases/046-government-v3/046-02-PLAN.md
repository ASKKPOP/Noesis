---
phase: 046-government-v3
plan: 02
title: 6 sole-producers + GovBillStore + types
status: planned
---

# Plan 02 — Producers + Store

## Goal
Turn the allowlist test GREEN (75→81) and ship the 6 `gov.*` sole-producers + `GovBillStore`. Producer unit tests GREEN.

## Steps
1. **broadcast-allowlist.ts** — after `'irs.disbursement_executed', // (75)` insert 6 entries `// (76)..(81)`:
   `gov.bill_drafted, gov.bill_cosponsored, gov.session_opened, gov.session_closed, gov.law_enacted, gov.law_repealed`. Update header "75 event types" → "81". → allowlist test GREEN.
2. **grid/src/gov/types.ts** — 6 payload interfaces + 6 `*_KEYS` alphabetical tuples (see RESEARCH event list). `supersedes_law_id: string | null` on law_enacted.
3. **6 producers** `grid/src/audit/append-gov-*.ts` — clone `append-irs-disbursement-authorized.ts` 9-step discipline exactly:
   - type guard → per-field regex/int checks → closed-tuple (Object.keys.sort vs *_KEYS) → explicit reconstruction (no spread) → payloadPrivacyCheck → `audit.append(eventType, actorDid, clean)`.
   - HEX64_RE for `*_civic_did_hash`/`*_hash`; UUID_RE for ids; `category` non-empty string ≤63; counts positive int; ticks non-negative int.
   - `gov.law_enacted`: `supersedes_law_id` null OR UUID; actorDid=`law_id`. `gov.law_repealed`: actorDid=`law_id`.
4. **grid/src/gov/gov-bill-store.ts** — `class GovBillStore(db, gridName)` + `createInMemoryGovStore()` (mirror governance/store.ts in-memory pattern):
   - `insertBill`, `getBill`, `addCosponsor` (returns new count; PK dedup → throw `duplicate_cosponsor`), `setBillStatus`, `setBillProposalId`,
   - `openSession`, `getSession`, `addArgument`, `closeSession`,
   - `enactLaw`, `getActiveLaws`, `repealLaw`.
   - tick from caller; no Date.now/Math.random.
5. **Producer unit tests** `grid/test/append-gov-*.test.ts` (6 files) — for each: happy path appends + returns AuditEntry with correct eventType; rejects bad hash (TypeError); rejects missing key (TypeError); rejects extra key (/closed-tuple/); privacy violation rejected. Mirror `append-irs-disbursement-authorized.test.ts` conventions (missing key → TypeError, not /closed-tuple/).

## Verify
- `npx vitest run grid/test/audit/broadcast-allowlist.test.ts grid/test/append-gov-*.test.ts` → GREEN.
- `npx tsc --noEmit` → 0 errors.
- `node scripts/check-sole-producer-discipline.mjs` → OK (recognizes 6 new audit producers).

## Done when
allowlist 81 GREEN, 6 producers + store compile, producer tests GREEN, sole-producer gate OK.
