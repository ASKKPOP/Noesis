---
phase: 33
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [OBS-08, OBS-08b, OBS-09, OBS-10]
threat_refs: [T-33-DOC-01]
tags: [doc-sync, allowlist-budget, obs-08b, d-33-f1]

must_haves:
  truths:
    - "REQUIREMENTS.md contains a new OBS-08b entry locking the human.identified producer + 5-key payload"
    - "ROADMAP.md Phase 33 section reads `Allowlist additions: +3` (was +2) and success criterion #4 reads `56 members` (was 55)"
    - "ROADMAP.md v2.6 Allowlist Growth Ledger lists 3 rows for Phase 33 (portal.auth.login=54, portal.auth.register=55, human.identified=56) and the running-total tail row says 56"
    - "ROADMAP.md milestone line in Overview + Milestones list reads `53 → 56` not `53 → 55`"
    - "STATE.md v2.6 allowlist additions paragraph mentions human.identified at position 56"
    - "STATE.md frontmatter and Total v2.6 allowlist growth lines say +3 (53 → 56)"
    - "STATE.md Phase Plan Summary table Phase 33 row reads `+3 (56)` (was `+2 (55)`)"
    - "Phase 33 REQUIREMENTS.md Traceability table contains a new OBS-08b row mapped to Phase 33, Pending"
    - "Downstream plans 33-02..33-06 read these updated docs and see the 56-member target — no plan in this phase set ships against the stale 55-member literal"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "OBS-08b requirement entry — appendHumanIdentified sole-producer, closed 5-key payload {grid_name, human_did, identity_hash, identity_method, tick}, identity_method ∈ {siwe, email}, allowlist position 56, wired into SIWE first-connect AND email signup paths"
      contains: "OBS-08b"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 33 metadata refresh: +3 allowlist delta, 56-member success criterion, three growth-ledger rows, milestone allowlist 56"
      contains: "53 → 56"
    - path: ".planning/STATE.md"
      provides: "v2.6 allowlist additions section names human.identified position 56; Phase Plan Summary row matches; frontmatter total +3"
      contains: "human.identified"
  key_links:
    - from: ".planning/REQUIREMENTS.md OBS-08b"
      to: "grid/src/audit/append-human-identified.ts (created in Plan 33-03)"
      via: "REQ → file path locked in REQUIREMENTS.md"
      pattern: "append-human-identified"
    - from: ".planning/ROADMAP.md Phase 33 success criterion #4"
      to: ".planning/REQUIREMENTS.md count assertion + scripts/check-state-doc-sync.mjs (extended in Plan 33-06)"
      via: "literal count `56 members` referenced from all three"
      pattern: "56 members"
    - from: ".planning/STATE.md Phase Plan Summary"
      to: ".planning/ROADMAP.md Phase 33 row"
      via: "matching delta `+3 (56)`"
      pattern: "\\+3"
---

<objective>
Lock the v2.6 documentation drift caused by the Phase 33 scope expansion (D-33-A1 added `human.identified` at allowlist position 56, revising the allowlist budget from +2 to +3). Per D-33-F1 + CLAUDE.md Documentation Sync Rule, REQUIREMENTS.md / ROADMAP.md / STATE.md must reflect the 53→56 target BEFORE any producer code lands so downstream plans, executor agents, and CI gates all read the same target literals.

This is the inverse of Phase 31/32 (where docs landed last). Reason: every downstream Phase 33 plan asserts a 56-member count, a sole-producer triad covering 3 files, and an OBS-08b requirement ID. If those docs still say 55 / 2 producers / no OBS-08b, every downstream verification step diverges.

**Purpose:** Single source of truth for "what Phase 33 ships." 0 wave-1 producer code; only doc updates.

**Output:** 3 modified files. No new files. No script changes (script extension lives in Plan 33-06 alongside the new sole-producer-discipline gate so they ship coherently).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/33-portal-auth-producers/33-CONTEXT.md
@.planning/phases/33-portal-auth-producers/33-PATTERNS.md
@CLAUDE.md
@PHILOSOPHY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Append OBS-08b to REQUIREMENTS.md and traceability table</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read in full — current file is 94 lines; understand OBS-08/09/10 block at lines 28-35 + Traceability table at lines 73-92)
    - .planning/phases/33-portal-auth-producers/33-CONTEXT.md §D-33-A1, §D-33-A2, §D-33-A3, §D-33-F1 (exact OBS-08b text to insert)
  </read_first>
  <action>
Open `.planning/REQUIREMENTS.md`. Make TWO edits.

**Edit 1 — Insert OBS-08b** between the existing OBS-08 line (line 33) and OBS-09 line (line 34). The exact new line to insert is:

```
- [ ] **OBS-08b**: `appendHumanIdentified(audit, { grid_name, human_did, identity_hash, identity_method, tick })` sole-producer file lands at `grid/src/audit/append-human-identified.ts`. Closed 5-key payload, `identity_method ∈ {'siwe', 'email'}` (closed enum via `IDENTITY_METHOD_ENUM = ['email', 'siwe'] as const`), `DID_RE` on `human_did`, `HEX64_RE` on `identity_hash`, non-empty string on `grid_name`, non-negative integer guard on `tick`, structural `Object.keys(payload).sort()` strict-equality check, `payloadPrivacyCheck` runs before `audit.append('human.identified', ...)`. Emits at allowlist position **56**. Wired into SIWE verify FIRST-CONNECT path (after `appendHumanJoined`, only when `isNew === true`, with `identity_hash = sha256(ethAddress.toLowerCase())` — byte-identical to `eth_address_hash` for correlation with pre-Phase-33 `human.joined` entries) AND email signup success (with `identity_hash = sha256(email.toLowerCase().trim())`). Pre-Phase-33 `human.joined` entries preserved unmodified per PHILOSOPHY §1 + Merkle invariant (`chain.ts:181`). Mirror discipline of `append-human-joined.ts:50-114`.
```

This insertion must sit between the OBS-08 bullet and the OBS-09 bullet inside the `### OBS — Missing portal.auth.* Producers (Phase 33 — GAP-B fix)` section. Do NOT reorder OBS-09 or OBS-10.

**Edit 2 — Traceability table** (currently at lines 73-92): insert a new row between the OBS-08 row (`| OBS-08 | 33 | Pending |`) and the OBS-09 row (`| OBS-09 | 33 | Pending |`):

```
| OBS-08b | 33 | Pending |
```

Also update the bottom-of-section coverage assertion. Current text (line 93): `**Coverage:** 15/15 v2.6 REQs mapped to phases. Zero orphans. Zero duplicates.` → change to: `**Coverage:** 16/16 v2.6 REQs mapped to phases. Zero orphans. Zero duplicates. (OBS-08b added Phase 33 D-33-F1.)`

Do NOT touch any other REQ line, any other phase block, or any "Future Requirements" / "Out of Scope" / "Traceability" rows unrelated to OBS-08b. CLAUDE.md §3 Surgical Changes applies.
  </action>
  <verify>
    <automated>grep -c '^- \[ \] \*\*OBS-08b\*\*' .planning/REQUIREMENTS.md && grep -c '| OBS-08b | 33 | Pending |' .planning/REQUIREMENTS.md && grep -c '16/16 v2.6 REQs' .planning/REQUIREMENTS.md && grep -c 'append-human-identified.ts' .planning/REQUIREMENTS.md && grep -c "identity_method ∈ {'siwe', 'email'}" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^- \[ \] \*\*OBS-08b\*\*' .planning/REQUIREMENTS.md` returns `1` (exactly one OBS-08b bullet).
    - `grep -c 'append-human-identified.ts' .planning/REQUIREMENTS.md` returns `1` (exactly one mention, inside the OBS-08b line).
    - `grep -c '| OBS-08b | 33 | Pending |' .planning/REQUIREMENTS.md` returns `1` (Traceability row present).
    - `grep -c 'identity_hash, identity_method, tick' .planning/REQUIREMENTS.md` returns `1` (closed 5-key payload locked).
    - `grep -c 'allowlist position \*\*56\*\*' .planning/REQUIREMENTS.md` returns `1` (position locked).
    - `grep -c '15/15 v2.6 REQs' .planning/REQUIREMENTS.md` returns `0` (old coverage line replaced).
    - `grep -c '16/16 v2.6 REQs' .planning/REQUIREMENTS.md` returns `1` (new coverage line present).
    - `grep -c 'OBS-08 ' .planning/REQUIREMENTS.md` and `grep -c 'OBS-09' .planning/REQUIREMENTS.md` and `grep -c 'OBS-10' .planning/REQUIREMENTS.md` each return ≥ 1 (existing requirements preserved).
    - File line count grew by approximately 2-3 lines (one new REQ bullet + one new table row + coverage line edit). Compare with `wc -l .planning/REQUIREMENTS.md` before/after.
  </acceptance_criteria>
  <done>OBS-08b is a first-class active v2.6 requirement; Traceability table includes it; coverage counter reflects 16/16; no other REQ touched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update ROADMAP.md for allowlist 53→56 (Phase 33 section + growth ledger + milestone line)</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read in full — file is 297 lines; the edits target lines 7, 20, 26, 82-99, 173-184)
    - .planning/phases/33-portal-auth-producers/33-CONTEXT.md §D-33-A1, §D-33-A2, §D-33-F1
    - .planning/phases/33-portal-auth-producers/33-PATTERNS.md "ALLOWLIST_MEMBERS append" block (canonical 3-row addition)
  </read_first>
  <action>
Open `.planning/ROADMAP.md`. Make FIVE surgical edits.

**Edit 1 — Overview line 7** (currently): `Allowlist grows **53 → 55** (+2 events in Phase 33).` → REPLACE with: `Allowlist grows **53 → 56** (+3 events in Phase 33).`

**Edit 2 — Milestones list line 20** (currently): `🔄 **v2.6 Resilience & Observability — Phases 31-35** (opened 2026-05-24)` → leave unchanged (no allowlist count on this line).

**Edit 3 — Phase 33 single-line entry around line 26** (currently): `[ ] **Phase 33: portal.auth.* Producers** — Wire \`appendPortalAuthLogin\` + \`appendPortalAuthRegister\` sole-producers into SIWE verify + email signup/signin. Add \`PORTAL_AUTH_FORBIDDEN_KEYS\`. Allowlist 53 → 55 (+2).` → REPLACE with: `[ ] **Phase 33: portal.auth.* Producers** — Wire \`appendPortalAuthLogin\` + \`appendPortalAuthRegister\` + \`appendHumanIdentified\` sole-producers into SIWE verify + email signup/signin. Add \`PORTAL_AUTH_FORBIDDEN_KEYS\`. Allowlist 53 → 56 (+3).`

**Edit 4 — Phase 33 full block (lines 82-99)** — apply these in-block changes:

- **Goal paragraph (line 83):** append `Adds \`human.identified\` universal identity-stamp event (D-33-A1) so /users + Phase 34 surfaces can correlate SIWE-born humans (via shared identity_hash with Phase 22 \`human.joined\`) and email-born humans (via sha256(email)) under a single event type going forward.` after the existing sentence.
- **Requirements line 85** (currently): `**Requirements**: OBS-08, OBS-09, OBS-10` → REPLACE with: `**Requirements**: OBS-08, OBS-08b, OBS-09, OBS-10`
- **Success criterion #2 (lines ~88-90)** — append after the existing email-signin sentence: `First-time SIWE connect ALSO emits \`human.identified\` (universal identity-stamp event, D-33-A1) immediately after \`human.joined\`, so /users can resolve both SIWE-born (via shared identity_hash with eth_address_hash) and email-born humans (sha256(email)) under one universal event. Email signup emits \`human.identified\` ONLY (NO \`human.joined\` — Phase 22's SIWE-only contract preserved per D-33-A7).`
- **Success criterion #4 (line ~90)** (currently): `\`grid/src/audit/broadcast-allowlist.ts\` ends at exactly 55 members: \`'portal.auth.login'\` at position 54, \`'portal.auth.register'\` at position 55. CI gate \`scripts/check-state-doc-sync.mjs\` asserts the literal count.` → REPLACE with: `\`grid/src/audit/broadcast-allowlist.ts\` ends at exactly 56 members: \`'portal.auth.login'\` at position 54, \`'portal.auth.register'\` at position 55, \`'human.identified'\` at position 56. CI gate \`scripts/check-state-doc-sync.mjs\` asserts the literal count.`
- **Success criterion #5 (line ~91)** — extend the sentence to add the third sole-producer file: `Only \`grid/src/audit/append-portal-auth-login.ts\` may call \`audit.append('portal.auth.login', ...)\`, only \`grid/src/audit/append-portal-auth-register.ts\` may call \`audit.append('portal.auth.register', ...)\`, and only \`grid/src/audit/append-human-identified.ts\` may call \`audit.append('human.identified', ...)\` — \`scripts/check-sole-producer-discipline.mjs\` greps every \`append-*.ts\` (and the equivalent sole-producer files across the audit-emitting subsystems) for the \`Object.keys(payload).sort()\` + \`payloadPrivacyCheck\` + \`audit.append\` triad and fails if any sole-producer file omits any of the three.`
- **Scope (ships) line (~92)** (currently): `**Scope (ships)**: OBS-08..10.` → REPLACE with: `**Scope (ships)**: OBS-08, OBS-08b, OBS-09, OBS-10.`
- **Allowlist additions line (~98)** (currently): `**Allowlist additions**: **+2**. Events: \`portal.auth.login\` (pos 54) \`{human_did, method, tick}\` where \`method ∈ {siwe, email}\`; \`portal.auth.register\` (pos 55) \`{human_did, method, tick}\`. Running total: **55**.` → REPLACE with: `**Allowlist additions**: **+3**. Events: \`portal.auth.login\` (pos 54) \`{human_did, method, tick}\` where \`method ∈ {siwe, email}\`; \`portal.auth.register\` (pos 55) \`{human_did, method, tick}\`; \`human.identified\` (pos 56) \`{grid_name, human_did, identity_hash, identity_method, tick}\` where \`identity_method ∈ {siwe, email}\`. Running total: **56**.`

**Edit 5 — Allowlist Growth Ledger table (lines 173-184)** — currently has 6 rows; replace with these 7 rows (insertion of the human.identified row + tail updates so running totals through Phase 33/34/35 read 54 / 55 / 56 / 56 / 56):

```
| Phase | Event Added | Payload Shape | Running Total |
|-------|-------------|---------------|---------------|
| 31 | *(none — wiring + reconcile + logging only)* | — | 53 |
| 32 | *(none — `/health/detailed` is a route, not an audit event)* | — | 53 |
| 33 | `portal.auth.login` (pos 54) | `{human_did, method, tick}` where `method ∈ {siwe, email}` | 54 |
| 33 | `portal.auth.register` (pos 55) | `{human_did, method, tick}` | 55 |
| 33 | `human.identified` (pos 56) | `{grid_name, human_did, identity_hash, identity_method, tick}` where `identity_method ∈ {siwe, email}` | 56 |
| 34 | *(none — UI cards consume existing data via REST)* | — | 56 |
| 35 | *(none — documentation + UAT only)* | — | 56 |
```

And the tail paragraph (line 186) (currently): `**Total v2.6 allowlist growth: +2 (53 → 55).** Freeze-except-by-explicit-addition rule preserved. Both new events carry closed 3-key tuples; PII (IP, UA, email plaintext, session tokens, signatures, nonces) is permanently forbidden via \`PORTAL_AUTH_FORBIDDEN_KEYS\`.` → REPLACE with: `**Total v2.6 allowlist growth: +3 (53 → 56).** Freeze-except-by-explicit-addition rule preserved. `portal.auth.login` and `portal.auth.register` carry closed 3-key tuples; `human.identified` carries a closed 5-key tuple with `identity_hash` (SHA-256 of lowercased ETH address for SIWE — byte-identical to Phase 22 `eth_address_hash` for correlation — or SHA-256 of normalized email for email path). PII (IP, UA, email plaintext, session tokens, signatures, nonces) is permanently forbidden via `PORTAL_AUTH_FORBIDDEN_KEYS` + word-boundary alternation in `FORBIDDEN_KEY_PATTERN`.`

Also update the trailing footer line at the very bottom of the file (line 296): `*Last updated: 2026-05-24 — v2.6 Resilience & Observability roadmap created (5 phases, 15 OBS-* REQs, allowlist 53 → 55 in Phase 33). Phase numbering continues from v2.5.*` → REPLACE with: `*Last updated: 2026-05-25 — v2.6 Phase 33 scope expanded (+human.identified per D-33-A1 / OBS-08b). 5 phases, 16 OBS-* REQs, allowlist 53 → 56 in Phase 33. Phase numbering continues from v2.5.*`

Do NOT touch Phase 31, 32, 34, or 35 blocks. Do NOT touch historical milestone sections (v1.0 through v2.5).
  </action>
  <verify>
    <automated>grep -c '53 → 56' .planning/ROADMAP.md && grep -c '56 members' .planning/ROADMAP.md && grep -c "appendHumanIdentified" .planning/ROADMAP.md && grep -c "human.identified" .planning/ROADMAP.md && grep -c "OBS-08, OBS-08b, OBS-09, OBS-10" .planning/ROADMAP.md && grep -c '\*\*Total v2.6 allowlist growth: +3' .planning/ROADMAP.md && [ $(grep -c '53 → 55' .planning/ROADMAP.md) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '53 → 56' .planning/ROADMAP.md` returns at least `3` (Overview line, Phase 33 single-line entry, Phase 33 block).
    - `grep -c '53 → 55' .planning/ROADMAP.md` returns `0` (no stale `53 → 55` references survive anywhere in the file).
    - `grep -c '56 members' .planning/ROADMAP.md` returns at least `1` (success criterion #4).
    - `grep -c "'human.identified'" .planning/ROADMAP.md` returns at least `2` (success criterion #4 + growth ledger row).
    - `grep -c "appendHumanIdentified" .planning/ROADMAP.md` returns at least `1` (Phase 33 single-line entry).
    - `grep -c "OBS-08, OBS-08b, OBS-09, OBS-10" .planning/ROADMAP.md` returns `1` (Requirements line).
    - `grep -c '\*\*Total v2.6 allowlist growth: +3' .planning/ROADMAP.md` returns `1`.
    - `grep -c 'allowlist 53 → 56 in Phase 33' .planning/ROADMAP.md` returns `1` (footer line).
    - Allowlist Growth Ledger table contains exactly 3 Phase 33 rows: `grep -E '^\| 33 \|' .planning/ROADMAP.md | wc -l` returns `3`.
    - Phase 31/32 historical entries unchanged: `grep -c 'Phase 31: Audit Pipeline Persistence' .planning/ROADMAP.md` and `grep -c 'Phase 32: Firehose Observability' .planning/ROADMAP.md` each return `1` (preserved).
    - Phase 34 entry unchanged: `grep -c 'Phase 34: Steward' .planning/ROADMAP.md` returns `1`, and `grep -c 'allowlist unchanged 56' .planning/ROADMAP.md` should appear if Phase 34's single-line entry was correctly carried forward (verify by reading lines around 27 — running total now 56 not 55). NOTE: Phase 34 + 35 single-line entries say `(allowlist unchanged 55)` — those must be updated to `(allowlist unchanged 56)`. Add this as a sub-edit if missed.
  </acceptance_criteria>
  <done>ROADMAP.md is the canonical statement that Phase 33 ships 3 producers, allowlist 53→56, OBS-08b is a tracked REQ; no stale `53 → 55` or `55 members` substrings survive.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update STATE.md for v2.6 allowlist additions section + Phase Plan Summary table + frontmatter</name>
  <files>.planning/STATE.md</files>
  <read_first>
    - .planning/STATE.md (read in full — file is 249 lines; edits target lines 42-50 [Phase Plan Summary], 151-159 [v2.6 allowlist additions], 161-169 [forbidden-key additions], 246-249 [Session Continuity])
    - .planning/phases/33-portal-auth-producers/33-CONTEXT.md §D-33-A1, §D-33-A2, §D-33-F1
  </read_first>
  <action>
Open `.planning/STATE.md`. Make FOUR surgical edits.

**Edit 1 — Phase Plan Summary table row for Phase 33** (around line 46) (currently): `| 33 — portal.auth.* Producers | appendPortalAuthLogin/Register sole-producers; SIWE + email wiring; PORTAL_AUTH_FORBIDDEN_KEYS | OBS-08..10 | +2 (55) |` → REPLACE with: `| 33 — portal.auth.* Producers | appendPortalAuthLogin/Register + appendHumanIdentified sole-producers; SIWE + email wiring; PORTAL_AUTH_FORBIDDEN_KEYS | OBS-08, OBS-08b, OBS-09, OBS-10 | +3 (56) |`

Update the rows below for Phase 34 + 35 — currently they read `0 (55)` in the Allowlist Delta column. Change BOTH to `0 (56)` to reflect the new running total:
- `| 34 — Steward \`/system\` Health Surfaces | ... | OBS-11..14 | 0 (55) |` → `| 34 — Steward \`/system\` Health Surfaces | ... | OBS-11..14 | 0 (56) |`
- `| 35 — UAT Re-Verification + Doc Close-Out | ... | OBS-15 | 0 (55) |` → `| 35 — UAT Re-Verification + Doc Close-Out | ... | OBS-15 | 0 (56) |`

**Edit 2 — Total v2.6 allowlist growth line** (line 50) (currently): `**Total v2.6 allowlist growth:** +2 (Phase 33 only).` → REPLACE with: `**Total v2.6 allowlist growth:** +3 (Phase 33 only — portal.auth.login, portal.auth.register, human.identified).`

**Edit 3 — v2.6 allowlist additions section** (lines 151-159) — REPLACE the existing bullet for Phase 33 (line 155) with the expanded 3-event description, AND update the trailing total paragraph (line 159):

OLD line 155: `- Phase 33 adds: \`portal.auth.login\` (pos 54) \`{human_did, method, tick}\`; \`portal.auth.register\` (pos 55) \`{human_did, method, tick}\``

NEW line 155 (replace with):
```
- Phase 33 adds:
    - `portal.auth.login` (pos 54) `{human_did, method, tick}` where `method ∈ {'siwe', 'email'}` — sole producer `grid/src/audit/append-portal-auth-login.ts`. Fires on every SIWE verify success AND email signin success (unconditional). Allowlist position 54.
    - `portal.auth.register` (pos 55) `{human_did, method, tick}` where `method ∈ {'siwe', 'email'}` — sole producer `grid/src/audit/append-portal-auth-register.ts`. Fires on SIWE first-connect (inside `if (!human)` block) AND email signup. Allowlist position 55.
    - `human.identified` (pos 56) `{grid_name, human_did, identity_hash, identity_method, tick}` where `identity_method ∈ {'siwe', 'email'}` — sole producer `grid/src/audit/append-human-identified.ts`. Universal identity-stamp event added per D-33-A1 + OBS-08b. SIWE path: `identity_hash = sha256(ethAddress.toLowerCase())` (byte-identical to Phase 22 `eth_address_hash` for correlation with pre-Phase-33 `human.joined` entries). Email path: `identity_hash = sha256(email.toLowerCase().trim())`. Fires on SIWE first-connect (after `appendHumanJoined`) AND email signup (NO `human.joined` for email — Phase 22's SIWE-only contract preserved per D-33-A7). Pre-Phase-33 `human.joined` entries preserved unmodified per PHILOSOPHY §1 + Merkle invariant (`chain.ts:181`). Allowlist position 56.
```

OLD line 159: `Total v2.6 allowlist growth: **+2 (53 → 55)**. Freeze-except-by-explicit-addition rule preserved.`

NEW line 159: `Total v2.6 allowlist growth: **+3 (53 → 56)**. Freeze-except-by-explicit-addition rule preserved.`

**Edit 4 — Session Continuity (lines 246-249)** — update the timestamp + stopped-at marker:

OLD:
```
Last session: 2026-05-25T02:39:35.377Z
Stopped at: Phase 33 context gathered
Resume file: .planning/phases/33-portal-auth-producers/33-CONTEXT.md
```

NEW:
```
Last session: 2026-05-25
Stopped at: Phase 33 doc-sync landed (allowlist budget locked at +3 / 53→56 per D-33-F1); ready to execute producer plans 33-02..33-06
Resume file: .planning/phases/33-portal-auth-producers/33-01-PLAN.md
```

Also update the YAML frontmatter `last_updated` (line 7) and `last_activity` (line 8) to today's date `2026-05-25` (or whatever current date `date -u +%Y-%m-%d` returns when executor runs).

Do NOT touch v2.5 / v2.4 / v2.3 / v2.2 / v2.0 historical context blocks. Do NOT touch Phase 31 close-out section. Do NOT touch v2.6 forbidden-key additions section (lines 161-169) — that block already lists the 13 PORTAL_AUTH_FORBIDDEN_KEYS correctly. CLAUDE.md §3 Surgical Changes applies.
  </action>
  <verify>
    <automated>grep -c '+3 (56)' .planning/STATE.md && grep -c 'human.identified' .planning/STATE.md && grep -c 'OBS-08, OBS-08b, OBS-09, OBS-10' .planning/STATE.md && grep -c '+3 (53 → 56)' .planning/STATE.md && grep -c 'append-human-identified.ts' .planning/STATE.md && [ $(grep -c '+2 (55)' .planning/STATE.md) -eq 0 ] && [ $(grep -c '+2 (53 → 55)' .planning/STATE.md) -eq 0 ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '+3 (56)' .planning/STATE.md` returns `1` (Phase Plan Summary table Phase 33 row).
    - `grep -c '0 (56)' .planning/STATE.md` returns at least `2` (Phase 34 + Phase 35 rows updated to new running total).
    - `grep -c '+3 (53 → 56)' .planning/STATE.md` returns `1` (Total v2.6 allowlist growth line).
    - `grep -c '+2 (55)' .planning/STATE.md` returns `0` (no stale row).
    - `grep -c '+2 (53 → 55)' .planning/STATE.md` returns `0` (no stale total).
    - `grep -c 'human.identified' .planning/STATE.md` returns at least `3` (Phase Plan Summary table mention + v2.6 allowlist additions bullet expansion + Phase Plan Summary expanded entry).
    - `grep -c 'append-human-identified.ts' .planning/STATE.md` returns at least `1`.
    - `grep -c 'OBS-08, OBS-08b, OBS-09, OBS-10' .planning/STATE.md` returns `1`.
    - `grep -c '(pos 56)' .planning/STATE.md` returns `1` (allowlist position 56 mention in additions section).
    - Frontmatter `last_updated` field has been updated (not still `2026-05-25T02:39:35.380Z` literal — check with `grep '^last_updated:' .planning/STATE.md`).
    - v2.5 / v2.4 / v2.3 / v2.2 / v2.0 sections unchanged: `grep -c 'v2.5 Key Decisions' .planning/STATE.md` returns `1`; `grep -c 'v2.6 Phase 31 close-out' .planning/STATE.md` returns `1`.
  </acceptance_criteria>
  <done>STATE.md is the single canonical machine-readable record of v2.6 allowlist budget = +3 / 53→56 with `human.identified` as the third addition; frontmatter timestamp refreshed; no stale literals survive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planner output → Executor agents → Downstream plan reading | Doc-sync writes the authoritative literals (allowlist 56, OBS-08b, three producer files) that every downstream Phase 33 plan asserts against. If this plan ships drift (e.g., 55 still survives in STATE.md), every downstream verification fires false-positive failures. |
| Planner output → Future-phase CI gates (Plan 33-06's check-state-doc-sync extension) | The Phase 33 CI gate asserts `ALLOWLIST_MEMBERS.length === 56` and presence of the 3 new event-name literals in STATE.md. If this doc-sync plan fails to land the 56 literal in STATE.md, the gate would block any future PR. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-DOC-01 | Tampering — silent doc drift (planner partial-edit leaves `55` co-existing with `56`) | All three files | mitigate | Negative acceptance criteria explicitly grep for the ABSENCE of stale literals (`grep -c '53 → 55' .planning/ROADMAP.md` must return 0). Task 1/2/3 acceptance bars block the merge unless both presence-of-new and absence-of-old assertions pass. |
| T-33-DOC-02 | Information Disclosure — PII or secret leaks into doc-sync edits | All three files | accept | Doc-sync edits are pure metadata (allowlist counts, REQ IDs, event names, payload shapes). Zero PII. Zero secrets. No env files touched. |
| T-33-DOC-03 | Repudiation — change history obscured | git history | mitigate | CLAUDE.md Documentation Sync Rule requires one git commit per coherent change. Plan 33-01 commit message: `docs(33): sync allowlist budget 53→56 per D-33-F1 (add OBS-08b)`. Single commit, three files, atomic narrative. |
| T-33-DOC-04 | Elevation of Privilege — REQ added that grants unscoped powers | OBS-08b REQ text | mitigate | OBS-08b text strictly scopes the producer file path, payload shape, allowlist position, and PHILOSOPHY §1 preservation clause. Closed enum + sole-producer file path matches existing Phase 22 discipline; no new authority. |
</threat_model>

<verification>
After Plan 33-01 lands:

1. `grep -c '53 → 56' .planning/ROADMAP.md` ≥ 3 AND `grep -c '53 → 55' .planning/ROADMAP.md` = 0 (ROADMAP fully migrated).
2. `grep -c 'OBS-08b' .planning/REQUIREMENTS.md` ≥ 2 (REQ bullet + Traceability row).
3. `grep -c '+3 (56)' .planning/STATE.md` = 1 (Phase Plan Summary updated).
4. `grep -c 'human.identified' .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` ≥ 6 (mentioned across all three).
5. Manual cross-check: Open each of the three files, search for `55 members`, `+2 (55)`, `53 → 55` — every result must be either inside a historical section (v2.5 close-out) or zero. If any active-v2.6 reference still says 55, FAIL the plan.
6. Run `git diff .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md` and visually confirm: insertions only; no historical content deleted.
</verification>

<success_criteria>
- Three files modified, no files created.
- REQUIREMENTS.md has OBS-08b as a tracked v2.6 REQ in OBS section + Traceability table + 16/16 coverage line.
- ROADMAP.md says `53 → 56` everywhere current-state literals appear; Allowlist Growth Ledger has 3 Phase 33 rows; success criterion #4 says `56 members`; Phase 33 single-line entry mentions appendHumanIdentified.
- STATE.md Phase Plan Summary row Phase 33 says `+3 (56)`; v2.6 allowlist additions section names human.identified position 56; Total v2.6 allowlist growth line says `+3 (53 → 56)`.
- Zero stale `55` references in any active-v2.6 metadata context.
- Single git commit (per CLAUDE.md Documentation Sync Rule cadence).
- Downstream plans 33-02..33-06 can be executed against these refreshed docs without any literal mismatch.
</success_criteria>

<output>
After completion, create `.planning/phases/33-portal-auth-producers/33-01-SUMMARY.md` capturing: doc-sync landed before any producer code (inverse of Phase 31/32 cadence per D-33-F1); allowlist budget locked at +3 / 53→56; OBS-08b is now a tracked REQ; three files changed in one atomic commit; downstream plans unblocked.
</output>
