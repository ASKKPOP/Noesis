---
phase: 13-operator-replay-export
plan: 06
type: execute
wave: 5
depends_on: [13-05]
files_modified:
  - .planning/STATE.md
  - package.json
  - package-lock.json
autonomous: true
gap_closure: true
requirements: [REPLAY-01, REPLAY-02]
must_haves:
  truths:
    - "STATE.md Accumulated Context says '27 events' and enumerates operator.exported as entry 27"
    - "node scripts/check-state-doc-sync.mjs exits 0"
    - "node scripts/replay-verify.mjs --help (or no-args usage) exits without ERR_MODULE_NOT_FOUND"
    - "npx vitest run test/replay/tarball-determinism.test.ts passes (2/2)"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Restored 27-event allowlist enumeration + Phase 13 Accumulated Context block"
      contains: "27 events"
    - path: "package.json"
      provides: "Root workspace dependency on tar@^7.5.13 so scripts/ and root-level vitest can resolve 'tar'"
      contains: "\"tar\""
  key_links:
    - from: "scripts/check-state-doc-sync.mjs"
      to: ".planning/STATE.md"
      via: "literal '27 events' assertion + 'operator.exported' allowlist member assertion"
      pattern: "27 events"
    - from: "scripts/replay-verify.mjs"
      to: "node_modules/tar"
      via: "import { Parser, Pack, Header, ReadEntry } from 'tar'"
      pattern: "from 'tar'"
    - from: "test/replay/tarball-determinism.test.ts"
      to: "grid/src/export/tarball-builder.ts"
      via: "import path resolution that transitively requires 'tar'"
      pattern: "tarball-determinism"
---

<objective>
Close two hard blockers identified in 13-VERIFICATION.md so Phase 13 can pass:

1. **Gap 1 (STATE.md regression):** Commit `f425d99` ("update tracking after wave 4") accidentally reverted the STATE.md changes from commit `cb7d136`. The allowlist heading reverted from "Phase 13 — post-Wave-3, Plan 13-04" back to "Phase 12 — post-ship, Plan 12-04", the count reverted from "27 events" to "26 events", entry 27 (operator.exported) was removed, and the Phase 13 Accumulated Context block was stripped. CI gate `scripts/check-state-doc-sync.mjs` exits 1 confirming the drift. This is a pure git regression — restore the verbatim content from `cb7d136`.

2. **Gap 2 (tar package unreachable from root):** The `tar` package is listed in `grid/package.json` as `"tar": "^7.5.13"` but is NOT installed at the root workspace level. `scripts/replay-verify.mjs` (which lives at repo root) imports `from 'tar'` and fails with `ERR_MODULE_NOT_FOUND`. The Wave 3 vitest workspace test `test/replay/tarball-determinism.test.ts` also fails for the same reason when run from repo root. Fix: add `tar@^7.5.13` to the root `package.json` dependencies and run `npm install` so the package becomes resolvable from `scripts/` and root-level vitest invocations.

Purpose: Both failures are blocking the Phase 13 verification from reaching a `pass` status — they are not implementation defects, they are mechanical git/dependency regressions. Output: a green `check-state-doc-sync.mjs` gate, a `replay-verify.mjs` CLI that loads its tar import, and a `tarball-determinism.test.ts` that runs from repo root.

This plan is gap-closure scoped: no new features, no new audit events, no new code paths. Two file edits and one `npm install`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operator-replay-export/13-VERIFICATION.md
@.planning/phases/13-operator-replay-export/13-CONTEXT.md
@.planning/phases/13-operator-replay-export/13-04-SUMMARY.md
@.planning/phases/13-operator-replay-export/13-05-SUMMARY.md
@CLAUDE.md
@scripts/check-state-doc-sync.mjs
@scripts/replay-verify.mjs
@package.json
@grid/package.json

<interfaces>
<!-- Key invariants/contracts the executor needs. Extracted from VERIFICATION.md and the cb7d136 commit diff. -->

The verbatim content reverted by `f425d99` (which Task 1 must restore) is shown below.
Source of truth: `git show cb7d136 -- .planning/STATE.md`. The executor SHOULD run that
command itself before editing to confirm byte-for-byte fidelity.

**Section 1 — Allowlist heading + count line (current STATE.md ~lines 61–63):**

CURRENT (broken):
```
### Broadcast allowlist (Phase 12 — post-ship, Plan 12-04)

**26 events.** In code-tuple order (authoritative source: `grid/src/audit/broadcast-allowlist.ts` `ALLOWLIST_MEMBERS`):
```

MUST become:
```
### Broadcast allowlist (Phase 13 — post-Wave-3, Plan 13-04)

**27 events.** In code-tuple order (authoritative source: `grid/src/audit/broadcast-allowlist.ts` `ALLOWLIST_MEMBERS`):
```

**Section 2 — Allowlist entry 27 (insert after current line 90 entry 26):**

After entry 26 (`proposal.tallied`), insert exactly:
```
27. `operator.exported` ← NEW in Phase 13 (REPLAY-02 / D-13-04) — H5-consent-gated audit-chain tarball export; closed 6-key payload `{tier, operator_id, start_tick, end_tick, tarball_hash, requested_at}`; sole producer `grid/src/replay/appendOperatorExported.ts` · Phase 13
```

**Section 3 — Regression gate line (current STATE.md ~line 94):**

CURRENT (broken):
```
Regression gate: `scripts/check-state-doc-sync.mjs` asserts this enumeration matches the frozen 26-event invariant.
```

MUST become:
```
Regression gate: `scripts/check-state-doc-sync.mjs` asserts this enumeration matches the frozen 27-event invariant.
```

**Section 4 — Session Continuity (current STATE.md ~lines 134–139):**

CURRENT (broken):
```
Last session: 2026-04-27T21:00:00.000Z
Stopped at: Phase 13 planned — 5-wave plan, gsd-plan-checker PASSED
Resume file: .planning/phases/13-operator-replay-export/13-01-PLAN.md
Next action: `/gsd-execute-phase 13`
```

MUST become:
```
Last session: 2026-04-27T23:30:00.000Z
Stopped at: Phase 13 gap-closure — 13-06 (STATE.md + tar) and 13-07 (panel wiring) executing
Resume file: .planning/phases/13-operator-replay-export/13-06-PLAN.md
Next action: `/gsd-execute-phase 13 --gaps-only`
```

**Section 5 — Phase 13 Accumulated Context block (insert immediately after the Phase 10b Accumulated Context block ends, before `## Accumulated Context (Phase 11 — Mesh Whisper — shipped 2026-04-23)`):**

```
## Accumulated Context (Phase 13 — Operator Replay & Export — Wave 4 executing 2026-04-27)

- **Phase 13 Wave 4 executing (2026-04-27):** Dashboard /grid/replay UI + ExportConsentDialog + wall-clock gate extension + atomic doc-sync (this entry). Plans 13-01 through 13-04 shipped; 13-05 (Wave 4) in execution.
- **Allowlist bumped 26→27:** `operator.exported` (pos 27) added in Plan 13-04 (Wave 3) — closed 6-key payload `{tier, operator_id, start_tick, end_tick, tarball_hash, requested_at}`; sole producer `grid/src/replay/appendOperatorExported.ts`. STATE.md allowlist updated atomically in Plan 13-05 (Wave 4) per CLAUDE.md doc-sync rule.
- **Phase 13 plan structure (5 plans, Waves 0–4):**
  - 13-01: Wave 0 — deps install + 9 RED test files + verbatim copy locked
  - 13-02: Wave 1 — ReadOnlyAuditChain + ReplayGrid + state-builder + check-replay-readonly CI gate
  - 13-03: Wave 2 — canonical-json + deterministic tarball + replay-verify CLI
  - 13-04: Wave 3 — appendOperatorExported + allowlist 26→27 + Fastify route + check-state-doc-sync bump + replay.* hard-ban
  - 13-05: Wave 4 — Dashboard /grid/replay UI + ExportConsentDialog + wall-clock gate extension + atomic doc-sync
- **ExportConsentDialog discipline (D-13-08 / T-10-10):** Copy-lock verbatim constants. Paste-suppressed Grid-ID input forces character-by-character typing. Closure-capture: `capturedGridIdRef.current = gridId` set only at open time. Cancel auto-downgrades to H1. `onConfirm` receives no arguments — parent component owns the fetch call.
- **ReplayClient discipline (D-13-07 / T-10-09):** `useEffect(() => () => agencyStore.setTier('H1'), [])` cleanup fires on unmount (route exit, browser back, hard navigation). Tier gate at H1/H2 shows TIER_GATE_COPY only. H3+ shows REPLAY badge + Scrubber + replay entry list. Inline redaction: H4-restricted fields render `'— Requires H4'`; H5-restricted fields render `'— Requires H5'`.
- **Wall-clock CI gate extended (D-13-04 / T-10-08):** `scripts/check-wallclock-forbidden.mjs` TIER_B_TS_ROOTS extended with `'dashboard/src/app/grid/replay'`. TIER_B_TS_PATTERNS extended with `setInterval` + `setTimeout`. File extension filter corrected to include `.tsx` and `.jsx` (bug fix — replay files are `.tsx`). Synthetic violation test confirmed exit 1.
- **REPLAY-05 surface shipped:** `/grid/replay` route at H3+ renders REPLAY badge, Scrubber (range + number inputs, tick-clamped), and audit entry list with inline redaction. Firehose/Inspector/RegionMap accept `replayMode` prop (currently wired but store read not yet switched — planned for Phase 14 Rigs integration).
```

**Tar package version (Task 2):** `^7.5.13` — same constraint as `grid/package.json`. The
verbatim line to add to root `package.json` is:

```json
"dependencies": {
  "tar": "^7.5.13"
}
```

The root `package.json` currently has NO `dependencies` field — Task 2 must create it
between the `"workspaces"` block and the `"scripts"` block (or wherever convention
places it; alphabetical-section order in this repo is workspaces → scripts → dependencies → devDependencies → engines → license, so place dependencies after scripts).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore STATE.md content reverted by commit f425d99 (Gap 1)</name>
  <files>.planning/STATE.md</files>
  <action>
Restore the four blocks of content reverted by commit `f425d99` so STATE.md matches the post-`cb7d136` state. Use the exact strings from the `<interfaces>` block above (Sections 1–5). Steps:

1. Read `.planning/STATE.md` in full to anchor your edits.

2. **Section 1 — Allowlist heading + count.** Replace the line `### Broadcast allowlist (Phase 12 — post-ship, Plan 12-04)` with `### Broadcast allowlist (Phase 13 — post-Wave-3, Plan 13-04)`. Replace `**26 events.** In code-tuple order ...` with `**27 events.** In code-tuple order ...` (preserve the rest of the line verbatim).

3. **Section 2 — Insert entry 27.** After the existing line 90 (`26. \`proposal.tallied\` ← NEW in Phase 12 ...`), add a new line for entry 27 exactly as shown in `<interfaces>` Section 2. Preserve the blank line that follows entry 26 → entry 27 → blank → "Phantom `trade.countered` ..." paragraph.

4. **Section 3 — Regression gate line.** Replace `... matches the frozen 26-event invariant.` with `... matches the frozen 27-event invariant.`

5. **Section 4 — Session Continuity block** (currently ~lines 134–139). Replace the four lines (`Last session:`, `Stopped at:`, `Resume file:`, `Next action:`) with the new values from `<interfaces>` Section 4 (timestamp `2026-04-27T23:30:00.000Z`, gap-closure stop-at line, `13-06-PLAN.md` resume file, `--gaps-only` next action).

6. **Section 5 — Phase 13 Accumulated Context block.** Insert the entire block from `<interfaces>` Section 5 immediately AFTER the Phase 10b Accumulated Context block ends (the last bullet of which is `- **Closed-enum allowlist gate confirmed:** ...`) and BEFORE the line `## Accumulated Context (Phase 11 — Mesh Whisper — shipped 2026-04-23)`. There must be exactly one blank line separating the inserted block's heading from the preceding bullet, and one blank line between the inserted block's last bullet and the Phase 11 heading.

7. Make NO other edits — do not refactor, do not reorder, do not add bullets that are not in the verbatim restoration content. The diff against `git show cb7d136 -- .planning/STATE.md` should show ZERO additional changes beyond the four blocks listed above.

Why no other changes: Doc-sync discipline (CLAUDE.md) requires the restoration to be byte-equivalent to the previously-shipped state plus the new gap-closure Session Continuity update. Adding extraneous content here muddies the regression-fix history.

Why these exact edits: `scripts/check-state-doc-sync.mjs` is a literal string-matching gate. It asserts the substring `27 events` appears in STATE.md AND the line containing `27.` and `operator.exported` appears in STATE.md AND the substring `27-event invariant` appears. Missing any of these three causes the CI gate to exit 1.
  </action>
  <verify>
    <automated>node scripts/check-state-doc-sync.mjs &amp;&amp; echo "DOC-SYNC-OK" || echo "DOC-SYNC-FAIL"</automated>
  </verify>
  <done>
    - `node scripts/check-state-doc-sync.mjs` exits 0 (prints "DOC-SYNC-OK").
    - `grep -c "27 events" .planning/STATE.md` returns ≥ 1.
    - `grep -c "operator.exported" .planning/STATE.md` returns ≥ 2 (one in entry 27, one in the Phase 13 Accumulated Context block).
    - `grep -c "Phase 13 — post-Wave-3, Plan 13-04" .planning/STATE.md` returns 1.
    - `grep -c "Accumulated Context (Phase 13 — Operator Replay" .planning/STATE.md` returns 1.
    - The Phase 11 Accumulated Context heading still appears AFTER the new Phase 13 block (verify with `grep -n "Accumulated Context (Phase 1" .planning/STATE.md` — Phase 13 line number must be less than Phase 11 line number).
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tar@^7.5.13 to root workspace so scripts/ and root vitest can resolve it (Gap 2)</name>
  <files>package.json, package-lock.json</files>
  <action>
The `tar` package is currently listed only in `grid/package.json`. `scripts/replay-verify.mjs` (which lives at repo root) imports `from 'tar'` and `npm` won't hoist a workspace-only dependency to a place that root-level Node resolution can find it. Two failures result:

- `node scripts/replay-verify.mjs` → `ERR_MODULE_NOT_FOUND` for `'tar'`
- `npx vitest run test/replay/tarball-determinism.test.ts` (run from repo root) → same error transitively

Fix: install `tar@^7.5.13` as a root-level dependency. Steps:

1. Read the current root `/Users/desirey/Programming/src/Noēsis/package.json`. Confirm there is no `dependencies` field and that `devDependencies` contains only `turbo` and `typescript`.

2. Run from repo root (NOT inside any workspace): `npm install tar@^7.5.13 --save`. This adds `"tar": "^7.5.13"` to root `package.json` `dependencies` and updates `package-lock.json`. Do NOT use `--save-dev` (this is a runtime dependency for `scripts/replay-verify.mjs`). Do NOT use `-w grid` (already there).

3. If `npm install` complains about workspace conflicts (e.g. tar version mismatch), confirm the `grid/package.json` constraint is `^7.5.13` (matching) — this is the expected case and `npm` will dedupe to a single hoisted copy. If `grid/package.json` had a stricter pin, escalate; do not silently widen it.

4. Verify the result by checking that `node_modules/tar` exists at repo root and resolves to a `package.json` with `"version"` matching `7.5.x`.

5. Commit message convention (for the eventual single git commit closing this plan): `chore(13-06): hoist tar@^7.5.13 to root workspace so scripts/replay-verify and root vitest resolve`.

Why hoist instead of move: `grid/src/export/tarball-builder.ts` also imports `from 'tar'` — moving it out of `grid/package.json` would break the grid workspace if it's ever published independently. Hoisting (root + workspace declaration) is the npm-idiomatic fix and matches how this repo already handles `vitest` and `oxc-jsx` (declared at multiple levels, deduped by npm).

Anti-pattern to avoid: do NOT add `tar` only to `package-lock.json` by hand-editing — run `npm install` and let it manage the lockfile. Do NOT add it to `devDependencies` — `scripts/replay-verify.mjs` is a CLI used at verify time, not a test-only import.
  </action>
  <verify>
    <automated>node -e "import('tar').then(()=&gt;console.log('TAR-OK')).catch(e=&gt;{console.error('TAR-FAIL',e.code);process.exit(1)})"</automated>
  </verify>
  <done>
    - Root `package.json` has a top-level `"dependencies": { "tar": "^7.5.13" }` block.
    - Root `package-lock.json` references `node_modules/tar` with a resolved version `7.5.x`.
    - `node -e "import('tar')"` from repo root succeeds without ERR_MODULE_NOT_FOUND.
    - `node scripts/replay-verify.mjs` (no args) prints its usage banner instead of throwing — it should exit with the help-message exit code (per the script's existing exit-code matrix), NOT with ERR_MODULE_NOT_FOUND.
    - `npx vitest run test/replay/tarball-determinism.test.ts` (run from repo root) shows 2 passing tests — the tar import resolves and the determinism assertions hold.
    - `grid/package.json` `tar` constraint is unchanged (`^7.5.13`) — this is a hoist, not a move.
  </done>
</task>

</tasks>

<verification>
Both gaps closed when the following all hold simultaneously:

```bash
# Gap 1 — STATE.md doc-sync
node scripts/check-state-doc-sync.mjs                     # exit 0
grep -c "27 events" .planning/STATE.md                    # ≥ 1
grep -c "Phase 13 — post-Wave-3" .planning/STATE.md       # = 1
grep -c "Accumulated Context (Phase 13" .planning/STATE.md # = 1

# Gap 2 — tar resolution
node -e "import('tar')"                                   # no error
node scripts/replay-verify.mjs                            # usage banner, no ERR_MODULE_NOT_FOUND
npx vitest run test/replay/tarball-determinism.test.ts    # 2/2 passing

# Sanity — nothing else broke
npm run pretest                                           # exit 0 (all 7 doc-sync/wallclock gates green)
```

If `npm run pretest` fails on a gate OTHER than `check-state-doc-sync` (e.g. `check-wallclock` or `check-whisper-plaintext`), STOP and investigate — the fix should not have touched code those gates protect.
</verification>

<success_criteria>
- `node scripts/check-state-doc-sync.mjs` exits 0.
- `node scripts/replay-verify.mjs` no longer throws `ERR_MODULE_NOT_FOUND`.
- `npx vitest run test/replay/tarball-determinism.test.ts` passes 2/2.
- STATE.md restored content matches `git show cb7d136 -- .planning/STATE.md` for the four affected blocks (allowlist heading, entry 27, regression-gate line, Phase 13 Accumulated Context block) plus the gap-closure-updated Session Continuity block.
- Root `package.json` declares `tar@^7.5.13` as a runtime dependency; `package-lock.json` reflects the install.
- No code changes outside the two listed files (`STATE.md`, `package.json`) except the auto-generated `package-lock.json` update.
- `npm run pretest` (runs all 7 doc-sync/wall-clock/whisper/governance gates) exits 0.
</success_criteria>

<output>
After completion, create `.planning/phases/13-operator-replay-export/13-06-SUMMARY.md` with:

- Confirmation that both gaps are closed (with the exact gate-output evidence)
- Diff stats for STATE.md (lines added vs. lines removed should be a small symmetric set)
- The exact `tar` version that npm resolved (`node_modules/tar/package.json` `version` field)
- Any deviation from this plan (expected: zero; if any, document and justify)
</output>
