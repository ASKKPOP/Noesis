---
phase: 13-operator-replay-export
plan: 05
type: execute
wave: 4
depends_on: ['13-01', '13-02', '13-03', '13-04']
files_modified:
  - dashboard/src/app/grid/replay/page.tsx
  - dashboard/src/app/grid/replay/replay-client.tsx
  - dashboard/src/app/grid/replay/scrubber.tsx
  - dashboard/src/app/grid/replay/use-replay-session.ts
  - dashboard/src/app/grid/replay/replay-client.test.tsx
  - dashboard/src/components/agency/export-consent-dialog.tsx
  - dashboard/src/components/agency/export-consent-dialog.test.tsx
  - dashboard/src/app/grid/grid-client.tsx
  - dashboard/src/app/grid/components/firehose.tsx
  - dashboard/src/app/grid/components/inspector.tsx
  - dashboard/src/app/grid/components/region-map.tsx
  - dashboard/tests/e2e/replay.spec.ts
  - scripts/check-wallclock-forbidden.mjs
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - README.md
autonomous: false
requirements: [REPLAY-02, REPLAY-05]
tags: [replay, ui, dashboard, agency, doc-sync, phase-13]

must_haves:
  truths:
    - "Visiting /grid/replay at H1/H2 renders the tier gate copy 'Replay requires H3' and no scrubber/Firehose/Inspector content."
    - "Visiting /grid/replay at H3+ renders the replay shell with REPLAY badge, amber border, and a wired scrubber."
    - "Firehose/Inspector/RegionMap render with replayMode=true on /grid/replay and read from the replay store, never the live store."
    - "H4-only fields render the literal placeholder '— Requires H4' inline; H5-only fields render '— Requires H5'."
    - "ExportConsentDialog opens only at H5; the Grid-ID input suppresses paste; Export button is disabled until typed === capturedGridId."
    - "Leaving /grid/replay (route change or unmount) calls agencyStore.setTier('H1') exactly once."
    - "scripts/check-wallclock-forbidden.mjs scans dashboard/src/app/grid/replay/** and exits 1 on Date.now / setInterval / setTimeout / Math.random / performance.now."
    - "Doc-sync is atomic: ROADMAP, STATE, MILESTONES, PROJECT, REQUIREMENTS, README all reflect the same Phase 13 plan list and 27-event allowlist count."
  artifacts:
    - path: "dashboard/src/app/grid/replay/page.tsx"
      provides: "Server component for /grid/replay route"
      contains: "export default async function ReplayPage"
    - path: "dashboard/src/app/grid/replay/replay-client.tsx"
      provides: "Client shell with tier gate, replay store provider, and tier auto-downgrade on unmount"
      exports: ["ReplayClient"]
    - path: "dashboard/src/app/grid/replay/scrubber.tsx"
      provides: "Range slider + numeric input + REPLAY badge banner"
      exports: ["Scrubber"]
    - path: "dashboard/src/app/grid/replay/use-replay-session.ts"
      provides: "Hook that fetches /api/v1/grid/audit slice and exposes target-tick state"
      exports: ["useReplaySession"]
    - path: "dashboard/src/components/agency/export-consent-dialog.tsx"
      provides: "H5-gated export dialog cloned from IrreversibilityDialog with Phase 13 verbatim copy"
      exports: ["ExportConsentDialog"]
    - path: "dashboard/tests/e2e/replay.spec.ts"
      provides: "E2E test: tier auto-downgrade fires on route exit"
    - path: "scripts/check-wallclock-forbidden.mjs"
      contains: "dashboard/src/app/grid/replay"
  key_links:
    - from: "dashboard/src/app/grid/replay/replay-client.tsx"
      to: "agencyStore.setTier('H1')"
      via: "useEffect cleanup"
      pattern: "setTier.*H1"
    - from: "dashboard/src/components/agency/export-consent-dialog.tsx"
      to: "POST /api/v1/operator/replay/export"
      via: "fetch on confirm"
      pattern: "fetch.*operator/replay/export"
    - from: "dashboard/src/app/grid/components/firehose.tsx"
      to: "replayMode prop"
      via: "conditional store read"
      pattern: "replayMode"
---

<objective>
Ship the Steward Console Rewind surface: `/grid/replay` route at H3+, replay-aware
Firehose/Inspector/RegionMap, the H5-gated ExportConsentDialog, tier auto-downgrade
discipline, the dashboard wall-clock CI gate extension, and atomic documentation
sync per CLAUDE.md (ROADMAP/STATE/MILESTONES/PROJECT/REQUIREMENTS/README all updated
in the same commit).

Purpose: REPLAY-02 needs a discoverable consent surface so operators cannot export
without typing the Grid-ID; REPLAY-05 needs the panel discovery surface so Stewards
can rewind without leaving the dashboard.

Output: A tier-gated viewer, a paste-suppressed consent dialog, and freshly-synced
planning docs reflecting the 27-event allowlist and the 5-plan Phase 13 structure.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-operator-replay-export/13-CONTEXT.md
@.planning/phases/13-operator-replay-export/13-RESEARCH.md
@.planning/phases/13-operator-replay-export/13-01-PLAN.md
@.planning/phases/13-operator-replay-export/13-02-PLAN.md
@.planning/phases/13-operator-replay-export/13-03-PLAN.md
@.planning/phases/13-operator-replay-export/13-04-PLAN.md
@dashboard/src/components/agency/irreversibility-dialog.tsx
@dashboard/src/app/grid/grid-client.tsx
@dashboard/src/lib/agency/agency-store.ts
@scripts/check-wallclock-forbidden.mjs
@CLAUDE.md

<interfaces>
<!-- Cloned from /grid client tree; Phase 13 viewer mirrors construction order. -->

From dashboard/src/components/agency/irreversibility-dialog.tsx (template):
```typescript
// Native <dialog>, role="alertdialog", aria-labelledby + aria-describedby
// Closure-capture: capturedDidRef.current = targetDid at open time
// Paste suppression: <input onPaste={(e) => e.preventDefault()} />
// Enter blocked in input: onKeyDown e.key === 'Enter' && e.preventDefault()
// autoFocus on Cancel button (safer default)
// Single onCancel source via native `close` event listener
```

From dashboard/src/lib/agency/agency-store.ts:
```typescript
export type Tier = 'H1' | 'H2' | 'H3' | 'H4' | 'H5';
export interface AgencyStore {
  currentTier: Tier;
  setTier(tier: Tier): void;
  subscribe(listener: () => void): () => void;
}
```

From scripts/check-wallclock-forbidden.mjs (Tier B TS roots — extension target):
```javascript
const TIER_B_TS_ROOTS = [
    'grid/src/bios',
    'grid/src/whisper',
    'grid/src/governance',
    // Phase 13 (D-13-01..D-13-09): replay viewer must derive all timing from
    // the rewound chain — no Date.now / setInterval / setTimeout / Math.random.
    'dashboard/src/app/grid/replay',  // ← APPEND THIS LINE
];
const TIER_B_TS_PATTERNS = [
    /\bDate\.now\b/,
    /\bperformance\.now\b/,
    /\bsetInterval\b/,    // ← APPEND (no auto-play per D-13-04)
    /\bsetTimeout\b/,     // ← APPEND
    /\bMath\.random\b/,   // ← APPEND
];
```

VERBATIM COPY (locked — tests assert literals; any paraphrase fails tests):
```typescript
// dashboard/src/components/agency/export-consent-dialog.tsx
const TITLE_COPY = 'Export audit chain slice';
const WARNING_COPY =
    'You are about to export an audit chain slice as a portable archive. The archive contains audit entries only — no plaintext bodies, memories, or whisper content. Once downloaded, you cannot recall it. Anyone with the file can verify and replay this slice forever.';
const GRID_ID_LABEL = 'Type the Grid-ID exactly to confirm:';
const HINT_MISMATCH = 'Grid-ID does not match. Type exactly as shown.';
const HINT_MATCH = 'Match confirmed.';
const CONFIRM_LABEL = 'Export forever';
const CANCEL_LABEL = 'Keep private';
const CONFIRM_ARIA = 'Export this audit chain slice. The archive cannot be recalled.';
const CANCEL_ARIA = 'Keep this slice private. No archive will be created.';

// dashboard/src/app/grid/replay/replay-client.tsx
const TIER_GATE_COPY = 'Replay requires H3'; // shown at H1/H2
const REPLAY_BADGE_COPY = 'REPLAY';            // amber-border banner
const H4_PLACEHOLDER = '— Requires H4';        // inline redaction
const H5_PLACEHOLDER = '— Requires H5';        // inline redaction
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ExportConsentDialog — H5-gated paste-suppressed dialog cloned from IrreversibilityDialog</name>
  <files>
    dashboard/src/components/agency/export-consent-dialog.tsx
    dashboard/src/components/agency/export-consent-dialog.test.tsx
  </files>

  <read_first>
    1. `dashboard/src/components/agency/irreversibility-dialog.tsx` — full file. This is the template. Clone the structure verbatim: native `<dialog>`, `role="alertdialog"`, `aria-labelledby` + `aria-describedby`, `useId()` for IDs, `capturedDidRef` closure-capture pattern, paste suppression on the input, Enter-blocking on the input, autoFocus on Cancel, single `onCancel` source via native `close` event, focus restoration to openerRef on close.
    2. `dashboard/src/components/agency/irreversibility-dialog.test.tsx` — clone the assertion structure (literal copy strings, paste suppression, exact-match guard, focus restoration, single-fire onCancel).
    3. `.planning/phases/13-operator-replay-export/13-CONTEXT.md` §D-13-09 — confirm the `operator.exported` payload shape so the dialog's `onConfirm` callback signature matches what the route accepts.
    4. `.planning/phases/13-operator-replay-export/13-01-PLAN.md` `<interfaces>` — confirm verbatim copy strings (re-quoted in this plan's `<interfaces>` block).
  </read_first>

  <behavior>
    Test 1: Renders TITLE_COPY, WARNING_COPY, GRID_ID_LABEL, CONFIRM_LABEL, CANCEL_LABEL exactly (literal-string assertions).
    Test 2: Paste event on Grid-ID input is suppressed (e.preventDefault called; input value unchanged after clipboard paste simulation).
    Test 3: Enter key on Grid-ID input is suppressed (no form submission, no onConfirm fire).
    Test 4: Confirm button disabled while typed !== capturedGridId; enabled at exact match; disabled again on mismatch typo.
    Test 5: HINT_MISMATCH renders below input when typed.length > 0 && typed !== capturedGridId.
    Test 6: HINT_MATCH renders when typed === capturedGridId.
    Test 7: capturedGridIdRef snapshots gridId at open time — changing the prop while open does not change the comparison target (closure-capture race).
    Test 8: autoFocus lands on Cancel button (safer default; Enter-on-open cannot export).
    Test 9: Single onCancel fire across ESC, backdrop click, Cancel button, and programmatic close (assert mock called exactly once per close path).
    Test 10: Focus restored to openerRef.current after close.
    Test 11: onConfirm receives no payload — the parent component owns the fetch call; the dialog only signals intent.
  </behavior>

  <action>
    Create `dashboard/src/components/agency/export-consent-dialog.test.tsx` with the 11 RED tests above (Wave 0 created the skeleton; this task fills it in). Use `@testing-library/react` and the same harness Phase 8 used for `irreversibility-dialog.test.tsx`.

    Create `dashboard/src/components/agency/export-consent-dialog.tsx`:
    - Clone `IrreversibilityDialog` line-for-line, then rename the component, props interface, and copy constants per D-13-09 + Phase 13 verbatim copy block.
    - Props interface:
      ```typescript
      export interface ExportConsentDialogProps {
        open: boolean;
        gridId: string;        // operator's Grid-ID (DID); shown for typing match
        onConfirm: () => void; // parent owns the fetch; dialog only signals
        onCancel: () => void;
        openerRef?: RefObject<HTMLElement | null>;
      }
      ```
    - Constants block at top: `TITLE_COPY`, `WARNING_COPY`, `GRID_ID_LABEL`, `CONFIRM_LABEL='Export forever'`, `CANCEL_LABEL='Keep private'`, `HINT_MISMATCH`, `HINT_MATCH`, `CONFIRM_ARIA`, `CANCEL_ARIA` (verbatim per `<interfaces>` block).
    - Closure-capture: `capturedGridIdRef.current = gridId` set in `useEffect([open])` only when open transitions false→true.
    - Paste suppression: `<input onPaste={(e) => e.preventDefault()} ... />`.
    - Enter suppression: `<input onKeyDown={(e) => { if (e.key === 'Enter') e.preventDefault(); }} ... />`.
    - autoFocus on Cancel button.
    - Single onCancel source via `useEffect` that adds a `close` event listener on the dialog node; ESC, backdrop, Cancel, programmatic all fire `dialog.close()` which fires the listener exactly once.
    - Confirm button `disabled={typed !== capturedGridIdRef.current}`.
    - When confirmed, call `onConfirm()` (no arguments — parent fetches `/api/v1/operator/replay/export`).
    - Run `cd dashboard && npx vitest run components/agency/export-consent-dialog --reporter=dot` — all 11 tests GREEN.
    - The fetch wiring lives in Task 2 (the replay client owns the dialog state + fetch; this dialog is pure UI).
  </action>

  <verify>
    <automated>cd dashboard && npx vitest run components/agency/export-consent-dialog --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `dashboard/src/components/agency/export-consent-dialog.tsx` exists with the 9 verbatim copy constants (assert via `grep -c "Export forever\|Keep private\|Type the Grid-ID exactly to confirm:" dashboard/src/components/agency/export-consent-dialog.tsx` ≥ 3).
    - 11 GREEN tests in `export-consent-dialog.test.tsx`.
    - Paste suppression assertion passes (the test simulates `await user.paste('foo')` and asserts input.value === '').
    - Closure-capture assertion passes (changing gridId prop while open does not change the comparison target — assert `rerender({ gridId: 'different' })` keeps the original Grid-ID as the typing target).
    - Single onCancel fire assertion passes for all 4 close paths.
    - `grep -c "useState\|useRef\|useEffect" dashboard/src/components/agency/export-consent-dialog.tsx` ≥ 3 (uses the same React primitives as the template; no exotic deps).
  </acceptance_criteria>

  <done>All 11 export-consent-dialog tests pass; verbatim copy strings present; paste/Enter suppression verified; closure-capture works; openerRef focus restoration works.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ReplayClient + Scrubber + replayMode prop wiring + tier auto-downgrade on unmount</name>
  <files>
    dashboard/src/app/grid/replay/page.tsx
    dashboard/src/app/grid/replay/replay-client.tsx
    dashboard/src/app/grid/replay/scrubber.tsx
    dashboard/src/app/grid/replay/use-replay-session.ts
    dashboard/src/app/grid/replay/replay-client.test.tsx
    dashboard/src/app/grid/components/firehose.tsx
    dashboard/src/app/grid/components/inspector.tsx
    dashboard/src/app/grid/components/region-map.tsx
  </files>

  <read_first>
    1. `dashboard/src/app/grid/page.tsx` + `grid-client.tsx` — full files. Clone the server-component + client-shell pattern: server fetches initial data, client mounts StoresProvider, client owns lifecycle effects.
    2. `dashboard/src/app/grid/components/firehose.tsx`, `inspector.tsx`, `region-map.tsx` — read each in full to understand current store-read paths. The `replayMode` prop will switch the read source from the live store to the replay store.
    3. `dashboard/src/lib/agency/agency-store.ts` — confirm `setTier('H1')` is the canonical downgrade call.
    4. `.planning/phases/13-operator-replay-export/13-CONTEXT.md` §D-13-04 — confirm "no auto-play" (no `setInterval` / `setTimeout`).
    5. `.planning/phases/13-operator-replay-export/13-CONTEXT.md` §D-13-07 — confirm tier auto-downgrade discipline on route exit.
    6. `.planning/phases/13-operator-replay-export/13-CONTEXT.md` §D-13-06 — confirm inline redaction with on-demand elevation (placeholder strings render inline, no separate redacted view).
    7. Wave 0 RED test files: `dashboard/src/app/grid/replay/replay-client.test.tsx` — confirm what scaffolds exist; this task fills GREEN.
  </read_first>

  <behavior>
    Test 1: ReplayClient at H1 renders TIER_GATE_COPY ('Replay requires H3') and NO scrubber, NO Firehose, NO Inspector, NO RegionMap.
    Test 2: ReplayClient at H2 renders the same tier gate.
    Test 3: ReplayClient at H3+ renders the REPLAY badge with amber border, the Scrubber, Firehose (replayMode=true), Inspector (replayMode=true), RegionMap (replayMode=true).
    Test 4: ReplayClient unmount calls agencyStore.setTier('H1') exactly once (assert via spy).
    Test 5: Route navigation away from /grid/replay (simulated via `unmount()`) calls setTier('H1') even at H3/H4/H5.
    Test 6: H4-only Inspector fields render '— Requires H4' literal at H3.
    Test 7: H5-only Inspector fields render '— Requires H5' literal at H3 and H4.
    Test 8: Scrubber renders a range input + a numeric input both bound to the same target-tick state.
    Test 9: Scrubber numeric input is clamped: typing a value > endTick clamps to endTick; typing < startTick clamps to startTick.
    Test 10: Scrubber emits no auto-advance (no setInterval/setTimeout in the rendered tree — use vitest fake-timers and assert clock not advanced after 5s wall-clock simulation).
    Test 11: Export button at H5 opens ExportConsentDialog with the operator's Grid-ID populated.
    Test 12: Export button at H3/H4 is disabled (tier-gated) with title attribute 'Export requires H5'.
    Test 13: On dialog confirm, fetch is called with POST /api/v1/operator/replay/export and the body matches the closed-tuple shape from D-13-09.
  </behavior>

  <action>
    `dashboard/src/app/grid/replay/page.tsx` (server component):
    - Pattern-clone of `dashboard/src/app/grid/page.tsx`.
    - Read `NEXT_PUBLIC_GRID_ORIGIN`. Best-effort fetch the audit slice metadata (start tick, end tick, entry count) from `/api/v1/grid/audit?limit=1` for initial slider bounds.
    - Hand to `<ReplayClient origin={origin} initialBounds={...} />`.
    - No `'use client'` directive (server component).

    `dashboard/src/app/grid/replay/replay-client.tsx` (client shell):
    - `'use client';`
    - useEffect cleanup: `return () => { agencyStore.setTier('H1'); };` (T-10-09 mitigation).
    - Read `agencyStore.currentTier` via `useSyncExternalStore`.
    - At H1/H2: render `<div className="tier-gate">{TIER_GATE_COPY}</div>`. Nothing else.
    - At H3+: render REPLAY badge banner (`<div className="replay-badge amber-border">REPLAY</div>`), the `<Scrubber />`, then `<Firehose replayMode={true} />`, `<Inspector replayMode={true} />`, `<RegionMap replayMode={true} />`.
    - Owns ExportConsentDialog state: `[exportDialogOpen, setExportDialogOpen] = useState(false)`. Export button at H5 opens it; at H3/H4 disabled with title 'Export requires H5'.
    - On dialog confirm: `await fetch(\`${origin}/api/v1/operator/replay/export\`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ tier: 'H5', operator_id, start_tick, end_tick }) })`. Reads response as blob, triggers browser download via temporary anchor element.

    `dashboard/src/app/grid/replay/scrubber.tsx`:
    - Pure presentational + lifted state via prop `value` + `onChange(tick: number)`.
    - Renders `<input type="range" min={startTick} max={endTick} value={value} onChange={...} />` and `<input type="number" min={startTick} max={endTick} value={value} onChange={...} />` bound to the same `value`.
    - Numeric input clamping: `Math.max(startTick, Math.min(endTick, parsed))` before calling onChange.
    - NO setInterval, NO setTimeout, NO Date.now (CI gate enforces this in Task 3).

    `dashboard/src/app/grid/replay/use-replay-session.ts`:
    - Hook signature: `useReplaySession(origin, startTick, endTick): { entries: AuditEntry[]; targetTick: number; setTargetTick: (t: number) => void; replayState: ReplayState | null }`.
    - Fetches `/api/v1/grid/audit?from=${startTick}&to=${endTick}` once on mount (no auto-refresh).
    - Does NOT call `Date.now`/`setInterval`/`setTimeout`/`Math.random` (CI gate).

    Modify `firehose.tsx`, `inspector.tsx`, `region-map.tsx`:
    - Add `replayMode?: boolean` prop with `false` default.
    - When `replayMode === true`, read from a passed-in replay-state prop (or replay store hook) instead of the live `useFirehoseStore` / `usePresenceStore`.
    - Inspector inline redaction: when `currentTier < H4`, render H4-restricted fields as `<span className="redaction-placeholder">— Requires H4</span>`; same pattern at `< H5` for H5-restricted fields.

    Run `cd dashboard && npx vitest run app/grid/replay --reporter=dot` — all 13 tests GREEN.
  </action>

  <verify>
    <automated>cd dashboard && npx vitest run app/grid/replay --reporter=dot</automated>
  </verify>

  <acceptance_criteria>
    - `/grid/replay` page renders without throwing at all 5 tiers (H1..H5).
    - `grep -c "setTier('H1')\|setTier(\"H1\")" dashboard/src/app/grid/replay/replay-client.tsx` ≥ 1.
    - `grep -c "replayMode" dashboard/src/app/grid/components/firehose.tsx dashboard/src/app/grid/components/inspector.tsx dashboard/src/app/grid/components/region-map.tsx` ≥ 3 (each component uses the prop).
    - Tier-gate test (Test 1) asserts the rendered DOM contains exactly one `<div>` with text 'Replay requires H3' and zero scrubber/Firehose/Inspector elements (use queryByTestId returning null for each).
    - Auto-downgrade test (Test 4) asserts via Vitest `vi.spyOn(agencyStore, 'setTier')` that the spy is called with 'H1' exactly once after `unmount()`.
    - No `setInterval`/`setTimeout`/`Date.now`/`performance.now`/`Math.random` in `dashboard/src/app/grid/replay/**` (CI gate in Task 3 enforces this; manual grep here as belt-and-suspenders: `! grep -rn "setInterval\|setTimeout\|Date\.now\|performance\.now\|Math\.random" dashboard/src/app/grid/replay/ --include="*.ts" --include="*.tsx"`).
    - All 13 tests GREEN.
  </acceptance_criteria>

  <done>Tier gate works at H1/H2; replay shell renders at H3+; tier auto-downgrade fires exactly once on unmount; replayMode prop wired on all three panels; ExportConsentDialog opens only at H5; all 13 tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wall-clock CI gate extension + atomic doc-sync (ROADMAP, STATE, MILESTONES, PROJECT, REQUIREMENTS, README)</name>
  <files>
    scripts/check-wallclock-forbidden.mjs
    dashboard/tests/e2e/replay.spec.ts
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/MILESTONES.md
    .planning/PROJECT.md
    .planning/REQUIREMENTS.md
    README.md
  </files>

  <read_first>
    1. `scripts/check-wallclock-forbidden.mjs` — full file. Locate the `TIER_B_TS_ROOTS` array and the `TIER_B_TS_PATTERNS` array (currently `Date.now`, `performance.now`).
    2. `dashboard/tests/e2e/grid-page.spec.ts` — clone the structure and the DASHBOARD_ROUTES pattern for the new replay e2e.
    3. `CLAUDE.md` — re-read the "Documentation Sync Rule" section. The atomic-commit clause says: "Before writing the plan, edit, or new research: identify which of the files above the change touches. In the same turn, update every affected file — don't defer."
    4. `.planning/ROADMAP.md` — locate the Phase 13 block (currently lines ~170-189). The `**Plans**: TBD` line is the placeholder to replace.
    5. `.planning/STATE.md` — locate the Accumulated Context section. The "26 events" literal must bump to 27 with `operator.exported` appended to the allowlist members list.
    6. `.planning/MILESTONES.md` — locate the v2.2 Living Grid section. Phase 13 entry will be appended only when the phase completes verification; for now, this task only updates the in-flight Phase 13 plan list under ROADMAP.
    7. `.planning/PROJECT.md` — locate the Active REQs section. REPLAY-01..05 must be listed as Active.
    8. `.planning/REQUIREMENTS.md` — locate REPLAY-01..05 entries.
    9. `README.md` — locate the "Current status" section.
  </read_first>

  <behavior>
    Test 1: scripts/check-wallclock-forbidden.mjs exits 0 on a clean run after edits.
    Test 2: scripts/check-wallclock-forbidden.mjs exits 1 when a synthetic `Date.now()` is added to dashboard/src/app/grid/replay/scrubber.tsx (revert immediately).
    Test 3: scripts/check-wallclock-forbidden.mjs exits 1 when a synthetic `setInterval` is added to dashboard/src/app/grid/replay/use-replay-session.ts (revert immediately).
    Test 4: e2e replay.spec.ts asserts navigating to /grid/replay then /grid triggers `agencyStore.setTier('H1')` (assert via dom-state probe — the live grid renders without REPLAY badge after navigation).
    Test 5: ROADMAP.md Phase 13 block contains 5 PLAN.md entries with the exact paths.
    Test 6: STATE.md Accumulated Context section contains "27 events" and the literal string `'operator.exported'`.
    Test 7: REQUIREMENTS.md REPLAY-01..05 entries are listed under Active (not Future, not Out-of-Scope).
  </behavior>

  <action>
    **Step 1 — Wall-clock CI gate extension** (`scripts/check-wallclock-forbidden.mjs`):
    - Append `'dashboard/src/app/grid/replay'` to `TIER_B_TS_ROOTS` with a Phase 13 comment block.
    - Append `/\\bsetInterval\\b/`, `/\\bsetTimeout\\b/`, `/\\bMath\\.random\\b/` to `TIER_B_TS_PATTERNS`.
    - Update the docblock at the top of the file to mention Phase 13 / D-13-04 (no auto-play) discipline.
    - Verify clean run: `node scripts/check-wallclock-forbidden.mjs` → exit 0.
    - Synthetic violation test: temporarily add `Date.now();` to `dashboard/src/app/grid/replay/scrubber.tsx`, run gate, expect exit 1, then revert.

    **Step 2 — E2E auto-downgrade test** (`dashboard/tests/e2e/replay.spec.ts`):
    - Clone the structure from `dashboard/tests/e2e/grid-page.spec.ts`.
    - Test: `test('tier auto-downgrade on /grid/replay route exit')`:
      1. Navigate to `/grid/replay` after setting tier to H3 via fixture.
      2. Assert REPLAY badge is visible.
      3. Navigate to `/grid` (back to live).
      4. Assert REPLAY badge is NOT visible (proxy for tier=H1; the H3-only panels have downgraded).
    - Add `/grid/replay` to any DASHBOARD_ROUTES exhaustive-list helper if one exists.

    **Step 3 — Atomic doc-sync** (per CLAUDE.md user-mandated rule — same commit, no deferral):

    `.planning/ROADMAP.md` Phase 13 block:
    - Replace the `**Plans**: TBD` line with:
      ```
      **Plans**:
      - [ ] 13-01-PLAN.md — Wave 0: deps install + 9 RED test files + verbatim copy locked
      - [ ] 13-02-PLAN.md — Wave 1: ReadOnlyAuditChain + ReplayGrid + state-builder + check-replay-readonly CI gate
      - [ ] 13-03-PLAN.md — Wave 2: canonical-json + deterministic tarball + replay-verify CLI
      - [ ] 13-04-PLAN.md — Wave 3: appendOperatorExported + allowlist 26→27 + Fastify route + check-state-doc-sync bump + replay.* hard-ban
      - [ ] 13-05-PLAN.md — Wave 4: Dashboard /grid/replay UI + ExportConsentDialog + wall-clock gate extension + atomic doc-sync
      ```
    - Confirm `**UI hint**: yes` already present; add if missing.

    `.planning/STATE.md` Accumulated Context:
    - Bump literal "26 events" → "27 events" wherever it appears.
    - Append `'operator.exported'` to the allowlist members list at position 27.
    - Add a Phase 13 line under "Active focus" or equivalent: "Phase 13 — Operator Replay & Export (5 plans, REPLAY-01..05)".
    - Append a session-state entry dated 2026-04-27: "Phase 13 plans authored (5 PLAN.md files, Waves 0-4). Allowlist will bump 26→27 on Phase 13 verify."

    `.planning/PROJECT.md`:
    - Confirm REPLAY-01..05 are listed under Active (move from Future if needed).
    - Add Phase 13 to the in-flight phase list.

    `.planning/REQUIREMENTS.md`:
    - Confirm REPLAY-01..05 entries exist and are tagged Active. Add if missing using existing entry format.

    `.planning/MILESTONES.md`:
    - Do NOT append a Phase 13 completion entry yet — that fires only when verification passes. Just confirm v2.2 Living Grid section lists Phase 13 as in-flight.

    `README.md`:
    - Update the "Current status" section to reference Phase 13 in-flight: "Phase 13 (Operator Replay & Export) plans authored — Steward Console rewind surface plus reproducible audit-chain tarball export."
    - If README's feature list claims any v2.2 capability that Phase 13 will deliver, ensure the language matches what these 5 plans actually ship (chain-only export at MVP per D-13-09; no plaintext memory/whisper export).

    **Step 4 — Manual e2e run validation**:
    - `cd dashboard && npx playwright test tests/e2e/replay.spec.ts` — must pass.
  </action>

  <verify>
    <automated>node scripts/check-wallclock-forbidden.mjs &amp;&amp; cd dashboard &amp;&amp; npx playwright test tests/e2e/replay.spec.ts</automated>
  </verify>

  <acceptance_criteria>
    - `node scripts/check-wallclock-forbidden.mjs` exits 0 on clean repo.
    - `grep -c "dashboard/src/app/grid/replay" scripts/check-wallclock-forbidden.mjs` ≥ 1.
    - `grep -c "setInterval\|setTimeout\|Math\\.random" scripts/check-wallclock-forbidden.mjs` ≥ 3.
    - `grep -c "13-05-PLAN.md\|13-04-PLAN.md\|13-03-PLAN.md\|13-02-PLAN.md\|13-01-PLAN.md" .planning/ROADMAP.md` ≥ 5.
    - `grep -c "27 events\|operator\\.exported" .planning/STATE.md` ≥ 2.
    - `grep -c "REPLAY-01\|REPLAY-02\|REPLAY-03\|REPLAY-04\|REPLAY-05" .planning/REQUIREMENTS.md` ≥ 5.
    - `grep -c "Phase 13" README.md` ≥ 1.
    - E2E test `tier auto-downgrade on /grid/replay route exit` passes.
    - All 6 doc files updated in this single task — no deferral (CLAUDE.md atomic-commit clause).
  </acceptance_criteria>

  <done>Wall-clock CI gate covers dashboard/src/app/grid/replay with 5 forbidden patterns; e2e replay test passes; all 6 source-of-truth docs reflect the 5-plan structure and the 27-event allowlist count; CLAUDE.md atomic-commit rule honored (one task, all docs synced).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification — /grid/replay scrubber + REPLAY badge in dark theme</name>
  <files>dashboard/src/app/grid/replay/replay-client.tsx, dashboard/src/components/agency/export-consent-dialog.tsx (visual review only — no edits)</files>
  <action>Human-verify checkpoint. No code changes. Execute the 16 verification steps in &lt;how-to-verify&gt; below; record pass/fail per step.</action>
  <verify><automated>MISSING — checkpoint:human-verify is manual by definition; pre-checkpoint automation (Tasks 1–3 vitest + playwright + CI gate) must be GREEN before this task runs</automated></verify>
  <done>All 16 visual checks pass under human review; resume-signal "approved" received.</done>

  <what-built>
    - `/grid/replay` route at H3+ renders the REPLAY badge banner with amber border.
    - Native HTML range slider thumb is visible and accessible in the dark theme.
    - ExportConsentDialog at H5 renders centered with verbatim copy and paste-suppressed Grid-ID input.
    - Tier auto-downgrade fires on route exit (the live `/grid` page no longer shows REPLAY content).
  </what-built>

  <how-to-verify>
    1. Start the dashboard dev server: `cd dashboard && npm run dev`.
    2. Open the browser to http://localhost:3001.
    3. In the agency selector, choose tier H3.
    4. Navigate to /grid/replay.
    5. Confirm the REPLAY badge banner is visible at the top of the page with an amber-colored border (verify against design-tokens.css amber color).
    6. Confirm the range slider renders with a visible, accessible thumb (drag it; tab-focus it; assert the thumb is keyboard-navigable per WCAG 2.1).
    7. Confirm the numeric input next to the slider accepts values within [startTick, endTick] and clamps out-of-range input.
    8. Verify Inspector inline redaction: open a Nous in the Inspector. At H3, fields tagged H4 should render the literal string '— Requires H4'; H5-only fields render '— Requires H5'.
    9. Bump tier to H5 in the agency selector. Click the Export button.
    10. Confirm ExportConsentDialog opens centered with: title 'Export audit chain slice', warning copy, Grid-ID input labeled 'Type the Grid-ID exactly to confirm:', Confirm button labeled 'Export forever' (initially disabled), Cancel button labeled 'Keep private' (autofocused).
    11. Try pasting into the Grid-ID input via Cmd+V (or right-click paste). Verify the input remains empty (paste suppression working).
    12. Type the Grid-ID character-by-character. Verify Confirm enables only at exact match.
    13. Click Cancel. Dialog closes; focus returns to the Export button.
    14. Click Export → type Grid-ID → click 'Export forever'. Verify a tarball downloads.
    15. Run `node scripts/replay-verify.mjs ~/Downloads/<the-tarball>.tar` — exit 0.
    16. Navigate from /grid/replay to /grid (use the nav). Confirm the live grid shows no REPLAY badge (tier auto-downgraded).
  </how-to-verify>

  <resume-signal>Type "approved" if all 16 visual checks pass, or describe specific issues (e.g., "slider thumb invisible in dark theme", "paste was not suppressed", "tier did not downgrade on route exit").</resume-signal>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Dashboard route | Navigation to /grid/replay; tier-gate enforcement happens client-side AND server-side |
| Dashboard → Grid (`/api/v1/operator/replay/export`) | H5-gated POST; rejected without typed Grid-ID confirmation |
| ExportConsentDialog clipboard surface | Paste suppression on Grid-ID input prevents one-click exfil-flow assistance |
| Tier auto-downgrade (route exit) | Cleanup effect MUST fire even on hard navigation, browser back, and tab close |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | dashboard/src/app/grid/replay | mitigate | 3-layer defense: (1) tier gate at H3 hides scrubber + panels at H1/H2; (2) inline redaction placeholders for H4/H5 fields read agencyStore.currentTier reactively (same source as live grid — no separate replay-tier state); (3) tier auto-downgrade to H1 on route exit via useEffect cleanup so a forgotten replay session does not leak into live grid as elevated tier |
| T-10-10 | Information Disclosure | export-consent-dialog.tsx | mitigate | Paste suppression on Grid-ID input forces character-by-character typing; closure-capture snapshots Grid-ID at open time (race-safe); confirm button disabled until exact match; Enter blocked in input (prevents form-submit one-shot); the dialog only signals intent — the parent component owns the fetch, so a leaked dialog state cannot trigger an export |
| T-10-08 | Tampering | scrubber.tsx + use-replay-session.ts | mitigate | CI gate `scripts/check-wallclock-forbidden.mjs` extended to scan `dashboard/src/app/grid/replay/**` for `Date.now`, `performance.now`, `setInterval`, `setTimeout`, `Math.random`. Replay timing derives only from the rewound chain's tick — no wall-clock primitives anywhere in the replay tree |
| T-13-05-01 | Tampering | replay-client.tsx tier auto-downgrade | mitigate | E2E test in `dashboard/tests/e2e/replay.spec.ts` asserts the live grid no longer renders REPLAY content after navigating from /grid/replay to /grid. Belt-and-suspenders: the cleanup effect uses the React useEffect return-function contract which fires on unmount and on dependency change |
| T-13-05-02 | Information Disclosure | inspector.tsx replayMode redaction | mitigate | Inline placeholder strings '— Requires H4' / '— Requires H5' render unconditionally when currentTier < required. Tests assert the literal placeholder text exists in the DOM at H3 for H4-required fields. No data is fetched-then-hidden — the redacted fields never reach the React tree at the wrong tier |
| T-13-05-03 | Elevation of Privilege | export button tier-gate bypass | mitigate | Server-side validation in 13-04 Plan (Task 2) rejects exports without the H5 tier in the request body. Client-side button disable at H3/H4 is a UX layer only; the route validation is the hard boundary. Direct nav to /grid/replay does not bypass — the tier gate component renders only at H3+ |
| T-13-05-04 | Repudiation | export action audit trail | mitigate | Plan 13-04 Task 1 ensures `appendOperatorExported` fires AFTER tarball build BEFORE response. Every successful export produces an `operator.exported` audit entry with the operator_id and tarball_hash; this entry itself is part of the chain-tail and replayable |
| T-13-05-05 | Denial of Service | scrubber rapid-drag fetch storm | accept | The scrubber's onChange debounces naturally via React batching; each drag-step does not trigger a refetch — `useReplaySession` fetches the audit slice ONCE on mount and rebuilds state from already-loaded entries on tick change. No fetch storm risk. If perf becomes an issue post-MVP, virtualize the slice in a follow-up |

</threat_model>

<verification>
- All 4 tasks complete and verified per their `<acceptance_criteria>`.
- Task 1: 11 ExportConsentDialog tests GREEN.
- Task 2: 13 ReplayClient/Scrubber tests GREEN.
- Task 3: Wall-clock CI gate exits 0 on clean repo, exits 1 on synthetic violations; e2e replay.spec.ts passes; all 6 source-of-truth docs synced atomically.
- Task 4: 16 visual checks pass under human review (REPLAY badge, slider, dialog copy, paste suppression, redaction placeholders, tier auto-downgrade).
- `cd dashboard && npx vitest run --reporter=dot` — full dashboard suite GREEN.
- `cd dashboard && npx playwright test` — all e2e tests GREEN.
- `node scripts/check-wallclock-forbidden.mjs` exits 0.
- `node scripts/check-replay-readonly.mjs` exits 0 (Wave 1 gate still green after Wave 4 changes — no `.append(` introduced in `grid/src/replay/**`).
- `node scripts/check-state-doc-sync.mjs` exits 0 (Wave 3 gate still green after STATE.md bump to 27 events).
</verification>

<success_criteria>
- `/grid/replay` renders the tier gate at H1/H2 and the replay shell at H3+.
- ExportConsentDialog ships verbatim copy, paste suppression, closure-capture race safety, and single-fire onCancel discipline.
- Firehose / Inspector / RegionMap accept `replayMode` prop and read from the replay store, never the live store, when set.
- Tier auto-downgrade to H1 fires on route exit; e2e test enforces this.
- Wall-clock CI gate scans `dashboard/src/app/grid/replay/**` and rejects 5 forbidden patterns.
- ROADMAP / STATE / MILESTONES / PROJECT / REQUIREMENTS / README all synced atomically per CLAUDE.md doc-sync rule.
- Phase 13 ships the Steward Console rewind surface (REPLAY-05) and the H5 export consent UI (REPLAY-02 surface).
</success_criteria>

<output>
After completion, create `.planning/phases/13-operator-replay-export/13-05-SUMMARY.md` with:
- Files created (page.tsx, replay-client.tsx, scrubber.tsx, use-replay-session.ts, export-consent-dialog.tsx, replay.spec.ts) and files modified (firehose, inspector, region-map, check-wallclock-forbidden.mjs, ROADMAP, STATE, MILESTONES, PROJECT, REQUIREMENTS, README).
- Test counts: 11 export-consent-dialog tests + 13 replay-client tests + 1 e2e auto-downgrade test = 25 GREEN tests.
- CI gate status: check-wallclock-forbidden.mjs extended; clean run confirmed.
- Doc-sync confirmation: 6 source-of-truth files updated in the same commit per CLAUDE.md.
- Visual verification sign-off (Task 4 checkpoint approval).
- Threats mitigated: T-10-09 (3-layer tier defense), T-10-10 (paste-suppressed Grid-ID), T-10-08 (wall-clock gate), T-13-05-01..05 (route exit, redaction, gate bypass, repudiation, DoS).
- Phase 13 status: ready for /gsd-verify-work.
</output>
