---
phase: 13-operator-replay-export
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - grid/package.json
  - grid/test/replay/readonly-chain.test.ts
  - grid/test/replay/replay-grid.test.ts
  - grid/test/replay/state-builder.test.ts
  - grid/test/replay/tarball-determinism.test.ts
  - grid/test/audit/operator-exported-allowlist.test.ts
  - grid/test/audit/operator-exported-payload-privacy.test.ts
  - grid/test/audit/operator-exported-producer-boundary.test.ts
  - dashboard/src/app/grid/replay/replay-client.test.tsx
  - dashboard/src/app/grid/replay/export-consent-dialog.test.tsx
autonomous: true
requirements: [REPLAY-01, REPLAY-02, REPLAY-03, REPLAY-04, REPLAY-05]

must_haves:
  truths:
    - "better-sqlite3 and tar packages installed and importable from grid/"
    - "RED test files exist and fail (no implementation yet) for every Wave 1-4 acceptance criterion"
    - "Test failure messages reference the eventual implementation file path so executor knows what to build"
    - "operator.exported payload tuple shape locked in test assertion (D-13-09)"
    - "ExportConsentDialog verbatim copy strings present in dashboard test as test constants (D-13-08)"
  artifacts:
    - path: grid/package.json
      provides: "better-sqlite3@^12.9.0 and tar@^7.5.13 dependency declarations"
      contains: "better-sqlite3"
    - path: grid/test/replay/readonly-chain.test.ts
      provides: "RED stub asserting ReadOnlyAuditChain.append() throws"
      min_lines: 30
    - path: grid/test/replay/replay-grid.test.ts
      provides: "RED stub asserting ReplayGrid uses isolated chain instance (!== live audit)"
      min_lines: 40
    - path: grid/test/replay/state-builder.test.ts
      provides: "RED stub asserting state-level replay byte-identical to live (D-13-04, REPLAY-04)"
      min_lines: 30
    - path: grid/test/replay/tarball-determinism.test.ts
      provides: "RED stub asserting tarball SHA-256 stable across 3 builds (REPLAY-01)"
      min_lines: 30
    - path: grid/test/audit/operator-exported-allowlist.test.ts
      provides: "RED stub asserting allowlist size 27 and operator.exported at position 27"
      min_lines: 25
    - path: grid/test/audit/operator-exported-payload-privacy.test.ts
      provides: "RED stub: 6+ forbidden keys × flat + nested cases (clones D-12 pattern)"
      min_lines: 60
    - path: grid/test/audit/operator-exported-producer-boundary.test.ts
      provides: "RED stub greps grid/src for chain.append('operator.exported') sole-producer rule"
      min_lines: 25
    - path: dashboard/src/app/grid/replay/replay-client.test.tsx
      provides: "RED stub: tier-gate H1/H2 shows 'Replay requires H3'; H4 redaction placeholder visible"
      min_lines: 50
    - path: dashboard/src/app/grid/replay/export-consent-dialog.test.tsx
      provides: "RED stub: verbatim copy assertion + paste-suppressed Grid-ID input"
      min_lines: 60
  key_links:
    - from: grid/test/replay/readonly-chain.test.ts
      to: grid/src/replay/readonly-chain.ts
      via: "import { ReadOnlyAuditChain } — file does NOT yet exist (Wave 1 creates)"
      pattern: "from '\\.\\./\\.\\./src/replay/readonly-chain"
    - from: grid/test/audit/operator-exported-allowlist.test.ts
      to: grid/src/audit/broadcast-allowlist.ts
      via: "ALLOWLIST_MEMBERS.length expected 27 — fails until Wave 3 bumps 26→27"
      pattern: "ALLOWLIST_MEMBERS"
---

<objective>
Wave 0 establishes RED test scaffolding for all of Phase 13, plus installs the two new runtime dependencies (`better-sqlite3` for in-memory ReplayGrid chain, `tar` for deterministic tarball export). Every test in this plan MUST fail when executed — they encode acceptance criteria for Waves 1-4 to satisfy.

Purpose: Lock acceptance criteria into executable form BEFORE any implementation lands. Wave 1-4 plans turn RED tests GREEN one by one. This is the Nyquist gate: nothing ships without an automated verifier specified upfront.

Output: 9 RED test files + 1 package.json modification. Subsequent waves read these tests as the canonical contract for what they must build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-operator-replay-export/13-CONTEXT.md
@.planning/phases/13-operator-replay-export/13-RESEARCH.md
@.planning/phases/13-operator-replay-export/13-UI-SPEC.md
@.planning/phases/13-operator-replay-export/13-VALIDATION.md
@grid/src/audit/broadcast-allowlist.ts
@grid/src/audit/append-nous-deleted.ts
@grid/src/audit/chain.ts
@dashboard/src/components/agency/irreversibility-dialog.tsx

<interfaces>
<!-- Key types and exports the executor will reference. Sourced from existing codebase. -->

From grid/src/audit/append-nous-deleted.ts (template for operator-exported producer-boundary tests):
```typescript
export const HEX64_RE = /^[0-9a-f]{64}$/;
export const DID_RE = /^did:noesis:[a-z0-9_\-]+$/i;
export const OPERATOR_ID_RE = /^[a-zA-Z0-9._\-]{1,64}$/;
// Exact-equality closed-tuple pattern: Object.keys(payload).sort() === EXPECTED_KEYS_SORTED
```

From grid/src/audit/broadcast-allowlist.ts (current state — 26 entries):
```typescript
const ALLOWLIST_MEMBERS: readonly string[] = [
    'nous.spawned', 'nous.moved', 'nous.spoke', 'nous.direct_message',
    'trade.proposed', 'trade.reviewed', 'trade.settled', 'law.triggered',
    'tick', 'grid.started', 'grid.stopped',
    'operator.inspected', 'operator.paused', 'operator.resumed',
    'operator.law_changed', 'operator.telos_forced',
    'telos.refined', 'operator.nous_deleted', 'ananke.drive_crossed',
    'bios.birth', 'bios.death', 'nous.whispered',
    'proposal.opened', 'ballot.committed', 'ballot.revealed', 'proposal.tallied',
];
// Phase 13 will append 'operator.exported' at position 27.
```

From grid/src/audit/chain.ts:
```typescript
export class AuditChain {
    append(eventType: string, actorDid: string, payload: Record<string, unknown>, targetDid?: string): AuditEntry;
    onAppend(listener: AppendListener): Unsubscribe;
    loadEntries(entries: AuditEntry[]): void;  // Does NOT fire onAppend listeners
    static computeHash(prevHash, eventType, actorDid, payload, timestamp): string;
    get head(): string;
}
```

From dashboard/src/components/agency/irreversibility-dialog.tsx (template for ExportConsentDialog):
- Pattern: native `<dialog>` + `showModal()`
- Paste suppression: `onPaste={(e) => e.preventDefault()}`
- Closure capture: `capturedDidRef.current = targetDid` at open time
- Verbatim copy constants pinned in test assertions

operator.exported closed-tuple shape (D-13-09):
```typescript
{
    tier: 'H5',                  // literal
    operator_id: string,         // OPERATOR_ID_RE
    start_tick: number,          // non-negative integer
    end_tick: number,            // start_tick <= end_tick, both non-negative integers
    tarball_hash: string,        // HEX64_RE (SHA-256 of tarball bytes)
    requested_at: number,        // Unix SECONDS, < 10_000_000_000
}
// Object.keys(payload).sort() === ['end_tick','operator_id','requested_at','start_tick','tarball_hash','tier']
```

ExportConsentDialog verbatim copy (D-13-08, from 13-UI-SPEC.md):
- TITLE_COPY: 'Export audit chain slice'
- WARNING_COPY: 'This export is permanent and cannot be undone. The tarball will contain the complete audit chain for the selected tick range. Anyone with the file can verify the chain.'
- CONFIRM_LABEL: 'Export forever'
- CANCEL_LABEL: 'Keep private'
- INPUT_LABEL: 'Type the Grid-ID exactly to confirm:'
- HINT_MATCH: 'Match confirmed.'
- HINT_MISMATCH: 'Grid-ID does not match. Type exactly as shown.'
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install runtime deps + scaffold Grid-side RED tests</name>
  <read_first>
    - grid/package.json (current state — confirm absent libs)
    - grid/src/audit/broadcast-allowlist.ts (current 26-entry layout)
    - grid/src/audit/append-nous-deleted.ts (template for sole-producer + payload checks)
    - grid/src/audit/chain.ts (AuditChain interface — RED tests will instantiate)
    - .planning/phases/13-operator-replay-export/13-VALIDATION.md (per-task verification map)
  </read_first>
  <files>
    grid/package.json,
    grid/test/replay/readonly-chain.test.ts,
    grid/test/replay/replay-grid.test.ts,
    grid/test/replay/state-builder.test.ts,
    grid/test/replay/tarball-determinism.test.ts,
    grid/test/audit/operator-exported-allowlist.test.ts,
    grid/test/audit/operator-exported-payload-privacy.test.ts,
    grid/test/audit/operator-exported-producer-boundary.test.ts
  </files>
  <behavior>
    - Test 1 (readonly-chain): `new ReadOnlyAuditChain(initialEntries).append(...)` throws TypeError with message containing 'read-only'.
    - Test 2 (replay-grid): `new ReplayGrid(...)` exposes `.audit` instance; `replayGrid.audit !== liveLauncher.audit` (separate instance).
    - Test 3 (state-builder): replaying a 50-tick canned scenario reproduces `liveLauncher.relationships.allEdges()` byte-identical to live (sorted JSON serialization).
    - Test 4 (tarball-determinism): building the same tarball 3× from the same input produces 3 identical SHA-256 hashes.
    - Test 5 (allowlist): `ALLOWLIST_MEMBERS.length === 27`; `ALLOWLIST_MEMBERS[26] === 'operator.exported'`; ALLOWLIST.has('operator.exported') is true.
    - Test 6 (payload-privacy): for each forbidden key in `EXPORT_FORBIDDEN_KEYS = ['body','entries','text','chain_data','plaintext','manifest_body']`, both flat (`{tier,...,body:'x'}`) and nested (`{tier,...,operator_id:{body:'x'}}`) cases throw at the producer boundary.
    - Test 7 (producer-boundary): grep over `grid/src/**/*.ts` finds exactly ONE file containing `chain.append('operator.exported'` or `audit.append('operator.exported'` — and that file is `grid/src/audit/append-operator-exported.ts`.
  </behavior>
  <action>
    Step 1 — Install deps. Edit `grid/package.json`. Per D-13 (Claude's Discretion: tarball format choices) and 13-RESEARCH.md §Pattern 7:
    - Append to `dependencies`: `"better-sqlite3": "^12.9.0"`, `"tar": "^7.5.13"`.
    - Append to `devDependencies`: `"@types/better-sqlite3": "^7.6.13"`, `"@types/tar": "^6.1.13"`.
    Run `cd grid && npm install` so `package-lock.json` is updated and the new modules are importable from tests.

    Step 2 — Create `grid/test/replay/readonly-chain.test.ts`:
    - Import: `import { describe, it, expect } from 'vitest';`
    - Import (will fail until Wave 1): `import { ReadOnlyAuditChain } from '../../src/replay/readonly-chain.js';`
    - Test cases:
      1. `it('append() throws TypeError with "read-only" in message', ...)` — instantiate with 1 entry, call `.append('tick', 'system', {})`, expect throws.
      2. `it('loadEntries() and read APIs work', ...)` — verify chain.length, chain.head, chain.at(0) all functional from the loaded entries.
      3. `it('does not fire onAppend listeners on construction', ...)` — `let fired = 0; chain.onAppend(() => fired++);` after construction `expect(fired).toBe(0)` (loadEntries silent restore).
    - Each test ends with comment `// RED until Wave 1 (Plan 13-02) creates grid/src/replay/readonly-chain.ts`.

    Step 3 — Create `grid/test/replay/replay-grid.test.ts`:
    - Import (will fail): `import { ReplayGrid } from '../../src/replay/replay-grid.js';`
    - Import: `import { GenesisLauncher } from '../../src/genesis/launcher.js';`
    - Test cases:
      1. `it('exposes isolated AuditChain instance distinct from live launcher', ...)` — construct ReplayGrid with 0 entries; assert `replay.audit !== liveLauncher.audit`.
      2. `it('rejects .append() through readonly chain', ...)` — try `replay.audit.append('tick','system',{})`, expect throws.
      3. `it('rebuilds derived state silently (no listener double-fire)', ...)` — pass an entries array containing 2 `nous.spawned` events; assert `replay.relationships.allEdges()` is correctly populated AFTER explicit `.rebuildFromChain()` (loadEntries silent caveat from chain.ts:74).
    - Each test ends with comment `// RED until Wave 1 (Plan 13-02) creates grid/src/replay/replay-grid.ts`.

    Step 4 — Create `grid/test/replay/state-builder.test.ts`:
    - Import (will fail): `import { buildStateAtTick } from '../../src/replay/state-builder.js';`
    - Test cases:
      1. `it('reproduces relationship edges byte-identical to live launcher at tick 50', ...)` — run a live launcher 50 ticks with 2 trades + 4 dialogues, capture `live.relationships.allEdges()`; create a ReplayGrid with the same entries; call `buildStateAtTick(replay, 50)`; assert sorted JSON serialization identical.
      2. `it('returns empty derived state at tick 0', ...)` — sanity check.
    - End with `// RED until Wave 2 (Plan 13-03) creates grid/src/replay/state-builder.ts`.

    Step 5 — Create `grid/test/replay/tarball-determinism.test.ts`:
    - Import (will fail): `import { buildExportTarball } from '../../src/replay/tarball.js';`
    - Test cases:
      1. `it('produces identical SHA-256 across 3 builds with same input', ...)` — fixture entries (5 entries), call `buildExportTarball({entries, startTick:0, endTick:5, gridName:'test-grid'})` 3 times, hash each output buffer, assert all 3 hashes equal.
      2. `it('manifest contains chain-tail hash matching last entry eventHash', ...)` — read manifest entry from tarball, assert `manifest.chain_tail_hash === entries[entries.length-1].eventHash`.
    - End with `// RED until Wave 2 (Plan 13-03) creates grid/src/replay/tarball.ts`.

    Step 6 — Create `grid/test/audit/operator-exported-allowlist.test.ts`:
    - Import: `import { ALLOWLIST_MEMBERS, ALLOWLIST } from '../../src/audit/broadcast-allowlist.js';`
    - Test cases:
      1. `it('allowlist size === 27 (Phase 13 bump)', ...)` — `expect(ALLOWLIST_MEMBERS.length).toBe(27);`.
      2. `it('operator.exported at position 27', ...)` — `expect(ALLOWLIST_MEMBERS[26]).toBe('operator.exported');`.
      3. `it('ALLOWLIST.has("operator.exported") is true', ...)`.
    - End with `// RED until Wave 3 (Plan 13-04) bumps allowlist 26→27`.

    Step 7 — Create `grid/test/audit/operator-exported-payload-privacy.test.ts`:
    - Import (will fail): `import { appendOperatorExported } from '../../src/audit/append-operator-exported.js';`
    - Import: `import { AuditChain } from '../../src/audit/chain.js';`
    - Define `EXPORT_FORBIDDEN_KEYS = ['body','entries','text','chain_data','plaintext','manifest_body']`.
    - For each forbidden key emit:
      - `it('rejects {key} flat at producer boundary', ...)` — call `appendOperatorExported(chain, validBase, { [key]: 'leak' })`, expect throws TypeError.
      - `it('rejects {key} nested under operator_id object', ...)` — splice `operator_id: { [key]: 'leak' }`, expect throws.
    - One happy-path test: `it('accepts the exact 6-key payload', ...)` with valid `{tier:'H5', operator_id:'op1', start_tick:0, end_tick:50, tarball_hash:'a'.repeat(64), requested_at:1714435200}`.
    - End with `// RED until Wave 3 (Plan 13-04) creates grid/src/audit/append-operator-exported.ts`.

    Step 8 — Create `grid/test/audit/operator-exported-producer-boundary.test.ts`:
    - Import: `import { readFileSync } from 'node:fs'; import { globSync } from 'node:fs';` (or use a recursive walker analogous to existing producer-boundary tests).
    - Test case: `it('exactly one file in grid/src/** calls audit/chain append for operator.exported', ...)`:
      - Walk `grid/src/**/*.ts`.
      - For each file, regex search `\.append\s*\(\s*['"]operator\.exported['"]`.
      - Collect matching files. Assert `matches.length === 1` AND `matches[0].endsWith('audit/append-operator-exported.ts')`.
    - End with `// RED until Wave 3 (Plan 13-04) creates the sole-producer file`.

    All test files: ensure imports use `.js` extension on relative imports (project convention — vitest resolves through transpile), and ensure the file paths reference `grid/src/replay/...` and `grid/src/audit/append-operator-exported.ts` (the future file paths Wave 1-3 will create).
  </action>
  <verify>
    <automated>cd grid &amp;&amp; npx vitest run --reporter=dot test/replay test/audit/operator-exported 2>&amp;1 | grep -E "(FAIL|fail|RED|✗)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"better-sqlite3"' grid/package.json` returns at least 1.
    - `grep -c '"tar"' grid/package.json` returns at least 1.
    - `ls grid/test/replay/readonly-chain.test.ts grid/test/replay/replay-grid.test.ts grid/test/replay/state-builder.test.ts grid/test/replay/tarball-determinism.test.ts` lists all 4 files.
    - `ls grid/test/audit/operator-exported-allowlist.test.ts grid/test/audit/operator-exported-payload-privacy.test.ts grid/test/audit/operator-exported-producer-boundary.test.ts` lists all 3 files.
    - `cd grid && npx vitest run test/replay test/audit/operator-exported --reporter=json 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['numFailedTests'])"` returns ≥ 14 (sum of test cases across the 7 files; failure-or-error counts as failed).
    - Every RED test file contains at least one `// RED until Wave N` comment marking the wave that turns it GREEN.
    - `node -e "require('better-sqlite3')"` from `grid/` exits 0 (module installed and loadable).
    - `node -e "require('tar')"` from `grid/` exits 0.
  </acceptance_criteria>
  <done>
    All 7 Grid-side RED test files present and failing for the right reason (missing imports / wrong allowlist size / missing producer-boundary file). Both new dependencies installed and loadable. No production code modified yet.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scaffold Dashboard-side RED tests for replay UI + export consent</name>
  <read_first>
    - .planning/phases/13-operator-replay-export/13-UI-SPEC.md (verbatim copy + Tailwind classes)
    - .planning/phases/13-operator-replay-export/13-CONTEXT.md §Decisions (D-13-01..09)
    - dashboard/src/components/agency/irreversibility-dialog.tsx (clone source — verbatim copy + paste suppression patterns)
    - dashboard/src/lib/agency/store.ts (agencyStore interface — for tier-gate tests; if path differs, grep `agencyStore` in `dashboard/src` to locate)
    - dashboard/src/components/inspector/* (Inspector reuse pattern — `replayMode` prop)
  </read_first>
  <files>
    dashboard/src/app/grid/replay/replay-client.test.tsx,
    dashboard/src/app/grid/replay/export-consent-dialog.test.tsx
  </files>
  <behavior>
    - Test 1 (replay-client tier gate): `<ReplayClient operatorTier="H1" />` renders text 'Replay requires H3 or higher' AND does NOT render the firehose/inspector/map.
    - Test 2 (replay-client tier gate): `<ReplayClient operatorTier="H2" />` ditto.
    - Test 3 (replay-client redaction): at H3, an entry containing a Telos frame renders the placeholder 'Requires H4' (NOT the Telos plaintext).
    - Test 4 (replay-client redaction): at H3, a whisper entry renders 'Requires H5'.
    - Test 5 (replay-client tier reset on unmount): unmounting `<ReplayClient />` calls `agencyStore.setTier('H1')` (clones D-13-07 / Phase 6 D-07).
    - Test 6 (replay-client wall-clock-free): `Date.now`, `setInterval`, `setTimeout`, `requestAnimationFrame`, `Math.random` do NOT appear in the source file (D-13-05 + Phase 11 grep gate extension).
    - Test 7 (export-consent verbatim copy): mounted dialog renders all 7 verbatim strings (TITLE / WARNING / CONFIRM / CANCEL / INPUT_LABEL / HINT_MATCH / HINT_MISMATCH) exactly as defined in 13-UI-SPEC.md.
    - Test 8 (export-consent paste suppressed): pasting into the Grid-ID input is preventDefault'd; typed value remains empty post-paste.
    - Test 9 (export-consent confirm-disabled-until-match): confirm button is `disabled` when typed !== gridId; enabled at exact equality.
    - Test 10 (export-consent auto-downgrade on close): closing dialog (cancel button) calls `agencyStore.setTier('H1')`.
  </behavior>
  <action>
    Step 1 — Create `dashboard/src/app/grid/replay/replay-client.test.tsx`:
    - Imports: `import { describe, it, expect, vi, beforeEach } from 'vitest';`, `import { render, screen, cleanup } from '@testing-library/react';`, `import { readFileSync } from 'node:fs';`, `import path from 'node:path';`.
    - Import (will fail): `import { ReplayClient } from './replay-client';`.
    - Mock `agencyStore` at the module level: `vi.mock('@/lib/agency/store', () => ({ agencyStore: { setTier: vi.fn(), getTier: vi.fn(() => 'H3') } }));` (use the actual import path — adjust per Inspector test convention; if dashboard uses `@/` path alias, use `@/lib/agency/store`).
    - Test cases — IMPORTANT: each test calls `cleanup()` between renders.
      1. Tier gate H1 — renders `<ReplayClient operatorTier="H1" entries={[]} startTick={0} endTick={50} gridId="test-grid" />`; `expect(screen.getByText(/Replay requires H3/i)).toBeTruthy();` AND `expect(screen.queryByTestId('replay-firehose')).toBeNull();`.
      2. Tier gate H2 — same pattern.
      3. H3 + Telos frame → 'Requires H4' placeholder visible: render with one fixture entry shaped as a Telos-revealing frame; assert `screen.getByText(/— Requires H4/)` present and the literal Telos goal text NOT present.
      4. H3 + whisper frame → 'Requires H5'.
      5. Tier reset on unmount — `const { unmount } = render(<ReplayClient operatorTier="H3" .../>); unmount(); expect(agencyStore.setTier).toHaveBeenCalledWith('H1');`.
      6. Wall-clock grep — `const src = readFileSync(path.resolve(__dirname, 'replay-client.tsx'), 'utf8'); for (const banned of ['Date.now','setInterval','setTimeout','requestAnimationFrame','Math.random']) { expect(src).not.toContain(banned); }` — this test will throw "ENOENT" until Wave 4 creates the file (acceptable RED state).
    - End with `// RED until Wave 4 (Plan 13-05) creates dashboard/src/app/grid/replay/replay-client.tsx`.

    Step 2 — Create `dashboard/src/app/grid/replay/export-consent-dialog.test.tsx`:
    - Imports: `import { describe, it, expect, vi } from 'vitest';`, `import { render, screen, fireEvent, cleanup } from '@testing-library/react';`.
    - Import (will fail): `import { ExportConsentDialog } from './export-consent-dialog';`.
    - Define verbatim copy CONSTANTS at top of file (from 13-UI-SPEC.md and D-13-08):
      ```
      const TITLE_COPY = 'Export audit chain slice';
      const WARNING_COPY = 'This export is permanent and cannot be undone. The tarball will contain the complete audit chain for the selected tick range. Anyone with the file can verify the chain.';
      const CONFIRM_LABEL = 'Export forever';
      const CANCEL_LABEL = 'Keep private';
      const INPUT_LABEL = 'Type the Grid-ID exactly to confirm:';
      const HINT_MATCH = 'Match confirmed.';
      const HINT_MISMATCH = 'Grid-ID does not match. Type exactly as shown.';
      ```
    - Mock `agencyStore`: `vi.mock('@/lib/agency/store', () => ({ agencyStore: { setTier: vi.fn() } }));`.
    - Test cases:
      1. Verbatim copy — render dialog with `open={true} gridId="grid-alpha" onConfirm={vi.fn()} onCancel={vi.fn()}`; for each of TITLE_COPY, WARNING_COPY, CONFIRM_LABEL, CANCEL_LABEL, INPUT_LABEL, assert `screen.getByText(<literal>)` present.
      2. HINT_MATCH and HINT_MISMATCH appear when typed matches / mismatches Grid-ID.
      3. Paste suppressed — get the input by `getByTestId('export-grid-id-input')`; fire paste with clipboard data 'grid-alpha'; assert input value remains ''.
      4. Confirm disabled until exact match — typed='grid-alph' → button has `disabled` attribute; typed='grid-alpha' → not disabled.
      5. Cancel → onCancel called → agencyStore.setTier called with 'H1' on close.
      6. Confirm payload shape — when confirm clicked, `onConfirm` is invoked with no args (parent constructs the operator.exported event server-side; dialog is presentation-only).
    - End with `// RED until Wave 4 (Plan 13-05) creates dashboard/src/app/grid/replay/export-consent-dialog.tsx`.

    Both dashboard test files MUST be picked up by `dashboard/vitest.config.ts` `include` glob — verify the path matches `src/**/*.test.tsx` pattern. If `dashboard/vitest.config.ts` does not include `src/app/**`, no change needed (current convention from STATE.md Plan 10a-05 confirms `src/**` is included).
  </action>
  <verify>
    <automated>cd dashboard &amp;&amp; npx vitest run --reporter=dot src/app/grid/replay 2>&amp;1 | grep -E "(FAIL|fail|✗|ENOENT|Cannot find module)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `ls dashboard/src/app/grid/replay/replay-client.test.tsx dashboard/src/app/grid/replay/export-consent-dialog.test.tsx` lists both files.
    - Both files contain the literal string `'Export audit chain slice'`.
    - Both files contain the literal string `'Export forever'`.
    - Both files contain the literal string `'Keep private'`.
    - `cd dashboard && npx vitest run src/app/grid/replay --reporter=json 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); print(d['numFailedTests'])"` returns ≥ 10 (test count across both files; failures-or-errors all count).
    - Each file ends with a `// RED until Wave 4` comment.
  </acceptance_criteria>
  <done>
    Both dashboard RED test files present and failing because their target source files (`replay-client.tsx`, `export-consent-dialog.tsx`) do not yet exist. Verbatim copy strings are LITERALS in test source — paraphrasing them in Wave 4 implementation will fail tests. No production component code created yet.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev environment → npm registry | Wave 0 installs untrusted packages (`better-sqlite3`, `tar`) — supply chain risk |
| test files → eventual implementation | RED tests encode acceptance criteria — paraphrase or weaker assertions in implementation phase would silently degrade enforcement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01-01 | Tampering | grid/test/audit/operator-exported-allowlist.test.ts | mitigate | Test asserts `ALLOWLIST_MEMBERS[26] === 'operator.exported'` (exact position 27 — D-13-09); any reorder of allowlist breaks test |
| T-13-01-02 | Information Disclosure | grid/test/audit/operator-exported-payload-privacy.test.ts | mitigate | 6 forbidden keys × 2 cases (flat + nested) × producer-boundary throw — locks the privacy matrix at RED-test level so Wave 3 cannot ship a weaker check |
| T-13-01-03 | Tampering | RED test wave numbering | mitigate | Each RED test ends with explicit `// RED until Wave N (Plan 13-NN)` comment so reviewer in Wave N can confirm GREEN flip happened in the right plan |
| T-10-07 (mapped) | Tampering / EoP | grid/test/replay/readonly-chain.test.ts | mitigate | Asserts `ReadOnlyAuditChain.append()` throws — pins T-10-07 (replay shares state with live Grid) at the test level |
| T-10-08 (mapped) | Spoofing | grid/test/replay/replay-grid.test.ts | mitigate | Asserts replay chain instance distinct from live launcher — pins T-10-08 (fake-timestamp emission) at construction-time isolation |
| T-10-09 (mapped) | Information Disclosure | dashboard/src/app/grid/replay/replay-client.test.tsx | mitigate | Tier-gate H1/H2 + redaction H4/H5 tests — pins T-10-09 (plaintext leak at H1) at the UI test level |
| T-10-10 (mapped) | Information Disclosure | dashboard/src/app/grid/replay/export-consent-dialog.test.tsx + grid/test/audit/operator-exported-payload-privacy.test.ts | mitigate | Verbatim copy lock + paste suppression + 6-key forbidden-keys matrix — pins T-10-10 (plaintext in tarball / no-consent export) |
| T-13-01-04 | Information Disclosure | npm install supply chain | accept | better-sqlite3 (12.9.0) and tar (7.5.13) are widely-deployed, audited packages. Lockfile pin via `package-lock.json` after `npm install` provides reproducible installs. No further mitigation in scope for Wave 0 |
</threat_model>

<verification>
- All 9 new test files exist in their declared paths.
- `cd grid && npx vitest run --reporter=dot test/replay test/audit/operator-exported` shows failures (RED state confirmed).
- `cd dashboard && npx vitest run --reporter=dot src/app/grid/replay` shows failures (RED state confirmed).
- `node -e "require('better-sqlite3'); require('tar')"` from `grid/` exits 0.
- `git diff --stat grid/package.json` shows `+2,+2` lines (deps + devDeps).
- No production code in `grid/src/replay/`, `grid/src/audit/append-operator-exported.ts`, or `dashboard/src/app/grid/replay/{replay-client,export-consent-dialog}.tsx` exists yet — these are Wave 1-4 deliverables.
- Existing test suite still passes: `cd grid && npx vitest run --reporter=dot` shows pre-Phase-13 baseline still green for tests not touching new RED files.
</verification>

<success_criteria>
- 9 RED test files committed.
- 2 new runtime dependencies + 2 type-only devDeps in `grid/package.json` and `package-lock.json`.
- Wave 0 verifier: `cd grid && npx vitest run test/replay test/audit/operator-exported` produces ≥ 14 failing tests (RED scaffolding intact); `cd dashboard && npx vitest run src/app/grid/replay` produces ≥ 10 failing tests.
- Pre-existing test suite remains green outside the new RED files.
- Every RED test file annotated with `// RED until Wave N (Plan 13-NN)` so subsequent wave executors trace which plan must turn it GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/13-operator-replay-export/13-01-SUMMARY.md` capturing:
- npm install output (top 5 lines, list of installed packages)
- list of RED test files created with line counts
- failing test count from vitest (Grid + Dashboard)
- git commit SHA
- any deviations from the plan (Rule 2 / Rule 3) for Wave 1-4 to know about
</output>
