---
phase: 62.6-ledger-b-subsystem-migration
plan: 05
type: execute
wave: 3
depends_on: ["62.6-01-PLAN", "62.6-02-PLAN", "62.6-03-PLAN", "62.6-04-PLAN"]
files_modified:
  - grid/test/integration/trade-review-flow.test.ts
  - grid/test/integration/trade-review-abort.test.ts
  - grid/test/civic/house-3-e2e.test.ts
  - grid/test/civic/house-4-e2e.test.ts
  - grid/test/marketplace-atomic-rollback.test.ts
  - grid/test/civic/upkeep-atomic-rollback.test.ts
autonomous: true
requirements: [D-8, D-9, D-11, D-12]
must_haves:
  truths:
    - "No transferWei call and no nous_registry.balance_wei money read/write remains in ANY of the four migrated subsystems (marketplace-store.ts, market.ts, upkeep-scanner.ts, cowork.ts, co-build.ts, nous-runner.ts) — proven by grep."
    - "Each subsystem has a failing-charge atomic-rollback proof: a rails throw leaves no partial state (no phantom debit, no phantom credit, no escrow row)."
    - "The broadcast allowlist is byte-unchanged (D-8): git diff on grid/src/audit/broadcast-allowlist.ts is empty for this phase."
    - "The sole-producer discipline, wallclock-forbidden, and wiki gates pass; the audit-chain zero-diff (R-31-01) holds; the payloadPrivacy / sole-producer triads for treasury.*/zoning.cowork_session/trade.* are intact."
    - "Zero custody (D-9): no transferFrom and no platform-key usage was introduced in grid/src."
    - "tsc is clean and the full grid unit suite is green (modulo the known pre-existing env flakes documented in 62.5)."
  artifacts:
    - path: "grid/test/marketplace-atomic-rollback.test.ts"
      provides: "failing buyer-debit / seller-credit → full rollback proof"
      contains: "rollback"
    - path: "grid/test/civic/upkeep-atomic-rollback.test.ts"
      provides: "failing upkeep charge → no partial state proof"
      contains: "insufficient"
  key_links:
    - from: "grep invariant gate"
      to: "the four subsystems"
      via: "no transferWei / nous_registry.balance_wei money"
      pattern: "transferWei"
---

<objective>
Prove the phase invariants after the four subsystem migrations land: no Ledger-B money read/write remains, each subsystem rolls back atomically on a failing charge, the allowlist is unchanged, the sole-producer / zero-diff / custody / wallclock / wiki gates pass, and the full suite is green.

Purpose: this is the invariant gate for 62.6. It does NOT add the CI gate forbidding transferWei (that is 62.5-05) and does NOT do the data fold (62.5-04) — it only proves the migration is complete and non-regressive.
Output: retargeted remaining trade + house e2e tests, two new atomic-rollback tests, and a green invariant sweep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62.6-ledger-b-subsystem-migration/62.6-CONTEXT.md
@.planning/phases/62.6-ledger-b-subsystem-migration/62.6-RESEARCH.md

<interfaces>
Test rails: grid/test/helpers/accounts-pool.ts — `makeAccountsPool({ otherQuery })`. Throw inside `otherQuery` (or seed an underfunded account so the rail throws `insufficient_balance`) to exercise a rollback path; the fake conn snapshots on beginTransaction and restores on rollback, mirroring the real rails' atomicity.

Existing invariant gates (run from repo root):
- `node scripts/check-sole-producer-discipline.mjs`
- `node scripts/check-wallclock-forbidden.mjs`
- `node scripts/check-wiki.mjs`
- `grid/test/review/zero-diff.test.ts` (R-31-01 audit-chain zero-diff)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Grep invariant sweep + retarget the remaining trade / house e2e tests</name>
  <read_first>
    - grid/test/integration/trade-review-flow.test.ts, grid/test/integration/trade-review-abort.test.ts
    - grid/test/civic/house-3-e2e.test.ts, grid/test/civic/house-4-e2e.test.ts
    - grid/test/helpers/accounts-pool.ts
  </read_first>
  <action>
Run the grep invariant sweep and confirm zero Ledger-B money in the four subsystems:
```
grep -c "transferWei" grid/src/marketplace/marketplace-store.ts grid/src/api/routes/market.ts grid/src/civic/upkeep-scanner.ts grid/src/civic/cowork.ts grid/src/civic/co-build.ts grid/src/integration/nous-runner.ts
grep -c "nous_registry" grid/src/marketplace/marketplace-store.ts
grep -rn "balance_wei" grid/src/civic/upkeep-scanner.ts grid/src/civic/cowork.ts grid/src/civic/co-build.ts
```
All must be 0. Retarget the remaining money-touching tests to the accounts-pool ledger: `trade-review-flow.test.ts` + `trade-review-abort.test.ts` (pass the new NousRunner deps; assert the review gate + settle on nous_accounts), and `house-3-e2e.test.ts` + `house-4-e2e.test.ts` (co-work/co-build settlement — retarget from Ledger B to the accounts-pool, per the 62.5-02 carry-forward note that these kept co-work on Ledger B). Where a construction merely fails to compile because the config changed, wire the optional deps; do not alter unrelated assertions.
  </action>
  <acceptance_criteria>
    - `grep -c "transferWei"` across the six subsystem source files sums to 0.
    - `grep -c "nous_registry" grid/src/marketplace/marketplace-store.ts` = 0.
    - `npx vitest run grid/test/integration/trade-review-flow.test.ts grid/test/integration/trade-review-abort.test.ts grid/test/civic/house-3-e2e.test.ts grid/test/civic/house-4-e2e.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx vitest run test/integration/trade-review-flow.test.ts test/integration/trade-review-abort.test.ts test/civic/house-3-e2e.test.ts test/civic/house-4-e2e.test.ts</automated>
  </verify>
  <done>The grep sweep confirms no Ledger-B money in the four subsystems; the remaining trade + house e2e tests are green on the unified ledger.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add per-subsystem atomic-rollback proofs (D-12)</name>
  <read_first>
    - grid/test/helpers/accounts-pool.ts (otherQuery throw hook + begin/rollback snapshot)
    - grid/src/marketplace/marketplace-store.ts (acceptBid/settle transactions)
    - grid/src/civic/upkeep-scanner.ts (chargeToTreasury branch)
  </read_first>
  <behavior>
    - marketplace acceptBid: an underfunded buyer (seeded below the bid) → `insufficient_wei` thrown AND no escrow row inserted AND buyer balance unchanged (rollback).
    - marketplace settle: a forced failure after the seller-credit (otherQuery throw on the status update) → seller balance and treasury unchanged (rollback).
    - upkeep: an underfunded owner → chargeToTreasury throws insufficient_balance → owner balance unchanged, treasury unchanged, parcel routed to the decay ladder (no phantom collection).
  </behavior>
  <action>
Create `grid/test/marketplace-atomic-rollback.test.ts` and `grid/test/civic/upkeep-atomic-rollback.test.ts` using `makeAccountsPool()`. For marketplace, seed an underfunded buyer and assert `acceptBid` throws `insufficient_wei` and `balanceOf(buyer)` is unchanged (the fake pool's rollback restored the snapshot); for settle, use `otherQuery` to throw on the escrow-status UPDATE and assert `balanceOf(seller)` and `treasuryOf()` are unchanged. For upkeep, seed the owner below `due`, run `onUpkeepTick`, and assert `treasuryOf()` and `balanceOf(owner)` are unchanged while the decay ladder advanced (`advanceCondition` spy called). These are the D-12 "failing charge → no partial state" proofs the phase requires.
  </action>
  <acceptance_criteria>
    - Both new test files exist and `npx vitest run grid/test/marketplace-atomic-rollback.test.ts grid/test/civic/upkeep-atomic-rollback.test.ts` passes.
    - Each asserts a rails throw AND unchanged balances (no partial state).
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx vitest run test/marketplace-atomic-rollback.test.ts test/civic/upkeep-atomic-rollback.test.ts</automated>
  </verify>
  <done>Two atomic-rollback proofs show a failing charge leaves marketplace and upkeep with no phantom debit/credit/escrow state (D-12).</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite + gate sweep (allowlist unchanged, sole-producer, wallclock, wiki, zero-diff, custody, tsc)</name>
  <read_first>
    - grid/src/audit/broadcast-allowlist.ts (must be byte-unchanged this phase)
    - scripts/check-sole-producer-discipline.mjs, scripts/check-wallclock-forbidden.mjs, scripts/check-wiki.mjs
    - grid/test/review/zero-diff.test.ts
  </read_first>
  <action>
Run the invariant gates and the full suite:
1. `git diff --stat grid/src/audit/broadcast-allowlist.ts` → MUST be empty (D-8 no allowlist change).
2. `grep -rn "transferFrom" grid/src` → MUST be empty (D-9 zero custody; no platform keys introduced).
3. `node scripts/check-sole-producer-discipline.mjs` → pass (treasury.*/zoning.cowork_session/trade.* triads intact).
4. `node scripts/check-wallclock-forbidden.mjs` → pass (D-11 deterministic tick, no new clock).
5. `node scripts/check-wiki.mjs` → pass (economy.md already documents unified account-based money; this migration matches design, so no page is stale — if the gate flags staleness, add the minimal economy.md note that marketplace/upkeep/co-work/agent-trade money now moves in nous_accounts/civic_treasury and re-run).
6. `cd grid && npx tsc --noEmit` → clean.
7. `cd grid && npx vitest run` → full unit suite green, modulo the known pre-existing env flakes documented in 62.5 (operator-scope-typing foreign-path EACCES, rig process tests, SNS watchdog under parallelism). Confirm the trade zero-diff test `test/review/zero-diff.test.ts` passes (R-31-01).
Record the full-suite green count and any flake list in the SUMMARY.
  </action>
  <acceptance_criteria>
    - `git diff --stat grid/src/audit/broadcast-allowlist.ts` empty; `grep -rn "transferFrom" grid/src` empty.
    - `node scripts/check-sole-producer-discipline.mjs`, `node scripts/check-wallclock-forbidden.mjs`, `node scripts/check-wiki.mjs` all exit 0.
    - `cd grid && npx tsc --noEmit` clean; `npx vitest run test/review/zero-diff.test.ts` passes.
    - Full `npx vitest run` green except the documented pre-existing env flakes.
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx tsc --noEmit && npx vitest run test/review/zero-diff.test.ts</automated>
  </verify>
  <done>Allowlist unchanged, custody clean, sole-producer/wallclock/wiki gates green, zero-diff holds, tsc clean, full suite green modulo known flakes — the phase invariants are proven.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the four migrated subsystems → audit chain / allowlist | the migration must not add events, change reason strings, or move emit sites |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.6-05-01 | Tampering | audit surface | mitigate | git-diff proves broadcast-allowlist.ts unchanged; sole-producer + zero-diff gates prove no event added/removed/reordered |
| T-62.6-05-02 | Repudiation | atomicity claim | mitigate | explicit failing-charge rollback tests per subsystem prove no phantom state |
| T-62.6-05-03 | Elevation of Privilege | zero custody | mitigate | grep proves no transferFrom / platform-key usage introduced |
| T-62.6-05-04 | Denial of Service | tick determinism | accept | wallclock-forbidden gate proves no new clock source was added by the upkeep migration |
</threat_model>

<verification>
- grep sweep: 0 transferWei / nous_registry money across the four subsystems.
- allowlist git-diff empty; transferFrom grep empty.
- sole-producer + wallclock + wiki gates green; zero-diff test green; tsc clean; full suite green modulo known flakes.
</verification>

<success_criteria>
The four subsystems hold no Ledger-B money; each rolls back atomically on a failing charge; the allowlist, sole-producer triads, wallclock, wiki, and audit-chain zero-diff are all intact; the full suite is green. 62.6 is complete and 62.5-04/05 are unblocked (data fold + record retirement + CI gate remain Phase 62.5 work, NOT done here).
</success_criteria>

<output>
After completion, create `.planning/phases/62.6-ledger-b-subsystem-migration/62.6-05-SUMMARY.md` — record the green count, the flake list, and an explicit statement that 62.5-04 (fold + transferWei removal) and 62.5-05 (record retirement + PR#8/#11 deletion + CI gate) are now unblocked and are the next Phase 62.5 waves.
</output>
