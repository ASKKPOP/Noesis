---
phase: 62.6-ledger-b-subsystem-migration
verified: 2026-07-10T00:00:00Z
status: passed
score: 8/8 must-haves verified
overrides_applied: 0
re_verification:
  previous_status: none
  previous_score: n/a
---

# Phase 62.6: Ledger-B Subsystem Migration Verification Report

**Phase Goal:** Migrate the four remaining Ledger-B money subsystems (marketplace+skim, upkeep, co-work/co-build, agent-trades) off `nous_registry.balance_wei` / `NousRegistry.transferWei` onto Ledger A (`nous_accounts` civic-DID + `civic_treasury`), preserving atomicity, audit-chain zero-diff (R-31-01), and behavior for funded citizens — WITHOUT doing 62.5-04 (data fold + transferWei removal) or 62.5-05 (record retirement + CI gate).
**Verified:** 2026-07-10
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | No money on Ledger B in the four subsystems | ✓ VERIFIED | `grep -c transferWei` = 0 across all six source files (marketplace-store, market.ts, upkeep-scanner, cowork, co-build, nous-runner); `nous_registry` = 0 in marketplace-store; `balance_wei` = 0 in upkeep/cowork/co-build. transferWei def still at registry.ts:136 (out of scope, correct). |
| 2 | Marketplace atomicity (O4/D-12) | ✓ VERIFIED | marketplace-store.ts:234 `debitAccountOnConn(conn,…)` inside `acceptBid` beginTransaction (212); :360 `creditAccountOnConn(conn,…)` inside `settle` beginTransaction (338); IRS fee upsert to `civic_treasury` at :366-374 preserved; no nested `NousAccountStore` in the escrow tx; `insufficient_balance`→`insufficient_wei` at :242. |
| 3 | Upkeep charges via chargeToTreasury; no did:noesis:system:treasury write; per-parcel isolation | ✓ VERIFIED | upkeep-scanner.ts:118 `accountStore.chargeToTreasury` on owner civic-DID; `treasuryDid` used only as reclaimed-parcel owner reassignment (:161, not a money write); `continue` on both funded (:138) and non-affordability error (:143) preserves isolation; insufficient_balance falls to decay ladder (:145-151). |
| 4 | D-13 (operator-locked) both parties resolved; either null ⇒ trade.rejected{not_found}; no new reason; review-fail unchanged | ✓ VERIFIED | nous-runner.ts:514-515 resolves both via `getByExistenceDid`; :516-521 either null → `trade.rejected{reason:'not_found'}`; reason map :537-540 only insufficient/self_transfer/invalid_amount/not_found; review-fail branch :476-477 (NO transfer/settled/rejected). Symmetric proposer-null tests: trade-settlement.test.ts:188 (reviewer-present) + :219 (reviewer-bypassed). |
| 5 | co-work/co-build via NousAccountStore.transfer; IOU fallback preserved (D-NH-06) | ✓ VERIFIED | cowork.ts:153 async `completeTask`, :170 `settleWei` with :172-175 insufficient→`recordIou` fallback, :182 unfunded→IOU, :165 cowork_must_pay guard; co-build.ts:158 async `completeSubTask`, :181-184 IOU fallback, :203 completeNode alias; civic-parcels.ts:840 `await completeTask` with :844 `settleWei→accountStore.transfer`. IOU test at cowork.test.ts:88. |
| 6 | Invariants: allowlist zero-diff, R-31-01 zero-diff strict, custody ban, deterministic tick | ✓ VERIFIED | `git diff 6d1ce52d^ HEAD -- broadcast-allowlist.ts` empty (D-8); zero-diff.test.ts 2/2 passed strict; `grep transferFrom grid/src` NONE (D-9); no new clock source (grep hits are "no clock.onTick"/"NEVER Date.now()" comments, D-11); sole-producer/wallclock/wiki gates all exit clean. |
| 7 | Suite health: tsc clean; full suite green except documented pre-existing env flakes | ✓ VERIFIED | `tsc --noEmit` exit 0, 0 lines. Full suite: 4103 passed / 8 failed (3 files) / 31 skipped. Failures are pre-existing env flakes only (operator-scope-typing EACCES on foreign path `/Users/desirey`, rig end-to-end-smoke + tarball-integrity DB access-denied/.env); none touch migrated subsystems. All 13 phase-relevant test files: 112 passed. |
| 8 | Scope discipline: 62.5-04 and 62.5-05 NOT done | ✓ VERIFIED | `transferWei` def still at registry.ts:136 (not removed); `did:noesis:system:treasury` still referenced in main.ts/registry.ts/api/routes/registry.ts/launcher.ts (not retired); no new CI gate script forbidding transferWei/nous_registry.balance_wei added. |

**Score:** 8/8 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `grid/src/marketplace/marketplace-store.ts` | buyer-debit/seller-credit on nous_accounts (store conn) | ✓ VERIFIED | debitAccountOnConn/creditAccountOnConn on `conn` inside escrow/settle tx |
| `grid/src/api/routes/market.ts` | skim via chargeToTreasury (try/catch) | ✓ VERIFIED | :433 chargeToTreasury, emit only on success (:444), catch swallows (:450) |
| `grid/src/civic/upkeep-scanner.ts` | atomic owner→civic_treasury via chargeToTreasury | ✓ VERIFIED | :118 chargeToTreasury; decay-ladder fallback intact |
| `grid/src/civic/cowork.ts` + `co-build.ts` | async settleWei seam + IOU fallback | ✓ VERIFIED | async completion fns; settleWei; insufficient→recordIou |
| `grid/src/integration/nous-runner.ts` | existence→civic resolve + transfer + D-13 gate | ✓ VERIFIED | getByExistenceDid both parties; accountStore.transfer; not_found gate |
| `grid/src/main.ts` | deps threaded into upkeep + NousRunner | ✓ VERIFIED | :296 accountStore into upkeep; :405-406 accountStore/civicDidStore/gridName into NousRunner |
| `grid/test/marketplace-atomic-rollback.test.ts` | failing-debit rollback proof | ✓ VERIFIED | exists; 2 tests pass |
| `grid/test/civic/upkeep-atomic-rollback.test.ts` | failing-charge no-partial-state proof | ✓ VERIFIED | exists; 1 test passes |

### Key Link Verification

| From | To | Via | Status |
|------|----|----|--------|
| marketplace-store acceptBid | nous_accounts (buyer) | debitAccountOnConn on conn inside escrow tx | ✓ WIRED |
| market.ts skim | civic_treasury | chargeToTreasury on sellerCivicDid | ✓ WIRED |
| upkeep-scanner onUpkeepTick | civic_treasury | chargeToTreasury on owner civic-DID | ✓ WIRED |
| civic-parcels board/complete | cowork completeTask | settleWei → NousAccountStore.transfer | ✓ WIRED |
| nous-runner trade_request | nous_accounts (resolved civic-DIDs) | getByExistenceDid → accountStore.transfer | ✓ WIRED |
| main.ts NousRunner | NousAccountStore + CivicDidStore | config accountStore/civicDidStore/gridName | ✓ WIRED |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Migrated subsystem + zero-diff + rollback tests | `vitest run` (13 files) | 112 passed / 1 skipped | ✓ PASS |
| TypeScript typecheck | `tsc --noEmit` | exit 0 | ✓ PASS |
| Sole-producer discipline | `check-sole-producer-discipline.mjs` | OK — 140 files full triad | ✓ PASS |
| Wallclock forbidden (D-11) | `check-wallclock-forbidden.mjs` | clean | ✓ PASS |
| Wiki completion gate | `check-wiki.mjs` | clean (40 pages) | ✓ PASS |
| Allowlist byte-unchanged (D-8) | `git diff … broadcast-allowlist.ts` | empty | ✓ PASS |
| Custody ban (D-9) | `grep transferFrom grid/src` | NONE | ✓ PASS |

### Anti-Patterns Found

None. The `transferWei` definition remaining at registry.ts:136 and `did:noesis:system:treasury` references are intentional and explicitly out of scope (62.5-04/62.5-05). The 1 skipped cowork test (`describe.skip` at cowork.test.ts:131) is a pre-existing Phase 60 HOUSE-3 `zoning.cowork_session` emit deferral, not a 62.6 money-path gap; the D-NH-06 IOU-fallback path IS covered (cowork.test.ts:88).

### Human Verification Required

None. This is a backend money-ledger migration fully covered by automated unit tests, atomic-rollback proofs, the R-31-01 zero-diff test, invariant gate scripts, tsc, and grep. No visual/UX/real-time/external-service surface.

### Gaps Summary

No gaps. All four subsystems (marketplace+skim, upkeep, co-work/co-build, agent-trades) move money exclusively on Ledger A (`nous_accounts` / `civic_treasury`); atomicity is preserved on the store's own connection (marketplace) or in single-transaction store primitives (chargeToTreasury/transfer); the D-13 citizens-only gate and D-NH-06 always-settles invariant are enforced and tested; the allowlist, sole-producer triads, wallclock determinism, wiki, custody ban, and audit-chain zero-diff are all intact; and the scope boundary holds — 62.5-04 (data fold + transferWei removal) and 62.5-05 (record retirement + CI gate) are correctly NOT done. The 8 full-suite test failures are pre-existing machine/DB environment flakes unrelated to this phase's code.

---

_Verified: 2026-07-10_
_Verifier: Claude (gsd-verifier)_
