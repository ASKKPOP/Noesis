---
phase: 62.6-ledger-b-subsystem-migration
plan: 05
subsystem: regression-invariant-gate
tags: [regression, invariant-gate, zero-diff, R-31-01, D-12, atomic-rollback, ledger-a, phase-complete]

# Dependency graph
requires:
  - phase: 62.6-ledger-b-subsystem-migration (plans 01-04)
    provides: "marketplace/skim, upkeep, co-work/co-build, agent-trades all migrated off transferWei/nous_registry.balance_wei onto nous_accounts/civic_treasury"
  - phase: 62.5-indb-ledger-unification
    provides: "nous_accounts rails, accounts-pool test fake, ensureAccount-on-naturalization invariant"
provides:
  - "Phase 62.6 invariant proof: no Ledger-B money read/write remains in the four migrated subsystems (grep sweep = 0)"
  - "The four deferred trade tests (trade-review-flow, trade-review-abort, zero-diff, e2e-tick-cycle) retargeted to the unified nous_accounts ledger with the R-31-01 zero-diff assertion kept strict"
  - "Two dedicated atomic-rollback proofs (marketplace, upkeep) showing a failing charge leaves no partial state (D-12)"
  - "Green full-suite modulo 4 documented pre-existing env flakes; allowlist byte-unchanged; sole-producer/wallclock/wiki/custody gates green"
affects: [62.5-04, 62.5-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Retarget an existence-DID-keyed trade test to the civic ledger by wiring accountStore/civicDidStore/gridName + a stub CivicDidStore existence→civic map, seeding nous_accounts, and asserting balanceOf() conservation"
    - "Prove atomic rollback with makeAccountsPool: throw inside otherQuery (or seed an underfunded account) → the fake conn's begin/rollback snapshot restores both ledger maps, mirroring the real rails' single-transaction atomicity"

key-files:
  created:
    - grid/test/marketplace-atomic-rollback.test.ts
    - grid/test/civic/upkeep-atomic-rollback.test.ts
  modified:
    - grid/test/integration/trade-review-flow.test.ts
    - grid/test/integration/trade-review-abort.test.ts
    - grid/test/review/zero-diff.test.ts
    - grid/src/integration/nous-runner.ts

key-decisions:
  - "e2e-tick-cycle needed no change — it already passed (its trade-touching path does not assert Ledger-B balances); only trade-review-flow, trade-review-abort, and zero-diff were still on the pre-migration assumptions."
  - "zero-diff kept STRICT: the accountStore/civicDidStore/gridName deps are wired IDENTICALLY into both the reviewer-on and reviewer-off runs, so the reviewer remains the sole difference. The async transfer does not perturb Date.now determinism (fake timers), so the field-by-field (incl. createdAt) non-reviewer-event equality still holds."
  - "The two src comments in nous-runner.ts that still contained the literal transferWei (review-fail branch) were reworded to non-money phrasing (no behavior change) so the four-subsystem grep sweep sums to exactly 0."
  - "Task 3 produced no wiki change: economy.md already documents unified account-based money (nous_accounts/civic_treasury); check-wiki is clean, so no page was stale."

requirements-completed: [D-8, D-9, D-11, D-12]

# Metrics
duration: ~35min
completed: 2026-07-10
---

# Phase 62.6 Plan 05: Regression + Invariant Gate Summary

**The four subsystem migrations are proven complete and non-regressive: no Ledger-B money read/write remains in marketplace/skim, upkeep, co-work/co-build, or agent-trades; each rolls back atomically on a failing charge; the allowlist is byte-unchanged; and the sole-producer, wallclock, wiki, custody, and R-31-01 audit-chain zero-diff gates are all green. Phase 62.6 is complete — 62.5-04 (data fold + transferWei removal) and 62.5-05 (record retirement + CI gate) are unblocked.**

## Performance
- **Duration:** ~35 min
- **Completed:** 2026-07-10
- **Tasks:** 3 (2 committed; Task 3 is a verification-only gate sweep)
- **Files created:** 2 · **Files modified:** 4

## Accomplishments
- **Retargeted the four deferred trade tests.** `trade-review-flow` (pass path), `trade-review-abort` (5 reviewer-fail codes), and `zero-diff` (R-31-01) were still constructing `NousRunner` on the pre-62.6-04 assumptions (no `accountStore`/`civicDidStore`/`gridName`, reviewer balance + settle on Ledger B). Each now wires the three optional deps + a stub `CivicDidStore` existence→civic map, seeds `nous_accounts`, and asserts on the unified ledger. `e2e-tick-cycle` needed no change (already green — its trade path does not assert Ledger-B balances).
- **Kept zero-diff strict (R-31-01).** The store deps are wired identically into BOTH runs, so the reviewer stays the sole difference; the async `NousAccountStore.transfer` does not perturb `Date.now()` (fake timers), so every non-reviewer event stays byte-identical (eventType/actor/target/payload/createdAt). The zero-diff assertion was NOT weakened — it genuinely re-proves audit determinism with the new deps wired.
- **Added two dedicated atomic-rollback proofs (D-12).** `marketplace-atomic-rollback.test.ts`: an underfunded `acceptBid` throws `insufficient_wei` with NO phantom debit and NO escrow row inserted; a forced throw on the settle escrow-status UPDATE rolls back the seller credit AND the treasury fee. `upkeep-atomic-rollback.test.ts`: an owner who cannot cover leaves `nous_accounts` and `civic_treasury` unchanged and routes the parcel to the decay ladder (no phantom collection, no `treasury.upkeep_collected`).
- **Grep invariant sweep = 0.** No `transferWei` call and no `nous_registry.balance_wei` money read/write remains in any of the six subsystem source files (marketplace-store, market.ts, upkeep-scanner, cowork, co-build, nous-runner). The two remaining `transferWei` comment mentions in nous-runner were reworded off the literal token (no behavior change).

## Invariant-Gate Results

| Gate | Command | Result |
|------|---------|--------|
| Grep sweep — transferWei (six files) | `grep -c transferWei …` | **0** (sum) ✅ |
| Grep sweep — nous_registry (marketplace) | `grep -c nous_registry marketplace-store.ts` | **0** ✅ |
| Grep sweep — balance_wei (upkeep/cowork/co-build) | `grep -rn balance_wei …` | **none** ✅ |
| Allowlist byte-unchanged (D-8) | `git diff … broadcast-allowlist.ts` (vs phase base `45e34f32`) | **empty** ✅ |
| Zero custody (D-9) | `grep -rn transferFrom grid/src` | **none** ✅ |
| Sole-producer discipline | `node scripts/check-sole-producer-discipline.mjs` | **OK — 140 files, full triad** ✅ |
| Wallclock forbidden (D-11) | `node scripts/check-wallclock-forbidden.mjs` | **clean** ✅ |
| Wiki completion gate | `node scripts/check-wiki.mjs` | **clean (40 pages/topics)** ✅ |
| Audit-chain zero-diff (R-31-01) | `vitest run test/review/zero-diff.test.ts` | **2 passed (strict)** ✅ |
| TypeScript | `npx tsc --noEmit` | **clean (exit 0)** ✅ |

## Full-Suite Result
`cd grid && npx vitest run` → **Test Files 471 passed | 4 failed | 5 skipped (480)**, **Tests 4101 passed | 10 failed | 31 skipped (4142)**.

Phase-relevant subsystem files (13): **91 passed | 1 skipped** — all green.

### The 4 failing files are documented PRE-EXISTING env flakes (NOT 62.6 regressions)
| File | Fails | Root cause (verified this session) |
|------|-------|-------------------------------------|
| `test/ci/operator-scope-typing.test.ts` | 5 | Hardcoded foreign dev path `/Users/desirey/Programming/src/Noesis/.test-tmp/…` → `EACCES: permission denied, mkdir`. Machine-specific; nothing to do with the ledger. |
| `test/health-watchdog-alert-sns.test.ts` | 2 | SNS watchdog parallelism flake — **passes in isolation** (`--no-file-parallelism`). |
| `test/rig/end-to-end-smoke.test.ts` | 2 | `rig.mjs` subprocess exits 1 on `ER_ACCESS_DENIED_ERROR` (MySQL user `noesis` access denied — DB-credential/env). |
| `test/rig/tarball-integrity.test.ts` | 1 | Same rig-subprocess `ER_ACCESS_DENIED_ERROR`. |

None touch the migrated subsystems (marketplace/upkeep/cowork/co-build/nous-runner); all are documented in the 62.5 flake list. My changes are comment-only in `grid/src` (nous-runner) plus test files, so they cannot have introduced these.

## Task Commits
1. **Task 1: grep sweep + retarget the remaining trade tests** — `263e59bb` (test)
2. **Task 2: atomic-rollback proofs for marketplace + upkeep (D-12)** — `c776d964` (test)
3. **Task 3: full-suite + gate sweep** — verification only, no file change (wiki clean → no economy.md edit).

## Deviations from Plan
### Auto-fixed Issues
None — the plan executed as written. Two non-source adjustments: (1) reworded the two `transferWei` comment mentions in `nous-runner.ts` (review-fail branch) to non-money phrasing so the Task 1 grep sums to exactly 0 (no behavior change); (2) discovered `e2e-tick-cycle` already passes, so it needed no retarget (the plan listed it as a candidate).

**Total deviations:** 0 source deviations.

## TDD Gate Compliance
Task 2 is marked `tdd="true"` but the migrated code already exists (landed in 62.6-01/02), so the two atomic-rollback proofs are GREEN-on-write regression tests, committed as `test(...)`. No RED gate is claimed — the feature commits are the Task 1/2 `feat(...)` commits of the earlier plans (62.6-01 `ec2d6985`/`a77f2cf6`, 62.6-02 `39f50f60`/`257f0e20`).

## Threat Flags
None. No new network endpoints, auth paths, schema, or audit surface. The plan's threat register (T-62.6-05-01..04) is satisfied: allowlist git-diff empty + sole-producer + zero-diff prove no event added/removed/reordered (T-01); the two failing-charge rollback tests prove no phantom state (T-02); `grep transferFrom` empty proves zero custody (T-03); the wallclock gate proves no new clock source (T-04).

## Known Stubs
None in source. Tests use the `makeAccountsPool` fake wei-rails pool and a stub `CivicDidStore` (in-memory existence→civic map) — test doubles for the MySQL-backed stores, not production stubs.

## Next Phase Readiness
- **Phase 62.6 is COMPLETE** (all 5 plans shipped). The four subsystems + the market skim hold no Ledger-B money; each rolls back atomically; the allowlist, sole-producer triads, wallclock, wiki, custody, and R-31-01 zero-diff are all intact.
- **62.5-04 unblocked** — the one-time data fold of `nous_registry.balance_wei` → `nous_accounts` + removal of `NousRegistry.transferWei` (only its definition + reference comments remain). Operator decision: **ZERO** faucet money at the fold.
- **62.5-05 unblocked** — retire the `did:noesis:system:treasury` registry record (writer-free since 62.6-01/02), delete PR #8 (treasury seed) / PR #11 (census exclusion), add the CI gate forbidding `transferWei`/`nous_registry.balance_wei` as money, clean-DB E2E.
- These two remain **Phase 62.5** waves and run after 62.6 — they were explicitly OUT OF SCOPE here.

## Self-Check: PASSED
All created/modified files present on disk; both task commits (`263e59bb`, `c776d964`) found in git history.

---
*Phase: 62.6-ledger-b-subsystem-migration*
*Completed: 2026-07-10*
