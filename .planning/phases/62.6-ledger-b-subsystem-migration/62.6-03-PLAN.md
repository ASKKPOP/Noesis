---
phase: 62.6-ledger-b-subsystem-migration
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - grid/src/civic/cowork.ts
  - grid/src/civic/co-build.ts
  - grid/src/api/routes/civic-parcels.ts
  - grid/test/civic/cowork.test.ts
  - grid/test/civic/co-build.test.ts
autonomous: true
requirements: [D-6, D-8, D-9, D-11, D-12]
must_haves:
  truths:
    - "Co-work and co-build host→worker settlement moves money via NousAccountStore.transfer (Nous→Nous, one atomic transaction), not via registry.transferWei."
    - "completeTask (cowork.ts) and completeSubTask/completeNode (co-build.ts) are async; the injectable transferWei? seam is replaced by an async settleWei? seam (default no-op) wired in the route to accountStore.transfer."
    - "D-NH-06 always-settles holds: a funded completion pays; if the transfer throws insufficient_balance the completion falls back to recordIou (never silently succeeds with no money moved and no IOU)."
    - "The credit-ledger IOU recording (recordIou) and bumpTrust remain untouched; the cowork_must_pay / cobuild_must_pay pay-nothing guards are unchanged."
    - "No registry.transferWei call remains in cowork.ts or co-build.ts; the board/complete route wires settleWei to NousAccountStore.transfer."
    - "zoning.cowork_session emits unchanged (participants hashed, count, parcel, tick span); no new audit event, no allowlist change (D-8); zero custody (D-9); no clock.onTick (request-driven, D-11)."
  artifacts:
    - path: "grid/src/civic/cowork.ts"
      provides: "async completeTask with settleWei seam + insufficient→IOU fallback"
      contains: "settleWei"
    - path: "grid/src/civic/co-build.ts"
      provides: "async completeSubTask/completeNode with settleWei seam"
      contains: "settleWei"
    - path: "grid/src/api/routes/civic-parcels.ts"
      provides: "board/complete route wiring settleWei → accountStore.transfer, funded recomputed from balance"
      contains: "accountStore.transfer"
  key_links:
    - from: "grid/src/api/routes/civic-parcels.ts board/complete"
      to: "grid/src/civic/cowork.ts completeTask"
      via: "settleWei seam calling NousAccountStore.transfer(host→worker)"
      pattern: "settleWei"
    - from: "grid/src/civic/cowork.ts / co-build.ts"
      to: "nous_accounts (worker)"
      via: "await settleWei(host, worker, amount)"
      pattern: "await.*settleWei"
---

<objective>
Migrate co-work and co-build host→worker settlement off the synchronous `registry.transferWei` seam onto an async `NousAccountStore.transfer` seam (Nous→Nous, one atomic transaction on `nous_accounts`), preserving the D-NH-06 always-settles invariant and the credit-ledger IOU fallback.

Purpose: these are Nous→Nous moves — they do NOT block retiring the treasury record, but they block removing `transferWei` (62.5-04). Migrating them completes the co-operative-labor money surface.
Output: cowork.ts + co-build.ts async settleWei seam; civic-parcels board/complete route wired; subsystem tests retargeted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/62.6-ledger-b-subsystem-migration/62.6-CONTEXT.md
@.planning/phases/62.6-ledger-b-subsystem-migration/62.6-RESEARCH.md

<interfaces>
From grid/src/economy/nous-account-store.ts (own transaction; throws 'insufficient_balance', 'invalid_transfer_self', 'invalid_amount'):
```typescript
class NousAccountStore {
  constructor(pool: Pool);
  transfer(p: { gridName: string; fromDid: string; toDid: string; amountWei: bigint; currentTick: number }): Promise<void>;
  getBalance(gridName: string, civicDid: string): Promise<bigint>;
}
```

Current cowork seam (grid/src/civic/cowork.ts): `CompleteDeps.transferWei?: (from,to,amount)=>void` (line 66); funded branch line 163 `(deps.transferWei ?? (() => {}))(hostDid, worker, amount);`. `completeTask` is currently SYNC (returns CoworkAgreement).

Current co-build seam (grid/src/civic/co-build.ts): `CoBuildDeps.transferWei?` (line 55); funded branch line 171 `(deps.transferWei ?? (() => {}))(session.host_did, workerDid, amount);`. `completeSubTask` SYNC; `completeNode = completeSubTask` alias.

Route wiring (grid/src/api/routes/civic-parcels.ts:815-861, board/complete): currently `const nous = services.registry; const agreement = completeTask(taskId, buyerDid, { funded: !!nous, ..., transferWei: nous ? (from,to,amount)=>{ nous.transferWei(from,to,amount); } : undefined, bumpTrust: ... });` — route handler is already `async`. `services.pool`, `services.gridName`, `currentTick(services)` are available (see civic-parcels.ts:155-158 pattern).

NOTE (verified): co-build's completeSubTask/completeNode have NO production route caller in grid/src (only cowork.ts's completeTask is wired, via board/complete). co-build is exercised through tests + the co-build.ts source. Migrate the co-build.ts seam + its tests; there is no build route to wire.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert the cowork + co-build settlement seams to an async settleWei with IOU fallback</name>
  <read_first>
    - grid/src/civic/cowork.ts (CompleteDeps 60-73, completeTask 147-185)
    - grid/src/civic/co-build.ts (CoBuildDeps 47-58, completeSubTask 152-181, completeNode alias 187)
    - grid/src/civic/credit-ledger.ts (recordIou signature)
  </read_first>
  <action>
**cowork.ts:** In `CompleteDeps` (line 60-73), replace `transferWei?: (from: string, to: string, amount: number) => void;` with `settleWei?: (fromCivic: string, toCivic: string, amountWei: number) => Promise<void>;`. Make `completeTask` async: change the signature to `export async function completeTask(agreementId: string, hostDid: string, deps: CompleteDeps): Promise<CoworkAgreement>`. Replace the funded branch (lines 161-167) with an always-settles block honoring D-NH-06 with an insufficient→IOU fallback:
```typescript
if (deps.funded) {
    try {
        await (deps.settleWei ?? (async () => {}))(hostDid, worker, amount);
    } catch (e) {
        if (e instanceof Error && e.message === 'insufficient_balance') {
            // D-NH-06 — a completion NEVER silently succeeds with no money moved: fall back to an IOU.
            (deps.recordIou ?? recordIou)(worker, hostDid, amount, a.agreement_id, deps.end_tick);
        } else {
            throw e;
        }
    }
} else {
    (deps.recordIou ?? recordIou)(worker, hostDid, amount, a.agreement_id, deps.end_tick);
}
```
Keep the `amount <= 0` → `cowork_must_pay` guard (lines 157-160), `bumpTrust` (line 170), the participants-hash construction, and the status transitions unchanged.

**co-build.ts:** In `CoBuildDeps` (line 47-58), replace `transferWei?: (from,to,amount)=>void` with `settleWei?: (fromCivic: string, toCivic: string, amountWei: number) => Promise<void>;`. Make `completeSubTask` async: `export async function completeSubTask(...): Promise<SubTaskSettlement>`. Replace the funded branch (lines 169-172) with the same try/catch → IOU-fallback pattern (`await (deps.settleWei ?? (async () => {}))(session.host_did, workerDid, amount);` on the funded path, catch `insufficient_balance` → `(deps.recordIou ?? recordIou)(workerDid, session.host_did, amount, session.session_id, tick)` and set `settlement = 'iou'`). Keep the `completeNode = completeSubTask` alias (it stays a valid async alias). Keep the `cobuild_must_pay` guard, `claimSubTask`, `attributionShare`, `isComplete`, `coBuildStaffOf` unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c "transferWei" grid/src/civic/cowork.ts` returns 0; `grep -c "transferWei" grid/src/civic/co-build.ts` returns 0.
    - `grep -n "settleWei" grid/src/civic/cowork.ts grid/src/civic/co-build.ts` returns matches.
    - `grep -n "async function completeTask" grid/src/civic/cowork.ts` and `grep -n "async function completeSubTask" grid/src/civic/co-build.ts` return matches.
    - The `cowork_must_pay` / `cobuild_must_pay` guards and `recordIou` unfunded branch remain present.
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx tsc --noEmit</automated>
  </verify>
  <done>completeTask/completeSubTask are async with an async settleWei seam; funded settlement uses settleWei with an insufficient→IOU fallback (D-NH-06); no transferWei seam remains; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the board/complete route to NousAccountStore.transfer and recompute funded from balance</name>
  <read_first>
    - grid/src/api/routes/civic-parcels.ts:815-861 (board/complete route) and 155-158 (accountStore/tick/gridName pattern)
    - grid/src/civic/cowork.ts (updated completeTask signature from Task 1)
    - grid/src/economy/nous-account-store.ts (transfer, getBalance)
  </read_first>
  <action>
In the `POST /api/v1/civic/parcels/:id/board/complete` handler (civic-parcels.ts:816-861): construct `const accountStore = new NousAccountStore(services.pool);` and `const tick = currentTick(services);` (mirror line 155-158; `NousAccountStore` is already imported in this file). Replace the `completeTask(...)` call block (834-842):
```typescript
const agreement = await completeTask(taskId, buyerDid, {
    funded: true,
    start_tick: startTick,
    end_tick: endTick,
    settleWei: (from, to, amt) => accountStore.transfer({
        gridName: services.gridName, fromDid: from, toDid: to, amountWei: BigInt(amt), currentTick: tick,
    }),
    bumpTrust: (parcelId, workerDid, delta) => parcelRegistry.bumpTrust(parcelId, workerDid, delta),
});
```
`funded: true` is safe because the settleWei path now catches `insufficient_balance` and falls back to an IOU inside completeTask (D-NH-06) — the host/worker are civic-DID citizens with nous_accounts rows. (Optionally pre-read `await accountStore.getBalance(services.gridName, buyerDid)` to set `funded` explicitly, but the in-transfer fallback already guarantees always-settles; keep the simpler form to reduce a redundant read.) Note the route handler is already `async`, so `await completeTask` is safe. Keep the `appendZoningCoworkSession` emit and the error mapping unchanged.
  </action>
  <acceptance_criteria>
    - `grep -n "accountStore.transfer" grid/src/api/routes/civic-parcels.ts` returns a match in the board/complete route.
    - `grep -n "settleWei:" grid/src/api/routes/civic-parcels.ts` returns a match.
    - `grep -c "nous.transferWei\|registry.transferWei" grid/src/api/routes/civic-parcels.ts` returns 0.
    - `grep -n "await completeTask" grid/src/api/routes/civic-parcels.ts` returns a match.
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx tsc --noEmit</automated>
  </verify>
  <done>The board/complete route wires settleWei to NousAccountStore.transfer(host→worker) and awaits the now-async completeTask; zoning.cowork_session emit unchanged; no transferWei remains in the route.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Retarget the cowork + co-build tests to the async settleWei seam</name>
  <read_first>
    - grid/test/civic/cowork.test.ts (pure spy `transferWei: vi.fn()`)
    - grid/test/civic/co-build.test.ts (pure spy `transferWei`)
    - grid/test/helpers/accounts-pool.ts
  </read_first>
  <behavior>
    - Funded completion: settleWei(host, worker, amount) awaited exactly once; worker credited, host debited (assert via a seeded accounts-pool or the spy).
    - insufficient_balance from settleWei: completion falls back to recordIou (worker=creditor, host=debtor); status still settles.
    - Unfunded (funded:false): recordIou path unchanged; cowork_must_pay/cobuild_must_pay guard on amount ≤ 0 still throws.
  </behavior>
  <action>
Rename the spy seam in both tests from `transferWei` to `settleWei` and make it async (`settleWei: vi.fn(async () => {})`). Add `await` to every `completeTask` / `completeSubTask` / `completeNode` call. Add a case where `settleWei` rejects with `new Error('insufficient_balance')` and assert `recordIou` was called (IOU fallback, D-NH-06). Optionally back settleWei with `makeAccountsPool()` + `NousAccountStore.transfer` to assert real balance conservation for the funded path. Keep the pay-nothing guard tests intact.
  </action>
  <acceptance_criteria>
    - `grep -c "transferWei" grid/test/civic/cowork.test.ts` returns 0; `grep -c "transferWei" grid/test/civic/co-build.test.ts` returns 0.
    - `grep -n "settleWei\|await completeTask\|await completeSubTask\|await completeNode" grid/test/civic/cowork.test.ts grid/test/civic/co-build.test.ts` returns matches.
    - `npx vitest run grid/test/civic/cowork.test.ts grid/test/civic/co-build.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>cd grid && npx vitest run test/civic/cowork.test.ts test/civic/co-build.test.ts</automated>
  </verify>
  <done>Cowork + co-build tests use the async settleWei seam, await the completion fns, and prove the insufficient→IOU fallback; no transferWei spy remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| civic writer → board/complete route → completeTask | authenticated host (buyerDid) confirms completion; worker DID is the stored claimant; amount is the stored agreement settlement |
| settleWei seam → nous_accounts | Nous→Nous transfer must be atomic; underfunded must degrade to an IOU, never a silent no-pay |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62.6-03-01 | Tampering | host→worker transfer | mitigate | NousAccountStore.transfer debits host and credits worker under SELECT ... FOR UPDATE in one transaction; a throw rolls back (no partial pay) |
| T-62.6-03-02 | Repudiation | D-NH-06 always-settles | mitigate | insufficient_balance is caught → recordIou; a completion can never end with no money moved and no IOU |
| T-62.6-03-03 | Information disclosure | zoning.cowork_session | accept | participants stay hashed (sorted DID set); no raw DIDs / board text on chain — emit unchanged |
| T-62.6-03-04 | Elevation of Privilege | zero custody | accept | in-DB bookkeeping only; no transferFrom, no platform keys |
</threat_model>

<verification>
- `cd grid && npx tsc --noEmit` clean.
- `grep -c "transferWei" grid/src/civic/cowork.ts grid/src/civic/co-build.ts grid/src/api/routes/civic-parcels.ts` totals 0.
- `npx vitest run test/civic/cowork.test.ts test/civic/co-build.test.ts` green.
</verification>

<success_criteria>
Co-work + co-build settle host→worker via NousAccountStore.transfer with an insufficient→IOU fallback (D-NH-06); the async seam is wired in the board/complete route; zoning.cowork_session unchanged; no transferWei remains in the subsystem; subsystem tests green.
</success_criteria>

<output>
After completion, create `.planning/phases/62.6-ledger-b-subsystem-migration/62.6-03-SUMMARY.md` — note that co-build has no production route (source seam + tests migrated) and record the funded/IOU-fallback policy chosen.
</output>
