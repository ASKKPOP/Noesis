---
phase: 58
plan: 03
type: execute
wave: 2
depends_on: [58-02]
files_modified:
  - grid/src/main.ts
  - grid/src/api/server.ts
  - grid/src/civic/parcel-store.ts
  - grid/test/civic/parcel-seed.test.ts
  - grid/test/civic/parcels-wiring.test.ts
autonomous: true
requirements: [R-58-02, R-58-05]
tags: [nous-house, house-1, seed, services-wiring, genesis-core]

must_haves:
  truths:
    - "On empty-grid boot, the seeder inserts EXACTLY 53 parcels with the correct zone-per-ring distribution and 5 pre-built commons venues"
    - "Seeding is idempotent on re-boot and emits NO audit events (world-creation, not commerce)"
    - "ParcelRegistry + ParcelStore are constructed inside the if (dbConn) block in main.ts and attached to GridServices via a parcels accessor"
    - "Boot emits [civic] parcels loaded: 53"
    - "GET /api/v1/civic/parcels smoke check passes (registry-not-wired bug class closed, R-H-01)"
  artifacts:
    - path: "grid/src/main.ts"
      provides: "ParcelStore.seedGenesisCore + hydrate on boot; GridServices.parcels attached; boot log line"
    - path: "grid/src/api/server.ts"
      provides: "GridServices.parcels?: optional accessor"
  key_links:
    - from: "grid/src/main.ts if (dbConn) block"
      to: "GridServices.parcels"
      via: "construct ParcelRegistry + ParcelStore, seed/hydrate, attach to services"
      pattern: "parcels"
    - from: "GENESIS_CORE_SEED_PLAN"
      to: "53 civic_parcels rows"
      via: "seedGenesisCore iterates the plan with INSERT IGNORE"
      pattern: "seedGenesisCore"
---

<objective>
On boot, seed exactly the 53-parcel Genesis Core (idempotent, no audit events) and attach ParcelRegistry + ParcelStore to GridServices — closing the registry-not-wired bug class (R-H-01, the `civicDidStore` not-wired incident) with a boot log line and a CI smoke check.
</objective>

<context>
@.planning/phases/58-house-1-foundations/58-CONTEXT.md
@grid/src/civic/founding-law.ts
@grid/src/civic/parcel-store.ts
@grid/src/civic/parcel-registry.ts
@grid/src/api/server.ts
@grid/src/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Genesis Core seeding (exactly 53, idempotent, no audit events)</name>
  <files>
    grid/src/civic/parcel-store.ts,
    grid/test/civic/parcel-seed.test.ts
  </files>
  <read_first>
    grid/src/civic/founding-law.ts (GENESIS_CORE_SEED_PLAN — the single source of the seed),
    grid/test/civic/parcel-seed.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Finalize `seedGenesisCore(registry)` in `parcel-store.ts` so that on an empty `civic_parcels` for the grid it inserts EXACTLY 53 parcels:
    - ring 0 government_quarter ×1 (a pre-built `venue`/`open` monument structure),
    - ring 1 infrastructure ×4 (pre-built `venue`/`open` commons: guest hall, market floor, shared workshop, archive),
    - ring 2 business ×8 + shopping ×8 + manufacture ×8 (unowned, priced 900 via gravityPrice(2)),
    - ring 3 residential ×24 (unowned, priced 400 via gravityPrice(3)).
    The 5 civic parcels (rings 0-1) are NOT purchasable (rings not in PURCHASABLE_RINGS). Seeding is idempotent (INSERT IGNORE) and emits NO audit events — it never touches the AuditChain. Ring 4+ is absent.

    Un-skip `parcel-seed.test.ts` and make it green (count 53, distribution, 5 commons venues, no audit events emitted during seed — assert a spy on any append-* sole-producer is never called).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/parcel-seed 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - parcel-seed.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/parcel-seed` is green (count 53, distribution, 5 commons venues, zero audit events)
    - re-running seedGenesisCore on the same grid does not duplicate rows (idempotent)
  </acceptance_criteria>
  <done>seedGenesisCore inserts exactly 53 parcels idempotently with no audit events; parcel-seed.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GridServices wiring in main.ts + boot log + smoke check</name>
  <files>
    grid/src/main.ts,
    grid/src/api/server.ts,
    grid/test/civic/parcels-wiring.test.ts
  </files>
  <read_first>
    grid/src/api/server.ts (GridServices interface; civicDidStore?: precedent at the optional-field site),
    grid/src/main.ts (the if (dbConn) block where DB-backed stores are constructed)
  </read_first>
  <behavior>
    Add `parcels?: { registry: ParcelRegistry; store: ParcelStore }` to the GridServices interface in `grid/src/api/server.ts` (mirror the existing optional `civicDidStore?:` style — surgical addition).

    In `grid/src/main.ts`, inside the `if (dbConn)` block: construct a `ParcelRegistry` and a `ParcelStore`, call `seedGenesisCore(registry)` then `hydrate(registry)`, attach `{ registry, store }` to GridServices, and emit the boot log line `[civic] parcels loaded: N` (N = hydrated row count, 53 on a fresh grid).

    Un-skip `parcels-wiring.test.ts` and make it green: `GridServices.parcels` is defined after wiring, and `GET /api/v1/civic/parcels` returns 200 with a `parcels` array (registry-not-wired smoke check, R-H-01). The route itself lands in Wave 3 — if it is not yet registered, the wiring test asserts the services accessor only and the GET assertion is added/un-skipped together with Wave 3. Keep the GET smoke assertion in this file but gate it so it goes green once the route exists (Wave 3 depends_on this plan).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/parcels-wiring && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "parcels?:" grid/src/api/server.ts` shows the new optional accessor
    - `grep -nE "ParcelStore|ParcelRegistry|parcels loaded" grid/src/main.ts` shows construction inside the dbConn block + the boot log
    - parcels-wiring.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/parcels-wiring` is green (services accessor defined; GET smoke green after Wave 3 lands)
    - typecheck passes
  </acceptance_criteria>
  <done>ParcelRegistry + ParcelStore constructed in the dbConn block and attached to GridServices; boot log [civic] parcels loaded: 53; wiring test green.</done>
</task>

</tasks>

<success_criteria>
- Exactly 53 parcels seeded idempotently with no audit events; ring 4+ absent
- GridServices.parcels attached in main.ts inside the dbConn block; boot log line present
- registry-not-wired bug class closed by the wiring + GET smoke check
- parcel-seed.test.ts and parcels-wiring.test.ts un-skipped and green
</success_criteria>

<output>
After completion, create `.planning/phases/58-house-1-foundations/58-03-SUMMARY.md`.
</output>
