---
phase: 58
plan: 06
type: execute
wave: 5
depends_on: [58-04]
files_modified:
  - dashboard/src/components/worldmap/OrbitalGenesisMap.tsx
  - dashboard/src/app/worldmap/orbital/page.tsx
  - dashboard/src/app/worldmap/page.tsx
  - dashboard/src/components/portal/CyberGrid.tsx
  - dashboard/test/orbital-genesis-map.test.tsx
autonomous: true
requirements: [R-58-11, R-58-12]
tags: [nous-house, house-1, visualization, orbital-map, dashboard, ny-clock]

must_haves:
  truths:
    - "OrbitalGenesisMap.tsx ported from docs/noesis-genesis-core-map.html: live fetch of GET /api/v1/civic/parcels with fallback to the embedded seed; Earth below (D-NH-12); Government Core monument; ghost frames flip to lit modules when owner_civic_did_hash present; occupancy lights from occupant counts"
    - "NY clock (D-NH-11) renders ONLY here at the display boundary (Date allowed in dashboard); no tick->calendar conversion leaks into grid/audit paths — scripts/check-wallclock-forbidden.mjs stays green"
    - "New route /worldmap/orbital is ADDITIVE: /worldmap keeps the existing CyberGrid city view; orbital is linked from landing 'Explore the map' and /portal/civic-map"
    - "No raw owner DID is rendered — only owner_civic_did_hash from the feed"
  artifacts:
    - path: "dashboard/src/components/worldmap/OrbitalGenesisMap.tsx"
      provides: "Live orbital Genesis Core map component"
    - path: "dashboard/src/app/worldmap/orbital/page.tsx"
      provides: "Additive /worldmap/orbital route"
  key_links:
    - from: "GET /api/v1/civic/parcels"
      to: "OrbitalGenesisMap rendered parcels"
      via: "live fetch with embedded-seed fallback"
      pattern: "civic/parcels"
    - from: "owner_civic_did_hash present"
      to: "ghost frame flips to lit solid module"
      via: "render predicate on the hashed owner field"
      pattern: "owner_civic_did_hash"
---

<objective>
Make the land visible (D-NH-01): port the canonical orbital map into the dashboard as a live component fed by the parcel API, ADDITIVELY alongside the existing city view, with Earth below (D-NH-12) and the NY clock at the display boundary only (D-NH-11). No tick->calendar conversion leaks into grid/audit paths.
</objective>

<context>
@.planning/phases/58-house-1-foundations/58-CONTEXT.md
@docs/noesis-genesis-core-map.html
@grid/src/api/routes/civic-parcels.ts
@dashboard/src/app/worldmap/page.tsx
@dashboard/src/components/portal/CyberGrid.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: OrbitalGenesisMap component (live fetch, Earth below, lit modules, occupancy)</name>
  <files>
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx,
    dashboard/test/orbital-genesis-map.test.tsx
  </files>
  <read_first>
    docs/noesis-genesis-core-map.html (the canonical visual to port — radial rings, Earth below, Government Core monument, ghost vs lit frames),
    grid/src/api/routes/civic-parcels.ts (the GET /api/v1/civic/parcels feed shape: ring/sector/level, owner_civic_did_hash, occupant count)
  </read_first>
  <behavior>
    Create `dashboard/src/components/worldmap/OrbitalGenesisMap.tsx`, ported from `docs/noesis-genesis-core-map.html`:
    - live fetch of `GET /api/v1/civic/parcels`, with a fallback to the embedded 53-parcel seed when the fetch fails.
    - Earth below (D-NH-12, blue-marble); Government Core monument at ring 0.
    - ghost frames flip to lit SOLID modules when `owner_civic_did_hash` is present; unowned parcels stay as ghost frames.
    - occupancy lights driven by occupant counts from the feed.
    - the NY clock (D-NH-11) rendered only here (`Date` is allowed in the dashboard display boundary; the wallclock gate scopes grid/dashboard-replay, NOT this map).
    - NEVER render a raw owner DID — only the hashed `owner_civic_did_hash`.

    Create `dashboard/test/orbital-genesis-map.test.tsx` asserting: renders 53 seed parcels from the fallback; lit vs ghost based on `owner_civic_did_hash`; occupancy lights from counts; and that no raw owner DID is rendered (only hashes from the feed).
  </behavior>
  <verify>
    <automated>cd dashboard && npm run test -- orbital-genesis-map 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "civic/parcels|owner_civic_did_hash" dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` shows the live fetch + hashed-owner render
    - the component renders Earth below + a Government Core monument
    - `cd dashboard && npm run test -- orbital-genesis-map` is green (53 fallback parcels, lit/ghost, occupancy, no raw DID)
  </acceptance_criteria>
  <done>OrbitalGenesisMap renders the live Genesis Core (Earth below, lit owned modules, occupancy, NY clock); test green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: additive /worldmap/orbital route + links; wallclock gate green</name>
  <files>
    dashboard/src/app/worldmap/orbital/page.tsx,
    dashboard/src/app/worldmap/page.tsx,
    dashboard/src/components/portal/CyberGrid.tsx
  </files>
  <read_first>
    dashboard/src/app/worldmap/page.tsx (existing city view — must be KEPT, this is additive),
    dashboard/src/components/portal/CyberGrid.tsx (landing 'Explore the map' link target + /portal/civic-map link)
  </read_first>
  <behavior>
    Add the route `dashboard/src/app/worldmap/orbital/page.tsx` rendering `OrbitalGenesisMap`. This is ADDITIVE — `/worldmap` keeps the existing CyberGrid city view unchanged. Link the orbital view from the landing 'Explore the map' entry point and from `/portal/civic-map` (surgical link additions only — do not restructure the existing city view).

    Confirm `scripts/check-wallclock-forbidden.mjs` stays green: the only Date/NY-calendar usage is inside the dashboard orbital component (display boundary), never in grid/audit/consensus paths.
  </behavior>
  <verify>
    <automated>cd dashboard && npm run build 2>&1 | tail -15; node scripts/check-wallclock-forbidden.mjs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls dashboard/src/app/worldmap/orbital/page.tsx` exists
    - `dashboard/src/app/worldmap/page.tsx` city view is unchanged in behavior (additive only)
    - landing 'Explore the map' and /portal/civic-map link to /worldmap/orbital
    - `node scripts/check-wallclock-forbidden.mjs` exits 0 (no tick->calendar leak into grid/audit)
    - dashboard typecheck/lint/test pass for the new component + route
  </acceptance_criteria>
  <done>/worldmap/orbital is additive beside the city view, linked from landing + civic-map; wallclock gate green.</done>
</task>

</tasks>

<success_criteria>
- OrbitalGenesisMap live-fetches the parcel feed (fallback to embedded seed); Earth below; lit owned modules; occupancy lights; NY clock at the display boundary only
- /worldmap/orbital additive beside the kept /worldmap city view; linked from landing + /portal/civic-map
- No raw owner DID rendered; scripts/check-wallclock-forbidden.mjs green
- orbital-genesis-map.test.tsx green
</success_criteria>

<output>
After completion, create `.planning/phases/58-house-1-foundations/58-06-SUMMARY.md`.
</output>
