---
phase: 58
plan: 02
type: execute
wave: 1
depends_on: [58-01]
files_modified:
  - grid/src/db/schema.ts
  - grid/src/civic/founding-law.ts
  - grid/src/civic/types.ts
  - grid/src/civic/parcel-store.ts
  - grid/src/civic/parcel-registry.ts
  - grid/test/civic/parcel-store.test.ts
  - grid/test/civic/founding-law.test.ts
autonomous: true
requirements: [R-58-01, R-58-03, R-58-04]
tags: [nous-house, house-1, persistence, migration, gravity-pricing, vector-address]

must_haves:
  truths:
    - "Migration v38 (next free after shipped v37) creates civic_parcels with ring/sector_deg/level vector-address columns and a working down migration"
    - "gravityPrice(ring) = 100 * (5 - ring)^2 lives ONLY in founding-law.ts; ring 3 = 400, ring 2 = 900; rings 0-1 not purchasable"
    - "Parcel/Structure types carry (ring, sector, level) — no code path assumes a 2D plane"
    - "ParcelStore is write-through: DB first then memory; hydrate(registry) reloads on boot; occupants NOT persisted"
    - "parcel-store.test.ts + founding-law.test.ts un-skipped and green"
  artifacts:
    - path: "grid/src/db/schema.ts"
      provides: "Migration v38 in the MIGRATIONS array creating/dropping civic_parcels"
    - path: "grid/src/civic/founding-law.ts"
      provides: "gravityPrice + PURCHASABLE_RINGS + GENESIS_CORE_SEED_PLAN constants (single patch point)"
    - path: "grid/src/civic/types.ts"
      provides: "Parcel gains ring/sector/level (D-NH-10)"
    - path: "grid/src/civic/parcel-store.ts"
      provides: "Write-through ParcelStore (seed/hydrate/persist*), DB source of truth"
  key_links:
    - from: "grid/src/civic/founding-law.ts gravityPrice"
      to: "grid/src/civic/parcel-store.ts seedGenesisCore"
      via: "seed plan reads gravityPrice(ring) for the price_bios of each purchasable parcel"
      pattern: "gravityPrice\\("
    - from: "civic_parcels rows"
      to: "ParcelRegistry in-memory map"
      via: "ParcelStore.hydrate(registry) on boot"
      pattern: "hydrate\\("
---

<objective>
Wake the dormant registry's storage layer. Create the `civic_parcels` table (migration v38), centralize gravity-pricing + seed constants in `founding-law.ts`, extend the Parcel/Structure types with the (ring, sector, level) vector address (D-NH-10), and implement a write-through `ParcelStore` where the DB is the source of truth and the registry is hydrated from it on boot.
</objective>

<context>
@.planning/phases/58-house-1-foundations/58-CONTEXT.md
@grid/src/civic/parcel-registry.ts
@grid/src/civic/types.ts
@grid/src/db/schema.ts

<lesson>
HumanRegistry incident (2026-06-11): an in-memory registry that wasn't backed by MySQL lost state on restart and diverged from the chain. The registry is a READ CACHE; MySQL is the source of truth. Every mutation writes DB-first then memory; boot hydrates memory from DB.
</lesson>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: founding-law constants + vector-address types</name>
  <files>
    grid/src/civic/founding-law.ts,
    grid/src/civic/types.ts,
    grid/test/civic/founding-law.test.ts
  </files>
  <read_first>
    grid/src/civic/types.ts (existing ZoneId, PURCHASABLE_ZONES, Parcel, Structure),
    grid/test/civic/founding-law.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/founding-law.ts` exporting:
    - `gravityPrice(ring: number): number` = `100 * (5 - ring) ** 2`. ring 3 -> 400, ring 2 -> 900.
    - `PURCHASABLE_RINGS: readonly number[] = [2, 3]` (rings 0-1 are civic land, not for sale).
    - `GENESIS_CORE_SEED_PLAN` — the per-ring zone/count plan for D-58-03 (ring 0 government_quarter ×1; ring 1 infrastructure ×4 prebuilt venue; ring 2 business ×8 + shopping ×8 + manufacture ×8; ring 3 residential ×24). This is the SINGLE patch point Phase 60 Polis amendments edit.

    Extend `grid/src/civic/types.ts`:
    - add `ring: number; sector: number; level: number` to `Parcel` (D-NH-10 vector address; `sector` in degrees, `level` defaults 0). Do NOT collapse to a 2D coordinate.
    - keep all existing fields, exports, and the `Structure` shape intact (surgical extension only).

    Un-skip `founding-law.test.ts` (remove `.skip`) and make it green.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/founding-law && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "gravityPrice" grid/src/civic/founding-law.ts` returns the export
    - founding-law.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/founding-law` is green (ring 3 = 400, ring 2 = 900, rings 0-1 not purchasable)
    - `grep -E "ring|sector|level" grid/src/civic/types.ts` shows the three new Parcel fields
    - typecheck passes
  </acceptance_criteria>
  <done>founding-law.ts holds the only copy of gravity pricing + the seed plan; Parcel carries (ring, sector, level); founding-law.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: migration v38 + write-through ParcelStore</name>
  <files>
    grid/src/db/schema.ts,
    grid/src/civic/parcel-store.ts,
    grid/src/civic/parcel-registry.ts,
    grid/test/civic/parcel-store.test.ts
  </files>
  <read_first>
    grid/src/db/schema.ts (MIGRATIONS array, latest shipped version is 37; v38 is next free),
    grid/src/civic/parcel-registry.ts (in-memory state surface ParcelStore must persist/hydrate),
    grid/test/civic/parcel-store.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Add migration **v38** to the MIGRATIONS array in `grid/src/db/schema.ts` creating `civic_parcels`:
    - `parcel_id VARCHAR PK`, `grid_name`, `zone_id`, `ring TINYINT`, `sector_deg DECIMAL(6,2)`, `level SMALLINT DEFAULT 0` (D-NH-10),
      `owner_civic_did` NULL (NULL = treasury), `price_bios`, `acquired_at_tick` NULL,
      `structure_name` NULL (plaintext, Grid-side only), `structure_type ENUM(home|shop|workshop|venue)` NULL,
      `visibility ENUM(open|private)` NULL, `built_at_tick` NULL, `named_address` NULL (wired Phase 60),
      `entry_policy ENUM(open|allowlist) DEFAULT open`, `entry_allowlist JSON` NULL,
      indices `idx_parcel_owner (owner_civic_did)`, `idx_parcel_zone (grid_name, zone_id)`.
    - a working `down` that drops `civic_parcels`. Version 38 is the next free version after shipped v37.

    Create `grid/src/civic/parcel-store.ts` — a write-through store:
    - `seedGenesisCore(registry)` — INSERT IGNORE per the GENESIS_CORE_SEED_PLAN (idempotent), prices purchasable parcels via `gravityPrice(ring)`, mirrors into the registry. Returns rows seeded.
    - `hydrate(registry)` — read all rows for the grid into the in-memory registry on boot. Returns row count.
    - `persistPurchase(parcel)` / `persistBuild(parcel)` / `persistEntryPolicy(parcel)` — UPDATE the row DB-first; the route mirrors into memory afterward.
    - Occupants are NOT persisted (presence is memory-only — do not add a column or a write for them).

    Un-skip `parcel-store.test.ts` and make it green: seed idempotency, write-through DB row after purchase, hydrate-on-boot, gravity prices (900/400), caps (<=1 home, <=1 business), commons-not-purchasable.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/parcel-store civic/founding-law 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "version: 38|civic_parcels" grid/src/db/schema.ts` shows the new migration and table
    - `grep -E "ring|sector_deg|level" grid/src/db/schema.ts` shows the vector-address columns
    - migration applies cleanly against a fresh DB in CI; down drops civic_parcels
    - parcel-store.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/parcel-store civic/founding-law` is green
    - no `clock.onTick` subscription added (single-onTick invariant); no occupants column
  </acceptance_criteria>
  <done>Migration v38 creates civic_parcels with vector-address columns; ParcelStore is write-through with hydrate-on-boot; parcel-store + founding-law suites green.</done>
</task>

</tasks>

<success_criteria>
- Migration v38 added and reversible; civic_parcels carries ring/sector_deg/level
- gravityPrice + seed plan centralized in founding-law.ts (single patch point)
- Parcel/Structure types carry the vector address; no 2D assumption
- ParcelStore is write-through (DB first, memory mirror, hydrate on boot); occupants memory-only
- parcel-store.test.ts and founding-law.test.ts un-skipped and green
</success_criteria>

<output>
After completion, create `.planning/phases/58-house-1-foundations/58-02-SUMMARY.md`.
</output>
