---
phase: 58
plan: 05
type: execute
wave: 4
depends_on: [58-04]
files_modified:
  - brain/src/noesis_brain/rpc/types.py
  - brain/src/noesis_brain/rpc/handler.py
  - brain/src/noesis_brain/wire/client.py
  - brain/src/noesis_brain/prompts/builder.py
  - brain/tests/test_civic_land_verbs.py
autonomous: true
requirements: [R-58-10]
tags: [nous-house, house-1, brain, action-types, my-places, capabilities]

must_haves:
  truths:
    - "ActionType gains civic-land members: list_parcels, buy_parcel, build, visit, leave, set_entry_policy with documented metadata shapes"
    - "Verbs are capabilities (not scripts): GridWireClient dispatches them to POST /api/v1/brain/actions; the Grid action handler routes each to the matching civic-parcels route"
    - "The Brain prompt builder gains a my_places block summarizing owned parcels + built structures (Smallville Lesson 2: home anchors routine)"
    - "Decision pressure stays with the Nous — no autoplay"
  artifacts:
    - path: "brain/src/noesis_brain/rpc/types.py"
      provides: "ActionType civic-land members + metadata shapes (buy_parcel {zone, max_price}; build {parcel, type, name})"
    - path: "brain/src/noesis_brain/prompts/builder.py"
      provides: "my_places prompt block sourced from the Nous's land state"
  key_links:
    - from: "ActionType.buy_parcel"
      to: "POST /api/v1/civic/parcels/:id/purchase"
      via: "GridWireClient -> POST /api/v1/brain/actions -> Grid action handler routing"
      pattern: "buy_parcel|civic/parcels"
    - from: "Nous land state"
      to: "my_places prompt block"
      via: "prompt builder renders owned parcels + built structures"
      pattern: "my_places"
---

<objective>
Give the Nous land capabilities (not scripts): new ActionType verbs dispatched to the Grid civic-parcels routes via GridWireClient, and a `my_places` prompt-context block so ownership anchors routine (Smallville Lesson 2: home is the anchor for daily routine).
</objective>

<context>
@.planning/phases/58-house-1-foundations/58-CONTEXT.md
@grid/src/api/routes/civic-parcels.ts
@brain/src/noesis_brain/rpc/types.py
@brain/src/noesis_brain/rpc/handler.py
@brain/src/noesis_brain/wire/client.py
@brain/src/noesis_brain/prompts/builder.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActionType civic-land verbs + dispatch routing</name>
  <files>
    brain/src/noesis_brain/rpc/types.py,
    brain/src/noesis_brain/rpc/handler.py,
    brain/src/noesis_brain/wire/client.py
  </files>
  <read_first>
    brain/src/noesis_brain/rpc/types.py (existing ActionType enum + metadata-shape convention),
    brain/src/noesis_brain/wire/client.py (GridWireClient dispatch to POST /api/v1/brain/actions),
    brain/src/noesis_brain/rpc/handler.py (Action production + routing precedent)
  </read_first>
  <behavior>
    Add ActionType members to `brain/src/noesis_brain/rpc/types.py`: `list_parcels`, `buy_parcel`, `build`, `visit`, `leave`, `set_entry_policy`, with documented metadata shapes:
    - `buy_parcel` metadata `{zone, max_price}`
    - `build` metadata `{parcel, type, name}`
    - `visit` metadata `{parcel}`; `set_entry_policy` metadata `{parcel, policy, allowlist?}`; `list_parcels` / `leave` need no extra metadata.

    The handler produces these Actions; `GridWireClient` dispatches them to `POST /api/v1/brain/actions`; the Grid action handler routes each to the matching civic-parcels route (`buy_parcel` -> `:id/purchase`, `build` -> `:id/build`, `visit` -> `:id/join`, `leave` -> `:id/leave`, `set_entry_policy` -> `:id/entry-policy`, `list_parcels` -> `GET /api/v1/civic/parcels`). Verbs are CAPABILITIES — no autoplay; decision pressure stays with the Nous.
  </behavior>
  <verify>
    <automated>cd brain && pytest tests/test_civic_land_verbs.py -k "action_type or dispatch" 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "list_parcels|buy_parcel|set_entry_policy" brain/src/noesis_brain/rpc/types.py` shows the new members
    - metadata validation rejects a buy_parcel missing zone/max_price and a build missing parcel/type/name
    - dispatch routing maps each verb to the correct civic-parcels route
  </acceptance_criteria>
  <done>ActionType has the 6 civic-land verbs with metadata shapes; dispatch routes them to the civic-parcels routes; no autoplay.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: my_places prompt block</name>
  <files>
    brain/src/noesis_brain/prompts/builder.py,
    brain/tests/test_civic_land_verbs.py
  </files>
  <read_first>
    brain/src/noesis_brain/prompts/builder.py (prompt-context block assembly convention),
    brain/tests/test_civic_land_verbs.py (Wave 0-style stub — author it here in this wave)
  </read_first>
  <behavior>
    Add a `my_places` block to the Brain prompt builder summarizing the Nous's owned parcels and built structures, sourced from the Nous's land state (e.g. "I own genesis:residential:0007; my home is built"). The block is EMPTY (omitted) when the Nous owns no land. This anchors routine (Smallville Lesson 2).

    Author `brain/tests/test_civic_land_verbs.py` asserting: the new ActionType values exist; metadata validation; dispatch routing; and that `my_places` renders when the Nous owns land and is empty otherwise.
  </behavior>
  <verify>
    <automated>cd brain && pytest tests/test_civic_land_verbs.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "my_places" brain/src/noesis_brain/prompts/builder.py` shows the block
    - `cd brain && pytest tests/test_civic_land_verbs.py` is green (ActionType values, metadata validation, dispatch routing, my_places renders/empty)
  </acceptance_criteria>
  <done>my_places prompt block renders owned land; test_civic_land_verbs.py green.</done>
</task>

</tasks>

<success_criteria>
- 6 civic-land ActionType verbs exist as capabilities with documented metadata shapes
- GridWireClient dispatches them; the Grid action handler routes each to the matching civic-parcels route
- my_places prompt block surfaces ownership (anchors routine); empty when no land
- brain/tests/test_civic_land_verbs.py green
</success_criteria>

<output>
After completion, create `.planning/phases/58-house-1-foundations/58-05-SUMMARY.md`.
</output>
