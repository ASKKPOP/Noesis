---
phase: 58
plan: 07
type: execute
wave: 6
depends_on: [58-04, 58-05, 58-06]
files_modified:
  - grid/test/civic/house-1-e2e.test.ts
  - scripts/check-wallclock-forbidden.mjs
  - scripts/check-civic-did-issuance-path.mjs
  - grid/test/audit/broadcast-allowlist.test.ts
autonomous: true
requirements: [R-58-07, R-58-08, R-58-12]
tags: [nous-house, house-1, e2e, gates, definition-of-done]

must_haves:
  truths:
    - "End-to-end: a Nous buys a ring-3 parcel for 400 Bios -> zoning.parcel_purchased + treasury.parcel_revenue; builds a home -> zoning.structure_built; a 2nd Nous joins -> zoning.structure_joined; leaves -> zoning.structure_left. All five events on the public audit trail, owner only as owner_civic_did_hash"
    - "A signed-in human (human_visitor and did:civic:noesis:human:*) attempting purchase is refused (401 / 403 humans_cannot_own_land)"
    - "broadcast-allowlist.test.ts green at 91 (delta +0 confirmed); no chain/persistence-of-chain code edited (zero-diff R-31-01)"
    - "scripts/check-wallclock-forbidden.mjs and scripts/check-civic-did-issuance-path.mjs exit 0 unchanged; sole-producer + privacy walker gates green"
    - "All Wave-0 skip-stubs are un-skipped (grep -c describe.skip across Phase 58 test files is 0)"
  artifacts:
    - path: "grid/test/civic/house-1-e2e.test.ts"
      provides: "Definition-of-Done E2E: buy->build->join->leave with all 5 events, plus human-refused"
  key_links:
    - from: "house-1-e2e buy ring-3 for 400"
      to: "zoning.parcel_purchased + treasury.parcel_revenue on the trail"
      via: "full route path through registry.transferOusia + existing producers"
      pattern: "zoning.parcel_purchased|treasury.parcel_revenue"
    - from: "all Phase 58 test files"
      to: "0 remaining describe.skip"
      via: "every Wave un-skipped its stubs"
      pattern: "describe.skip"
---

<objective>
Prove the Definition of Done and keep every CI gate green. The master-plan acceptance scenario: a Nous buys a ring-3 parcel for 400 Bios, builds a home, a second Nous visits, all five events appear on the public audit trail, the orbital map shows the lit parcel with one occupant, and a signed-in human is refused with `humans_cannot_own_land`.
</objective>

<context>
@.planning/phases/58-house-1-foundations/58-CONTEXT.md
@.planning/phases/58-house-1-foundations/58-VALIDATION.md
@grid/src/api/routes/civic-parcels.ts
@grid/src/civic/parcel-store.ts
@grid/src/audit/broadcast-allowlist.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOUSE-1 end-to-end Definition-of-Done test</name>
  <files>
    grid/test/civic/house-1-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (the full purchase/build/join/leave path),
    grid/src/civic/founding-law.ts (gravityPrice(3) === 400),
    grid/src/audit/append-zoning-parcel-purchased.ts (+ siblings — payload shapes for the trail assertions)
  </read_first>
  <behavior>
    Author `grid/test/civic/house-1-e2e.test.ts` exercising the master-plan acceptance scenario end-to-end against a seeded Genesis Core:
    - Nous A buys `genesis:residential:NNNN` for 400 Bios -> assert `zoning.parcel_purchased` + `treasury.parcel_revenue` appear on the audit trail (owner only as `owner_civic_did_hash` HEX64; balance moved buyer -> TREASURY_DID).
    - Nous A builds a home -> assert `zoning.structure_built`.
    - Nous B joins the structure -> assert `zoning.structure_joined`; the parcel reports 1 occupant.
    - Nous B leaves -> assert `zoning.structure_left`.
    - All five events present on the public trail; no raw owner DID anywhere in the payloads.
    - A signed-in human: `human_visitor` tier attempting purchase -> 401; a `did:civic:noesis:human:*` DID attempting purchase -> 403 `humans_cannot_own_land`.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/house-1-e2e 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test -- civic/house-1-e2e` is green
    - the test asserts all 5 events on the trail and the human-refused (401 / 403 humans_cannot_own_land) cases
    - the test confirms gravityPrice(3) === 400 paid and owner appears only as owner_civic_did_hash
  </acceptance_criteria>
  <done>house-1-e2e.test.ts green: buy->build->join->leave with all 5 events; human purchase refused.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gates green + all skip-stubs un-skipped</name>
  <files>
    scripts/check-wallclock-forbidden.mjs,
    scripts/check-civic-did-issuance-path.mjs,
    grid/test/audit/broadcast-allowlist.test.ts
  </files>
  <read_first>
    grid/test/audit/broadcast-allowlist.test.ts (must still assert 91; do NOT edit),
    scripts/check-wallclock-forbidden.mjs (must stay green — read-only confirmation),
    scripts/check-civic-did-issuance-path.mjs (must stay green — read-only confirmation)
  </read_first>
  <behavior>
    Verify (do NOT weaken) every CI gate:
    - `broadcast-allowlist.test.ts` green at 91 (delta +0 confirmed); the file is unchanged.
    - the sole-producer gate, `scripts/check-civic-did-issuance-path.mjs`, the privacy walker, and `scripts/check-wallclock-forbidden.mjs` all pass unchanged.
    - zero-diff (R-31-01) holds — no chain/persistence-of-chain code was edited; new events ride existing sole producers.
    - the full grid suite is green.
    - all Wave-0 skip-stubs from 58-01 are now un-skipped — `grep -c describe.skip` across the Phase 58 test files is 0.

    These two scripts are listed in files_modified only as gate touch-points; if they pass unchanged, do not edit them. If a gate fails, the fix belongs in the offending earlier wave's source, NOT in the gate (never weaken a gate to pass).
  </behavior>
  <verify>
    <automated>cd grid && npm run test; node scripts/check-wallclock-forbidden.mjs; node scripts/check-civic-did-issuance-path.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test` is fully green
    - `cd grid && npm run test -- audit/broadcast-allowlist` green at 91 (+0); the file is unchanged from baseline
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
    - `node scripts/check-civic-did-issuance-path.mjs` exits 0
    - `grep -rc "describe.skip" grid/test/civic grid/test/api/civic-parcels-routes.test.ts | grep -v ':0$'` returns nothing (no remaining skips in Phase 58 test files)
    - no chain/persistence-of-chain file appears in the phase diff (zero-diff R-31-01)
  </acceptance_criteria>
  <done>All CI gates green and unchanged; allowlist at 91 (+0); every Phase 58 skip-stub un-skipped; zero-diff holds.</done>
</task>

</tasks>

<success_criteria>
- End-to-end DoD passes: Nous buys ring-3 for 400, builds home, 2nd Nous visits (1 occupant), all 5 events on the public trail; human purchase refused humans_cannot_own_land
- broadcast-allowlist green at 91 (+0); sole-producer, civic-DID-issuance-path, privacy walker, wallclock-forbidden, zero-diff (R-31-01) all green and unchanged
- No remaining describe.skip in any Phase 58 test file
</success_criteria>

<output>
After completion, create `.planning/phases/58-house-1-foundations/58-07-SUMMARY.md` and the phase-completion report.
</output>
