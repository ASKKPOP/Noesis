---
phase: 09-relationship-graph-derived-view
plan: 04
type: execute
wave: 2
depends_on: ["09-02", "09-03"]
files_modified:
  - grid/src/api/operator/relationships.ts
  - grid/src/api/index.ts
  - grid/src/genesis/launcher.ts
  - grid/src/genesis/types.ts
  - grid/test/api/relationships-privacy.test.ts
  - grid/test/relationships/listener-launcher-order.test.ts
autonomous: true
requirements:
  - REL-04
tags:
  - phase-09
  - relationships
  - api
  - launcher
  - wave-2
must_haves:
  truths:
    - "GenesisLauncher constructs RelationshipListener AFTER the aggregator, sharing the same AuditChain instance (D-9-04)"
    - "Launcher schedules storage.scheduleSnapshot() every config.snapshotCadenceTicks ticks (default 100) via tick-driven observer, not wall-clock (D-9-03, D-9-12)"
    - "On Grid startup, launcher calls listener.rebuildFromChain() after audit chain is loaded — idempotent rebuild holds across restarts (REL-02)"
    - "Four tier-graded endpoints exist — H1 GET per-Nous top-N, H2 POST numeric, H5 GET /api/v1/operator/relationships/:edge_key/events, H1 GET full-graph — each with privacy-matrix enforced response shape (D-9-06)"
    - "H1 response keys ⊆ {counterparty_did, warmth_bucket, recency_tick, edge_hash}; numeric valence/weight NEVER in H1 (T-09-07)"
    - "H2/H5 emit operator.inspected audit entries (Phase 6 reuse) — zero new allowlist members (D-9-13)"
    - "Graph endpoint includes server-computed {x, y} per node (OQ-5 resolution)"
  artifacts:
    - path: "grid/src/api/operator/relationships.ts"
      provides: "Fastify plugin registering 4 endpoints — H1 GET /:did/relationships, H2 POST /:did/relationships/inspect, H5 GET /api/v1/operator/relationships/:edge_key/events, H1 GET /grid/relationships/graph"
      exports: ["relationshipsRoutes"]
      min_lines: 200
    - path: "grid/src/genesis/launcher.ts"
      provides: "Extended GenesisLauncher with RelationshipListener + RelationshipStorage wiring, rebuildFromChain on bootstrap, snapshot cadence tick-observer"
      contains: "new RelationshipListener(this.audit"
    - path: "grid/test/api/relationships-privacy.test.ts"
      provides: "Privacy matrix: H1/H2/H5/graph response shapes — exact key-set equality (T-09-07 gate)"
  key_links:
    - from: "grid/src/genesis/launcher.ts"
      to: "grid/src/relationships/listener.ts"
      via: "constructor AFTER aggregator"
      pattern: "new RelationshipListener\\(this\\.audit"
    - from: "grid/src/api/operator/relationships.ts"
      to: "grid/src/relationships/listener.ts"
      via: "services.relationships.getTopNFor(...)"
      pattern: "services\\.relationships\\.getTopNFor"
    - from: "grid/src/api/operator/relationships.ts"
      to: "grid/src/api/operator/_validation.ts"
      via: "validateTierBody + tombstoneCheck + DID_REGEX + appendOperatorEvent (Phase 6 clone)"
      pattern: "validateTierBody|tombstoneCheck|DID_REGEX"
---

<objective>
Wire `RelationshipListener` + `RelationshipStorage` into `GenesisLauncher` per D-9-04 (construct AFTER aggregator, same AuditChain) and D-9-03 (100-tick snapshot cadence), and expose four tier-graded Fastify endpoints. The H1 public GET returns bucketed warmth only; H2 POST requires `validateTierBody` + `tombstoneCheck`; H5 is a canonical-edge-keyed GET (`/api/v1/operator/relationships/:edge_key/events?tier=H5&operator_id=...`) per D-9-06 — tier + operator_id arrive as query params (GET has no body). Both H2 and H5 emit `operator.inspected` (Phase 6 reuse, zero allowlist growth per D-9-13). The graph endpoint includes server-computed x/y per OQ-5.

Purpose: REL-04 requires Inspector top-N with p95 <100ms at 10K edges, and H5 operators need per-edge raw dialogue turns. The tier-graded privacy contract (D-9-06) is enforced at RESPONSE SHAPE, not just authorization — the privacy-matrix test closes T-09-07 (plaintext weight leak). Server-side graph layout keeps the client dependency-free.

Output: Wired launcher that rebuilds on boot + snapshots on schedule, four Fastify routes, a construction-order regression test, and a privacy-matrix regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md
@.planning/phases/09-relationship-graph-derived-view/09-PATTERNS.md
@.planning/phases/09-relationship-graph-derived-view/09-UI-SPEC.md
@.planning/phases/09-relationship-graph-derived-view/09-VALIDATION.md

<interfaces>
<!-- Pattern sources (PATTERNS.md) -->

From grid/src/api/operator/memory-query.ts lines 39-144 (exact clone template):
- DID_REGEX check → 400 on invalid
- tombstoneCheck with 410 Gone short-circuit (Phase 8 D-28)
- validateTierBody for H2 body shape; for H5 GET the same validation runs on query params
- appendOperatorEvent closed-tuple emit
- Clamp integer `top` parameter via Number.isInteger + range check

From grid/src/api/operator/_validation.ts:
- DID_REGEX: /^did:noesis:[a-z0-9_\-]+$/i
- validateTierBody(body, 'H2'|'H5') → {tier, operator_id} or throws
- tombstoneCheck(registry, did) throws TombstonedDidError
- appendOperatorEvent(audit, {tier, operator_id, action, target_did, ...}) — closed-tuple shape

From grid/src/genesis/launcher.ts lines 56-60 (existing aggregator wiring; relationships MUST follow AFTER):
```
const dialogueCfg = config.dialogue ?? { windowTicks: 5, minExchanges: 2 };
this.aggregator = new DialogueAggregator(this.audit, dialogueCfg);
// <-- new RelationshipListener goes HERE (D-9-04)
```

From grid/src/relationships/ (Plans 01/02/03):
- RelationshipListener(audit, config); methods: getTopNFor, getEdge, allEdges, rebuildFromChain, reset, size
- RelationshipStorage(pool); methods: snapshot, loadSnapshot, scheduleSnapshot
- DEFAULT_RELATIONSHIP_CONFIG frozen
- canonical.ts exports: `sortedPairKey(didA, didB)`, `canonicalEdge(edge)`, `edgeHash(edge)` — the H5 `:edge_key` path param is the SHA-256 hex prefix of the canonical 6-key serialization of {did_a, did_b, valence, weight, recency_tick, last_event_hash} per D-9-10. Operators obtain edge_key from H1/H2 responses (`edge_hash` / `last_event_hash` field); server reconstructs the edge by looking up an in-memory `Map<edge_key_prefix, Edge>` maintained by the listener, or by scanning `allEdges()` for a matching prefix.

From RESEARCH.md §Graph Layout Algorithm lines 491-525 (computeNodePosition verbatim):
- createHash('sha256').update(did) → 32 bytes
- First 4 bytes → angle in [0, 2π); next 4 → jitter [-J, +J]
- Project onto unit circle at (centerX + r*cos, centerY + r*sin)
- layoutCfg defaults: radius=400, centerX=500, centerY=500, jitterRadius=50

From D-9-06 tier → response shape + route:
- H1: `GET /api/v1/nous/:did/relationships` → {edges: [{counterparty_did, warmth_bucket, recency_tick, edge_hash}]}
- H2: `POST /api/v1/nous/:did/relationships/inspect` → {edges: [{counterparty_did, valence, weight, recency_tick, last_event_hash}]}
- H5: `GET /api/v1/operator/relationships/:edge_key/events?tier=H5&operator_id=...` → {events: [{tick, event_type, payload, entry_hash}]} — raw edge-events per OQ-4 full-scan; edge_key is the canonical D-9-10 hash prefix
- Graph (H1): `GET /api/v1/grid/relationships/graph` → {nodes: [{did, x, y}], edges: [{did_a, did_b, warmth_bucket, edge_hash}]}

From RESEARCH.md Open Questions (answered):
- OQ-1: H1 GET + separate H2 POST endpoint (matches /memory/query POST precedent)
- OQ-3: No pagination on graph endpoint for MVP — accept ~1MB at N=100 Nous
- OQ-4: H5 endpoint does full chain scan (loadEntries().filter()); ~10ms at 100K chain length, acceptable at H5 frequency (<1/day per grid)
- OQ-5: Server-side x/y computation; ship {did, x, y} in response
- OQ-2/OQ-7: Snapshot is fire-and-forget setImmediate; full-table REPLACE INTO every 100 ticks
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend GenesisLauncher — construct listener + storage, rebuildFromChain, snapshot cadence, construction-order test</name>
  <files>
    grid/src/genesis/launcher.ts,
    grid/src/genesis/types.ts,
    grid/test/relationships/listener-launcher-order.test.ts
  </files>
  <read_first>
    - grid/src/genesis/launcher.ts (existing file — read constructor + bootstrap + any tick subscriber in full; ~200 lines)
    - grid/src/genesis/types.ts (GenesisConfig shape — need to add optional `relationship: RelationshipConfig` field)
    - grid/src/clock/* (WorldClock tick subscription — how aggregator or existing observers hook a per-tick callback)
    - grid/src/audit/chain.ts (loadEntries behavior — rebuildFromChain depends on audit chain being loaded BEFORE listener.rebuildFromChain())
    - grid/src/relationships/listener.ts (Plan 02 — constructor + rebuildFromChain signature)
    - grid/src/relationships/storage.ts (Plan 03 — scheduleSnapshot signature)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md D-9-03 (snapshot cadence = 100 ticks), D-9-04 (construction order)
  </read_first>
  <action>
**Step A — GenesisConfig** (`grid/src/genesis/types.ts`): Add optional `relationship?: RelationshipConfig` field. Import type from `../relationships/index.js`.

**Step B — Launcher constructor** (`grid/src/genesis/launcher.ts`):

Add two readonly fields on the class:
```
readonly relationships: RelationshipListener;
private readonly relationshipStorage: RelationshipStorage | null;
```

In the constructor, AFTER `this.aggregator = new DialogueAggregator(...)` (currently line 59), append:
```
// Phase 9 REL-01 (D-9-04): RelationshipListener is constructed AFTER the aggregator
// so its onAppend listener is wired to the same AuditChain instance, matching
// the order the Phase 7 zero-diff regression tests validate. See 09-CONTEXT.md D-9-04.
const relationshipCfg = config.relationship ?? DEFAULT_RELATIONSHIP_CONFIG;
this.relationships = new RelationshipListener(this.audit, relationshipCfg);

// Storage is optional — if no mysql2 pool is present (unit-test launcher), snapshot
// scheduling is a no-op. The audit chain remains the source of truth; losing snapshot
// cadence only loses the fast-boot path.
this.relationshipStorage = this.pool ? new RelationshipStorage(this.pool) : null;
```
(Replace `this.pool` with the actual pool field name used by the existing launcher. If no pool exists on the launcher yet, trace how GridStore gets its pool in Phase 1 and use the same access path; if persistence is behind a flag, gate the storage construction on the same flag.)

**Step C — bootstrap() rebuild call:** After the AuditChain is loaded (persistent-chain restore OR fresh chain) and BEFORE the clock starts ticking, add:
```
// P-9-02: AuditChain.loadEntries() does NOT fire onAppend. Manual replay is required
// to reconstruct the derived view. Must run BEFORE the clock starts — otherwise new
// entries could be double-processed (once by rebuild, once by the live onAppend).
this.relationships.rebuildFromChain();
```
Find the right insertion point by locating `this.clock.start()` or equivalent in bootstrap() and placing the call immediately before.

**Step D — Snapshot cadence observer:** In the existing per-tick subscriber (check WorldClock integration or how the aggregator gets drained each tick), add:
```
// D-9-03: Snapshot every N ticks (default 100). Fire-and-forget per OQ-2 — tick is
// never blocked on MySQL I/O. Missed snapshots are losslessly recoverable via
// rebuildFromChain() on restart.
if (this.relationshipStorage && currentTick > 0 && currentTick % relationshipCfg.snapshotCadenceTicks === 0) {
    this.relationshipStorage.scheduleSnapshot(this.relationships.allEdges(), currentTick);
}
```
If no generic tick-observer hook exists, wire the cadence check into the same path WorldClock uses to invoke aggregator drainage. The scheduling primitive MUST be `currentTick % N === 0` only — no `setInterval`, no wall-clock.

**Step E — Pause/resume semantics:** The existing launcher already calls `this.aggregator.reset()` on pause (around line 178). For relationships, add the OPPOSITE behavior — do NOT reset. Add an explicit comment next to the existing aggregator.reset():
```
// NOTE: No reset of this.relationships on pause. Dialogue windows are pause-bounded
// (D-04 Phase 7); relationship warmth is time-integrated state that survives pauses.
// recency_tick on each edge correctly anchors decay relative to the next resume tick.
```

**Step F — listener-launcher-order.test.ts:** Create `grid/test/relationships/listener-launcher-order.test.ts` asserting D-9-04:

Option A (event-order assertion): Spy on `AuditChain.prototype.onAppend`. Construct a minimal `GenesisLauncher` via the existing test factory. Assert `onAppend.mock.calls.length >= 2` AND the aggregator callback registered before the relationships callback (compare call indices / callback identity).

Option B (simpler — pointer equality): Assert `launcher.aggregator` and `launcher.relationships` both exist and share the same audit chain: `expect(getAuditOf(launcher.aggregator)).toBe(launcher.audit); expect(getAuditOf(launcher.relationships)).toBe(launcher.audit);` — where `getAuditOf` accesses the private `audit` field via test-only helper.

Pick whichever the existing test harness makes cheaper. Both satisfy D-9-04.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis/grid &amp;&amp; npx tsc --noEmit &amp;&amp; npm test -- --run test/relationships/listener-launcher-order.test.ts test/relationships/determinism-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "new RelationshipListener(this.audit" grid/src/genesis/launcher.ts` → 1
    - `grep -c "this.aggregator = new DialogueAggregator" grid/src/genesis/launcher.ts` → 1 (preserved)
    - Line number of `new RelationshipListener` > line number of `new DialogueAggregator` in launcher.ts (D-9-04 order)
    - `grep -c "rebuildFromChain" grid/src/genesis/launcher.ts` → 1 (bootstrap-time)
    - `grep -c "scheduleSnapshot" grid/src/genesis/launcher.ts` → 1 (tick-cadence)
    - `grep -c "snapshotCadenceTicks" grid/src/genesis/launcher.ts` → ≥1
    - `grep -c "this.relationships.reset" grid/src/genesis/launcher.ts` → 0 (relationships survive pauses)
    - Wall-clock count unchanged: `grep -cE "Date\\.now|performance\\.now|setInterval|setTimeout|Math\\.random" grid/src/genesis/launcher.ts` equals baseline count before this task
    - `cd grid && npm test -- --run test/relationships/listener-launcher-order.test.ts` passes
    - `cd grid && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Launcher constructs listener after aggregator, rebuilds on boot, schedules snapshots tick-driven. Pauses don't reset the Map. D-9-04 order is regression-gated.
  </done>
</task>

<task type="auto">
  <name>Task 2: Relationships Fastify plugin — four endpoints with tier-graded privacy (D-9-06)</name>
  <files>
    grid/src/api/operator/relationships.ts,
    grid/src/api/index.ts
  </files>
  <read_first>
    - grid/src/api/operator/memory-query.ts (exact clone — read lines 39-144 in full; copy the tier-validated handler scaffold verbatim)
    - grid/src/api/operator/_validation.ts (DID_REGEX, validateTierBody, tombstoneCheck, appendOperatorEvent, TombstonedDidError)
    - grid/src/api/operator/index.ts (how other operator routes register with Fastify)
    - grid/src/api/index.ts (top-level router — where to register relationshipsRoutes)
    - grid/src/api/server.ts (services object assembly — how memory/registry are plumbed in; mirror for relationships)
    - grid/src/relationships/canonical.ts (warmthBucket, edgeHash, decayedWeight, sortedPairKey, canonicalEdge — needed for H5 `:edge_key` resolution)
    - grid/src/relationships/listener.ts (getTopNFor, getEdge, allEdges)
    - grid/src/audit/chain.ts (loadEntries for H5 scan)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md D-9-06 (tier map), D-9-07 (top-N=5 default, cap 20), D-9-10 (canonical edge serialization → edge_key), D-9-11 (self-loop reject), D-9-13 (zero allowlist growth)
    - .planning/phases/09-relationship-graph-derived-view/09-RESEARCH.md §Code Examples lines 778-824 (tier-validated handler verbatim), §Graph Layout lines 491-525 (computeNodePosition verbatim), §Open Questions OQ-1/3/4/5
    - .planning/phases/09-relationship-graph-derived-view/09-UI-SPEC.md §EdgeEventsModal (H5 consumer contract)
  </read_first>
  <action>
Create `grid/src/api/operator/relationships.ts` exporting Fastify plugin `relationshipsRoutes`. Clone `memory-query.ts` structure verbatim (tier validation, DID check, tombstone check, closed-tuple audit emit); only read-side (`services.relationships` vs `services.memory`) and response shape differ.

**Pure helper (top of file or sibling module):**
```
// RESEARCH.md §Graph Layout Algorithm lines 491-510 verbatim
import { createHash } from 'crypto';

interface LayoutConfig { readonly radius: number; readonly centerX: number; readonly centerY: number; readonly jitterRadius: number; }
interface NodePosition { readonly did: string; readonly x: number; readonly y: number; }

const DEFAULT_LAYOUT: LayoutConfig = { radius: 400, centerX: 500, centerY: 500, jitterRadius: 50 };

function computeNodePosition(did: string, cfg: LayoutConfig = DEFAULT_LAYOUT): NodePosition {
    const hash = createHash('sha256').update(did).digest();
    const angleRaw = hash.readUInt32LE(0) / 0x1_0000_0000;
    const angle = angleRaw * 2 * Math.PI;
    const jitterRaw = hash.readUInt32LE(4) / 0x1_0000_0000;
    const jitter = (jitterRaw - 0.5) * 2 * cfg.jitterRadius;
    const r = cfg.radius + jitter;
    return { did, x: cfg.centerX + r * Math.cos(angle), y: cfg.centerY + r * Math.sin(angle) };
}
```

**Route 1 — `GET /api/v1/nous/:did/relationships` (H1 public, no body):**
1. `if (!DID_REGEX.test(req.params.did)) { reply.code(400); return {error: 'invalid_did'}; }`
2. Wrap `tombstoneCheck(services.registry, did)` in try/catch; on `TombstonedDidError` return 410 with `{error: 'gone', deleted_at_tick: err.deletedAtTick}`.
3. Clamp `top`: `const topRaw = Number(req.query.top ?? services.config.relationship.topNDefault); const top = Number.isInteger(topRaw) && topRaw >= 1 && topRaw <= services.config.relationship.topNMax ? topRaw : services.config.relationship.topNDefault;` (D-9-07)
4. `const currentTick = services.clock.currentTick;`
5. `const raw = services.relationships.getTopNFor(did, top, currentTick);`
6. Return shape (EXACTLY these 4 keys per edge, no numeric valence/weight):
   ```
   return {
       edges: raw.map(e => ({
           counterparty_did: e.counterpartyDid,
           warmth_bucket: warmthBucket(
               decayedWeight({weight: e.weight, recency_tick: e.recency_tick, ...} as Edge, currentTick, services.config.relationship.tau),
               services.config.relationship,
           ),
           recency_tick: e.recency_tick,
           edge_hash: edgeHash(reconstructEdge(did, e)),
       })),
   };
   ```
   where `reconstructEdge(did, topNResult)` builds the Edge `{did_a, did_b, valence, weight, recency_tick, last_event_hash}` from the query'd DID + the top-N row, using `sortedPairKey` to determine did_a/did_b order. This rebuilds the canonical form for hashing.
7. **NO audit emit** — H1 is public read; verify `memory-query.ts` H1 path is also non-emitting.

**Route 2 — `POST /api/v1/nous/:did/relationships/inspect` (H2):**
1. DID_REGEX + tombstoneCheck (same as Route 1).
2. `const { tier, operator_id } = validateTierBody(req.body, 'H2');` — 400 on tier-spoof (T-09-07 layer 1 mitigation).
3. Clamp `top` from `req.body.top` same way.
4. Get `raw = services.relationships.getTopNFor(did, top, currentTick);`.
5. Return H2 shape with numeric valence + decayed-weight:
   ```
   return {
       edges: raw.map(e => ({
           counterparty_did: e.counterpartyDid,
           valence: e.valence,
           weight: decayedWeight({weight: e.weight, recency_tick: e.recency_tick, ...} as Edge, currentTick, services.config.relationship.tau),
           recency_tick: e.recency_tick,
           last_event_hash: e.last_event_hash,
       })),
   };
   ```
6. Emit `operator.inspected` (D-9-13 — Phase 6 reuse, no new allowlist member):
   ```
   appendOperatorEvent(services.audit, {
       tier: 'H2',
       operator_id,
       action: 'inspect_relationships',
       target_did: did,
       tick: currentTick,
   });
   ```
   (Use the exact closed-tuple key set Phase 6 `appendOperatorEvent` accepts; if the helper requires a specific `action` enum and `inspect_relationships` isn't in it, either extend the enum per Phase 6 D-11 rules OR use the closest existing action like `inspect_memory` with a `scope: 'relationships'` field — prefer the enum extension if Phase 6 allows it, but confirm by reading `appendOperatorEvent` signature + its own test.)

**Route 3 — `GET /api/v1/operator/relationships/:edge_key/events` (H5, per D-9-06):**

This is a GET per D-9-06; tier + operator_id arrive as query params because GET has no body. Path param is the canonical edge-key hash prefix per D-9-10.

1. `const edgeKey = req.params.edge_key;` — validate it's a hex-only string of expected length (e.g., `/^[a-f0-9]{16,64}$/i`); 400 on bad format with `{error: 'invalid_edge_key'}`.
2. Extract and validate query params (use a helper mirroring `validateTierBody` but for query):
   ```
   const tier = String(req.query.tier ?? '');
   const operator_id = String(req.query.operator_id ?? '');
   if (tier !== 'H5') { reply.code(400); return {error: 'tier_mismatch'}; }
   if (!operator_id) { reply.code(400); return {error: 'missing_operator_id'}; }
   ```
   If `validateTierBody` can be trivially adapted to accept a `{tier, operator_id}` shape regardless of source (body vs query), prefer reusing it — call `validateTierBody({tier, operator_id}, 'H5')`.
3. **Edge resolution:** Look up the edge by hash prefix. The listener maintains an `allEdges()` iterable; find the edge whose `edgeHash(edge)` starts with `edgeKey` (or compare full hash if `edgeKey` is full length). If no match → 404 `{error: 'edge_not_found'}`.
   ```
   const edge = Array.from(services.relationships.allEdges()).find(
       e => edgeHash(e).startsWith(edgeKey) || edgeHash(e) === edgeKey
   );
   if (!edge) { reply.code(404); return {error: 'edge_not_found'}; }
   const { did_a, did_b } = edge;
   ```
   (Plan 02 MAY expose a direct `getEdgeByKey(edgeKey)` helper on the listener — prefer that if it exists; otherwise the linear scan is acceptable at H5 operator frequency <1/day per grid per OQ-4.)
4. Tombstone-check BOTH resolved DIDs (`did_a` and `did_b`). Self-loop cannot occur because canonical edges enforce `did_a < did_b`, but defensively assert `did_a !== did_b`.
5. Full audit chain scan (OQ-4 approved; <1/day operator usage, ~10ms at 100K entries):
   ```
   const entries = services.audit.loadEntries();
   const events = entries
       .filter(entry => involvesEdge(entry, did_a, did_b))
       .map(entry => ({
           tick: entry.payload.tick,
           event_type: entry.eventType,
           payload: entry.payload,  // H5 sees raw payload per D-9-06
           entry_hash: entry.eventHash,
       }));
   return { edge_key: edgeKey, events };
   ```
   Where `involvesEdge(entry, didA, didB)` decodes the same four event types the listener handles (`nous.spoke` → `from_did/to_did`, `trade.settled` → participants, `trade.reviewed` → reviewer/subject, `telos.refined` → nous/partner) and returns true iff the event links the two DIDs. Extract this decoder as a shared helper (it already exists conceptually in listener.handleEntry — hoist to `grid/src/relationships/event-decoder.ts` if cleaner, or inline here).
6. Emit `operator.inspected` with `tier: 'H5', action: 'inspect_edge_events', target_did: did_a, counterparty_did: did_b` closed tuple (respect Phase 6 payload shape; extend allowed closed tuple per D-11 if needed).

**Route 4 — `GET /api/v1/grid/relationships/graph` (H1 full-graph):**
1. Optional query `minWarmth` in {`cold`, `warm`, `hot`}; default `cold` (returns all).
2. Iterate `services.relationships.allEdges()`; filter by `warmthBucket(decayedWeight(edge, currentTick, tau), cfg) >= minWarmth` (bucket comparison: cold < warm < hot).
3. Collect unique DIDs across filtered edges.
4. Compute `nodes[]` once per DID via `computeNodePosition(did)`.
5. Return:
   ```
   return {
       nodes: nodes.map(n => ({did: n.did, x: n.x, y: n.y})),
       edges: filtered.map(e => ({
           did_a: e.did_a,
           did_b: e.did_b,
           warmth_bucket: warmthBucket(...),
           edge_hash: edgeHash(e),
       })),
   };
   ```
6. **NO audit emit**, **NO valence/weight in response** — graph is H1 tier.

**Registration:** In `grid/src/api/index.ts`:
```
import { relationshipsRoutes } from './operator/relationships.js';
// ...
app.register(relationshipsRoutes, { prefix: '/api/v1' });
```
Mirror the existing `memoryQueryRoutes` registration.

**Services shape:** In `grid/src/api/server.ts` (or wherever the services object is assembled), add:
```
services.relationships = launcher.relationships;
services.config = { ...services.config, relationship: config.relationship ?? DEFAULT_RELATIONSHIP_CONFIG };
```
Trace the existing `services.memory` wiring and mirror the pattern.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis/grid &amp;&amp; npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export.*relationshipsRoutes" grid/src/api/operator/relationships.ts` → 1
    - `grep -c "DID_REGEX" grid/src/api/operator/relationships.ts` → ≥1 (H1 + H2 at minimum; H5 validates edge_key format instead of dual DIDs)
    - `grep -c "tombstoneCheck" grid/src/api/operator/relationships.ts` → ≥2 (H1, H2, and H5 post-resolution)
    - `grep -c "validateTierBody\\|tier === 'H5'\\|tier !== 'H5'" grid/src/api/operator/relationships.ts` → ≥2 (H2 body + H5 query)
    - `grep -c "appendOperatorEvent" grid/src/api/operator/relationships.ts` → 2 (H2 + H5 — no emit for H1 or graph)
    - `grep -c "computeNodePosition" grid/src/api/operator/relationships.ts` → ≥1
    - `grep -c "createHash('sha256')" grid/src/api/operator/relationships.ts` → ≥1 (layout seed)
    - `grep -cE "/api/v1/operator/relationships/:edge_key/events|/operator/relationships/:edge_key" grid/src/api/operator/relationships.ts` → ≥1 (D-9-06 H5 route literal)
    - `grep -c "POST.*relationships/.*/events" grid/src/api/operator/relationships.ts` → 0 (old POST form must not appear)
    - H1 endpoint response shape grep: `grep -cE "counterparty_did:.*warmth_bucket:.*recency_tick:.*edge_hash:" grid/src/api/operator/relationships.ts` → ≥1 (all four H1 keys together); `grep -cE "valence:.*weight:" grid/src/api/operator/relationships.ts` → ≥1 BUT verify those occurrences are in H2 route only (privacy-matrix test in Task 3 is the authoritative check)
    - `grep -c "relationshipsRoutes" grid/src/api/index.ts` → 1 (registration)
    - `grep -c "this.audit.append" grid/src/api/operator/relationships.ts` → 0 (pure — emissions only via appendOperatorEvent, which Phase 6 closed-tuple gate already verifies)
    - `cd grid && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Four endpoints registered and compile. H1 shape has exactly 4 keys. H2 adds numeric. H5 is GET keyed by canonical edge-hash prefix (D-9-06 + D-9-10), tier/operator_id in query params. Graph returns nodes+edges with server-computed positions. H2/H5 emit via Phase 6 operator.inspected (zero new allowlist members). Full TypeScript build green.
  </done>
</task>

<task type="auto">
  <name>Task 3: relationships-privacy.test.ts — response-shape key-set assertions (T-09-07 gate)</name>
  <files>grid/test/api/relationships-privacy.test.ts</files>
  <read_first>
    - grid/test/audit/operator-payload-privacy.test.ts (exact clone template per PATTERNS.md — privacy-matrix shape)
    - grid/test/api/ (any existing Fastify integration test — how to construct `app` + inject requests)
    - grid/src/api/operator/relationships.ts (Task 2 — the four routes under test)
    - grid/src/relationships/canonical.ts (edgeHash — test needs to derive the :edge_key path param from a seeded edge)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md D-9-06 (authoritative tier → response-shape table), D-9-10 (canonical edge_key)
    - .planning/phases/09-relationship-graph-derived-view/09-RESEARCH.md §Common Pitfalls T-09-07 lines 704-712 (the exact invariant being gated)
  </read_first>
  <action>
Create `grid/test/api/relationships-privacy.test.ts`. Clone `operator-payload-privacy.test.ts` structure. Use Fastify's `app.inject(...)` to hit each route without a real socket.

**Fixture setup (once per describe):**
- Construct a launcher + listener; seed 3 DIDs with `nous.spoke` + `trade.settled` events to create ≥2 edges.
- Build a Fastify app; register `relationshipsRoutes` + services object with `relationships: launcher.relationships`, `audit`, `registry`, `clock`, `config.relationship`.
- Resolve an `edge_key` for the H5 tests by picking one edge from `launcher.relationships.allEdges()` and computing `edgeHash(edge)`. Store as `EDGE_KEY` constant for the H5 test.

**Test 1 — H1 GET response shape:**
```
const res = await app.inject({ method: 'GET', url: `/api/v1/nous/${DID_A}/relationships?top=5` });
expect(res.statusCode).toBe(200);
const body = res.json();
expect(body).toHaveProperty('edges');
for (const edge of body.edges) {
    // EXACT key set — no extras tolerated (T-09-07).
    expect(new Set(Object.keys(edge))).toEqual(new Set(['counterparty_did', 'warmth_bucket', 'recency_tick', 'edge_hash']));
    expect(typeof edge.warmth_bucket).toBe('string');
    expect(['cold', 'warm', 'hot']).toContain(edge.warmth_bucket);
    // Plaintext-leak guard.
    expect(edge).not.toHaveProperty('valence');
    expect(edge).not.toHaveProperty('weight');
}
```

**Test 2 — H1 GET rejects invalid DID:**
- URL `/api/v1/nous/not-a-did/relationships` → 400 with `{error: 'invalid_did'}`.

**Test 3 — H1 GET on tombstoned DID → 410:**
- Tombstone DID_A via Phase 8 pathway.
- Request → 410 `{error: 'gone', deleted_at_tick: N}`.

**Test 4 — H1 top clamp:**
- `?top=9999` → still returns ≤ `topNMax` edges (default 20).
- `?top=abc` → falls back to `topNDefault` (5).
- `?top=-1` → falls back to default.

**Test 5 — H2 POST response shape:**
```
const res = await app.inject({
    method: 'POST',
    url: `/api/v1/nous/${DID_A}/relationships/inspect`,
    payload: { tier: 'H2', operator_id: 'op-test-001', top: 5 },
});
expect(res.statusCode).toBe(200);
const body = res.json();
for (const edge of body.edges) {
    expect(new Set(Object.keys(edge))).toEqual(new Set(['counterparty_did', 'valence', 'weight', 'recency_tick', 'last_event_hash']));
    expect(typeof edge.valence).toBe('number');
    expect(edge.valence).toBeGreaterThanOrEqual(-1);
    expect(edge.valence).toBeLessThanOrEqual(1);
    expect(edge.weight).toBeGreaterThanOrEqual(0);
    expect(edge.weight).toBeLessThanOrEqual(1);
}
```

**Test 6 — H2 emits operator.inspected (D-9-13 zero allowlist growth):**
- Record `chain.length` before request; POST H2; assert `chain.length === before + 1`; assert newest entry has `eventType === 'operator.inspected'` and payload tier H2 + action referencing relationships.

**Test 7 — H2 tier spoof 400:**
- POST with `{tier: 'H5', operator_id: 'op-x'}` to the H2 route — `validateTierBody` rejects the tier mismatch → 400.

**Test 8 — H5 GET events shape (D-9-06 canonical route + D-9-10 edge_key):**
```
const res = await app.inject({
    method: 'GET',
    url: `/api/v1/operator/relationships/${EDGE_KEY}/events?tier=H5&operator_id=op-test-001`,
});
expect(res.statusCode).toBe(200);
const body = res.json();
expect(body).toHaveProperty('events');
expect(body).toHaveProperty('edge_key');
for (const ev of body.events) {
    expect(new Set(Object.keys(ev))).toEqual(new Set(['tick', 'event_type', 'payload', 'entry_hash']));
}
```

**Test 9 — H5 unknown edge_key → 404:**
- GET with a valid-format but unknown edge_key (e.g., `'0'.repeat(16)`) → 404 `{error: 'edge_not_found'}`.

**Test 10 — H5 emits operator.inspected:**
- Same pattern as Test 6, payload has tier H5 + action inspect_edge_events + counterparty_did field.

**Test 11 — H5 tier query-param mismatch → 400:**
- GET `?tier=H2&operator_id=...` on the H5 route → 400 `{error: 'tier_mismatch'}` (or whatever `validateTierBody` returns).

**Test 12 — Graph GET response shape:**
```
const res = await app.inject({ method: 'GET', url: `/api/v1/grid/relationships/graph` });
expect(res.statusCode).toBe(200);
const body = res.json();
expect(body).toHaveProperty('nodes');
expect(body).toHaveProperty('edges');
for (const node of body.nodes) {
    expect(new Set(Object.keys(node))).toEqual(new Set(['did', 'x', 'y']));
    expect(typeof node.x).toBe('number');
    expect(typeof node.y).toBe('number');
}
for (const edge of body.edges) {
    expect(new Set(Object.keys(edge))).toEqual(new Set(['did_a', 'did_b', 'warmth_bucket', 'edge_hash']));
    expect(edge).not.toHaveProperty('valence');
    expect(edge).not.toHaveProperty('weight');
}
```

**Test 13 — Graph layout determinism (OQ-5 + REL-04):**
- Two calls to graph endpoint against identical state return byte-identical `nodes` array (same DID → same x,y).

**Test 14 — Full privacy matrix (Phase 6-style assertion):**
- Iterate every edge in every response; NO path contains both numeric `valence` AND `weight` except the H2 route. NO path contains raw `payload` except H5.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis/grid &amp;&amp; npm test -- --run test/api/relationships-privacy.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm test -- --run test/api/relationships-privacy.test.ts` passes with ≥14 test cases across H1/H2/H5/graph
    - `grep -c "new Set(Object.keys" grid/test/api/relationships-privacy.test.ts` → ≥4 (exact key-set equality per tier)
    - `grep -c "not.toHaveProperty('valence'" grid/test/api/relationships-privacy.test.ts` → ≥2 (H1 + graph)
    - `grep -c "not.toHaveProperty('weight'" grid/test/api/relationships-privacy.test.ts` → ≥2
    - `grep -c "operator.inspected" grid/test/api/relationships-privacy.test.ts` → ≥2 (H2 + H5 audit-emit assertions)
    - `grep -cE "method: 'GET'.*operator/relationships/.+/events|/api/v1/operator/relationships/" grid/test/api/relationships-privacy.test.ts` → ≥1 (H5 uses canonical D-9-06 GET route)
    - `grep -c "edge_not_found" grid/test/api/relationships-privacy.test.ts` → 1
    - Test uses `app.inject` (Fastify test mode) — `grep -c "app.inject" grid/test/api/relationships-privacy.test.ts` → ≥10
    - No `.skip` or `.todo`
  </acceptance_criteria>
  <done>
    Privacy matrix is regression-gated at response-shape level. T-09-07 (plaintext weight leak) cannot happen without this test failing. D-9-06 H5 GET route is shape-tested. D-9-13 (zero allowlist growth) is verified — H2/H5 route emits are all operator.inspected, never a new event type.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → H1 GET | No body; untrusted query params (`top`, `minWarmth`) — clamped + validated |
| Client → H2 POST | Untrusted body (`tier`, `operator_id`) — validateTierBody enforces strict equality and shape |
| Client → H5 GET | Untrusted query params (`tier`, `operator_id`) + untrusted path param (`edge_key`) — validated for hex-format and strict tier equality |
| Grid → response | Response shape is the privacy boundary — H1 NEVER contains numeric valence/weight |
| Launcher → listener subscription order | Aggregator MUST register before relationships (D-9-04) — regression-gated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-07 | Information Disclosure | H1 response shape | mitigate | relationships-privacy.test.ts Test 1 + Test 12 assert exact key set; no valence/weight leak |
| T-09-14 | Elevation of Privilege | tier spoof via body or query | mitigate | validateTierBody strict equality (Phase 6 pattern); Tests 7 + 11 cover body + query paths |
| T-09-15 | Spoofing | DID forgery in params | mitigate | DID_REGEX at route entry; 400 on invalid; H5 edge_key hex-format check |
| T-09-16 | Denial of Service | H5 full chain scan at 100K entries | accept | ~10ms cost; H5 operator frequency <1/day per grid per SUMMARY.md; weekly CI load test in Plan 06 |
| T-09-17 | Information Disclosure | operator.inspected payload leaks metadata | mitigate | Closed-tuple shape inherited from Phase 6 D-11; payload privacy test (Phase 6) still green |
| T-09-08 | Tampering | canonical edge enforces did_a < did_b; self-loops cannot reach H5 by construction | mitigate | D-9-11 at listener level + defensive `did_a !== did_b` in Route 3 |
| T-09-13 | Tampering | listener registered BEFORE aggregator → Phase 7 zero-diff invariant breaks | mitigate | listener-launcher-order.test.ts Task 1 regression |
</threat_model>

<verification>
Full scoped suite:
```
cd grid && npm test -- --run test/
```
Expected: all Wave 0 + Wave 1 + Wave 2 tests green; full grid suite still green.

TypeScript:
```
cd grid && npx tsc --noEmit
```
Expected: 0 errors.

Manual smoke (after execute-phase):
```
curl http://localhost:3001/api/v1/nous/did:noesis:a/relationships?top=5
curl -X POST http://localhost:3001/api/v1/nous/did:noesis:a/relationships/inspect \
    -H 'Content-Type: application/json' \
    -d '{"tier":"H2","operator_id":"op-smoke","top":5}'
curl "http://localhost:3001/api/v1/operator/relationships/${EDGE_KEY}/events?tier=H5&operator_id=op-smoke"
curl http://localhost:3001/api/v1/grid/relationships/graph
```
Expected: valid JSON responses matching D-9-06 shapes.
</verification>

<success_criteria>
- Launcher wires listener + storage in correct order; rebuildFromChain on boot; tick-driven snapshot cadence.
- Four endpoints exist and compile — H5 follows D-9-06 canonical `GET /api/v1/operator/relationships/:edge_key/events?tier=H5&operator_id=...` shape.
- Privacy matrix enforces exact key sets per tier.
- D-9-04, D-9-06, D-9-07, D-9-10, D-9-11, D-9-13 all regression-gated.
- No new allowlist members — H2/H5 both emit operator.inspected.
</success_criteria>

<output>
After completion, create `.planning/phases/09-relationship-graph-derived-view/09-04-SUMMARY.md` per template — include endpoint smoke curl outputs (H1/H2/H5/graph), listener-launcher-order test evidence, and privacy-matrix test evidence.
</output>
</output>
