---
phase: 09-relationship-graph-derived-view
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - grid/src/relationships/types.ts
  - grid/src/relationships/config.ts
  - grid/src/relationships/canonical.ts
  - grid/src/relationships/index.ts
  - grid/test/relationships/canonical.test.ts
  - grid/test/relationships/self-edge-rejection.test.ts
  - grid/test/relationships/determinism-source.test.ts
  - sql/009_relationships.sql
  - dashboard/package.json
  - dashboard/package-lock.json
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
tags:
  - phase-09
  - relationships
  - scaffolding
  - wave-0
must_haves:
  truths:
    - "Edge type, RelationshipConfig, and WarmthBucket shapes exist and compile"
    - "canonicalEdge(), edgeHash(), decayedWeight(), warmthBucket(), sortedPairKey() are pure and unit-tested"
    - "Self-loop is silently rejected at canonical layer (D-9-11)"
    - "MySQL migration sql/009_relationships.sql is well-formed and idempotent (CREATE TABLE IF NOT EXISTS)"
    - "swr is installed in dashboard (T-09-11 mitigation prerequisite)"
    - "Grep gate forbidding Date.now/performance.now/setInterval/setTimeout/Math.random in grid/src/relationships/** passes (D-9-12)"
  artifacts:
    - path: "grid/src/relationships/types.ts"
      provides: "Edge, RelationshipConfig, WarmthBucket, SpokeObservation, RelationshipBumpEvent types"
      exports: ["Edge", "RelationshipConfig", "WarmthBucket"]
    - path: "grid/src/relationships/config.ts"
      provides: "DEFAULT_RELATIONSHIP_CONFIG frozen constant with τ=1000, warmthColdMax=0.20, warmthWarmMax=0.60, snapshotCadenceTicks=100, topNDefault=5, topNMax=20"
      contains: "Object.freeze"
    - path: "grid/src/relationships/canonical.ts"
      provides: "canonicalEdge, edgeHash, decayedWeight, warmthBucket, sortedPairKey pure functions"
      exports: ["canonicalEdge", "edgeHash", "decayedWeight", "warmthBucket", "sortedPairKey"]
    - path: "sql/009_relationships.sql"
      provides: "relationships derived table (D-9-03 persistent snapshot target)"
      contains: "CREATE TABLE IF NOT EXISTS relationships"
  key_links:
    - from: "grid/src/relationships/canonical.ts"
      to: "grid/src/relationships/types.ts"
      via: "import type { Edge, RelationshipConfig, WarmthBucket }"
      pattern: "import\\s+type\\s+\\{[^}]*Edge[^}]*\\}\\s+from\\s+['\\\"]\\./types"
    - from: "grid/src/relationships/index.ts"
      to: "grid/src/relationships/canonical.ts"
      via: "re-export"
      pattern: "export\\s+\\{[^}]*canonicalEdge[^}]*\\}"
---

<objective>
Scaffold the `grid/src/relationships/` module and `grid/test/relationships/` test directory with pure primitives: types, frozen config, canonical serialization/hash/decay/bucket helpers, the sole-producer canonical-layer self-loop guard, and the SQL migration that Wave 1 storage will target. Also install `swr` in the dashboard (prerequisite for Wave 3 `useRelationships`) and lock the D-9-12 wall-clock grep gate from day one.

Purpose: Without these primitives, Wave 1 (listener, storage) has no type surface to compile against, and Wave 3 has no data-fetching library. Encoding the D-9-11 self-loop rejection at the canonical layer (not just the listener) guarantees any future helper that constructs an edge inherits the invariant. The grep gate landing in Wave 0 means every subsequent task runs under it — no retrofitting.

Output: Compilable grid/src/relationships/ module exposing types + canonical helpers, a migration file that matches `sql/0NN_name.sql` convention, an installed swr dependency, and three regression tests (canonical round-trip, self-loop rejection, determinism-source grep gate) that lock the D-9-10/D-9-11/D-9-12 invariants.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md
@.planning/phases/09-relationship-graph-derived-view/09-PATTERNS.md
@.planning/phases/09-relationship-graph-derived-view/09-UI-SPEC.md
@.planning/phases/09-relationship-graph-derived-view/09-VALIDATION.md

<interfaces>
<!-- Pattern sources (PATTERNS.md §clone-template-map) that Wave 0 tasks MUST clone verbatim before adapting. -->

From grid/src/dialogue/types.ts (exact analog for types.ts shape):
```typescript
// Read this file before writing grid/src/relationships/types.ts.
// Shape conventions: readonly fields, branded types where applicable,
// explicit string-literal unions for enums (no TypeScript enum).
```

From grid/src/dialogue/dialogue-id.ts (exact analog for canonical.ts shape — pure crypto helpers, no side effects):
```typescript
// Read this file before writing grid/src/relationships/canonical.ts.
// Pattern: import { createHash } from 'crypto'; export pure functions that
// take primitive inputs and return hex strings or serialized JSON.
```

From grid/src/audit/broadcast-allowlist.ts (enforcement pattern for Object.freeze):
```typescript
// Read before writing grid/src/relationships/config.ts.
// Pattern: `const X = Object.freeze({ ... });` — runtime-immutable + TS `as const`.
```

From sql/005_audit.sql (column-type convention: `BIGINT UNSIGNED` for tick):
```sql
-- Read first 20 lines to confirm assumption A1 from CONTEXT.md:
-- sql/001_audit.sql was renumbered to sql/005_audit.sql; tick columns use BIGINT UNSIGNED.
-- If this assumption fails, match the existing type in sql/005_audit.sql.
```

From CONTEXT.md `<specifics>` block:
```typescript
// The RelationshipConfig interface is fully specified (13 keys). Copy verbatim:
//   tau: number                       // τ=1000 (D-9-01)
//   bumpSpokeValence: number          // +0.01 (D-9-02)
//   bumpSpokeWeight: number           // +0.02
//   bumpTradeSettledValence: number   // +0.10
//   bumpTradeSettledWeight: number    // +0.10
//   bumpTradeRejectedValence: number  // -0.10
//   bumpTradeRejectedWeight: number   // +0.05 (rejected still counts as contact)
//   bumpTelosRefinedValence: number   // +0.05
//   bumpTelosRefinedWeight: number    // +0.05
//   warmthColdMax: number             // 0.20 (D-9-06)
//   warmthWarmMax: number             // 0.60
//   snapshotCadenceTicks: number      // 100 (D-9-03)
//   topNDefault: number               // 5 (D-9-07)
//   topNMax: number                   // 20
```

From RESEARCH.md §Code Examples (canonical.ts verbatim scaffold, lines 829-866):
```typescript
// This block is the reference implementation. Copy it into canonical.ts unchanged
// except for filename/comment adjustment. Do not re-derive the logic.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types, config, canonical primitives (D-9-01, D-9-02, D-9-06, D-9-10, D-9-11)</name>
  <files>
    grid/src/relationships/types.ts,
    grid/src/relationships/config.ts,
    grid/src/relationships/canonical.ts,
    grid/src/relationships/index.ts
  </files>
  <read_first>
    - grid/src/relationships/types.ts (target; must not exist yet — Wave 0 creates it)
    - grid/src/dialogue/types.ts (analog; PATTERNS.md §types.ts exact clone)
    - grid/src/dialogue/dialogue-id.ts (analog; PATTERNS.md §canonical.ts exact clone)
    - grid/src/audit/broadcast-allowlist.ts (Object.freeze pattern for config.ts)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md `<specifics>` (13-key RelationshipConfig interface)
    - .planning/phases/09-relationship-graph-derived-view/09-RESEARCH.md §Code Examples lines 829-866 (canonical.ts verbatim reference)
  </read_first>
  <action>
Create the four primitive files. No `import` from anything outside `grid/src/relationships/**` or Node stdlib.

1. **`grid/src/relationships/types.ts`** — Export these types:
   - `type WarmthBucket = 'cold' | 'warm' | 'hot'`
   - `interface Edge { readonly did_a: string; readonly did_b: string; valence: number; weight: number; recency_tick: number; last_event_hash: string; }` — `did_a &lt; did_b` always (sorted by caller via `sortedPairKey`).
   - `interface RelationshipConfig { readonly tau: number; readonly bumpSpokeValence: number; readonly bumpSpokeWeight: number; readonly bumpTradeSettledValence: number; readonly bumpTradeSettledWeight: number; readonly bumpTradeRejectedValence: number; readonly bumpTradeRejectedWeight: number; readonly bumpTelosRefinedValence: number; readonly bumpTelosRefinedWeight: number; readonly warmthColdMax: number; readonly warmthWarmMax: number; readonly snapshotCadenceTicks: number; readonly topNDefault: number; readonly topNMax: number; }` — all 14 keys `readonly`. Match CONTEXT.md `<specifics>` block exactly.
   - `interface SpokeObservation { readonly did_a: string; readonly did_b: string; readonly tick: number; readonly entryHash: string; readonly bumpKind: 'nous.spoke' | 'trade.settled' | 'trade.reviewed.rejected' | 'telos.refined'; }` — normalized observation after handleEntry decoding.

2. **`grid/src/relationships/config.ts`** — Export `DEFAULT_RELATIONSHIP_CONFIG: RelationshipConfig` via `Object.freeze({ ... }) as const`. Values per D-9-01 / D-9-02 / D-9-03 / D-9-06 / D-9-07:
   ```
   tau: 1000, bumpSpokeValence: 0.01, bumpSpokeWeight: 0.02,
   bumpTradeSettledValence: 0.10, bumpTradeSettledWeight: 0.10,
   bumpTradeRejectedValence: -0.10, bumpTradeRejectedWeight: 0.05,
   bumpTelosRefinedValence: 0.05, bumpTelosRefinedWeight: 0.05,
   warmthColdMax: 0.20, warmthWarmMax: 0.60,
   snapshotCadenceTicks: 100, topNDefault: 5, topNMax: 20
   ```

3. **`grid/src/relationships/canonical.ts`** — Copy RESEARCH.md §Code Examples lines 829-866 VERBATIM (no re-derivation). Exports: `canonicalEdge(edge: Edge): string` using `.toFixed(3)` on `valence` and `weight` (D-9-10 six-key canonical order: `did_a, did_b, valence, weight, recency_tick, last_event_hash`), `edgeHash(edge: Edge): string` via `createHash('sha256').update(canonicalEdge(edge)).digest('hex')`, `decayedWeight(edge: Edge, currentTick: number, tau: number): number` per REL-03 formula with `if (currentTick &lt;= edge.recency_tick) return edge.weight` guard, `warmthBucket(weight: number, config: RelationshipConfig): WarmthBucket` using `config.warmthColdMax` / `config.warmthWarmMax`, `sortedPairKey(didA: string, didB: string): string` that THROWS `new Error('self-loop rejected')` when `didA === didB` (D-9-11).

4. **`grid/src/relationships/index.ts`** — Barrel export matching `grid/src/dialogue/index.ts`: `export * from './types.js'; export * from './config.js'; export * from './canonical.js';` (NOTE: `.js` extensions required — grid uses NodeNext module resolution per Phase 1 convention; verify against `grid/src/dialogue/index.ts` before writing).

Use Node's built-in `crypto` module only. No new npm deps. No `import { performance }`, no `Date.now`, no `setInterval` anywhere.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis/grid && npx tsc --noEmit -p tsconfig.json 2>&amp;1 | grep -E 'relationships/' || echo "relationships module compiles clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "export type WarmthBucket" grid/src/relationships/types.ts` → 1
    - `grep -c "export interface Edge" grid/src/relationships/types.ts` → 1
    - `grep -c "export interface RelationshipConfig" grid/src/relationships/types.ts` → 1
    - `grep -c "Object.freeze" grid/src/relationships/config.ts` → ≥1
    - `grep -c "tau: 1000" grid/src/relationships/config.ts` → 1
    - `grep -c "snapshotCadenceTicks: 100" grid/src/relationships/config.ts` → 1
    - `grep -c "topNDefault: 5" grid/src/relationships/config.ts` → 1
    - `grep -c "topNMax: 20" grid/src/relationships/config.ts` → 1
    - `grep -c "\.toFixed(3)" grid/src/relationships/canonical.ts` → ≥2
    - `grep -c "Math.exp(-delta / tau)" grid/src/relationships/canonical.ts` → 1
    - `grep -c "self-loop rejected" grid/src/relationships/canonical.ts` → 1
    - `grep -c "sortedPairKey" grid/src/relationships/index.ts` → 1 (via re-export)
    - No `Date.now|performance.now|setInterval|setTimeout|Math.random` in `grid/src/relationships/**` (verified by Task 3)
    - `cd grid && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>
    Four files exist and compile under grid's existing tsconfig. All exports are type-safe. Canonical primitives work as pure functions with no I/O. Self-loop throws at sortedPairKey boundary. Barrel export re-exports all three submodules.
  </done>
</task>

<task type="auto">
  <name>Task 2: MySQL migration sql/009_relationships.sql + swr install</name>
  <files>
    sql/009_relationships.sql,
    dashboard/package.json,
    dashboard/package-lock.json
  </files>
  <read_first>
    - sql/005_audit.sql (convention anchor — verify `BIGINT UNSIGNED` column type for tick; Assumption A1 from CONTEXT.md)
    - sql/008_economy.sql (most recent migration — verify file-naming + CREATE TABLE IF NOT EXISTS idiom + ENGINE=InnoDB default)
    - .planning/phases/09-relationship-graph-derived-view/09-RESEARCH.md §MySQL Schema Draft lines 411-464 (verbatim SQL source)
    - dashboard/package.json (verify `swr` is NOT currently listed — Wave 0 adds it per PATTERNS.md §Reality Correction)
  </read_first>
  <action>
**Step A — Migration file.** Create `sql/009_relationships.sql` by copying RESEARCH.md §MySQL Schema Draft lines 415-456 verbatim (the `CREATE TABLE IF NOT EXISTS relationships (...)` block + column comments). Before writing, verify against `sql/005_audit.sql` line 4-6 that tick columns use `BIGINT UNSIGNED` — if the existing migration uses `BIGINT SIGNED`, match it and update the comment. Columns must include:

- `edge_key VARCHAR(160) NOT NULL PRIMARY KEY` (sorted DID pair joined by `|`, D-9-10 canonical key)
- `did_a VARCHAR(80) NOT NULL` (lexicographically smaller)
- `did_b VARCHAR(80) NOT NULL` (lexicographically larger)
- `valence DECIMAL(4,3) NOT NULL DEFAULT 0.000` (3-decimal fixed-point, matches `toFixed(3)`)
- `weight DECIMAL(4,3) NOT NULL DEFAULT 0.000` (pre-decay snapshot value)
- `recency_tick BIGINT UNSIGNED NOT NULL` (for lazy-decay math at read time)
- `last_event_hash CHAR(64) NOT NULL` (SHA-256 hex per-edge anchor)
- `snapshot_tick BIGINT UNSIGNED NOT NULL` (rebuild-verification anchor)
- `INDEX idx_did_a (did_a)`, `INDEX idx_did_b (did_b)`, `INDEX idx_snapshot_tick (snapshot_tick)`
- `ENGINE=InnoDB DEFAULT CHARSET=utf8mb4`

File header comment: `-- sql/009_relationships.sql -- Phase 9 — derived relationship edges (pure-observer snapshot of audit chain) -- Follows 0NN_name.sql convention from 001_domains.sql .. 008_economy.sql.`

**Step B — Install swr in dashboard.** Run `cd dashboard && npm install swr@^2.2.0` (latest stable ^2). Do NOT pin a specific sub-version — use caret range to match Phase 6 dep conventions. After install, verify `dashboard/package.json` has `"swr": "^2.2.0"` (or similar ^2.x) in `dependencies` (not devDependencies). Commit both `package.json` and `package-lock.json` deltas.

Rationale for swr install now (Wave 0 vs Wave 3): the T-09-11 N+1 mitigation is blocking for Wave 3's `useRelationships` hook. Landing the dep in Wave 0 means Waves 1-3 all see a consistent lockfile.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis &amp;&amp; grep -c "CREATE TABLE IF NOT EXISTS relationships" sql/009_relationships.sql &amp;&amp; grep -c '"swr"' dashboard/package.json</automated>
  </verify>
  <acceptance_criteria>
    - `test -f sql/009_relationships.sql` exits 0
    - `grep -c "CREATE TABLE IF NOT EXISTS relationships" sql/009_relationships.sql` → 1
    - `grep -c "DECIMAL(4,3)" sql/009_relationships.sql` → ≥2 (valence + weight)
    - `grep -c "CHAR(64)" sql/009_relationships.sql` → 1 (last_event_hash)
    - `grep -c "BIGINT UNSIGNED" sql/009_relationships.sql` → ≥2 (recency_tick + snapshot_tick)
    - `grep -c "INDEX idx_" sql/009_relationships.sql` → 3
    - `grep -c "ENGINE=InnoDB" sql/009_relationships.sql` → 1
    - `grep -cE '"swr":\s*"\^2' dashboard/package.json` → 1
    - `test -f dashboard/package-lock.json` exits 0 and `grep -c '"swr"' dashboard/package-lock.json` → ≥1
  </acceptance_criteria>
  <done>
    Migration file matches `sql/0NN_name.sql` naming convention and can be applied via whatever migration runner Phase 1 established. swr is installed as a dashboard runtime dependency. package-lock.json is updated in-tree.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wave 0 regression tests — canonical round-trip, self-loop rejection, wall-clock grep gate (D-9-10, D-9-11, D-9-12)</name>
  <files>
    grid/test/relationships/canonical.test.ts,
    grid/test/relationships/self-edge-rejection.test.ts,
    grid/test/relationships/determinism-source.test.ts
  </files>
  <read_first>
    - grid/test/dialogue/dialogue-id.test.ts (analog for canonical.test.ts — unit test style)
    - grid/test/dialogue/boundary.test.ts OR grid/test/audit/*producer-boundary*.test.ts (analog for determinism-source.test.ts — grep-walking test shape)
    - .planning/phases/09-relationship-graph-derived-view/09-RESEARCH.md §Producer-Boundary Grep Gate Regex lines 527-582 (the `walk()` + readdirSync/readFileSync pattern)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md D-9-11 (self-loop silent-reject semantics: listener drops, canonical.sortedPairKey throws — the listener catches)
    - .planning/phases/09-relationship-graph-derived-view/09-CONTEXT.md D-9-12 (wall-clock ban scope: `grid/src/relationships/**`)
  </read_first>
  <action>
Create three vitest 2.x tests under `grid/test/relationships/` (no watch flag — every invocation `--run`).

1. **`canonical.test.ts`** — Unit tests for `canonical.ts` exports:
   - **canonicalEdge determinism**: Given `Edge { did_a: 'did:noesis:a', did_b: 'did:noesis:b', valence: 0.12345, weight: 0.98765, recency_tick: 42, last_event_hash: 'f0'.repeat(32) }`, assert `canonicalEdge(edge)` equals the JSON string `{"did_a":"did:noesis:a","did_b":"did:noesis:b","valence":"0.123","weight":"0.988","recency_tick":42,"last_event_hash":"f0f0...f0"}` (key order locked per D-9-10).
   - **edgeHash stability**: Same edge twice → same hex. Different edge → different hex.
   - **decayedWeight formula**: Edge `{weight: 1.0, recency_tick: 0, ...}` at `currentTick=1000, tau=1000` → result approximately `0.367879` (Math.exp(-1)); tolerate `±1e-6`. At `currentTick=edge.recency_tick` → exactly `edge.weight` (guard branch). At `currentTick &lt; edge.recency_tick` → exactly `edge.weight` (paused-tick guard).
   - **warmthBucket thresholds** (with default config): `0.15 → 'cold'`, `0.20 → 'warm'` (strict `&lt;`, so 0.20 is NOT cold), `0.59 → 'warm'`, `0.60 → 'hot'` (strict `&lt;` for warm upper), `0.95 → 'hot'`.
   - **sortedPairKey sort**: `sortedPairKey('did:noesis:b', 'did:noesis:a')` → `'did:noesis:a|did:noesis:b'` (always smaller first).

2. **`self-edge-rejection.test.ts`** — Tests the D-9-11 invariant at canonical layer:
   - `expect(() => sortedPairKey('did:noesis:x', 'did:noesis:x')).toThrow('self-loop rejected')`.
   - Note in file header comment: "The listener (Wave 1) catches this throw and silently returns — no audit emit, no Map write. This test only verifies the canonical-layer guard; listener behavior is tested in Wave 1's listener.test.ts."

3. **`determinism-source.test.ts`** — Wall-clock ban grep gate (D-9-12). Clone the `walk()` + `readdirSync` + `readFileSync` shape from RESEARCH.md §Producer-Boundary Grep Gate Regex lines 549-582, but adapted:
   - Constant: `const GRID_SRC = join(__dirname, '../../src/relationships');`
   - Regex: `const WALL_CLOCK_PATTERN = /\b(?:Date\.now|performance\.now|setInterval|setTimeout|Math\.random)\b/;`
   - Iterate `walk(GRID_SRC)`; for each `.ts` file, assert `!WALL_CLOCK_PATTERN.test(readFileSync(f, 'utf-8'))`. Collect offenders into an array; assert `offenders.toEqual([])` at the end (gives readable failure message naming the offending file).
   - MUST run even when `grid/src/relationships/` is nearly empty (Wave 0 state) — passes trivially at start, catches regressions as Waves 1-2 add files.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noēsis/grid &amp;&amp; npm test -- --run test/relationships/</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm test -- --run test/relationships/canonical.test.ts` passes with ≥5 test cases
    - `cd grid && npm test -- --run test/relationships/self-edge-rejection.test.ts` passes with ≥1 test case asserting throw
    - `cd grid && npm test -- --run test/relationships/determinism-source.test.ts` passes with 0 offenders in `grid/src/relationships/**`
    - `grep -c "WALL_CLOCK_PATTERN" grid/test/relationships/determinism-source.test.ts` → ≥1
    - `grep -cE "Date\\\\\\.now|performance\\\\\\.now|setInterval|setTimeout|Math\\\\\\.random" grid/test/relationships/determinism-source.test.ts` → ≥1 (the regex literal itself — test file is allowed to contain the pattern)
    - `grep -c "self-loop rejected" grid/test/relationships/self-edge-rejection.test.ts` → 1
    - No test uses a `.skip` or `.todo` marker (grep the three files for `\.skip\(|\.todo\(` → 0)
    - Full `cd grid && npm test -- --run` suite stays green (pre-existing + new tests)
  </acceptance_criteria>
  <done>
    All three test files exist under `grid/test/relationships/`; all pass via `npm test -- --run`. The D-9-10 canonical shape, D-9-11 self-loop invariant, and D-9-12 wall-clock ban are all enforced by automated regression at Wave 0 and will continue to gate Waves 1-4.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new in Wave 0 | Scaffolding only — no network, no SQL execution, no audit-chain interaction yet. Canonical primitives are pure functions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06-seed | Tampering | grid/src/relationships/canonical.ts | mitigate | D-9-10 canonical serialization locks 6-key order + `.toFixed(3)`; unit test in canonical.test.ts asserts JSON shape byte-identically |
| T-09-08-seed | Tampering | grid/src/relationships/canonical.ts::sortedPairKey | mitigate | D-9-11 self-loop throws at canonical boundary; self-edge-rejection.test.ts regression gate |
| T-09-09-seed | Tampering | grid/src/relationships/** (whole scope) | mitigate | D-9-12 grep gate forbids wall-clock + Math.random; determinism-source.test.ts enforces before any listener/storage code lands |
| T-09-13 | Tampering | sql/009_relationships.sql column types | mitigate | `DECIMAL(4,3)` matches `toFixed(3)` canonical precision — storage round-trip cannot drift from in-mem Map |
</threat_model>

<verification>
Run full grid suite after all three tasks:

```
cd grid && npm test -- --run
```

Expected: all pre-existing tests green + 3 new `test/relationships/*.test.ts` files green. Dashboard unaffected by Wave 0 (swr install only alters lockfile + package.json; no code imports it yet).

Also verify:
- `grep -r "import.*relationships" grid/src/` returns zero hits OUTSIDE `grid/src/relationships/` (module is self-contained; no wiring yet).
- `cd dashboard && npm run build` still succeeds (swr addition must not break the Next.js build).
</verification>

<success_criteria>
- All three files in `grid/src/relationships/` compile + export the types/functions listed in `must_haves.artifacts`.
- Migration `sql/009_relationships.sql` lands with D-9-10-compatible column precision.
- `swr ^2.x` is a dashboard runtime dependency.
- Three Wave 0 regression tests pass and lock D-9-10, D-9-11, D-9-12 invariants.
- `grid/src/relationships/**` scope stays free of wall-clock primitives.
- Full grid + dashboard build stays green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-relationship-graph-derived-view/09-01-SUMMARY.md` per template, with the Wave 0 evidence listed above (test run logs, `ls grid/src/relationships/`, swr package version confirmation).
</output>
