---
phase: 30-resources-support
plan: 01
title: Help Center Hub + Sidebar Nav Update
goal: Replace placeholder /portal/help page with a full help center hub; update PortalSidebar nav to wire all /portal/help/* sub-routes
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/src/app/portal/help/page.tsx
  - dashboard/src/components/portal/PortalSidebar.tsx
autonomous: true
requirements: [HELP-01]
must_haves:
  truths:
    - "/portal/help renders a hub page with four section cards linking to guide, faq, glossary, contact"
    - "PortalSidebar nav group shows Help Center, Getting Started, FAQ, Glossary, Support as sub-items"
    - "Hub page has a search bar placeholder (client-side, not wired to backend)"
    - "Breadcrumb nav shows 'Help' on hub, 'Help / FAQ' on sub-pages pattern is documented in code"
  artifacts:
    - path: dashboard/src/app/portal/help/page.tsx
      provides: "Help center hub landing page — four section cards + search bar"
      contains: "Help Center"
    - path: dashboard/src/components/portal/PortalSidebar.tsx
      provides: "Updated nav with /portal/help/* sub-routes"
      contains: "/portal/help/guide"
  key_links:
    - from: "dashboard/src/app/portal/help/page.tsx"
      to: "/portal/help/guide, /portal/help/faq, /portal/help/glossary, /portal/help/contact"
      via: "Next.js Link hrefs on section cards"
      pattern: "href.*help/"
---

<objective>
Replace the existing `/portal/help` FAQ placeholder with a proper help center hub page. The hub shows four section cards (Getting Started, FAQ, Glossary, Contact), a client-side search bar placeholder, and a clean editorial layout. Update PortalSidebar to surface the full `/portal/help/*` hierarchy in the nav.

Purpose: Phase 30 needs a discoverable entry point — this is the first plan because all sub-pages (Plans 02–05) are reachable from here.

Output: Updated `help/page.tsx` (hub) and `PortalSidebar.tsx` (nav wiring).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/30-resources-support/30-CONTEXT.md
@dashboard/src/app/portal/help/page.tsx
@dashboard/src/components/portal/PortalSidebar.tsx
@dashboard/src/app/portal/privacy/page.tsx

<interfaces>
<!-- Extracted from PortalSidebar.tsx — current nav structure for the Help & Docs group -->
```typescript
// Lines 63-68 of PortalSidebar.tsx (approximate):
{ href: '/portal/docs',     label: 'Documents', phase: '30' },
{ href: '/portal/glossary', label: 'Glossary' },
{ href: '/portal/help',     label: 'Help & FAQ' },
{ href: '/portal/privacy',  label: 'Privacy Policy' },
{ href: '/portal/terms',    label: 'Terms of Service' },
{ href: '/portal/status',   label: 'Project Status' },
```

<!-- CSS variables in use across portal pages -->
```
--navy, --parchment, --parchment-2, --bronze, --ink, --muted, --rule
--serif (Cormorant Garamond), --sans-portal, --mono-portal
```

<!-- Server component pattern (no 'use client') — from privacy/page.tsx -->
No hooks, no dynamic import, pure JSX with style={{ ... }} CSS variable refs.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite /portal/help as the Help Center hub page</name>
  <files>dashboard/src/app/portal/help/page.tsx</files>
  <action>
Replace the existing placeholder content with a proper help center hub. This is a server component — no `'use client'`, no hooks.

Structure:
1. **Header block**: H1 "Help Center" in `--serif`, subtitle "Guides, FAQs, glossary, and support for the Noēsis Portal." in `--sans-portal`/`--muted`
2. **Search bar** (decorative for now, no client state): `<input placeholder="Search help articles..." />` styled with `--parchment` bg, `--rule` border, `--ink` text, `--sans-portal` font. Mark it with a `TODO: wire client-side filter` comment. The input is NOT interactive yet — it's a static placeholder (no `onChange`). To make this server-only, render it as a plain `<input>` with no handler; the browser allows typing but nothing happens.
3. **Section cards grid** (2×2 CSS grid, gap 16px, 640px max-width):
   - **Getting Started** — icon: arrow-right SVG, title, desc "Step-by-step walkthrough from wallet setup to your first Nous.", link to `/portal/help/guide`
   - **FAQ** — icon: question-mark SVG, title, desc "Answers to common questions about wallets, Cyber Coin, Nous agents, and the Grid.", link to `/portal/help/faq`
   - **Glossary** — icon: book SVG, title, desc "Definitions for every Noēsis term — Nous, Ousia, Agora, Lore, Norms, and more.", link to `/portal/help/glossary`
   - **Contact Support** — icon: envelope SVG, title, desc "Submit a bug report, feature request, or account issue.", link to `/portal/help/contact`

Each card: `background: 'var(--parchment)'`, `border: '1px solid var(--rule)'`, `borderRadius: 6`, `padding: '20px 24px'`, `display: 'flex'`, `flexDirection: 'column'`, `gap: 8`. Title in `--ink`/`--sans-portal`/14px/weight 600. Desc in `--muted`/`--sans-portal`/13px. Use Next.js `<Link>` for hrefs.

4. **Footer note**: "Need immediate help? Email us at support@noesis.ai" in `--muted`/`--mono-portal`/10px.

Use `import Link from 'next/link'` for section card links. No dynamic import needed — pure server component.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noesis/dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `grep -c "Help Center" dashboard/src/app/portal/help/page.tsx` returns at least 1
    - `grep -c "portal/help/guide" dashboard/src/app/portal/help/page.tsx` returns at least 1
    - `grep -c "portal/help/faq" dashboard/src/app/portal/help/page.tsx` returns at least 1
    - `grep -c "portal/help/glossary" dashboard/src/app/portal/help/page.tsx` returns at least 1
    - `grep -c "portal/help/contact" dashboard/src/app/portal/help/page.tsx` returns at least 1
    - `grep -c "use client" dashboard/src/app/portal/help/page.tsx` returns 0 (server component)
    - TypeScript compiles with no errors in this file
  </done>
</task>

<task type="auto">
  <name>Task 2: Update PortalSidebar nav to wire /portal/help/* hierarchy</name>
  <files>dashboard/src/components/portal/PortalSidebar.tsx</files>
  <action>
Read the full PortalSidebar.tsx (especially lines 60-80 where the Help & Docs nav group lives). Locate the nav items array for the "Help & Docs" section group. Replace the existing three entries with the full Phase 30 structure:

```typescript
// Replace the existing docs/glossary/help entries with:
{ href: '/portal/help',          label: 'Help Center' },
{ href: '/portal/help/guide',    label: 'Getting Started' },
{ href: '/portal/help/faq',      label: 'FAQ' },
{ href: '/portal/help/glossary', label: 'Glossary' },
{ href: '/portal/help/contact',  label: 'Support' },
```

The old entries (`/portal/docs`, `/portal/glossary`, `/portal/help`) are removed and replaced. Keep all other nav entries (privacy, terms, status) unchanged.

The `isActive` check for `/portal/help` uses `pathname.startsWith(item.href)` — which means `/portal/help/faq` will also highlight the "Help Center" nav item. To prevent this, set `exact: true` on the `/portal/help` entry so it only activates when `pathname === '/portal/help'`. The sub-items (guide, faq, glossary, contact) use `startsWith` (the default `exact: false` behavior).

After the edit, verify the array has no remaining references to `/portal/docs` or the old `/portal/glossary` standalone route.
  </action>
  <verify>
    <automated>cd /Users/desirey/Programming/src/Noesis/dashboard && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - `grep -c "portal/help/guide" dashboard/src/components/portal/PortalSidebar.tsx` returns at least 1
    - `grep -c "portal/help/faq" dashboard/src/components/portal/PortalSidebar.tsx` returns at least 1
    - `grep -c "portal/help/contact" dashboard/src/components/portal/PortalSidebar.tsx` returns at least 1
    - `grep -c "'/portal/docs'" dashboard/src/components/portal/PortalSidebar.tsx` returns 0 (old route removed)
    - TypeScript compiles with no errors
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → server component | Help hub is server-rendered; no user input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | /portal/help hub | accept | Static editorial content only; no user data rendered; no auth required |
</threat_model>

<verification>
- `cd /Users/desirey/Programming/src/Noesis/dashboard && npx tsc --noEmit` exits 0
- `grep "portal/help/guide\|portal/help/faq\|portal/help/glossary\|portal/help/contact" dashboard/src/app/portal/help/page.tsx` returns 4 matches
- `grep "portal/help/guide" dashboard/src/components/portal/PortalSidebar.tsx` returns 1 match
- No `'use client'` in help/page.tsx
</verification>

<success_criteria>
- Help center hub renders at /portal/help with four section cards linking to all sub-routes
- PortalSidebar nav shows Getting Started, FAQ, Glossary, Support as distinct nav items under Help Center
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/30-resources-support/30-01-SUMMARY.md` documenting:
- What changed in help/page.tsx and PortalSidebar.tsx
- Any deviations from the plan with rationale
</output>
