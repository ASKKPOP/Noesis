---
phase: 14-researcher-rigs
plan: 04
type: execute
wave: 3
depends_on: [14-01, 14-02, 14-03]
files_modified:
  - grid/test/rig/rig-bench.test.ts
  - .github/workflows/nightly-rig-bench.yml
  - .github/workflows/rig-invariants.yml
  - scripts/rig-bench-runner.mjs
autonomous: true
requirements: [RIG-04, RIG-05]

must_haves:
  truths:
    - "rig-bench.test.ts exists as the canonical 50 Nous × 10000 ticks smoke test that turns the Wave 0 RIG-04 nightly stub GREEN — runs scripts/rig.mjs config/rigs/bench-50.toml end-to-end with NOESIS_FIXTURE_MODE=1 and asserts wall-clock <60min and exit_reason='tick_budget_exhausted'"
    - "scripts/rig-bench-runner.mjs is a thin wrapper that invokes rig.mjs as a subprocess, captures the chronos.rig_closed payload from the rig's audit chain (NOT broadcast — D-14-08), and emits a pass/fail report — used by both the Vitest test and the nightly workflow"
    - ".github/workflows/nightly-rig-bench.yml runs on cron (02:00 UTC daily) AND on workflow_dispatch — provisions MySQL 8 service container, applies rig schema migrations, runs scripts/rig.mjs config/rigs/bench-50.toml, asserts <60min wall-clock, uploads the deterministic JSONL tarball as an artifact"
    - ".github/workflows/rig-invariants.yml runs on every push and PR — runs scripts/check-rig-invariants.mjs (T-10-12 forbidden-symbol grep + T-10-13 bypass-flag grep) AND `cd grid && npx vitest run test/rig/` (per-commit fast unit tests) — finishes in <90s feedback latency per VALIDATION.md sampling rate"
    - "Nightly workflow chronos.rig_closed assertion validates the closed 5-key tuple (seed, tick, exit_reason, chain_entry_count, chain_tail_hash) — rejects any extra/missing keys"
    - "rig-bench.test.ts is tagged with describe.skipIf(!process.env.NOESIS_RUN_NIGHTLY) so per-commit Vitest runs SKIP it (it would exceed 90s feedback latency); only the nightly workflow sets NOESIS_RUN_NIGHTLY=1"
    - "Allowlist count remains 27 — chronos.rig_closed and any rig.* prefix MUST NOT be added (D-14-08); Plan 05 adds the regression gate, this plan must not regress it"
    - "Per-commit fast suite continues to pass: cd grid && npx vitest run test/rig/ exits 0 with all Wave 0/1/2 tests GREEN"
    - "Tarball produced by nightly run is byte-identical across two runs of the same seed (deterministic — verified by sha256 comparison in workflow)"
  artifacts:
    - path: grid/test/rig/rig-bench.test.ts
      provides: "Vitest 50×10k smoke — gated behind NOESIS_RUN_NIGHTLY env; skipped per-commit, run nightly"
      min_lines: 80
      contains: "describe.skipIf"
    - path: scripts/rig-bench-runner.mjs
      provides: "Node ESM helper — spawns rig.mjs as subprocess, parses the rig audit chain for chronos.rig_closed, validates the closed 5-key tuple, returns {passed, exitReason, wallClockMs, chainEntryCount, chainTailHash}"
      min_lines: 80
      contains: "chronos.rig_closed"
    - path: .github/workflows/nightly-rig-bench.yml
      provides: "GitHub Actions workflow — daily cron + manual dispatch; MySQL 8 service; runs RIG-04 50×10k smoke; uploads tarball artifact"
      min_lines: 60
      contains: "schedule"
    - path: .github/workflows/rig-invariants.yml
      provides: "Per-commit gate workflow — runs scripts/check-rig-invariants.mjs + fast Vitest rig suite; <90s feedback latency"
      min_lines: 40
      contains: "check-rig-invariants"
  key_links:
    - from: "grid/test/rig/rig-bench.test.ts"
      to: "scripts/rig-bench-runner.mjs"
      via: "import { runRigBench } from '../../../scripts/rig-bench-runner.mjs'"
      pattern: "runRigBench"
    - from: "scripts/rig-bench-runner.mjs"
      to: "scripts/rig.mjs"
      via: "node:child_process spawn('node', ['scripts/rig.mjs', configPath])"
      pattern: "spawn.*rig\\.mjs"
    - from: ".github/workflows/nightly-rig-bench.yml"
      to: "scripts/rig.mjs"
      via: "run: node scripts/rig.mjs config/rigs/bench-50.toml"
      pattern: "rig\\.mjs.*bench-50"
    - from: ".github/workflows/rig-invariants.yml"
      to: "scripts/check-rig-invariants.mjs"
      via: "run: node scripts/check-rig-invariants.mjs"
      pattern: "check-rig-invariants"
---

<objective>
Lock down the Phase 14 performance contract (RIG-04: 50 Nous × 10000 ticks completes in <60 min on a researcher laptop) and the chronos.rig_closed observability contract (RIG-05) with a real end-to-end nightly smoke test plus per-commit CI gates.

Purpose: RESEARCH.md §pitfalls explicitly warned that setInterval-based loops added 11+ seconds of overhead at 10k ticks — the Wave 2 direct for-loop fix MUST be empirically verified against the 60-minute budget. A microbenchmark (Wave 1 producer-bench.test.ts) is necessary but not sufficient: the 60-minute budget is end-to-end, not per-emit. Only a real GenesisLauncher invocation with 50 Nous × 10000 ticks under fixture mode can certify the budget.

Per-commit feedback latency must stay under 90s (per 14-VALIDATION.md sampling rate), so the heavy 50×10k smoke is gated behind NOESIS_RUN_NIGHTLY=1 and runs only in the nightly workflow. The per-commit `rig-invariants.yml` workflow runs the fast Wave 0/1/2 unit tests + the grep gates and exits in well under 90s.

Output:
- A nightly workflow that proves RIG-04 holds against a real (fixture-backed) Grid run, with the deterministic JSONL tarball uploaded as a buildable artifact.
- A per-commit workflow that prevents regressions in T-10-12 (forbidden symbols), T-10-13 (bypass flags), and the fast Vitest rig suite.
- A bench runner module that is reusable both from Vitest (when NOESIS_RUN_NIGHTLY=1) and from the workflow shell step — single source of truth for the chronos.rig_closed payload assertion.

Critical constraints carried forward:
- D-14-08: chronos.rig_closed lives ONLY on the rig's own AuditChain; never broadcast. The bench runner MUST read it directly from the rig schema (or from the tarball), NOT from any production broadcast bus. Plan 05 adds the regression gate; this plan must not regress it by accidentally subscribing to a production broadcast.
- D-14-02: The bench runner spawns `node scripts/rig.mjs` as a subprocess — it does NOT import rig.mjs in-process. This preserves the NOESIS_RIG_PARENT guard semantics (Wave 2 sets NOESIS_RIG_PARENT='1' inside rig.mjs; if rig.mjs were ever to spawn another rig the inner one rejects).
- D-14-06: The nightly workflow sets NOESIS_FIXTURE_MODE=1 and provides a fixture file — no real LLM calls allowed.
- 14-VALIDATION.md sampling rate: per-commit suite < 90s; nightly suite is the only place a >60min run is acceptable.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-researcher-rigs/14-CONTEXT.md
@.planning/phases/14-researcher-rigs/14-RESEARCH.md
@.planning/phases/14-researcher-rigs/14-VALIDATION.md
@.planning/phases/14-researcher-rigs/14-01-PLAN.md
@.planning/phases/14-researcher-rigs/14-02-PLAN.md
@.planning/phases/14-researcher-rigs/14-03-PLAN.md
@scripts/rig.mjs
@scripts/check-rig-invariants.mjs
@config/rigs/bench-50.toml
@config/rigs/fixtures/example-fixture.jsonl

<interfaces>
<!-- These contracts are produced by Waves 0/1/2 (Plans 01-03). Do NOT re-derive — use as-is. -->

From scripts/rig.mjs (Plan 03):
- CLI signature: `node scripts/rig.mjs <path/to/config.toml>`
- Exit codes:
  - 0: chronos.rig_closed emitted, tarball written successfully
  - 2: NOESIS_RIG_PARENT was set (nested-rig rejection per D-14-02)
  - 3: TOML config invalid / mutually-exclusive manifest violation (D-14-03)
  - 4: Fixture cache miss in strict mode (D-14-05)
  - 5: SIGTERM/SIGINT received → exit_reason='operator_h5_terminate'
- Stdout: human-readable progress; final line is JSON: `{"event":"rig_closed","payload":{...}}`
- Tarball location: `${RIG_OUTPUT_DIR ?? './rig-output'}/{configName}-{seed8}.tar.gz`

From grid/src/rig/types.ts (Plan 01):
```typescript
export type RigExitReason = 'tick_budget_exhausted' | 'all_nous_dead' | 'operator_h5_terminate';
export interface RigClosedPayload {
  readonly seed: string;
  readonly tick: number;
  readonly exit_reason: RigExitReason;
  readonly chain_entry_count: number;
  readonly chain_tail_hash: string; // SHA-256 hex of canonicalStringify(lastEntry)
}
```

From config/rigs/bench-50.toml (Plan 03):
- 50 Nous, 10000 tick budget, in-memory transport, fixture mode required
- configName = 'bench-50'
- seed is a deterministic 32-char hex string fixed in the TOML

From scripts/check-rig-invariants.mjs (Plan 01):
- CLI signature: `node scripts/check-rig-invariants.mjs`
- Exit 0: no forbidden symbols (T-10-12) AND no bypass flags (T-10-13)
- Exit 1: with diagnostic listing each violation
- Scans: scripts/rig.mjs, scripts/rig-config-loader.mjs, scripts/rig-bench-runner.mjs (this plan adds the third file to the scan list — see Task 1)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create rig-bench-runner.mjs (subprocess wrapper) and extend invariants gate scope</name>
  <files>
    scripts/rig-bench-runner.mjs
    scripts/check-rig-invariants.mjs
  </files>
  <read_first>
    - scripts/rig.mjs (Plan 03 output — confirm exit codes and stdout JSON contract)
    - scripts/check-rig-invariants.mjs (Plan 01 output — extend the file scan list to include the new bench runner so T-10-12/T-10-13 grep gates cover it too)
    - grid/src/rig/types.ts (Plan 01 — RigClosedPayload shape)
    - .planning/phases/14-researcher-rigs/14-CONTEXT.md §D-14-08 (closed 5-key tuple — no extra/missing keys allowed)
  </read_first>
  <acceptance_criteria>
    - scripts/rig-bench-runner.mjs exports `runRigBench(configPath, opts)` returning a Promise resolving to `{ passed, exitReason, wallClockMs, chainEntryCount, chainTailHash, tarballPath, stdout, stderr }`
    - The function spawns `node scripts/rig.mjs <configPath>` as a child process via `node:child_process.spawn`; never imports rig.mjs in-process (preserves D-14-02 NOESIS_RIG_PARENT propagation semantics)
    - Captures wall-clock via `process.hrtime.bigint()` deltas around the spawn lifecycle
    - Parses the final stdout line as JSON; asserts the payload has EXACTLY 5 keys: seed, tick, exit_reason, chain_entry_count, chain_tail_hash — `Object.keys(payload).sort().join(',')` must equal `'chain_entry_count,chain_tail_hash,exit_reason,seed,tick'`. Any extra or missing key → throw with diagnostic.
    - Asserts `payload.exit_reason` is one of the three valid enum values; throws on any other value.
    - Optional opts: `{ env?: Record<string,string>, timeoutMs?: number, expectExitReason?: RigExitReason }` — if `expectExitReason` is provided, mismatch is treated as `passed=false`.
    - Inherits NOESIS_FIXTURE_MODE='1' into the child process env by default; caller can override via opts.env.
    - On non-zero exit code from rig.mjs that is NOT the expected exit reason path, returns `passed=false` with stdout/stderr captured for diagnostics; does NOT throw (this lets the workflow upload artifacts even on failure).
    - scripts/check-rig-invariants.mjs SCAN_TARGETS array is extended to include 'scripts/rig-bench-runner.mjs' so T-10-12 (httpServer.listen|wsHub) and T-10-13 (--skip-*|--bypass-*|--disable-*|--no-reviewer|--no-tier) grep gates also cover the bench runner.
  </acceptance_criteria>
  <action>
    Create scripts/rig-bench-runner.mjs (~80 lines) as a Node ESM module. Use `node:child_process.spawn('node', ['scripts/rig.mjs', configPath], { env: { ...process.env, NOESIS_FIXTURE_MODE: '1', ...(opts?.env ?? {}) }, stdio: ['ignore', 'pipe', 'pipe'] })`. Buffer stdout/stderr. On 'close' event, parse the LAST non-empty line of stdout as JSON (the rig.mjs final emission per Plan 03). Validate the closed 5-key tuple exactly: build `const expected = ['chain_entry_count','chain_tail_hash','exit_reason','seed','tick'].join(',')` and compare against `Object.keys(payload).sort().join(',')`. If mismatch, throw `RigClosedTupleViolation` with the diff. Validate exit_reason against the three-element set `new Set(['tick_budget_exhausted','all_nous_dead','operator_h5_terminate'])`. Compute wallClockMs from hrtime delta (cast bigint nanoseconds → ms). Return the structured result. Implement an optional `timeoutMs` via `setTimeout` + `child.kill('SIGTERM')` — important: SIGTERM is the operator_h5_terminate path per Plan 03, so `expectExitReason='operator_h5_terminate'` is a valid test case for the SIGTERM-handler flow.

    Add a default export plus named export `runRigBench` for ergonomic imports.

    Then edit scripts/check-rig-invariants.mjs: locate the SCAN_TARGETS array (created in Plan 01) and append `'scripts/rig-bench-runner.mjs'`. This is a one-line addition; the existing grep logic already iterates the array.
  </action>
  <verify>
    <automated>node scripts/check-rig-invariants.mjs && node -e "import('./scripts/rig-bench-runner.mjs').then(m => { if (typeof m.runRigBench !== 'function') process.exit(1); })"</automated>
  </verify>
  <done>
    - scripts/rig-bench-runner.mjs exists, exports runRigBench, no syntax errors
    - scripts/check-rig-invariants.mjs SCAN_TARGETS includes the new file; grep gate exits 0
    - No httpServer.listen / wsHub references in the new file (T-10-12)
    - No --skip-* / --bypass-* / --disable-* / --no-reviewer / --no-tier strings in the new file (T-10-13)
  </done>
</task>

<task type="auto">
  <name>Task 2: Create rig-bench.test.ts (Vitest 50×10k smoke, NOESIS_RUN_NIGHTLY-gated)</name>
  <files>grid/test/rig/rig-bench.test.ts</files>
  <read_first>
    - scripts/rig-bench-runner.mjs (Task 1 output — runRigBench API)
    - config/rigs/bench-50.toml (Plan 03 — confirm configName, seed, tickBudget=10000)
    - config/rigs/fixtures/example-fixture.jsonl (Plan 03 — confirm fixture is sufficient for 50 Nous × 10k ticks; if not, document fixture extension as a follow-up — but the bench should still RUN to completion under permissive=true if cache misses occur)
    - .planning/phases/14-researcher-rigs/14-VALIDATION.md row 14-W2-01 (rig-bench.test.ts is the canonical Wave 2 nightly smoke per the validation map)
    - 14-CONTEXT.md §D-14-04 (60-minute budget on researcher laptop — CI machine may be slower; budget to 90 min in CI to absorb cold-start overhead, but assert the rig.mjs internal stopwatch <60min — see Action below)
  </read_first>
  <acceptance_criteria>
    - File exports a Vitest describe block: `describe.skipIf(!process.env.NOESIS_RUN_NIGHTLY)('RIG-04 nightly smoke: 50 Nous × 10000 ticks', () => { ... })` — per-commit runs SKIP this entire describe (verifies via `cd grid && npx vitest run test/rig/rig-bench.test.ts` finishing in <2s when NOESIS_RUN_NIGHTLY is unset).
    - Test 1: 'completes within 60-minute wall-clock budget' — calls `await runRigBench('config/rigs/bench-50.toml', { timeoutMs: 90 * 60 * 1000 })`, asserts result.passed === true AND result.wallClockMs < 60 * 60 * 1000 (the CI-side timeout is 90min to allow cold-start, but the assertion enforces the 60min RIG-04 contract).
    - Test 2: 'closes with tick_budget_exhausted (not premature death)' — same call, asserts result.exitReason === 'tick_budget_exhausted'. (Bench-50.toml provides enough fixture coverage / permissive fallback that no Nous starves before tick 10000; if the fixture file is insufficient and Nous die naturally, this test will fail and surface the fixture gap as a real problem.)
    - Test 3: 'emits canonical chronos.rig_closed 5-key tuple' — asserts result.chainEntryCount > 0, result.chainTailHash matches /^[a-f0-9]{64}$/ (SHA-256 hex), and (already validated inside runRigBench) the payload had exactly the 5 keys.
    - Each test sets a per-test timeout of 95 minutes via `{ timeout: 95 * 60 * 1000 }` (Vitest test option, third arg).
  </acceptance_criteria>
  <action>
    Create grid/test/rig/rig-bench.test.ts. Top of file:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { runRigBench } from '../../../scripts/rig-bench-runner.mjs';
    ```
    Use `describe.skipIf(!process.env.NOESIS_RUN_NIGHTLY)` so the entire suite is a no-op when the env flag is unset (per-commit feedback-latency rule from 14-VALIDATION.md).

    Path note: from `grid/test/rig/`, the relative path to `scripts/rig-bench-runner.mjs` at repo root is `../../../scripts/rig-bench-runner.mjs`. Verify by running the test under NOESIS_RUN_NIGHTLY=1 locally before committing if MySQL is available; otherwise rely on the nightly workflow run.

    Three `it` blocks as per acceptance criteria. The runRigBench call is shared — extract into a `beforeAll` with a `let result: Awaited<ReturnType<typeof runRigBench>>;` cache so the 50×10k run only happens ONCE per test file invocation. Use `beforeAll(async () => { result = await runRigBench('config/rigs/bench-50.toml', { timeoutMs: 90 * 60 * 1000, expectExitReason: 'tick_budget_exhausted' }); }, 95 * 60 * 1000);`.

    Then each `it` reads from the shared `result`. This avoids running the bench three times.
  </action>
  <verify>
    <automated>cd grid && npx vitest run test/rig/rig-bench.test.ts</automated>
  </verify>
  <done>
    - File exists, vitest run completes in <2s when NOESIS_RUN_NIGHTLY is unset (entire describe block reports as skipped)
    - When the nightly workflow sets NOESIS_RUN_NIGHTLY=1 + MySQL is available, all three tests pass (validated by Task 3 workflow run)
  </done>
</task>

<task type="auto">
  <name>Task 3: Create CI workflows (.github/workflows/rig-invariants.yml + nightly-rig-bench.yml)</name>
  <files>
    .github/workflows/rig-invariants.yml
    .github/workflows/nightly-rig-bench.yml
  </files>
  <read_first>
    - 14-VALIDATION.md §"Sampling Rate" — per-commit feedback latency <90s; nightly is the only place >60min runs are allowed
    - scripts/rig-bench-runner.mjs (Task 1) and grid/test/rig/rig-bench.test.ts (Task 2) — workflows wire these up
    - config/rigs/bench-50.toml (Plan 03 — workflow runs this config)
    - 14-CONTEXT.md §D-14-01 (MySQL isolated schema — workflow must provision a MySQL service container)
    - scripts/check-rig-invariants.mjs (Plan 01) — first gate in rig-invariants.yml
  </read_first>
  <acceptance_criteria>
    - .github/workflows/rig-invariants.yml triggers on push (any branch) AND pull_request — three steps: (1) checkout + setup-node 20, (2) `node scripts/check-rig-invariants.mjs` (T-10-12 + T-10-13 grep gates), (3) `cd grid && npm ci && npx vitest run test/rig/` (fast unit suite — Wave 0/1/2 tests only, since Wave 3 rig-bench.test.ts skips when NOESIS_RUN_NIGHTLY is unset). Workflow finishes in <90s on a standard ubuntu-latest runner.
    - .github/workflows/nightly-rig-bench.yml triggers on `schedule: - cron: '0 2 * * *'` (02:00 UTC daily) AND `workflow_dispatch` (manual). Uses MySQL 8 service container with health check. Steps: (1) checkout + setup-node 20, (2) install deps, (3) wait for MySQL ready, (4) run `node scripts/rig.mjs config/rigs/bench-50.toml` with NOESIS_FIXTURE_MODE=1, NOESIS_RUN_NIGHTLY=1, MYSQL_URL set to the service container — wrapped in `time` so wall-clock is logged, (5) assert exit code 0 and final stdout JSON line has `exit_reason: "tick_budget_exhausted"` (use `tail -n 1 stdout.log | jq` if jq is available, else a Node one-liner), (6) upload the produced tarball as a workflow artifact via `actions/upload-artifact@v4`.
    - Nightly workflow timeout-minutes: 90 (CI-side cap, with the 60min assertion enforced by the rig.mjs internal stopwatch / by the bench runner's wallClockMs check when invoked via the Vitest path).
    - Nightly workflow ALSO runs `cd grid && NOESIS_RUN_NIGHTLY=1 npx vitest run test/rig/rig-bench.test.ts` as an alternate verification path that exercises the runRigBench wrapper end-to-end. (Either path on its own is sufficient; running both gives belt-and-suspenders coverage and catches regressions in the wrapper itself.)
    - Both workflows use `permissions: contents: read` (least-privilege; no token write needed).
    - Neither workflow sets any --skip-* / --bypass-* / --disable-* flag (T-10-13 invariant).
  </acceptance_criteria>
  <action>
    First, create the .github/workflows/ directory if it does not exist (Plan 04 is the first phase to add CI workflow files — verify with `ls -la .github/workflows/ 2>/dev/null` before writing; mkdir -p if needed).

    Write .github/workflows/rig-invariants.yml (~40 lines):
    ```yaml
    name: rig-invariants
    on:
      push:
      pull_request:
    permissions:
      contents: read
    jobs:
      invariants:
        runs-on: ubuntu-latest
        timeout-minutes: 5
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
              cache-dependency-path: grid/package-lock.json
          - name: T-10-12 + T-10-13 grep gates
            run: node scripts/check-rig-invariants.mjs
          - name: Fast Vitest rig suite (Wave 0/1/2 only — NOESIS_RUN_NIGHTLY unset)
            working-directory: grid
            run: |
              npm ci
              npx vitest run test/rig/
    ```

    Write .github/workflows/nightly-rig-bench.yml (~80 lines):
    ```yaml
    name: nightly-rig-bench
    on:
      schedule:
        - cron: '0 2 * * *'
      workflow_dispatch:
    permissions:
      contents: read
    jobs:
      bench:
        runs-on: ubuntu-latest
        timeout-minutes: 90
        services:
          mysql:
            image: mysql:8
            env:
              MYSQL_ROOT_PASSWORD: root
              MYSQL_DATABASE: noesis_rig
            ports:
              - 3306:3306
            options: >-
              --health-cmd="mysqladmin ping -h localhost -uroot -proot"
              --health-interval=10s
              --health-timeout=5s
              --health-retries=10
        env:
          MYSQL_URL: mysql://root:root@127.0.0.1:3306/noesis_rig
          NOESIS_FIXTURE_MODE: '1'
          NOESIS_RUN_NIGHTLY: '1'
          RIG_OUTPUT_DIR: ${{ github.workspace }}/rig-output
        steps:
          - uses: actions/checkout@v4
          - uses: actions/setup-node@v4
            with:
              node-version: '20'
              cache: 'npm'
              cache-dependency-path: grid/package-lock.json
          - name: Install deps
            working-directory: grid
            run: npm ci
          - name: Wait for MySQL
            run: |
              for i in {1..30}; do
                if mysqladmin ping -h 127.0.0.1 -P 3306 -uroot -proot --silent; then
                  echo "MySQL is ready"; exit 0
                fi
                echo "Waiting for MySQL... ($i/30)"; sleep 2
              done
              echo "MySQL never became ready"; exit 1
          - name: Path A — direct rig.mjs invocation
            run: |
              mkdir -p "$RIG_OUTPUT_DIR"
              { time node scripts/rig.mjs config/rigs/bench-50.toml | tee rig-stdout.log; } 2> rig-time.log
              EXIT_REASON=$(tail -n 1 rig-stdout.log | node -e "let d=''; process.stdin.on('data',c=>d+=c); process.stdin.on('end',()=>{const j=JSON.parse(d); console.log(j.payload.exit_reason);})")
              if [ "$EXIT_REASON" != "tick_budget_exhausted" ]; then
                echo "RIG-04 fail: exit_reason was '$EXIT_REASON', expected 'tick_budget_exhausted'"
                cat rig-time.log
                exit 1
              fi
              cat rig-time.log
          - name: Path B — Vitest wrapper exercise
            working-directory: grid
            run: npx vitest run test/rig/rig-bench.test.ts
          - name: Upload deterministic JSONL tarball artifact
            if: always()
            uses: actions/upload-artifact@v4
            with:
              name: rig-bench-50-tarball
              path: ${{ env.RIG_OUTPUT_DIR }}/*.tar.gz
              if-no-files-found: warn
              retention-days: 7
          - name: Upload run logs
            if: always()
            uses: actions/upload-artifact@v4
            with:
              name: rig-bench-50-logs
              path: |
                rig-stdout.log
                rig-time.log
              retention-days: 7
    ```

    Important: the `Path A` step uses `tee` so stdout is both displayed in the workflow log AND captured to `rig-stdout.log` for the JSON-parse step. The `time` builtin output is captured separately to `rig-time.log` via shell redirection; the brace-grouping `{ ...; } 2> rig-time.log` is required to scope the stderr redirect to the `time` output rather than the inner pipeline.

    DO NOT add any --skip / --bypass / --disable / --no-reviewer / --no-tier flags anywhere — T-10-13 grep gate from Plan 01 will fail the build if any appear (it scans this workflow file too once you add it to the SCAN_TARGETS list — but workflows are not currently scanned; defer that scope expansion to Plan 05 if needed, since check-rig-invariants.mjs is the gate, not check-state-doc-sync.mjs).
  </action>
  <verify>
    <automated>node -e "const yaml=require('js-yaml');const fs=require('fs');for (const f of ['.github/workflows/rig-invariants.yml','.github/workflows/nightly-rig-bench.yml']) { const doc=yaml.load(fs.readFileSync(f,'utf8')); if(!doc.jobs) { console.error('NO JOBS:',f); process.exit(1); } console.log(f,'OK'); }" 2>/dev/null || (test -f .github/workflows/rig-invariants.yml && test -f .github/workflows/nightly-rig-bench.yml && echo "Files exist; YAML lint deferred to GitHub Actions runner")</automated>
  </verify>
  <done>
    - Both workflow files exist under .github/workflows/
    - rig-invariants.yml has on: push + pull_request, runs check-rig-invariants.mjs + fast vitest, timeout-minutes: 5
    - nightly-rig-bench.yml has schedule: cron + workflow_dispatch, MySQL 8 service, both Path A and Path B steps, uploads artifact, timeout-minutes: 90
    - No --skip / --bypass / --disable / --no-reviewer / --no-tier strings appear in either file (T-10-13 invariant)
    - First nightly workflow run (after merge) succeeds; tarball artifact is downloadable and is byte-identical to a second run of the same seed (manual sha256 comparison after second nightly run — documented as RIG-04 / RIG-05 acceptance evidence in 14-05 doc-sync plan)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner ↔ MySQL service container | Untrusted CI environment runs the rig; service container shares localhost network |
| nightly workflow ↔ artifact storage | Tarball uploaded to GitHub artifacts is publicly accessible to repo collaborators |
| rig.mjs subprocess ↔ rig-bench-runner.mjs parent | stdout JSON contract is the trust boundary for the closed 5-key tuple assertion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-04-01 | Tampering | rig-bench-runner.mjs stdout-parsing | mitigate | Parse only the LAST non-empty line of stdout; validate exact 5-key tuple via `Object.keys(...).sort().join(',') === expected` — rejects any extra keys injected via log noise |
| T-14-04-02 | Information Disclosure | rig-bench-50-tarball artifact | accept | Per D-14-08 the rig audit chain is researcher-internal; nightly bench-50 uses fixtures (NOESIS_FIXTURE_MODE=1) so no real Nous personality data is in the tarball. Public artifact upload is acceptable at this scope. Plan 05 documents this trust assumption in STATE.md. |
| T-14-04-03 | Denial of Service | nightly workflow runaway | mitigate | timeout-minutes: 90 at the job level; runRigBench timeoutMs: 90 * 60 * 1000 at the test level; rig.mjs internal tickBudget: 10000 caps the loop. Three-layer defense. |
| T-14-04-04 | Elevation of Privilege | workflow GITHUB_TOKEN scope | mitigate | `permissions: contents: read` on both workflows — least-privilege; no PR write, no package write, no actions write |
| T-14-04-05 | Spoofing | NOESIS_FIXTURE_MODE bypass attempt | mitigate | Plan 01's check-rig-invariants.mjs grep gate (T-10-13) bans any --skip / --bypass / --disable flag in scanned files; Task 1 extends SCAN_TARGETS to include rig-bench-runner.mjs |
| T-14-04-06 | Repudiation | nightly run results | mitigate | Both rig-stdout.log and rig-time.log are uploaded as separate artifacts (`if: always()`) so failure forensics are preserved even if subsequent steps fail |
</threat_model>

<verification>
After all three tasks complete:

1. Per-commit suite (workflow `rig-invariants.yml` simulation):
   ```bash
   node scripts/check-rig-invariants.mjs
   cd grid && npx vitest run test/rig/
   ```
   Both must exit 0 and complete in well under 90 seconds combined.

2. Workflow files parse as valid YAML (sanity check before merge):
   ```bash
   node -e "const yaml=require('js-yaml'); for (const f of ['.github/workflows/rig-invariants.yml','.github/workflows/nightly-rig-bench.yml']) { yaml.load(require('fs').readFileSync(f,'utf8')); console.log(f,'OK'); }"
   ```
   (js-yaml is optional; if not installed, fall back to `python3 -c 'import yaml; yaml.safe_load(open(f))'` or rely on GitHub Actions' own validator on first push.)

3. Nightly workflow first run (after merge):
   - Manual trigger via `gh workflow run nightly-rig-bench.yml`
   - Wall-clock under 60 minutes (RIG-04)
   - exit_reason == 'tick_budget_exhausted' (RIG-05 closure)
   - Tarball artifact downloadable
   - Second manual trigger produces a byte-identical tarball (sha256 match — documents RIG-05 determinism for Plan 05's STATE.md update)

4. Allowlist invariant (regression check):
   ```bash
   node scripts/check-state-doc-sync.mjs
   ```
   Still exits 0 with "27 events" — Plan 04 must not regress the count.
</verification>

<success_criteria>
- RIG-04 empirically verified: nightly bench-50 (50 Nous × 10000 ticks) completes <60 min wall-clock with exit_reason='tick_budget_exhausted'
- RIG-05 closure tuple validated: chronos.rig_closed payload has exactly 5 keys, exit_reason in valid enum, chain_tail_hash is SHA-256 hex
- Per-commit feedback latency <90s preserved (rig-invariants.yml workflow)
- Allowlist count remains 27 — chronos.* / rig.* prefixes NOT added to grid/src/audit/broadcast-allowlist.ts (Plan 05 adds the regression gate)
- Two consecutive nightly runs of the same seed produce byte-identical tarballs (deterministic build)
</success_criteria>

<output>
After completion, create `.planning/phases/14-researcher-rigs/14-04-SUMMARY.md` documenting:
- The first observed nightly wall-clock (sub-60min budget consumption)
- The exact chronos.rig_closed payload from the nightly run (seed, tick=10000, exit_reason='tick_budget_exhausted', chain_entry_count=N, chain_tail_hash=...)
- The sha256 of the bench-50 tarball (so Plan 05 can pin it as evidence for STATE.md / MILESTONES.md)
- Any fixture extensions needed (if Nous die naturally before tick 10000, document the fixture file additions)
</output>
