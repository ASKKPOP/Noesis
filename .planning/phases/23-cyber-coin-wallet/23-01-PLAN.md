---
phase: "23-cyber-coin-wallet"
plan: "23-01"
type: "feature"
wave: 1
depends_on: ["22-04"]
files_modified:
  - "dashboard/src/app/portal/wallet/page.tsx"
  - "dashboard/src/components/portal/WalletPanel.tsx"
  - "dashboard/src/components/portal/PortalSidebar.tsx"
autonomous: true
must_haves:
  truths:
    - "WalletPanel renders live ETH balance via wagmi useBalance"
    - "WalletPanel renders USDT ERC-20 balance via useReadContract"
    - "Send form supports ETH (useSendTransaction) and USDT (useWriteContract)"
    - "MAX button fills ETH send field with full balance"
    - "Transaction confirmation tracked via useWaitForTransactionReceipt"
    - "Session-local tx history with pending/confirmed/failed states"
    - "notifyGrid() fires best-effort POST /api/v1/portal/wallet/transfer on confirm"
    - "Wallet sidebar nav item is active (phase tag removed)"
  artifacts:
    - "dashboard/src/components/portal/WalletPanel.tsx"
    - "dashboard/src/app/portal/wallet/page.tsx"
---

# Plan 23-01: Cyber Coin Wallet Dashboard UI

**Backfilled:** 2026-05-20 — implemented in commit `245c65e` outside GSD workflow

## Goal

Build the wallet UI inside `/portal/wallet`: live ETH + USDT balances, send form with confirmation, session-local tx history, and best-effort Grid notification for `human.transferred`.

## Tasks

1. **WalletPanel component** — ETH balance (useBalance), USDT balance (useReadContract on mainnet contract `0xdAC17...`), send form, tx history list ✅
2. **Send form** — ETH via useSendTransaction, USDT via useWriteContract, MAX button, `useWaitForTransactionReceipt` confirmation tracking ✅
3. **Grid notification** — `notifyGrid()` calls `POST /api/v1/portal/wallet/transfer` best-effort after confirmation ✅
4. **Sidebar** — Wallet nav item activated, version bumped ✅

## Out of Scope (deferred to Phase 24)

- Grid endpoint `POST /api/v1/portal/wallet/transfer`
- `appendHumanTransferred.ts` sole-producer emitter
- Allowlist position 45 (`human.transferred`)

## Notes

Grid endpoint was originally in scope for this phase but was not implemented. WalletPanel silently swallows the 404 from `notifyGrid()`. The gap is tracked in CONTEXT.md and must be completed in Phase 24.
