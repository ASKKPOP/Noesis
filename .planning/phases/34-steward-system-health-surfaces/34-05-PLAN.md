---
phase: 34-steward-system-health-surfaces
plan: 05
type: execute
wave: 3
depends_on:
  - "34-01"
  - "34-02"
  - "34-03"
  - "34-04"
files_modified:
  - .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md
autonomous: false
requirements:
  - OBS-11
  - OBS-12
  - OBS-13
  - OBS-14
tags:
  - uat
  - playbook
  - docs
user_setup: []

must_haves:
  truths:
    - "34-HUMAN-UAT.md exists at .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md."
    - "The playbook follows the Phase 31/32 structure: Why this UAT exists → Prerequisites → Step 0 deploy → numbered verification steps → operator sign-off."
    - "Step 0 calls out `docker compose build grid && docker compose up -d grid` for the Plan 01 reasons-payload change AND `docker compose build steward && docker compose up -d steward` for the Steward UI changes (per project memory feedback_deploy_docker.md)."
    - "The playbook has one numbered verification step per REQ: Step 1 (OBS-11 Audit Pipeline Health card), Step 2 (OBS-12 Firehose Diagnostics card), Step 3 (OBS-13 Events per Minute by Family sparkline), Step 4 (OBS-14 firehose watchdog reconnect)."
    - "Step 5 covers the ROADMAP §Phase 34 SC #5 MySQL outage scenario: docker stop noesis-mysql → cards turn amber/red within 60s → docker start noesis-mysql → cards turn green within 60s without browser refresh."
    - "Each step has Expected Output + Operator Notes sign-off lines (mirrors Phase 31/32 format)."
    - "The playbook includes a final 'Document close-out' section noting that 34-VERIFICATION.md should be created on PASS and the phase should advance to Phase 35."
  artifacts:
    - path: ".planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md"
      provides: "Operator cutover playbook for Phase 34 (REQ OBS-11/12/13/14 manual verification)"
      min_lines: 120
      contains: "docker stop noesis-mysql"
  key_links:
    - from: ".planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md"
      to: "ROADMAP.md §Phase 34 5 success criteria"
      via: "each success criterion mapped to a numbered UAT step"
      pattern: "OBS-11|OBS-12|OBS-13|OBS-14"
---

<objective>
Ship the operator-facing UAT playbook for Phase 34. Mirrors the proven structure of `31-HUMAN-UAT.md` and `32-HUMAN-UAT.md`: a deploy-the-changes step (Step 0), one numbered step per REQ (Steps 1-4), the cross-cutting MySQL outage scenario (Step 5), and an operator sign-off footer.

The playbook is the manual half of the Phase 34 close-out. vitest covers all in-process contract tests (Plan 01) but cannot exercise:
- Multi-tab Steward at `/system` watching cards refresh every 5s without page reload.
- Visual color band transitions (green → amber → red).
- Reasons sub-line rendering with live `reasons[]` array under the divergence big-number.
- Firehose watchdog reconnect with a real WS connection + a real stalled `last_frame_at`.
- The `docker stop noesis-mysql` cutover scenario from ROADMAP §Phase 34 SC #5.

Purpose: An operator running this playbook end-to-end gives the human-side confirmation that OBS-11/12/13/14 ship.

Output: One new markdown file at `.planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md`.

Frozen contracts respected:
- Per project memory `feedback_deploy_docker.md`: EVERY source change requires Docker rebuild + restart before any verification.
- Per project memory `feedback_task_completion_ritual.md`: commit + push happen BEFORE this UAT runs (the executor commits the code, the operator runs the playbook against the deployed stack).
- Bilingual responses requirement is a chat-response rule, NOT a documentation rule — this playbook is English-only per existing Phase 31/32 precedent.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/34-steward-system-health-surfaces/34-CONTEXT.md
@.planning/phases/34-steward-system-health-surfaces/34-01-PLAN.md
@.planning/phases/34-steward-system-health-surfaces/34-02-PLAN.md
@.planning/phases/34-steward-system-health-surfaces/34-03-PLAN.md
@.planning/phases/34-steward-system-health-surfaces/34-04-PLAN.md
@.planning/phases/31-audit-pipeline-persistence/31-HUMAN-UAT.md
@.planning/phases/32-firehose-observability/32-HUMAN-UAT.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md</name>
  <files>.planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/32-firehose-observability/32-HUMAN-UAT.md (structural template — Step 0 deploy block, per-step Expected Output + Operator Notes, final sign-off)
    - .planning/phases/31-audit-pipeline-persistence/31-HUMAN-UAT.md (alternative structural template; pick whichever cleaner)
    - .planning/ROADMAP.md §"Phase 34" 5 success criteria (lines 110-126)
    - .planning/REQUIREMENTS.md §"OBS — Steward Console Health Surfaces (Phase 34)" OBS-11/12/13/14
  </read_first>
  <action>
    Create new file `.planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md` with the following content:

    ```markdown
    # 34-HUMAN-UAT — Steward `/system` Health Surfaces Operator Verification

    **Phase:** 34 — Steward `/system` Health Surfaces
    **REQs verified:** OBS-11, OBS-12, OBS-13, OBS-14
    **Risks pinned:** R-34-01 (multi-tab poll accepted MVP), R-34-02 (REST-not-WS sparkline tested in Step 3), R-34-03 (watchdog suppression window tested in Step 4)
    **Last updated:** [operator fills date on first run]

    ## Why this UAT exists

    The five Phase 34 success criteria from ROADMAP.md include behaviors that cannot be verified inside vitest:

    1. Operator opens `/system` and SEES the three new cards above the Allowlist Monitor with live data (requires a running Grid + a running Steward + a real browser).
    2. Cards REFRESH every 5s without page reload — observed visually across 60 seconds.
    3. The Events per Minute by Family sparkline RENDERS even when the firehose is down (the orthogonal-channel claim — REST-driven survives WS failure).
    4. The firehose watchdog forces reconnect when `last_frame_at` goes stale despite `client_count > 0` — observed via the connection status pill on `/firehose`.
    5. The MySQL outage cutover scenario (`docker stop noesis-mysql` → cards turn red within 60s → restart → cards turn green within 60s) requires real Docker lifecycle interaction.

    ## Prerequisites

    - macOS or Linux operator workstation with `docker`, `docker compose`, `curl`, `jq`, `node` (>=20).
    - Phase 31 + Phase 32 + Phase 33 already shipped (verifiable by `audit_reconcile_ok` heartbeat lines in `docker compose logs grid`).
    - Steward dev or production container available; if running Steward from source, `cd steward && npm run build && npm start` works.
    - Browser with developer console open (Chrome / Firefox / Safari all acceptable; Chrome preferred for the Network tab inspection in Step 3).

    ## Step 0 — Deploy and confirm baseline

    Per project memory `feedback_deploy_docker.md`, EVERY source change requires a Docker rebuild + restart before any verification.

    ```sh
    # 1. Rebuild and restart Grid for the Plan 01 reasons-payload extension.
    docker compose build grid
    docker compose up -d grid

    # 2. Rebuild and restart Steward for the Plan 02-04 UI changes.
    docker compose build steward
    docker compose up -d steward

    # 3. Wait 90 seconds for cold-start grace AND for use-health-detailed to accumulate
    #    a baseline frame-counter delta. NO commands during this window.
    sleep 90

    # 4. Confirm Phase 31 + 32 baseline still healthy.
    curl -s http://localhost:8080/health/detailed | jq '.status, .reasons, (.audit | {persisted_max_id, divergence}), (.firehose | {client_count, frames_sent_total, last_frame_at})'
    ```

    **Expected output (Step 0.4):** JSON showing:
    - `.status` is `"ok"` (or `"degraded"` if there's a pre-existing stale frame; investigate before continuing).
    - `.reasons` is `[]` for `ok` status, or a populated array of snake_case keys for non-ok.
    - `.audit.persisted_max_id` is a positive number.
    - `.audit.divergence` is `0` or a small number.
    - `.firehose.client_count` is `0` (no Steward client open yet) or whatever count of pre-existing connections.

    **If `.reasons` is missing from the response:** Plan 01 was not deployed. Run `docker compose logs grid --since 5m | grep -i health-watchdog` and verify the Grid restart picked up the latest image.

    Operator notes (fill in):
    - Deploy completed at: __________________
    - `/health/detailed` returns `reasons` field: [ ] yes / [ ] no

    ---

    ## Step 1 — Audit Pipeline Health card visible on /system (OBS-11)

    1. Open Steward in a browser at `http://localhost:3000/system` (adjust port if Steward is on a different one).
    2. Scroll to the section ABOVE the existing "Allowlist Monitor" heading.
    3. Verify the **Audit Pipeline Health** card is present, with:
       - h2 heading "Audit Pipeline Health".
       - A large divergence big-number (likely "0") rendered in green (`#2d7a2d`) with a light-green background panel.
       - A row beneath the panel showing `In-memory: N  Persisted: M` where N and M are equal under normal load.

    4. Open the browser DevTools Network tab and filter by "health/detailed". Confirm `GET /health/detailed` fires approximately every 5 seconds (Status 200, Type "xhr" or "fetch").

    **Expected output (Step 1):**
    - Card renders with green divergence banner.
    - Polling fires every 5s (visible in Network tab).
    - No console errors.

    **Manual divergence color test:** Step 5 below induces the amber/red color change via MySQL outage.

    Operator notes:
    - Card visible above Allowlist Monitor: [ ] yes / [ ] no
    - 5s polling confirmed in Network tab: [ ] yes / [ ] no
    - Divergence value displayed: __________________
    - Console errors: [ ] none / [ ] some (paste below): __________________

    ---

    ## Step 2 — Firehose Diagnostics card visible on /system (OBS-12)

    1. With Steward `/system` still open, scroll to the **Firehose Diagnostics** card (immediately below Audit Pipeline Health).
    2. Confirm three big-number stat cards: **Connected Clients**, **Frames Sent (1m)**, **Time Since Last Frame**.
    3. Confirm a two-row sparkline below the stat cards: a top row of grey bars (frames sent per 5s) and a bottom row of terracotta bars (frames dropped per 5s).
    4. The Time Since Last Frame value should be a small number ("3s", "8s") if there's any audit activity. If `Connected Clients` is 0, the value may show "—" — that's expected per the OBS-12 `null` semantics.

    5. To exercise the live render: open a SECOND browser tab at `/firehose`. The first tab's `/system` Firehose Diagnostics card should show `Connected Clients: 1` (or more) within 5s. Frames Sent (1m) should start incrementing as audit events fire.

    **Expected output (Step 2):**
    - All three stat cards render with the big-serif font.
    - Sparkline shows ≤ 12 bars per row.
    - Opening `/firehose` in a second tab increments `Connected Clients`.
    - With audit traffic flowing, Frames Sent (1m) is > 0.

    Operator notes:
    - All three stat cards visible: [ ] yes / [ ] no
    - Sparkline rendered (two rows of bars): [ ] yes / [ ] no
    - Opening /firehose incremented Connected Clients: [ ] yes / [ ] no
    - Frames Sent (1m) > 0 after 30s of observation: [ ] yes / [ ] no

    ---

    ## Step 3 — Events per Minute by Family sparkline visible (OBS-13)

    1. With Steward `/system` still open, scroll to the **Events per Minute by Family** card (immediately below Firehose Diagnostics).
    2. Confirm an SVG sparkline renders, ~600px wide × ~90px tall, with stacked vertical bars (one bar per 5s × 60 buckets = 5 minutes).
    3. Below the SVG, a family legend renders showing the family names + color swatches (e.g., `nous`, `human`, `portal`, `bios`).
    4. The "X events / 5min" count should be > 0 if there's been audit activity in the last 5 minutes.

    5. **REST-not-WS resilience test (R-34-02):** in the browser DevTools, switch to the Network tab and confirm requests to `/api/v1/audit/trail?limit=200` fire every 5s. Then open another browser tab at `/firehose` and observe — the WS connection there is independent of the sparkline (which uses REST). Close the `/firehose` tab. The sparkline on `/system` keeps updating because it never depended on the WS.

    6. **Optional firehose-down stress test:** kill the WS handler on the Grid (this requires a Grid code change for testing; alternatively skip this test or use the watchdog stall scenario from Step 4 instead). The sparkline should KEEP rendering because it reads `/api/v1/audit/trail`, not the firehose.

    **Expected output (Step 3):**
    - SVG sparkline renders.
    - Family legend shows at least one family.
    - Network tab shows `/api/v1/audit/trail?limit=200` polling at 5s.
    - The card does NOT have a WS connection in the Network tab (REST-only).

    Operator notes:
    - SVG sparkline rendered: [ ] yes / [ ] no
    - Family legend visible: [ ] yes / [ ] no
    - Network tab confirms REST-only (no WS for this card): [ ] yes / [ ] no
    - Events count > 0 after 5 minutes uptime: [ ] yes / [ ] no

    ---

    ## Step 4 — Firehose watchdog forces reconnect (OBS-14)

    Inducing the "WS opens but never delivers" failure mode requires either:
    - **Option A (recommended):** Open `/firehose`, then on the Grid side use `docker exec` to interrupt the WS frame loop. NOTE: this requires a Grid-side test harness — Phase 32 ships `uat-half-close-socket.mjs` per `32-HUMAN-UAT.md` for a related case but not exactly for this. Easier alternative:
    - **Option B (preferred for Phase 34 UAT):** Simulate by manually advancing the Steward client's `last_frame_at` interpretation. Since the watchdog reads from `/health/detailed`, the cleanest test is to wait for a natural quiet period in the audit stream — but that requires waiting for production-grade silence.
    - **Option C (most reliable):** Pause the Grid (via Steward's `/system` Clock Control "Pause Clock" button with a reason like "OBS-14 watchdog UAT"), wait 60+ seconds for `last_frame_at` to grow stale, then observe the connection status pill on `/firehose` flips from "connected" → "disconnected" → "connecting" → "connected" (auto-reconnect via watchdog).

    Use Option C:

    1. Open Steward at `/firehose` in one browser tab. Confirm the status pill reads "connected".
    2. In a second tab, open `/system` and click the **Pause Clock** button (Clock Control card). Enter reason: `OBS-14 watchdog UAT`. Click "Pause Clock".
    3. Wait 75 seconds (60s stale threshold + 15s buffer for the next 5s `/health/detailed` poll).
    4. Switch back to the `/firehose` tab. The status pill should have flipped to "disconnected" then immediately to "connecting", then back to "connected" within 30s.
    5. Resume the clock from `/system` Clock Control. Audit traffic resumes; the firehose receives frames again.

    **Expected output (Step 4):**
    - With clock paused for 75s, `/firehose` status pill shows reconnection cycle.
    - The watchdog effect's suppression window (60s per R-34-03) prevents a tight loop.
    - In the browser DevTools console, no "Maximum update depth exceeded" or "Too many re-renders" errors.

    **If the watchdog does NOT trigger:** Inspect `/health/detailed` directly — `data.firehose.client_count` must be > 0 for the watchdog to fire. If `client_count` is 0 (perhaps the `/firehose` tab lost focus and browser hibernated the WS), close the tab, refresh, and retry.

    Operator notes:
    - /firehose status pill flipped through reconnection cycle: [ ] yes / [ ] no
    - Reconnection completed within 30s after the 75s wait: [ ] yes / [ ] no
    - No console errors during the cycle: [ ] yes / [ ] no

    ---

    ## Step 5 — MySQL outage cutover scenario (Phase 34 ROADMAP SC #5)

    This is the integration test of all four REQs together — the operator's most important manual verification.

    1. Open Steward at `/system` in a browser. Confirm the Audit Pipeline Health card shows green (divergence 0).
    2. In a terminal, induce a MySQL outage:
       ```sh
       docker stop noesis-mysql
       ```
    3. Observe the `/system` page. Within 60 seconds the Audit Pipeline Health card should:
       - Stay at the current divergence number (in-memory chain grows but persistence stalls).
       - The "Last persist error" field should populate with `ECONNREFUSED` (or similar) at a recent timestamp.
       - Eventually the divergence big-number turns amber (1-10) or red (>10) as the in-memory chain pulls ahead.
       - The reasons sub-line should populate with `Audit divergence elevated`, `Persist failing with divergence`, or `Reconcile loop stale` depending on which threshold trips first.
    4. After confirming the red/amber state, restart MySQL:
       ```sh
       docker start noesis-mysql
       ```
    5. Within 60 seconds (one reconcile cycle), the Audit Pipeline Health card should:
       - Return to green (divergence 0).
       - "Last persist error" should clear or become stale (the reconcile loop catches up).
       - reasons sub-line should disappear (empty array).
    6. **No browser refresh required.** The 5s polling drives the updates.

    **Expected output (Step 5):**
    - Card turns amber/red within 60s of `docker stop noesis-mysql`.
    - Card returns to green within 60s of `docker start noesis-mysql`.
    - reasons sub-line populates during the outage and clears on recovery.
    - No browser refresh required at any step.

    Operator notes:
    - Card turned amber/red within 60s of stop: [ ] yes / [ ] no (time observed: ___s)
    - reasons sub-line populated during outage: [ ] yes / [ ] no (text observed: ___________________)
    - Card returned to green within 60s of start: [ ] yes / [ ] no (time observed: ___s)
    - No browser refresh required: [ ] yes / [ ] no

    ---

    ## Document close-out

    On all-PASS:

    1. Create `.planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md` summarizing:
       - Operator name + date.
       - Pass/fail per step (1-5).
       - Any anomalies observed.
    2. Commit + push the VERIFICATION file (per project memory `feedback_push_after_commit.md`).
    3. Update STATE.md `stopped_at` to "Phase 34 UAT complete".
    4. Phase 35 (UAT Re-Verification + Documentation Close-Out) begins.

    On any-FAIL:

    1. File the failure mode in `34-VERIFICATION.md` with as much detail as possible (browser console logs, Network tab waterfall, `docker compose logs grid` snippet).
    2. Plan a gap-closure phase (use `/gsd-plan-phase 34 --gaps`).
    3. DO NOT advance to Phase 35 until the gap closes.

    ---

    *Phase: 34-steward-system-health-surfaces*
    *Playbook: 34-HUMAN-UAT.md*
    ```

    Discipline:
    - DO NOT add Korean translation — this file is documentation, not a chat response (bilingual rule applies to chat only per project memory `feedback_bilingual_responses.md`).
    - DO NOT add steps for features not in OBS-11/12/13/14 scope. Resist scope creep.
    - DO NOT skip the deploy step (`docker compose build` for both grid AND steward) — project memory `feedback_deploy_docker.md` is non-negotiable.
    - DO NOT skip the "no browser refresh required" sub-step in Step 5 — that's the central UX claim of OBS-11/12.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md && wc -l .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md`.
    - File has at least 120 lines: `[ $(wc -l < .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md) -ge 120 ]`.
    - grep -c "^## Step 0 — Deploy" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "^## Step 1 —" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "^## Step 2 —" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "^## Step 3 —" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "^## Step 4 —" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "^## Step 5 —" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → exactly 1.
    - grep -c "docker stop noesis-mysql" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "docker start noesis-mysql" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "docker compose build grid" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "docker compose build steward" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "OBS-11" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "OBS-12" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "OBS-13" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "OBS-14" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1.
    - grep -c "Operator notes" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 5 (one per step).
    - grep -c "No browser refresh required" .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md → at least 1 (central UX claim).
  </acceptance_criteria>
  <done>
    Playbook file exists with Step 0 (deploy) + Steps 1-5 (per-REQ + MySQL outage scenario) + close-out section; operator sign-off lines per step; matches Phase 31/32 structure; references all 4 REQ IDs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Operator runs 34-HUMAN-UAT.md end-to-end on deployed stack</name>
  <files>.planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md (the operator playbook produced by Task 1)
    - .planning/phases/31-audit-pipeline-persistence/31-VERIFICATION.md (structural reference if it exists — verification file shape)
    - .planning/phases/32-firehose-observability/32-HUMAN-UAT.md (structural reference for sign-off conventions)
  </read_first>
  <what-built>
    The Phase 34 code changes (Plans 01-04) shipped:
    - HealthDetailedPayload extended with reasons[] (Plan 01).
    - Steward use-health-detailed hook + health-reason-labels + event-family-colors + ALLOWLIST_STATIC fix (Plan 02).
    - 3 new /system cards (Plan 03).
    - Firehose watchdog (Plan 04).
    - Operator UAT playbook (this plan, Task 1).
  </what-built>
  <how-to-verify>
    1. Ensure all Plan 01-04 code commits are pushed to the active branch.
    2. Open `.planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md` in your editor.
    3. Execute Step 0 (rebuild + restart Grid AND Steward via docker compose).
    4. Execute Steps 1-5 in order, filling in the Operator notes sections inline.
    5. On all-PASS: create `34-VERIFICATION.md` per the Document close-out section.
    6. On any-FAIL: document the failure in `34-VERIFICATION.md` and run `/gsd-plan-phase 34 --gaps`.
    Total expected time: ~15 minutes for the happy path; ~5 minutes more if Step 4 watchdog needs retry.
  </how-to-verify>
  <action>
    OPERATOR ACTION REQUIRED (this task is a human checkpoint — Claude does NOT execute the playbook):

    1. Confirm Plans 01-04 are merged and pushed.
    2. Run the deploy block from `34-HUMAN-UAT.md` Step 0 (docker compose build + up for both grid and steward; sleep 90s).
    3. Walk through `34-HUMAN-UAT.md` Steps 1-5 in order using a browser. Fill in the Operator notes sections inline (yes/no boxes + observed timings).
    4. On all-PASS: create `.planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md` summarizing operator name, date, per-step pass/fail outcomes, any anomalies. Commit + push.
    5. On any-FAIL: document the failure mode in `34-VERIFICATION.md` (console logs, Network waterfall, docker logs snippet) and invoke `/gsd-plan-phase 34 --gaps`.
  </action>
  <verify>
    <automated>test -f .planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md</automated>
  </verify>
  <resume-signal>
    Type "approved" once all 5 UAT steps pass AND `34-VERIFICATION.md` is committed.
    Type "blocked: <description>" if any step fails — this triggers gap-closure planning.
  </resume-signal>
  <acceptance_criteria>
    - File exists: `test -f .planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md` returns 0.
    - grep -c "Step 1" .planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md → at least 1 (each step referenced).
    - grep -c "OBS-11" .planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md → at least 1.
    - grep -c "OBS-14" .planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md → at least 1.
    - The operator commits + pushes the file (commit log shows the verification record).
  </acceptance_criteria>
  <done>
    34-VERIFICATION.md exists with per-step pass/fail outcomes; operator returned "approved" OR a gap-closure phase was invoked on FAIL.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator workstation → Docker daemon | `docker stop noesis-mysql` requires Docker access; operator-controlled, audit responsibility on the operator. |
| Operator browser → Steward + Grid endpoints | All endpoints touched in UAT are read-only or operator-authenticated (Clock pause requires operator role). No new attack surface introduced by the playbook itself. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-16 | Denial of Service | Step 5 deliberately stops MySQL in production | accept | The UAT explicitly runs against the production docker compose stack (per ROADMAP SC #5 wording). The operator restores MySQL within ~60s; in-memory audit chain is preserved (Phase 31 R-31-03 invariant); reconcile loop catches up. Operator-induced outage during scheduled UAT is the established pattern (mirrors Phase 31 31-HUMAN-UAT cutover playbook). |
| T-34-17 | Repudiation | Operator sign-off fields | accept | The playbook records operator decisions inline. For audit purposes, the `34-VERIFICATION.md` file written by the operator is the durable record (filed in git per close-out section). No identity beyond git author is required — solo developer + Claude workflow per project guidelines. |
| T-34-18 | Tampering | Clock pause used as a watchdog test surrogate | accept | Step 4 Option C uses the existing operator clock-pause action (Phase 6 `operator.paused` event, allowlist position 13). The pause is followed by a resume in the same step; no permanent state change. |
</threat_model>

<verification>
1. File exists at `.planning/phases/34-steward-system-health-surfaces/34-HUMAN-UAT.md` with ≥120 lines.
2. All 4 REQ IDs (OBS-11, OBS-12, OBS-13, OBS-14) referenced.
3. Step 0 includes both `docker compose build grid` and `docker compose build steward`.
4. Step 5 covers the MySQL outage cutover scenario with both stop + start.
5. Operator sign-off lines on every step.
6. Checkpoint blocks phase advancement until operator returns "approved".
</verification>

<success_criteria>
- Playbook file created with all 5 steps, deploy block, and close-out section.
- Operator runs the playbook end-to-end and signs off (or gap-files on FAIL).
- Phase 34 advances to UAT-pass state, ready for Phase 35 close-out.
</success_criteria>

<output>
After completion (post-operator-approval), the operator (or executor) creates `.planning/phases/34-steward-system-health-surfaces/34-VERIFICATION.md` per the close-out instructions in the playbook. The plan SUMMARY (`34-05-SUMMARY.md`) captures: playbook file created, line count, checkpoint status (awaiting operator UAT vs approved vs blocked).
</output>
