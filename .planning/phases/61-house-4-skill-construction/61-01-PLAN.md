---
phase: 61
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - grid/test/civic/blueprint.test.ts
  - grid/test/civic/blueprint-skill-held.test.ts
  - grid/test/civic/blueprint-executor.test.ts
  - grid/test/civic/co-build.test.ts
  - grid/test/civic/location-teaching.test.ts
  - grid/test/api/civic-build-routes.test.ts
  - grid/test/audit/append-skill-blueprint-executed.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - grid/test/civic/house-4-e2e.test.ts
autonomous: true
requirements: [R-61-01, R-61-02, R-61-03, R-61-04, R-61-05, R-61-06, R-61-07, R-61-08, R-61-11]
tags: [nous-house, house-4, skill-construction, blueprint, co-build, location-teaching, tdd-scaffold, allowlist-100]

must_haves:
  truths:
    - "Wave 0 skip-stub test files exist for every HOUSE-4 deliverable (blueprint storage, skill-held check, build executor, co-build DAG sessions, location-aware teaching, build route, the 1 sole-producer event, e2e)"
    - "broadcast-allowlist.test.ts moves EVERY .toBe(99) -> .toBe(100) and adds a presence assertion for skill.blueprint_executed; any sibling count suite asserting 99 moves 99->100 in lockstep"
    - "The allowlist suite is EXPECTED-RED at 100 until Wave 3 (61-04) lands the member — all OTHER new stub blocks are describe.skip and pass green"
    - "Stub it-names express the closed tuple (blueprint_executed 4-key {blueprint_hash, builder_civic_did_hash, parcel_id, tick}), the skill-held check via EXISTING skill-event history (ZERO new diffusion), co-build DAG-weighted always-paid attribution, location-teaching present-occupant selector (skill.taught tuple unchanged), humans rejected, single-onTick (no new clock.onTick)"
  artifacts:
    - path: "grid/test/civic/blueprint.test.ts"
      provides: "describe.skip stub: blueprint_hash IS a Phase 18 skill hash; recipe {objects:[{kind, area}], arrangement (DAG), material_cost_bios}; kinds restricted to Phase 59 furniture catalog; non-catalog kind rejected; civic_blueprints store; row write emits nothing on chain"
    - path: "grid/test/civic/blueprint-skill-held.test.ts"
      provides: "describe.skip stub: skill-held check via EXISTING skill.taught/inferred audit-chain history (learner_did === builder for blueprint_hash, mirroring culture.ts); skill_not_held when absent; no new skill store"
    - path: "grid/test/civic/blueprint-executor.test.ts"
      provides: "describe.skip stub: buildFromBlueprint skill-held -> material debit (insufficient 402) -> extendInterior per recipe object -> skill.blueprint_executed; owner OR co-build staff; humans rejected"
    - path: "grid/test/civic/co-build.test.ts"
      provides: "describe.skip stub: recipe arrangement -> atomic sub-task DAG (node = catalog object(s) + edges + weight); each sub-task a Phase 60 board task carrying pay; completion ALWAYS settles (Ousia/IOU, never free); DAG-weighted attribution; reuses zoning.cowork_session; no new allowlist event"
    - path: "grid/test/civic/location-teaching.test.ts"
      provides: "describe.skip stub: optional parcel_id selector; workshop diffuses to present Nous (Phase 58 occupant map) via EXISTING appendSkillTaught/Inferred; skill.taught 5-tuple unchanged; humans present excluded"
    - path: "grid/test/api/civic-build-routes.test.ts"
      provides: "describe.skip stub: POST .../build-from-blueprint owner OR co-build staff; humans rejected; skill_not_held; insufficient funds 402; ROUTE_DID_POLICY coverage"
    - path: "grid/test/audit/append-skill-blueprint-executed.test.ts"
      provides: "describe.skip stub: closed 4-tuple {blueprint_hash, builder_civic_did_hash, parcel_id, tick}; both hashes HEX64; actorDid = builder_civic_did_hash; no recipe/content/raw-DID key"
    - path: "grid/test/audit/broadcast-allowlist.test.ts"
      provides: "UPDATED: every .toBe(99) -> .toBe(100) + a presence assertion for skill.blueprint_executed (EXPECTED-RED until Wave 3)"
      contains: "toBe(100)"
    - path: "grid/test/civic/house-4-e2e.test.ts"
      provides: "describe.skip stub: full HOUSE-4 scenario (learn blueprint -> build-from-blueprint skill-held check -> skill.blueprint_executed; co-build DAG decomposition + DAG-weighted paid/IOU attribution; location teaching diffuses to present Nous; humans rejected; allowlist 100)"
  key_links:
    - from: "Wave 0 stubs"
      to: "Waves 1-5 implementations"
      via: "describe.skip blocks -> describe (un-skip when implementation lands)"
      pattern: "describe(\\.skip)?\\("
    - from: "broadcast-allowlist.test.ts -> 100"
      to: "Wave 3 allowlist member"
      via: "expected-RED until skill.blueprint_executed lands"
      pattern: "toBe\\(100\\)"
---

<objective>
Create Wave 0 test infrastructure for Phase 61 Nous House HOUSE-4. Per GSD Wave 0 discipline, lock every HOUSE-4 contract in skip-stub test files BEFORE any implementation lands. CRITICALLY: move `broadcast-allowlist.test.ts` (plus any sibling count suite asserting 99) from `.toBe(99)` to `.toBe(100)` (with a presence assertion for `skill.blueprint_executed`) so any accidental allowlist drift fails CI. The allowlist suite is **EXPECTED-RED at 100 until Wave 3 (61-04)** lands the member — every other new stub block is `describe.skip` and passes green. Each later wave un-skips the stubs it satisfies.

Output: 8 new skip-stub test files + 1 UPDATED allowlist test (now asserting 100, expected-RED until Wave 3).
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@.planning/phases/61-house-4-skill-construction/61-VALIDATION.md
@grid/src/civic/parcel-registry.ts
@grid/src/civic/types.ts
@grid/src/civic/furniture.ts
@grid/src/civic/founding-law.ts
@grid/src/civic/cowork.ts
@grid/src/civic/credit-ledger.ts
@grid/src/skills/types.ts
@grid/src/skills/appendSkillTaught.ts
@grid/src/api/routes/culture.ts
@grid/src/audit/broadcast-allowlist.ts
@grid/src/audit/append-treasury-parcel-revenue.ts
@grid/test/audit/broadcast-allowlist.test.ts

<interfaces>
<!-- Phase 61 introduces these symbol names. Tests reference them by import path.
     Implementations land in later waves; Wave 0 tests are describe.skip stubs that DESCRIBE the contracts. -->

From grid/src/civic/blueprint.ts (Wave 1/2 creates):
```typescript
export interface BlueprintObject { kind: string; area: string; }      // kind MUST be a Phase 59 furniture-catalog kind
export interface BlueprintNode { node_id: string; objects: BlueprintObject[]; depends_on: string[]; weight: number; }  // arrangement DAG node
export interface BlueprintRecipe { blueprint_hash: string; objects: BlueprintObject[]; arrangement: BlueprintNode[]; material_cost_bios: number; }
export function storeBlueprint(recipe: BlueprintRecipe): void;          // civic_blueprints write-through; emits NO chain event
export function getBlueprint(blueprint_hash: string): BlueprintRecipe | null;
// builder holds the skill? (Wave 1 — uses the EXISTING skill-event history, NOT a new store)
export function builderHoldsSkill(blueprint_hash: string, builderDid: string, deps: SkillHeldDeps): boolean;
// build executor (Wave 2):
export function buildFromBlueprint(addr: string, builderDid: string, blueprint_hash: string, tick: number, deps: BuildDeps): Structure;  // skill_not_held | insufficient_funds | applies recipe -> emits skill.blueprint_executed
```

From grid/src/civic/co-build.ts (Wave 2 creates):
```typescript
export interface CoBuildSession { session_id: string; parcel_id: string; blueprint_hash: string; nodes: BlueprintNode[]; completed: Set<string>; }
export function decomposeRecipe(recipe: BlueprintRecipe): BlueprintNode[];   // the arrangement DAG -> atomic sub-tasks
export function attributionShare(session: CoBuildSession, did: string): number;  // DAG-weighted = Σ(completed-node weights for did) / Σ(all-node weights)
// each sub-task posts to the Phase 60 co-work board (cowork.ts) carrying pay; completion ALWAYS settles (Ousia or IOU, never free)
```

From grid/src/civic/location-teaching.ts (Wave 2 creates):
```typescript
// optional parcel_id selector on teaching; reuses appendSkillTaught/appendSkillInferred verbatim.
export function teachHere(parcel_id: string, teacherDid: string, skill_hash: string, parent_hash: string, tick: number, deps: TeachHereDeps): string[];  // returns the present-Nous learner DIDs taught; humans excluded; skill.taught 5-tuple unchanged
```

From grid/src/audit/append-skill-blueprint-executed.ts (Wave 3 creates — closed 4-tuple):
```typescript
export function appendSkillBlueprintExecuted(audit, { blueprint_hash, builder_civic_did_hash, parcel_id, tick }): AuditEntry;  // actorDid = builder_civic_did_hash; both hashes HEX64
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Civic skip-stubs (blueprint / skill-held / executor / co-build / location-teaching)</name>
  <files>
    grid/test/civic/blueprint.test.ts,
    grid/test/civic/blueprint-skill-held.test.ts,
    grid/test/civic/blueprint-executor.test.ts,
    grid/test/civic/co-build.test.ts,
    grid/test/civic/location-teaching.test.ts
  </files>
  <read_first>
    grid/src/civic/parcel-registry.ts (Phase 59 extendInterior(addr, ownerDid, {area, kind}) — the SINGLE recipe-apply path the executor replays; build()/roleOf()/transferOusia surfaces),
    grid/src/civic/furniture.ts (the Phase 59 closed furniture catalog + isValidFurniture gate — recipe kinds must be catalog kinds),
    grid/src/civic/founding-law.ts (TREASURY_DID + gravity/cost constants — material_cost_bios is debited to TREASURY_DID),
    grid/src/civic/cowork.ts (Phase 60 Cowork Agreement + board post/claim/complete + always-settle path — co-build sub-tasks reuse it),
    grid/src/civic/credit-ledger.ts (Phase 60 recordIou/settleIou/outstandingFor — the IOU fallback for unfunded sub-tasks),
    grid/src/skills/types.ts + grid/src/skills/appendSkillTaught.ts (the EXISTING skill machinery blueprints diffuse through — SKILL_TAUGHT_KEYS 5-tuple stays unchanged),
    grid/src/api/routes/culture.ts (the skill.taught/inferred audit-chain LINEAGE QUERY the skill-held check mirrors),
    .planning/phases/60-house-3-commerce-cowork/60-01-PLAN.md (canonical Wave 0 describe.skip pattern)
  </read_first>
  <behavior>
    Create five vitest files, all using `describe.skip` so the suite passes before later-wave implementation. Each imports the not-yet-existing symbols (acceptable — describe.skip bodies never execute the callsites).

    `blueprint.test.ts` — skip-stub asserts:
    - a `blueprint_hash` IS a Phase 18 skill hash (HEX64); `storeBlueprint(recipe)` write-through stores the Grid-side recipe `{objects:[{kind, area}], arrangement (DAG), material_cost_bios}`; `getBlueprint(hash)` reads it back.
    - the recipe `kind`s are restricted to the Phase 59 closed furniture catalog (`furniture.ts`); a recipe object with a non-catalog kind is REJECTED by the existing validation gate.
    - the `civic_blueprints` row write emits NOTHING on chain (a spy on the audit producers receives nothing — Grid-side recipe storage, mirroring Phase 58 world-creation discipline).
    - diffusion reuses the EXISTING `skill.taught` / `skill.inferred` machinery — the blueprint test imports NO new diffusion producer (zero new diffusion code).

    `blueprint-skill-held.test.ts` — skip-stub asserts:
    - `builderHoldsSkill(blueprint_hash, builderDid, deps)` returns true when a `skill.taught` OR `skill.inferred` with `learner_did === builderDid` for `blueprint_hash` exists in the EXISTING audit-chain history (the same lineage query `culture.ts` uses) — the Brain attests, the Grid confirms.
    - it returns false (and the build route maps to `skill_not_held`) when no such event exists.
    - NO new skill store is created — the check reads the existing skill-event history only.

    `blueprint-executor.test.ts` — skip-stub asserts:
    - `buildFromBlueprint(addr, builderDid, blueprint_hash, tick, deps)` (1) confirms skill-held (else `skill_not_held`), (2) debits `material_cost_bios` from the builder's Ousia → `TREASURY_DID` via `transferOusia` (insufficient → 402 `insufficient_funds`), (3) applies the recipe by replaying `ParcelRegistry.extendInterior(addr, ownerDid, {area, kind})` for each recipe object (the executor is the SINGLE caller).
    - the builder is the owner OR a staff Nous in an active co-build session (Phase 60 role edge / Cowork Agreement); a non-authorized Nous → 403.
    - a `did:civic:noesis:human:*` builder → REJECTED (VOTE-05 / D-NH-07).
    - a successful build emits `skill.blueprint_executed` ONCE (the producer is called once per executed build — wired in Wave 3; here the stub names the contract).

    `co-build.test.ts` — skip-stub asserts:
    - `decomposeRecipe(recipe)` turns the recipe `arrangement` into atomic sub-tasks (each node = catalog object(s) + `depends_on` edges + a `weight`); dependency edges are respected (a node cannot complete before its `depends_on`).
    - each sub-task is posted to the Phase 60 co-work board (a Cowork Agreement task carrying pay); completion ALWAYS settles (Ousia via `transferOusia` when funded, else a Phase 60 `recordIou` — a sub-task completion that pays NOTHING throws; never free, D-NH-06).
    - `attributionShare(session, did)` is DAG-weighted: Σ(completed-node weights for `did`) ÷ Σ(all-node weights) (a participant who completed half the weight gets a 0.5 share).
    - sub-task settlement reuses the Phase 60 `zoning.cowork_session` event + the IOU/Ousia path — NO new allowlist event is added for sub-tasks; on FULL completion the executor applies the assembled recipe + emits the single `skill.blueprint_executed`.

    `location-teaching.test.ts` — skip-stub asserts:
    - `teachHere(parcel_id, teacherDid, skill_hash, parent_hash, tick, deps)` selects the learner set from the Nous PRESENT in the parcel's structure (the Phase 58 in-memory occupant map) — a `workshop` becomes a school.
    - it REUSES the existing `appendSkillTaught` / `appendSkillInferred` producers verbatim (one `skill.taught` per present learner); the `skill.taught` 5-tuple `{learner_did, parent_hash, skill_hash, teacher_did, tick}` is UNCHANGED (NO new payload key; `parcel_id` is a Grid-side selector, off the chain).
    - a present human (`did:civic:noesis:human:*`) is NEVER a learner.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- --reporter=verbose civic/blueprint civic/blueprint-skill-held civic/blueprint-executor civic/co-build civic/location-teaching 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `ls grid/test/civic/blueprint.test.ts grid/test/civic/blueprint-skill-held.test.ts grid/test/civic/blueprint-executor.test.ts grid/test/civic/co-build.test.ts grid/test/civic/location-teaching.test.ts` lists all 5 files
    - `grep -c "describe.skip" grid/test/civic/blueprint.test.ts` >= 1 (and the other 4 likewise)
    - `grep -E "blueprint_hash|material_cost_bios|arrangement|furniture" grid/test/civic/blueprint.test.ts` returns matching lines
    - `grep -E "skill.taught|skill.inferred|learner_did|skill_not_held" grid/test/civic/blueprint-skill-held.test.ts` returns matching lines
    - `grep -E "extendInterior|transferOusia|insufficient_funds|skill.blueprint_executed|did:civic:noesis:human" grid/test/civic/blueprint-executor.test.ts` returns matching lines
    - `grep -E "decomposeRecipe|attributionShare|recordIou|cowork|weight" grid/test/civic/co-build.test.ts` returns matching lines
    - `grep -E "teachHere|present|occupant|appendSkillTaught|did:civic:noesis:human" grid/test/civic/location-teaching.test.ts` returns matching lines
    - `cd grid && npm run test -- civic/blueprint civic/blueprint-skill-held civic/blueprint-executor civic/co-build civic/location-teaching` exits 0 with all blocks skipped
  </acceptance_criteria>
  <done>5 civic skip-stub files exist expressing the blueprint/skill-held/executor/co-build/location-teaching contracts; suite exits 0 with all blocks skipped.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build route + sole-producer skip-stub + e2e stub; UPDATE allowlist to 100 (expected-RED)</name>
  <files>
    grid/test/api/civic-build-routes.test.ts,
    grid/test/audit/append-skill-blueprint-executed.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts,
    grid/test/civic/house-4-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/policy.ts (ROUTE_DID_POLICY tiers: public / civic_did_required / human_visitor),
    grid/src/api/routes/civic-parcels.ts (Phase 58/59/60 route shape the build-from-blueprint route extends; build()/bind-shop/board routes),
    grid/src/audit/append-treasury-parcel-revenue.ts (the sole-producer triad to CLONE — closed-tuple Object.keys().sort() + payloadPrivacyCheck + audit.append; PARCEL_ID_RE),
    grid/src/skills/appendSkillTaught.ts (the skill.* producer precedent — HEX64_RE for skill/blueprint hashes; actorDid discipline),
    grid/src/audit/broadcast-allowlist.ts (ALLOWLIST_MEMBERS at 99, FORBIDDEN_KEY_PATTERN, HEX64 discipline; the skill.* prefix is pre-cleared),
    grid/test/audit/broadcast-allowlist.test.ts (existing 99-member assertions — UPDATE the count to 100)
  </read_first>
  <behavior>
    Create three new skip-stub files and UPDATE the allowlist test. Also UPDATE any sibling suite asserting the count 99→100 in lockstep.

    `civic-build-routes.test.ts` — describe.skip stub covering:
    - `POST .../build-from-blueprint {blueprint_hash}`: builder is the owner OR a staff Nous in an active co-build session; a non-authorized Nous → 403 `not_authorized`; a `did:civic:noesis:human:*` builder → rejected (humans never build).
    - the builder must HOLD the skill → `skill_not_held` (403/422) when the EXISTING skill-event history has no matching `skill.taught`/`skill.inferred`.
    - insufficient Ousia for `material_cost_bios` → 402 `insufficient_funds`.
    - a successful build emits `skill.blueprint_executed` (participants/recipe content never crosses — only the hash/parcel/tick).
    - the route has a `ROUTE_DID_POLICY` entry (`node scripts/check-did-policy-coverage.mjs` will assert coverage in Wave 3).

    `append-skill-blueprint-executed.test.ts` — describe.skip stub asserts the closed 4-tuple `{blueprint_hash, builder_civic_did_hash, parcel_id, tick}`: both hashes HEX64; `parcel_id` PARCEL_ID_RE; `tick` non-negative int; `actorDid = builder_civic_did_hash`; a 5th key throws (closed tuple); no `FORBIDDEN_KEY_PATTERN` key; no raw DID/recipe/sub-task content.

    `house-4-e2e.test.ts` — describe.skip stub naming the full scenario: a Nous learns a `blueprint_hash` via the EXISTING `skill.taught`/`skill.inferred` machinery (no new event) → calls `build-from-blueprint` on an owned parcel → the Grid confirms skill-held from the existing skill-event history → debits `material_cost_bios` → applies the recipe via `extendInterior` → `skill.blueprint_executed {blueprint_hash, builder_civic_did_hash, parcel_id, tick}` on the trail (no raw DID/recipe). A co-build decomposes a recipe into the `arrangement` DAG; each sub-task settles in Ousia or a Phase 60 IOU (never free); attribution is DAG-weighted; `zoning.cowork_session` per session + one `skill.blueprint_executed` on full completion. A skill taught inside a `workshop` diffuses to the present Nous (not absent ones, not humans). A builder who does not hold the skill → `skill_not_held`. A human attempting any build → rejected. Allowlist at 100.

    `broadcast-allowlist.test.ts` — **UPDATE**: move EVERY `.toBe(99)` → `.toBe(100)` (both `ALLOWLIST.size` and `ALLOWLIST_MEMBERS.length` assertions), and ADD a presence assertion for the new member `skill.blueprint_executed`. This suite is **EXPECTED-RED until Wave 3** lands the member in `ALLOWLIST_MEMBERS` — document this expected-RED in the SUMMARY. Do NOT touch events 1–99 ordering. Apply the same 99→100 move to any sibling count suite.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- --reporter=verbose api/civic-build-routes audit/append-skill-blueprint-executed audit/broadcast-allowlist 2>&1 | tail -35</automated>
  </verify>
  <acceptance_criteria>
    - `ls grid/test/api/civic-build-routes.test.ts grid/test/audit/append-skill-blueprint-executed.test.ts grid/test/civic/house-4-e2e.test.ts` lists all 3 files
    - `grep -c "describe.skip" grid/test/api/civic-build-routes.test.ts` >= 1 (and the append stub + e2e likewise)
    - `grep -E "build-from-blueprint|skill_not_held|insufficient_funds|did:civic:noesis:human" grid/test/api/civic-build-routes.test.ts` returns matching lines
    - `grep -E "blueprint_hash|builder_civic_did_hash|parcel_id|tick" grid/test/audit/append-skill-blueprint-executed.test.ts` returns matching lines
    - `grep -c "toBe(99)" grid/test/audit/broadcast-allowlist.test.ts` is 0 (all moved to 100)
    - `grep -c "toBe(100)" grid/test/audit/broadcast-allowlist.test.ts` equals the prior count of toBe(99) lines
    - `grep "skill.blueprint_executed" grid/test/audit/broadcast-allowlist.test.ts` returns the presence assertion
    - any sibling count suite shows 0 remaining `toBe(99)`
    - `cd grid && npm run test -- api/civic-build-routes audit/append-skill-blueprint-executed civic/house-4-e2e` exits 0 (all skipped)
    - `cd grid && npm run test -- audit/broadcast-allowlist` is RED at 100 (expected until Wave 3)
  </acceptance_criteria>
  <done>3 skip-stub files exist; broadcast-allowlist.test.ts (and any sibling count suite) updated to 100 with a presence assertion (expected-RED until Wave 3).</done>
</task>

</tasks>

<success_criteria>
- 8 new skip-stub test files committed under `grid/test/`
- Every new file uses `describe.skip(...)` so Vitest reports them skipped, not failed
- `broadcast-allowlist.test.ts` (and any sibling count suite) moved from 99 → 100 with a presence assertion for `skill.blueprint_executed` (expected-RED until Wave 3 — documented)
- Each later wave (1–5) can cite a pre-existing test file it un-skips
- `cd grid && npm run test -- civic/blueprint civic/blueprint-skill-held civic/blueprint-executor civic/co-build civic/location-teaching api/civic-build-routes audit/append-skill-blueprint-executed civic/house-4-e2e` exits 0 with new blocks skipped
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-01-SUMMARY.md` summarising the 8 new skip-stub files, their coverage of R-61-01..08/11, and the allowlist move to 100 (expected-RED until Wave 3).
</output>
