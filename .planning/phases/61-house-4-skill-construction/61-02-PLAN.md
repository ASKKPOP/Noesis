---
phase: 61
plan: 02
type: execute
wave: 1
depends_on: [61-01]
files_modified:
  - grid/src/db/schema.ts
  - grid/src/civic/blueprint.ts
  - grid/src/civic/blueprint-store.ts
  - grid/src/civic/types.ts
  - grid/test/civic/blueprint.test.ts
  - grid/test/civic/blueprint-skill-held.test.ts
autonomous: true
requirements: [R-61-01, R-61-02, R-61-03]
tags: [nous-house, house-4, persistence, migration-v41, blueprint, skill-held]

must_haves:
  truths:
    - "Migration v41 (next free after shipped v40) creates civic_blueprints (one row per blueprint_hash: recipe body JSON — objects, arrangement DAG, material_cost_bios — grid-scoped); down drops the table; applies cleanly on top of v40"
    - "blueprint.ts holds the recipe type {objects:[{kind, area}], arrangement (DAG), material_cost_bios}; recipe kinds are restricted to the Phase 59 closed furniture catalog (furniture.ts); a non-catalog kind is rejected; the civic_blueprints row write emits NO chain event"
    - "A blueprint_hash IS a Phase 18 skill hash; diffusion reuses the EXISTING skill.taught/skill.inferred machinery (ZERO new diffusion code)"
    - "builderHoldsSkill verifies the builder HOLDS the skill via the EXISTING skill-event history (a skill.taught/skill.inferred with learner_did === builder for blueprint_hash in the audit chain, mirroring culture.ts); no new skill store"
    - "blueprint-store.ts persists/hydrates civic_blueprints DB-first then memory; hydrate() reads it on boot"
    - "NO new clock.onTick subscription anywhere in grid/src (grep stays exactly 1)"
    - "blueprint.test.ts + blueprint-skill-held.test.ts un-skipped and green"
  artifacts:
    - path: "grid/src/db/schema.ts"
      provides: "Migration v41: CREATE civic_blueprints (recipe body JSON keyed by blueprint_hash); down drops it"
    - path: "grid/src/civic/blueprint.ts"
      provides: "BlueprintObject/BlueprintNode/BlueprintRecipe types; storeBlueprint/getBlueprint; catalog-kind validation; builderHoldsSkill via existing skill-event history"
    - path: "grid/src/civic/blueprint-store.ts"
      provides: "persistBlueprint (DB-first) + hydrate reads civic_blueprints"
    - path: "grid/src/civic/types.ts"
      provides: "surgical: blueprint recipe type re-export if needed (no Structure mutation)"
  key_links:
    - from: "grid/src/civic/blueprint.ts recipe kinds"
      to: "grid/src/civic/furniture.ts isValidFurniture catalog gate"
      via: "recipe objects must be catalog kinds; non-catalog kind rejected by the existing gate"
      pattern: "isValidFurniture|FURNITURE_CATALOG"
    - from: "grid/src/civic/blueprint.ts builderHoldsSkill"
      to: "the EXISTING skill.taught/skill.inferred audit-chain history (culture.ts lineage query)"
      via: "learner_did === builder for blueprint_hash exists in the chain; no new skill store"
      pattern: "skill.taught|skill.inferred|learner_did"
    - from: "civic_blueprints v41 table"
      to: "in-memory blueprint cache"
      via: "blueprint-store.hydrate(registry) reads recipes on boot"
      pattern: "hydrate\\(|civic_blueprints"
---

<objective>
Lay the HOUSE-4 storage + skill model. Add migration v41 (CREATE civic_blueprints), the blueprint recipe type (catalog-kind objects + arrangement DAG + material cost) with the Grid-side recipe store, and the skill-held verification that reuses the EXISTING Phase 18 skill-event history (zero new diffusion / no new skill store). Write-through DB-first then memory; hydrate on boot. Un-skip blueprint.test.ts + blueprint-skill-held.test.ts.
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@grid/src/db/schema.ts
@grid/src/civic/types.ts
@grid/src/civic/furniture.ts
@grid/src/civic/parcel-store.ts
@grid/src/skills/types.ts
@grid/src/api/routes/culture.ts

<lesson>
HumanRegistry incident (2026-06-11): an in-memory registry not backed by MySQL lost state on restart. The registry is a READ CACHE; MySQL is the source of truth. The v41 `civic_blueprints` table is written DB-first then mirrored to memory; boot hydrates memory from DB. The blueprint HASH already lives in the Phase 18 skill lineage (the audit chain) — `civic_blueprints` only stores the Grid-side recipe BODY the executor applies; the skill is what DIFFUSES (zero new diffusion code).
</lesson>

<context_note>
**ZERO new diffusion code:** a `blueprint_hash` is a Phase 18 skill hash; blueprints are taught/learned via the EXISTING `appendSkillTaught` / `appendSkillInferred` producers and the `skill.taught` / `skill.inferred` audit-chain history (`grid/src/api/routes/culture.ts` already queries this lineage). HOUSE-4 adds the recipe BODY (`civic_blueprints`) + the skill-HELD check — it does NOT add a diffusion path or event. The skill-held check MIRRORS the `culture.ts` lineage query: a builder holds `blueprint_hash` iff a `skill.taught` OR `skill.inferred` with `learner_did === builder` for that hash exists in the chain (the Brain attests, the Grid confirms). Recipe `kind`s are restricted to the Phase 59 closed furniture catalog — a non-catalog kind is rejected by the EXISTING `furniture.ts` gate (no new furniture kinds). The `civic_blueprints` row write emits NO chain event (Grid-side recipe storage, mirroring Phase 58 world-creation discipline). No new `clock.onTick` — storage + the skill-held check are request-driven.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: migration v41 + blueprint.ts (recipe type + catalog validation + store)</name>
  <files>
    grid/src/db/schema.ts,
    grid/src/civic/blueprint.ts,
    grid/src/civic/blueprint-store.ts,
    grid/src/civic/types.ts,
    grid/test/civic/blueprint.test.ts
  </files>
  <read_first>
    grid/src/db/schema.ts (MIGRATIONS array — latest shipped version is 40 (Phase 60 house3_roles_credit_cowork_bound_shop); v41 is next free; v38 created civic_parcels, v39 added interior/upkeep, v40 added roles/credit/cowork tables),
    grid/src/civic/furniture.ts (FURNITURE_CATALOG + isValidFurniture — recipe kinds must validate against this closed catalog),
    grid/src/civic/types.ts (Interior/InteriorArea/InteriorObject from Phase 59 — the recipe applies to this tree via extendInterior; surgical extension only),
    grid/src/civic/parcel-store.ts (Phase 58/59/60 DB-first persist* + hydrate pattern to MIRROR),
    grid/test/civic/blueprint.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Add migration **v41** to the MIGRATIONS array in `grid/src/db/schema.ts`:
    - `CREATE TABLE IF NOT EXISTS civic_blueprints (blueprint_hash CHAR(64) NOT NULL, grid_name VARCHAR(63) NOT NULL, recipe_json JSON NOT NULL, material_cost_bios BIGINT UNSIGNED NOT NULL, created_tick INT UNSIGNED NOT NULL, PRIMARY KEY (blueprint_hash), INDEX idx_blueprint_grid (grid_name)) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4` — one row per blueprint_hash; `recipe_json` holds the objects + arrangement DAG; `material_cost_bios` is the build debit.
    - a working `down` that DROPs `civic_blueprints`. v41 is the next free version after shipped v40; it must apply cleanly on top of v40.

    Create `grid/src/civic/blueprint.ts`:
    - `export interface BlueprintObject { kind: string; area: string; }` (`kind` MUST be a Phase 59 furniture-catalog kind).
    - `export interface BlueprintNode { node_id: string; objects: BlueprintObject[]; depends_on: string[]; weight: number; }` (the arrangement DAG node — used by co-build in Wave 2).
    - `export interface BlueprintRecipe { blueprint_hash: string; objects: BlueprintObject[]; arrangement: BlueprintNode[]; material_cost_bios: number; }`.
    - `export function validateRecipe(recipe): void` — reject a recipe whose `blueprint_hash` is not HEX64, or whose any object `kind` is not in the Phase 59 catalog (delegate to the EXISTING `furniture.ts` `isValidFurniture` / `FURNITURE_CATALOG` — `recipe_kind_not_in_catalog`). NO new furniture kinds.
    - `export function storeBlueprint(recipe, deps): void` — validate then write-through via `blueprint-store.persistBlueprint` DB-first, mirror to memory; emit NOTHING on chain (Grid-side recipe storage).
    - `export function getBlueprint(blueprint_hash): BlueprintRecipe | null` — read from the in-memory cache (hydrated on boot).

    Create `grid/src/civic/blueprint-store.ts` mirroring the Phase 58/59/60 DB-first pattern:
    - `persistBlueprint(recipe, tick)` — UPSERT `civic_blueprints` DB-first; caller mirrors into memory.
    - `hydrate(cache)` — READ `civic_blueprints` into the in-memory recipe cache on boot.

    Extend `grid/src/civic/types.ts` ONLY if a shared re-export is needed (do NOT mutate `Structure` or `Interior` — the recipe applies via the existing `extendInterior`).

    Un-skip `blueprint.test.ts` (remove `.skip`) and make it green: recipe round-trips through the store; a non-catalog kind is rejected; the row write emits nothing on chain; a blueprint_hash is HEX64.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/blueprint && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "version: 41|civic_blueprints|recipe_json|material_cost_bios" grid/src/db/schema.ts` shows the migration + table
    - migration v41 applies cleanly on top of v40; down drops civic_blueprints
    - `grep -E "BlueprintRecipe|BlueprintNode|validateRecipe|isValidFurniture|FURNITURE_CATALOG" grid/src/civic/blueprint.ts` shows the recipe type + catalog gate
    - `grep -nE "persistBlueprint|hydrate" grid/src/civic/blueprint-store.ts` shows the store writer + hydrate
    - the storeBlueprint path calls NO append-* producer (no chain event)
    - blueprint.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/blueprint` is green (round-trip; non-catalog kind rejected; no chain event)
    - typecheck passes
  </acceptance_criteria>
  <done>Migration v41 creates civic_blueprints; blueprint.ts holds the recipe type with catalog-kind validation + the Grid-side store; the row write emits no chain event; blueprint.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: skill-held check via the EXISTING skill-event history</name>
  <files>
    grid/src/civic/blueprint.ts,
    grid/test/civic/blueprint-skill-held.test.ts
  </files>
  <read_first>
    grid/src/api/routes/culture.ts (the skill.taught/skill.inferred audit-chain LINEAGE QUERY to MIRROR — iterates the chain filtering eventType === 'skill.taught' || 'skill.inferred' and reading learner_did/skill_hash from the payload),
    grid/src/skills/types.ts (SKILL_TAUGHT_KEYS / SKILL_INFERRED_KEYS — learner_did + skill_hash are the fields the check reads),
    grid/src/civic/blueprint.ts (the Task-1 recipe type + store the check sits beside),
    grid/test/civic/blueprint-skill-held.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Extend `grid/src/civic/blueprint.ts` with the skill-held verification — NO new skill store; it reads the EXISTING skill-event history:
    - `export interface SkillHeldDeps { auditChain: { entries(): Iterable<AuditEntry> } }` (or the existing chain-iteration surface `culture.ts` uses).
    - `export function builderHoldsSkill(blueprint_hash, builderDid, deps): boolean` — return true iff a `skill.taught` OR `skill.inferred` audit entry exists with `payload.learner_did === builderDid` AND `payload.skill_hash === blueprint_hash` (the blueprint hash IS the skill hash). This MIRRORS the `culture.ts` lineage query — the Brain attests it learned the blueprint (the `skill.taught`/`skill.inferred` already on chain), the Grid confirms by reading that history. No new event, no new store.
    - return false when no such event exists (the build route maps this to `skill_not_held`).

    Un-skip `blueprint-skill-held.test.ts` and make it green: builderHoldsSkill is true after a matching `skill.taught` (or `skill.inferred`) in the (test-injected) chain; false otherwise; a DIFFERENT learner_did or a DIFFERENT skill_hash does not satisfy it; the check creates no new store and emits no event.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/blueprint-skill-held civic/blueprint 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "builderHoldsSkill|skill.taught|skill.inferred|learner_did|skill_hash" grid/src/civic/blueprint.ts` shows the skill-held check reading the existing history
    - the check imports NO new skill store (reads the audit chain / lineage surface only)
    - blueprint-skill-held.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/blueprint-skill-held civic/blueprint` is green (true on match; false on no-match / wrong learner / wrong hash)
    - `grep -rn "clock.onTick" grid/src | wc -l` stays exactly 1 (single-onTick; the check is request-driven)
  </acceptance_criteria>
  <done>builderHoldsSkill verifies the builder holds the skill via the EXISTING skill.taught/skill.inferred audit-chain history (mirroring culture.ts); no new skill store, no new event; blueprint-skill-held.test.ts green; single-onTick preserved.</done>
</task>

</tasks>

<success_criteria>
- Migration v41 added and reversible; civic_blueprints created (recipe body JSON keyed by blueprint_hash)
- blueprint.ts is the recipe type (catalog-kind objects + arrangement DAG + material cost) with catalog validation + the Grid-side store; the row write emits no chain event
- a blueprint_hash IS a Phase 18 skill hash; diffusion reuses the existing skill machinery (zero new diffusion code)
- builderHoldsSkill verifies skill-held via the EXISTING skill-event history (no new store, no new event)
- blueprint-store.ts persists/hydrates civic_blueprints DB-first
- blueprint.test.ts + blueprint-skill-held.test.ts un-skipped and green; single-onTick preserved (grep clock.onTick == 1)
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-02-SUMMARY.md`.
</output>
