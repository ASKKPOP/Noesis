---
phase: 61
plan: 05
type: execute
wave: 4
depends_on: [61-04]
files_modified:
  - brain/src/noesis_brain/rpc/types.py
  - brain/src/noesis_brain/wire/client.py
  - brain/src/noesis_brain/prompts/system.py
  - brain/test/test_civic_construction_verbs.py
  - brain/test/ananke/test_loader.py
  - dashboard/src/app/worldmap/orbital/page.tsx
  - dashboard/src/components/worldmap/OrbitalGenesisMap.tsx
  - dashboard/test/construction-surfaces.test.tsx
autonomous: true
requirements: [R-61-09, R-61-10]
tags: [nous-house, house-4, brain-verbs, my-places, dashboard, construction-surfaces, capabilities, actiontype-count]

must_haves:
  truths:
    - "ActionType gains learn_blueprint/build_from_blueprint/co_build/teach_here dispatched via GridWireClient CIVIC_LAND_ROUTES to the Wave-3 route(s); capabilities, not scripts (no autoplay)"
    - "The ActionType count assertion in brain/test/ananke/test_loader.py is bumped 44 -> 48 in THIS wave (the orphan that bit 59/60); the comment trail gains a +4 (Phase 61) line"
    - "The my_places / skill prompt block surfaces held blueprints, buildable parcels, and teach-here context so the Nous feels its construction capabilities"
    - "Dashboard /worldmap/orbital: blueprint library panel (held hashes), build panel (build-from-blueprint with skill-held status), co-build DAG board panel (per-node claim/complete + DAG-weighted attribution), teach-here indicator on workshop structures; additive beside the Phase 58/59/60 viewers"
    - "cd brain && .venv/bin/pytest test/test_civic_construction_verbs.py test/ananke/test_loader.py green; cd dashboard && npm run test:unit -- construction-surfaces green"
  artifacts:
    - path: "brain/src/noesis_brain/rpc/types.py"
      provides: "ActionType members learn_blueprint/build_from_blueprint/co_build/teach_here"
    - path: "brain/src/noesis_brain/wire/client.py"
      provides: "GridWireClient CIVIC_LAND_ROUTES dispatch of the 4 new verbs"
    - path: "brain/src/noesis_brain/prompts/system.py"
      provides: "my_places / skill prompt enriched with held blueprints / buildable parcels / teach-here"
    - path: "brain/test/ananke/test_loader.py"
      provides: "ActionType count assertion bumped 44 -> 48"
    - path: "dashboard/src/components/worldmap/OrbitalGenesisMap.tsx"
      provides: "blueprint library + build panel + co-build DAG board + teach-here indicator (additive)"
  key_links:
    - from: "brain ActionType verbs"
      to: "Wave-3 build-from-blueprint route (+ existing skill.taught / board routes)"
      via: "GridWireClient dispatch — capabilities, not scripts (no autoplay)"
      pattern: "learn_blueprint|build_from_blueprint|co_build|teach_here"
    - from: "brain/test/ananke/test_loader.py count assertion"
      to: "the 4 new ActionType members"
      via: "len(list(ActionType)) bumped 44 -> 48 in the SAME wave (orphan guard)"
      pattern: "len\\(list\\(ActionType\\)\\) == 48"
    - from: "dashboard construction surfaces"
      to: "Phase 58/59/60 viewers"
      via: "additive panels on /worldmap/orbital (existing views unchanged)"
      pattern: "OrbitalGenesisMap"
---

<objective>
Surface the HOUSE-4 capabilities to the Nous and to humans: Brain verbs (learn_blueprint / build_from_blueprint / co_build / teach_here) as capabilities with my_places / skill prompt enrichment (held blueprints, buildable parcels, teach-here), and dashboard panels (blueprint library, build panel, co-build DAG board, teach-here indicator) additive on /worldmap/orbital. CRITICALLY: bump the ActionType count assertion 44 → 48 in the SAME wave (the orphan that bit 59/60).
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@brain/src/noesis_brain/rpc/types.py
@brain/src/noesis_brain/wire/client.py
@brain/src/noesis_brain/prompts/system.py
@brain/test/ananke/test_loader.py
@dashboard/src/app/worldmap/orbital/page.tsx
@dashboard/src/components/worldmap/OrbitalGenesisMap.tsx

<context_note>
**Capabilities, not scripts (no autoplay):** the 4 new verbs are added to `ActionType` and dispatched through `GridWireClient` `CIVIC_LAND_ROUTES` — decision pressure stays with the Nous; no autoplay loop fires them. `learn_blueprint` rides the EXISTING skill machinery (it is a teach/learn capability — it does NOT add a new diffusion route); `build_from_blueprint` → the Wave-3 `:id/build-from-blueprint` route; `co_build` → the Phase 60 board routes (claim/complete a co-build sub-task); `teach_here` → the teach route carrying the optional `parcel_id` selector. The `my_places` / skill prompt block is enriched so the Nous FEELS its construction capabilities (held blueprints, which parcels it can build on, where it can teach). **ActionType count is currently 44** (`brain/test/ananke/test_loader.py:70` asserts `len(list(ActionType)) == 44`); adding 4 verbs makes it **48** — the assertion MUST be updated to 48 in THIS wave (an unupdated count assertion is the orphan that bit 59/60). The dashboard surfaces are ADDITIVE — the Phase 58 exterior, Phase 59 interior, and Phase 60 commerce surfaces stay unchanged. Test env: brain = `python3.12 .venv` + `.venv/bin/pytest test/`; dashboard = `npm run test:unit` + plugin-react-swc for vitest-4 JSX.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brain verbs (4 capabilities) + my_places enrichment + ActionType count 44->48</name>
  <files>
    brain/src/noesis_brain/rpc/types.py,
    brain/src/noesis_brain/wire/client.py,
    brain/src/noesis_brain/prompts/system.py,
    brain/test/test_civic_construction_verbs.py,
    brain/test/ananke/test_loader.py
  </files>
  <read_first>
    brain/src/noesis_brain/rpc/types.py (ActionType enum — the Phase 58/59/60 civic-land verbs precedent + build_civic_land_action schema gate; the SKILL_TAUGHT verb learn_blueprint relates to),
    brain/src/noesis_brain/wire/client.py (CIVIC_LAND_ROUTES dict — add the 4 new verbs -> their routes),
    brain/src/noesis_brain/prompts/system.py (build_my_places_section — the my_places block to enrich),
    brain/test/ananke/test_loader.py (the ActionType count assertion at ~line 70 — currently 44; bump to 48 + add the +4 Phase 61 comment line),
    brain/test/test_civic_commerce_verbs.py (the Phase 60 verb-test precedent to MIRROR)
  </read_first>
  <behavior>
    `brain/src/noesis_brain/rpc/types.py` — add 4 `ActionType` members with the Phase 58/59/60 comment discipline (capabilities, metadata shapes):
    - `LEARN_BLUEPRINT = "learn_blueprint"` — Metadata: `{blueprint_hash}` (rides the EXISTING skill machinery; the Nous learns the blueprint skill).
    - `BUILD_FROM_BLUEPRINT = "build_from_blueprint"` — `-> :id/build-from-blueprint`. Metadata: `{parcel, blueprint_hash}`.
    - `CO_BUILD = "co_build"` — `-> :id/board/...` (claim/complete a co-build sub-task). Metadata: `{parcel, node_id}`.
    - `TEACH_HERE = "teach_here"` — Metadata: `{parcel, skill_hash}` (teach in the present structure — the parcel_id selector).
    Update `build_civic_land_action` (the schema gate) to validate the 4 new metadata shapes.

    `brain/src/noesis_brain/wire/client.py` — add the 4 verbs to `CIVIC_LAND_ROUTES` mapping each to its route (`BUILD_FROM_BLUEPRINT → POST :id/build-from-blueprint`; `CO_BUILD → POST :id/board/claim` or `/complete` per metadata; `LEARN_BLUEPRINT` + `TEACH_HERE` ride the existing skill/teach dispatch). Capabilities, not scripts — no autoplay.

    `brain/src/noesis_brain/prompts/system.py` — enrich the `my_places` / skill block so the Nous surfaces: the blueprint hashes it HOLDS, the parcels it can BUILD on (owned/co-build), and the teach-here context (where it stands). No autoplay — context only.

    `brain/test/ananke/test_loader.py` — bump the count assertion `len(list(ActionType)) == 44` → `== 48` and add a `+ 4 (Phase 61: LEARN_BLUEPRINT, BUILD_FROM_BLUEPRINT, CO_BUILD, TEACH_HERE) = 48` line to the comment trail.

    Create `brain/test/test_civic_construction_verbs.py` mirroring `test_civic_commerce_verbs.py`: the 4 verbs build valid Actions via `build_civic_land_action`; each dispatches to its route; metadata validation rejects malformed shapes; the verbs are capabilities (no autoplay).
  </behavior>
  <verify>
    <automated>cd brain && .venv/bin/pytest test/test_civic_construction_verbs.py test/ananke/test_loader.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "LEARN_BLUEPRINT|BUILD_FROM_BLUEPRINT|CO_BUILD|TEACH_HERE" brain/src/noesis_brain/rpc/types.py` shows the 4 members
    - `grep -nE "BUILD_FROM_BLUEPRINT|CO_BUILD|build-from-blueprint" brain/src/noesis_brain/wire/client.py` shows the dispatch
    - `grep -nE "blueprint|teach" brain/src/noesis_brain/prompts/system.py` shows the my_places enrichment
    - `grep -n "len(list(ActionType)) == 48" brain/test/ananke/test_loader.py` shows the bumped count
    - `cd brain && .venv/bin/pytest test/test_civic_construction_verbs.py test/ananke/test_loader.py` is green
  </acceptance_criteria>
  <done>4 construction verbs added as capabilities + dispatched via GridWireClient; my_places enriched with held blueprints / buildable parcels / teach-here; ActionType count bumped 44 -> 48; brain tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard construction surfaces (additive)</name>
  <files>
    dashboard/src/app/worldmap/orbital/page.tsx,
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx,
    dashboard/test/construction-surfaces.test.tsx
  </files>
  <read_first>
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx (the Phase 58/59/60 orbital map + exterior/interior/commerce surfaces — add ADDITIVE panels, do not rewrite),
    dashboard/src/app/worldmap/orbital/page.tsx (the orbital page composition),
    dashboard/test/commerce-surfaces.test.tsx (the Phase 60 dashboard-surface test precedent to MIRROR — plugin-react-swc for vitest-4 JSX)
  </read_first>
  <behavior>
    Add ADDITIVE construction surfaces to `dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` (+ wire into `page.tsx`):
    - a blueprint library panel listing the held blueprint hashes (short-hash labels).
    - a build panel surfacing build-from-blueprint with the skill-held status (held / not-held) + the material cost.
    - a co-build DAG board panel showing the sub-task DAG (per-node claim/complete state + the DAG-weighted attribution shares).
    - a teach-here indicator on `workshop` structures (this structure is a school).
    All ADDITIVE — the Phase 58 exterior, Phase 59 interior viewer, and Phase 60 commerce surfaces (shop badge / roles / board / IOU strip) stay unchanged.

    Create `dashboard/test/construction-surfaces.test.tsx` mirroring `commerce-surfaces.test.tsx`: the blueprint library renders held hashes; the build panel shows skill-held status; the co-build DAG board renders nodes + attribution; the teach-here indicator appears on workshops; the existing surfaces are untouched.
  </behavior>
  <verify>
    <automated>cd dashboard && npm run test:unit -- construction-surfaces 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "blueprint|co-build|cobuild|teach|attribution" dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` shows the new panels
    - the Phase 58/59/60 surfaces are unchanged (additive only)
    - `cd dashboard && npm run test:unit -- construction-surfaces` is green
  </acceptance_criteria>
  <done>Dashboard /worldmap/orbital gains the blueprint library, build panel, co-build DAG board, and teach-here indicator (additive beside the Phase 58/59/60 viewers); construction-surfaces.test.tsx green.</done>
</task>

</tasks>

<success_criteria>
- ActionType gains learn_blueprint / build_from_blueprint / co_build / teach_here dispatched via GridWireClient as capabilities (no autoplay)
- the ActionType count assertion bumped 44 -> 48 in THIS wave; brain tests green
- my_places / skill prompt surfaces held blueprints / buildable parcels / teach-here
- dashboard /worldmap/orbital gains the blueprint library, build panel, co-build DAG board, and teach-here indicator (additive)
- test_civic_construction_verbs.py + construction-surfaces.test.tsx green
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-05-SUMMARY.md`.
</output>
