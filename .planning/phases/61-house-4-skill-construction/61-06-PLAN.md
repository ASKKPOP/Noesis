---
phase: 61
plan: 06
type: execute
wave: 5
depends_on: [61-04, 61-05]
files_modified:
  - grid/test/civic/house-4-e2e.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - scripts/check-sole-producer-discipline.mjs
  - scripts/check-wallclock-forbidden.mjs
  - scripts/check-civic-did-issuance-path.mjs
  - scripts/check-cross-house-injection.mjs
autonomous: true
requirements: [R-61-11]
tags: [nous-house, house-4, e2e, gates, definition-of-done]

must_haves:
  truths:
    - "End-to-end: a Nous learns a blueprint via the EXISTING skill.taught/skill.inferred machinery (no new event) -> calls build-from-blueprint on an owned parcel -> the Grid confirms skill-held from the existing skill-event history -> debits material_cost_bios -> applies the recipe via extendInterior -> skill.blueprint_executed {blueprint_hash, builder_civic_did_hash, parcel_id, tick} on the trail (no raw DID/recipe)"
    - "End-to-end: a co-build decomposes a recipe into the arrangement DAG; each sub-task settles in Ousia or a Phase 60 IOU (never free); attribution is DAG-weighted; zoning.cowork_session per session + ONE skill.blueprint_executed on full completion"
    - "End-to-end: a skill taught inside a workshop diffuses to the present Nous (not absent ones, not humans); a builder who does not hold the skill -> skill_not_held; a human attempting any build -> rejected"
    - "No recipe body / sub-task content / teaching location on any payload — only blueprint_hash / hashed builder / parcel_id / tick; all DIDs HEX64"
    - "broadcast-allowlist 100; sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, cross-house-injection, privacy walker, zero-diff R-31-01 all exit 0; grep clock.onTick == 1; no remaining describe.skip in Phase 61 test files"
  artifacts:
    - path: "grid/test/civic/house-4-e2e.test.ts"
      provides: "Definition-of-Done E2E: learn blueprint -> build-from-blueprint skill-held check -> skill.blueprint_executed; co-build DAG decomposition + DAG-weighted paid/IOU attribution; location teaching diffuses to present Nous; humans rejected; allowlist 100"
  key_links:
    - from: "house-4-e2e build completion"
      to: "skill.blueprint_executed + (extendInterior recipe apply)"
      via: "skill-held check -> material debit -> recipe apply -> ONE emit; hashed builder only on chain"
      pattern: "skill.blueprint_executed|extendInterior|builderHoldsSkill"
    - from: "all Phase 61 test files"
      to: "0 remaining describe.skip"
      via: "every Wave un-skipped its stubs"
      pattern: "describe\\.skip"
---

<objective>
Prove the Definition of Done and keep every CI gate green. The HOUSE-4 acceptance scenario: a Nous learns a blueprint (via the existing skill machinery), builds from it (skill-held check → material debit → recipe apply, emitting `skill.blueprint_executed`), co-builds a recipe by DAG-decomposing it into paid sub-tasks with DAG-weighted attribution (Ousia or IOU — never free), and teaches a blueprint inside a workshop that diffuses to the present Nous — while humans cannot build/co-build/teach and no recipe body / sub-task content / teaching location ever crosses the chain.
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@.planning/phases/61-house-4-skill-construction/61-VALIDATION.md
@grid/src/civic/blueprint.ts
@grid/src/civic/co-build.ts
@grid/src/civic/location-teaching.ts
@grid/src/api/routes/civic-parcels.ts
@grid/src/audit/append-skill-blueprint-executed.ts
@grid/src/audit/broadcast-allowlist.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOUSE-4 end-to-end Definition-of-Done test</name>
  <files>
    grid/test/civic/house-4-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (build-from-blueprint + board routes),
    grid/src/civic/blueprint.ts (buildFromBlueprint + builderHoldsSkill + the recipe store),
    grid/src/civic/co-build.ts (decomposeRecipe / claimSubTask / completeSubTask / attributionShare — the DAG-weighted paid path),
    grid/src/civic/location-teaching.ts (teachHere — present-occupant diffusion),
    grid/src/skills/appendSkillTaught.ts (the EXISTING skill machinery blueprints diffuse through),
    grid/src/audit/append-skill-blueprint-executed.ts (the payload shape for the trail assertion)
  </read_first>
  <behavior>
    Un-skip + author `grid/test/civic/house-4-e2e.test.ts` exercising the HOUSE-4 acceptance scenario end-to-end against a seeded Genesis Core (build on the Phase 58/59/60 buy→build→furnish→co-work path):
    - a Nous LEARNS a `blueprint_hash` via the EXISTING `skill.taught` (or `skill.inferred`) machinery (no new event); the Grid-side `civic_blueprints` row holds the recipe (catalog-kind objects + arrangement DAG + `material_cost_bios`).
    - the Nous calls `build-from-blueprint {blueprint_hash}` on an owned parcel → the Grid confirms skill-held from the EXISTING skill-event history → debits `material_cost_bios` (Ousia → `TREASURY_DID`) → applies the recipe via `extendInterior` for each object → `skill.blueprint_executed {blueprint_hash, builder_civic_did_hash, parcel_id, tick}` on the trail (no raw DID, no recipe body).
    - a builder who does NOT hold the skill → `skill_not_held`; insufficient Ousia → 402.
    - a CO-BUILD decomposes a recipe into its `arrangement` DAG; two worker Nous claim + complete sub-tasks. **Funded** → `transferOusia` per sub-task; **unfunded** → a Phase 60 `recordIou` (never free). Attribution is DAG-weighted (`attributionShare` = Σ completed-node weights ÷ Σ all-node weights). The trail shows `zoning.cowork_session` per session (participants_hash only) and ONE `skill.blueprint_executed` on full completion.
    - a skill taught inside a `workshop` (`teachHere`) diffuses to the present Nous (each gets `skill.taught` with the unchanged 5-tuple) and NOT to an absent Nous; a present human is NOT a learner.
    - a HUMAN (`did:civic:noesis:human:*`) attempting any build → rejected.
    - assert no recipe body, no sub-task content, no teaching location anywhere in the payloads (only `blueprint_hash` / hashed builder / `parcel_id` / `tick`; all DIDs HEX64).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/house-4-e2e 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test -- civic/house-4-e2e` is green
    - the test asserts the new event on the trail (skill.blueprint_executed) + the skill-held check, the skill_not_held + insufficient_funds cases, the co-build DAG decomposition + DAG-weighted paid/IOU attribution, the location-teaching present-vs-absent-vs-human diffusion, and the human-rejected-from-build case
    - the test confirms no recipe body / sub-task content / teaching location on any payload and all DIDs HEX64
  </acceptance_criteria>
  <done>house-4-e2e.test.ts green: learn blueprint -> build-from-blueprint skill-held -> skill.blueprint_executed; co-build DAG paid/IOU DAG-weighted attribution; location teaching diffuses to present Nous; humans rejected; no content on the chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: all gates green + all Phase 61 skip-stubs un-skipped</name>
  <files>
    grid/test/audit/broadcast-allowlist.test.ts,
    scripts/check-sole-producer-discipline.mjs,
    scripts/check-wallclock-forbidden.mjs,
    scripts/check-civic-did-issuance-path.mjs,
    scripts/check-cross-house-injection.mjs
  </files>
  <read_first>
    grid/test/audit/broadcast-allowlist.test.ts (must assert 100; do NOT weaken),
    scripts/check-sole-producer-discipline.mjs (must stay green — read-only confirmation),
    scripts/check-wallclock-forbidden.mjs (must stay green — read-only confirmation),
    scripts/check-civic-did-issuance-path.mjs (must stay green — read-only confirmation),
    scripts/check-cross-house-injection.mjs (the Wave-3-extended gate — must stay green)
  </read_first>
  <behavior>
    Verify (do NOT weaken) every CI gate:
    - `broadcast-allowlist.test.ts` GREEN at 100 (delta +1 confirmed) with the presence assertion for `skill.blueprint_executed`; any sibling count suite GREEN at 100.
    - `scripts/check-sole-producer-discipline.mjs` passes (one producer for `skill.blueprint_executed`; no foreign `audit.append`).
    - the privacy walker passes (no payload key matches `FORBIDDEN_KEY_PATTERN`; recipe body / sub-task content / teaching location never broadcast).
    - `scripts/check-cross-house-injection.mjs` passes (co-build / blueprint content is DATA never instructions).
    - `scripts/check-civic-did-issuance-path.mjs` and `scripts/check-wallclock-forbidden.mjs` pass unchanged.
    - zero-diff (R-31-01) holds — no chain / persistence-of-chain code was edited; the 1 event rides a new standard sole producer.
    - single-onTick invariant preserved — `grep -rn "clock.onTick" grid/src` shows exactly one subscription.
    - the full grid suite is green; all Wave-0 skip-stubs from 61-01 are now un-skipped — `grep -c describe.skip` across the Phase 61 test files is 0.

    These gate scripts are listed in files_modified only as touch-points; if they pass unchanged, do not edit them. If a gate fails, the fix belongs in the offending earlier wave's source, NOT in the gate (never weaken a gate to pass).
  </behavior>
  <verify>
    <automated>cd grid && npm run test; node scripts/check-sole-producer-discipline.mjs; node scripts/check-wallclock-forbidden.mjs; node scripts/check-civic-did-issuance-path.mjs; node scripts/check-cross-house-injection.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test` is fully green
    - `cd grid && npm run test -- audit/broadcast-allowlist` GREEN at 100 with the presence assertion
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
    - `node scripts/check-civic-did-issuance-path.mjs` exits 0
    - `node scripts/check-cross-house-injection.mjs` exits 0
    - `grep -rn "clock.onTick" grid/src | wc -l` shows exactly 1 (single-onTick)
    - `grep -rc "describe.skip" grid/test/civic/blueprint.test.ts grid/test/civic/blueprint-skill-held.test.ts grid/test/civic/blueprint-executor.test.ts grid/test/civic/co-build.test.ts grid/test/civic/location-teaching.test.ts grid/test/api/civic-build-routes.test.ts grid/test/audit/append-skill-blueprint-executed.test.ts grid/test/civic/house-4-e2e.test.ts | grep -v ':0$'` returns nothing (no remaining skips in Phase 61 test files)
    - no chain/persistence-of-chain file appears in the phase diff (zero-diff R-31-01)
  </acceptance_criteria>
  <done>All CI gates green; allowlist at 100 (+1); single-onTick preserved; cross-house-injection gate green; every Phase 61 skip-stub un-skipped; zero-diff holds.</done>
</task>

</tasks>

<success_criteria>
- End-to-end DoD passes: learn blueprint -> build-from-blueprint skill-held check -> skill.blueprint_executed; co-build DAG decomposition with paid/IOU DAG-weighted attribution; location-aware teaching diffuses to present Nous; humans rejected; no content on the chain
- broadcast-allowlist GREEN at 100 (+1); sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, cross-house-injection, privacy walker, zero-diff (R-31-01) all green and unchanged
- single-onTick invariant preserved (exactly one clock.onTick in grid/src)
- No remaining describe.skip in any Phase 61 test file
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-06-SUMMARY.md` and the phase-completion report. This is the FINAL Nous House phase — note the HOUSE total of +9 allowlist events (HOUSE-1 +0, HOUSE-2 +4, HOUSE-3 +4, HOUSE-4 +1) in the completion report.
</output>
