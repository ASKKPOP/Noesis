---
phase: 61
plan: 04
type: execute
wave: 3
depends_on: [61-03]
files_modified:
  - grid/src/audit/append-skill-blueprint-executed.ts
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/civic/blueprint.ts
  - grid/src/api/routes/civic-parcels.ts
  - grid/src/api/policy.ts
  - scripts/check-cross-house-injection.mjs
  - grid/test/audit/append-skill-blueprint-executed.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - grid/test/api/civic-build-routes.test.ts
autonomous: true
requirements: [R-61-04, R-61-07, R-61-08]
tags: [nous-house, house-4, sole-producer, allowlist-100, build-route, cross-house-injection, privacy]

must_haves:
  truths:
    - "append-skill-blueprint-executed.ts clones the append-treasury-parcel-revenue / appendSkillTaught triad: closed-tuple Object.keys().sort() check, explicit no-spread reconstruction, payloadPrivacyCheck gate, single audit.append"
    - "Closed tuple exactly: skill.blueprint_executed {blueprint_hash, builder_civic_did_hash, parcel_id, tick} (both hashes HEX64; parcel_id PARCEL_ID_RE; tick non-negative int; actorDid = builder_civic_did_hash)"
    - "broadcast-allowlist.ts grows 99->100 under the pre-cleared skill.* prefix with exactly ONE new member; broadcast-allowlist.test.ts GREEN at 100 (size + length) with the presence assertion; events 1-99 untouched/unreordered"
    - "POST .../build-from-blueprint wired with a ROUTE_DID_POLICY entry; owner OR co-build staff; humans rejected; skill_not_held; insufficient_funds 402; the build emits skill.blueprint_executed exactly once via the Wave-2 executor seam"
    - "scripts/check-cross-house-injection.mjs extended to cover the co-build / blueprint paths (co-build board content + recipe arrangement is DATA never instructions); node scripts/check-sole-producer-discipline.mjs + node scripts/check-did-policy-coverage.mjs + the cross-house gate all exit 0"
  artifacts:
    - path: "grid/src/audit/append-skill-blueprint-executed.ts"
      provides: "sole producer for skill.blueprint_executed (closed 4-tuple; actorDid = builder_civic_did_hash; both hashes HEX64)"
    - path: "grid/src/audit/broadcast-allowlist.ts"
      provides: "ALLOWLIST_MEMBERS grown 99->100 under skill.* with a sole-producer reference comment"
    - path: "grid/src/api/routes/civic-parcels.ts"
      provides: "POST .../build-from-blueprint route calling the Wave-2 executor + the new producer"
    - path: "grid/src/api/policy.ts"
      provides: "ROUTE_DID_POLICY entry for build-from-blueprint"
    - path: "scripts/check-cross-house-injection.mjs"
      provides: "EXTENDED CI gate (A11e): co-build / blueprint content is DATA never instructions"
  key_links:
    - from: "the append-skill-blueprint-executed.ts producer"
      to: "grid/src/audit/broadcast-allowlist.ts ALLOWLIST_MEMBERS (99->100)"
      via: "the event is allowlisted under its pre-cleared skill.* prefix"
      pattern: "skill.blueprint_executed"
    - from: "grid/src/api/routes/civic-parcels.ts build-from-blueprint"
      to: "grid/src/civic/blueprint.ts buildFromBlueprint + append-skill-blueprint-executed.ts"
      via: "the route calls the Wave-2 executor seam which emits the single producer"
      pattern: "buildFromBlueprint|appendSkillBlueprintExecuted"
    - from: "grid/src/api/routes/civic-parcels.ts build-from-blueprint"
      to: "grid/src/api/policy.ts ROUTE_DID_POLICY"
      via: "the new route has a DID-policy entry (check-did-policy-coverage gate)"
      pattern: "ROUTE_DID_POLICY"
---

<objective>
Land the single sole-producer audit producer and grow the broadcast allowlist 99→100 (turning the Wave-0 expected-RED suite GREEN), wire the build-from-blueprint route that emits it (calling the Wave-2 executor seam), and extend the cross-house-injection gate (A11e) to cover the co-build / blueprint paths. Un-skip append-skill-blueprint-executed.test.ts + civic-build-routes.test.ts; broadcast-allowlist goes green at 100.
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@grid/src/audit/append-treasury-parcel-revenue.ts
@grid/src/skills/appendSkillTaught.ts
@grid/src/audit/broadcast-allowlist.ts
@grid/src/civic/blueprint.ts
@grid/src/api/routes/civic-parcels.ts
@grid/src/api/policy.ts
@scripts/check-cross-house-injection.mjs

<context_note>
**Sole-producer discipline:** the new producer CLONES the `append-treasury-parcel-revenue.ts` / `appendSkillTaught.ts` triad EXACTLY — a closed-tuple `Object.keys(payload).sort()` check (a foreign key throws), explicit no-spread reconstruction of the payload, a `payloadPrivacyCheck` gate, and a SINGLE `audit.append` callsite (no other file may call `audit.append` for `skill.blueprint_executed`). `check-sole-producer-discipline.mjs` enforces one producer per event. The closed tuple is ALPHABETICAL. **Privacy walker:** no payload key matches `FORBIDDEN_KEY_PATTERN` (body/session_id/text/content); the builder DID is hashed HEX64; `blueprint_hash` is a HEX64 skill hash; the recipe body + sub-task content NEVER cross. **The `skill.*` prefix is pre-cleared** (Phase 18 added `skill.taught`/`skill.inferred`/`skill.rejected`); this wave moves the allowlist 99→100 with exactly ONE new `skill.*` member. **The cross-house-injection gate (A11e) already exists** (Phase 60) — this wave EXTENDS its file-walk to cover `grid/src/civic/co-build.ts` + `grid/src/civic/blueprint.ts`: co-build board content + the recipe `arrangement` is DATA, never instructions (no concatenation into a Telos/Charter directive).
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: append-skill-blueprint-executed.ts + allowlist 99->100 (suite turns GREEN)</name>
  <files>
    grid/src/audit/append-skill-blueprint-executed.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/test/audit/append-skill-blueprint-executed.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts
  </files>
  <read_first>
    grid/src/audit/append-treasury-parcel-revenue.ts (the triad to CLONE — closed-tuple Object.keys().sort(), no-spread reconstruction, payloadPrivacyCheck, single audit.append; PARCEL_ID_RE),
    grid/src/skills/appendSkillTaught.ts (the skill.* producer precedent — HEX64_RE for the blueprint hash; the actorDid + closed-tuple discipline),
    grid/src/audit/broadcast-allowlist.ts (ALLOWLIST_MEMBERS at 99, FORBIDDEN_KEY_PATTERN, HEX64 discipline — append ONE under skill.*),
    grid/test/audit/broadcast-allowlist.test.ts (Wave 0 updated to 100 — turns GREEN here),
    scripts/check-sole-producer-discipline.mjs (the gate the producer must satisfy)
  </read_first>
  <behavior>
    Create `grid/src/audit/append-skill-blueprint-executed.ts`, cloning the `append-treasury-parcel-revenue.ts` triad EXACTLY:
    - `appendSkillBlueprintExecuted(audit, { blueprint_hash, builder_civic_did_hash, parcel_id, tick })`: closed 4-tuple (ALPHABETICAL keys); validate `blueprint_hash` HEX64 (it is the skill hash), `builder_civic_did_hash` HEX64, `parcel_id` PARCEL_ID_RE, `tick` non-negative int; no-spread reconstruction; `payloadPrivacyCheck` gate; single `audit.append('skill.blueprint_executed', actorDid, cleanPayload)` with `actorDid = builder_civic_did_hash`.

    `grid/src/audit/broadcast-allowlist.ts` — append ONE member `skill.blueprint_executed` to `ALLOWLIST_MEMBERS` (99 → 100) under the pre-cleared `skill.*` prefix with a sole-producer reference comment. Do NOT touch events 1–99 ordering.

    Un-skip `append-skill-blueprint-executed.test.ts` + confirm `broadcast-allowlist.test.ts` (Wave 0) now passes at 100 with the presence assertion. The append suite asserts the closed tuple (a foreign key throws), the value-format validators, `actorDid = builder_civic_did_hash`, and the privacy properties (no `FORBIDDEN_KEY_PATTERN` key; both hashes HEX64; no raw DID/recipe/sub-task content).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- audit/append-skill-blueprint-executed audit/broadcast-allowlist && node scripts/check-sole-producer-discipline.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - append-skill-blueprint-executed.ts exists with a closed-tuple `Object.keys(payload).sort()` check + no-spread reconstruction + `payloadPrivacyCheck` + single `audit.append`
    - `grep -E "skill.blueprint_executed" grid/src/audit/broadcast-allowlist.ts` shows the ONE new member
    - `cd grid && npm run test -- audit/broadcast-allowlist` GREEN at 100 (`ALLOWLIST.size === 100` + `ALLOWLIST_MEMBERS.length === 100`) with the presence assertion
    - append-skill-blueprint-executed.test.ts has no remaining `describe.skip` and is green
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
    - events 1–99 untouched/unreordered
  </acceptance_criteria>
  <done>The skill.blueprint_executed sole-producer triad landed; allowlist 99->100; broadcast-allowlist GREEN at 100 with the presence assertion; sole-producer-discipline green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: build-from-blueprint route + ROUTE_DID_POLICY + cross-house-injection extension</name>
  <files>
    grid/src/civic/blueprint.ts,
    grid/src/api/routes/civic-parcels.ts,
    grid/src/api/policy.ts,
    scripts/check-cross-house-injection.mjs,
    grid/test/api/civic-build-routes.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (Phase 58/59/60 route shape — registerCivicParcelRoutes; the build / bind-shop / board routes the build-from-blueprint route extends),
    grid/src/api/policy.ts (ROUTE_DID_POLICY tiers — the new route needs an entry),
    grid/src/civic/blueprint.ts (the Wave-2 buildFromBlueprint executor + the emit seam to attach the producer to),
    scripts/check-cross-house-injection.mjs (the Phase 60 gate to EXTEND to the co-build / blueprint paths),
    grid/test/api/civic-build-routes.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Wire the new route in `grid/src/api/routes/civic-parcels.ts`:
    - `POST /api/v1/civic/parcels/:id/build-from-blueprint {blueprint_hash}`: civic_did_required; the builder is the owner OR a staff Nous in an active co-build session; a `did:civic:noesis:human:*` builder → 403 (humans never build); a non-authorized Nous → 403 `not_authorized`; a builder who does not hold the skill → 403/422 `skill_not_held`; insufficient Ousia → 402 `insufficient_funds`. On success the route calls the Wave-2 `buildFromBlueprint` executor whose emit seam now invokes `appendSkillBlueprintExecuted(audit, {blueprint_hash, builder_civic_did_hash, parcel_id, tick})` ONCE (the single producer). Attach the producer to the executor's `emitBlueprintExecuted` seam here.

    `grid/src/api/policy.ts` — add a `ROUTE_DID_POLICY` entry for `build-from-blueprint` (`civic_did_required`; humans rejected). `node scripts/check-did-policy-coverage.mjs` must exit 0.

    Extend `scripts/check-cross-house-injection.mjs` (A11e) — add `grid/src/civic/co-build.ts` + `grid/src/civic/blueprint.ts` to the file-walk: assert co-build board content + the recipe `arrangement` does NOT flow into a Telos/Charter command interpreter or a prompt-assembly path that could escalate it to an instruction (content stays a quoted/structured DATA field). The gate must still exit 0 against the Wave 1–3 source and fail on a synthetic escalation.

    Un-skip `civic-build-routes.test.ts` and make it green: human rejected; not_authorized 403; skill_not_held; insufficient_funds 402; a held + funded build emits `skill.blueprint_executed` once (hashed builder, no recipe body); ROUTE_DID_POLICY coverage.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- api/civic-build-routes && node scripts/check-did-policy-coverage.mjs && node scripts/check-cross-house-injection.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "build-from-blueprint" grid/src/api/routes/civic-parcels.ts` shows the new route
    - `grep -nE "appendSkillBlueprintExecuted" grid/src` shows the producer called from exactly one site (the executor seam attached at the route)
    - `grep -nE "build-from-blueprint|build_from_blueprint" grid/src/api/policy.ts` shows the ROUTE_DID_POLICY entry
    - `grep -nE "co-build|blueprint|arrangement|Telos|Charter" scripts/check-cross-house-injection.mjs` shows the gate inspects the new paths
    - civic-build-routes.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- api/civic-build-routes` is green (humans rejected; skill_not_held; 402)
    - `node scripts/check-did-policy-coverage.mjs` exits 0; `node scripts/check-cross-house-injection.mjs` exits 0; privacy-walker gate green
  </acceptance_criteria>
  <done>The build-from-blueprint route is wired with ROUTE_DID_POLICY coverage; it calls the Wave-2 executor whose emit seam fires the single skill.blueprint_executed producer; humans rejected; the cross-house-injection gate is extended to the co-build / blueprint paths; civic-build-routes.test.ts green.</done>
</task>

</tasks>

<success_criteria>
- The skill.blueprint_executed sole-producer triad landed (closed tuple + no-spread + payloadPrivacyCheck + single audit.append); the closed tuple matches the spec exactly
- allowlist 99 → 100; broadcast-allowlist GREEN at 100 with the presence assertion; events 1–99 untouched; check-sole-producer-discipline green
- the build-from-blueprint route wired with ROUTE_DID_POLICY coverage (check-did-policy-coverage green); owner OR co-build staff; humans rejected; skill_not_held; insufficient_funds 402; the build emits the single producer once
- scripts/check-cross-house-injection.mjs extended to the co-build / blueprint paths (exits 0; fails on a synthetic escalation)
- append-skill-blueprint-executed.test.ts + civic-build-routes.test.ts un-skipped and green; privacy-walker gate green
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-04-SUMMARY.md`.
</output>
