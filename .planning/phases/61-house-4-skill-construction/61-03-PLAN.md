---
phase: 61
plan: 03
type: execute
wave: 2
depends_on: [61-02]
files_modified:
  - grid/src/civic/blueprint.ts
  - grid/src/civic/co-build.ts
  - grid/src/civic/location-teaching.ts
  - grid/src/civic/parcel-registry.ts
  - grid/test/civic/blueprint-executor.test.ts
  - grid/test/civic/co-build.test.ts
  - grid/test/civic/location-teaching.test.ts
autonomous: true
requirements: [R-61-04, R-61-05, R-61-06]
tags: [nous-house, house-4, build-executor, co-build, dag-attribution, location-teaching]

must_haves:
  truths:
    - "buildFromBlueprint verifies skill-held -> debits material_cost_bios (Ousia -> TREASURY_DID via transferOusia; insufficient -> 402) -> applies the recipe by replaying ParcelRegistry.extendInterior for each recipe object (the executor is the SINGLE caller); the builder is the owner OR a staff Nous in a co-build session; humans rejected (did:civic:noesis:human:*)"
    - "co-build.ts decomposeRecipe turns the arrangement into atomic sub-tasks (node = catalog object(s) + depends_on edges + weight); each sub-task is a Phase 60 co-work board task carrying pay; completion ALWAYS settles (Ousia or Phase 60 IOU, never free, D-NH-06); a sub-task paying nothing throws"
    - "attributionShare(session, did) is DAG-weighted = Σ(completed-node weights for did) / Σ(all-node weights); on full completion the executor applies the assembled recipe + emits ONE skill.blueprint_executed (Wave 3 wires the emit); sub-task settlement reuses the Phase 60 zoning.cowork_session + IOU/Ousia path — NO new allowlist event"
    - "location-teaching.ts teachHere selects the learner set from the Nous PRESENT in a workshop (the Phase 58 occupant map); REUSES the existing appendSkillTaught/appendSkillInferred producers; the skill.taught 5-tuple is unchanged (parcel_id is a Grid-side selector, off chain); humans present are never learners"
    - "NO new clock.onTick subscription (grep stays exactly 1); allowlist still 99 until Wave 3"
    - "blueprint-executor.test.ts + co-build.test.ts + location-teaching.test.ts un-skipped and green"
  artifacts:
    - path: "grid/src/civic/blueprint.ts"
      provides: "buildFromBlueprint: skill-held -> material debit -> extendInterior per object -> (Wave-3) emit skill.blueprint_executed; owner OR co-build staff; humans rejected"
    - path: "grid/src/civic/co-build.ts"
      provides: "decomposeRecipe (arrangement -> sub-task DAG); sub-tasks reuse the Phase 60 co-work board (always-paid); attributionShare DAG-weighted"
    - path: "grid/src/civic/location-teaching.ts"
      provides: "teachHere: present-occupant learner selector reusing the existing skill producers; skill.taught tuple unchanged; humans excluded"
    - path: "grid/src/civic/parcel-registry.ts"
      provides: "co-build authorization read (owner OR staff edge) + the occupant-set read the location selector uses"
  key_links:
    - from: "grid/src/civic/blueprint.ts buildFromBlueprint"
      to: "grid/src/civic/parcel-registry.ts extendInterior"
      via: "the executor replays extendInterior for each recipe object (SINGLE recipe-apply caller)"
      pattern: "extendInterior|transferOusia"
    - from: "grid/src/civic/co-build.ts sub-tasks"
      to: "grid/src/civic/cowork.ts board post/claim/complete + credit-ledger recordIou"
      via: "each sub-task is a Phase 60 board task; completion always settles (Ousia or IOU, never free)"
      pattern: "postTask|claimTask|completeTask|recordIou"
    - from: "grid/src/civic/location-teaching.ts teachHere"
      to: "grid/src/skills/appendSkillTaught.ts / appendSkillInferred.ts"
      via: "present-occupant learner selector reuses the existing producers; skill.taught 5-tuple unchanged"
      pattern: "appendSkillTaught|appendSkillInferred"
---

<objective>
Land the HOUSE-4 build mechanism. The build executor (skill-held → material debit → recipe apply via extendInterior), co-build DAG sessions (decompose the arrangement → paid sub-tasks → DAG-weighted attribution, reusing the Phase 60 board + IOU ledger), and location-aware teaching (present-occupant diffusion reusing the existing skill producers). Un-skip blueprint-executor.test.ts + co-build.test.ts + location-teaching.test.ts. The single skill.blueprint_executed emit is WIRED in Wave 3; this wave constructs the call site + payload.
</objective>

<context>
@.planning/phases/61-house-4-skill-construction/61-CONTEXT.md
@grid/src/civic/blueprint.ts
@grid/src/civic/parcel-registry.ts
@grid/src/civic/cowork.ts
@grid/src/civic/credit-ledger.ts
@grid/src/civic/founding-law.ts
@grid/src/skills/appendSkillTaught.ts
@grid/src/skills/appendSkillInferred.ts

<context_note>
**The build executor is the SINGLE recipe-apply caller.** It replays the Phase 59 `ParcelRegistry.extendInterior(addr, ownerDid, {area, kind})` for each recipe object — no per-object chain event (the interior tree mutation is Grid-side; the whole build emits ONE `skill.blueprint_executed` in Wave 3). **D-NH-06 — co-build is ALWAYS PAID:** each claimed sub-task settles in Ousia (`transferOusia` when funded) or records a Phase 60 mutual-credit IOU (`recordIou` when not) — a sub-task paying nothing throws. **Attribution is DAG-weighted** (A5 contribution split): a participant's share = Σ(completed-node weights for them) ÷ Σ(all-node weights), computed Grid-side from metered completion — objective, not renegotiated. **Location-aware teaching reuses the EXISTING diffusion verbatim** — `teachHere` only SELECTS the learners (the Phase 58 occupants present in the workshop) and calls the existing `appendSkillTaught` / `appendSkillInferred` per learner; the `skill.taught` 5-tuple is unchanged and `parcel_id` never becomes a payload key (Grid-side selector only). Humans present are never learners. **NO new `clock.onTick`** — everything here is request-driven. **Allowlist stays 99 this wave** (the +1 lands in Wave 3); co-build sub-tasks reuse the Phase 60 `zoning.cowork_session` event.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: build executor (skill-held -> material debit -> recipe apply)</name>
  <files>
    grid/src/civic/blueprint.ts,
    grid/src/civic/parcel-registry.ts,
    grid/test/civic/blueprint-executor.test.ts
  </files>
  <read_first>
    grid/src/civic/parcel-registry.ts (extendInterior(addr, ownerDid, {area, kind}) — the SINGLE recipe-apply path; roleOf(addr, did) for co-build staff authorization; transferOusia / ownerDid surfaces; build() precedent),
    grid/src/civic/blueprint.ts (Wave-1 recipe type + getBlueprint + builderHoldsSkill — the executor consumes them),
    grid/src/civic/founding-law.ts (TREASURY_DID — material_cost_bios is debited here),
    grid/src/civic/roles.ts (isHumanDid + the staff role edge the co-build builder may hold),
    grid/test/civic/blueprint-executor.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Extend `grid/src/civic/blueprint.ts` with the build executor:
    - `export interface BuildDeps { registry: ParcelRegistry; transferOusia(from, to, amount): void; auditChain: ...; tick: number }` (the chain dep enables the Wave-1 skill-held check + the Wave-3 emit).
    - `export function buildFromBlueprint(addr, builderDid, blueprint_hash, tick, deps): Structure` —
      1. reject `isHumanDid(builderDid)` (`builder_forbidden_human` — VOTE-05 / D-NH-07).
      2. authorize: the builder is the owner (`registry.roleOf(addr, builderDid) === 'owner'`) OR a staff Nous in an active co-build session for this parcel; else `not_authorized` (403).
      3. `getBlueprint(blueprint_hash)` → `blueprint_not_found` if absent.
      4. `builderHoldsSkill(blueprint_hash, builderDid, deps)` → `skill_not_held` (403/422) if false.
      5. debit `recipe.material_cost_bios` from the builder's Ousia → `TREASURY_DID` via `deps.transferOusia` (insufficient → `insufficient_funds` 402).
      6. apply the recipe: for each `recipe.objects[i]` call `registry.extendInterior(addr, ownerDid, {area, kind})` (the executor is the SINGLE caller; the interior tree mutation is Grid-side, no per-object chain event).
      7. construct the `skill.blueprint_executed` payload `{blueprint_hash, builder_civic_did_hash: hash(builderDid), parcel_id: addr, tick}` — the EMIT itself is wired in Wave 3 (here the executor calls an injected `deps.emitBlueprintExecuted?(payload)` seam so Wave 3 attaches the real producer).
      8. return the updated structure.

    `grid/src/civic/parcel-registry.ts` — add the co-build authorization read (owner OR staff edge) if not already exposed; do NOT add a new `clock.onTick` or a new recipe-apply path (reuse `extendInterior`).

    Un-skip `blueprint-executor.test.ts` and make it green: human rejected; non-authorized 403; skill_not_held; insufficient_funds 402; a held + funded owner build debits material cost, applies every recipe object via extendInterior, and constructs the blueprint_executed payload (hashed builder, no recipe body).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/blueprint-executor civic/blueprint 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "buildFromBlueprint|builderHoldsSkill|extendInterior|transferOusia|skill_not_held|insufficient_funds|isHumanDid" grid/src/civic/blueprint.ts` shows the executor pipeline
    - the executor calls `extendInterior` once per recipe object (the SINGLE recipe-apply caller); no per-object chain event
    - blueprint-executor.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/blueprint-executor civic/blueprint` is green (human rejected; not_authorized; skill_not_held; insufficient_funds 402; held+funded build applies recipe)
    - `grep -rn "clock.onTick" grid/src | wc -l` stays exactly 1
  </acceptance_criteria>
  <done>buildFromBlueprint verifies skill-held -> debits material cost -> applies the recipe via extendInterior; owner OR co-build staff; humans rejected; the blueprint_executed payload is constructed (emit wired in Wave 3); blueprint-executor.test.ts green; single-onTick preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: co-build DAG sessions (decompose + paid sub-tasks + DAG-weighted attribution)</name>
  <files>
    grid/src/civic/co-build.ts,
    grid/test/civic/co-build.test.ts
  </files>
  <read_first>
    grid/src/civic/blueprint.ts (BlueprintRecipe + BlueprintNode — the arrangement DAG decomposeRecipe walks),
    grid/src/civic/cowork.ts (Phase 60 CoworkAgreement + postTask/claimTask/completeTask + the ALWAYS-settle path — each sub-task is a board task),
    grid/src/civic/credit-ledger.ts (Phase 60 recordIou/settleIou/outstandingFor — the IOU fallback for an unfunded sub-task),
    grid/test/civic/co-build.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/co-build.ts`:
    - `export interface CoBuildSession { session_id; parcel_id; blueprint_hash; nodes: BlueprintNode[]; completed: Set<string>; claims: Map<string, string> }` (claims: node_id → worker_did).
    - `export function decomposeRecipe(recipe): BlueprintNode[]` — return the recipe `arrangement` DAG nodes (each = catalog object(s) + `depends_on` edges + `weight`); a node is claimable only when its `depends_on` are all in `completed` (dependency edges respected).
    - `export function claimSubTask(session, node_id, workerDid, deps)` — post/claim the node as a Phase 60 co-work board task (reuse `cowork.ts` postTask/claimTask) carrying the node's pay; reject claiming a node whose deps are unmet.
    - `export function completeSubTask(session, node_id, deps)` — completion ALWAYS settles (D-NH-06): `transferOusia(host → worker)` when funded, else `recordIou(worker, host, amount, session_id, tick)` — a completion that would pay NOTHING throws `cobuild_must_pay`; mark the node completed.
    - `export function attributionShare(session, did): number` — DAG-weighted: Σ(completed-node weights claimed by `did`) ÷ Σ(all-node weights).
    - `export function isComplete(session): boolean` — all nodes completed → the executor (Task 1) applies the assembled recipe + emits the single `skill.blueprint_executed` (Wave 3).

    Sub-task settlement reuses the Phase 60 `zoning.cowork_session` event + the IOU/Ousia path — add NO new allowlist member.

    Un-skip `co-build.test.ts` and make it green: decompose respects dependency edges; a sub-task completion settles in Ousia or an IOU (never free — pay-nothing throws); attributionShare is the completed-weight fraction; full completion flips isComplete.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/co-build civic/blueprint-executor 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "decomposeRecipe|claimSubTask|completeSubTask|attributionShare|isComplete" grid/src/civic/co-build.ts` shows the session API
    - `grep -nE "recordIou|transferOusia|cobuild_must_pay|depends_on|weight" grid/src/civic/co-build.ts` shows always-paid + DAG-weighted + dependency edges
    - co-build sub-tasks reuse the Phase 60 board + IOU ledger (import cowork.ts + credit-ledger.ts); NO new append-* producer for sub-tasks; allowlist still 99
    - co-build.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/co-build` is green (dependency edges; always-paid; DAG-weighted attribution; full completion)
    - `grep -rn "clock.onTick" grid/src | wc -l` stays exactly 1
  </acceptance_criteria>
  <done>co-build.ts decomposes the arrangement DAG into atomic paid sub-tasks (reusing the Phase 60 board + IOU ledger, always paid), computes DAG-weighted attribution, and flips isComplete on full completion; no new allowlist event; co-build.test.ts green; single-onTick preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: location-aware teaching (present-occupant diffusion)</name>
  <files>
    grid/src/civic/location-teaching.ts,
    grid/src/civic/parcel-registry.ts,
    grid/test/civic/location-teaching.test.ts
  </files>
  <read_first>
    grid/src/skills/appendSkillTaught.ts + grid/src/skills/appendSkillInferred.ts (the EXISTING diffusion producers to REUSE verbatim — the skill.taught 5-tuple {learner_did, parent_hash, skill_hash, teacher_did, tick} stays unchanged),
    grid/src/civic/parcel-registry.ts (the Phase 58 in-memory occupant map — the present-Nous set the selector reads; isHumanDid for the human exclusion),
    grid/src/civic/roles.ts (isHumanDid — present humans are never learners),
    grid/test/civic/location-teaching.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/location-teaching.ts`:
    - `export interface TeachHereDeps { registry: ParcelRegistry; teach(learnerDid, skill_hash, parent_hash, teacherDid, tick): void }` (`teach` wraps the EXISTING `appendSkillTaught` — one skill.taught per learner, the 5-tuple unchanged).
    - `export function teachHere(parcel_id, teacherDid, skill_hash, parent_hash, tick, deps): string[]` —
      1. read the present-occupant set for `parcel_id` from the Phase 58 occupant map (`deps.registry`), restricted to a `workshop` structure (a workshop becomes a school).
      2. exclude the teacher and any `did:civic:noesis:human:*` (`isHumanDid`) — humans present are never learners.
      3. for each remaining present Nous, call `deps.teach(learnerDid, skill_hash, parent_hash, teacherDid, tick)` — REUSING the existing `appendSkillTaught` producer (the `skill.taught` 5-tuple is unchanged; `parcel_id` is the SELECTOR only, never a payload key).
      4. return the list of learner DIDs taught.
    - `parcel_id` stays Grid-side (the diffusion selector) — it NEVER becomes a `skill.taught` payload key (off the chain).

    `grid/src/civic/parcel-registry.ts` — expose the present-occupant read for a parcel if not already available (read-only; the occupant map is the existing Phase 58 in-memory presence).

    Un-skip `location-teaching.test.ts` and make it green: teachHere diffuses to the present Nous (not absent ones); a present human is excluded; the existing `appendSkillTaught` producer is reused (the skill.taught 5-tuple is unchanged); `parcel_id` is not a payload key.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/location-teaching civic/co-build 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "teachHere|present|occupant|appendSkillTaught|isHumanDid" grid/src/civic/location-teaching.ts` shows the present-occupant selector + the existing-producer reuse + the human exclusion
    - the selector does NOT add `parcel_id` to any skill.taught payload (the 5-tuple is unchanged); no new diffusion producer is created
    - location-teaching.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/location-teaching` is green (present diffuse; absent do not; humans excluded; skill.taught tuple unchanged)
    - `grep -rn "clock.onTick" grid/src | wc -l` stays exactly 1
  </acceptance_criteria>
  <done>teachHere selects the present-occupant learner set for a workshop and diffuses via the EXISTING appendSkillTaught producer (skill.taught 5-tuple unchanged; parcel_id off chain); humans present excluded; location-teaching.test.ts green; single-onTick preserved.</done>
</task>

</tasks>

<success_criteria>
- buildFromBlueprint: skill-held -> material debit (insufficient 402) -> extendInterior per recipe object -> blueprint_executed payload constructed; owner OR co-build staff; humans rejected
- co-build.ts decomposes the arrangement DAG into atomic paid sub-tasks (reusing the Phase 60 board + IOU ledger, always paid), with DAG-weighted attribution + full-completion gating; no new allowlist event
- location-teaching.ts diffuses to the present Nous via the EXISTING skill producers (skill.taught 5-tuple unchanged; parcel_id off chain); humans present excluded
- blueprint-executor.test.ts + co-build.test.ts + location-teaching.test.ts un-skipped and green
- single-onTick preserved (grep clock.onTick == 1); allowlist still 99 (the +1 lands in Wave 3)
</success_criteria>

<output>
After completion, create `.planning/phases/61-house-4-skill-construction/61-03-SUMMARY.md`.
</output>
