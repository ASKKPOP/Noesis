---
phase: 60
plan: 08
type: execute
wave: 7
depends_on: [60-05, 60-06, 60-07]
files_modified:
  - grid/test/civic/house-3-e2e.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - scripts/check-sole-producer-discipline.mjs
  - scripts/check-wallclock-forbidden.mjs
  - scripts/check-civic-did-issuance-path.mjs
  - scripts/check-cross-house-injection.mjs
autonomous: true
requirements: [R-60-15]
tags: [nous-house, house-3, e2e, gates, definition-of-done]

must_haves:
  truths:
    - "End-to-end: owner grants a staff role to a second Nous (zoning.role_granted) -> posts a task -> the staff Nous claims + completes it -> completion settles in Ousia when funded, else records an IOU (never free) -> zoning.cowork_session emitted (participants_hash only)"
    - "End-to-end: owner binds a shop + names it place://aurora-cafe.genesis -> a sale at the addressed shop emits treasury.structure_revenue with the zone tax; a duplicate place name -> 409; a ring-expansion bill enacted via gov.law_enacted seeds a new ring"
    - "End-to-end: revoking the staff role drains the IOU ledger then traverses the severance FSM to ARCHIVED (zoning.role_revoked); a human attempting any role grant is rejected"
    - "No interior/board/task/scope/place CONTENT on any payload — only hashes/counts/enums/ticks; all DIDs HEX64"
    - "broadcast-allowlist 99; sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, cross-house-injection, privacy walker, zero-diff R-31-01 all exit 0; grep clock.onTick == 1; no remaining describe.skip in Phase 60 test files"
  artifacts:
    - path: "grid/test/civic/house-3-e2e.test.ts"
      provides: "Definition-of-Done E2E: grant role -> co-work settle/IOU -> cowork_session; bind+name shop -> sale -> structure_revenue; duplicate name 409; ring-expansion enacts; revoke via severance to ARCHIVED; humans rejected"
  key_links:
    - from: "house-3-e2e co-work completion"
      to: "zoning.cowork_session + (Ousia transfer OR recordIou)"
      via: "completion always settles; participants_hash only on chain"
      pattern: "cowork_session|recordIou|transferOusia"
    - from: "all Phase 60 test files"
      to: "0 remaining describe.skip"
      via: "every Wave un-skipped its stubs"
      pattern: "describe\\.skip"
---

<objective>
Prove the Definition of Done and keep every CI gate green. The HOUSE-3 acceptance scenario: an owner grants a staff role to a second Nous, posts a task to the co-work board, the staff Nous claims + completes it (settling in Ousia or recording an IOU — never free, emitting `zoning.cowork_session`), the owner binds a shop + names it `place://aurora-cafe.genesis` (a sale emitting `treasury.structure_revenue` at the zone tax), a duplicate name is rejected (409), a ring-expansion bill enacted via `gov.law_enacted` seeds a new ring, and revoking the staff role drains the IOU ledger then traverses the severance FSM to ARCHIVED (`zoning.role_revoked`) — while humans cannot own/staff and no board/task/scope/place content ever crosses the chain.
</objective>

<context>
@.planning/phases/60-house-3-commerce-cowork/60-CONTEXT.md
@.planning/phases/60-house-3-commerce-cowork/60-VALIDATION.md
@grid/src/civic/roles.ts
@grid/src/civic/severance.ts
@grid/src/civic/credit-ledger.ts
@grid/src/civic/cowork.ts
@grid/src/civic/place-registry.ts
@grid/src/civic/ring-expansion.ts
@grid/src/api/routes/civic-parcels.ts
@grid/src/audit/broadcast-allowlist.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HOUSE-3 end-to-end Definition-of-Done test</name>
  <files>
    grid/test/civic/house-3-e2e.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (roles/invite/bind-shop/name/board routes),
    grid/src/civic/cowork.ts (post/claim/complete + always-settle path),
    grid/src/civic/credit-ledger.ts (recordIou/settleIou/outstandingFor — the IOU fallback + severance drain),
    grid/src/civic/severance.ts (the FSM revoke traverses),
    grid/src/civic/ring-expansion.ts (seed_ring template),
    grid/src/audit/append-zoning-role-granted.ts (+ siblings — payload shapes for the trail assertions)
  </read_first>
  <behavior>
    Un-skip + author `grid/test/civic/house-3-e2e.test.ts` exercising the HOUSE-3 acceptance scenario end-to-end against a seeded Genesis Core (build on the Phase 58/59 buy→build→furnish path):
    - an owner grants a **staff** role to a second Nous → `zoning.role_granted {grantor_civic_did_hash, holder_civic_did_hash, parcel_id, role:'staff', tick}` on the trail (no raw DIDs); `roleOf === 'staff'`.
    - the owner posts a task to the `task_board`; the staff Nous claims + completes it. **Funded** path → `registry.transferOusia(host → worker)`; **unfunded** path → `recordIou(worker, host, amount, agreement_id, tick)` (never free); both emit `zoning.cowork_session {end_tick, parcel_id, participant_count, participants_hash, start_tick}` (participants_hash only — no board/task text).
    - the owner binds a `shop` structure (`bind-shop`) and names it `place://aurora-cafe.genesis` (`name`); a sale at the addressed shop (Phase 44 confirm-settlement) emits `treasury.structure_revenue {amount_bios, parcel_id, tick, zone_tax_bps}` at the zone-tax rate.
    - a SECOND attempt to register `aurora-cafe` → 409 `place_name_taken`.
    - a ring-expansion bill enacted via `gov.law_enacted` (`{action:'seed_ring', ring:N}`) seeds a new ring at frontier prices (idempotent, no audit emit).
    - revoking the staff role drains the IOU ledger (`outstandingFor` → 0 after settle) THEN traverses the severance FSM to ARCHIVED → `zoning.role_revoked {holder_civic_did_hash, parcel_id, reason:'severance_complete', tick}`.
    - a HUMAN (`did:civic:noesis:human:*`) attempting any role grant → rejected.
    - assert no raw DID, no board/task/scope/place CONTENT anywhere in the payloads (all HEX64 / hashes / counts / enums / ticks).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/house-3-e2e 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test -- civic/house-3-e2e` is green
    - the test asserts all 4 new events on the trail (role_granted, cowork_session, structure_revenue, role_revoked) + the co-work settle/IOU, the duplicate-name 409, the ring-expansion seed, the revoke-via-severance-to-ARCHIVED, and the human-rejected cases
    - the test confirms no board/task/scope/place content on any payload and all DIDs HEX64
  </acceptance_criteria>
  <done>house-3-e2e.test.ts green: grant role → co-work settle/IOU → cowork_session; bind+name shop → sale → structure_revenue; duplicate name 409; ring-expansion enacts; revoke via severance to ARCHIVED; humans rejected; no content on the chain.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: all gates green + all Phase 60 skip-stubs un-skipped</name>
  <files>
    grid/test/audit/broadcast-allowlist.test.ts,
    scripts/check-sole-producer-discipline.mjs,
    scripts/check-wallclock-forbidden.mjs,
    scripts/check-civic-did-issuance-path.mjs,
    scripts/check-cross-house-injection.mjs
  </files>
  <read_first>
    grid/test/audit/broadcast-allowlist.test.ts (must assert 99; do NOT weaken),
    scripts/check-sole-producer-discipline.mjs (must stay green — read-only confirmation),
    scripts/check-wallclock-forbidden.mjs (must stay green — read-only confirmation),
    scripts/check-civic-did-issuance-path.mjs (must stay green — read-only confirmation),
    scripts/check-cross-house-injection.mjs (the Wave-5 gate — must stay green)
  </read_first>
  <behavior>
    Verify (do NOT weaken) every CI gate:
    - `broadcast-allowlist.test.ts` GREEN at 99 (delta +4 confirmed) with presence assertions for all 4 new members; any sibling count suite (e.g. `human-civic-application.test.ts`) GREEN at 99.
    - `scripts/check-sole-producer-discipline.mjs` passes (one producer per event; no foreign `audit.append` for the 4 events).
    - the privacy walker passes (no payload key matches `FORBIDDEN_KEY_PATTERN`; board/task/scope/place content never broadcast).
    - `scripts/check-cross-house-injection.mjs` passes (visitor/board content is DATA never instructions).
    - `scripts/check-civic-did-issuance-path.mjs` and `scripts/check-wallclock-forbidden.mjs` pass unchanged.
    - zero-diff (R-31-01) holds — no chain / persistence-of-chain code was edited; the 4 events ride new standard sole producers.
    - single-onTick invariant preserved — `grep -rn "clock.onTick" grid/src` shows exactly one subscription.
    - the full grid suite is green; all Wave-0 skip-stubs from 60-01 are now un-skipped — `grep -c describe.skip` across the Phase 60 test files is 0.

    These gate scripts are listed in files_modified only as touch-points; if they pass unchanged, do not edit them. If a gate fails, the fix belongs in the offending earlier wave's source, NOT in the gate (never weaken a gate to pass).
  </behavior>
  <verify>
    <automated>cd grid && npm run test; node scripts/check-sole-producer-discipline.mjs; node scripts/check-wallclock-forbidden.mjs; node scripts/check-civic-did-issuance-path.mjs; node scripts/check-cross-house-injection.mjs 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `cd grid && npm run test` is fully green
    - `cd grid && npm run test -- audit/broadcast-allowlist` GREEN at 99 with presence assertions for all 4
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
    - `node scripts/check-civic-did-issuance-path.mjs` exits 0
    - `node scripts/check-cross-house-injection.mjs` exits 0
    - `grep -rn "clock.onTick" grid/src | wc -l` shows exactly 1 (single-onTick)
    - `grep -rc "describe.skip" grid/test/civic/roles.test.ts grid/test/civic/severance.test.ts grid/test/civic/credit-ledger.test.ts grid/test/civic/cowork.test.ts grid/test/civic/place-registry.test.ts grid/test/civic/ring-expansion.test.ts grid/test/civic/shop-binding.test.ts grid/test/api/civic-commerce-routes.test.ts grid/test/audit/append-zoning-role-granted.test.ts grid/test/audit/append-zoning-role-revoked.test.ts grid/test/audit/append-treasury-structure-revenue.test.ts grid/test/audit/append-zoning-cowork-session.test.ts grid/test/civic/house-3-e2e.test.ts | grep -v ':0$'` returns nothing (no remaining skips in Phase 60 test files)
    - no chain/persistence-of-chain file appears in the phase diff (zero-diff R-31-01)
  </acceptance_criteria>
  <done>All CI gates green; allowlist at 99 (+4); single-onTick preserved; cross-house-injection gate green; every Phase 60 skip-stub un-skipped; zero-diff holds.</done>
</task>

</tasks>

<success_criteria>
- End-to-end DoD passes: grant role → co-work settle/IOU → cowork_session; bind+name shop → sale → structure_revenue; duplicate name 409; ring-expansion enacts a new ring; revoke via severance to ARCHIVED; humans rejected; no content on the chain
- broadcast-allowlist GREEN at 99 (+4); sole-producer-discipline, civic-did-issuance-path, wallclock-forbidden, cross-house-injection, privacy walker, zero-diff (R-31-01) all green and unchanged
- single-onTick invariant preserved (exactly one clock.onTick in grid/src)
- No remaining describe.skip in any Phase 60 test file
</success_criteria>

<output>
After completion, create `.planning/phases/60-house-3-commerce-cowork/60-08-SUMMARY.md` and the phase-completion report.
</output>
