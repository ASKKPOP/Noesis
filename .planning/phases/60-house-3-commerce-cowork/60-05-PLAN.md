---
phase: 60
plan: 05
type: execute
wave: 4
depends_on: [60-04]
files_modified:
  - grid/src/audit/append-zoning-role-granted.ts
  - grid/src/audit/append-zoning-role-revoked.ts
  - grid/src/audit/append-treasury-structure-revenue.ts
  - grid/src/audit/append-zoning-cowork-session.ts
  - grid/src/audit/broadcast-allowlist.ts
  - grid/src/api/routes/civic-parcels.ts
  - grid/src/api/policy.ts
  - grid/test/audit/append-zoning-role-granted.test.ts
  - grid/test/audit/append-zoning-role-revoked.test.ts
  - grid/test/audit/append-treasury-structure-revenue.test.ts
  - grid/test/audit/append-zoning-cowork-session.test.ts
  - grid/test/audit/broadcast-allowlist.test.ts
  - grid/test/api/civic-commerce-routes.test.ts
autonomous: true
requirements: [R-60-04, R-60-11, R-60-12]
tags: [nous-house, house-3, sole-producers, allowlist-99, commerce-routes, invitations, privacy]

must_haves:
  truths:
    - "4 dedicated append-*.ts clone the append-treasury-parcel-revenue triad: closed-tuple Object.keys().sort() check, explicit no-spread reconstruction, payloadPrivacyCheck gate, single audit.append"
    - "Closed tuples exactly: role_granted {grantor_civic_did_hash, holder_civic_did_hash, parcel_id, role, tick} (role in {staff,guest}); role_revoked {holder_civic_did_hash, parcel_id, reason, tick}; structure_revenue {amount_bios, parcel_id, tick, zone_tax_bps}; cowork_session {end_tick, parcel_id, participant_count, participants_hash, start_tick}"
    - "broadcast-allowlist.ts grows 95->99 under the pre-cleared zoning.*/treasury.* prefixes; broadcast-allowlist.test.ts GREEN at 99 (size + length) with the 4 presence assertions; events 82-95 untouched/unreordered"
    - "New routes wired with ROUTE_DID_POLICY entries: bind-shop, unbind-shop, name, roles(grant)/revoke, invite, board post/claim/complete; owner/staff/guest authorization enforced; humans rejected from role grants"
    - "Invitations: POST .../invite appends to the Phase 58 entry allowlist AND mints a guest role edge"
    - "node scripts/check-sole-producer-discipline.mjs + node scripts/check-did-policy-coverage.mjs + privacy-walker all exit 0"
  artifacts:
    - path: "grid/src/audit/append-zoning-role-granted.ts"
      provides: "sole producer for zoning.role_granted (closed 5-tuple; actorDid = grantor_civic_did_hash; role in {staff,guest})"
    - path: "grid/src/audit/append-zoning-role-revoked.ts"
      provides: "sole producer for zoning.role_revoked (closed 4-tuple; actorDid = parcel_id; reason enum)"
    - path: "grid/src/audit/append-treasury-structure-revenue.ts"
      provides: "sole producer for treasury.structure_revenue (closed 4-tuple; actorDid = parcel_id; mirrors #83)"
    - path: "grid/src/audit/append-zoning-cowork-session.ts"
      provides: "sole producer for zoning.cowork_session (closed 5-tuple; participants_hash HEX64; NO raw DIDs/board text)"
    - path: "grid/src/audit/broadcast-allowlist.ts"
      provides: "ALLOWLIST_MEMBERS grown 95->99 under zoning.*/treasury.* with sole-producer reference comments"
    - path: "grid/src/api/routes/civic-parcels.ts"
      provides: "bind/unbind-shop, name, roles grant/revoke, invite, board post/claim/complete routes emitting the new events"
    - path: "grid/src/api/policy.ts"
      provides: "ROUTE_DID_POLICY entries for every new commerce/role/cowork/place route"
  key_links:
    - from: "the 4 append-*.ts producers"
      to: "grid/src/audit/broadcast-allowlist.ts ALLOWLIST_MEMBERS (95->99)"
      via: "each event is allowlisted under its pre-cleared zoning.*/treasury.* prefix"
      pattern: "zoning.role_granted|zoning.role_revoked|treasury.structure_revenue|zoning.cowork_session"
    - from: "grid/src/api/routes/civic-parcels.ts new routes"
      to: "grid/src/api/policy.ts ROUTE_DID_POLICY"
      via: "every new route has a DID-policy entry (check-did-policy-coverage gate)"
      pattern: "ROUTE_DID_POLICY"
    - from: "POST .../invite"
      to: "Phase 58 setEntryPolicy allowlist + grantRole(guest)"
      via: "invitation appends to the entry allowlist AND mints a guest role edge"
      pattern: "setEntryPolicy|grantRole"
---

<objective>
Land the 4 sole-producer audit producers and grow the broadcast allowlist 95→99 (turning the Wave-0 expected-RED suite GREEN), then wire the new commerce/role/cowork/place/invite routes that emit them. Un-skip the 4 append-*.test.ts suites + civic-commerce-routes.test.ts; broadcast-allowlist goes green at 99.
</objective>

<context>
@.planning/phases/60-house-3-commerce-cowork/60-CONTEXT.md
@grid/src/audit/append-treasury-parcel-revenue.ts
@grid/src/audit/broadcast-allowlist.ts
@grid/src/api/routes/civic-parcels.ts
@grid/src/api/policy.ts
@grid/src/civic/roles.ts
@grid/src/civic/cowork.ts
@grid/src/civic/place-registry.ts

<context_note>
**Sole-producer discipline:** each of the 4 new producers CLONES the `append-treasury-parcel-revenue.ts` triad EXACTLY — a closed-tuple `Object.keys(payload).sort()` check (a foreign key throws), explicit no-spread reconstruction of the payload, a `payloadPrivacyCheck` gate, and a SINGLE `audit.append` callsite (no other file may call `audit.append` for these event types). `check-sole-producer-discipline.mjs` enforces one producer per event. Closed tuples are ALPHABETICAL. **Privacy walker:** no payload key matches `FORBIDDEN_KEY_PATTERN` (body/session_id/text/content); all DIDs hashed HEX64 (or the aggregated `participants_hash`); co-work board/task CONTENT, scope_ref bodies, and place names NEVER cross — only enums/counts/hashes. The allowlist is the pre-cleared zoning.*/treasury.* prefixes; this wave moves it 95→99.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 4 sole-producer append-*.ts + allowlist 95->99 (suite turns GREEN)</name>
  <files>
    grid/src/audit/append-zoning-role-granted.ts,
    grid/src/audit/append-zoning-role-revoked.ts,
    grid/src/audit/append-treasury-structure-revenue.ts,
    grid/src/audit/append-zoning-cowork-session.ts,
    grid/src/audit/broadcast-allowlist.ts,
    grid/test/audit/append-zoning-role-granted.test.ts,
    grid/test/audit/append-zoning-role-revoked.test.ts,
    grid/test/audit/append-treasury-structure-revenue.test.ts,
    grid/test/audit/append-zoning-cowork-session.test.ts,
    grid/test/audit/broadcast-allowlist.test.ts
  </files>
  <read_first>
    grid/src/audit/append-treasury-parcel-revenue.ts (the triad to CLONE — closed-tuple Object.keys().sort(), no-spread reconstruction, payloadPrivacyCheck, single audit.append; PARCEL_ID_RE; #83 land-attribution actorDid = parcel_id),
    grid/src/audit/broadcast-allowlist.ts (ALLOWLIST_MEMBERS at 95, FORBIDDEN_KEY_PATTERN, HEX64 discipline — append the 4 under zoning.*/treasury.*),
    grid/test/audit/broadcast-allowlist.test.ts (Wave 0 updated to 99 — turns GREEN here),
    scripts/check-sole-producer-discipline.mjs (the gate the 4 producers must satisfy)
  </read_first>
  <behavior>
    Create the 4 dedicated producers, each cloning the `append-treasury-parcel-revenue.ts` triad EXACTLY:
    - `append-zoning-role-granted.ts` → `appendZoningRoleGranted(audit, { grantor_civic_did_hash, holder_civic_did_hash, parcel_id, role, tick })`: closed 5-tuple (ALPHABETICAL keys); validate both hashes HEX64, `role ∈ {'staff','guest'}` (owner implicit, never granted), `parcel_id` PARCEL_ID_RE, `tick` non-negative int; no-spread reconstruction; `payloadPrivacyCheck`; single `audit.append` with `actorDid = grantor_civic_did_hash`.
    - `append-zoning-role-revoked.ts` → `appendZoningRoleRevoked(audit, { holder_civic_did_hash, parcel_id, reason, tick })`: closed 4-tuple; `holder_civic_did_hash` HEX64, `parcel_id` PARCEL_ID_RE, `reason ∈ {'owner_revoked','for_cause','severance_complete'}`, `tick` non-negative; `actorDid = parcel_id`.
    - `append-treasury-structure-revenue.ts` → `appendTreasuryStructureRevenue(audit, { amount_bios, parcel_id, tick, zone_tax_bps })`: closed 4-tuple; `amount_bios` positive int, `zone_tax_bps` positive int, `parcel_id` PARCEL_ID_RE, `tick` non-negative; `actorDid = parcel_id` (mirrors #83 — NO buyer/seller DID).
    - `append-zoning-cowork-session.ts` → `appendZoningCoworkSession(audit, { end_tick, parcel_id, participant_count, participants_hash, start_tick })`: closed 5-tuple; `participants_hash` HEX64 (single hash over the sorted participant DID set — NO raw DIDs/board text), `participant_count` positive int, `parcel_id` PARCEL_ID_RE, `start_tick`/`end_tick` non-negative; `actorDid = parcel_id`.

    `grid/src/audit/broadcast-allowlist.ts` — append the 4 members to `ALLOWLIST_MEMBERS` (95 → 99) under the pre-cleared `zoning.*` / `treasury.*` prefixes with sole-producer reference comments. Do NOT touch events 82–95 ordering.

    Un-skip the 4 append test suites + confirm `broadcast-allowlist.test.ts` (Wave 0) now passes at 99 with the 4 presence assertions. Each append suite asserts the closed tuple (a foreign key throws), the value-format validators, `actorDid`, and the privacy properties (no `FORBIDDEN_KEY_PATTERN` key; DIDs HEX64; no raw board/scope/name content).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- audit/append-zoning-role-granted audit/append-zoning-role-revoked audit/append-treasury-structure-revenue audit/append-zoning-cowork-session audit/broadcast-allowlist && node scripts/check-sole-producer-discipline.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - the 4 append-*.ts exist; each has a closed-tuple `Object.keys(payload).sort()` check + no-spread reconstruction + `payloadPrivacyCheck` + single `audit.append`
    - `grep -E "zoning.role_granted|zoning.role_revoked|treasury.structure_revenue|zoning.cowork_session" grid/src/audit/broadcast-allowlist.ts` shows the 4 new members
    - `cd grid && npm run test -- audit/broadcast-allowlist` GREEN at 99 (`ALLOWLIST.size === 99` + `ALLOWLIST_MEMBERS.length === 99`) with the 4 presence assertions
    - the 4 append test suites have no remaining `describe.skip` and are green
    - `node scripts/check-sole-producer-discipline.mjs` exits 0
    - events 82–95 untouched/unreordered
  </acceptance_criteria>
  <done>4 sole-producer triads landed; allowlist 95→99; broadcast-allowlist GREEN at 99 with the 4 presence assertions; sole-producer-discipline green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: commerce/role/cowork/place/invite routes + ROUTE_DID_POLICY coverage</name>
  <files>
    grid/src/api/routes/civic-parcels.ts,
    grid/src/api/policy.ts,
    grid/test/api/civic-commerce-routes.test.ts
  </files>
  <read_first>
    grid/src/api/routes/civic-parcels.ts (Phase 58/59 route shape the commerce routes extend),
    grid/src/api/policy.ts (ROUTE_DID_POLICY tiers — every new route needs an entry),
    grid/src/civic/parcel-registry.ts (grantRole/revokeRole/bindShop/unbindShop/namePlace surfaces),
    grid/src/civic/cowork.ts (postTask/claimTask/completeTask + the participants set for the cowork_session emit),
    grid/test/api/civic-commerce-routes.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Wire the new routes in `grid/src/api/routes/civic-parcels.ts`, each emitting its event via the Wave-4 producer (or none, for IOU/place/invite which add no chain event):
    - `POST .../bind-shop {shop_id}` / `POST .../unbind-shop`: owner-only; bind requires structure type `shop`; non-owner → 403 `not_owner`; non-shop → 422 `structure_not_shop`; unbind routes through severance.
    - `POST .../name {place_name}`: owner-only; duplicate → 409 `place_name_taken`; wires the dormant `named_address` (NO chain event).
    - `POST .../invite {invitee_civic_did}`: owner or staff; appends to the Phase 58 entry allowlist (`setEntryPolicy`) AND mints a guest role edge via `grantRole(..., 'guest', ...)` → emits `zoning.role_granted`; a `did:civic:noesis:human:*` invitee → 403 from the role edge (the allowlist itself never holds a role).
    - `POST .../roles {holder_civic_did, role}` (grant, owner-only, role ∈ {staff,guest}) → emits `zoning.role_granted`; `POST .../roles/revoke {holder_civic_did, reason}` → routes through severance → emits `zoning.role_revoked`. A `did:civic:noesis:human:*` holder → 403 (humans never hold a role edge).
    - `POST .../board/post {task_ref, pay_bios}` (owner/staff) / `POST .../board/claim {task_id}` (role with board access) / `POST .../board/complete {task_id}` (host): completion ALWAYS settles (Ousia or IOU) and emits `zoning.cowork_session` (participants_hash only — no board/task text).

    `grid/src/api/policy.ts` — add a `ROUTE_DID_POLICY` entry for EVERY new route above (`civic_did_required` for owner/staff/guest actions; humans rejected from role grants). `node scripts/check-did-policy-coverage.mjs` must exit 0.

    Un-skip `civic-commerce-routes.test.ts` and make it green: bind/unbind owner-only + type-shop; name 409 on duplicate; invite appends allowlist + mints guest edge + rejects humans; roles grant/revoke (humans 403); board post/claim/complete settles + emits cowork_session; ROUTE_DID_POLICY coverage.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- api/civic-commerce-routes && node scripts/check-did-policy-coverage.mjs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "bind-shop|unbind-shop|/name|/roles|/invite|/board/(post|claim|complete)" grid/src/api/routes/civic-parcels.ts` shows the new routes
    - `grep -nE "appendZoningRoleGranted|appendZoningRoleRevoked|appendZoningCoworkSession|appendTreasuryStructureRevenue" grid/src` shows each producer called from exactly one site
    - `grep -nE "setEntryPolicy|grantRole" grid/src/api/routes/civic-parcels.ts` confirms invite appends allowlist + mints guest edge
    - civic-commerce-routes.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- api/civic-commerce-routes` is green (humans rejected from role grants; duplicate name 409)
    - `node scripts/check-did-policy-coverage.mjs` exits 0; privacy-walker gate green
  </acceptance_criteria>
  <done>All new commerce/role/cowork/place/invite routes wired with ROUTE_DID_POLICY coverage; the 3 chain-emitting routes call their sole producers; invite appends the entry allowlist + mints a guest edge; humans rejected from role grants; civic-commerce-routes.test.ts green.</done>
</task>

</tasks>

<success_criteria>
- 4 sole-producer triads landed (closed tuple + no-spread + payloadPrivacyCheck + single audit.append); closed tuples match the spec exactly
- allowlist 95 → 99; broadcast-allowlist GREEN at 99 with the 4 presence assertions; events 82–95 untouched; check-sole-producer-discipline green
- new routes wired with ROUTE_DID_POLICY coverage (check-did-policy-coverage green); owner/staff/guest authorization; humans rejected from role grants
- invitations append the Phase 58 entry allowlist AND mint a guest role edge
- the 4 append suites + civic-commerce-routes.test.ts un-skipped and green; privacy-walker gate green
</success_criteria>

<output>
After completion, create `.planning/phases/60-house-3-commerce-cowork/60-05-SUMMARY.md`.
</output>
