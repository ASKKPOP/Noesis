---
phase: 60
plan: 06
type: execute
wave: 5
depends_on: [60-05]
files_modified:
  - grid/src/civic/ring-expansion.ts
  - grid/src/civic/founding-law.ts
  - grid/src/governance/engine.ts
  - scripts/check-cross-house-injection.mjs
  - grid/test/civic/ring-expansion.test.ts
autonomous: true
requirements: [R-60-10, R-60-12]
tags: [nous-house, house-3, ring-expansion, polis-amendment, cross-house-injection, template, governance]

must_haves:
  truths:
    - "ring-expansion.ts consumes existing Phase 46 gov.law_enacted; a matching enacted bill ({action:'seed_ring', ring:N} parsed Grid-side) seeds the legislated ring at frontier prices via Phase 58 seedZone + gravity formula, idempotent (INSERT IGNORE), emitting NO audit event; VOTE-05 + bill pipeline reused verbatim; NO new allowlist member added"
    - "The same body-parse hook supports Polis amendment of UPKEEP_RATE_BPS/ZONE_TAX_BPS; constants remain the default fallback (single patch point preserved)"
    - "scripts/check-cross-house-injection.mjs exists and passes: visitor/board/scope_ref content entering a House channel is treated as DATA, never instructions (no escalation into Telos/Charter commands)"
    - "allowlist still exactly 99; zero-diff R-31-01 + civic-did-issuance-path gates untouched-green"
    - "ring-expansion.test.ts un-skipped and green; NO new clock.onTick subscription (grep stays exactly 1)"
  artifacts:
    - path: "grid/src/civic/ring-expansion.ts"
      provides: "TEMPLATE consuming gov.law_enacted; seed_ring -> seedZone + gravity (idempotent, no audit emit); same hook parses UPKEEP_RATE_BPS/ZONE_TAX_BPS amendments"
    - path: "grid/src/civic/founding-law.ts"
      provides: "Polis-amendable read path for UPKEEP_RATE_BPS/ZONE_TAX_BPS (constants are the default fallback)"
    - path: "grid/src/governance/engine.ts"
      provides: "gov.law_enacted dispatch to ring-expansion.onLawEnacted (template hook; reuses Phase 46 verbatim)"
    - path: "scripts/check-cross-house-injection.mjs"
      provides: "NEW CI gate (A11e): visitor/board content is DATA never instructions"
  key_links:
    - from: "Phase 46 gov.law_enacted (existing)"
      to: "grid/src/civic/ring-expansion.ts onLawEnacted"
      via: "TEMPLATE consumes existing enacted bills; NOT a new governance path"
      pattern: "gov.law_enacted|onLawEnacted"
    - from: "grid/src/civic/ring-expansion.ts seed_ring"
      to: "Phase 58 seedZone + gravity-price formula"
      via: "seeds the legislated ring at frontier prices (idempotent, no audit emit)"
      pattern: "seedZone|seed_ring"
    - from: "same body-parse hook"
      to: "grid/src/civic/founding-law.ts UPKEEP_RATE_BPS / ZONE_TAX_BPS amendment"
      via: "Polis amendment rides the same gov.law_enacted body-parse; constants remain the default fallback"
      pattern: "UPKEEP_RATE_BPS|ZONE_TAX_BPS"
---

<objective>
Land the council ring-expansion bill TEMPLATE consuming existing Phase 46 gov.law_enacted (no new governance path, no new allowlist event), the sibling Polis-amendment read hook for UPKEEP_RATE_BPS/ZONE_TAX_BPS, and the NEW cross-house prompt-injection CI gate (A11e). Un-skip ring-expansion.test.ts.
</objective>

<context>
@.planning/phases/60-house-3-commerce-cowork/60-CONTEXT.md
@grid/src/governance/engine.ts
@grid/src/civic/founding-law.ts
@grid/src/civic/parcel-registry.ts
@grid/src/audit/broadcast-allowlist.ts

<context_note>
**TEMPLATE, not a new governance path:** ring expansion CONSUMES the EXISTING Phase 46 `gov.law_enacted` event — the bill → co-sponsorship → VOTE-05 → `gov.law_enacted` pipeline is reused VERBATIM. The seeder runs ONLY when an enacted bill body matches `{action:'seed_ring', ring:N}` (parsed Grid-side). It seeds via the Phase 58 `seedZone` + gravity-price formula at frontier prices, is IDEMPOTENT (INSERT IGNORE — re-enacting adds no duplicate parcels), and emits NO audit event (world-creation per Phase 58). NO new allowlist member is added (delta stays +4 → 99). The SAME body-parse hook powers Polis amendment of `UPKEEP_RATE_BPS`/`ZONE_TAX_BPS` (D-NH-03 amendability, deferred from Phase 59) — the founding-law constants become the default FALLBACK behind a read-through indirection. VOTE-05 Nous-only invariant + tamper-evident audit (R-31-01) are preserved. No new `clock.onTick` — the hook is event-driven off the existing governance dispatch.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ring-expansion template + Polis amendment hook</name>
  <files>
    grid/src/civic/ring-expansion.ts,
    grid/src/civic/founding-law.ts,
    grid/src/governance/engine.ts,
    grid/test/civic/ring-expansion.test.ts
  </files>
  <read_first>
    grid/src/governance/engine.ts (Phase 46 bill -> VOTE-05 -> gov.law_enacted pipeline — the existing dispatch to hook into, NOT duplicate),
    grid/src/civic/founding-law.ts (seedZone + GENESIS_CORE_SEED_PLAN + gravity-price formula from Phase 58; UPKEEP_RATE_BPS + ZONE_TAX_BPS to make Polis-amendable),
    grid/src/civic/parcel-registry.ts (the seed-into-registry surface),
    grid/test/civic/ring-expansion.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/ring-expansion.ts`:
    - `export function onLawEnacted(billBody, deps)` — parse `billBody` Grid-side. If it matches `{action:'seed_ring', ring:N}`, seed ring N at frontier prices via the Phase 58 `seedZone` + gravity-price formula (idempotent INSERT IGNORE — re-enacting adds no duplicate parcels); emit NO audit event (world-creation per Phase 58).
    - If it matches `{action:'amend_law', key:'UPKEEP_RATE_BPS'|'ZONE_TAX_BPS', value:...}`, record the Polis override (the founding-law read-through indirection picks it up; constants remain the default fallback).
    - non-matching bodies are ignored (the template only fires on its own shapes).

    Extend `grid/src/civic/founding-law.ts` — wire a Polis-amendable read path for `UPKEEP_RATE_BPS` + `ZONE_TAX_BPS` (a `getUpkeepRateBps()` / `getZoneTaxBps(zoneId)` indirection that returns the Polis override if set, else the constant). The constants remain the default FALLBACK; the single patch point is preserved.

    `grid/src/governance/engine.ts` — at the EXISTING `gov.law_enacted` dispatch, fire-and-forget call `ringExpansion.onLawEnacted(bill.body, deps)`. Do NOT add a new governance path, a new event, or a new `clock.onTick`; reuse the Phase 46 pipeline verbatim.

    Un-skip `ring-expansion.test.ts` and make it green: a `seed_ring` enacted bill seeds the ring (idempotent, no audit emit); a non-matching bill is ignored; an `amend_law` bill overrides the founding-law read path; the seeder adds NO allowlist member; VOTE-05 + bill pipeline reused (no duplication).
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/ring-expansion audit/broadcast-allowlist 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "onLawEnacted|seed_ring|seedZone|amend_law" grid/src/civic/ring-expansion.ts` shows the template + seed + amend hooks
    - `grep -nE "getUpkeepRateBps|getZoneTaxBps" grid/src/civic/founding-law.ts` shows the Polis-amendable read path (constants are the default fallback)
    - `grep -nE "onLawEnacted" grid/src/governance/engine.ts` shows the fire-and-forget call at the existing gov.law_enacted dispatch
    - the seeder emits NO audit event (no append-* call in ring-expansion.ts); NO new allowlist member added (allowlist still 99)
    - ring-expansion.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/ring-expansion audit/broadcast-allowlist` is green (allowlist still 99)
    - `grep -rn "clock.onTick" grid/src | wc -l` stays exactly 1
  </acceptance_criteria>
  <done>ring-expansion.ts is a TEMPLATE consuming gov.law_enacted (seed_ring seeds via seedZone + gravity, idempotent, no audit emit; amend_law overrides the founding-law read path); VOTE-05 + bill pipeline reused verbatim; no new allowlist event; ring-expansion.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: cross-house prompt-injection CI gate (A11e)</name>
  <files>
    scripts/check-cross-house-injection.mjs
  </files>
  <read_first>
    scripts/check-sole-producer-discipline.mjs (the existing CI-gate script SHAPE to mirror — file walk + assertion + non-zero exit on violation),
    scripts/check-wallclock-forbidden.mjs (another gate shape precedent),
    grid/src/civic/cowork.ts (the board/task content path the gate protects),
    grid/src/civic/parcel-registry.ts (the visitor/guest channel the gate protects)
  </read_first>
  <behavior>
    Create `scripts/check-cross-house-injection.mjs` — a NEW CI gate (A11e) enforcing that content received from any visitor/guest Nous or co-work board is treated as DATA, never instructions:
    - walk the House channel code paths that ingest visitor/guest/board/scope_ref content (`grid/src/civic/cowork.ts`, the invite/board routes in `grid/src/api/routes/civic-parcels.ts`, the visitor-facing surfaces).
    - assert that such content does NOT flow into a Telos/Charter command interpreter or any prompt-assembly path that could escalate it to an instruction (e.g. no concatenation of `task_ref`/`scope_ref`/board text into a Charter/Telos directive string; content stays a quoted/structured DATA field).
    - exit non-zero with a clear message on any violation; exit 0 when clean. Mirror the existing gate scripts' file-walk + assertion shape.

    This gate lands WITH the HOUSE-3 visitor channels (it has nothing to guard before this phase). Run it and confirm it exits 0 against the Wave 1–5 source.
  </behavior>
  <verify>
    <automated>node scripts/check-cross-house-injection.mjs; echo "exit: $?" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `ls scripts/check-cross-house-injection.mjs` shows the gate exists
    - `node scripts/check-cross-house-injection.mjs` exits 0 against the current source (visitor/board content is DATA, not instructions)
    - the gate fails (non-zero) on a synthetic violation (board/scope content concatenated into a Telos/Charter directive) — confirmed by a temporary local check, then reverted
    - `grep -nE "Telos|Charter|cowork|scope_ref|task_ref|visitor" scripts/check-cross-house-injection.mjs` shows the gate inspects the right paths
  </acceptance_criteria>
  <done>scripts/check-cross-house-injection.mjs is a NEW CI gate (A11e) asserting visitor/board content is DATA never instructions; exits 0 against the current source; fails on a synthetic escalation.</done>
</task>

</tasks>

<success_criteria>
- ring-expansion.ts is a TEMPLATE consuming existing Phase 46 gov.law_enacted (seed_ring seeds via seedZone + gravity, idempotent, no audit emit); VOTE-05 + bill pipeline reused verbatim; no new governance path; no new allowlist event
- the same body-parse hook supports Polis amendment of UPKEEP_RATE_BPS/ZONE_TAX_BPS; constants remain the default fallback (single patch point)
- scripts/check-cross-house-injection.mjs (A11e) exists and exits 0: visitor/board content is DATA never instructions
- allowlist still exactly 99; zero-diff R-31-01 + civic-did-issuance-path gates untouched-green; single-onTick preserved
- ring-expansion.test.ts un-skipped and green
</success_criteria>

<output>
After completion, create `.planning/phases/60-house-3-commerce-cowork/60-06-SUMMARY.md`.
</output>
