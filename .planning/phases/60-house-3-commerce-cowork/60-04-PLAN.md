---
phase: 60
plan: 04
type: execute
wave: 3
depends_on: [60-03]
files_modified:
  - grid/src/economy/shop-registry.ts
  - grid/src/economy/types.ts
  - grid/src/civic/founding-law.ts
  - grid/src/civic/place-registry.ts
  - grid/src/civic/parcel-registry.ts
  - grid/src/civic/parcel-store.ts
  - grid/src/api/routes/market.ts
  - grid/test/civic/shop-binding.test.ts
  - grid/test/civic/place-registry.test.ts
autonomous: true
requirements: [R-60-05, R-60-06, R-60-09]
tags: [nous-house, house-3, shop-binding, structure-revenue, zone-tax, place-nds, namedAddress]

must_haves:
  truths:
    - "ShopRegistry shop + listing shape carries parcel_id?; bind-shop sets structure.boundShopId + stamps parcel_id (owner-only, structure type shop); unbind routes through the severance FSM; bound_shop_id persisted (v40)"
    - "ZONE_TAX_BPS (business 1200 / shopping 1000 / manufacture 900 / residential 500) + structureRevenueDue live ONLY in founding-law.ts (single Polis-amendable patch point)"
    - "The Phase 44 confirm-settlement path detects a parcel-bound shop and skims the zone tax to TREASURY_DID via registry.transferOusia, ready to emit treasury.structure_revenue (producer lands Wave 4)"
    - "place-registry.ts registers place://<name>.<grid> with uniqueness; a duplicate name throws place_name_taken; registration uses registered_tick (NOT Date.now()); raw names stay Grid-side (name_hash discipline)"
    - "The dormant Phase 58 named_address column is wired here; the protocol nous:// DomainRegistry is untouched"
    - "NO new clock.onTick subscription (grep stays exactly 1); scripts/check-wallclock-forbidden.mjs green"
    - "shop-binding.test.ts + place-registry.test.ts un-skipped and green"
  artifacts:
    - path: "grid/src/economy/shop-registry.ts"
      provides: "parcel_id? on shop/listing; bind/unbind stamps parcel_id; closes the Phase 4 land gap"
    - path: "grid/src/civic/founding-law.ts"
      provides: "ZONE_TAX_BPS table + structureRevenueDue (single Polis-amendable patch point)"
    - path: "grid/src/civic/place-registry.ts"
      provides: "registerPlace place://<name>.<grid> with uniqueness; tick-keyed (registered_tick); raw names off chain"
    - path: "grid/src/api/routes/market.ts"
      provides: "confirm-settlement skims the zone tax to TREASURY_DID for a parcel-bound shop"
  key_links:
    - from: "grid/src/api/routes/market.ts confirm-settlement"
      to: "grid/src/civic/founding-law.ts structureRevenueDue + registry.transferOusia(-> TREASURY_DID)"
      via: "a sale at a parcel-bound shop skims the per-zone tax at settlement (streaming, never filed)"
      pattern: "structureRevenueDue|TREASURY_DID"
    - from: "grid/src/civic/place-registry.ts registerPlace"
      to: "civic_parcels.named_address (Phase 58 dormant column)"
      via: "place:// name wired to the dormant namedAddress field; registered_tick NOT Date.now()"
      pattern: "registered_tick|named_address"
    - from: "grid/src/civic/parcel-registry.ts unbindShop"
      to: "grid/src/civic/severance.ts advanceSeverance"
      via: "unbind routes through the severance FSM (never a hard kill)"
      pattern: "advanceSeverance"
---

<objective>
Bind the Phase 4 ShopRegistry to land (parcel_id on shops/listings, bind/unbind), skim the per-zone structure-revenue tax at the Phase 44 settlement path, and stand up the Grid-side tick-based place:// NDS registry wiring the dormant namedAddress field. Un-skip shop-binding.test.ts + place-registry.test.ts.
</objective>

<context>
@.planning/phases/60-house-3-commerce-cowork/60-CONTEXT.md
@grid/src/economy/shop-registry.ts
@grid/src/economy/types.ts
@grid/src/civic/founding-law.ts
@grid/src/civic/parcel-registry.ts
@grid/src/civic/parcel-store.ts
@grid/src/api/routes/market.ts

<context_note>
**Single patch point (D-V3-34):** the per-zone tax rates mirror the v3.0 Polis tax table — they live ONLY in `founding-law.ts` as `ZONE_TAX_BPS` so Phase-60 Polis amendments (Wave 5) edit only this file. **Wallclock gate:** `place-registry.ts` is a NEW Grid-side registry keyed on an integer `registered_tick` — it must NOT reuse the protocol `nous://` DomainRegistry's wallclock `registeredAt`, and must NOT call `Date.now()`. Raw place names stay Grid-side; only a `name_hash` would ever cross the boundary. No new `clock.onTick` — structure-revenue skim is request-driven at the settlement callsite.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: shop⇄structure binding + ZONE_TAX_BPS + structure-revenue skim at settlement</name>
  <files>
    grid/src/economy/shop-registry.ts,
    grid/src/economy/types.ts,
    grid/src/civic/founding-law.ts,
    grid/src/civic/parcel-registry.ts,
    grid/src/civic/parcel-store.ts,
    grid/src/api/routes/market.ts,
    grid/test/civic/shop-binding.test.ts
  </files>
  <read_first>
    grid/src/economy/shop-registry.ts (Phase 4 ShopRegistry shop/listing shape — add parcel_id? to bind it to land),
    grid/src/economy/types.ts (Shop/Listing types — surgical extension),
    grid/src/civic/founding-law.ts (UPKEEP + IOU constants — ZONE_TAX_BPS lands beside them as the single patch point),
    grid/src/api/routes/market.ts (Phase 44 confirm-settlement / escrow path — the skim hooks in here),
    grid/src/civic/severance.ts (unbind routes through the FSM),
    grid/test/civic/shop-binding.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Extend `grid/src/economy/types.ts` + `grid/src/economy/shop-registry.ts` (surgical): add `parcel_id?: string` to the Shop + Listing shapes. Add a `bindShop(shopId, parcelId)` that stamps `parcel_id` and an `unbindShop(shopId)` that clears it.

    `grid/src/civic/parcel-registry.ts` — add `bindShop(address, ownerDid, shopId)`:
    - owner-only (`not_owner` otherwise); the structure MUST be type `shop` (`structure_not_shop` otherwise); set `structure.boundShopId = shopId` + call `ShopRegistry.bindShop(shopId, parcelId)` + persist `bound_shop_id` DB-first.
    - add `unbindShop(address, ownerDid)` routing through the Wave-1 severance FSM (never a hard kill); on REVOKE clear `boundShopId` + `ShopRegistry.unbindShop`.

    Extend `grid/src/civic/founding-law.ts` (the SINGLE patch point — Wave 5 Polis amendments edit only this file):
    - `export const ZONE_TAX_BPS = { business: 1200, shopping: 1000, manufacture: 900, residential: 500 } as const;` (mirrors v3.0 D-V3-34).
    - `export function structureRevenueDue(parcel: Parcel, saleAmountBios: number): number` = `Math.floor(saleAmountBios * ZONE_TAX_BPS[parcel.zoneId] / 10000)`.

    `grid/src/api/routes/market.ts` — in the Phase 44 `confirm-settlement` path, after the existing trade settlement: if the sold listing's shop is parcel-bound (`parcel_id` present), compute `structureRevenueDue(parcel, saleAmount)` and route the skim to `TREASURY_DID` via `registry.transferOusia` (streaming, collected at settlement, never filed). The `treasury.structure_revenue` emit is wired in Wave 4 — here the funds-move + the call shape only (leave a clearly-marked Wave-4 emit point). Do NOT alter the existing trade settlement behavior.

    `grid/src/civic/parcel-store.ts` — add `persistBoundShop(parcelId, shopId | null)` (DB-first) + extend `hydrate(registry)` to READ `bound_shop_id` on boot.

    Un-skip `shop-binding.test.ts` and make it green: bind-shop owner-only + structure type shop; stamps parcel_id; structureRevenueDue zone-tax math (all 4 zones); unbind routes through severance.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/shop-binding && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "parcel_id" grid/src/economy/shop-registry.ts grid/src/economy/types.ts` shows the new field
    - `grep -nE "bindShop|unbindShop|boundShopId|structure_not_shop" grid/src/civic/parcel-registry.ts` shows the bind/unbind methods
    - `grep -E "ZONE_TAX_BPS|structureRevenueDue" grid/src/civic/founding-law.ts` shows the table + helper (business 1200 / shopping 1000 / manufacture 900 / residential 500)
    - `grep -nE "structureRevenueDue|TREASURY_DID|transferOusia" grid/src/api/routes/market.ts` confirms the settlement skim
    - `grep -nE "persistBoundShop|bound_shop_id" grid/src/civic/parcel-store.ts` confirms DB-first + hydrate
    - shop-binding.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/shop-binding` is green
    - typecheck passes; existing market trade behavior unchanged
  </acceptance_criteria>
  <done>ShopRegistry binds to land (parcel_id); bind/unbind on ParcelRegistry (owner-only, type shop, unbind via severance); ZONE_TAX_BPS + structureRevenueDue only in founding-law; confirm-settlement skims the zone tax to TREASURY_DID; shop-binding.test.ts green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: place:// NDS registry (tick-based, dormant namedAddress wired)</name>
  <files>
    grid/src/civic/place-registry.ts,
    grid/src/civic/parcel-registry.ts,
    grid/src/civic/parcel-store.ts,
    grid/test/civic/place-registry.test.ts
  </files>
  <read_first>
    grid/src/civic/types.ts (Phase 58 dormant namedAddress field on Structure — wired here),
    grid/src/civic/parcel-registry.ts (the owner-check surface for the name route),
    grid/src/civic/parcel-store.ts (named_address column already present from Phase 58 — wire the writer/hydrate),
    grid/test/civic/place-registry.test.ts (Wave 0 stub to un-skip)
  </read_first>
  <behavior>
    Create `grid/src/civic/place-registry.ts` — a NEW Grid-side tick-based registry (NOT the protocol `nous://` DomainRegistry):
    - `registerPlace(name, parcel_id, tick): string` — registers `place://<name>.<grid>` (grid = `genesis`); enforce uniqueness across the registry (a duplicate name → throw `place_name_taken`, which the route maps to 409); key the registration on the integer `registered_tick` (the supplied `tick`) — NEVER `Date.now()` and NEVER the protocol registry's wallclock `registeredAt`.
    - on success set the dormant `named_address` field on the parcel's structure (`structure.namedAddress = place://<name>.<grid>`) and persist DB-first.
    - raw place names stay Grid-side: expose a `name_hash` for any cross-boundary use; no `*_name` key is ever placed on a payload.

    `grid/src/civic/parcel-registry.ts` — add `namePlace(address, ownerDid, placeName)` (owner-only; delegates to `place-registry.registerPlace`).

    `grid/src/civic/parcel-store.ts` — add `persistNamedAddress(parcelId, namedAddress)` (DB-first on the existing `named_address` column) + extend `hydrate(registry)` to READ `named_address` on boot.

    Un-skip `place-registry.test.ts` and make it green: registerPlace returns `place://<name>.genesis` + sets namedAddress; a duplicate throws `place_name_taken`; registration uses `registered_tick` (NOT Date.now()); raw names stay Grid-side.
  </behavior>
  <verify>
    <automated>cd grid && npm run test -- civic/place-registry && node scripts/check-wallclock-forbidden.mjs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "registerPlace|place_name_taken|registered_tick" grid/src/civic/place-registry.ts` shows the registry + uniqueness + tick key
    - `grep -c "Date.now" grid/src/civic/place-registry.ts` is 0 (tick-based, wallclock gate honored)
    - `grep -nE "namePlace" grid/src/civic/parcel-registry.ts` shows the owner-only method
    - `grep -nE "persistNamedAddress|named_address" grid/src/civic/parcel-store.ts` confirms DB-first + hydrate on the Phase 58 column
    - place-registry.test.ts has no remaining `describe.skip`
    - `cd grid && npm run test -- civic/place-registry` is green (uniqueness, 409 throw, tick-based)
    - `node scripts/check-wallclock-forbidden.mjs` exits 0
  </acceptance_criteria>
  <done>place-registry.ts is the NEW Grid-side tick-based place:// NDS (uniqueness + place_name_taken + registered_tick); the dormant namedAddress field is wired; the protocol nous:// registry is untouched; place-registry.test.ts green; wallclock gate green.</done>
</task>

</tasks>

<success_criteria>
- ShopRegistry shop/listing carries parcel_id; bind/unbind on ParcelRegistry (owner-only, type shop, unbind via severance); bound_shop_id persisted
- ZONE_TAX_BPS + structureRevenueDue centralized in founding-law.ts (single Polis-amendable patch point)
- The Phase 44 confirm-settlement path skims the zone tax to TREASURY_DID via transferOusia for a parcel-bound shop (emit wired Wave 4)
- place-registry.ts is the NEW Grid-side tick-based place:// NDS (uniqueness + place_name_taken + registered_tick); dormant namedAddress wired; protocol nous:// untouched
- shop-binding.test.ts + place-registry.test.ts un-skipped and green; node scripts/check-wallclock-forbidden.mjs exits 0; single-onTick preserved
</success_criteria>

<output>
After completion, create `.planning/phases/60-house-3-commerce-cowork/60-04-SUMMARY.md`.
</output>
