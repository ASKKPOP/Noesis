---
phase: 60
plan: 07
type: execute
wave: 6
depends_on: [60-05]
files_modified:
  - brain/src/noesis_brain/rpc/types.py
  - brain/src/noesis_brain/wire/client.py
  - brain/src/noesis_brain/prompts/system.py
  - brain/test/test_civic_commerce_verbs.py
  - dashboard/src/app/worldmap/orbital/page.tsx
  - dashboard/src/components/worldmap/OrbitalGenesisMap.tsx
  - dashboard/test/commerce-surfaces.test.tsx
autonomous: true
requirements: [R-60-13, R-60-14]
tags: [nous-house, house-3, brain-verbs, my-places, dashboard, commerce-surfaces, capabilities]

must_haves:
  truths:
    - "ActionType gains grant_role/revoke_role/invite/bind_shop/name_place/post_task/claim_task/complete_task dispatched via GridWireClient to the new routes; capabilities, not scripts (no autoplay)"
    - "build_my_places_section surfaces bound-shop status, place name, active role grants, and outstanding IOU balance so the Nous feels its commercial relationships"
    - "Dashboard /worldmap/orbital: shop module shows place:// name + bound-shop badge; roles panel lists staff/guest edges + trust; co-work board panel shows posted/claimed/completed; IOU strip shows outstanding balances; additive beside the Phase 58/59 exterior + interior viewers"
    - "cd brain && .venv/bin/pytest test/test_civic_commerce_verbs.py green; cd dashboard && npm run test:unit -- commerce-surfaces green"
  artifacts:
    - path: "brain/src/noesis_brain/rpc/types.py"
      provides: "ActionType members grant_role/revoke_role/invite/bind_shop/name_place/post_task/claim_task/complete_task"
    - path: "brain/src/noesis_brain/wire/client.py"
      provides: "GridWireClient dispatch of the 8 new verbs to the Wave-4 routes"
    - path: "brain/src/noesis_brain/prompts/system.py"
      provides: "build_my_places_section enriched with bound-shop / place name / role grants / outstanding IOU"
    - path: "dashboard/src/components/worldmap/OrbitalGenesisMap.tsx"
      provides: "shop badge + place name, roles panel, co-work board panel, IOU strip (additive)"
  key_links:
    - from: "brain ActionType verbs"
      to: "Wave-4 commerce/role/cowork/place routes"
      via: "GridWireClient dispatch — capabilities, not scripts (no autoplay)"
      pattern: "grant_role|bind_shop|post_task|claim_task|complete_task"
    - from: "build_my_places_section"
      to: "bound-shop / place name / role grants / outstanding IOU"
      via: "prompt enrichment so the Nous feels its commercial relationships"
      pattern: "my_places|outstanding|bound_shop|place"
    - from: "dashboard commerce surfaces"
      to: "Phase 58/59 exterior + interior viewers"
      via: "additive panels on /worldmap/orbital (existing views unchanged)"
      pattern: "OrbitalGenesisMap"
---

<objective>
Surface the new capabilities to the Nous and to humans: Brain verbs (grant_role/revoke_role/invite/bind_shop/name_place/post_task/claim_task/complete_task) as capabilities with my_places enrichment (bound-shop, place name, role grants, outstanding IOU), and dashboard panels (shop badge + place name, roles, co-work board, IOU strip) additive on /worldmap/orbital.
</objective>

<context>
@.planning/phases/60-house-3-commerce-cowork/60-CONTEXT.md
@brain/src/noesis_brain/rpc/types.py
@brain/src/noesis_brain/wire/client.py
@brain/src/noesis_brain/prompts/system.py
@dashboard/src/app/worldmap/orbital/page.tsx
@dashboard/src/components/worldmap/OrbitalGenesisMap.tsx

<context_note>
**Capabilities, not scripts (no autoplay):** the 8 new verbs are added to `ActionType` and dispatched through `GridWireClient` to the Wave-4 routes — decision pressure stays with the Nous; no autoplay loop fires them. The `my_places` prompt block is enriched so the Nous FEELS its commercial relationships (bound-shop status, place name, active role grants, outstanding IOU balance). The dashboard surfaces are ADDITIVE — the Phase 58 orbital exterior and the Phase 59 interior viewer stay unchanged. Test env: brain = `python3.12 .venv` + `.venv/bin/pytest test/`; dashboard = `npm run test:unit` + plugin-react-swc for vitest-4 JSX.
</context_note>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Brain verbs (8 capabilities) + my_places enrichment</name>
  <files>
    brain/src/noesis_brain/rpc/types.py,
    brain/src/noesis_brain/wire/client.py,
    brain/src/noesis_brain/prompts/system.py,
    brain/test/test_civic_commerce_verbs.py
  </files>
  <read_first>
    brain/src/noesis_brain/rpc/types.py (the Phase 58/59 ActionType members — extend, do not rewrite),
    brain/src/noesis_brain/wire/client.py (GridWireClient dispatch of existing civic verbs — mirror the pattern),
    brain/src/noesis_brain/prompts/system.py (the Phase 59 my_places block surfacing condition + upkeep — enrich here),
    brain/test/test_civic_interior_verbs.py (the Phase 59 verb-test shape to mirror)
  </read_first>
  <behavior>
    Add to `brain/src/noesis_brain/rpc/types.py` `ActionType`: `grant_role`, `revoke_role`, `invite`, `bind_shop`, `name_place`, `post_task`, `claim_task`, `complete_task`.

    Add dispatch for each in `brain/src/noesis_brain/wire/client.py` `GridWireClient` — each maps to its Wave-4 Grid route (`.../roles`, `.../roles/revoke`, `.../invite`, `.../bind-shop`, `.../name`, `.../board/post`, `.../board/claim`, `.../board/complete`). Capabilities, not scripts — no autoplay loop fires them.

    Enrich `build_my_places_section` in `brain/src/noesis_brain/prompts/system.py` to surface, per owned parcel: bound-shop status (+ `place://` name if set), active role grants (staff/guest holders), and the Nous's outstanding IOU balance — so the Nous feels its commercial relationships.

    Create `brain/test/test_civic_commerce_verbs.py` asserting: the 8 verbs exist on `ActionType`; each dispatches to the correct route via `GridWireClient`; `build_my_places_section` includes bound-shop / place name / role grants / outstanding IOU when present; the verbs are capabilities (no autoplay).
  </behavior>
  <verify>
    <automated>cd brain && .venv/bin/pytest test/test_civic_commerce_verbs.py 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "grant_role|revoke_role|invite|bind_shop|name_place|post_task|claim_task|complete_task" brain/src/noesis_brain/rpc/types.py` shows the 8 verbs
    - `grep -nE "roles|invite|bind-shop|/name|/board/" brain/src/noesis_brain/wire/client.py` shows the dispatch
    - `grep -nE "bound_shop|place|outstanding|IOU|role" brain/src/noesis_brain/prompts/system.py` shows the my_places enrichment
    - `cd brain && .venv/bin/pytest test/test_civic_commerce_verbs.py` is green
    - no autoplay loop fires the verbs (capabilities only)
  </acceptance_criteria>
  <done>8 new ActionType verbs dispatched via GridWireClient to the Wave-4 routes (capabilities, no autoplay); build_my_places_section surfaces bound-shop / place name / role grants / outstanding IOU; test_civic_commerce_verbs.py green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: dashboard commerce surfaces (shop badge, roles, board, IOU strip) — additive</name>
  <files>
    dashboard/src/app/worldmap/orbital/page.tsx,
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx,
    dashboard/test/commerce-surfaces.test.tsx
  </files>
  <read_first>
    dashboard/src/components/worldmap/OrbitalGenesisMap.tsx (the Phase 58 exterior + Phase 59 interior viewer — extend additively, do NOT alter existing views),
    dashboard/src/app/worldmap/orbital/page.tsx (the orbital route),
    dashboard/test/interior-viewer.test.tsx (the Phase 59 dashboard test shape to mirror; vitest-4 plugin-react-swc)
  </read_first>
  <behavior>
    Extend `dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` ADDITIVELY (the Phase 58 exterior + Phase 59 interior viewer stay unchanged):
    - an owned shop module shows its `place://` name + a bound-shop badge.
    - a roles panel lists granted staff/guest edges with their `trust_score`.
    - a co-work board panel shows posted/claimed/completed tasks (status only — no raw board/task text beyond what the Grid serves).
    - an IOU ledger strip shows outstanding bilateral balances.

    Create `dashboard/test/commerce-surfaces.test.tsx` asserting: the shop module renders the place name + bound-shop badge; the roles panel lists staff/guest edges + trust; the board panel shows posted/claimed/completed; the IOU strip shows outstanding balances; the Phase 58/59 exterior + interior viewers are unchanged.
  </behavior>
  <verify>
    <automated>cd dashboard && npm run test:unit -- commerce-surfaces 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "place://|bound|role|trust|board|IOU|outstanding" dashboard/src/components/worldmap/OrbitalGenesisMap.tsx` shows the new surfaces
    - commerce-surfaces.test.tsx exists and is green
    - `cd dashboard && npm run test:unit -- commerce-surfaces` is green
    - the Phase 58 orbital exterior + Phase 59 interior viewer are unchanged (additive only)
  </acceptance_criteria>
  <done>Dashboard /worldmap/orbital gains the shop badge + place name, roles panel, co-work board panel, and IOU strip additively; commerce-surfaces.test.tsx green; Phase 58/59 viewers unchanged.</done>
</task>

</tasks>

<success_criteria>
- 8 new ActionType verbs dispatched via GridWireClient to the Wave-4 routes (capabilities, no autoplay)
- build_my_places_section surfaces bound-shop / place name / role grants / outstanding IOU
- dashboard /worldmap/orbital gains shop badge + place name, roles panel, co-work board panel, IOU strip — additive beside the Phase 58/59 viewers
- test_civic_commerce_verbs.py + commerce-surfaces.test.tsx green
</success_criteria>

<output>
After completion, create `.planning/phases/60-house-3-commerce-cowork/60-07-SUMMARY.md`.
</output>
