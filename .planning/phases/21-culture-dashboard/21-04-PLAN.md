---
phase: 21-culture-dashboard
plan: 04
type: execute
wave: 2
depends_on:
  - "21-03"
files_modified:
  - dashboard/src/components/culture/skill-lineage-graph.tsx
  - dashboard/src/components/culture/__tests__/skill-lineage-graph.test.tsx
autonomous: true
requirements:
  - CULTURE-01

must_haves:
  truths:
    - "SkillLineageGraph renders an SVG with data-testid='skill-lineage-svg' when data has nodes"
    - "Loading state renders <div role='status'>Loading skill lineage...</div>"
    - "Error state renders <div role='alert'>Skill lineage could not be loaded.</div>"
    - "Empty state renders EmptyState with title 'No skill events yet.'"
    - "skill.taught edges render without strokeDasharray"
    - "skill.inferred edges render with strokeDasharray='4 2'"
    - "Nous nodes render with fill='#F59E0B', skill nodes with fill='#4ADE80'"
    - "Every node has a <title> child with the full DID or hash"
    - "Every edge has a <title> child with the tick number"
  artifacts:
    - path: "dashboard/src/components/culture/skill-lineage-graph.tsx"
      provides: "SkillLineageGraph SVG component (D-9-08 pattern)"
      exports: ["SkillLineageGraph"]
    - path: "dashboard/src/components/culture/__tests__/skill-lineage-graph.test.tsx"
      provides: "Passing tests for CULTURE-01"
      contains: "skill-lineage-svg"
  key_links:
    - from: "dashboard/src/components/culture/skill-lineage-graph.tsx"
      to: "dashboard/src/lib/hooks/use-culture.ts"
      via: "useSkillLineage()"
      pattern: "useSkillLineage"
    - from: "dashboard/src/components/culture/skill-lineage-graph.tsx"
      to: "dashboard/src/components/primitives/empty-state.tsx"
      via: "EmptyState component"
      pattern: "EmptyState"
---

<objective>
Implement `SkillLineageGraph` — the SVG component that renders the skill diffusion tree using the D-9-08 pattern. Clone `RelationshipGraph` and adapt for the lineage data shape: deux node types (Nous amber / skill green), two edge styles (taught solid / inferred dashed), `<title>` hover on all elements.

Purpose: Make CULTURE-01 visible to operators — the skill diffusion tree as a raw SVG directed graph, server-computed layout, zero external dependencies.

Output: `skill-lineage-graph.tsx` component + fully-passing tests replacing the Plan 01 stubs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-culture-dashboard/21-CONTEXT.md
@.planning/phases/21-culture-dashboard/21-RESEARCH.md
@.planning/phases/21-culture-dashboard/21-PATTERNS.md
@.planning/phases/21-culture-dashboard/21-UI-SPEC.md

<interfaces>
<!-- Source files to read before implementing -->

From dashboard/src/app/grid/relationships/relationship-graph.tsx (the EXACT clone template):
Key facts:
- 'use client' at line 1
- VIEWPORT = { width: 1000, height: 1000 } as const
- NODE_RADIUS = 6
- EDGE_STROKE_WIDTH = 1.5
- Uses useGraph() hook; adapt to useSkillLineage()
- SVG node key is n.did; adapt to n.id (D-21-03 shape uses 'id' not 'did')
- Edge endpoints: e.source_did / e.target_did; adapt to e.source / e.target
- No <title> on edges in RelationshipGraph; ADD <title>tick {e.tick}</title> inside each <line>
- No <title> on nodes in RelationshipGraph; ADD <title>{n.id}</title> inside each <g>
- No <text> labels in RelationshipGraph; ADD <text x={n.x + 8} y={n.y + 4} fontSize="10" fill="#A3A3A3">{n.label}</text>

From dashboard/src/components/primitives/empty-state.tsx:
```typescript
interface EmptyStateProps {
    title: string;
    description?: string;
    testId?: string;
}
export function EmptyState({ title, description, testId }: EmptyStateProps): React.ReactElement
```

SkillLineageResponse type (from Plan 03 — use-culture.ts):
```typescript
interface SkillLineageResponse {
    nodes: Array<{ id: string; label: string; type: 'nous' | 'skill'; x: number; y: number }>;
    edges: Array<{ source: string; target: string; tick: number; type: 'taught' | 'inferred' }>;
}
```

Exact SVG spec from UI-SPEC.md:
- Nous node fill: '#F59E0B'
- Skill node fill: '#4ADE80'
- Edge stroke: '#9CA3AF'
- Edge strokeWidth: 1.5
- taught edges: no strokeDasharray
- inferred edges: strokeDasharray="4 2"
- data-testid="skill-lineage-svg"
- aria-label="Skill lineage tree showing how skills propagate between Nous"

Exact copy texts from UI-SPEC.md §Copywriting:
- loading: "Loading skill lineage…"  (note: ellipsis character …, not three dots)
- error: "Skill lineage could not be loaded."
- EmptyState title: "No skill events yet."
- EmptyState description: "Skills appear here once Nous begin teaching and inferring from peers."
- Edge <title>: "tick {N}" (e.g., "tick 42")
- Node <title>: full id string
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement SkillLineageGraph component and fill in passing tests</name>
  <files>
    dashboard/src/components/culture/skill-lineage-graph.tsx,
    dashboard/src/components/culture/__tests__/skill-lineage-graph.test.tsx
  </files>

  <read_first>
    - dashboard/src/app/grid/relationships/relationship-graph.tsx (read the full file — this is the line-by-line clone template)
    - dashboard/src/components/primitives/empty-state.tsx (read the props interface and usage)
    - dashboard/src/components/culture/__tests__/skill-lineage-graph.test.tsx (the stub from Plan 01 — replace todos with concrete tests)
    - dashboard/src/lib/hooks/use-culture.ts (confirm useSkillLineage export and return type)
  </read_first>

  <behavior>
    - Test 1: When useSkillLineage returns { isLoading: true }, screen contains text "Loading skill lineage…"
    - Test 2: When useSkillLineage returns { error: new Error('net') }, screen contains text "Skill lineage could not be loaded."
    - Test 3: When useSkillLineage returns { data: { nodes: [], edges: [] } }, EmptyState renders with title "No skill events yet."
    - Test 4: When useSkillLineage returns { data: { nodes: [{id:'did:noesis:a', label:'a', type:'nous', x:100, y:50}], edges:[] } }, SVG with data-testid="skill-lineage-svg" is in the document
    - Test 5: Given one nous node and one skill node, two circle elements render
    - Test 6: Given one taught edge, the line element has no strokeDasharray attribute
    - Test 7: Given one inferred edge, the line element has strokeDasharray="4 2"
    - Test 8: Each node group contains a title element with the node's id value
    - Test 9: Each edge line contains a title element with text "tick {N}"
  </behavior>

  <action>
**Step 1 — Read tests first.** Read `dashboard/src/components/culture/__tests__/skill-lineage-graph.test.tsx` (the stub). Note the it.todo() entries and replace each with a concrete test.

**Step 2 — Write the tests.** In the test file:
- Import `{ SkillLineageGraph }` from `'../skill-lineage-graph'` (uncomment the import line)
- Mock `@/lib/hooks/use-culture` using `jest.mock`:
  ```typescript
  import { useSkillLineage } from '@/lib/hooks/use-culture';
  jest.mock('@/lib/hooks/use-culture');
  const mockUseSkillLineage = useSkillLineage as jest.Mock;
  ```
- For each test, call `mockUseSkillLineage.mockReturnValue(...)` with the appropriate SWR-like shape:
  - Loading: `{ data: undefined, isLoading: true, error: undefined }`
  - Error: `{ data: undefined, isLoading: false, error: new Error('net') }`
  - Empty: `{ data: { nodes: [], edges: [] }, isLoading: false, error: undefined }`
  - With data: provide minimal fixture matching `SkillLineageResponse` shape
- Use `render()` + `screen.getBy*` / `screen.queryBy*` / `container.querySelector` for assertions
- For SVG attribute assertions: `element.getAttribute('strokeDasharray')` or check rendered output via `container.innerHTML`

**Step 3 — Implement the component.** Create `dashboard/src/components/culture/skill-lineage-graph.tsx`:

```typescript
'use client';
import React from 'react';
import { useSkillLineage } from '@/lib/hooks/use-culture';
import { EmptyState } from '@/components/primitives/empty-state';

const VIEWPORT = { width: 1000, height: 1000 } as const;
const NODE_RADIUS = 6;
const EDGE_STROKE_WIDTH = 1.5;
const NODE_NOUS_FILL = '#F59E0B';
const NODE_SKILL_FILL = '#4ADE80';
const EDGE_STROKE = '#9CA3AF';

export function SkillLineageGraph(): React.ReactElement | null {
    const { data, error, isLoading } = useSkillLineage();

    if (isLoading) {
        return <div role="status" className="text-xs text-neutral-400">Loading skill lineage…</div>;
    }
    if (error) {
        return <div role="alert" className="text-xs text-rose-400">Skill lineage could not be loaded.</div>;
    }
    if (!data) return null;

    if (data.nodes.length === 0) {
        return (
            <EmptyState
                title="No skill events yet."
                description="Skills appear here once Nous begin teaching and inferring from peers."
                testId="skill-lineage-empty"
            />
        );
    }

    const nodePos = new Map(data.nodes.map((n) => [n.id, n]));

    return (
        <svg
            viewBox={`0 0 ${VIEWPORT.width} ${VIEWPORT.height}`}
            className="w-full h-auto max-w-[800px] mx-auto"
            role="img"
            aria-label="Skill lineage tree showing how skills propagate between Nous"
            data-testid="skill-lineage-svg"
        >
            <g className="edges">
                {data.edges.map((e, i) => {
                    const a = nodePos.get(e.source);
                    const b = nodePos.get(e.target);
                    if (!a || !b) return null;
                    return (
                        <line
                            key={i}
                            x1={a.x} y1={a.y} x2={b.x} y2={b.y}
                            stroke={EDGE_STROKE}
                            strokeWidth={EDGE_STROKE_WIDTH}
                            strokeDasharray={e.type === 'inferred' ? '4 2' : undefined}
                        >
                            <title>tick {e.tick}</title>
                        </line>
                    );
                })}
            </g>
            <g className="nodes">
                {data.nodes.map((n) => (
                    <g key={n.id}>
                        <circle
                            cx={n.x} cy={n.y} r={NODE_RADIUS}
                            fill={n.type === 'nous' ? NODE_NOUS_FILL : NODE_SKILL_FILL}
                        />
                        <title>{n.id}</title>
                        <text
                            x={n.x + 8} y={n.y + 4}
                            fontSize="10"
                            fill="#A3A3A3"
                            aria-hidden="true"
                        >
                            {n.label}
                        </text>
                    </g>
                ))}
            </g>
        </svg>
    );
}
```

Then add the legend below the SVG (inside the component return, after the `</svg>` close, wrapped in a React Fragment `<>`):
```tsx
<div className="mt-4 flex flex-col gap-1" data-testid="skill-lineage-legend">
    <h3 className="text-xs font-semibold text-neutral-100">Edge types</h3>
    <ul className="mt-1 flex flex-col gap-1">
        <li className="flex items-center gap-2 text-[11px] text-neutral-400">
            <svg width="24" height="4" aria-hidden="true">
                <line x1="0" y1="2" x2="24" y2="2" stroke="#9CA3AF" strokeWidth="1.5" />
            </svg>
            taught — direct peer-to-peer transmission
        </li>
        <li className="flex items-center gap-2 text-[11px] text-neutral-400">
            <svg width="24" height="4" aria-hidden="true">
                <line x1="0" y1="2" x2="24" y2="2" stroke="#9CA3AF" strokeWidth="1.5" strokeDasharray="4 2" />
            </svg>
            inferred — derived from observed behavior
        </li>
        <li className="flex items-center gap-2 text-[11px] text-neutral-400">
            <span aria-hidden="true" className="inline-block h-2 w-2 rounded-full bg-[#F59E0B]" />
            Nous
        </li>
        <li className="flex items-center gap-2 text-[11px] text-neutral-400">
            <span aria-hidden="true" className="inline-block h-2 w-2 rounded-full bg-[#4ADE80]" />
            Skill hash
        </li>
    </ul>
</div>
```

Wrap SVG + legend in `<>...</>` fragment and return that from the function.

**CRITICAL — D-9-08 enforcement:** Do NOT import d3, react-flow, cytoscape, recharts, or nivo. All layout positions come from `data.nodes` (server-computed). Zero client-side layout math.
  </action>

  <verify>
    <automated>cd /Users/desirey/Programming/src/Noesis/dashboard && npm test -- --testPathPattern="skill-lineage" 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `dashboard/src/components/culture/skill-lineage-graph.tsx` exists
    - File starts with `'use client';`
    - File exports `SkillLineageGraph`
    - File contains `data-testid="skill-lineage-svg"`
    - File contains `strokeDasharray` with value `'4 2'` for inferred edges
    - File contains `NODE_NOUS_FILL = '#F59E0B'`
    - File contains `NODE_SKILL_FILL = '#4ADE80'`
    - File does NOT import d3, react-flow, cytoscape, recharts, or nivo
    - `cd dashboard && npm test -- --testPathPattern="skill-lineage"` exits 0 with all 9 tests green (no todos)
    - `node scripts/check-relationship-graph-deps.mjs` still exits 0
  </acceptance_criteria>

  <done>SkillLineageGraph implemented; all 9 skill-lineage tests pass; D-9-08 grep gate still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Grid API response → SVG render | Hashes and DIDs flow from Grid JSON response to SVG text elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Tampering | SVG text injection via node label/id | mitigate | React's JSX rendering escapes all string content inserted into `<text>` and `<title>` elements. No `dangerouslySetInnerHTML` used. The `n.label` and `n.id` values are skill hashes (hex) or DID segments (alphanumeric with colon/hyphen) — validated upstream at sole-producer boundary. |
| T-21-04-02 | Information Disclosure | Skill hash and DID exposure in SVG title elements | accept | Consistent with Phase 9 RelationshipGraph which exposes DIDs in node titles. Hash and DID are non-sensitive identifiers. No rule content, no lore content, no PII crosses the wire per STATE.md invariants. |
</threat_model>

<verification>
`cd dashboard && npm test -- --testPathPattern="skill-lineage"` — all tests green.
`node scripts/check-relationship-graph-deps.mjs` — exit 0 (Gate C now scans skill-lineage-graph.tsx).
`grep "d3\|react-flow\|cytoscape\|recharts\|nivo" dashboard/src/components/culture/skill-lineage-graph.tsx` — empty (no banned imports).
`grep "data-testid=\"skill-lineage-svg\"" dashboard/src/components/culture/skill-lineage-graph.tsx` — match.
</verification>

<success_criteria>
- SkillLineageGraph component exists at the correct path
- Renders SVG with data-testid="skill-lineage-svg" when data has nodes
- Loading, error, and empty states render correctly with exact copy from UI-SPEC
- taught edges: no strokeDasharray; inferred edges: strokeDasharray="4 2"
- Nous nodes: fill="#F59E0B"; skill nodes: fill="#4ADE80"
- All nodes have `<title>` with full id; all edges have `<title>` with "tick {N}"
- Legend rendered below SVG
- No d3/recharts/cytoscape/react-flow/nivo imports
- All 9 tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/21-culture-dashboard/21-04-SUMMARY.md`
</output>
