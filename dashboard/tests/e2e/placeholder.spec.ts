/**
 * Placeholder E2E spec — ensures `npx playwright test --list` exits 0
 * so the Wave 0 validation gate can prove the config parses. Plan 06
 * replaces this with real `nous-moves.spec.ts`.
 */
import { test, expect } from '@playwright/test';

test.skip('placeholder — replaced by Plan 06 nous-moves.spec.ts', () => {
    expect(true).toBe(true);
});
